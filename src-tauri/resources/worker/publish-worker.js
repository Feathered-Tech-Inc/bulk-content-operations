"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) =>
    function __require() {
        return (mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports);
    };
var __export = (target, all3) => {
    for (var name in all3) __defProp(target, name, { get: all3[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
    if ((from && typeof from === "object") || typeof from === "function") {
        for (let key of __getOwnPropNames(from)) if (!__hasOwnProp.call(to, key) && key !== except) __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
};
var __toESM = (mod, isNodeMode, target) => (
    (target = mod != null ? __create(__getProtoOf(mod)) : {}),
    __copyProps(
        // If the importer is in node compatibility mode or this is not an ESM
        // file that has been converted to a CommonJS file using a Babel-
        // compatible transform (i.e. "__esModule" has not been set), then set
        // "default" to the CommonJS "module.exports" for node compatibility.
        isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
        mod
    )
);
var __toCommonJS = mod => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// node_modules/.pnpm/fast-copy@3.0.2/node_modules/fast-copy/dist/cjs/index.cjs
var require_cjs = __commonJS({
    "node_modules/.pnpm/fast-copy@3.0.2/node_modules/fast-copy/dist/cjs/index.cjs"(exports2) {
        "use strict";
        Object.defineProperty(exports2, "__esModule", { value: true });
        var toStringFunction2 = Function.prototype.toString;
        var create45 = Object.create;
        var toStringObject2 = Object.prototype.toString;
        var LegacyCache2 =
            /** @class */
            (function () {
                function LegacyCache3() {
                    this._keys = [];
                    this._values = [];
                }
                LegacyCache3.prototype.has = function (key) {
                    return !!~this._keys.indexOf(key);
                };
                LegacyCache3.prototype.get = function (key) {
                    return this._values[this._keys.indexOf(key)];
                };
                LegacyCache3.prototype.set = function (key, value) {
                    this._keys.push(key);
                    this._values.push(value);
                };
                return LegacyCache3;
            })();
        function createCacheLegacy2() {
            return new LegacyCache2();
        }
        function createCacheModern2() {
            return /* @__PURE__ */ new WeakMap();
        }
        var createCache2 = typeof WeakMap !== "undefined" ? createCacheModern2 : createCacheLegacy2;
        function getCleanClone2(prototype2) {
            if (!prototype2) {
                return create45(null);
            }
            var Constructor = prototype2.constructor;
            if (Constructor === Object) {
                return prototype2 === Object.prototype ? {} : create45(prototype2);
            }
            if (Constructor && ~toStringFunction2.call(Constructor).indexOf("[native code]")) {
                try {
                    return new Constructor();
                } catch (_a3) {}
            }
            return create45(prototype2);
        }
        function getRegExpFlagsLegacy2(regExp) {
            var flags = "";
            if (regExp.global) {
                flags += "g";
            }
            if (regExp.ignoreCase) {
                flags += "i";
            }
            if (regExp.multiline) {
                flags += "m";
            }
            if (regExp.unicode) {
                flags += "u";
            }
            if (regExp.sticky) {
                flags += "y";
            }
            return flags;
        }
        function getRegExpFlagsModern2(regExp) {
            return regExp.flags;
        }
        var getRegExpFlags2 = /test/g.flags === "g" ? getRegExpFlagsModern2 : getRegExpFlagsLegacy2;
        function getTagLegacy2(value) {
            var type = toStringObject2.call(value);
            return type.substring(8, type.length - 1);
        }
        function getTagModern2(value) {
            return value[Symbol.toStringTag] || getTagLegacy2(value);
        }
        var getTag2 = typeof Symbol !== "undefined" ? getTagModern2 : getTagLegacy2;
        var defineProperty2 = Object.defineProperty;
        var getOwnPropertyDescriptor2 = Object.getOwnPropertyDescriptor;
        var getOwnPropertyNames2 = Object.getOwnPropertyNames;
        var getOwnPropertySymbols2 = Object.getOwnPropertySymbols;
        var _a2 = Object.prototype;
        var hasOwnProperty3 = _a2.hasOwnProperty;
        var propertyIsEnumerable3 = _a2.propertyIsEnumerable;
        var SUPPORTS_SYMBOL2 = typeof getOwnPropertySymbols2 === "function";
        function getStrictPropertiesModern2(object) {
            return getOwnPropertyNames2(object).concat(getOwnPropertySymbols2(object));
        }
        var getStrictProperties2 = SUPPORTS_SYMBOL2 ? getStrictPropertiesModern2 : getOwnPropertyNames2;
        function copyOwnPropertiesStrict2(value, clone, state) {
            var properties = getStrictProperties2(value);
            for (var index3 = 0, length_1 = properties.length, property = void 0, descriptor = void 0; index3 < length_1; ++index3) {
                property = properties[index3];
                if (property === "callee" || property === "caller") {
                    continue;
                }
                descriptor = getOwnPropertyDescriptor2(value, property);
                if (!descriptor) {
                    clone[property] = state.copier(value[property], state);
                    continue;
                }
                if (!descriptor.get && !descriptor.set) {
                    descriptor.value = state.copier(descriptor.value, state);
                }
                try {
                    defineProperty2(clone, property, descriptor);
                } catch (error) {
                    clone[property] = descriptor.value;
                }
            }
            return clone;
        }
        function copyArrayLoose2(array, state) {
            var clone = new state.Constructor();
            state.cache.set(array, clone);
            for (var index3 = 0, length_2 = array.length; index3 < length_2; ++index3) {
                clone[index3] = state.copier(array[index3], state);
            }
            return clone;
        }
        function copyArrayStrict2(array, state) {
            var clone = new state.Constructor();
            state.cache.set(array, clone);
            return copyOwnPropertiesStrict2(array, clone, state);
        }
        function copyArrayBuffer2(arrayBuffer, _state) {
            return arrayBuffer.slice(0);
        }
        function copyBlob2(blob, _state) {
            return blob.slice(0, blob.size, blob.type);
        }
        function copyDataView2(dataView, state) {
            return new state.Constructor(copyArrayBuffer2(dataView.buffer));
        }
        function copyDate2(date, state) {
            return new state.Constructor(date.getTime());
        }
        function copyMapLoose2(map, state) {
            var clone = new state.Constructor();
            state.cache.set(map, clone);
            map.forEach(function (value, key) {
                clone.set(key, state.copier(value, state));
            });
            return clone;
        }
        function copyMapStrict2(map, state) {
            return copyOwnPropertiesStrict2(map, copyMapLoose2(map, state), state);
        }
        function copyObjectLooseLegacy2(object, state) {
            var clone = getCleanClone2(state.prototype);
            state.cache.set(object, clone);
            for (var key in object) {
                if (hasOwnProperty3.call(object, key)) {
                    clone[key] = state.copier(object[key], state);
                }
            }
            return clone;
        }
        function copyObjectLooseModern2(object, state) {
            var clone = getCleanClone2(state.prototype);
            state.cache.set(object, clone);
            for (var key in object) {
                if (hasOwnProperty3.call(object, key)) {
                    clone[key] = state.copier(object[key], state);
                }
            }
            var symbols = getOwnPropertySymbols2(object);
            for (var index3 = 0, length_3 = symbols.length, symbol = void 0; index3 < length_3; ++index3) {
                symbol = symbols[index3];
                if (propertyIsEnumerable3.call(object, symbol)) {
                    clone[symbol] = state.copier(object[symbol], state);
                }
            }
            return clone;
        }
        var copyObjectLoose2 = SUPPORTS_SYMBOL2 ? copyObjectLooseModern2 : copyObjectLooseLegacy2;
        function copyObjectStrict2(object, state) {
            var clone = getCleanClone2(state.prototype);
            state.cache.set(object, clone);
            return copyOwnPropertiesStrict2(object, clone, state);
        }
        function copyPrimitiveWrapper2(primitiveObject, state) {
            return new state.Constructor(primitiveObject.valueOf());
        }
        function copyRegExp2(regExp, state) {
            var clone = new state.Constructor(regExp.source, getRegExpFlags2(regExp));
            clone.lastIndex = regExp.lastIndex;
            return clone;
        }
        function copySelf2(value, _state) {
            return value;
        }
        function copySetLoose2(set, state) {
            var clone = new state.Constructor();
            state.cache.set(set, clone);
            set.forEach(function (value) {
                clone.add(state.copier(value, state));
            });
            return clone;
        }
        function copySetStrict2(set, state) {
            return copyOwnPropertiesStrict2(set, copySetLoose2(set, state), state);
        }
        var isArray3 = Array.isArray;
        var assign2 = Object.assign;
        var getPrototypeOf3 =
            Object.getPrototypeOf ||
            function (obj) {
                return obj.__proto__;
            };
        var DEFAULT_LOOSE_OPTIONS2 = {
            array: copyArrayLoose2,
            arrayBuffer: copyArrayBuffer2,
            blob: copyBlob2,
            dataView: copyDataView2,
            date: copyDate2,
            error: copySelf2,
            map: copyMapLoose2,
            object: copyObjectLoose2,
            regExp: copyRegExp2,
            set: copySetLoose2
        };
        var DEFAULT_STRICT_OPTIONS2 = assign2({}, DEFAULT_LOOSE_OPTIONS2, {
            array: copyArrayStrict2,
            map: copyMapStrict2,
            object: copyObjectStrict2,
            set: copySetStrict2
        });
        function getTagSpecificCopiers2(options) {
            return {
                Arguments: options.object,
                Array: options.array,
                ArrayBuffer: options.arrayBuffer,
                Blob: options.blob,
                Boolean: copyPrimitiveWrapper2,
                DataView: options.dataView,
                Date: options.date,
                Error: options.error,
                Float32Array: options.arrayBuffer,
                Float64Array: options.arrayBuffer,
                Int8Array: options.arrayBuffer,
                Int16Array: options.arrayBuffer,
                Int32Array: options.arrayBuffer,
                Map: options.map,
                Number: copyPrimitiveWrapper2,
                Object: options.object,
                Promise: copySelf2,
                RegExp: options.regExp,
                Set: options.set,
                String: copyPrimitiveWrapper2,
                WeakMap: copySelf2,
                WeakSet: copySelf2,
                Uint8Array: options.arrayBuffer,
                Uint8ClampedArray: options.arrayBuffer,
                Uint16Array: options.arrayBuffer,
                Uint32Array: options.arrayBuffer,
                Uint64Array: options.arrayBuffer
            };
        }
        function createCopier2(options) {
            var normalizedOptions = assign2({}, DEFAULT_LOOSE_OPTIONS2, options);
            var tagSpecificCopiers = getTagSpecificCopiers2(normalizedOptions);
            var array = tagSpecificCopiers.Array,
                object = tagSpecificCopiers.Object;
            function copier(value, state) {
                state.prototype = state.Constructor = void 0;
                if (!value || typeof value !== "object") {
                    return value;
                }
                if (state.cache.has(value)) {
                    return state.cache.get(value);
                }
                state.prototype = getPrototypeOf3(value);
                state.Constructor = state.prototype && state.prototype.constructor;
                if (!state.Constructor || state.Constructor === Object) {
                    return object(value, state);
                }
                if (isArray3(value)) {
                    return array(value, state);
                }
                var tagSpecificCopier = tagSpecificCopiers[getTag2(value)];
                if (tagSpecificCopier) {
                    return tagSpecificCopier(value, state);
                }
                return typeof value.then === "function" ? value : object(value, state);
            }
            return function copy(value) {
                return copier(value, {
                    Constructor: void 0,
                    cache: createCache2(),
                    copier,
                    prototype: void 0
                });
            };
        }
        function createStrictCopier2(options) {
            return createCopier2(assign2({}, DEFAULT_STRICT_OPTIONS2, options));
        }
        var copyStrict2 = createStrictCopier2({});
        var index2 = createCopier2({});
        exports2.copyStrict = copyStrict2;
        exports2.createCopier = createCopier2;
        exports2.createStrictCopier = createStrictCopier2;
        exports2.default = index2;
    }
});

// node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_freeGlobal.js
var require_freeGlobal = __commonJS({
    "node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_freeGlobal.js"(exports2, module2) {
        var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
        module2.exports = freeGlobal;
    }
});

// node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_root.js
var require_root = __commonJS({
    "node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_root.js"(exports2, module2) {
        var freeGlobal = require_freeGlobal();
        var freeSelf = typeof self == "object" && self && self.Object === Object && self;
        var root = freeGlobal || freeSelf || Function("return this")();
        module2.exports = root;
    }
});

// node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_Symbol.js
var require_Symbol = __commonJS({
    "node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_Symbol.js"(exports2, module2) {
        var root = require_root();
        var Symbol2 = root.Symbol;
        module2.exports = Symbol2;
    }
});

// node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getRawTag.js
var require_getRawTag = __commonJS({
    "node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getRawTag.js"(exports2, module2) {
        var Symbol2 = require_Symbol();
        var objectProto = Object.prototype;
        var hasOwnProperty3 = objectProto.hasOwnProperty;
        var nativeObjectToString = objectProto.toString;
        var symToStringTag = Symbol2 ? Symbol2.toStringTag : void 0;
        function getRawTag(value) {
            var isOwn = hasOwnProperty3.call(value, symToStringTag),
                tag = value[symToStringTag];
            try {
                value[symToStringTag] = void 0;
                var unmasked = true;
            } catch (e) {}
            var result = nativeObjectToString.call(value);
            if (unmasked) {
                if (isOwn) {
                    value[symToStringTag] = tag;
                } else {
                    delete value[symToStringTag];
                }
            }
            return result;
        }
        module2.exports = getRawTag;
    }
});

// node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_objectToString.js
var require_objectToString = __commonJS({
    "node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_objectToString.js"(exports2, module2) {
        var objectProto = Object.prototype;
        var nativeObjectToString = objectProto.toString;
        function objectToString(value) {
            return nativeObjectToString.call(value);
        }
        module2.exports = objectToString;
    }
});

// node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseGetTag.js
var require_baseGetTag = __commonJS({
    "node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_baseGetTag.js"(exports2, module2) {
        var Symbol2 = require_Symbol();
        var getRawTag = require_getRawTag();
        var objectToString = require_objectToString();
        var nullTag = "[object Null]";
        var undefinedTag = "[object Undefined]";
        var symToStringTag = Symbol2 ? Symbol2.toStringTag : void 0;
        function baseGetTag(value) {
            if (value == null) {
                return value === void 0 ? undefinedTag : nullTag;
            }
            return symToStringTag && symToStringTag in Object(value) ? getRawTag(value) : objectToString(value);
        }
        module2.exports = baseGetTag;
    }
});

// node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isArray.js
var require_isArray = __commonJS({
    "node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isArray.js"(exports2, module2) {
        var isArray3 = Array.isArray;
        module2.exports = isArray3;
    }
});

// node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isObjectLike.js
var require_isObjectLike = __commonJS({
    "node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isObjectLike.js"(exports2, module2) {
        function isObjectLike(value) {
            return value != null && typeof value == "object";
        }
        module2.exports = isObjectLike;
    }
});

// node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isString.js
var require_isString = __commonJS({
    "node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isString.js"(exports2, module2) {
        var baseGetTag = require_baseGetTag();
        var isArray3 = require_isArray();
        var isObjectLike = require_isObjectLike();
        var stringTag = "[object String]";
        function isString2(value) {
            return typeof value == "string" || (!isArray3(value) && isObjectLike(value) && baseGetTag(value) == stringTag);
        }
        module2.exports = isString2;
    }
});

// node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/type.js
var require_type = __commonJS({
    "node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/type.js"(exports2, module2) {
        "use strict";
        module2.exports = TypeError;
    }
});

// node_modules/.pnpm/object-inspect@1.13.4/node_modules/object-inspect/util.inspect.js
var require_util_inspect = __commonJS({
    "node_modules/.pnpm/object-inspect@1.13.4/node_modules/object-inspect/util.inspect.js"(exports2, module2) {
        module2.exports = require("util").inspect;
    }
});

// node_modules/.pnpm/object-inspect@1.13.4/node_modules/object-inspect/index.js
var require_object_inspect = __commonJS({
    "node_modules/.pnpm/object-inspect@1.13.4/node_modules/object-inspect/index.js"(exports2, module2) {
        var hasMap = typeof Map === "function" && Map.prototype;
        var mapSizeDescriptor = Object.getOwnPropertyDescriptor && hasMap ? Object.getOwnPropertyDescriptor(Map.prototype, "size") : null;
        var mapSize = hasMap && mapSizeDescriptor && typeof mapSizeDescriptor.get === "function" ? mapSizeDescriptor.get : null;
        var mapForEach = hasMap && Map.prototype.forEach;
        var hasSet = typeof Set === "function" && Set.prototype;
        var setSizeDescriptor = Object.getOwnPropertyDescriptor && hasSet ? Object.getOwnPropertyDescriptor(Set.prototype, "size") : null;
        var setSize = hasSet && setSizeDescriptor && typeof setSizeDescriptor.get === "function" ? setSizeDescriptor.get : null;
        var setForEach = hasSet && Set.prototype.forEach;
        var hasWeakMap = typeof WeakMap === "function" && WeakMap.prototype;
        var weakMapHas = hasWeakMap ? WeakMap.prototype.has : null;
        var hasWeakSet = typeof WeakSet === "function" && WeakSet.prototype;
        var weakSetHas = hasWeakSet ? WeakSet.prototype.has : null;
        var hasWeakRef = typeof WeakRef === "function" && WeakRef.prototype;
        var weakRefDeref = hasWeakRef ? WeakRef.prototype.deref : null;
        var booleanValueOf = Boolean.prototype.valueOf;
        var objectToString = Object.prototype.toString;
        var functionToString = Function.prototype.toString;
        var $match = String.prototype.match;
        var $slice = String.prototype.slice;
        var $replace = String.prototype.replace;
        var $toUpperCase = String.prototype.toUpperCase;
        var $toLowerCase = String.prototype.toLowerCase;
        var $test = RegExp.prototype.test;
        var $concat = Array.prototype.concat;
        var $join = Array.prototype.join;
        var $arrSlice = Array.prototype.slice;
        var $floor = Math.floor;
        var bigIntValueOf = typeof BigInt === "function" ? BigInt.prototype.valueOf : null;
        var gOPS = Object.getOwnPropertySymbols;
        var symToString = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? Symbol.prototype.toString : null;
        var hasShammedSymbols = typeof Symbol === "function" && typeof Symbol.iterator === "object";
        var toStringTag2 = typeof Symbol === "function" && Symbol.toStringTag && (typeof Symbol.toStringTag === hasShammedSymbols ? "object" : "symbol") ? Symbol.toStringTag : null;
        var isEnumerable = Object.prototype.propertyIsEnumerable;
        var gPO =
            (typeof Reflect === "function" ? Reflect.getPrototypeOf : Object.getPrototypeOf) ||
            ([].__proto__ === Array.prototype
                ? function (O) {
                      return O.__proto__;
                  }
                : null);
        function addNumericSeparator(num, str) {
            if (num === Infinity || num === -Infinity || num !== num || (num && num > -1e3 && num < 1e3) || $test.call(/e/, str)) {
                return str;
            }
            var sepRegex = /[0-9](?=(?:[0-9]{3})+(?![0-9]))/g;
            if (typeof num === "number") {
                var int = num < 0 ? -$floor(-num) : $floor(num);
                if (int !== num) {
                    var intStr = String(int);
                    var dec = $slice.call(str, intStr.length + 1);
                    return $replace.call(intStr, sepRegex, "$&_") + "." + $replace.call($replace.call(dec, /([0-9]{3})/g, "$&_"), /_$/, "");
                }
            }
            return $replace.call(str, sepRegex, "$&_");
        }
        var utilInspect = require_util_inspect();
        var inspectCustom = utilInspect.custom;
        var inspectSymbol = isSymbol(inspectCustom) ? inspectCustom : null;
        var quotes = {
            __proto__: null,
            double: '"',
            single: "'"
        };
        var quoteREs = {
            __proto__: null,
            double: /(["\\])/g,
            single: /(['\\])/g
        };
        module2.exports = function inspect_(obj, options, depth, seen) {
            var opts = options || {};
            if (has(opts, "quoteStyle") && !has(quotes, opts.quoteStyle)) {
                throw new TypeError('option "quoteStyle" must be "single" or "double"');
            }
            if (has(opts, "maxStringLength") && (typeof opts.maxStringLength === "number" ? opts.maxStringLength < 0 && opts.maxStringLength !== Infinity : opts.maxStringLength !== null)) {
                throw new TypeError('option "maxStringLength", if provided, must be a positive integer, Infinity, or `null`');
            }
            var customInspect = has(opts, "customInspect") ? opts.customInspect : true;
            if (typeof customInspect !== "boolean" && customInspect !== "symbol") {
                throw new TypeError("option \"customInspect\", if provided, must be `true`, `false`, or `'symbol'`");
            }
            if (has(opts, "indent") && opts.indent !== null && opts.indent !== "	" && !(parseInt(opts.indent, 10) === opts.indent && opts.indent > 0)) {
                throw new TypeError('option "indent" must be "\\t", an integer > 0, or `null`');
            }
            if (has(opts, "numericSeparator") && typeof opts.numericSeparator !== "boolean") {
                throw new TypeError('option "numericSeparator", if provided, must be `true` or `false`');
            }
            var numericSeparator = opts.numericSeparator;
            if (typeof obj === "undefined") {
                return "undefined";
            }
            if (obj === null) {
                return "null";
            }
            if (typeof obj === "boolean") {
                return obj ? "true" : "false";
            }
            if (typeof obj === "string") {
                return inspectString(obj, opts);
            }
            if (typeof obj === "number") {
                if (obj === 0) {
                    return Infinity / obj > 0 ? "0" : "-0";
                }
                var str = String(obj);
                return numericSeparator ? addNumericSeparator(obj, str) : str;
            }
            if (typeof obj === "bigint") {
                var bigIntStr = String(obj) + "n";
                return numericSeparator ? addNumericSeparator(obj, bigIntStr) : bigIntStr;
            }
            var maxDepth = typeof opts.depth === "undefined" ? 5 : opts.depth;
            if (typeof depth === "undefined") {
                depth = 0;
            }
            if (depth >= maxDepth && maxDepth > 0 && typeof obj === "object") {
                return isArray3(obj) ? "[Array]" : "[Object]";
            }
            var indent = getIndent(opts, depth);
            if (typeof seen === "undefined") {
                seen = [];
            } else if (indexOf(seen, obj) >= 0) {
                return "[Circular]";
            }
            function inspect(value, from, noIndent) {
                if (from) {
                    seen = $arrSlice.call(seen);
                    seen.push(from);
                }
                if (noIndent) {
                    var newOpts = {
                        depth: opts.depth
                    };
                    if (has(opts, "quoteStyle")) {
                        newOpts.quoteStyle = opts.quoteStyle;
                    }
                    return inspect_(value, newOpts, depth + 1, seen);
                }
                return inspect_(value, opts, depth + 1, seen);
            }
            if (typeof obj === "function" && !isRegExp2(obj)) {
                var name = nameOf(obj);
                var keys = arrObjKeys(obj, inspect);
                return "[Function" + (name ? ": " + name : " (anonymous)") + "]" + (keys.length > 0 ? " { " + $join.call(keys, ", ") + " }" : "");
            }
            if (isSymbol(obj)) {
                var symString = hasShammedSymbols ? $replace.call(String(obj), /^(Symbol\(.*\))_[^)]*$/, "$1") : symToString.call(obj);
                return typeof obj === "object" && !hasShammedSymbols ? markBoxed(symString) : symString;
            }
            if (isElement(obj)) {
                var s = "<" + $toLowerCase.call(String(obj.nodeName));
                var attrs = obj.attributes || [];
                for (var i = 0; i < attrs.length; i++) {
                    s += " " + attrs[i].name + "=" + wrapQuotes(quote(attrs[i].value), "double", opts);
                }
                s += ">";
                if (obj.childNodes && obj.childNodes.length) {
                    s += "...";
                }
                s += "</" + $toLowerCase.call(String(obj.nodeName)) + ">";
                return s;
            }
            if (isArray3(obj)) {
                if (obj.length === 0) {
                    return "[]";
                }
                var xs = arrObjKeys(obj, inspect);
                if (indent && !singleLineValues(xs)) {
                    return "[" + indentedJoin(xs, indent) + "]";
                }
                return "[ " + $join.call(xs, ", ") + " ]";
            }
            if (isError(obj)) {
                var parts = arrObjKeys(obj, inspect);
                if (!("cause" in Error.prototype) && "cause" in obj && !isEnumerable.call(obj, "cause")) {
                    return "{ [" + String(obj) + "] " + $join.call($concat.call("[cause]: " + inspect(obj.cause), parts), ", ") + " }";
                }
                if (parts.length === 0) {
                    return "[" + String(obj) + "]";
                }
                return "{ [" + String(obj) + "] " + $join.call(parts, ", ") + " }";
            }
            if (typeof obj === "object" && customInspect) {
                if (inspectSymbol && typeof obj[inspectSymbol] === "function" && utilInspect) {
                    return utilInspect(obj, { depth: maxDepth - depth });
                } else if (customInspect !== "symbol" && typeof obj.inspect === "function") {
                    return obj.inspect();
                }
            }
            if (isMap(obj)) {
                var mapParts = [];
                if (mapForEach) {
                    mapForEach.call(obj, function (value, key) {
                        mapParts.push(inspect(key, obj, true) + " => " + inspect(value, obj));
                    });
                }
                return collectionOf("Map", mapSize.call(obj), mapParts, indent);
            }
            if (isSet(obj)) {
                var setParts = [];
                if (setForEach) {
                    setForEach.call(obj, function (value) {
                        setParts.push(inspect(value, obj));
                    });
                }
                return collectionOf("Set", setSize.call(obj), setParts, indent);
            }
            if (isWeakMap(obj)) {
                return weakCollectionOf("WeakMap");
            }
            if (isWeakSet(obj)) {
                return weakCollectionOf("WeakSet");
            }
            if (isWeakRef(obj)) {
                return weakCollectionOf("WeakRef");
            }
            if (isNumber2(obj)) {
                return markBoxed(inspect(Number(obj)));
            }
            if (isBigInt(obj)) {
                return markBoxed(inspect(bigIntValueOf.call(obj)));
            }
            if (isBoolean2(obj)) {
                return markBoxed(booleanValueOf.call(obj));
            }
            if (isString2(obj)) {
                return markBoxed(inspect(String(obj)));
            }
            if (typeof window !== "undefined" && obj === window) {
                return "{ [object Window] }";
            }
            if ((typeof globalThis !== "undefined" && obj === globalThis) || (typeof global !== "undefined" && obj === global)) {
                return "{ [object globalThis] }";
            }
            if (!isDate2(obj) && !isRegExp2(obj)) {
                var ys = arrObjKeys(obj, inspect);
                var isPlainObject2 = gPO ? gPO(obj) === Object.prototype : obj instanceof Object || obj.constructor === Object;
                var protoTag = obj instanceof Object ? "" : "null prototype";
                var stringTag = !isPlainObject2 && toStringTag2 && Object(obj) === obj && toStringTag2 in obj ? $slice.call(toStr(obj), 8, -1) : protoTag ? "Object" : "";
                var constructorTag = isPlainObject2 || typeof obj.constructor !== "function" ? "" : obj.constructor.name ? obj.constructor.name + " " : "";
                var tag = constructorTag + (stringTag || protoTag ? "[" + $join.call($concat.call([], stringTag || [], protoTag || []), ": ") + "] " : "");
                if (ys.length === 0) {
                    return tag + "{}";
                }
                if (indent) {
                    return tag + "{" + indentedJoin(ys, indent) + "}";
                }
                return tag + "{ " + $join.call(ys, ", ") + " }";
            }
            return String(obj);
        };
        function wrapQuotes(s, defaultStyle, opts) {
            var style = opts.quoteStyle || defaultStyle;
            var quoteChar = quotes[style];
            return quoteChar + s + quoteChar;
        }
        function quote(s) {
            return $replace.call(String(s), /"/g, "&quot;");
        }
        function canTrustToString(obj) {
            return !toStringTag2 || !(typeof obj === "object" && (toStringTag2 in obj || typeof obj[toStringTag2] !== "undefined"));
        }
        function isArray3(obj) {
            return toStr(obj) === "[object Array]" && canTrustToString(obj);
        }
        function isDate2(obj) {
            return toStr(obj) === "[object Date]" && canTrustToString(obj);
        }
        function isRegExp2(obj) {
            return toStr(obj) === "[object RegExp]" && canTrustToString(obj);
        }
        function isError(obj) {
            return toStr(obj) === "[object Error]" && canTrustToString(obj);
        }
        function isString2(obj) {
            return toStr(obj) === "[object String]" && canTrustToString(obj);
        }
        function isNumber2(obj) {
            return toStr(obj) === "[object Number]" && canTrustToString(obj);
        }
        function isBoolean2(obj) {
            return toStr(obj) === "[object Boolean]" && canTrustToString(obj);
        }
        function isSymbol(obj) {
            if (hasShammedSymbols) {
                return obj && typeof obj === "object" && obj instanceof Symbol;
            }
            if (typeof obj === "symbol") {
                return true;
            }
            if (!obj || typeof obj !== "object" || !symToString) {
                return false;
            }
            try {
                symToString.call(obj);
                return true;
            } catch (e) {}
            return false;
        }
        function isBigInt(obj) {
            if (!obj || typeof obj !== "object" || !bigIntValueOf) {
                return false;
            }
            try {
                bigIntValueOf.call(obj);
                return true;
            } catch (e) {}
            return false;
        }
        var hasOwn =
            Object.prototype.hasOwnProperty ||
            function (key) {
                return key in this;
            };
        function has(obj, key) {
            return hasOwn.call(obj, key);
        }
        function toStr(obj) {
            return objectToString.call(obj);
        }
        function nameOf(f) {
            if (f.name) {
                return f.name;
            }
            var m = $match.call(functionToString.call(f), /^function\s*([\w$]+)/);
            if (m) {
                return m[1];
            }
            return null;
        }
        function indexOf(xs, x) {
            if (xs.indexOf) {
                return xs.indexOf(x);
            }
            for (var i = 0, l = xs.length; i < l; i++) {
                if (xs[i] === x) {
                    return i;
                }
            }
            return -1;
        }
        function isMap(x) {
            if (!mapSize || !x || typeof x !== "object") {
                return false;
            }
            try {
                mapSize.call(x);
                try {
                    setSize.call(x);
                } catch (s) {
                    return true;
                }
                return x instanceof Map;
            } catch (e) {}
            return false;
        }
        function isWeakMap(x) {
            if (!weakMapHas || !x || typeof x !== "object") {
                return false;
            }
            try {
                weakMapHas.call(x, weakMapHas);
                try {
                    weakSetHas.call(x, weakSetHas);
                } catch (s) {
                    return true;
                }
                return x instanceof WeakMap;
            } catch (e) {}
            return false;
        }
        function isWeakRef(x) {
            if (!weakRefDeref || !x || typeof x !== "object") {
                return false;
            }
            try {
                weakRefDeref.call(x);
                return true;
            } catch (e) {}
            return false;
        }
        function isSet(x) {
            if (!setSize || !x || typeof x !== "object") {
                return false;
            }
            try {
                setSize.call(x);
                try {
                    mapSize.call(x);
                } catch (m) {
                    return true;
                }
                return x instanceof Set;
            } catch (e) {}
            return false;
        }
        function isWeakSet(x) {
            if (!weakSetHas || !x || typeof x !== "object") {
                return false;
            }
            try {
                weakSetHas.call(x, weakSetHas);
                try {
                    weakMapHas.call(x, weakMapHas);
                } catch (s) {
                    return true;
                }
                return x instanceof WeakSet;
            } catch (e) {}
            return false;
        }
        function isElement(x) {
            if (!x || typeof x !== "object") {
                return false;
            }
            if (typeof HTMLElement !== "undefined" && x instanceof HTMLElement) {
                return true;
            }
            return typeof x.nodeName === "string" && typeof x.getAttribute === "function";
        }
        function inspectString(str, opts) {
            if (str.length > opts.maxStringLength) {
                var remaining = str.length - opts.maxStringLength;
                var trailer = "... " + remaining + " more character" + (remaining > 1 ? "s" : "");
                return inspectString($slice.call(str, 0, opts.maxStringLength), opts) + trailer;
            }
            var quoteRE = quoteREs[opts.quoteStyle || "single"];
            quoteRE.lastIndex = 0;
            var s = $replace.call($replace.call(str, quoteRE, "\\$1"), /[\x00-\x1f]/g, lowbyte);
            return wrapQuotes(s, "single", opts);
        }
        function lowbyte(c) {
            var n = c.charCodeAt(0);
            var x = {
                8: "b",
                9: "t",
                10: "n",
                12: "f",
                13: "r"
            }[n];
            if (x) {
                return "\\" + x;
            }
            return "\\x" + (n < 16 ? "0" : "") + $toUpperCase.call(n.toString(16));
        }
        function markBoxed(str) {
            return "Object(" + str + ")";
        }
        function weakCollectionOf(type) {
            return type + " { ? }";
        }
        function collectionOf(type, size, entries, indent) {
            var joinedEntries = indent ? indentedJoin(entries, indent) : $join.call(entries, ", ");
            return type + " (" + size + ") {" + joinedEntries + "}";
        }
        function singleLineValues(xs) {
            for (var i = 0; i < xs.length; i++) {
                if (indexOf(xs[i], "\n") >= 0) {
                    return false;
                }
            }
            return true;
        }
        function getIndent(opts, depth) {
            var baseIndent;
            if (opts.indent === "	") {
                baseIndent = "	";
            } else if (typeof opts.indent === "number" && opts.indent > 0) {
                baseIndent = $join.call(Array(opts.indent + 1), " ");
            } else {
                return null;
            }
            return {
                base: baseIndent,
                prev: $join.call(Array(depth + 1), baseIndent)
            };
        }
        function indentedJoin(xs, indent) {
            if (xs.length === 0) {
                return "";
            }
            var lineJoiner = "\n" + indent.prev + indent.base;
            return lineJoiner + $join.call(xs, "," + lineJoiner) + "\n" + indent.prev;
        }
        function arrObjKeys(obj, inspect) {
            var isArr = isArray3(obj);
            var xs = [];
            if (isArr) {
                xs.length = obj.length;
                for (var i = 0; i < obj.length; i++) {
                    xs[i] = has(obj, i) ? inspect(obj[i], obj) : "";
                }
            }
            var syms = typeof gOPS === "function" ? gOPS(obj) : [];
            var symMap;
            if (hasShammedSymbols) {
                symMap = {};
                for (var k = 0; k < syms.length; k++) {
                    symMap["$" + syms[k]] = syms[k];
                }
            }
            for (var key in obj) {
                if (!has(obj, key)) {
                    continue;
                }
                if (isArr && String(Number(key)) === key && key < obj.length) {
                    continue;
                }
                if (hasShammedSymbols && symMap["$" + key] instanceof Symbol) {
                    continue;
                } else if ($test.call(/[^\w$]/, key)) {
                    xs.push(inspect(key, obj) + ": " + inspect(obj[key], obj));
                } else {
                    xs.push(key + ": " + inspect(obj[key], obj));
                }
            }
            if (typeof gOPS === "function") {
                for (var j = 0; j < syms.length; j++) {
                    if (isEnumerable.call(obj, syms[j])) {
                        xs.push("[" + inspect(syms[j]) + "]: " + inspect(obj[syms[j]], obj));
                    }
                }
            }
            return xs;
        }
    }
});

// node_modules/.pnpm/side-channel-list@1.0.1/node_modules/side-channel-list/index.js
var require_side_channel_list = __commonJS({
    "node_modules/.pnpm/side-channel-list@1.0.1/node_modules/side-channel-list/index.js"(exports2, module2) {
        "use strict";
        var inspect = require_object_inspect();
        var $TypeError = require_type();
        var listGetNode = function (list, key, isDelete) {
            var prev = list;
            var curr;
            for (; (curr = prev.next) != null; prev = curr) {
                if (curr.key === key) {
                    prev.next = curr.next;
                    if (!isDelete) {
                        curr.next = /** @type {NonNullable<typeof list.next>} */ list.next;
                        list.next = curr;
                    }
                    return curr;
                }
            }
        };
        var listGet = function (objects, key) {
            if (!objects) {
                return void 0;
            }
            var node = listGetNode(objects, key);
            return node && node.value;
        };
        var listSet = function (objects, key, value) {
            var node = listGetNode(objects, key);
            if (node) {
                node.value = value;
            } else {
                objects.next =
                    /** @type {import('./list.d.ts').ListNode<typeof value, typeof key>} */
                    {
                        // eslint-disable-line no-param-reassign, no-extra-parens
                        key,
                        next: objects.next,
                        value
                    };
            }
        };
        var listHas = function (objects, key) {
            if (!objects) {
                return false;
            }
            return !!listGetNode(objects, key);
        };
        var listDelete = function (objects, key) {
            if (objects) {
                return listGetNode(objects, key, true);
            }
        };
        module2.exports = function getSideChannelList() {
            var $o;
            var channel = {
                assert: function (key) {
                    if (!channel.has(key)) {
                        throw new $TypeError("Side channel does not contain " + inspect(key));
                    }
                },
                delete: function (key) {
                    var deletedNode = listDelete($o, key);
                    if (deletedNode && $o && !$o.next) {
                        $o = void 0;
                    }
                    return !!deletedNode;
                },
                get: function (key) {
                    return listGet($o, key);
                },
                has: function (key) {
                    return listHas($o, key);
                },
                set: function (key, value) {
                    if (!$o) {
                        $o = {
                            next: void 0
                        };
                    }
                    listSet(
                        /** @type {NonNullable<typeof $o>} */
                        $o,
                        key,
                        value
                    );
                }
            };
            return channel;
        };
    }
});

// node_modules/.pnpm/es-object-atoms@1.1.2/node_modules/es-object-atoms/index.js
var require_es_object_atoms = __commonJS({
    "node_modules/.pnpm/es-object-atoms@1.1.2/node_modules/es-object-atoms/index.js"(exports2, module2) {
        "use strict";
        module2.exports = Object;
    }
});

// node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/index.js
var require_es_errors = __commonJS({
    "node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/index.js"(exports2, module2) {
        "use strict";
        module2.exports = Error;
    }
});

// node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/eval.js
var require_eval = __commonJS({
    "node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/eval.js"(exports2, module2) {
        "use strict";
        module2.exports = EvalError;
    }
});

// node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/range.js
var require_range = __commonJS({
    "node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/range.js"(exports2, module2) {
        "use strict";
        module2.exports = RangeError;
    }
});

// node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/ref.js
var require_ref = __commonJS({
    "node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/ref.js"(exports2, module2) {
        "use strict";
        module2.exports = ReferenceError;
    }
});

// node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/syntax.js
var require_syntax = __commonJS({
    "node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/syntax.js"(exports2, module2) {
        "use strict";
        module2.exports = SyntaxError;
    }
});

// node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/uri.js
var require_uri = __commonJS({
    "node_modules/.pnpm/es-errors@1.3.0/node_modules/es-errors/uri.js"(exports2, module2) {
        "use strict";
        module2.exports = URIError;
    }
});

// node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/abs.js
var require_abs = __commonJS({
    "node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/abs.js"(exports2, module2) {
        "use strict";
        module2.exports = Math.abs;
    }
});

// node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/floor.js
var require_floor = __commonJS({
    "node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/floor.js"(exports2, module2) {
        "use strict";
        module2.exports = Math.floor;
    }
});

// node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/max.js
var require_max = __commonJS({
    "node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/max.js"(exports2, module2) {
        "use strict";
        module2.exports = Math.max;
    }
});

// node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/min.js
var require_min = __commonJS({
    "node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/min.js"(exports2, module2) {
        "use strict";
        module2.exports = Math.min;
    }
});

// node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/pow.js
var require_pow = __commonJS({
    "node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/pow.js"(exports2, module2) {
        "use strict";
        module2.exports = Math.pow;
    }
});

// node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/round.js
var require_round = __commonJS({
    "node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/round.js"(exports2, module2) {
        "use strict";
        module2.exports = Math.round;
    }
});

// node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/isNaN.js
var require_isNaN = __commonJS({
    "node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/isNaN.js"(exports2, module2) {
        "use strict";
        module2.exports =
            Number.isNaN ||
            function isNaN2(a) {
                return a !== a;
            };
    }
});

// node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/sign.js
var require_sign = __commonJS({
    "node_modules/.pnpm/math-intrinsics@1.1.0/node_modules/math-intrinsics/sign.js"(exports2, module2) {
        "use strict";
        var $isNaN = require_isNaN();
        module2.exports = function sign(number) {
            if ($isNaN(number) || number === 0) {
                return number;
            }
            return number < 0 ? -1 : 1;
        };
    }
});

// node_modules/.pnpm/gopd@1.2.0/node_modules/gopd/gOPD.js
var require_gOPD = __commonJS({
    "node_modules/.pnpm/gopd@1.2.0/node_modules/gopd/gOPD.js"(exports2, module2) {
        "use strict";
        module2.exports = Object.getOwnPropertyDescriptor;
    }
});

// node_modules/.pnpm/gopd@1.2.0/node_modules/gopd/index.js
var require_gopd = __commonJS({
    "node_modules/.pnpm/gopd@1.2.0/node_modules/gopd/index.js"(exports2, module2) {
        "use strict";
        var $gOPD = require_gOPD();
        if ($gOPD) {
            try {
                $gOPD([], "length");
            } catch (e) {
                $gOPD = null;
            }
        }
        module2.exports = $gOPD;
    }
});

// node_modules/.pnpm/es-define-property@1.0.1/node_modules/es-define-property/index.js
var require_es_define_property = __commonJS({
    "node_modules/.pnpm/es-define-property@1.0.1/node_modules/es-define-property/index.js"(exports2, module2) {
        "use strict";
        var $defineProperty = Object.defineProperty || false;
        if ($defineProperty) {
            try {
                $defineProperty({}, "a", { value: 1 });
            } catch (e) {
                $defineProperty = false;
            }
        }
        module2.exports = $defineProperty;
    }
});

// node_modules/.pnpm/has-symbols@1.1.0/node_modules/has-symbols/shams.js
var require_shams = __commonJS({
    "node_modules/.pnpm/has-symbols@1.1.0/node_modules/has-symbols/shams.js"(exports2, module2) {
        "use strict";
        module2.exports = function hasSymbols() {
            if (typeof Symbol !== "function" || typeof Object.getOwnPropertySymbols !== "function") {
                return false;
            }
            if (typeof Symbol.iterator === "symbol") {
                return true;
            }
            var obj = {};
            var sym = /* @__PURE__ */ Symbol("test");
            var symObj = Object(sym);
            if (typeof sym === "string") {
                return false;
            }
            if (Object.prototype.toString.call(sym) !== "[object Symbol]") {
                return false;
            }
            if (Object.prototype.toString.call(symObj) !== "[object Symbol]") {
                return false;
            }
            var symVal = 42;
            obj[sym] = symVal;
            for (var _ in obj) {
                return false;
            }
            if (typeof Object.keys === "function" && Object.keys(obj).length !== 0) {
                return false;
            }
            if (typeof Object.getOwnPropertyNames === "function" && Object.getOwnPropertyNames(obj).length !== 0) {
                return false;
            }
            var syms = Object.getOwnPropertySymbols(obj);
            if (syms.length !== 1 || syms[0] !== sym) {
                return false;
            }
            if (!Object.prototype.propertyIsEnumerable.call(obj, sym)) {
                return false;
            }
            if (typeof Object.getOwnPropertyDescriptor === "function") {
                var descriptor =
                    /** @type {PropertyDescriptor} */
                    Object.getOwnPropertyDescriptor(obj, sym);
                if (descriptor.value !== symVal || descriptor.enumerable !== true) {
                    return false;
                }
            }
            return true;
        };
    }
});

// node_modules/.pnpm/has-symbols@1.1.0/node_modules/has-symbols/index.js
var require_has_symbols = __commonJS({
    "node_modules/.pnpm/has-symbols@1.1.0/node_modules/has-symbols/index.js"(exports2, module2) {
        "use strict";
        var origSymbol = typeof Symbol !== "undefined" && Symbol;
        var hasSymbolSham = require_shams();
        module2.exports = function hasNativeSymbols() {
            if (typeof origSymbol !== "function") {
                return false;
            }
            if (typeof Symbol !== "function") {
                return false;
            }
            if (typeof origSymbol("foo") !== "symbol") {
                return false;
            }
            if (typeof (/* @__PURE__ */ Symbol("bar")) !== "symbol") {
                return false;
            }
            return hasSymbolSham();
        };
    }
});

// node_modules/.pnpm/get-proto@1.0.1/node_modules/get-proto/Reflect.getPrototypeOf.js
var require_Reflect_getPrototypeOf = __commonJS({
    "node_modules/.pnpm/get-proto@1.0.1/node_modules/get-proto/Reflect.getPrototypeOf.js"(exports2, module2) {
        "use strict";
        module2.exports = (typeof Reflect !== "undefined" && Reflect.getPrototypeOf) || null;
    }
});

// node_modules/.pnpm/get-proto@1.0.1/node_modules/get-proto/Object.getPrototypeOf.js
var require_Object_getPrototypeOf = __commonJS({
    "node_modules/.pnpm/get-proto@1.0.1/node_modules/get-proto/Object.getPrototypeOf.js"(exports2, module2) {
        "use strict";
        var $Object = require_es_object_atoms();
        module2.exports = $Object.getPrototypeOf || null;
    }
});

// node_modules/.pnpm/function-bind@1.1.2/node_modules/function-bind/implementation.js
var require_implementation = __commonJS({
    "node_modules/.pnpm/function-bind@1.1.2/node_modules/function-bind/implementation.js"(exports2, module2) {
        "use strict";
        var ERROR_MESSAGE = "Function.prototype.bind called on incompatible ";
        var toStr = Object.prototype.toString;
        var max = Math.max;
        var funcType = "[object Function]";
        var concatty = function concatty2(a, b) {
            var arr = [];
            for (var i = 0; i < a.length; i += 1) {
                arr[i] = a[i];
            }
            for (var j = 0; j < b.length; j += 1) {
                arr[j + a.length] = b[j];
            }
            return arr;
        };
        var slicy = function slicy2(arrLike, offset) {
            var arr = [];
            for (var i = offset || 0, j = 0; i < arrLike.length; i += 1, j += 1) {
                arr[j] = arrLike[i];
            }
            return arr;
        };
        var joiny = function (arr, joiner) {
            var str = "";
            for (var i = 0; i < arr.length; i += 1) {
                str += arr[i];
                if (i + 1 < arr.length) {
                    str += joiner;
                }
            }
            return str;
        };
        module2.exports = function bind2(that) {
            var target = this;
            if (typeof target !== "function" || toStr.apply(target) !== funcType) {
                throw new TypeError(ERROR_MESSAGE + target);
            }
            var args = slicy(arguments, 1);
            var bound;
            var binder = function () {
                if (this instanceof bound) {
                    var result = target.apply(this, concatty(args, arguments));
                    if (Object(result) === result) {
                        return result;
                    }
                    return this;
                }
                return target.apply(that, concatty(args, arguments));
            };
            var boundLength = max(0, target.length - args.length);
            var boundArgs = [];
            for (var i = 0; i < boundLength; i++) {
                boundArgs[i] = "$" + i;
            }
            bound = Function("binder", "return function (" + joiny(boundArgs, ",") + "){ return binder.apply(this,arguments); }")(binder);
            if (target.prototype) {
                var Empty = function Empty2() {};
                Empty.prototype = target.prototype;
                bound.prototype = new Empty();
                Empty.prototype = null;
            }
            return bound;
        };
    }
});

// node_modules/.pnpm/function-bind@1.1.2/node_modules/function-bind/index.js
var require_function_bind = __commonJS({
    "node_modules/.pnpm/function-bind@1.1.2/node_modules/function-bind/index.js"(exports2, module2) {
        "use strict";
        var implementation = require_implementation();
        module2.exports = Function.prototype.bind || implementation;
    }
});

// node_modules/.pnpm/call-bind-apply-helpers@1.0.2/node_modules/call-bind-apply-helpers/functionCall.js
var require_functionCall = __commonJS({
    "node_modules/.pnpm/call-bind-apply-helpers@1.0.2/node_modules/call-bind-apply-helpers/functionCall.js"(exports2, module2) {
        "use strict";
        module2.exports = Function.prototype.call;
    }
});

// node_modules/.pnpm/call-bind-apply-helpers@1.0.2/node_modules/call-bind-apply-helpers/functionApply.js
var require_functionApply = __commonJS({
    "node_modules/.pnpm/call-bind-apply-helpers@1.0.2/node_modules/call-bind-apply-helpers/functionApply.js"(exports2, module2) {
        "use strict";
        module2.exports = Function.prototype.apply;
    }
});

// node_modules/.pnpm/call-bind-apply-helpers@1.0.2/node_modules/call-bind-apply-helpers/reflectApply.js
var require_reflectApply = __commonJS({
    "node_modules/.pnpm/call-bind-apply-helpers@1.0.2/node_modules/call-bind-apply-helpers/reflectApply.js"(exports2, module2) {
        "use strict";
        module2.exports = typeof Reflect !== "undefined" && Reflect && Reflect.apply;
    }
});

// node_modules/.pnpm/call-bind-apply-helpers@1.0.2/node_modules/call-bind-apply-helpers/actualApply.js
var require_actualApply = __commonJS({
    "node_modules/.pnpm/call-bind-apply-helpers@1.0.2/node_modules/call-bind-apply-helpers/actualApply.js"(exports2, module2) {
        "use strict";
        var bind2 = require_function_bind();
        var $apply = require_functionApply();
        var $call = require_functionCall();
        var $reflectApply = require_reflectApply();
        module2.exports = $reflectApply || bind2.call($call, $apply);
    }
});

// node_modules/.pnpm/call-bind-apply-helpers@1.0.2/node_modules/call-bind-apply-helpers/index.js
var require_call_bind_apply_helpers = __commonJS({
    "node_modules/.pnpm/call-bind-apply-helpers@1.0.2/node_modules/call-bind-apply-helpers/index.js"(exports2, module2) {
        "use strict";
        var bind2 = require_function_bind();
        var $TypeError = require_type();
        var $call = require_functionCall();
        var $actualApply = require_actualApply();
        module2.exports = function callBindBasic(args) {
            if (args.length < 1 || typeof args[0] !== "function") {
                throw new $TypeError("a function is required");
            }
            return $actualApply(bind2, $call, args);
        };
    }
});

// node_modules/.pnpm/dunder-proto@1.0.1/node_modules/dunder-proto/get.js
var require_get = __commonJS({
    "node_modules/.pnpm/dunder-proto@1.0.1/node_modules/dunder-proto/get.js"(exports2, module2) {
        "use strict";
        var callBind = require_call_bind_apply_helpers();
        var gOPD = require_gopd();
        var hasProtoAccessor;
        try {
            hasProtoAccessor = /** @type {{ __proto__?: typeof Array.prototype }} */ [].__proto__ === Array.prototype;
        } catch (e) {
            if (!e || typeof e !== "object" || !("code" in e) || e.code !== "ERR_PROTO_ACCESS") {
                throw e;
            }
        }
        var desc =
            !!hasProtoAccessor &&
            gOPD &&
            gOPD(
                Object.prototype,
                /** @type {keyof typeof Object.prototype} */
                "__proto__"
            );
        var $Object = Object;
        var $getPrototypeOf = $Object.getPrototypeOf;
        module2.exports =
            desc && typeof desc.get === "function"
                ? callBind([desc.get])
                : typeof $getPrototypeOf === "function"
                  ? /** @type {import('./get')} */
                    function getDunder(value) {
                        return $getPrototypeOf(value == null ? value : $Object(value));
                    }
                  : false;
    }
});

// node_modules/.pnpm/get-proto@1.0.1/node_modules/get-proto/index.js
var require_get_proto = __commonJS({
    "node_modules/.pnpm/get-proto@1.0.1/node_modules/get-proto/index.js"(exports2, module2) {
        "use strict";
        var reflectGetProto = require_Reflect_getPrototypeOf();
        var originalGetProto = require_Object_getPrototypeOf();
        var getDunderProto = require_get();
        module2.exports = reflectGetProto
            ? function getProto(O) {
                  return reflectGetProto(O);
              }
            : originalGetProto
              ? function getProto(O) {
                    if (!O || (typeof O !== "object" && typeof O !== "function")) {
                        throw new TypeError("getProto: not an object");
                    }
                    return originalGetProto(O);
                }
              : getDunderProto
                ? function getProto(O) {
                      return getDunderProto(O);
                  }
                : null;
    }
});

// node_modules/.pnpm/hasown@2.0.4/node_modules/hasown/index.js
var require_hasown = __commonJS({
    "node_modules/.pnpm/hasown@2.0.4/node_modules/hasown/index.js"(exports2, module2) {
        "use strict";
        var call = Function.prototype.call;
        var $hasOwn = Object.prototype.hasOwnProperty;
        var bind2 = require_function_bind();
        module2.exports = bind2.call(call, $hasOwn);
    }
});

// node_modules/.pnpm/get-intrinsic@1.3.0/node_modules/get-intrinsic/index.js
var require_get_intrinsic = __commonJS({
    "node_modules/.pnpm/get-intrinsic@1.3.0/node_modules/get-intrinsic/index.js"(exports2, module2) {
        "use strict";
        var undefined2;
        var $Object = require_es_object_atoms();
        var $Error = require_es_errors();
        var $EvalError = require_eval();
        var $RangeError = require_range();
        var $ReferenceError = require_ref();
        var $SyntaxError = require_syntax();
        var $TypeError = require_type();
        var $URIError = require_uri();
        var abs = require_abs();
        var floor = require_floor();
        var max = require_max();
        var min = require_min();
        var pow = require_pow();
        var round = require_round();
        var sign = require_sign();
        var $Function = Function;
        var getEvalledConstructor = function (expressionSyntax) {
            try {
                return $Function('"use strict"; return (' + expressionSyntax + ").constructor;")();
            } catch (e) {}
        };
        var $gOPD = require_gopd();
        var $defineProperty = require_es_define_property();
        var throwTypeError = function () {
            throw new $TypeError();
        };
        var ThrowTypeError = $gOPD
            ? (function () {
                  try {
                      arguments.callee;
                      return throwTypeError;
                  } catch (calleeThrows) {
                      try {
                          return $gOPD(arguments, "callee").get;
                      } catch (gOPDthrows) {
                          return throwTypeError;
                      }
                  }
              })()
            : throwTypeError;
        var hasSymbols = require_has_symbols()();
        var getProto = require_get_proto();
        var $ObjectGPO = require_Object_getPrototypeOf();
        var $ReflectGPO = require_Reflect_getPrototypeOf();
        var $apply = require_functionApply();
        var $call = require_functionCall();
        var needsEval = {};
        var TypedArray = typeof Uint8Array === "undefined" || !getProto ? undefined2 : getProto(Uint8Array);
        var INTRINSICS = {
            __proto__: null,
            "%AggregateError%": typeof AggregateError === "undefined" ? undefined2 : AggregateError,
            "%Array%": Array,
            "%ArrayBuffer%": typeof ArrayBuffer === "undefined" ? undefined2 : ArrayBuffer,
            "%ArrayIteratorPrototype%": hasSymbols && getProto ? getProto([][Symbol.iterator]()) : undefined2,
            "%AsyncFromSyncIteratorPrototype%": undefined2,
            "%AsyncFunction%": needsEval,
            "%AsyncGenerator%": needsEval,
            "%AsyncGeneratorFunction%": needsEval,
            "%AsyncIteratorPrototype%": needsEval,
            "%Atomics%": typeof Atomics === "undefined" ? undefined2 : Atomics,
            "%BigInt%": typeof BigInt === "undefined" ? undefined2 : BigInt,
            "%BigInt64Array%": typeof BigInt64Array === "undefined" ? undefined2 : BigInt64Array,
            "%BigUint64Array%": typeof BigUint64Array === "undefined" ? undefined2 : BigUint64Array,
            "%Boolean%": Boolean,
            "%DataView%": typeof DataView === "undefined" ? undefined2 : DataView,
            "%Date%": Date,
            "%decodeURI%": decodeURI,
            "%decodeURIComponent%": decodeURIComponent,
            "%encodeURI%": encodeURI,
            "%encodeURIComponent%": encodeURIComponent,
            "%Error%": $Error,
            "%eval%": eval,
            // eslint-disable-line no-eval
            "%EvalError%": $EvalError,
            "%Float16Array%": typeof Float16Array === "undefined" ? undefined2 : Float16Array,
            "%Float32Array%": typeof Float32Array === "undefined" ? undefined2 : Float32Array,
            "%Float64Array%": typeof Float64Array === "undefined" ? undefined2 : Float64Array,
            "%FinalizationRegistry%": typeof FinalizationRegistry === "undefined" ? undefined2 : FinalizationRegistry,
            "%Function%": $Function,
            "%GeneratorFunction%": needsEval,
            "%Int8Array%": typeof Int8Array === "undefined" ? undefined2 : Int8Array,
            "%Int16Array%": typeof Int16Array === "undefined" ? undefined2 : Int16Array,
            "%Int32Array%": typeof Int32Array === "undefined" ? undefined2 : Int32Array,
            "%isFinite%": isFinite,
            "%isNaN%": isNaN,
            "%IteratorPrototype%": hasSymbols && getProto ? getProto(getProto([][Symbol.iterator]())) : undefined2,
            "%JSON%": typeof JSON === "object" ? JSON : undefined2,
            "%Map%": typeof Map === "undefined" ? undefined2 : Map,
            "%MapIteratorPrototype%": typeof Map === "undefined" || !hasSymbols || !getProto ? undefined2 : getProto(/* @__PURE__ */ new Map()[Symbol.iterator]()),
            "%Math%": Math,
            "%Number%": Number,
            "%Object%": $Object,
            "%Object.getOwnPropertyDescriptor%": $gOPD,
            "%parseFloat%": parseFloat,
            "%parseInt%": parseInt,
            "%Promise%": typeof Promise === "undefined" ? undefined2 : Promise,
            "%Proxy%": typeof Proxy === "undefined" ? undefined2 : Proxy,
            "%RangeError%": $RangeError,
            "%ReferenceError%": $ReferenceError,
            "%Reflect%": typeof Reflect === "undefined" ? undefined2 : Reflect,
            "%RegExp%": RegExp,
            "%Set%": typeof Set === "undefined" ? undefined2 : Set,
            "%SetIteratorPrototype%": typeof Set === "undefined" || !hasSymbols || !getProto ? undefined2 : getProto(/* @__PURE__ */ new Set()[Symbol.iterator]()),
            "%SharedArrayBuffer%": typeof SharedArrayBuffer === "undefined" ? undefined2 : SharedArrayBuffer,
            "%String%": String,
            "%StringIteratorPrototype%": hasSymbols && getProto ? getProto(""[Symbol.iterator]()) : undefined2,
            "%Symbol%": hasSymbols ? Symbol : undefined2,
            "%SyntaxError%": $SyntaxError,
            "%ThrowTypeError%": ThrowTypeError,
            "%TypedArray%": TypedArray,
            "%TypeError%": $TypeError,
            "%Uint8Array%": typeof Uint8Array === "undefined" ? undefined2 : Uint8Array,
            "%Uint8ClampedArray%": typeof Uint8ClampedArray === "undefined" ? undefined2 : Uint8ClampedArray,
            "%Uint16Array%": typeof Uint16Array === "undefined" ? undefined2 : Uint16Array,
            "%Uint32Array%": typeof Uint32Array === "undefined" ? undefined2 : Uint32Array,
            "%URIError%": $URIError,
            "%WeakMap%": typeof WeakMap === "undefined" ? undefined2 : WeakMap,
            "%WeakRef%": typeof WeakRef === "undefined" ? undefined2 : WeakRef,
            "%WeakSet%": typeof WeakSet === "undefined" ? undefined2 : WeakSet,
            "%Function.prototype.call%": $call,
            "%Function.prototype.apply%": $apply,
            "%Object.defineProperty%": $defineProperty,
            "%Object.getPrototypeOf%": $ObjectGPO,
            "%Math.abs%": abs,
            "%Math.floor%": floor,
            "%Math.max%": max,
            "%Math.min%": min,
            "%Math.pow%": pow,
            "%Math.round%": round,
            "%Math.sign%": sign,
            "%Reflect.getPrototypeOf%": $ReflectGPO
        };
        if (getProto) {
            try {
                null.error;
            } catch (e) {
                errorProto = getProto(getProto(e));
                INTRINSICS["%Error.prototype%"] = errorProto;
            }
        }
        var errorProto;
        var doEval = function doEval2(name) {
            var value;
            if (name === "%AsyncFunction%") {
                value = getEvalledConstructor("async function () {}");
            } else if (name === "%GeneratorFunction%") {
                value = getEvalledConstructor("function* () {}");
            } else if (name === "%AsyncGeneratorFunction%") {
                value = getEvalledConstructor("async function* () {}");
            } else if (name === "%AsyncGenerator%") {
                var fn = doEval2("%AsyncGeneratorFunction%");
                if (fn) {
                    value = fn.prototype;
                }
            } else if (name === "%AsyncIteratorPrototype%") {
                var gen = doEval2("%AsyncGenerator%");
                if (gen && getProto) {
                    value = getProto(gen.prototype);
                }
            }
            INTRINSICS[name] = value;
            return value;
        };
        var LEGACY_ALIASES = {
            __proto__: null,
            "%ArrayBufferPrototype%": ["ArrayBuffer", "prototype"],
            "%ArrayPrototype%": ["Array", "prototype"],
            "%ArrayProto_entries%": ["Array", "prototype", "entries"],
            "%ArrayProto_forEach%": ["Array", "prototype", "forEach"],
            "%ArrayProto_keys%": ["Array", "prototype", "keys"],
            "%ArrayProto_values%": ["Array", "prototype", "values"],
            "%AsyncFunctionPrototype%": ["AsyncFunction", "prototype"],
            "%AsyncGenerator%": ["AsyncGeneratorFunction", "prototype"],
            "%AsyncGeneratorPrototype%": ["AsyncGeneratorFunction", "prototype", "prototype"],
            "%BooleanPrototype%": ["Boolean", "prototype"],
            "%DataViewPrototype%": ["DataView", "prototype"],
            "%DatePrototype%": ["Date", "prototype"],
            "%ErrorPrototype%": ["Error", "prototype"],
            "%EvalErrorPrototype%": ["EvalError", "prototype"],
            "%Float32ArrayPrototype%": ["Float32Array", "prototype"],
            "%Float64ArrayPrototype%": ["Float64Array", "prototype"],
            "%FunctionPrototype%": ["Function", "prototype"],
            "%Generator%": ["GeneratorFunction", "prototype"],
            "%GeneratorPrototype%": ["GeneratorFunction", "prototype", "prototype"],
            "%Int8ArrayPrototype%": ["Int8Array", "prototype"],
            "%Int16ArrayPrototype%": ["Int16Array", "prototype"],
            "%Int32ArrayPrototype%": ["Int32Array", "prototype"],
            "%JSONParse%": ["JSON", "parse"],
            "%JSONStringify%": ["JSON", "stringify"],
            "%MapPrototype%": ["Map", "prototype"],
            "%NumberPrototype%": ["Number", "prototype"],
            "%ObjectPrototype%": ["Object", "prototype"],
            "%ObjProto_toString%": ["Object", "prototype", "toString"],
            "%ObjProto_valueOf%": ["Object", "prototype", "valueOf"],
            "%PromisePrototype%": ["Promise", "prototype"],
            "%PromiseProto_then%": ["Promise", "prototype", "then"],
            "%Promise_all%": ["Promise", "all"],
            "%Promise_reject%": ["Promise", "reject"],
            "%Promise_resolve%": ["Promise", "resolve"],
            "%RangeErrorPrototype%": ["RangeError", "prototype"],
            "%ReferenceErrorPrototype%": ["ReferenceError", "prototype"],
            "%RegExpPrototype%": ["RegExp", "prototype"],
            "%SetPrototype%": ["Set", "prototype"],
            "%SharedArrayBufferPrototype%": ["SharedArrayBuffer", "prototype"],
            "%StringPrototype%": ["String", "prototype"],
            "%SymbolPrototype%": ["Symbol", "prototype"],
            "%SyntaxErrorPrototype%": ["SyntaxError", "prototype"],
            "%TypedArrayPrototype%": ["TypedArray", "prototype"],
            "%TypeErrorPrototype%": ["TypeError", "prototype"],
            "%Uint8ArrayPrototype%": ["Uint8Array", "prototype"],
            "%Uint8ClampedArrayPrototype%": ["Uint8ClampedArray", "prototype"],
            "%Uint16ArrayPrototype%": ["Uint16Array", "prototype"],
            "%Uint32ArrayPrototype%": ["Uint32Array", "prototype"],
            "%URIErrorPrototype%": ["URIError", "prototype"],
            "%WeakMapPrototype%": ["WeakMap", "prototype"],
            "%WeakSetPrototype%": ["WeakSet", "prototype"]
        };
        var bind2 = require_function_bind();
        var hasOwn = require_hasown();
        var $concat = bind2.call($call, Array.prototype.concat);
        var $spliceApply = bind2.call($apply, Array.prototype.splice);
        var $replace = bind2.call($call, String.prototype.replace);
        var $strSlice = bind2.call($call, String.prototype.slice);
        var $exec = bind2.call($call, RegExp.prototype.exec);
        var rePropName = /[^%.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|%$))/g;
        var reEscapeChar = /\\(\\)?/g;
        var stringToPath = function stringToPath2(string) {
            var first = $strSlice(string, 0, 1);
            var last = $strSlice(string, -1);
            if (first === "%" && last !== "%") {
                throw new $SyntaxError("invalid intrinsic syntax, expected closing `%`");
            } else if (last === "%" && first !== "%") {
                throw new $SyntaxError("invalid intrinsic syntax, expected opening `%`");
            }
            var result = [];
            $replace(string, rePropName, function (match, number, quote, subString) {
                result[result.length] = quote ? $replace(subString, reEscapeChar, "$1") : number || match;
            });
            return result;
        };
        var getBaseIntrinsic = function getBaseIntrinsic2(name, allowMissing) {
            var intrinsicName = name;
            var alias;
            if (hasOwn(LEGACY_ALIASES, intrinsicName)) {
                alias = LEGACY_ALIASES[intrinsicName];
                intrinsicName = "%" + alias[0] + "%";
            }
            if (hasOwn(INTRINSICS, intrinsicName)) {
                var value = INTRINSICS[intrinsicName];
                if (value === needsEval) {
                    value = doEval(intrinsicName);
                }
                if (typeof value === "undefined" && !allowMissing) {
                    throw new $TypeError("intrinsic " + name + " exists, but is not available. Please file an issue!");
                }
                return {
                    alias,
                    name: intrinsicName,
                    value
                };
            }
            throw new $SyntaxError("intrinsic " + name + " does not exist!");
        };
        module2.exports = function GetIntrinsic(name, allowMissing) {
            if (typeof name !== "string" || name.length === 0) {
                throw new $TypeError("intrinsic name must be a non-empty string");
            }
            if (arguments.length > 1 && typeof allowMissing !== "boolean") {
                throw new $TypeError('"allowMissing" argument must be a boolean');
            }
            if ($exec(/^%?[^%]*%?$/, name) === null) {
                throw new $SyntaxError("`%` may not be present anywhere but at the beginning and end of the intrinsic name");
            }
            var parts = stringToPath(name);
            var intrinsicBaseName = parts.length > 0 ? parts[0] : "";
            var intrinsic = getBaseIntrinsic("%" + intrinsicBaseName + "%", allowMissing);
            var intrinsicRealName = intrinsic.name;
            var value = intrinsic.value;
            var skipFurtherCaching = false;
            var alias = intrinsic.alias;
            if (alias) {
                intrinsicBaseName = alias[0];
                $spliceApply(parts, $concat([0, 1], alias));
            }
            for (var i = 1, isOwn = true; i < parts.length; i += 1) {
                var part = parts[i];
                var first = $strSlice(part, 0, 1);
                var last = $strSlice(part, -1);
                if ((first === '"' || first === "'" || first === "`" || last === '"' || last === "'" || last === "`") && first !== last) {
                    throw new $SyntaxError("property names with quotes must have matching quotes");
                }
                if (part === "constructor" || !isOwn) {
                    skipFurtherCaching = true;
                }
                intrinsicBaseName += "." + part;
                intrinsicRealName = "%" + intrinsicBaseName + "%";
                if (hasOwn(INTRINSICS, intrinsicRealName)) {
                    value = INTRINSICS[intrinsicRealName];
                } else if (value != null) {
                    if (!(part in value)) {
                        if (!allowMissing) {
                            throw new $TypeError("base intrinsic for " + name + " exists, but the property is not available.");
                        }
                        return void undefined2;
                    }
                    if ($gOPD && i + 1 >= parts.length) {
                        var desc = $gOPD(value, part);
                        isOwn = !!desc;
                        if (isOwn && "get" in desc && !("originalValue" in desc.get)) {
                            value = desc.get;
                        } else {
                            value = value[part];
                        }
                    } else {
                        isOwn = hasOwn(value, part);
                        value = value[part];
                    }
                    if (isOwn && !skipFurtherCaching) {
                        INTRINSICS[intrinsicRealName] = value;
                    }
                }
            }
            return value;
        };
    }
});

// node_modules/.pnpm/call-bound@1.0.4/node_modules/call-bound/index.js
var require_call_bound = __commonJS({
    "node_modules/.pnpm/call-bound@1.0.4/node_modules/call-bound/index.js"(exports2, module2) {
        "use strict";
        var GetIntrinsic = require_get_intrinsic();
        var callBindBasic = require_call_bind_apply_helpers();
        var $indexOf = callBindBasic([GetIntrinsic("%String.prototype.indexOf%")]);
        module2.exports = function callBoundIntrinsic(name, allowMissing) {
            var intrinsic =
                /** @type {(this: unknown, ...args: unknown[]) => unknown} */
                GetIntrinsic(name, !!allowMissing);
            if (typeof intrinsic === "function" && $indexOf(name, ".prototype.") > -1) {
                return callBindBasic(
                    /** @type {const} */
                    [intrinsic]
                );
            }
            return intrinsic;
        };
    }
});

// node_modules/.pnpm/side-channel-map@1.0.1/node_modules/side-channel-map/index.js
var require_side_channel_map = __commonJS({
    "node_modules/.pnpm/side-channel-map@1.0.1/node_modules/side-channel-map/index.js"(exports2, module2) {
        "use strict";
        var GetIntrinsic = require_get_intrinsic();
        var callBound = require_call_bound();
        var inspect = require_object_inspect();
        var $TypeError = require_type();
        var $Map = GetIntrinsic("%Map%", true);
        var $mapGet = callBound("Map.prototype.get", true);
        var $mapSet = callBound("Map.prototype.set", true);
        var $mapHas = callBound("Map.prototype.has", true);
        var $mapDelete = callBound("Map.prototype.delete", true);
        var $mapSize = callBound("Map.prototype.size", true);
        module2.exports =
            !!$Map &&
            /** @type {Exclude<import('.'), false>} */
            function getSideChannelMap() {
                var $m;
                var channel = {
                    assert: function (key) {
                        if (!channel.has(key)) {
                            throw new $TypeError("Side channel does not contain " + inspect(key));
                        }
                    },
                    delete: function (key) {
                        if ($m) {
                            var result = $mapDelete($m, key);
                            if ($mapSize($m) === 0) {
                                $m = void 0;
                            }
                            return result;
                        }
                        return false;
                    },
                    get: function (key) {
                        if ($m) {
                            return $mapGet($m, key);
                        }
                    },
                    has: function (key) {
                        if ($m) {
                            return $mapHas($m, key);
                        }
                        return false;
                    },
                    set: function (key, value) {
                        if (!$m) {
                            $m = new $Map();
                        }
                        $mapSet($m, key, value);
                    }
                };
                return channel;
            };
    }
});

// node_modules/.pnpm/side-channel-weakmap@1.0.2/node_modules/side-channel-weakmap/index.js
var require_side_channel_weakmap = __commonJS({
    "node_modules/.pnpm/side-channel-weakmap@1.0.2/node_modules/side-channel-weakmap/index.js"(exports2, module2) {
        "use strict";
        var GetIntrinsic = require_get_intrinsic();
        var callBound = require_call_bound();
        var inspect = require_object_inspect();
        var getSideChannelMap = require_side_channel_map();
        var $TypeError = require_type();
        var $WeakMap = GetIntrinsic("%WeakMap%", true);
        var $weakMapGet = callBound("WeakMap.prototype.get", true);
        var $weakMapSet = callBound("WeakMap.prototype.set", true);
        var $weakMapHas = callBound("WeakMap.prototype.has", true);
        var $weakMapDelete = callBound("WeakMap.prototype.delete", true);
        module2.exports = $WeakMap
            ? /** @type {Exclude<import('.'), false>} */
              function getSideChannelWeakMap() {
                  var $wm;
                  var $m;
                  var channel = {
                      assert: function (key) {
                          if (!channel.has(key)) {
                              throw new $TypeError("Side channel does not contain " + inspect(key));
                          }
                      },
                      delete: function (key) {
                          if ($WeakMap && key && (typeof key === "object" || typeof key === "function")) {
                              if ($wm) {
                                  return $weakMapDelete($wm, key);
                              }
                          } else if (getSideChannelMap) {
                              if ($m) {
                                  return $m["delete"](key);
                              }
                          }
                          return false;
                      },
                      get: function (key) {
                          if ($WeakMap && key && (typeof key === "object" || typeof key === "function")) {
                              if ($wm) {
                                  return $weakMapGet($wm, key);
                              }
                          }
                          return $m && $m.get(key);
                      },
                      has: function (key) {
                          if ($WeakMap && key && (typeof key === "object" || typeof key === "function")) {
                              if ($wm) {
                                  return $weakMapHas($wm, key);
                              }
                          }
                          return !!$m && $m.has(key);
                      },
                      set: function (key, value) {
                          if ($WeakMap && key && (typeof key === "object" || typeof key === "function")) {
                              if (!$wm) {
                                  $wm = new $WeakMap();
                              }
                              $weakMapSet($wm, key, value);
                          } else if (getSideChannelMap) {
                              if (!$m) {
                                  $m = getSideChannelMap();
                              }
                              $m.set(key, value);
                          }
                      }
                  };
                  return channel;
              }
            : getSideChannelMap;
    }
});

// node_modules/.pnpm/side-channel@1.1.1/node_modules/side-channel/index.js
var require_side_channel = __commonJS({
    "node_modules/.pnpm/side-channel@1.1.1/node_modules/side-channel/index.js"(exports2, module2) {
        "use strict";
        var $TypeError = require_type();
        var inspect = require_object_inspect();
        var getSideChannelList = require_side_channel_list();
        var getSideChannelMap = require_side_channel_map();
        var getSideChannelWeakMap = require_side_channel_weakmap();
        var makeChannel = getSideChannelWeakMap || getSideChannelMap || getSideChannelList;
        module2.exports = function getSideChannel() {
            var $channelData;
            var channel = {
                assert: function (key) {
                    if (!channel.has(key)) {
                        var keyDesc = key && Object(key) === key ? "the given object key" : inspect(key);
                        throw new $TypeError("Side channel does not contain " + keyDesc);
                    }
                },
                delete: function (key) {
                    return !!$channelData && $channelData["delete"](key);
                },
                get: function (key) {
                    return $channelData && $channelData.get(key);
                },
                has: function (key) {
                    return !!$channelData && $channelData.has(key);
                },
                set: function (key, value) {
                    if (!$channelData) {
                        $channelData = makeChannel();
                    }
                    $channelData.set(key, value);
                }
            };
            return channel;
        };
    }
});

// node_modules/.pnpm/qs@6.15.2/node_modules/qs/lib/formats.js
var require_formats = __commonJS({
    "node_modules/.pnpm/qs@6.15.2/node_modules/qs/lib/formats.js"(exports2, module2) {
        "use strict";
        var replace = String.prototype.replace;
        var percentTwenties = /%20/g;
        var Format = {
            RFC1738: "RFC1738",
            RFC3986: "RFC3986"
        };
        module2.exports = {
            default: Format.RFC3986,
            formatters: {
                RFC1738: function (value) {
                    return replace.call(value, percentTwenties, "+");
                },
                RFC3986: function (value) {
                    return String(value);
                }
            },
            RFC1738: Format.RFC1738,
            RFC3986: Format.RFC3986
        };
    }
});

// node_modules/.pnpm/qs@6.15.2/node_modules/qs/lib/utils.js
var require_utils = __commonJS({
    "node_modules/.pnpm/qs@6.15.2/node_modules/qs/lib/utils.js"(exports2, module2) {
        "use strict";
        var formats = require_formats();
        var getSideChannel = require_side_channel();
        var has = Object.prototype.hasOwnProperty;
        var isArray3 = Array.isArray;
        var overflowChannel = getSideChannel();
        var markOverflow = function markOverflow2(obj, maxIndex) {
            overflowChannel.set(obj, maxIndex);
            return obj;
        };
        var isOverflow = function isOverflow2(obj) {
            return overflowChannel.has(obj);
        };
        var getMaxIndex = function getMaxIndex2(obj) {
            return overflowChannel.get(obj);
        };
        var setMaxIndex = function setMaxIndex2(obj, maxIndex) {
            overflowChannel.set(obj, maxIndex);
        };
        var hexTable = (function () {
            var array = [];
            for (var i = 0; i < 256; ++i) {
                array[array.length] = "%" + ((i < 16 ? "0" : "") + i.toString(16)).toUpperCase();
            }
            return array;
        })();
        var compactQueue = function compactQueue2(queue) {
            while (queue.length > 1) {
                var item = queue.pop();
                var obj = item.obj[item.prop];
                if (isArray3(obj)) {
                    var compacted = [];
                    for (var j = 0; j < obj.length; ++j) {
                        if (typeof obj[j] !== "undefined") {
                            compacted[compacted.length] = obj[j];
                        }
                    }
                    item.obj[item.prop] = compacted;
                }
            }
        };
        var arrayToObject2 = function arrayToObject3(source, options) {
            var obj = options && options.plainObjects ? { __proto__: null } : {};
            for (var i = 0; i < source.length; ++i) {
                if (typeof source[i] !== "undefined") {
                    obj[i] = source[i];
                }
            }
            return obj;
        };
        var merge2 = function merge3(target, source, options) {
            if (!source) {
                return target;
            }
            if (typeof source !== "object" && typeof source !== "function") {
                if (isArray3(target)) {
                    var nextIndex = target.length;
                    if (options && typeof options.arrayLimit === "number" && nextIndex > options.arrayLimit) {
                        return markOverflow(arrayToObject2(target.concat(source), options), nextIndex);
                    }
                    target[nextIndex] = source;
                } else if (target && typeof target === "object") {
                    if (isOverflow(target)) {
                        var newIndex = getMaxIndex(target) + 1;
                        target[newIndex] = source;
                        setMaxIndex(target, newIndex);
                    } else if (options && options.strictMerge) {
                        return [target, source];
                    } else if ((options && (options.plainObjects || options.allowPrototypes)) || !has.call(Object.prototype, source)) {
                        target[source] = true;
                    }
                } else {
                    return [target, source];
                }
                return target;
            }
            if (!target || typeof target !== "object") {
                if (isOverflow(source)) {
                    var sourceKeys = Object.keys(source);
                    var result = options && options.plainObjects ? { __proto__: null, 0: target } : { 0: target };
                    for (var m = 0; m < sourceKeys.length; m++) {
                        var oldKey = parseInt(sourceKeys[m], 10);
                        result[oldKey + 1] = source[sourceKeys[m]];
                    }
                    return markOverflow(result, getMaxIndex(source) + 1);
                }
                var combined = [target].concat(source);
                if (options && typeof options.arrayLimit === "number" && combined.length > options.arrayLimit) {
                    return markOverflow(arrayToObject2(combined, options), combined.length - 1);
                }
                return combined;
            }
            var mergeTarget = target;
            if (isArray3(target) && !isArray3(source)) {
                mergeTarget = arrayToObject2(target, options);
            }
            if (isArray3(target) && isArray3(source)) {
                source.forEach(function (item, i) {
                    if (has.call(target, i)) {
                        var targetItem = target[i];
                        if (targetItem && typeof targetItem === "object" && item && typeof item === "object") {
                            target[i] = merge3(targetItem, item, options);
                        } else {
                            target[target.length] = item;
                        }
                    } else {
                        target[i] = item;
                    }
                });
                return target;
            }
            return Object.keys(source).reduce(function (acc, key) {
                var value = source[key];
                if (has.call(acc, key)) {
                    acc[key] = merge3(acc[key], value, options);
                } else {
                    acc[key] = value;
                }
                if (isOverflow(source) && !isOverflow(acc)) {
                    markOverflow(acc, getMaxIndex(source));
                }
                if (isOverflow(acc)) {
                    var keyNum = parseInt(key, 10);
                    if (String(keyNum) === key && keyNum >= 0 && keyNum > getMaxIndex(acc)) {
                        setMaxIndex(acc, keyNum);
                    }
                }
                return acc;
            }, mergeTarget);
        };
        var assign2 = function assignSingleSource(target, source) {
            return Object.keys(source).reduce(function (acc, key) {
                acc[key] = source[key];
                return acc;
            }, target);
        };
        var decode = function (str, defaultDecoder, charset) {
            var strWithoutPlus = str.replace(/\+/g, " ");
            if (charset === "iso-8859-1") {
                return strWithoutPlus.replace(/%[0-9a-f]{2}/gi, unescape);
            }
            try {
                return decodeURIComponent(strWithoutPlus);
            } catch (e) {
                return strWithoutPlus;
            }
        };
        var limit = 1024;
        var encode3 = function encode4(str, defaultEncoder, charset, kind, format) {
            if (str.length === 0) {
                return str;
            }
            var string = str;
            if (typeof str === "symbol") {
                string = Symbol.prototype.toString.call(str);
            } else if (typeof str !== "string") {
                string = String(str);
            }
            if (charset === "iso-8859-1") {
                return escape(string).replace(/%u[0-9a-f]{4}/gi, function ($0) {
                    return "%26%23" + parseInt($0.slice(2), 16) + "%3B";
                });
            }
            var out = "";
            for (var j = 0; j < string.length; j += limit) {
                var segment = string.length >= limit ? string.slice(j, j + limit) : string;
                var arr = [];
                for (var i = 0; i < segment.length; ++i) {
                    var c = segment.charCodeAt(i);
                    if (c === 45 || c === 46 || c === 95 || c === 126 || (c >= 48 && c <= 57) || (c >= 65 && c <= 90) || (c >= 97 && c <= 122) || (format === formats.RFC1738 && (c === 40 || c === 41))) {
                        arr[arr.length] = segment.charAt(i);
                        continue;
                    }
                    if (c < 128) {
                        arr[arr.length] = hexTable[c];
                        continue;
                    }
                    if (c < 2048) {
                        arr[arr.length] = hexTable[192 | (c >> 6)] + hexTable[128 | (c & 63)];
                        continue;
                    }
                    if (c < 55296 || c >= 57344) {
                        arr[arr.length] = hexTable[224 | (c >> 12)] + hexTable[128 | ((c >> 6) & 63)] + hexTable[128 | (c & 63)];
                        continue;
                    }
                    i += 1;
                    c = 65536 + (((c & 1023) << 10) | (segment.charCodeAt(i) & 1023));
                    arr[arr.length] = hexTable[240 | (c >> 18)] + hexTable[128 | ((c >> 12) & 63)] + hexTable[128 | ((c >> 6) & 63)] + hexTable[128 | (c & 63)];
                }
                out += arr.join("");
            }
            return out;
        };
        var compact = function compact2(value) {
            var queue = [{ obj: { o: value }, prop: "o" }];
            var refs = [];
            for (var i = 0; i < queue.length; ++i) {
                var item = queue[i];
                var obj = item.obj[item.prop];
                var keys = Object.keys(obj);
                for (var j = 0; j < keys.length; ++j) {
                    var key = keys[j];
                    var val = obj[key];
                    if (typeof val === "object" && val !== null && refs.indexOf(val) === -1) {
                        queue[queue.length] = { obj, prop: key };
                        refs[refs.length] = val;
                    }
                }
            }
            compactQueue(queue);
            return value;
        };
        var isRegExp2 = function isRegExp3(obj) {
            return Object.prototype.toString.call(obj) === "[object RegExp]";
        };
        var isBuffer2 = function isBuffer3(obj) {
            if (!obj || typeof obj !== "object") {
                return false;
            }
            return !!(obj.constructor && obj.constructor.isBuffer && obj.constructor.isBuffer(obj));
        };
        var combine = function combine2(a, b, arrayLimit, plainObjects) {
            if (isOverflow(a)) {
                var newIndex = getMaxIndex(a) + 1;
                a[newIndex] = b;
                setMaxIndex(a, newIndex);
                return a;
            }
            var result = [].concat(a, b);
            if (result.length > arrayLimit) {
                return markOverflow(arrayToObject2(result, { plainObjects }), result.length - 1);
            }
            return result;
        };
        var maybeMap = function maybeMap2(val, fn) {
            if (isArray3(val)) {
                var mapped = [];
                for (var i = 0; i < val.length; i += 1) {
                    mapped[mapped.length] = fn(val[i]);
                }
                return mapped;
            }
            return fn(val);
        };
        module2.exports = {
            arrayToObject: arrayToObject2,
            assign: assign2,
            combine,
            compact,
            decode,
            encode: encode3,
            isBuffer: isBuffer2,
            isOverflow,
            isRegExp: isRegExp2,
            markOverflow,
            maybeMap,
            merge: merge2
        };
    }
});

// node_modules/.pnpm/qs@6.15.2/node_modules/qs/lib/stringify.js
var require_stringify = __commonJS({
    "node_modules/.pnpm/qs@6.15.2/node_modules/qs/lib/stringify.js"(exports2, module2) {
        "use strict";
        var getSideChannel = require_side_channel();
        var utils = require_utils();
        var formats = require_formats();
        var has = Object.prototype.hasOwnProperty;
        var arrayPrefixGenerators = {
            brackets: function brackets(prefix) {
                return prefix + "[]";
            },
            comma: "comma",
            indices: function indices(prefix, key) {
                return prefix + "[" + key + "]";
            },
            repeat: function repeat(prefix) {
                return prefix;
            }
        };
        var isArray3 = Array.isArray;
        var push = Array.prototype.push;
        var pushToArray = function (arr, valueOrArray) {
            push.apply(arr, isArray3(valueOrArray) ? valueOrArray : [valueOrArray]);
        };
        var toISO = Date.prototype.toISOString;
        var defaultFormat = formats["default"];
        var defaults2 = {
            addQueryPrefix: false,
            allowDots: false,
            allowEmptyArrays: false,
            arrayFormat: "indices",
            charset: "utf-8",
            charsetSentinel: false,
            commaRoundTrip: false,
            delimiter: "&",
            encode: true,
            encodeDotInKeys: false,
            encoder: utils.encode,
            encodeValuesOnly: false,
            filter: void 0,
            format: defaultFormat,
            formatter: formats.formatters[defaultFormat],
            // deprecated
            indices: false,
            serializeDate: function serializeDate(date) {
                return toISO.call(date);
            },
            skipNulls: false,
            strictNullHandling: false
        };
        var isNonNullishPrimitive = function isNonNullishPrimitive2(v) {
            return typeof v === "string" || typeof v === "number" || typeof v === "boolean" || typeof v === "symbol" || typeof v === "bigint";
        };
        var sentinel = {};
        var stringify = function stringify2(object, prefix, generateArrayPrefix, commaRoundTrip, allowEmptyArrays, strictNullHandling, skipNulls, encodeDotInKeys, encoder, filter2, sort, allowDots, serializeDate, format, formatter, encodeValuesOnly, charset, sideChannel) {
            var obj = object;
            var tmpSc = sideChannel;
            var step = 0;
            var findFlag = false;
            while ((tmpSc = tmpSc.get(sentinel)) !== void 0 && !findFlag) {
                var pos = tmpSc.get(object);
                step += 1;
                if (typeof pos !== "undefined") {
                    if (pos === step) {
                        throw new RangeError("Cyclic object value");
                    } else {
                        findFlag = true;
                    }
                }
                if (typeof tmpSc.get(sentinel) === "undefined") {
                    step = 0;
                }
            }
            if (typeof filter2 === "function") {
                obj = filter2(prefix, obj);
            } else if (obj instanceof Date) {
                obj = serializeDate(obj);
            } else if (generateArrayPrefix === "comma" && isArray3(obj)) {
                obj = utils.maybeMap(obj, function (value2) {
                    if (value2 instanceof Date) {
                        return serializeDate(value2);
                    }
                    return value2;
                });
            }
            if (obj === null) {
                if (strictNullHandling) {
                    return formatter(encoder && !encodeValuesOnly ? encoder(prefix, defaults2.encoder, charset, "key", format) : prefix);
                }
                obj = "";
            }
            if (isNonNullishPrimitive(obj) || utils.isBuffer(obj)) {
                if (encoder) {
                    var keyValue = encodeValuesOnly ? prefix : encoder(prefix, defaults2.encoder, charset, "key", format);
                    return [formatter(keyValue) + "=" + formatter(encoder(obj, defaults2.encoder, charset, "value", format))];
                }
                return [formatter(prefix) + "=" + formatter(String(obj))];
            }
            var values = [];
            if (typeof obj === "undefined") {
                return values;
            }
            var objKeys;
            if (generateArrayPrefix === "comma" && isArray3(obj)) {
                if (encodeValuesOnly && encoder) {
                    obj = utils.maybeMap(obj, function (v) {
                        return v == null ? v : encoder(v);
                    });
                }
                objKeys = [{ value: obj.length > 0 ? obj.join(",") || null : void 0 }];
            } else if (isArray3(filter2)) {
                objKeys = filter2;
            } else {
                var keys = Object.keys(obj);
                objKeys = sort ? keys.sort(sort) : keys;
            }
            var encodedPrefix = encodeDotInKeys ? String(prefix).replace(/\./g, "%2E") : String(prefix);
            var adjustedPrefix = commaRoundTrip && isArray3(obj) && obj.length === 1 ? encodedPrefix + "[]" : encodedPrefix;
            if (allowEmptyArrays && isArray3(obj) && obj.length === 0) {
                return adjustedPrefix + "[]";
            }
            for (var j = 0; j < objKeys.length; ++j) {
                var key = objKeys[j];
                var value = typeof key === "object" && key && typeof key.value !== "undefined" ? key.value : obj[key];
                if (skipNulls && value === null) {
                    continue;
                }
                var encodedKey = allowDots && encodeDotInKeys ? String(key).replace(/\./g, "%2E") : String(key);
                var keyPrefix = isArray3(obj) ? (typeof generateArrayPrefix === "function" ? generateArrayPrefix(adjustedPrefix, encodedKey) : adjustedPrefix) : adjustedPrefix + (allowDots ? "." + encodedKey : "[" + encodedKey + "]");
                sideChannel.set(object, step);
                var valueSideChannel = getSideChannel();
                valueSideChannel.set(sentinel, sideChannel);
                pushToArray(
                    values,
                    stringify2(
                        value,
                        keyPrefix,
                        generateArrayPrefix,
                        commaRoundTrip,
                        allowEmptyArrays,
                        strictNullHandling,
                        skipNulls,
                        encodeDotInKeys,
                        generateArrayPrefix === "comma" && encodeValuesOnly && isArray3(obj) ? null : encoder,
                        filter2,
                        sort,
                        allowDots,
                        serializeDate,
                        format,
                        formatter,
                        encodeValuesOnly,
                        charset,
                        valueSideChannel
                    )
                );
            }
            return values;
        };
        var normalizeStringifyOptions = function normalizeStringifyOptions2(opts) {
            if (!opts) {
                return defaults2;
            }
            if (typeof opts.allowEmptyArrays !== "undefined" && typeof opts.allowEmptyArrays !== "boolean") {
                throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
            }
            if (typeof opts.encodeDotInKeys !== "undefined" && typeof opts.encodeDotInKeys !== "boolean") {
                throw new TypeError("`encodeDotInKeys` option can only be `true` or `false`, when provided");
            }
            if (opts.encoder !== null && typeof opts.encoder !== "undefined" && typeof opts.encoder !== "function") {
                throw new TypeError("Encoder has to be a function.");
            }
            var charset = opts.charset || defaults2.charset;
            if (typeof opts.charset !== "undefined" && opts.charset !== "utf-8" && opts.charset !== "iso-8859-1") {
                throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
            }
            var format = formats["default"];
            if (typeof opts.format !== "undefined") {
                if (!has.call(formats.formatters, opts.format)) {
                    throw new TypeError("Unknown format option provided.");
                }
                format = opts.format;
            }
            var formatter = formats.formatters[format];
            var filter2 = defaults2.filter;
            if (typeof opts.filter === "function" || isArray3(opts.filter)) {
                filter2 = opts.filter;
            }
            var arrayFormat;
            if (opts.arrayFormat in arrayPrefixGenerators) {
                arrayFormat = opts.arrayFormat;
            } else if ("indices" in opts) {
                arrayFormat = opts.indices ? "indices" : "repeat";
            } else {
                arrayFormat = defaults2.arrayFormat;
            }
            if ("commaRoundTrip" in opts && typeof opts.commaRoundTrip !== "boolean") {
                throw new TypeError("`commaRoundTrip` must be a boolean, or absent");
            }
            var allowDots = typeof opts.allowDots === "undefined" ? (opts.encodeDotInKeys === true ? true : defaults2.allowDots) : !!opts.allowDots;
            return {
                addQueryPrefix: typeof opts.addQueryPrefix === "boolean" ? opts.addQueryPrefix : defaults2.addQueryPrefix,
                allowDots,
                allowEmptyArrays: typeof opts.allowEmptyArrays === "boolean" ? !!opts.allowEmptyArrays : defaults2.allowEmptyArrays,
                arrayFormat,
                charset,
                charsetSentinel: typeof opts.charsetSentinel === "boolean" ? opts.charsetSentinel : defaults2.charsetSentinel,
                commaRoundTrip: !!opts.commaRoundTrip,
                delimiter: typeof opts.delimiter === "undefined" ? defaults2.delimiter : opts.delimiter,
                encode: typeof opts.encode === "boolean" ? opts.encode : defaults2.encode,
                encodeDotInKeys: typeof opts.encodeDotInKeys === "boolean" ? opts.encodeDotInKeys : defaults2.encodeDotInKeys,
                encoder: typeof opts.encoder === "function" ? opts.encoder : defaults2.encoder,
                encodeValuesOnly: typeof opts.encodeValuesOnly === "boolean" ? opts.encodeValuesOnly : defaults2.encodeValuesOnly,
                filter: filter2,
                format,
                formatter,
                serializeDate: typeof opts.serializeDate === "function" ? opts.serializeDate : defaults2.serializeDate,
                skipNulls: typeof opts.skipNulls === "boolean" ? opts.skipNulls : defaults2.skipNulls,
                sort: typeof opts.sort === "function" ? opts.sort : null,
                strictNullHandling: typeof opts.strictNullHandling === "boolean" ? opts.strictNullHandling : defaults2.strictNullHandling
            };
        };
        module2.exports = function (object, opts) {
            var obj = object;
            var options = normalizeStringifyOptions(opts);
            var objKeys;
            var filter2;
            if (typeof options.filter === "function") {
                filter2 = options.filter;
                obj = filter2("", obj);
            } else if (isArray3(options.filter)) {
                filter2 = options.filter;
                objKeys = filter2;
            }
            var keys = [];
            if (typeof obj !== "object" || obj === null) {
                return "";
            }
            var generateArrayPrefix = arrayPrefixGenerators[options.arrayFormat];
            var commaRoundTrip = generateArrayPrefix === "comma" && options.commaRoundTrip;
            if (!objKeys) {
                objKeys = Object.keys(obj);
            }
            if (options.sort) {
                objKeys.sort(options.sort);
            }
            var sideChannel = getSideChannel();
            for (var i = 0; i < objKeys.length; ++i) {
                var key = objKeys[i];
                if (typeof key === "undefined" || key === null) {
                    continue;
                }
                var value = obj[key];
                if (options.skipNulls && value === null) {
                    continue;
                }
                pushToArray(
                    keys,
                    stringify(
                        value,
                        key,
                        generateArrayPrefix,
                        commaRoundTrip,
                        options.allowEmptyArrays,
                        options.strictNullHandling,
                        options.skipNulls,
                        options.encodeDotInKeys,
                        options.encode ? options.encoder : null,
                        options.filter,
                        options.sort,
                        options.allowDots,
                        options.serializeDate,
                        options.format,
                        options.formatter,
                        options.encodeValuesOnly,
                        options.charset,
                        sideChannel
                    )
                );
            }
            var joined = keys.join(options.delimiter);
            var prefix = options.addQueryPrefix === true ? "?" : "";
            if (options.charsetSentinel) {
                if (options.charset === "iso-8859-1") {
                    prefix += "utf8=%26%2310003%3B" + options.delimiter;
                } else {
                    prefix += "utf8=%E2%9C%93" + options.delimiter;
                }
            }
            return joined.length > 0 ? prefix + joined : "";
        };
    }
});

// node_modules/.pnpm/qs@6.15.2/node_modules/qs/lib/parse.js
var require_parse = __commonJS({
    "node_modules/.pnpm/qs@6.15.2/node_modules/qs/lib/parse.js"(exports2, module2) {
        "use strict";
        var utils = require_utils();
        var has = Object.prototype.hasOwnProperty;
        var isArray3 = Array.isArray;
        var defaults2 = {
            allowDots: false,
            allowEmptyArrays: false,
            allowPrototypes: false,
            allowSparse: false,
            arrayLimit: 20,
            charset: "utf-8",
            charsetSentinel: false,
            comma: false,
            decodeDotInKeys: false,
            decoder: utils.decode,
            delimiter: "&",
            depth: 5,
            duplicates: "combine",
            ignoreQueryPrefix: false,
            interpretNumericEntities: false,
            parameterLimit: 1e3,
            parseArrays: true,
            plainObjects: false,
            strictDepth: false,
            strictMerge: true,
            strictNullHandling: false,
            throwOnLimitExceeded: false
        };
        var interpretNumericEntities = function (str) {
            return str.replace(/&#(\d+);/g, function ($0, numberStr) {
                return String.fromCharCode(parseInt(numberStr, 10));
            });
        };
        var parseArrayValue = function (val, options, currentArrayLength) {
            if (val && typeof val === "string" && options.comma && val.indexOf(",") > -1) {
                return val.split(",");
            }
            if (options.throwOnLimitExceeded && currentArrayLength >= options.arrayLimit) {
                throw new RangeError("Array limit exceeded. Only " + options.arrayLimit + " element" + (options.arrayLimit === 1 ? "" : "s") + " allowed in an array.");
            }
            return val;
        };
        var isoSentinel = "utf8=%26%2310003%3B";
        var charsetSentinel = "utf8=%E2%9C%93";
        var parseValues = function parseQueryStringValues(str, options) {
            var obj = { __proto__: null };
            var cleanStr = options.ignoreQueryPrefix ? str.replace(/^\?/, "") : str;
            cleanStr = cleanStr.replace(/%5B/gi, "[").replace(/%5D/gi, "]");
            var limit = options.parameterLimit === Infinity ? void 0 : options.parameterLimit;
            var parts = cleanStr.split(options.delimiter, options.throwOnLimitExceeded && typeof limit !== "undefined" ? limit + 1 : limit);
            if (options.throwOnLimitExceeded && typeof limit !== "undefined" && parts.length > limit) {
                throw new RangeError("Parameter limit exceeded. Only " + limit + " parameter" + (limit === 1 ? "" : "s") + " allowed.");
            }
            var skipIndex = -1;
            var i;
            var charset = options.charset;
            if (options.charsetSentinel) {
                for (i = 0; i < parts.length; ++i) {
                    if (parts[i].indexOf("utf8=") === 0) {
                        if (parts[i] === charsetSentinel) {
                            charset = "utf-8";
                        } else if (parts[i] === isoSentinel) {
                            charset = "iso-8859-1";
                        }
                        skipIndex = i;
                        i = parts.length;
                    }
                }
            }
            for (i = 0; i < parts.length; ++i) {
                if (i === skipIndex) {
                    continue;
                }
                var part = parts[i];
                var bracketEqualsPos = part.indexOf("]=");
                var pos = bracketEqualsPos === -1 ? part.indexOf("=") : bracketEqualsPos + 1;
                var key;
                var val;
                if (pos === -1) {
                    key = options.decoder(part, defaults2.decoder, charset, "key");
                    val = options.strictNullHandling ? null : "";
                } else {
                    key = options.decoder(part.slice(0, pos), defaults2.decoder, charset, "key");
                    if (key !== null) {
                        val = utils.maybeMap(parseArrayValue(part.slice(pos + 1), options, isArray3(obj[key]) ? obj[key].length : 0), function (encodedVal) {
                            return options.decoder(encodedVal, defaults2.decoder, charset, "value");
                        });
                    }
                }
                if (val && options.interpretNumericEntities && charset === "iso-8859-1") {
                    val = interpretNumericEntities(String(val));
                }
                if (part.indexOf("[]=") > -1) {
                    val = isArray3(val) ? [val] : val;
                }
                if (options.comma && isArray3(val) && val.length > options.arrayLimit) {
                    if (options.throwOnLimitExceeded) {
                        throw new RangeError("Array limit exceeded. Only " + options.arrayLimit + " element" + (options.arrayLimit === 1 ? "" : "s") + " allowed in an array.");
                    }
                    val = utils.combine([], val, options.arrayLimit, options.plainObjects);
                }
                if (key !== null) {
                    var existing = has.call(obj, key);
                    if (existing && (options.duplicates === "combine" || part.indexOf("[]=") > -1)) {
                        obj[key] = utils.combine(obj[key], val, options.arrayLimit, options.plainObjects);
                    } else if (!existing || options.duplicates === "last") {
                        obj[key] = val;
                    }
                }
            }
            return obj;
        };
        var parseObject = function (chain, val, options, valuesParsed) {
            var currentArrayLength = 0;
            if (chain.length > 0 && chain[chain.length - 1] === "[]") {
                var parentKey = chain.slice(0, -1).join("");
                currentArrayLength = Array.isArray(val) && val[parentKey] ? val[parentKey].length : 0;
            }
            var leaf = valuesParsed ? val : parseArrayValue(val, options, currentArrayLength);
            for (var i = chain.length - 1; i >= 0; --i) {
                var obj;
                var root = chain[i];
                if (root === "[]" && options.parseArrays) {
                    if (utils.isOverflow(leaf)) {
                        obj = leaf;
                    } else {
                        obj = options.allowEmptyArrays && (leaf === "" || (options.strictNullHandling && leaf === null)) ? [] : utils.combine([], leaf, options.arrayLimit, options.plainObjects);
                    }
                } else {
                    obj = options.plainObjects ? { __proto__: null } : {};
                    var cleanRoot = root.charAt(0) === "[" && root.charAt(root.length - 1) === "]" ? root.slice(1, -1) : root;
                    var decodedRoot = options.decodeDotInKeys ? cleanRoot.replace(/%2E/g, ".") : cleanRoot;
                    var index2 = parseInt(decodedRoot, 10);
                    var isValidArrayIndex = !isNaN(index2) && root !== decodedRoot && String(index2) === decodedRoot && index2 >= 0 && options.parseArrays;
                    if (!options.parseArrays && decodedRoot === "") {
                        obj = { 0: leaf };
                    } else if (isValidArrayIndex && index2 < options.arrayLimit) {
                        obj = [];
                        obj[index2] = leaf;
                    } else if (isValidArrayIndex && options.throwOnLimitExceeded) {
                        throw new RangeError("Array limit exceeded. Only " + options.arrayLimit + " element" + (options.arrayLimit === 1 ? "" : "s") + " allowed in an array.");
                    } else if (isValidArrayIndex) {
                        obj[index2] = leaf;
                        utils.markOverflow(obj, index2);
                    } else if (decodedRoot !== "__proto__") {
                        obj[decodedRoot] = leaf;
                    }
                }
                leaf = obj;
            }
            return leaf;
        };
        var splitKeyIntoSegments = function splitKeyIntoSegments2(originalKey, options) {
            var key = options.allowDots ? originalKey.replace(/\.([^.[]+)/g, "[$1]") : originalKey;
            if (options.depth <= 0) {
                if (!options.plainObjects && has.call(Object.prototype, key)) {
                    if (!options.allowPrototypes) {
                        return;
                    }
                }
                return [key];
            }
            var segments = [];
            var first = key.indexOf("[");
            var parent = first >= 0 ? key.slice(0, first) : key;
            if (parent) {
                if (!options.plainObjects && has.call(Object.prototype, parent)) {
                    if (!options.allowPrototypes) {
                        return;
                    }
                }
                segments[segments.length] = parent;
            }
            var n = key.length;
            var open = first;
            var collected = 0;
            while (open >= 0 && collected < options.depth) {
                var level = 1;
                var i = open + 1;
                var close = -1;
                while (i < n && close < 0) {
                    var cu = key.charCodeAt(i);
                    if (cu === 91) {
                        level += 1;
                    } else if (cu === 93) {
                        level -= 1;
                        if (level === 0) {
                            close = i;
                        }
                    }
                    i += 1;
                }
                if (close < 0) {
                    segments[segments.length] = "[" + key.slice(open) + "]";
                    return segments;
                }
                var seg = key.slice(open, close + 1);
                var content = seg.slice(1, -1);
                if (!options.plainObjects && has.call(Object.prototype, content) && !options.allowPrototypes) {
                    return;
                }
                segments[segments.length] = seg;
                collected += 1;
                open = key.indexOf("[", close + 1);
            }
            if (open >= 0) {
                if (options.strictDepth === true) {
                    throw new RangeError("Input depth exceeded depth option of " + options.depth + " and strictDepth is true");
                }
                segments[segments.length] = "[" + key.slice(open) + "]";
            }
            return segments;
        };
        var parseKeys = function parseQueryStringKeys(givenKey, val, options, valuesParsed) {
            if (!givenKey) {
                return;
            }
            var keys = splitKeyIntoSegments(givenKey, options);
            if (!keys) {
                return;
            }
            return parseObject(keys, val, options, valuesParsed);
        };
        var normalizeParseOptions = function normalizeParseOptions2(opts) {
            if (!opts) {
                return defaults2;
            }
            if (typeof opts.allowEmptyArrays !== "undefined" && typeof opts.allowEmptyArrays !== "boolean") {
                throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
            }
            if (typeof opts.decodeDotInKeys !== "undefined" && typeof opts.decodeDotInKeys !== "boolean") {
                throw new TypeError("`decodeDotInKeys` option can only be `true` or `false`, when provided");
            }
            if (opts.decoder !== null && typeof opts.decoder !== "undefined" && typeof opts.decoder !== "function") {
                throw new TypeError("Decoder has to be a function.");
            }
            if (typeof opts.charset !== "undefined" && opts.charset !== "utf-8" && opts.charset !== "iso-8859-1") {
                throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
            }
            if (typeof opts.throwOnLimitExceeded !== "undefined" && typeof opts.throwOnLimitExceeded !== "boolean") {
                throw new TypeError("`throwOnLimitExceeded` option must be a boolean");
            }
            var charset = typeof opts.charset === "undefined" ? defaults2.charset : opts.charset;
            var duplicates = typeof opts.duplicates === "undefined" ? defaults2.duplicates : opts.duplicates;
            if (duplicates !== "combine" && duplicates !== "first" && duplicates !== "last") {
                throw new TypeError("The duplicates option must be either combine, first, or last");
            }
            var allowDots = typeof opts.allowDots === "undefined" ? (opts.decodeDotInKeys === true ? true : defaults2.allowDots) : !!opts.allowDots;
            return {
                allowDots,
                allowEmptyArrays: typeof opts.allowEmptyArrays === "boolean" ? !!opts.allowEmptyArrays : defaults2.allowEmptyArrays,
                allowPrototypes: typeof opts.allowPrototypes === "boolean" ? opts.allowPrototypes : defaults2.allowPrototypes,
                allowSparse: typeof opts.allowSparse === "boolean" ? opts.allowSparse : defaults2.allowSparse,
                arrayLimit: typeof opts.arrayLimit === "number" ? opts.arrayLimit : defaults2.arrayLimit,
                charset,
                charsetSentinel: typeof opts.charsetSentinel === "boolean" ? opts.charsetSentinel : defaults2.charsetSentinel,
                comma: typeof opts.comma === "boolean" ? opts.comma : defaults2.comma,
                decodeDotInKeys: typeof opts.decodeDotInKeys === "boolean" ? opts.decodeDotInKeys : defaults2.decodeDotInKeys,
                decoder: typeof opts.decoder === "function" ? opts.decoder : defaults2.decoder,
                delimiter: typeof opts.delimiter === "string" || utils.isRegExp(opts.delimiter) ? opts.delimiter : defaults2.delimiter,
                // eslint-disable-next-line no-implicit-coercion, no-extra-parens
                depth: typeof opts.depth === "number" || opts.depth === false ? +opts.depth : defaults2.depth,
                duplicates,
                ignoreQueryPrefix: opts.ignoreQueryPrefix === true,
                interpretNumericEntities: typeof opts.interpretNumericEntities === "boolean" ? opts.interpretNumericEntities : defaults2.interpretNumericEntities,
                parameterLimit: typeof opts.parameterLimit === "number" ? opts.parameterLimit : defaults2.parameterLimit,
                parseArrays: opts.parseArrays !== false,
                plainObjects: typeof opts.plainObjects === "boolean" ? opts.plainObjects : defaults2.plainObjects,
                strictDepth: typeof opts.strictDepth === "boolean" ? !!opts.strictDepth : defaults2.strictDepth,
                strictMerge: typeof opts.strictMerge === "boolean" ? !!opts.strictMerge : defaults2.strictMerge,
                strictNullHandling: typeof opts.strictNullHandling === "boolean" ? opts.strictNullHandling : defaults2.strictNullHandling,
                throwOnLimitExceeded: typeof opts.throwOnLimitExceeded === "boolean" ? opts.throwOnLimitExceeded : false
            };
        };
        module2.exports = function (str, opts) {
            var options = normalizeParseOptions(opts);
            if (str === "" || str === null || typeof str === "undefined") {
                return options.plainObjects ? { __proto__: null } : {};
            }
            var tempObj = typeof str === "string" ? parseValues(str, options) : str;
            var obj = options.plainObjects ? { __proto__: null } : {};
            var keys = Object.keys(tempObj);
            for (var i = 0; i < keys.length; ++i) {
                var key = keys[i];
                var newObj = parseKeys(key, tempObj[key], options, typeof str === "string");
                obj = utils.merge(obj, newObj, options);
            }
            if (options.allowSparse === true) {
                return obj;
            }
            return utils.compact(obj);
        };
    }
});

// node_modules/.pnpm/qs@6.15.2/node_modules/qs/lib/index.js
var require_lib = __commonJS({
    "node_modules/.pnpm/qs@6.15.2/node_modules/qs/lib/index.js"(exports2, module2) {
        "use strict";
        var stringify = require_stringify();
        var parse = require_parse();
        var formats = require_formats();
        module2.exports = {
            formats,
            parse,
            stringify
        };
    }
});

// node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_overArg.js
var require_overArg = __commonJS({
    "node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_overArg.js"(exports2, module2) {
        function overArg(func, transform) {
            return function (arg) {
                return func(transform(arg));
            };
        }
        module2.exports = overArg;
    }
});

// node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getPrototype.js
var require_getPrototype = __commonJS({
    "node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/_getPrototype.js"(exports2, module2) {
        var overArg = require_overArg();
        var getPrototype = overArg(Object.getPrototypeOf, Object);
        module2.exports = getPrototype;
    }
});

// node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isPlainObject.js
var require_isPlainObject = __commonJS({
    "node_modules/.pnpm/lodash@4.18.1/node_modules/lodash/isPlainObject.js"(exports2, module2) {
        var baseGetTag = require_baseGetTag();
        var getPrototype = require_getPrototype();
        var isObjectLike = require_isObjectLike();
        var objectTag = "[object Object]";
        var funcProto = Function.prototype;
        var objectProto = Object.prototype;
        var funcToString = funcProto.toString;
        var hasOwnProperty3 = objectProto.hasOwnProperty;
        var objectCtorString = funcToString.call(Object);
        function isPlainObject2(value) {
            if (!isObjectLike(value) || baseGetTag(value) != objectTag) {
                return false;
            }
            var proto = getPrototype(value);
            if (proto === null) {
                return true;
            }
            var Ctor = hasOwnProperty3.call(proto, "constructor") && proto.constructor;
            return typeof Ctor == "function" && Ctor instanceof Ctor && funcToString.call(Ctor) == objectCtorString;
        }
        module2.exports = isPlainObject2;
    }
});

// node_modules/.pnpm/contentful-sdk-core@9.4.5/node_modules/contentful-sdk-core/dist/cjs/index.cjs
var require_cjs2 = __commonJS({
    "node_modules/.pnpm/contentful-sdk-core@9.4.5/node_modules/contentful-sdk-core/dist/cjs/index.cjs"(exports2) {
        "use strict";
        var copy = require_cjs();
        var process2 = require("process");
        var isString2 = require_isString();
        var qs = require_lib();
        var isPlainObject2 = require_isPlainObject();
        function _interopDefault(e) {
            return e && e.__esModule ? e : { default: e };
        }
        var copy__default = /* @__PURE__ */ _interopDefault(copy);
        var process__default = /* @__PURE__ */ _interopDefault(process2);
        var isString__default = /* @__PURE__ */ _interopDefault(isString2);
        var qs__default = /* @__PURE__ */ _interopDefault(qs);
        var isPlainObject__default = /* @__PURE__ */ _interopDefault(isPlainObject2);
        function asyncToken(instance, getToken) {
            instance.interceptors.request.use(function (config) {
                return getToken().then(accessToken => {
                    config.headers.set("Authorization", `Bearer ${accessToken}`);
                    return config;
                });
            });
        }
        function isNode() {
            return typeof process__default.default !== "undefined" && !process__default.default.browser;
        }
        function isReactNative2() {
            return typeof window !== "undefined" && "navigator" in window && "product" in window.navigator && window.navigator.product === "ReactNative";
        }
        function getNodeVersion() {
            return process__default.default.versions && process__default.default.versions.node ? `v${process__default.default.versions.node}` : process__default.default.version;
        }
        function getWindow() {
            return window;
        }
        function noop2() {
            return void 0;
        }
        var delay = ms =>
            new Promise(resolve => {
                setTimeout(resolve, ms);
            });
        var defaultWait = attempts => {
            return Math.pow(Math.SQRT2, attempts);
        };
        function rateLimit(instance, maxRetry = 5) {
            const { responseLogger = noop2, requestLogger = noop2 } = instance.defaults;
            instance.interceptors.request.use(
                function (config) {
                    requestLogger(config);
                    return config;
                },
                function (error) {
                    requestLogger(error);
                    return Promise.reject(error);
                }
            );
            instance.interceptors.response.use(
                function (response) {
                    responseLogger(response);
                    return response;
                },
                async function (error) {
                    const { response } = error;
                    const { config } = error;
                    responseLogger(error);
                    if (!config || !instance.defaults.retryOnError) {
                        return Promise.reject(error);
                    }
                    const doneAttempts = config.attempts || 1;
                    if (doneAttempts > maxRetry) {
                        error.attempts = config.attempts;
                        return Promise.reject(error);
                    }
                    let retryErrorType = null;
                    let wait = defaultWait(doneAttempts);
                    if (!response) {
                        retryErrorType = "Connection";
                    } else if (response.status >= 500 && response.status < 600) {
                        retryErrorType = `Server ${response.status}`;
                    } else if (response.status === 429) {
                        retryErrorType = "Rate limit";
                        if (response.headers && error.response.headers["x-contentful-ratelimit-reset"]) {
                            wait = response.headers["x-contentful-ratelimit-reset"];
                        }
                    }
                    if (retryErrorType) {
                        wait = Math.floor(wait * 1e3 + Math.random() * 200 + 500);
                        instance.defaults.logHandler("warning", `${retryErrorType} error occurred. Waiting for ${wait} ms before retrying...`);
                        config.attempts = doneAttempts + 1;
                        delete config.httpAgent;
                        delete config.httpsAgent;
                        return delay(wait).then(() => instance(config));
                    }
                    return Promise.reject(error);
                }
            );
        }
        var AbortError = class extends Error {
            name = "AbortError";
            constructor() {
                super("Throttled function aborted");
            }
        };
        function pThrottle({ limit, interval, strict, onDelay }) {
            if (!Number.isFinite(limit)) {
                throw new TypeError("Expected `limit` to be a finite number");
            }
            if (!Number.isFinite(interval)) {
                throw new TypeError("Expected `interval` to be a finite number");
            }
            const queue = /* @__PURE__ */ new Map();
            let currentTick = 0;
            let activeCount = 0;
            function windowedDelay() {
                const now = Date.now();
                if (now - currentTick > interval) {
                    activeCount = 1;
                    currentTick = now;
                    return 0;
                }
                if (activeCount < limit) {
                    activeCount++;
                } else {
                    currentTick += interval;
                    activeCount = 1;
                }
                return currentTick - now;
            }
            const getDelay = windowedDelay;
            return function (function_) {
                const throttled = function (...arguments_) {
                    if (!throttled.isEnabled) {
                        return (async () => function_.apply(this, arguments_))();
                    }
                    let timeoutId;
                    return new Promise((resolve, reject) => {
                        const execute = () => {
                            resolve(function_.apply(this, arguments_));
                            queue.delete(timeoutId);
                        };
                        const delay2 = getDelay();
                        if (delay2 > 0) {
                            timeoutId = setTimeout(execute, delay2);
                            queue.set(timeoutId, reject);
                            onDelay?.();
                        } else {
                            execute();
                        }
                    });
                };
                throttled.abort = () => {
                    for (const timeout of queue.keys()) {
                        clearTimeout(timeout);
                        queue.get(timeout)(new AbortError());
                    }
                    queue.clear();
                };
                throttled.isEnabled = true;
                Object.defineProperty(throttled, "queueSize", {
                    get() {
                        return queue.size;
                    }
                });
                return throttled;
            };
        }
        var PERCENTAGE_REGEX = /(?<value>\d+)(%)/;
        function calculateLimit(type, max = 7) {
            let limit = max;
            if (PERCENTAGE_REGEX.test(type)) {
                const groups = type.match(PERCENTAGE_REGEX)?.groups;
                if (groups && groups.value) {
                    const percentage = parseInt(groups.value) / 100;
                    limit = Math.round(max * percentage);
                }
            }
            return Math.min(30, Math.max(1, limit));
        }
        function createThrottle(limit, logger) {
            logger("info", `Throttle request to ${limit}/s`);
            return pThrottle({
                limit,
                interval: 1e3,
                strict: false
            });
        }
        var rateLimitThrottle = (axiosInstance, type = "auto") => {
            const { logHandler = noop2 } = axiosInstance.defaults;
            let limit = isString__default.default(type) ? calculateLimit(type) : calculateLimit("auto", type);
            let throttle2 = createThrottle(limit, logHandler);
            let isCalculated = false;
            let requestInterceptorId = axiosInstance.interceptors.request.use(
                config => {
                    return throttle2(() => config)();
                },
                function (error) {
                    return Promise.reject(error);
                }
            );
            const responseInterceptorId = axiosInstance.interceptors.response.use(
                response => {
                    if (!isCalculated && isString__default.default(type) && (type === "auto" || PERCENTAGE_REGEX.test(type)) && response.headers && response.headers["x-contentful-ratelimit-second-limit"]) {
                        const rawLimit = parseInt(response.headers["x-contentful-ratelimit-second-limit"]);
                        const nextLimit = calculateLimit(type, rawLimit);
                        if (nextLimit !== limit) {
                            if (requestInterceptorId) {
                                axiosInstance.interceptors.request.eject(requestInterceptorId);
                            }
                            limit = nextLimit;
                            throttle2 = createThrottle(nextLimit, logHandler);
                            requestInterceptorId = axiosInstance.interceptors.request.use(
                                config => {
                                    return throttle2(() => config)();
                                },
                                function (error) {
                                    return Promise.reject(error);
                                }
                            );
                        }
                        isCalculated = true;
                    }
                    return response;
                },
                function (error) {
                    return Promise.reject(error);
                }
            );
            return () => {
                axiosInstance.interceptors.request.eject(requestInterceptorId);
                axiosInstance.interceptors.response.eject(responseInterceptorId);
            };
        };
        var HOST_REGEX = /^(?!\w+:\/\/)([^\s:]+\.?[^\s:]+)(?::(\d+))?(?!:)$/;
        function createDefaultOptions(options) {
            const defaultConfig = {
                insecure: false,
                retryOnError: true,
                // eslint-disable-next-line @typescript-eslint/no-explicit-any
                logHandler: (level, data) => {
                    if (level === "error" && data) {
                        const title = [data.name, data.message].filter(a => a).join(" - ");
                        console.error(`[error] ${title}`);
                        console.error(data);
                        return;
                    }
                    console.log(`[${level}] ${data}`);
                },
                // Passed to axios
                headers: {},
                httpAgent: false,
                httpsAgent: false,
                timeout: 3e4,
                throttle: 0,
                basePath: "",
                adapter: void 0,
                maxContentLength: 1073741824,
                // 1GB
                maxBodyLength: 1073741824
                // 1GB
            };
            const config = {
                ...defaultConfig,
                ...options
            };
            if (!config.accessToken) {
                const missingAccessTokenError = new TypeError("Expected parameter accessToken");
                config.logHandler("error", missingAccessTokenError);
                throw missingAccessTokenError;
            }
            const protocol = config.insecure ? "http" : "https";
            const space = config.space ? `${config.space}/` : "";
            let hostname = config.defaultHostname;
            let port = config.insecure ? 80 : 443;
            if (config.host && HOST_REGEX.test(config.host)) {
                const parsed = config.host.split(":");
                if (parsed.length === 2) {
                    [hostname, port] = parsed;
                } else {
                    hostname = parsed[0];
                }
            }
            if (config.basePath) {
                config.basePath = `/${config.basePath.split("/").filter(Boolean).join("/")}`;
            }
            const baseURL2 = options.baseURL || `${protocol}://${hostname}:${port}${config.basePath}/spaces/${space}`;
            if (!config.headers.Authorization && typeof config.accessToken !== "function") {
                config.headers.Authorization = "Bearer " + config.accessToken;
            }
            const axiosOptions = {
                // Axios
                baseURL: baseURL2,
                headers: config.headers,
                httpAgent: config.httpAgent,
                httpsAgent: config.httpsAgent,
                proxy: config.proxy,
                timeout: config.timeout,
                adapter: config.adapter,
                fetchOptions: config.fetchOptions,
                maxContentLength: config.maxContentLength,
                maxBodyLength: config.maxBodyLength,
                paramsSerializer: {
                    serialize: params => {
                        return qs__default.default.stringify(params);
                    }
                },
                // Contentful
                logHandler: config.logHandler,
                responseLogger: config.responseLogger,
                requestLogger: config.requestLogger,
                retryOnError: config.retryOnError
            };
            return axiosOptions;
        }
        function copyHttpClientParams(options) {
            const copiedOptions = copy__default.default(options);
            copiedOptions.httpAgent = options.httpAgent;
            copiedOptions.httpsAgent = options.httpsAgent;
            return copiedOptions;
        }
        function createHttpClient2(axios2, options) {
            const axiosOptions = createDefaultOptions(options);
            const instance = axios2.create(axiosOptions);
            instance.httpClientParams = options;
            instance.cloneWithNewParams = function (newParams) {
                return createHttpClient2(axios2, {
                    ...copyHttpClientParams(options),
                    ...newParams
                });
            };
            if (options.onBeforeRequest) {
                instance.interceptors.request.use(options.onBeforeRequest);
            }
            if (typeof options.accessToken === "function") {
                asyncToken(instance, options.accessToken);
            }
            if (options.throttle) {
                rateLimitThrottle(instance, options.throttle);
            }
            rateLimit(instance, options.retryLimit);
            if (options.onError) {
                instance.interceptors.response.use(response => response, options.onError);
            }
            return instance;
        }
        function createRequestConfig6({ query: query2 }) {
            const config = {};
            delete query2.resolveLinks;
            config.params = copy__default.default(query2);
            return config;
        }
        function enforceObjPath(obj, path) {
            if (!(path in obj)) {
                const err = new Error();
                err.name = "PropertyMissing";
                err.message = `Required property ${path} missing from:

${JSON.stringify(obj)}

`;
                throw err;
            }
            return true;
        }
        function deepFreeze(object) {
            const propNames = Object.getOwnPropertyNames(object);
            for (const name of propNames) {
                const value = object[name];
                if (value && typeof value === "object") {
                    deepFreeze(value);
                }
            }
            return Object.freeze(object);
        }
        function freezeSys62(obj) {
            deepFreeze(obj.sys || {});
            return obj;
        }
        function getBrowserOS() {
            const win = getWindow();
            if (!win) {
                return null;
            }
            const userAgent = win.navigator.userAgent;
            const platform = win.navigator.platform;
            const macosPlatforms = ["Macintosh", "MacIntel", "MacPPC", "Mac68K"];
            const windowsPlatforms = ["Win32", "Win64", "Windows", "WinCE"];
            const iosPlatforms = ["iPhone", "iPad", "iPod"];
            if (macosPlatforms.indexOf(platform) !== -1) {
                return "macOS";
            } else if (iosPlatforms.indexOf(platform) !== -1) {
                return "iOS";
            } else if (windowsPlatforms.indexOf(platform) !== -1) {
                return "Windows";
            } else if (/Android/.test(userAgent)) {
                return "Android";
            } else if (/Linux/.test(platform)) {
                return "Linux";
            }
            return null;
        }
        function getNodeOS() {
            const platform = process__default.default.platform || "linux";
            const version = process__default.default.version || "0.0.0";
            const platformMap = {
                android: "Android",
                aix: "Linux",
                darwin: "macOS",
                freebsd: "Linux",
                linux: "Linux",
                openbsd: "Linux",
                sunos: "Linux",
                win32: "Windows"
            };
            if (platform in platformMap) {
                return `${platformMap[platform] || "Linux"}/${version}`;
            }
            return null;
        }
        function getUserAgentHeader2(sdk, application, integration, feature) {
            const headerParts = [];
            if (application) {
                headerParts.push(`app ${application}`);
            }
            if (integration) {
                headerParts.push(`integration ${integration}`);
            }
            if (feature) {
                headerParts.push("feature " + feature);
            }
            headerParts.push(`sdk ${sdk}`);
            let platform = null;
            try {
                if (isReactNative2()) {
                    platform = getBrowserOS();
                    headerParts.push("platform ReactNative");
                } else if (isNode()) {
                    platform = getNodeOS();
                    headerParts.push(`platform node.js/${getNodeVersion()}`);
                } else {
                    platform = getBrowserOS();
                    headerParts.push("platform browser");
                }
            } catch (e) {
                platform = null;
            }
            if (platform) {
                headerParts.push(`os ${platform}`);
            }
            return `${headerParts.filter(item => item !== "").join("; ")};`;
        }
        function toPlainObject72(data) {
            return Object.defineProperty(data, "toPlainObject", {
                enumerable: false,
                configurable: false,
                writable: false,
                value: function () {
                    return copy__default.default(this);
                }
            });
        }
        function obscureHeaders(config) {
            if (config?.headers?.["Authorization"]) {
                const token = `...${config.headers["Authorization"].toString().substr(-5)}`;
                config.headers["Authorization"] = `Bearer ${token}`;
            }
            if (config?.headers?.["X-Contentful-Resource-Resolution"]) {
                const token = `...${config.headers["X-Contentful-Resource-Resolution"].toString().substr(-5)}`;
                config.headers["X-Contentful-Resource-Resolution"] = token;
            }
        }
        function errorHandler4(errorResponse) {
            const { config, response } = errorResponse;
            let errorName;
            obscureHeaders(config);
            if (!isPlainObject__default.default(response) || !isPlainObject__default.default(config)) {
                throw errorResponse;
            }
            const data = response?.data;
            const errorData = {
                status: response?.status,
                statusText: response?.statusText,
                message: "",
                details: {}
            };
            if (config && isPlainObject__default.default(config)) {
                errorData.request = {
                    url: config.url,
                    headers: config.headers,
                    method: config.method,
                    payloadData: config.data
                };
            }
            if (data && typeof data === "object") {
                if ("requestId" in data) {
                    errorData.requestId = data.requestId || "UNKNOWN";
                }
                if ("message" in data) {
                    errorData.message = data.message || "";
                }
                if ("details" in data) {
                    errorData.details = data.details || {};
                }
                errorName = data.sys?.id;
            }
            const error = new Error();
            error.name = errorName && errorName !== "Unknown" ? errorName : `${response?.status} ${response?.statusText}`;
            try {
                error.message = JSON.stringify(errorData, null, "  ");
            } catch {
                error.message = errorData?.message ?? "";
            }
            throw error;
        }
        exports2.createDefaultOptions = createDefaultOptions;
        exports2.createHttpClient = createHttpClient2;
        exports2.createRequestConfig = createRequestConfig6;
        exports2.enforceObjPath = enforceObjPath;
        exports2.errorHandler = errorHandler4;
        exports2.freezeSys = freezeSys62;
        exports2.getUserAgentHeader = getUserAgentHeader2;
        exports2.toPlainObject = toPlainObject72;
    }
});

// node_modules/.pnpm/delayed-stream@1.0.0/node_modules/delayed-stream/lib/delayed_stream.js
var require_delayed_stream = __commonJS({
    "node_modules/.pnpm/delayed-stream@1.0.0/node_modules/delayed-stream/lib/delayed_stream.js"(exports2, module2) {
        var Stream = require("stream").Stream;
        var util4 = require("util");
        module2.exports = DelayedStream;
        function DelayedStream() {
            this.source = null;
            this.dataSize = 0;
            this.maxDataSize = 1024 * 1024;
            this.pauseStream = true;
            this._maxDataSizeExceeded = false;
            this._released = false;
            this._bufferedEvents = [];
        }
        util4.inherits(DelayedStream, Stream);
        DelayedStream.create = function (source, options) {
            var delayedStream = new this();
            options = options || {};
            for (var option in options) {
                delayedStream[option] = options[option];
            }
            delayedStream.source = source;
            var realEmit = source.emit;
            source.emit = function () {
                delayedStream._handleEmit(arguments);
                return realEmit.apply(source, arguments);
            };
            source.on("error", function () {});
            if (delayedStream.pauseStream) {
                source.pause();
            }
            return delayedStream;
        };
        Object.defineProperty(DelayedStream.prototype, "readable", {
            configurable: true,
            enumerable: true,
            get: function () {
                return this.source.readable;
            }
        });
        DelayedStream.prototype.setEncoding = function () {
            return this.source.setEncoding.apply(this.source, arguments);
        };
        DelayedStream.prototype.resume = function () {
            if (!this._released) {
                this.release();
            }
            this.source.resume();
        };
        DelayedStream.prototype.pause = function () {
            this.source.pause();
        };
        DelayedStream.prototype.release = function () {
            this._released = true;
            this._bufferedEvents.forEach(
                function (args) {
                    this.emit.apply(this, args);
                }.bind(this)
            );
            this._bufferedEvents = [];
        };
        DelayedStream.prototype.pipe = function () {
            var r = Stream.prototype.pipe.apply(this, arguments);
            this.resume();
            return r;
        };
        DelayedStream.prototype._handleEmit = function (args) {
            if (this._released) {
                this.emit.apply(this, args);
                return;
            }
            if (args[0] === "data") {
                this.dataSize += args[1].length;
                this._checkIfMaxDataSizeExceeded();
            }
            this._bufferedEvents.push(args);
        };
        DelayedStream.prototype._checkIfMaxDataSizeExceeded = function () {
            if (this._maxDataSizeExceeded) {
                return;
            }
            if (this.dataSize <= this.maxDataSize) {
                return;
            }
            this._maxDataSizeExceeded = true;
            var message = "DelayedStream#maxDataSize of " + this.maxDataSize + " bytes exceeded.";
            this.emit("error", new Error(message));
        };
    }
});

// node_modules/.pnpm/combined-stream@1.0.8/node_modules/combined-stream/lib/combined_stream.js
var require_combined_stream = __commonJS({
    "node_modules/.pnpm/combined-stream@1.0.8/node_modules/combined-stream/lib/combined_stream.js"(exports2, module2) {
        var util4 = require("util");
        var Stream = require("stream").Stream;
        var DelayedStream = require_delayed_stream();
        module2.exports = CombinedStream;
        function CombinedStream() {
            this.writable = false;
            this.readable = true;
            this.dataSize = 0;
            this.maxDataSize = 2 * 1024 * 1024;
            this.pauseStreams = true;
            this._released = false;
            this._streams = [];
            this._currentStream = null;
            this._insideLoop = false;
            this._pendingNext = false;
        }
        util4.inherits(CombinedStream, Stream);
        CombinedStream.create = function (options) {
            var combinedStream = new this();
            options = options || {};
            for (var option in options) {
                combinedStream[option] = options[option];
            }
            return combinedStream;
        };
        CombinedStream.isStreamLike = function (stream4) {
            return typeof stream4 !== "function" && typeof stream4 !== "string" && typeof stream4 !== "boolean" && typeof stream4 !== "number" && !Buffer.isBuffer(stream4);
        };
        CombinedStream.prototype.append = function (stream4) {
            var isStreamLike = CombinedStream.isStreamLike(stream4);
            if (isStreamLike) {
                if (!(stream4 instanceof DelayedStream)) {
                    var newStream = DelayedStream.create(stream4, {
                        maxDataSize: Infinity,
                        pauseStream: this.pauseStreams
                    });
                    stream4.on("data", this._checkDataSize.bind(this));
                    stream4 = newStream;
                }
                this._handleErrors(stream4);
                if (this.pauseStreams) {
                    stream4.pause();
                }
            }
            this._streams.push(stream4);
            return this;
        };
        CombinedStream.prototype.pipe = function (dest, options) {
            Stream.prototype.pipe.call(this, dest, options);
            this.resume();
            return dest;
        };
        CombinedStream.prototype._getNext = function () {
            this._currentStream = null;
            if (this._insideLoop) {
                this._pendingNext = true;
                return;
            }
            this._insideLoop = true;
            try {
                do {
                    this._pendingNext = false;
                    this._realGetNext();
                } while (this._pendingNext);
            } finally {
                this._insideLoop = false;
            }
        };
        CombinedStream.prototype._realGetNext = function () {
            var stream4 = this._streams.shift();
            if (typeof stream4 == "undefined") {
                this.end();
                return;
            }
            if (typeof stream4 !== "function") {
                this._pipeNext(stream4);
                return;
            }
            var getStream = stream4;
            getStream(
                function (stream5) {
                    var isStreamLike = CombinedStream.isStreamLike(stream5);
                    if (isStreamLike) {
                        stream5.on("data", this._checkDataSize.bind(this));
                        this._handleErrors(stream5);
                    }
                    this._pipeNext(stream5);
                }.bind(this)
            );
        };
        CombinedStream.prototype._pipeNext = function (stream4) {
            this._currentStream = stream4;
            var isStreamLike = CombinedStream.isStreamLike(stream4);
            if (isStreamLike) {
                stream4.on("end", this._getNext.bind(this));
                stream4.pipe(this, { end: false });
                return;
            }
            var value = stream4;
            this.write(value);
            this._getNext();
        };
        CombinedStream.prototype._handleErrors = function (stream4) {
            var self2 = this;
            stream4.on("error", function (err) {
                self2._emitError(err);
            });
        };
        CombinedStream.prototype.write = function (data) {
            this.emit("data", data);
        };
        CombinedStream.prototype.pause = function () {
            if (!this.pauseStreams) {
                return;
            }
            if (this.pauseStreams && this._currentStream && typeof this._currentStream.pause == "function") this._currentStream.pause();
            this.emit("pause");
        };
        CombinedStream.prototype.resume = function () {
            if (!this._released) {
                this._released = true;
                this.writable = true;
                this._getNext();
            }
            if (this.pauseStreams && this._currentStream && typeof this._currentStream.resume == "function") this._currentStream.resume();
            this.emit("resume");
        };
        CombinedStream.prototype.end = function () {
            this._reset();
            this.emit("end");
        };
        CombinedStream.prototype.destroy = function () {
            this._reset();
            this.emit("close");
        };
        CombinedStream.prototype._reset = function () {
            this.writable = false;
            this._streams = [];
            this._currentStream = null;
        };
        CombinedStream.prototype._checkDataSize = function () {
            this._updateDataSize();
            if (this.dataSize <= this.maxDataSize) {
                return;
            }
            var message = "DelayedStream#maxDataSize of " + this.maxDataSize + " bytes exceeded.";
            this._emitError(new Error(message));
        };
        CombinedStream.prototype._updateDataSize = function () {
            this.dataSize = 0;
            var self2 = this;
            this._streams.forEach(function (stream4) {
                if (!stream4.dataSize) {
                    return;
                }
                self2.dataSize += stream4.dataSize;
            });
            if (this._currentStream && this._currentStream.dataSize) {
                this.dataSize += this._currentStream.dataSize;
            }
        };
        CombinedStream.prototype._emitError = function (err) {
            this._reset();
            this.emit("error", err);
        };
    }
});

// node_modules/.pnpm/mime-db@1.52.0/node_modules/mime-db/db.json
var require_db = __commonJS({
    "node_modules/.pnpm/mime-db@1.52.0/node_modules/mime-db/db.json"(exports2, module2) {
        module2.exports = {
            "application/1d-interleaved-parityfec": {
                source: "iana"
            },
            "application/3gpdash-qoe-report+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/3gpp-ims+xml": {
                source: "iana",
                compressible: true
            },
            "application/3gpphal+json": {
                source: "iana",
                compressible: true
            },
            "application/3gpphalforms+json": {
                source: "iana",
                compressible: true
            },
            "application/a2l": {
                source: "iana"
            },
            "application/ace+cbor": {
                source: "iana"
            },
            "application/activemessage": {
                source: "iana"
            },
            "application/activity+json": {
                source: "iana",
                compressible: true
            },
            "application/alto-costmap+json": {
                source: "iana",
                compressible: true
            },
            "application/alto-costmapfilter+json": {
                source: "iana",
                compressible: true
            },
            "application/alto-directory+json": {
                source: "iana",
                compressible: true
            },
            "application/alto-endpointcost+json": {
                source: "iana",
                compressible: true
            },
            "application/alto-endpointcostparams+json": {
                source: "iana",
                compressible: true
            },
            "application/alto-endpointprop+json": {
                source: "iana",
                compressible: true
            },
            "application/alto-endpointpropparams+json": {
                source: "iana",
                compressible: true
            },
            "application/alto-error+json": {
                source: "iana",
                compressible: true
            },
            "application/alto-networkmap+json": {
                source: "iana",
                compressible: true
            },
            "application/alto-networkmapfilter+json": {
                source: "iana",
                compressible: true
            },
            "application/alto-updatestreamcontrol+json": {
                source: "iana",
                compressible: true
            },
            "application/alto-updatestreamparams+json": {
                source: "iana",
                compressible: true
            },
            "application/aml": {
                source: "iana"
            },
            "application/andrew-inset": {
                source: "iana",
                extensions: ["ez"]
            },
            "application/applefile": {
                source: "iana"
            },
            "application/applixware": {
                source: "apache",
                extensions: ["aw"]
            },
            "application/at+jwt": {
                source: "iana"
            },
            "application/atf": {
                source: "iana"
            },
            "application/atfx": {
                source: "iana"
            },
            "application/atom+xml": {
                source: "iana",
                compressible: true,
                extensions: ["atom"]
            },
            "application/atomcat+xml": {
                source: "iana",
                compressible: true,
                extensions: ["atomcat"]
            },
            "application/atomdeleted+xml": {
                source: "iana",
                compressible: true,
                extensions: ["atomdeleted"]
            },
            "application/atomicmail": {
                source: "iana"
            },
            "application/atomsvc+xml": {
                source: "iana",
                compressible: true,
                extensions: ["atomsvc"]
            },
            "application/atsc-dwd+xml": {
                source: "iana",
                compressible: true,
                extensions: ["dwd"]
            },
            "application/atsc-dynamic-event-message": {
                source: "iana"
            },
            "application/atsc-held+xml": {
                source: "iana",
                compressible: true,
                extensions: ["held"]
            },
            "application/atsc-rdt+json": {
                source: "iana",
                compressible: true
            },
            "application/atsc-rsat+xml": {
                source: "iana",
                compressible: true,
                extensions: ["rsat"]
            },
            "application/atxml": {
                source: "iana"
            },
            "application/auth-policy+xml": {
                source: "iana",
                compressible: true
            },
            "application/bacnet-xdd+zip": {
                source: "iana",
                compressible: false
            },
            "application/batch-smtp": {
                source: "iana"
            },
            "application/bdoc": {
                compressible: false,
                extensions: ["bdoc"]
            },
            "application/beep+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/calendar+json": {
                source: "iana",
                compressible: true
            },
            "application/calendar+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xcs"]
            },
            "application/call-completion": {
                source: "iana"
            },
            "application/cals-1840": {
                source: "iana"
            },
            "application/captive+json": {
                source: "iana",
                compressible: true
            },
            "application/cbor": {
                source: "iana"
            },
            "application/cbor-seq": {
                source: "iana"
            },
            "application/cccex": {
                source: "iana"
            },
            "application/ccmp+xml": {
                source: "iana",
                compressible: true
            },
            "application/ccxml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["ccxml"]
            },
            "application/cdfx+xml": {
                source: "iana",
                compressible: true,
                extensions: ["cdfx"]
            },
            "application/cdmi-capability": {
                source: "iana",
                extensions: ["cdmia"]
            },
            "application/cdmi-container": {
                source: "iana",
                extensions: ["cdmic"]
            },
            "application/cdmi-domain": {
                source: "iana",
                extensions: ["cdmid"]
            },
            "application/cdmi-object": {
                source: "iana",
                extensions: ["cdmio"]
            },
            "application/cdmi-queue": {
                source: "iana",
                extensions: ["cdmiq"]
            },
            "application/cdni": {
                source: "iana"
            },
            "application/cea": {
                source: "iana"
            },
            "application/cea-2018+xml": {
                source: "iana",
                compressible: true
            },
            "application/cellml+xml": {
                source: "iana",
                compressible: true
            },
            "application/cfw": {
                source: "iana"
            },
            "application/city+json": {
                source: "iana",
                compressible: true
            },
            "application/clr": {
                source: "iana"
            },
            "application/clue+xml": {
                source: "iana",
                compressible: true
            },
            "application/clue_info+xml": {
                source: "iana",
                compressible: true
            },
            "application/cms": {
                source: "iana"
            },
            "application/cnrp+xml": {
                source: "iana",
                compressible: true
            },
            "application/coap-group+json": {
                source: "iana",
                compressible: true
            },
            "application/coap-payload": {
                source: "iana"
            },
            "application/commonground": {
                source: "iana"
            },
            "application/conference-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/cose": {
                source: "iana"
            },
            "application/cose-key": {
                source: "iana"
            },
            "application/cose-key-set": {
                source: "iana"
            },
            "application/cpl+xml": {
                source: "iana",
                compressible: true,
                extensions: ["cpl"]
            },
            "application/csrattrs": {
                source: "iana"
            },
            "application/csta+xml": {
                source: "iana",
                compressible: true
            },
            "application/cstadata+xml": {
                source: "iana",
                compressible: true
            },
            "application/csvm+json": {
                source: "iana",
                compressible: true
            },
            "application/cu-seeme": {
                source: "apache",
                extensions: ["cu"]
            },
            "application/cwt": {
                source: "iana"
            },
            "application/cybercash": {
                source: "iana"
            },
            "application/dart": {
                compressible: true
            },
            "application/dash+xml": {
                source: "iana",
                compressible: true,
                extensions: ["mpd"]
            },
            "application/dash-patch+xml": {
                source: "iana",
                compressible: true,
                extensions: ["mpp"]
            },
            "application/dashdelta": {
                source: "iana"
            },
            "application/davmount+xml": {
                source: "iana",
                compressible: true,
                extensions: ["davmount"]
            },
            "application/dca-rft": {
                source: "iana"
            },
            "application/dcd": {
                source: "iana"
            },
            "application/dec-dx": {
                source: "iana"
            },
            "application/dialog-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/dicom": {
                source: "iana"
            },
            "application/dicom+json": {
                source: "iana",
                compressible: true
            },
            "application/dicom+xml": {
                source: "iana",
                compressible: true
            },
            "application/dii": {
                source: "iana"
            },
            "application/dit": {
                source: "iana"
            },
            "application/dns": {
                source: "iana"
            },
            "application/dns+json": {
                source: "iana",
                compressible: true
            },
            "application/dns-message": {
                source: "iana"
            },
            "application/docbook+xml": {
                source: "apache",
                compressible: true,
                extensions: ["dbk"]
            },
            "application/dots+cbor": {
                source: "iana"
            },
            "application/dskpp+xml": {
                source: "iana",
                compressible: true
            },
            "application/dssc+der": {
                source: "iana",
                extensions: ["dssc"]
            },
            "application/dssc+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xdssc"]
            },
            "application/dvcs": {
                source: "iana"
            },
            "application/ecmascript": {
                source: "iana",
                compressible: true,
                extensions: ["es", "ecma"]
            },
            "application/edi-consent": {
                source: "iana"
            },
            "application/edi-x12": {
                source: "iana",
                compressible: false
            },
            "application/edifact": {
                source: "iana",
                compressible: false
            },
            "application/efi": {
                source: "iana"
            },
            "application/elm+json": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/elm+xml": {
                source: "iana",
                compressible: true
            },
            "application/emergencycalldata.cap+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/emergencycalldata.comment+xml": {
                source: "iana",
                compressible: true
            },
            "application/emergencycalldata.control+xml": {
                source: "iana",
                compressible: true
            },
            "application/emergencycalldata.deviceinfo+xml": {
                source: "iana",
                compressible: true
            },
            "application/emergencycalldata.ecall.msd": {
                source: "iana"
            },
            "application/emergencycalldata.providerinfo+xml": {
                source: "iana",
                compressible: true
            },
            "application/emergencycalldata.serviceinfo+xml": {
                source: "iana",
                compressible: true
            },
            "application/emergencycalldata.subscriberinfo+xml": {
                source: "iana",
                compressible: true
            },
            "application/emergencycalldata.veds+xml": {
                source: "iana",
                compressible: true
            },
            "application/emma+xml": {
                source: "iana",
                compressible: true,
                extensions: ["emma"]
            },
            "application/emotionml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["emotionml"]
            },
            "application/encaprtp": {
                source: "iana"
            },
            "application/epp+xml": {
                source: "iana",
                compressible: true
            },
            "application/epub+zip": {
                source: "iana",
                compressible: false,
                extensions: ["epub"]
            },
            "application/eshop": {
                source: "iana"
            },
            "application/exi": {
                source: "iana",
                extensions: ["exi"]
            },
            "application/expect-ct-report+json": {
                source: "iana",
                compressible: true
            },
            "application/express": {
                source: "iana",
                extensions: ["exp"]
            },
            "application/fastinfoset": {
                source: "iana"
            },
            "application/fastsoap": {
                source: "iana"
            },
            "application/fdt+xml": {
                source: "iana",
                compressible: true,
                extensions: ["fdt"]
            },
            "application/fhir+json": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/fhir+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/fido.trusted-apps+json": {
                compressible: true
            },
            "application/fits": {
                source: "iana"
            },
            "application/flexfec": {
                source: "iana"
            },
            "application/font-sfnt": {
                source: "iana"
            },
            "application/font-tdpfr": {
                source: "iana",
                extensions: ["pfr"]
            },
            "application/font-woff": {
                source: "iana",
                compressible: false
            },
            "application/framework-attributes+xml": {
                source: "iana",
                compressible: true
            },
            "application/geo+json": {
                source: "iana",
                compressible: true,
                extensions: ["geojson"]
            },
            "application/geo+json-seq": {
                source: "iana"
            },
            "application/geopackage+sqlite3": {
                source: "iana"
            },
            "application/geoxacml+xml": {
                source: "iana",
                compressible: true
            },
            "application/gltf-buffer": {
                source: "iana"
            },
            "application/gml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["gml"]
            },
            "application/gpx+xml": {
                source: "apache",
                compressible: true,
                extensions: ["gpx"]
            },
            "application/gxf": {
                source: "apache",
                extensions: ["gxf"]
            },
            "application/gzip": {
                source: "iana",
                compressible: false,
                extensions: ["gz"]
            },
            "application/h224": {
                source: "iana"
            },
            "application/held+xml": {
                source: "iana",
                compressible: true
            },
            "application/hjson": {
                extensions: ["hjson"]
            },
            "application/http": {
                source: "iana"
            },
            "application/hyperstudio": {
                source: "iana",
                extensions: ["stk"]
            },
            "application/ibe-key-request+xml": {
                source: "iana",
                compressible: true
            },
            "application/ibe-pkg-reply+xml": {
                source: "iana",
                compressible: true
            },
            "application/ibe-pp-data": {
                source: "iana"
            },
            "application/iges": {
                source: "iana"
            },
            "application/im-iscomposing+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/index": {
                source: "iana"
            },
            "application/index.cmd": {
                source: "iana"
            },
            "application/index.obj": {
                source: "iana"
            },
            "application/index.response": {
                source: "iana"
            },
            "application/index.vnd": {
                source: "iana"
            },
            "application/inkml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["ink", "inkml"]
            },
            "application/iotp": {
                source: "iana"
            },
            "application/ipfix": {
                source: "iana",
                extensions: ["ipfix"]
            },
            "application/ipp": {
                source: "iana"
            },
            "application/isup": {
                source: "iana"
            },
            "application/its+xml": {
                source: "iana",
                compressible: true,
                extensions: ["its"]
            },
            "application/java-archive": {
                source: "apache",
                compressible: false,
                extensions: ["jar", "war", "ear"]
            },
            "application/java-serialized-object": {
                source: "apache",
                compressible: false,
                extensions: ["ser"]
            },
            "application/java-vm": {
                source: "apache",
                compressible: false,
                extensions: ["class"]
            },
            "application/javascript": {
                source: "iana",
                charset: "UTF-8",
                compressible: true,
                extensions: ["js", "mjs"]
            },
            "application/jf2feed+json": {
                source: "iana",
                compressible: true
            },
            "application/jose": {
                source: "iana"
            },
            "application/jose+json": {
                source: "iana",
                compressible: true
            },
            "application/jrd+json": {
                source: "iana",
                compressible: true
            },
            "application/jscalendar+json": {
                source: "iana",
                compressible: true
            },
            "application/json": {
                source: "iana",
                charset: "UTF-8",
                compressible: true,
                extensions: ["json", "map"]
            },
            "application/json-patch+json": {
                source: "iana",
                compressible: true
            },
            "application/json-seq": {
                source: "iana"
            },
            "application/json5": {
                extensions: ["json5"]
            },
            "application/jsonml+json": {
                source: "apache",
                compressible: true,
                extensions: ["jsonml"]
            },
            "application/jwk+json": {
                source: "iana",
                compressible: true
            },
            "application/jwk-set+json": {
                source: "iana",
                compressible: true
            },
            "application/jwt": {
                source: "iana"
            },
            "application/kpml-request+xml": {
                source: "iana",
                compressible: true
            },
            "application/kpml-response+xml": {
                source: "iana",
                compressible: true
            },
            "application/ld+json": {
                source: "iana",
                compressible: true,
                extensions: ["jsonld"]
            },
            "application/lgr+xml": {
                source: "iana",
                compressible: true,
                extensions: ["lgr"]
            },
            "application/link-format": {
                source: "iana"
            },
            "application/load-control+xml": {
                source: "iana",
                compressible: true
            },
            "application/lost+xml": {
                source: "iana",
                compressible: true,
                extensions: ["lostxml"]
            },
            "application/lostsync+xml": {
                source: "iana",
                compressible: true
            },
            "application/lpf+zip": {
                source: "iana",
                compressible: false
            },
            "application/lxf": {
                source: "iana"
            },
            "application/mac-binhex40": {
                source: "iana",
                extensions: ["hqx"]
            },
            "application/mac-compactpro": {
                source: "apache",
                extensions: ["cpt"]
            },
            "application/macwriteii": {
                source: "iana"
            },
            "application/mads+xml": {
                source: "iana",
                compressible: true,
                extensions: ["mads"]
            },
            "application/manifest+json": {
                source: "iana",
                charset: "UTF-8",
                compressible: true,
                extensions: ["webmanifest"]
            },
            "application/marc": {
                source: "iana",
                extensions: ["mrc"]
            },
            "application/marcxml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["mrcx"]
            },
            "application/mathematica": {
                source: "iana",
                extensions: ["ma", "nb", "mb"]
            },
            "application/mathml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["mathml"]
            },
            "application/mathml-content+xml": {
                source: "iana",
                compressible: true
            },
            "application/mathml-presentation+xml": {
                source: "iana",
                compressible: true
            },
            "application/mbms-associated-procedure-description+xml": {
                source: "iana",
                compressible: true
            },
            "application/mbms-deregister+xml": {
                source: "iana",
                compressible: true
            },
            "application/mbms-envelope+xml": {
                source: "iana",
                compressible: true
            },
            "application/mbms-msk+xml": {
                source: "iana",
                compressible: true
            },
            "application/mbms-msk-response+xml": {
                source: "iana",
                compressible: true
            },
            "application/mbms-protection-description+xml": {
                source: "iana",
                compressible: true
            },
            "application/mbms-reception-report+xml": {
                source: "iana",
                compressible: true
            },
            "application/mbms-register+xml": {
                source: "iana",
                compressible: true
            },
            "application/mbms-register-response+xml": {
                source: "iana",
                compressible: true
            },
            "application/mbms-schedule+xml": {
                source: "iana",
                compressible: true
            },
            "application/mbms-user-service-description+xml": {
                source: "iana",
                compressible: true
            },
            "application/mbox": {
                source: "iana",
                extensions: ["mbox"]
            },
            "application/media-policy-dataset+xml": {
                source: "iana",
                compressible: true,
                extensions: ["mpf"]
            },
            "application/media_control+xml": {
                source: "iana",
                compressible: true
            },
            "application/mediaservercontrol+xml": {
                source: "iana",
                compressible: true,
                extensions: ["mscml"]
            },
            "application/merge-patch+json": {
                source: "iana",
                compressible: true
            },
            "application/metalink+xml": {
                source: "apache",
                compressible: true,
                extensions: ["metalink"]
            },
            "application/metalink4+xml": {
                source: "iana",
                compressible: true,
                extensions: ["meta4"]
            },
            "application/mets+xml": {
                source: "iana",
                compressible: true,
                extensions: ["mets"]
            },
            "application/mf4": {
                source: "iana"
            },
            "application/mikey": {
                source: "iana"
            },
            "application/mipc": {
                source: "iana"
            },
            "application/missing-blocks+cbor-seq": {
                source: "iana"
            },
            "application/mmt-aei+xml": {
                source: "iana",
                compressible: true,
                extensions: ["maei"]
            },
            "application/mmt-usd+xml": {
                source: "iana",
                compressible: true,
                extensions: ["musd"]
            },
            "application/mods+xml": {
                source: "iana",
                compressible: true,
                extensions: ["mods"]
            },
            "application/moss-keys": {
                source: "iana"
            },
            "application/moss-signature": {
                source: "iana"
            },
            "application/mosskey-data": {
                source: "iana"
            },
            "application/mosskey-request": {
                source: "iana"
            },
            "application/mp21": {
                source: "iana",
                extensions: ["m21", "mp21"]
            },
            "application/mp4": {
                source: "iana",
                extensions: ["mp4s", "m4p"]
            },
            "application/mpeg4-generic": {
                source: "iana"
            },
            "application/mpeg4-iod": {
                source: "iana"
            },
            "application/mpeg4-iod-xmt": {
                source: "iana"
            },
            "application/mrb-consumer+xml": {
                source: "iana",
                compressible: true
            },
            "application/mrb-publish+xml": {
                source: "iana",
                compressible: true
            },
            "application/msc-ivr+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/msc-mixer+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/msword": {
                source: "iana",
                compressible: false,
                extensions: ["doc", "dot"]
            },
            "application/mud+json": {
                source: "iana",
                compressible: true
            },
            "application/multipart-core": {
                source: "iana"
            },
            "application/mxf": {
                source: "iana",
                extensions: ["mxf"]
            },
            "application/n-quads": {
                source: "iana",
                extensions: ["nq"]
            },
            "application/n-triples": {
                source: "iana",
                extensions: ["nt"]
            },
            "application/nasdata": {
                source: "iana"
            },
            "application/news-checkgroups": {
                source: "iana",
                charset: "US-ASCII"
            },
            "application/news-groupinfo": {
                source: "iana",
                charset: "US-ASCII"
            },
            "application/news-transmission": {
                source: "iana"
            },
            "application/nlsml+xml": {
                source: "iana",
                compressible: true
            },
            "application/node": {
                source: "iana",
                extensions: ["cjs"]
            },
            "application/nss": {
                source: "iana"
            },
            "application/oauth-authz-req+jwt": {
                source: "iana"
            },
            "application/oblivious-dns-message": {
                source: "iana"
            },
            "application/ocsp-request": {
                source: "iana"
            },
            "application/ocsp-response": {
                source: "iana"
            },
            "application/octet-stream": {
                source: "iana",
                compressible: false,
                extensions: ["bin", "dms", "lrf", "mar", "so", "dist", "distz", "pkg", "bpk", "dump", "elc", "deploy", "exe", "dll", "deb", "dmg", "iso", "img", "msi", "msp", "msm", "buffer"]
            },
            "application/oda": {
                source: "iana",
                extensions: ["oda"]
            },
            "application/odm+xml": {
                source: "iana",
                compressible: true
            },
            "application/odx": {
                source: "iana"
            },
            "application/oebps-package+xml": {
                source: "iana",
                compressible: true,
                extensions: ["opf"]
            },
            "application/ogg": {
                source: "iana",
                compressible: false,
                extensions: ["ogx"]
            },
            "application/omdoc+xml": {
                source: "apache",
                compressible: true,
                extensions: ["omdoc"]
            },
            "application/onenote": {
                source: "apache",
                extensions: ["onetoc", "onetoc2", "onetmp", "onepkg"]
            },
            "application/opc-nodeset+xml": {
                source: "iana",
                compressible: true
            },
            "application/oscore": {
                source: "iana"
            },
            "application/oxps": {
                source: "iana",
                extensions: ["oxps"]
            },
            "application/p21": {
                source: "iana"
            },
            "application/p21+zip": {
                source: "iana",
                compressible: false
            },
            "application/p2p-overlay+xml": {
                source: "iana",
                compressible: true,
                extensions: ["relo"]
            },
            "application/parityfec": {
                source: "iana"
            },
            "application/passport": {
                source: "iana"
            },
            "application/patch-ops-error+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xer"]
            },
            "application/pdf": {
                source: "iana",
                compressible: false,
                extensions: ["pdf"]
            },
            "application/pdx": {
                source: "iana"
            },
            "application/pem-certificate-chain": {
                source: "iana"
            },
            "application/pgp-encrypted": {
                source: "iana",
                compressible: false,
                extensions: ["pgp"]
            },
            "application/pgp-keys": {
                source: "iana",
                extensions: ["asc"]
            },
            "application/pgp-signature": {
                source: "iana",
                extensions: ["asc", "sig"]
            },
            "application/pics-rules": {
                source: "apache",
                extensions: ["prf"]
            },
            "application/pidf+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/pidf-diff+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/pkcs10": {
                source: "iana",
                extensions: ["p10"]
            },
            "application/pkcs12": {
                source: "iana"
            },
            "application/pkcs7-mime": {
                source: "iana",
                extensions: ["p7m", "p7c"]
            },
            "application/pkcs7-signature": {
                source: "iana",
                extensions: ["p7s"]
            },
            "application/pkcs8": {
                source: "iana",
                extensions: ["p8"]
            },
            "application/pkcs8-encrypted": {
                source: "iana"
            },
            "application/pkix-attr-cert": {
                source: "iana",
                extensions: ["ac"]
            },
            "application/pkix-cert": {
                source: "iana",
                extensions: ["cer"]
            },
            "application/pkix-crl": {
                source: "iana",
                extensions: ["crl"]
            },
            "application/pkix-pkipath": {
                source: "iana",
                extensions: ["pkipath"]
            },
            "application/pkixcmp": {
                source: "iana",
                extensions: ["pki"]
            },
            "application/pls+xml": {
                source: "iana",
                compressible: true,
                extensions: ["pls"]
            },
            "application/poc-settings+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/postscript": {
                source: "iana",
                compressible: true,
                extensions: ["ai", "eps", "ps"]
            },
            "application/ppsp-tracker+json": {
                source: "iana",
                compressible: true
            },
            "application/problem+json": {
                source: "iana",
                compressible: true
            },
            "application/problem+xml": {
                source: "iana",
                compressible: true
            },
            "application/provenance+xml": {
                source: "iana",
                compressible: true,
                extensions: ["provx"]
            },
            "application/prs.alvestrand.titrax-sheet": {
                source: "iana"
            },
            "application/prs.cww": {
                source: "iana",
                extensions: ["cww"]
            },
            "application/prs.cyn": {
                source: "iana",
                charset: "7-BIT"
            },
            "application/prs.hpub+zip": {
                source: "iana",
                compressible: false
            },
            "application/prs.nprend": {
                source: "iana"
            },
            "application/prs.plucker": {
                source: "iana"
            },
            "application/prs.rdf-xml-crypt": {
                source: "iana"
            },
            "application/prs.xsf+xml": {
                source: "iana",
                compressible: true
            },
            "application/pskc+xml": {
                source: "iana",
                compressible: true,
                extensions: ["pskcxml"]
            },
            "application/pvd+json": {
                source: "iana",
                compressible: true
            },
            "application/qsig": {
                source: "iana"
            },
            "application/raml+yaml": {
                compressible: true,
                extensions: ["raml"]
            },
            "application/raptorfec": {
                source: "iana"
            },
            "application/rdap+json": {
                source: "iana",
                compressible: true
            },
            "application/rdf+xml": {
                source: "iana",
                compressible: true,
                extensions: ["rdf", "owl"]
            },
            "application/reginfo+xml": {
                source: "iana",
                compressible: true,
                extensions: ["rif"]
            },
            "application/relax-ng-compact-syntax": {
                source: "iana",
                extensions: ["rnc"]
            },
            "application/remote-printing": {
                source: "iana"
            },
            "application/reputon+json": {
                source: "iana",
                compressible: true
            },
            "application/resource-lists+xml": {
                source: "iana",
                compressible: true,
                extensions: ["rl"]
            },
            "application/resource-lists-diff+xml": {
                source: "iana",
                compressible: true,
                extensions: ["rld"]
            },
            "application/rfc+xml": {
                source: "iana",
                compressible: true
            },
            "application/riscos": {
                source: "iana"
            },
            "application/rlmi+xml": {
                source: "iana",
                compressible: true
            },
            "application/rls-services+xml": {
                source: "iana",
                compressible: true,
                extensions: ["rs"]
            },
            "application/route-apd+xml": {
                source: "iana",
                compressible: true,
                extensions: ["rapd"]
            },
            "application/route-s-tsid+xml": {
                source: "iana",
                compressible: true,
                extensions: ["sls"]
            },
            "application/route-usd+xml": {
                source: "iana",
                compressible: true,
                extensions: ["rusd"]
            },
            "application/rpki-ghostbusters": {
                source: "iana",
                extensions: ["gbr"]
            },
            "application/rpki-manifest": {
                source: "iana",
                extensions: ["mft"]
            },
            "application/rpki-publication": {
                source: "iana"
            },
            "application/rpki-roa": {
                source: "iana",
                extensions: ["roa"]
            },
            "application/rpki-updown": {
                source: "iana"
            },
            "application/rsd+xml": {
                source: "apache",
                compressible: true,
                extensions: ["rsd"]
            },
            "application/rss+xml": {
                source: "apache",
                compressible: true,
                extensions: ["rss"]
            },
            "application/rtf": {
                source: "iana",
                compressible: true,
                extensions: ["rtf"]
            },
            "application/rtploopback": {
                source: "iana"
            },
            "application/rtx": {
                source: "iana"
            },
            "application/samlassertion+xml": {
                source: "iana",
                compressible: true
            },
            "application/samlmetadata+xml": {
                source: "iana",
                compressible: true
            },
            "application/sarif+json": {
                source: "iana",
                compressible: true
            },
            "application/sarif-external-properties+json": {
                source: "iana",
                compressible: true
            },
            "application/sbe": {
                source: "iana"
            },
            "application/sbml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["sbml"]
            },
            "application/scaip+xml": {
                source: "iana",
                compressible: true
            },
            "application/scim+json": {
                source: "iana",
                compressible: true
            },
            "application/scvp-cv-request": {
                source: "iana",
                extensions: ["scq"]
            },
            "application/scvp-cv-response": {
                source: "iana",
                extensions: ["scs"]
            },
            "application/scvp-vp-request": {
                source: "iana",
                extensions: ["spq"]
            },
            "application/scvp-vp-response": {
                source: "iana",
                extensions: ["spp"]
            },
            "application/sdp": {
                source: "iana",
                extensions: ["sdp"]
            },
            "application/secevent+jwt": {
                source: "iana"
            },
            "application/senml+cbor": {
                source: "iana"
            },
            "application/senml+json": {
                source: "iana",
                compressible: true
            },
            "application/senml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["senmlx"]
            },
            "application/senml-etch+cbor": {
                source: "iana"
            },
            "application/senml-etch+json": {
                source: "iana",
                compressible: true
            },
            "application/senml-exi": {
                source: "iana"
            },
            "application/sensml+cbor": {
                source: "iana"
            },
            "application/sensml+json": {
                source: "iana",
                compressible: true
            },
            "application/sensml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["sensmlx"]
            },
            "application/sensml-exi": {
                source: "iana"
            },
            "application/sep+xml": {
                source: "iana",
                compressible: true
            },
            "application/sep-exi": {
                source: "iana"
            },
            "application/session-info": {
                source: "iana"
            },
            "application/set-payment": {
                source: "iana"
            },
            "application/set-payment-initiation": {
                source: "iana",
                extensions: ["setpay"]
            },
            "application/set-registration": {
                source: "iana"
            },
            "application/set-registration-initiation": {
                source: "iana",
                extensions: ["setreg"]
            },
            "application/sgml": {
                source: "iana"
            },
            "application/sgml-open-catalog": {
                source: "iana"
            },
            "application/shf+xml": {
                source: "iana",
                compressible: true,
                extensions: ["shf"]
            },
            "application/sieve": {
                source: "iana",
                extensions: ["siv", "sieve"]
            },
            "application/simple-filter+xml": {
                source: "iana",
                compressible: true
            },
            "application/simple-message-summary": {
                source: "iana"
            },
            "application/simplesymbolcontainer": {
                source: "iana"
            },
            "application/sipc": {
                source: "iana"
            },
            "application/slate": {
                source: "iana"
            },
            "application/smil": {
                source: "iana"
            },
            "application/smil+xml": {
                source: "iana",
                compressible: true,
                extensions: ["smi", "smil"]
            },
            "application/smpte336m": {
                source: "iana"
            },
            "application/soap+fastinfoset": {
                source: "iana"
            },
            "application/soap+xml": {
                source: "iana",
                compressible: true
            },
            "application/sparql-query": {
                source: "iana",
                extensions: ["rq"]
            },
            "application/sparql-results+xml": {
                source: "iana",
                compressible: true,
                extensions: ["srx"]
            },
            "application/spdx+json": {
                source: "iana",
                compressible: true
            },
            "application/spirits-event+xml": {
                source: "iana",
                compressible: true
            },
            "application/sql": {
                source: "iana"
            },
            "application/srgs": {
                source: "iana",
                extensions: ["gram"]
            },
            "application/srgs+xml": {
                source: "iana",
                compressible: true,
                extensions: ["grxml"]
            },
            "application/sru+xml": {
                source: "iana",
                compressible: true,
                extensions: ["sru"]
            },
            "application/ssdl+xml": {
                source: "apache",
                compressible: true,
                extensions: ["ssdl"]
            },
            "application/ssml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["ssml"]
            },
            "application/stix+json": {
                source: "iana",
                compressible: true
            },
            "application/swid+xml": {
                source: "iana",
                compressible: true,
                extensions: ["swidtag"]
            },
            "application/tamp-apex-update": {
                source: "iana"
            },
            "application/tamp-apex-update-confirm": {
                source: "iana"
            },
            "application/tamp-community-update": {
                source: "iana"
            },
            "application/tamp-community-update-confirm": {
                source: "iana"
            },
            "application/tamp-error": {
                source: "iana"
            },
            "application/tamp-sequence-adjust": {
                source: "iana"
            },
            "application/tamp-sequence-adjust-confirm": {
                source: "iana"
            },
            "application/tamp-status-query": {
                source: "iana"
            },
            "application/tamp-status-response": {
                source: "iana"
            },
            "application/tamp-update": {
                source: "iana"
            },
            "application/tamp-update-confirm": {
                source: "iana"
            },
            "application/tar": {
                compressible: true
            },
            "application/taxii+json": {
                source: "iana",
                compressible: true
            },
            "application/td+json": {
                source: "iana",
                compressible: true
            },
            "application/tei+xml": {
                source: "iana",
                compressible: true,
                extensions: ["tei", "teicorpus"]
            },
            "application/tetra_isi": {
                source: "iana"
            },
            "application/thraud+xml": {
                source: "iana",
                compressible: true,
                extensions: ["tfi"]
            },
            "application/timestamp-query": {
                source: "iana"
            },
            "application/timestamp-reply": {
                source: "iana"
            },
            "application/timestamped-data": {
                source: "iana",
                extensions: ["tsd"]
            },
            "application/tlsrpt+gzip": {
                source: "iana"
            },
            "application/tlsrpt+json": {
                source: "iana",
                compressible: true
            },
            "application/tnauthlist": {
                source: "iana"
            },
            "application/token-introspection+jwt": {
                source: "iana"
            },
            "application/toml": {
                compressible: true,
                extensions: ["toml"]
            },
            "application/trickle-ice-sdpfrag": {
                source: "iana"
            },
            "application/trig": {
                source: "iana",
                extensions: ["trig"]
            },
            "application/ttml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["ttml"]
            },
            "application/tve-trigger": {
                source: "iana"
            },
            "application/tzif": {
                source: "iana"
            },
            "application/tzif-leap": {
                source: "iana"
            },
            "application/ubjson": {
                compressible: false,
                extensions: ["ubj"]
            },
            "application/ulpfec": {
                source: "iana"
            },
            "application/urc-grpsheet+xml": {
                source: "iana",
                compressible: true
            },
            "application/urc-ressheet+xml": {
                source: "iana",
                compressible: true,
                extensions: ["rsheet"]
            },
            "application/urc-targetdesc+xml": {
                source: "iana",
                compressible: true,
                extensions: ["td"]
            },
            "application/urc-uisocketdesc+xml": {
                source: "iana",
                compressible: true
            },
            "application/vcard+json": {
                source: "iana",
                compressible: true
            },
            "application/vcard+xml": {
                source: "iana",
                compressible: true
            },
            "application/vemmi": {
                source: "iana"
            },
            "application/vividence.scriptfile": {
                source: "apache"
            },
            "application/vnd.1000minds.decision-model+xml": {
                source: "iana",
                compressible: true,
                extensions: ["1km"]
            },
            "application/vnd.3gpp-prose+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp-prose-pc3ch+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp-v2x-local-service-information": {
                source: "iana"
            },
            "application/vnd.3gpp.5gnas": {
                source: "iana"
            },
            "application/vnd.3gpp.access-transfer-events+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.bsf+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.gmop+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.gtpc": {
                source: "iana"
            },
            "application/vnd.3gpp.interworking-data": {
                source: "iana"
            },
            "application/vnd.3gpp.lpp": {
                source: "iana"
            },
            "application/vnd.3gpp.mc-signalling-ear": {
                source: "iana"
            },
            "application/vnd.3gpp.mcdata-affiliation-command+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcdata-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcdata-payload": {
                source: "iana"
            },
            "application/vnd.3gpp.mcdata-service-config+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcdata-signalling": {
                source: "iana"
            },
            "application/vnd.3gpp.mcdata-ue-config+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcdata-user-profile+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcptt-affiliation-command+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcptt-floor-request+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcptt-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcptt-location-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcptt-mbms-usage-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcptt-service-config+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcptt-signed+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcptt-ue-config+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcptt-ue-init-config+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcptt-user-profile+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcvideo-affiliation-command+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcvideo-affiliation-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcvideo-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcvideo-location-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcvideo-mbms-usage-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcvideo-service-config+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcvideo-transmission-request+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcvideo-ue-config+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mcvideo-user-profile+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.mid-call+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.ngap": {
                source: "iana"
            },
            "application/vnd.3gpp.pfcp": {
                source: "iana"
            },
            "application/vnd.3gpp.pic-bw-large": {
                source: "iana",
                extensions: ["plb"]
            },
            "application/vnd.3gpp.pic-bw-small": {
                source: "iana",
                extensions: ["psb"]
            },
            "application/vnd.3gpp.pic-bw-var": {
                source: "iana",
                extensions: ["pvb"]
            },
            "application/vnd.3gpp.s1ap": {
                source: "iana"
            },
            "application/vnd.3gpp.sms": {
                source: "iana"
            },
            "application/vnd.3gpp.sms+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.srvcc-ext+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.srvcc-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.state-and-event-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp.ussd+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp2.bcmcsinfo+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.3gpp2.sms": {
                source: "iana"
            },
            "application/vnd.3gpp2.tcap": {
                source: "iana",
                extensions: ["tcap"]
            },
            "application/vnd.3lightssoftware.imagescal": {
                source: "iana"
            },
            "application/vnd.3m.post-it-notes": {
                source: "iana",
                extensions: ["pwn"]
            },
            "application/vnd.accpac.simply.aso": {
                source: "iana",
                extensions: ["aso"]
            },
            "application/vnd.accpac.simply.imp": {
                source: "iana",
                extensions: ["imp"]
            },
            "application/vnd.acucobol": {
                source: "iana",
                extensions: ["acu"]
            },
            "application/vnd.acucorp": {
                source: "iana",
                extensions: ["atc", "acutc"]
            },
            "application/vnd.adobe.air-application-installer-package+zip": {
                source: "apache",
                compressible: false,
                extensions: ["air"]
            },
            "application/vnd.adobe.flash.movie": {
                source: "iana"
            },
            "application/vnd.adobe.formscentral.fcdt": {
                source: "iana",
                extensions: ["fcdt"]
            },
            "application/vnd.adobe.fxp": {
                source: "iana",
                extensions: ["fxp", "fxpl"]
            },
            "application/vnd.adobe.partial-upload": {
                source: "iana"
            },
            "application/vnd.adobe.xdp+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xdp"]
            },
            "application/vnd.adobe.xfdf": {
                source: "iana",
                extensions: ["xfdf"]
            },
            "application/vnd.aether.imp": {
                source: "iana"
            },
            "application/vnd.afpc.afplinedata": {
                source: "iana"
            },
            "application/vnd.afpc.afplinedata-pagedef": {
                source: "iana"
            },
            "application/vnd.afpc.cmoca-cmresource": {
                source: "iana"
            },
            "application/vnd.afpc.foca-charset": {
                source: "iana"
            },
            "application/vnd.afpc.foca-codedfont": {
                source: "iana"
            },
            "application/vnd.afpc.foca-codepage": {
                source: "iana"
            },
            "application/vnd.afpc.modca": {
                source: "iana"
            },
            "application/vnd.afpc.modca-cmtable": {
                source: "iana"
            },
            "application/vnd.afpc.modca-formdef": {
                source: "iana"
            },
            "application/vnd.afpc.modca-mediummap": {
                source: "iana"
            },
            "application/vnd.afpc.modca-objectcontainer": {
                source: "iana"
            },
            "application/vnd.afpc.modca-overlay": {
                source: "iana"
            },
            "application/vnd.afpc.modca-pagesegment": {
                source: "iana"
            },
            "application/vnd.age": {
                source: "iana",
                extensions: ["age"]
            },
            "application/vnd.ah-barcode": {
                source: "iana"
            },
            "application/vnd.ahead.space": {
                source: "iana",
                extensions: ["ahead"]
            },
            "application/vnd.airzip.filesecure.azf": {
                source: "iana",
                extensions: ["azf"]
            },
            "application/vnd.airzip.filesecure.azs": {
                source: "iana",
                extensions: ["azs"]
            },
            "application/vnd.amadeus+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.amazon.ebook": {
                source: "apache",
                extensions: ["azw"]
            },
            "application/vnd.amazon.mobi8-ebook": {
                source: "iana"
            },
            "application/vnd.americandynamics.acc": {
                source: "iana",
                extensions: ["acc"]
            },
            "application/vnd.amiga.ami": {
                source: "iana",
                extensions: ["ami"]
            },
            "application/vnd.amundsen.maze+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.android.ota": {
                source: "iana"
            },
            "application/vnd.android.package-archive": {
                source: "apache",
                compressible: false,
                extensions: ["apk"]
            },
            "application/vnd.anki": {
                source: "iana"
            },
            "application/vnd.anser-web-certificate-issue-initiation": {
                source: "iana",
                extensions: ["cii"]
            },
            "application/vnd.anser-web-funds-transfer-initiation": {
                source: "apache",
                extensions: ["fti"]
            },
            "application/vnd.antix.game-component": {
                source: "iana",
                extensions: ["atx"]
            },
            "application/vnd.apache.arrow.file": {
                source: "iana"
            },
            "application/vnd.apache.arrow.stream": {
                source: "iana"
            },
            "application/vnd.apache.thrift.binary": {
                source: "iana"
            },
            "application/vnd.apache.thrift.compact": {
                source: "iana"
            },
            "application/vnd.apache.thrift.json": {
                source: "iana"
            },
            "application/vnd.api+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.aplextor.warrp+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.apothekende.reservation+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.apple.installer+xml": {
                source: "iana",
                compressible: true,
                extensions: ["mpkg"]
            },
            "application/vnd.apple.keynote": {
                source: "iana",
                extensions: ["key"]
            },
            "application/vnd.apple.mpegurl": {
                source: "iana",
                extensions: ["m3u8"]
            },
            "application/vnd.apple.numbers": {
                source: "iana",
                extensions: ["numbers"]
            },
            "application/vnd.apple.pages": {
                source: "iana",
                extensions: ["pages"]
            },
            "application/vnd.apple.pkpass": {
                compressible: false,
                extensions: ["pkpass"]
            },
            "application/vnd.arastra.swi": {
                source: "iana"
            },
            "application/vnd.aristanetworks.swi": {
                source: "iana",
                extensions: ["swi"]
            },
            "application/vnd.artisan+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.artsquare": {
                source: "iana"
            },
            "application/vnd.astraea-software.iota": {
                source: "iana",
                extensions: ["iota"]
            },
            "application/vnd.audiograph": {
                source: "iana",
                extensions: ["aep"]
            },
            "application/vnd.autopackage": {
                source: "iana"
            },
            "application/vnd.avalon+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.avistar+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.balsamiq.bmml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["bmml"]
            },
            "application/vnd.balsamiq.bmpr": {
                source: "iana"
            },
            "application/vnd.banana-accounting": {
                source: "iana"
            },
            "application/vnd.bbf.usp.error": {
                source: "iana"
            },
            "application/vnd.bbf.usp.msg": {
                source: "iana"
            },
            "application/vnd.bbf.usp.msg+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.bekitzur-stech+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.bint.med-content": {
                source: "iana"
            },
            "application/vnd.biopax.rdf+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.blink-idb-value-wrapper": {
                source: "iana"
            },
            "application/vnd.blueice.multipass": {
                source: "iana",
                extensions: ["mpm"]
            },
            "application/vnd.bluetooth.ep.oob": {
                source: "iana"
            },
            "application/vnd.bluetooth.le.oob": {
                source: "iana"
            },
            "application/vnd.bmi": {
                source: "iana",
                extensions: ["bmi"]
            },
            "application/vnd.bpf": {
                source: "iana"
            },
            "application/vnd.bpf3": {
                source: "iana"
            },
            "application/vnd.businessobjects": {
                source: "iana",
                extensions: ["rep"]
            },
            "application/vnd.byu.uapi+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.cab-jscript": {
                source: "iana"
            },
            "application/vnd.canon-cpdl": {
                source: "iana"
            },
            "application/vnd.canon-lips": {
                source: "iana"
            },
            "application/vnd.capasystems-pg+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.cendio.thinlinc.clientconf": {
                source: "iana"
            },
            "application/vnd.century-systems.tcp_stream": {
                source: "iana"
            },
            "application/vnd.chemdraw+xml": {
                source: "iana",
                compressible: true,
                extensions: ["cdxml"]
            },
            "application/vnd.chess-pgn": {
                source: "iana"
            },
            "application/vnd.chipnuts.karaoke-mmd": {
                source: "iana",
                extensions: ["mmd"]
            },
            "application/vnd.ciedi": {
                source: "iana"
            },
            "application/vnd.cinderella": {
                source: "iana",
                extensions: ["cdy"]
            },
            "application/vnd.cirpack.isdn-ext": {
                source: "iana"
            },
            "application/vnd.citationstyles.style+xml": {
                source: "iana",
                compressible: true,
                extensions: ["csl"]
            },
            "application/vnd.claymore": {
                source: "iana",
                extensions: ["cla"]
            },
            "application/vnd.cloanto.rp9": {
                source: "iana",
                extensions: ["rp9"]
            },
            "application/vnd.clonk.c4group": {
                source: "iana",
                extensions: ["c4g", "c4d", "c4f", "c4p", "c4u"]
            },
            "application/vnd.cluetrust.cartomobile-config": {
                source: "iana",
                extensions: ["c11amc"]
            },
            "application/vnd.cluetrust.cartomobile-config-pkg": {
                source: "iana",
                extensions: ["c11amz"]
            },
            "application/vnd.coffeescript": {
                source: "iana"
            },
            "application/vnd.collabio.xodocuments.document": {
                source: "iana"
            },
            "application/vnd.collabio.xodocuments.document-template": {
                source: "iana"
            },
            "application/vnd.collabio.xodocuments.presentation": {
                source: "iana"
            },
            "application/vnd.collabio.xodocuments.presentation-template": {
                source: "iana"
            },
            "application/vnd.collabio.xodocuments.spreadsheet": {
                source: "iana"
            },
            "application/vnd.collabio.xodocuments.spreadsheet-template": {
                source: "iana"
            },
            "application/vnd.collection+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.collection.doc+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.collection.next+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.comicbook+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.comicbook-rar": {
                source: "iana"
            },
            "application/vnd.commerce-battelle": {
                source: "iana"
            },
            "application/vnd.commonspace": {
                source: "iana",
                extensions: ["csp"]
            },
            "application/vnd.contact.cmsg": {
                source: "iana",
                extensions: ["cdbcmsg"]
            },
            "application/vnd.coreos.ignition+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.cosmocaller": {
                source: "iana",
                extensions: ["cmc"]
            },
            "application/vnd.crick.clicker": {
                source: "iana",
                extensions: ["clkx"]
            },
            "application/vnd.crick.clicker.keyboard": {
                source: "iana",
                extensions: ["clkk"]
            },
            "application/vnd.crick.clicker.palette": {
                source: "iana",
                extensions: ["clkp"]
            },
            "application/vnd.crick.clicker.template": {
                source: "iana",
                extensions: ["clkt"]
            },
            "application/vnd.crick.clicker.wordbank": {
                source: "iana",
                extensions: ["clkw"]
            },
            "application/vnd.criticaltools.wbs+xml": {
                source: "iana",
                compressible: true,
                extensions: ["wbs"]
            },
            "application/vnd.cryptii.pipe+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.crypto-shade-file": {
                source: "iana"
            },
            "application/vnd.cryptomator.encrypted": {
                source: "iana"
            },
            "application/vnd.cryptomator.vault": {
                source: "iana"
            },
            "application/vnd.ctc-posml": {
                source: "iana",
                extensions: ["pml"]
            },
            "application/vnd.ctct.ws+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.cups-pdf": {
                source: "iana"
            },
            "application/vnd.cups-postscript": {
                source: "iana"
            },
            "application/vnd.cups-ppd": {
                source: "iana",
                extensions: ["ppd"]
            },
            "application/vnd.cups-raster": {
                source: "iana"
            },
            "application/vnd.cups-raw": {
                source: "iana"
            },
            "application/vnd.curl": {
                source: "iana"
            },
            "application/vnd.curl.car": {
                source: "apache",
                extensions: ["car"]
            },
            "application/vnd.curl.pcurl": {
                source: "apache",
                extensions: ["pcurl"]
            },
            "application/vnd.cyan.dean.root+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.cybank": {
                source: "iana"
            },
            "application/vnd.cyclonedx+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.cyclonedx+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.d2l.coursepackage1p0+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.d3m-dataset": {
                source: "iana"
            },
            "application/vnd.d3m-problem": {
                source: "iana"
            },
            "application/vnd.dart": {
                source: "iana",
                compressible: true,
                extensions: ["dart"]
            },
            "application/vnd.data-vision.rdz": {
                source: "iana",
                extensions: ["rdz"]
            },
            "application/vnd.datapackage+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.dataresource+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.dbf": {
                source: "iana",
                extensions: ["dbf"]
            },
            "application/vnd.debian.binary-package": {
                source: "iana"
            },
            "application/vnd.dece.data": {
                source: "iana",
                extensions: ["uvf", "uvvf", "uvd", "uvvd"]
            },
            "application/vnd.dece.ttml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["uvt", "uvvt"]
            },
            "application/vnd.dece.unspecified": {
                source: "iana",
                extensions: ["uvx", "uvvx"]
            },
            "application/vnd.dece.zip": {
                source: "iana",
                extensions: ["uvz", "uvvz"]
            },
            "application/vnd.denovo.fcselayout-link": {
                source: "iana",
                extensions: ["fe_launch"]
            },
            "application/vnd.desmume.movie": {
                source: "iana"
            },
            "application/vnd.dir-bi.plate-dl-nosuffix": {
                source: "iana"
            },
            "application/vnd.dm.delegation+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.dna": {
                source: "iana",
                extensions: ["dna"]
            },
            "application/vnd.document+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.dolby.mlp": {
                source: "apache",
                extensions: ["mlp"]
            },
            "application/vnd.dolby.mobile.1": {
                source: "iana"
            },
            "application/vnd.dolby.mobile.2": {
                source: "iana"
            },
            "application/vnd.doremir.scorecloud-binary-document": {
                source: "iana"
            },
            "application/vnd.dpgraph": {
                source: "iana",
                extensions: ["dpg"]
            },
            "application/vnd.dreamfactory": {
                source: "iana",
                extensions: ["dfac"]
            },
            "application/vnd.drive+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ds-keypoint": {
                source: "apache",
                extensions: ["kpxx"]
            },
            "application/vnd.dtg.local": {
                source: "iana"
            },
            "application/vnd.dtg.local.flash": {
                source: "iana"
            },
            "application/vnd.dtg.local.html": {
                source: "iana"
            },
            "application/vnd.dvb.ait": {
                source: "iana",
                extensions: ["ait"]
            },
            "application/vnd.dvb.dvbisl+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.dvb.dvbj": {
                source: "iana"
            },
            "application/vnd.dvb.esgcontainer": {
                source: "iana"
            },
            "application/vnd.dvb.ipdcdftnotifaccess": {
                source: "iana"
            },
            "application/vnd.dvb.ipdcesgaccess": {
                source: "iana"
            },
            "application/vnd.dvb.ipdcesgaccess2": {
                source: "iana"
            },
            "application/vnd.dvb.ipdcesgpdd": {
                source: "iana"
            },
            "application/vnd.dvb.ipdcroaming": {
                source: "iana"
            },
            "application/vnd.dvb.iptv.alfec-base": {
                source: "iana"
            },
            "application/vnd.dvb.iptv.alfec-enhancement": {
                source: "iana"
            },
            "application/vnd.dvb.notif-aggregate-root+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.dvb.notif-container+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.dvb.notif-generic+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.dvb.notif-ia-msglist+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.dvb.notif-ia-registration-request+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.dvb.notif-ia-registration-response+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.dvb.notif-init+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.dvb.pfr": {
                source: "iana"
            },
            "application/vnd.dvb.service": {
                source: "iana",
                extensions: ["svc"]
            },
            "application/vnd.dxr": {
                source: "iana"
            },
            "application/vnd.dynageo": {
                source: "iana",
                extensions: ["geo"]
            },
            "application/vnd.dzr": {
                source: "iana"
            },
            "application/vnd.easykaraoke.cdgdownload": {
                source: "iana"
            },
            "application/vnd.ecdis-update": {
                source: "iana"
            },
            "application/vnd.ecip.rlp": {
                source: "iana"
            },
            "application/vnd.eclipse.ditto+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ecowin.chart": {
                source: "iana",
                extensions: ["mag"]
            },
            "application/vnd.ecowin.filerequest": {
                source: "iana"
            },
            "application/vnd.ecowin.fileupdate": {
                source: "iana"
            },
            "application/vnd.ecowin.series": {
                source: "iana"
            },
            "application/vnd.ecowin.seriesrequest": {
                source: "iana"
            },
            "application/vnd.ecowin.seriesupdate": {
                source: "iana"
            },
            "application/vnd.efi.img": {
                source: "iana"
            },
            "application/vnd.efi.iso": {
                source: "iana"
            },
            "application/vnd.emclient.accessrequest+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.enliven": {
                source: "iana",
                extensions: ["nml"]
            },
            "application/vnd.enphase.envoy": {
                source: "iana"
            },
            "application/vnd.eprints.data+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.epson.esf": {
                source: "iana",
                extensions: ["esf"]
            },
            "application/vnd.epson.msf": {
                source: "iana",
                extensions: ["msf"]
            },
            "application/vnd.epson.quickanime": {
                source: "iana",
                extensions: ["qam"]
            },
            "application/vnd.epson.salt": {
                source: "iana",
                extensions: ["slt"]
            },
            "application/vnd.epson.ssf": {
                source: "iana",
                extensions: ["ssf"]
            },
            "application/vnd.ericsson.quickcall": {
                source: "iana"
            },
            "application/vnd.espass-espass+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.eszigno3+xml": {
                source: "iana",
                compressible: true,
                extensions: ["es3", "et3"]
            },
            "application/vnd.etsi.aoc+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.asic-e+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.etsi.asic-s+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.etsi.cug+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.iptvcommand+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.iptvdiscovery+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.iptvprofile+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.iptvsad-bc+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.iptvsad-cod+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.iptvsad-npvr+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.iptvservice+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.iptvsync+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.iptvueprofile+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.mcid+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.mheg5": {
                source: "iana"
            },
            "application/vnd.etsi.overload-control-policy-dataset+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.pstn+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.sci+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.simservs+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.timestamp-token": {
                source: "iana"
            },
            "application/vnd.etsi.tsl+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.etsi.tsl.der": {
                source: "iana"
            },
            "application/vnd.eu.kasparian.car+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.eudora.data": {
                source: "iana"
            },
            "application/vnd.evolv.ecig.profile": {
                source: "iana"
            },
            "application/vnd.evolv.ecig.settings": {
                source: "iana"
            },
            "application/vnd.evolv.ecig.theme": {
                source: "iana"
            },
            "application/vnd.exstream-empower+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.exstream-package": {
                source: "iana"
            },
            "application/vnd.ezpix-album": {
                source: "iana",
                extensions: ["ez2"]
            },
            "application/vnd.ezpix-package": {
                source: "iana",
                extensions: ["ez3"]
            },
            "application/vnd.f-secure.mobile": {
                source: "iana"
            },
            "application/vnd.familysearch.gedcom+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.fastcopy-disk-image": {
                source: "iana"
            },
            "application/vnd.fdf": {
                source: "iana",
                extensions: ["fdf"]
            },
            "application/vnd.fdsn.mseed": {
                source: "iana",
                extensions: ["mseed"]
            },
            "application/vnd.fdsn.seed": {
                source: "iana",
                extensions: ["seed", "dataless"]
            },
            "application/vnd.ffsns": {
                source: "iana"
            },
            "application/vnd.ficlab.flb+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.filmit.zfc": {
                source: "iana"
            },
            "application/vnd.fints": {
                source: "iana"
            },
            "application/vnd.firemonkeys.cloudcell": {
                source: "iana"
            },
            "application/vnd.flographit": {
                source: "iana",
                extensions: ["gph"]
            },
            "application/vnd.fluxtime.clip": {
                source: "iana",
                extensions: ["ftc"]
            },
            "application/vnd.font-fontforge-sfd": {
                source: "iana"
            },
            "application/vnd.framemaker": {
                source: "iana",
                extensions: ["fm", "frame", "maker", "book"]
            },
            "application/vnd.frogans.fnc": {
                source: "iana",
                extensions: ["fnc"]
            },
            "application/vnd.frogans.ltf": {
                source: "iana",
                extensions: ["ltf"]
            },
            "application/vnd.fsc.weblaunch": {
                source: "iana",
                extensions: ["fsc"]
            },
            "application/vnd.fujifilm.fb.docuworks": {
                source: "iana"
            },
            "application/vnd.fujifilm.fb.docuworks.binder": {
                source: "iana"
            },
            "application/vnd.fujifilm.fb.docuworks.container": {
                source: "iana"
            },
            "application/vnd.fujifilm.fb.jfi+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.fujitsu.oasys": {
                source: "iana",
                extensions: ["oas"]
            },
            "application/vnd.fujitsu.oasys2": {
                source: "iana",
                extensions: ["oa2"]
            },
            "application/vnd.fujitsu.oasys3": {
                source: "iana",
                extensions: ["oa3"]
            },
            "application/vnd.fujitsu.oasysgp": {
                source: "iana",
                extensions: ["fg5"]
            },
            "application/vnd.fujitsu.oasysprs": {
                source: "iana",
                extensions: ["bh2"]
            },
            "application/vnd.fujixerox.art-ex": {
                source: "iana"
            },
            "application/vnd.fujixerox.art4": {
                source: "iana"
            },
            "application/vnd.fujixerox.ddd": {
                source: "iana",
                extensions: ["ddd"]
            },
            "application/vnd.fujixerox.docuworks": {
                source: "iana",
                extensions: ["xdw"]
            },
            "application/vnd.fujixerox.docuworks.binder": {
                source: "iana",
                extensions: ["xbd"]
            },
            "application/vnd.fujixerox.docuworks.container": {
                source: "iana"
            },
            "application/vnd.fujixerox.hbpl": {
                source: "iana"
            },
            "application/vnd.fut-misnet": {
                source: "iana"
            },
            "application/vnd.futoin+cbor": {
                source: "iana"
            },
            "application/vnd.futoin+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.fuzzysheet": {
                source: "iana",
                extensions: ["fzs"]
            },
            "application/vnd.genomatix.tuxedo": {
                source: "iana",
                extensions: ["txd"]
            },
            "application/vnd.gentics.grd+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.geo+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.geocube+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.geogebra.file": {
                source: "iana",
                extensions: ["ggb"]
            },
            "application/vnd.geogebra.slides": {
                source: "iana"
            },
            "application/vnd.geogebra.tool": {
                source: "iana",
                extensions: ["ggt"]
            },
            "application/vnd.geometry-explorer": {
                source: "iana",
                extensions: ["gex", "gre"]
            },
            "application/vnd.geonext": {
                source: "iana",
                extensions: ["gxt"]
            },
            "application/vnd.geoplan": {
                source: "iana",
                extensions: ["g2w"]
            },
            "application/vnd.geospace": {
                source: "iana",
                extensions: ["g3w"]
            },
            "application/vnd.gerber": {
                source: "iana"
            },
            "application/vnd.globalplatform.card-content-mgt": {
                source: "iana"
            },
            "application/vnd.globalplatform.card-content-mgt-response": {
                source: "iana"
            },
            "application/vnd.gmx": {
                source: "iana",
                extensions: ["gmx"]
            },
            "application/vnd.google-apps.document": {
                compressible: false,
                extensions: ["gdoc"]
            },
            "application/vnd.google-apps.presentation": {
                compressible: false,
                extensions: ["gslides"]
            },
            "application/vnd.google-apps.spreadsheet": {
                compressible: false,
                extensions: ["gsheet"]
            },
            "application/vnd.google-earth.kml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["kml"]
            },
            "application/vnd.google-earth.kmz": {
                source: "iana",
                compressible: false,
                extensions: ["kmz"]
            },
            "application/vnd.gov.sk.e-form+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.gov.sk.e-form+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.gov.sk.xmldatacontainer+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.grafeq": {
                source: "iana",
                extensions: ["gqf", "gqs"]
            },
            "application/vnd.gridmp": {
                source: "iana"
            },
            "application/vnd.groove-account": {
                source: "iana",
                extensions: ["gac"]
            },
            "application/vnd.groove-help": {
                source: "iana",
                extensions: ["ghf"]
            },
            "application/vnd.groove-identity-message": {
                source: "iana",
                extensions: ["gim"]
            },
            "application/vnd.groove-injector": {
                source: "iana",
                extensions: ["grv"]
            },
            "application/vnd.groove-tool-message": {
                source: "iana",
                extensions: ["gtm"]
            },
            "application/vnd.groove-tool-template": {
                source: "iana",
                extensions: ["tpl"]
            },
            "application/vnd.groove-vcard": {
                source: "iana",
                extensions: ["vcg"]
            },
            "application/vnd.hal+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.hal+xml": {
                source: "iana",
                compressible: true,
                extensions: ["hal"]
            },
            "application/vnd.handheld-entertainment+xml": {
                source: "iana",
                compressible: true,
                extensions: ["zmm"]
            },
            "application/vnd.hbci": {
                source: "iana",
                extensions: ["hbci"]
            },
            "application/vnd.hc+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.hcl-bireports": {
                source: "iana"
            },
            "application/vnd.hdt": {
                source: "iana"
            },
            "application/vnd.heroku+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.hhe.lesson-player": {
                source: "iana",
                extensions: ["les"]
            },
            "application/vnd.hl7cda+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/vnd.hl7v2+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/vnd.hp-hpgl": {
                source: "iana",
                extensions: ["hpgl"]
            },
            "application/vnd.hp-hpid": {
                source: "iana",
                extensions: ["hpid"]
            },
            "application/vnd.hp-hps": {
                source: "iana",
                extensions: ["hps"]
            },
            "application/vnd.hp-jlyt": {
                source: "iana",
                extensions: ["jlt"]
            },
            "application/vnd.hp-pcl": {
                source: "iana",
                extensions: ["pcl"]
            },
            "application/vnd.hp-pclxl": {
                source: "iana",
                extensions: ["pclxl"]
            },
            "application/vnd.httphone": {
                source: "iana"
            },
            "application/vnd.hydrostatix.sof-data": {
                source: "iana",
                extensions: ["sfd-hdstx"]
            },
            "application/vnd.hyper+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.hyper-item+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.hyperdrive+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.hzn-3d-crossword": {
                source: "iana"
            },
            "application/vnd.ibm.afplinedata": {
                source: "iana"
            },
            "application/vnd.ibm.electronic-media": {
                source: "iana"
            },
            "application/vnd.ibm.minipay": {
                source: "iana",
                extensions: ["mpy"]
            },
            "application/vnd.ibm.modcap": {
                source: "iana",
                extensions: ["afp", "listafp", "list3820"]
            },
            "application/vnd.ibm.rights-management": {
                source: "iana",
                extensions: ["irm"]
            },
            "application/vnd.ibm.secure-container": {
                source: "iana",
                extensions: ["sc"]
            },
            "application/vnd.iccprofile": {
                source: "iana",
                extensions: ["icc", "icm"]
            },
            "application/vnd.ieee.1905": {
                source: "iana"
            },
            "application/vnd.igloader": {
                source: "iana",
                extensions: ["igl"]
            },
            "application/vnd.imagemeter.folder+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.imagemeter.image+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.immervision-ivp": {
                source: "iana",
                extensions: ["ivp"]
            },
            "application/vnd.immervision-ivu": {
                source: "iana",
                extensions: ["ivu"]
            },
            "application/vnd.ims.imsccv1p1": {
                source: "iana"
            },
            "application/vnd.ims.imsccv1p2": {
                source: "iana"
            },
            "application/vnd.ims.imsccv1p3": {
                source: "iana"
            },
            "application/vnd.ims.lis.v2.result+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ims.lti.v2.toolconsumerprofile+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ims.lti.v2.toolproxy+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ims.lti.v2.toolproxy.id+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ims.lti.v2.toolsettings+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ims.lti.v2.toolsettings.simple+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.informedcontrol.rms+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.informix-visionary": {
                source: "iana"
            },
            "application/vnd.infotech.project": {
                source: "iana"
            },
            "application/vnd.infotech.project+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.innopath.wamp.notification": {
                source: "iana"
            },
            "application/vnd.insors.igm": {
                source: "iana",
                extensions: ["igm"]
            },
            "application/vnd.intercon.formnet": {
                source: "iana",
                extensions: ["xpw", "xpx"]
            },
            "application/vnd.intergeo": {
                source: "iana",
                extensions: ["i2g"]
            },
            "application/vnd.intertrust.digibox": {
                source: "iana"
            },
            "application/vnd.intertrust.nncp": {
                source: "iana"
            },
            "application/vnd.intu.qbo": {
                source: "iana",
                extensions: ["qbo"]
            },
            "application/vnd.intu.qfx": {
                source: "iana",
                extensions: ["qfx"]
            },
            "application/vnd.iptc.g2.catalogitem+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.iptc.g2.conceptitem+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.iptc.g2.knowledgeitem+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.iptc.g2.newsitem+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.iptc.g2.newsmessage+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.iptc.g2.packageitem+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.iptc.g2.planningitem+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ipunplugged.rcprofile": {
                source: "iana",
                extensions: ["rcprofile"]
            },
            "application/vnd.irepository.package+xml": {
                source: "iana",
                compressible: true,
                extensions: ["irp"]
            },
            "application/vnd.is-xpr": {
                source: "iana",
                extensions: ["xpr"]
            },
            "application/vnd.isac.fcs": {
                source: "iana",
                extensions: ["fcs"]
            },
            "application/vnd.iso11783-10+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.jam": {
                source: "iana",
                extensions: ["jam"]
            },
            "application/vnd.japannet-directory-service": {
                source: "iana"
            },
            "application/vnd.japannet-jpnstore-wakeup": {
                source: "iana"
            },
            "application/vnd.japannet-payment-wakeup": {
                source: "iana"
            },
            "application/vnd.japannet-registration": {
                source: "iana"
            },
            "application/vnd.japannet-registration-wakeup": {
                source: "iana"
            },
            "application/vnd.japannet-setstore-wakeup": {
                source: "iana"
            },
            "application/vnd.japannet-verification": {
                source: "iana"
            },
            "application/vnd.japannet-verification-wakeup": {
                source: "iana"
            },
            "application/vnd.jcp.javame.midlet-rms": {
                source: "iana",
                extensions: ["rms"]
            },
            "application/vnd.jisp": {
                source: "iana",
                extensions: ["jisp"]
            },
            "application/vnd.joost.joda-archive": {
                source: "iana",
                extensions: ["joda"]
            },
            "application/vnd.jsk.isdn-ngn": {
                source: "iana"
            },
            "application/vnd.kahootz": {
                source: "iana",
                extensions: ["ktz", "ktr"]
            },
            "application/vnd.kde.karbon": {
                source: "iana",
                extensions: ["karbon"]
            },
            "application/vnd.kde.kchart": {
                source: "iana",
                extensions: ["chrt"]
            },
            "application/vnd.kde.kformula": {
                source: "iana",
                extensions: ["kfo"]
            },
            "application/vnd.kde.kivio": {
                source: "iana",
                extensions: ["flw"]
            },
            "application/vnd.kde.kontour": {
                source: "iana",
                extensions: ["kon"]
            },
            "application/vnd.kde.kpresenter": {
                source: "iana",
                extensions: ["kpr", "kpt"]
            },
            "application/vnd.kde.kspread": {
                source: "iana",
                extensions: ["ksp"]
            },
            "application/vnd.kde.kword": {
                source: "iana",
                extensions: ["kwd", "kwt"]
            },
            "application/vnd.kenameaapp": {
                source: "iana",
                extensions: ["htke"]
            },
            "application/vnd.kidspiration": {
                source: "iana",
                extensions: ["kia"]
            },
            "application/vnd.kinar": {
                source: "iana",
                extensions: ["kne", "knp"]
            },
            "application/vnd.koan": {
                source: "iana",
                extensions: ["skp", "skd", "skt", "skm"]
            },
            "application/vnd.kodak-descriptor": {
                source: "iana",
                extensions: ["sse"]
            },
            "application/vnd.las": {
                source: "iana"
            },
            "application/vnd.las.las+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.las.las+xml": {
                source: "iana",
                compressible: true,
                extensions: ["lasxml"]
            },
            "application/vnd.laszip": {
                source: "iana"
            },
            "application/vnd.leap+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.liberty-request+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.llamagraphics.life-balance.desktop": {
                source: "iana",
                extensions: ["lbd"]
            },
            "application/vnd.llamagraphics.life-balance.exchange+xml": {
                source: "iana",
                compressible: true,
                extensions: ["lbe"]
            },
            "application/vnd.logipipe.circuit+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.loom": {
                source: "iana"
            },
            "application/vnd.lotus-1-2-3": {
                source: "iana",
                extensions: ["123"]
            },
            "application/vnd.lotus-approach": {
                source: "iana",
                extensions: ["apr"]
            },
            "application/vnd.lotus-freelance": {
                source: "iana",
                extensions: ["pre"]
            },
            "application/vnd.lotus-notes": {
                source: "iana",
                extensions: ["nsf"]
            },
            "application/vnd.lotus-organizer": {
                source: "iana",
                extensions: ["org"]
            },
            "application/vnd.lotus-screencam": {
                source: "iana",
                extensions: ["scm"]
            },
            "application/vnd.lotus-wordpro": {
                source: "iana",
                extensions: ["lwp"]
            },
            "application/vnd.macports.portpkg": {
                source: "iana",
                extensions: ["portpkg"]
            },
            "application/vnd.mapbox-vector-tile": {
                source: "iana",
                extensions: ["mvt"]
            },
            "application/vnd.marlin.drm.actiontoken+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.marlin.drm.conftoken+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.marlin.drm.license+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.marlin.drm.mdcf": {
                source: "iana"
            },
            "application/vnd.mason+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.maxar.archive.3tz+zip": {
                source: "iana",
                compressible: false
            },
            "application/vnd.maxmind.maxmind-db": {
                source: "iana"
            },
            "application/vnd.mcd": {
                source: "iana",
                extensions: ["mcd"]
            },
            "application/vnd.medcalcdata": {
                source: "iana",
                extensions: ["mc1"]
            },
            "application/vnd.mediastation.cdkey": {
                source: "iana",
                extensions: ["cdkey"]
            },
            "application/vnd.meridian-slingshot": {
                source: "iana"
            },
            "application/vnd.mfer": {
                source: "iana",
                extensions: ["mwf"]
            },
            "application/vnd.mfmp": {
                source: "iana",
                extensions: ["mfm"]
            },
            "application/vnd.micro+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.micrografx.flo": {
                source: "iana",
                extensions: ["flo"]
            },
            "application/vnd.micrografx.igx": {
                source: "iana",
                extensions: ["igx"]
            },
            "application/vnd.microsoft.portable-executable": {
                source: "iana"
            },
            "application/vnd.microsoft.windows.thumbnail-cache": {
                source: "iana"
            },
            "application/vnd.miele+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.mif": {
                source: "iana",
                extensions: ["mif"]
            },
            "application/vnd.minisoft-hp3000-save": {
                source: "iana"
            },
            "application/vnd.mitsubishi.misty-guard.trustweb": {
                source: "iana"
            },
            "application/vnd.mobius.daf": {
                source: "iana",
                extensions: ["daf"]
            },
            "application/vnd.mobius.dis": {
                source: "iana",
                extensions: ["dis"]
            },
            "application/vnd.mobius.mbk": {
                source: "iana",
                extensions: ["mbk"]
            },
            "application/vnd.mobius.mqy": {
                source: "iana",
                extensions: ["mqy"]
            },
            "application/vnd.mobius.msl": {
                source: "iana",
                extensions: ["msl"]
            },
            "application/vnd.mobius.plc": {
                source: "iana",
                extensions: ["plc"]
            },
            "application/vnd.mobius.txf": {
                source: "iana",
                extensions: ["txf"]
            },
            "application/vnd.mophun.application": {
                source: "iana",
                extensions: ["mpn"]
            },
            "application/vnd.mophun.certificate": {
                source: "iana",
                extensions: ["mpc"]
            },
            "application/vnd.motorola.flexsuite": {
                source: "iana"
            },
            "application/vnd.motorola.flexsuite.adsi": {
                source: "iana"
            },
            "application/vnd.motorola.flexsuite.fis": {
                source: "iana"
            },
            "application/vnd.motorola.flexsuite.gotap": {
                source: "iana"
            },
            "application/vnd.motorola.flexsuite.kmr": {
                source: "iana"
            },
            "application/vnd.motorola.flexsuite.ttc": {
                source: "iana"
            },
            "application/vnd.motorola.flexsuite.wem": {
                source: "iana"
            },
            "application/vnd.motorola.iprm": {
                source: "iana"
            },
            "application/vnd.mozilla.xul+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xul"]
            },
            "application/vnd.ms-3mfdocument": {
                source: "iana"
            },
            "application/vnd.ms-artgalry": {
                source: "iana",
                extensions: ["cil"]
            },
            "application/vnd.ms-asf": {
                source: "iana"
            },
            "application/vnd.ms-cab-compressed": {
                source: "iana",
                extensions: ["cab"]
            },
            "application/vnd.ms-color.iccprofile": {
                source: "apache"
            },
            "application/vnd.ms-excel": {
                source: "iana",
                compressible: false,
                extensions: ["xls", "xlm", "xla", "xlc", "xlt", "xlw"]
            },
            "application/vnd.ms-excel.addin.macroenabled.12": {
                source: "iana",
                extensions: ["xlam"]
            },
            "application/vnd.ms-excel.sheet.binary.macroenabled.12": {
                source: "iana",
                extensions: ["xlsb"]
            },
            "application/vnd.ms-excel.sheet.macroenabled.12": {
                source: "iana",
                extensions: ["xlsm"]
            },
            "application/vnd.ms-excel.template.macroenabled.12": {
                source: "iana",
                extensions: ["xltm"]
            },
            "application/vnd.ms-fontobject": {
                source: "iana",
                compressible: true,
                extensions: ["eot"]
            },
            "application/vnd.ms-htmlhelp": {
                source: "iana",
                extensions: ["chm"]
            },
            "application/vnd.ms-ims": {
                source: "iana",
                extensions: ["ims"]
            },
            "application/vnd.ms-lrm": {
                source: "iana",
                extensions: ["lrm"]
            },
            "application/vnd.ms-office.activex+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ms-officetheme": {
                source: "iana",
                extensions: ["thmx"]
            },
            "application/vnd.ms-opentype": {
                source: "apache",
                compressible: true
            },
            "application/vnd.ms-outlook": {
                compressible: false,
                extensions: ["msg"]
            },
            "application/vnd.ms-package.obfuscated-opentype": {
                source: "apache"
            },
            "application/vnd.ms-pki.seccat": {
                source: "apache",
                extensions: ["cat"]
            },
            "application/vnd.ms-pki.stl": {
                source: "apache",
                extensions: ["stl"]
            },
            "application/vnd.ms-playready.initiator+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ms-powerpoint": {
                source: "iana",
                compressible: false,
                extensions: ["ppt", "pps", "pot"]
            },
            "application/vnd.ms-powerpoint.addin.macroenabled.12": {
                source: "iana",
                extensions: ["ppam"]
            },
            "application/vnd.ms-powerpoint.presentation.macroenabled.12": {
                source: "iana",
                extensions: ["pptm"]
            },
            "application/vnd.ms-powerpoint.slide.macroenabled.12": {
                source: "iana",
                extensions: ["sldm"]
            },
            "application/vnd.ms-powerpoint.slideshow.macroenabled.12": {
                source: "iana",
                extensions: ["ppsm"]
            },
            "application/vnd.ms-powerpoint.template.macroenabled.12": {
                source: "iana",
                extensions: ["potm"]
            },
            "application/vnd.ms-printdevicecapabilities+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ms-printing.printticket+xml": {
                source: "apache",
                compressible: true
            },
            "application/vnd.ms-printschematicket+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ms-project": {
                source: "iana",
                extensions: ["mpp", "mpt"]
            },
            "application/vnd.ms-tnef": {
                source: "iana"
            },
            "application/vnd.ms-windows.devicepairing": {
                source: "iana"
            },
            "application/vnd.ms-windows.nwprinting.oob": {
                source: "iana"
            },
            "application/vnd.ms-windows.printerpairing": {
                source: "iana"
            },
            "application/vnd.ms-windows.wsd.oob": {
                source: "iana"
            },
            "application/vnd.ms-wmdrm.lic-chlg-req": {
                source: "iana"
            },
            "application/vnd.ms-wmdrm.lic-resp": {
                source: "iana"
            },
            "application/vnd.ms-wmdrm.meter-chlg-req": {
                source: "iana"
            },
            "application/vnd.ms-wmdrm.meter-resp": {
                source: "iana"
            },
            "application/vnd.ms-word.document.macroenabled.12": {
                source: "iana",
                extensions: ["docm"]
            },
            "application/vnd.ms-word.template.macroenabled.12": {
                source: "iana",
                extensions: ["dotm"]
            },
            "application/vnd.ms-works": {
                source: "iana",
                extensions: ["wps", "wks", "wcm", "wdb"]
            },
            "application/vnd.ms-wpl": {
                source: "iana",
                extensions: ["wpl"]
            },
            "application/vnd.ms-xpsdocument": {
                source: "iana",
                compressible: false,
                extensions: ["xps"]
            },
            "application/vnd.msa-disk-image": {
                source: "iana"
            },
            "application/vnd.mseq": {
                source: "iana",
                extensions: ["mseq"]
            },
            "application/vnd.msign": {
                source: "iana"
            },
            "application/vnd.multiad.creator": {
                source: "iana"
            },
            "application/vnd.multiad.creator.cif": {
                source: "iana"
            },
            "application/vnd.music-niff": {
                source: "iana"
            },
            "application/vnd.musician": {
                source: "iana",
                extensions: ["mus"]
            },
            "application/vnd.muvee.style": {
                source: "iana",
                extensions: ["msty"]
            },
            "application/vnd.mynfc": {
                source: "iana",
                extensions: ["taglet"]
            },
            "application/vnd.nacamar.ybrid+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.ncd.control": {
                source: "iana"
            },
            "application/vnd.ncd.reference": {
                source: "iana"
            },
            "application/vnd.nearst.inv+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.nebumind.line": {
                source: "iana"
            },
            "application/vnd.nervana": {
                source: "iana"
            },
            "application/vnd.netfpx": {
                source: "iana"
            },
            "application/vnd.neurolanguage.nlu": {
                source: "iana",
                extensions: ["nlu"]
            },
            "application/vnd.nimn": {
                source: "iana"
            },
            "application/vnd.nintendo.nitro.rom": {
                source: "iana"
            },
            "application/vnd.nintendo.snes.rom": {
                source: "iana"
            },
            "application/vnd.nitf": {
                source: "iana",
                extensions: ["ntf", "nitf"]
            },
            "application/vnd.noblenet-directory": {
                source: "iana",
                extensions: ["nnd"]
            },
            "application/vnd.noblenet-sealer": {
                source: "iana",
                extensions: ["nns"]
            },
            "application/vnd.noblenet-web": {
                source: "iana",
                extensions: ["nnw"]
            },
            "application/vnd.nokia.catalogs": {
                source: "iana"
            },
            "application/vnd.nokia.conml+wbxml": {
                source: "iana"
            },
            "application/vnd.nokia.conml+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.nokia.iptv.config+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.nokia.isds-radio-presets": {
                source: "iana"
            },
            "application/vnd.nokia.landmark+wbxml": {
                source: "iana"
            },
            "application/vnd.nokia.landmark+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.nokia.landmarkcollection+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.nokia.n-gage.ac+xml": {
                source: "iana",
                compressible: true,
                extensions: ["ac"]
            },
            "application/vnd.nokia.n-gage.data": {
                source: "iana",
                extensions: ["ngdat"]
            },
            "application/vnd.nokia.n-gage.symbian.install": {
                source: "iana",
                extensions: ["n-gage"]
            },
            "application/vnd.nokia.ncd": {
                source: "iana"
            },
            "application/vnd.nokia.pcd+wbxml": {
                source: "iana"
            },
            "application/vnd.nokia.pcd+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.nokia.radio-preset": {
                source: "iana",
                extensions: ["rpst"]
            },
            "application/vnd.nokia.radio-presets": {
                source: "iana",
                extensions: ["rpss"]
            },
            "application/vnd.novadigm.edm": {
                source: "iana",
                extensions: ["edm"]
            },
            "application/vnd.novadigm.edx": {
                source: "iana",
                extensions: ["edx"]
            },
            "application/vnd.novadigm.ext": {
                source: "iana",
                extensions: ["ext"]
            },
            "application/vnd.ntt-local.content-share": {
                source: "iana"
            },
            "application/vnd.ntt-local.file-transfer": {
                source: "iana"
            },
            "application/vnd.ntt-local.ogw_remote-access": {
                source: "iana"
            },
            "application/vnd.ntt-local.sip-ta_remote": {
                source: "iana"
            },
            "application/vnd.ntt-local.sip-ta_tcp_stream": {
                source: "iana"
            },
            "application/vnd.oasis.opendocument.chart": {
                source: "iana",
                extensions: ["odc"]
            },
            "application/vnd.oasis.opendocument.chart-template": {
                source: "iana",
                extensions: ["otc"]
            },
            "application/vnd.oasis.opendocument.database": {
                source: "iana",
                extensions: ["odb"]
            },
            "application/vnd.oasis.opendocument.formula": {
                source: "iana",
                extensions: ["odf"]
            },
            "application/vnd.oasis.opendocument.formula-template": {
                source: "iana",
                extensions: ["odft"]
            },
            "application/vnd.oasis.opendocument.graphics": {
                source: "iana",
                compressible: false,
                extensions: ["odg"]
            },
            "application/vnd.oasis.opendocument.graphics-template": {
                source: "iana",
                extensions: ["otg"]
            },
            "application/vnd.oasis.opendocument.image": {
                source: "iana",
                extensions: ["odi"]
            },
            "application/vnd.oasis.opendocument.image-template": {
                source: "iana",
                extensions: ["oti"]
            },
            "application/vnd.oasis.opendocument.presentation": {
                source: "iana",
                compressible: false,
                extensions: ["odp"]
            },
            "application/vnd.oasis.opendocument.presentation-template": {
                source: "iana",
                extensions: ["otp"]
            },
            "application/vnd.oasis.opendocument.spreadsheet": {
                source: "iana",
                compressible: false,
                extensions: ["ods"]
            },
            "application/vnd.oasis.opendocument.spreadsheet-template": {
                source: "iana",
                extensions: ["ots"]
            },
            "application/vnd.oasis.opendocument.text": {
                source: "iana",
                compressible: false,
                extensions: ["odt"]
            },
            "application/vnd.oasis.opendocument.text-master": {
                source: "iana",
                extensions: ["odm"]
            },
            "application/vnd.oasis.opendocument.text-template": {
                source: "iana",
                extensions: ["ott"]
            },
            "application/vnd.oasis.opendocument.text-web": {
                source: "iana",
                extensions: ["oth"]
            },
            "application/vnd.obn": {
                source: "iana"
            },
            "application/vnd.ocf+cbor": {
                source: "iana"
            },
            "application/vnd.oci.image.manifest.v1+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oftn.l10n+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oipf.contentaccessdownload+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oipf.contentaccessstreaming+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oipf.cspg-hexbinary": {
                source: "iana"
            },
            "application/vnd.oipf.dae.svg+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oipf.dae.xhtml+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oipf.mippvcontrolmessage+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oipf.pae.gem": {
                source: "iana"
            },
            "application/vnd.oipf.spdiscovery+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oipf.spdlist+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oipf.ueprofile+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oipf.userprofile+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.olpc-sugar": {
                source: "iana",
                extensions: ["xo"]
            },
            "application/vnd.oma-scws-config": {
                source: "iana"
            },
            "application/vnd.oma-scws-http-request": {
                source: "iana"
            },
            "application/vnd.oma-scws-http-response": {
                source: "iana"
            },
            "application/vnd.oma.bcast.associated-procedure-parameter+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.bcast.drm-trigger+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.bcast.imd+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.bcast.ltkm": {
                source: "iana"
            },
            "application/vnd.oma.bcast.notification+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.bcast.provisioningtrigger": {
                source: "iana"
            },
            "application/vnd.oma.bcast.sgboot": {
                source: "iana"
            },
            "application/vnd.oma.bcast.sgdd+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.bcast.sgdu": {
                source: "iana"
            },
            "application/vnd.oma.bcast.simple-symbol-container": {
                source: "iana"
            },
            "application/vnd.oma.bcast.smartcard-trigger+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.bcast.sprov+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.bcast.stkm": {
                source: "iana"
            },
            "application/vnd.oma.cab-address-book+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.cab-feature-handler+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.cab-pcc+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.cab-subs-invite+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.cab-user-prefs+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.dcd": {
                source: "iana"
            },
            "application/vnd.oma.dcdc": {
                source: "iana"
            },
            "application/vnd.oma.dd2+xml": {
                source: "iana",
                compressible: true,
                extensions: ["dd2"]
            },
            "application/vnd.oma.drm.risd+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.group-usage-list+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.lwm2m+cbor": {
                source: "iana"
            },
            "application/vnd.oma.lwm2m+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.lwm2m+tlv": {
                source: "iana"
            },
            "application/vnd.oma.pal+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.poc.detailed-progress-report+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.poc.final-report+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.poc.groups+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.poc.invocation-descriptor+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.poc.optimized-progress-report+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.push": {
                source: "iana"
            },
            "application/vnd.oma.scidm.messages+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oma.xcap-directory+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.omads-email+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/vnd.omads-file+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/vnd.omads-folder+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/vnd.omaloc-supl-init": {
                source: "iana"
            },
            "application/vnd.onepager": {
                source: "iana"
            },
            "application/vnd.onepagertamp": {
                source: "iana"
            },
            "application/vnd.onepagertamx": {
                source: "iana"
            },
            "application/vnd.onepagertat": {
                source: "iana"
            },
            "application/vnd.onepagertatp": {
                source: "iana"
            },
            "application/vnd.onepagertatx": {
                source: "iana"
            },
            "application/vnd.openblox.game+xml": {
                source: "iana",
                compressible: true,
                extensions: ["obgx"]
            },
            "application/vnd.openblox.game-binary": {
                source: "iana"
            },
            "application/vnd.openeye.oeb": {
                source: "iana"
            },
            "application/vnd.openofficeorg.extension": {
                source: "apache",
                extensions: ["oxt"]
            },
            "application/vnd.openstreetmap.data+xml": {
                source: "iana",
                compressible: true,
                extensions: ["osm"]
            },
            "application/vnd.opentimestamps.ots": {
                source: "iana"
            },
            "application/vnd.openxmlformats-officedocument.custom-properties+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.customxmlproperties+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.drawing+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.drawingml.chart+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.drawingml.chartshapes+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.drawingml.diagramcolors+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.drawingml.diagramdata+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.drawingml.diagramlayout+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.drawingml.diagramstyle+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.extended-properties+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.commentauthors+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.comments+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.handoutmaster+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.notesmaster+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.notesslide+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.presentation": {
                source: "iana",
                compressible: false,
                extensions: ["pptx"]
            },
            "application/vnd.openxmlformats-officedocument.presentationml.presentation.main+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.presprops+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.slide": {
                source: "iana",
                extensions: ["sldx"]
            },
            "application/vnd.openxmlformats-officedocument.presentationml.slide+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.slidelayout+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.slidemaster+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.slideshow": {
                source: "iana",
                extensions: ["ppsx"]
            },
            "application/vnd.openxmlformats-officedocument.presentationml.slideshow.main+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.slideupdateinfo+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.tablestyles+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.tags+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.template": {
                source: "iana",
                extensions: ["potx"]
            },
            "application/vnd.openxmlformats-officedocument.presentationml.template.main+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.presentationml.viewprops+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.calcchain+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.chartsheet+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.comments+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.connections+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.dialogsheet+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.externallink+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.pivotcachedefinition+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.pivotcacherecords+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.pivottable+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.querytable+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.revisionheaders+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.revisionlog+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.sharedstrings+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": {
                source: "iana",
                compressible: false,
                extensions: ["xlsx"]
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.sheetmetadata+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.styles+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.table+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.tablesinglecells+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.template": {
                source: "iana",
                extensions: ["xltx"]
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.template.main+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.usernames+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.volatiledependencies+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.theme+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.themeoverride+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.vmldrawing": {
                source: "iana"
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.comments+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.document": {
                source: "iana",
                compressible: false,
                extensions: ["docx"]
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.document.glossary+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.document.main+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.endnotes+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.fonttable+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.footer+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.footnotes+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.numbering+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.settings+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.styles+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.template": {
                source: "iana",
                extensions: ["dotx"]
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.template.main+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-officedocument.wordprocessingml.websettings+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-package.core-properties+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-package.digital-signature-xmlsignature+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.openxmlformats-package.relationships+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oracle.resource+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.orange.indata": {
                source: "iana"
            },
            "application/vnd.osa.netdeploy": {
                source: "iana"
            },
            "application/vnd.osgeo.mapguide.package": {
                source: "iana",
                extensions: ["mgp"]
            },
            "application/vnd.osgi.bundle": {
                source: "iana"
            },
            "application/vnd.osgi.dp": {
                source: "iana",
                extensions: ["dp"]
            },
            "application/vnd.osgi.subsystem": {
                source: "iana",
                extensions: ["esa"]
            },
            "application/vnd.otps.ct-kip+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.oxli.countgraph": {
                source: "iana"
            },
            "application/vnd.pagerduty+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.palm": {
                source: "iana",
                extensions: ["pdb", "pqa", "oprc"]
            },
            "application/vnd.panoply": {
                source: "iana"
            },
            "application/vnd.paos.xml": {
                source: "iana"
            },
            "application/vnd.patentdive": {
                source: "iana"
            },
            "application/vnd.patientecommsdoc": {
                source: "iana"
            },
            "application/vnd.pawaafile": {
                source: "iana",
                extensions: ["paw"]
            },
            "application/vnd.pcos": {
                source: "iana"
            },
            "application/vnd.pg.format": {
                source: "iana",
                extensions: ["str"]
            },
            "application/vnd.pg.osasli": {
                source: "iana",
                extensions: ["ei6"]
            },
            "application/vnd.piaccess.application-licence": {
                source: "iana"
            },
            "application/vnd.picsel": {
                source: "iana",
                extensions: ["efif"]
            },
            "application/vnd.pmi.widget": {
                source: "iana",
                extensions: ["wg"]
            },
            "application/vnd.poc.group-advertisement+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.pocketlearn": {
                source: "iana",
                extensions: ["plf"]
            },
            "application/vnd.powerbuilder6": {
                source: "iana",
                extensions: ["pbd"]
            },
            "application/vnd.powerbuilder6-s": {
                source: "iana"
            },
            "application/vnd.powerbuilder7": {
                source: "iana"
            },
            "application/vnd.powerbuilder7-s": {
                source: "iana"
            },
            "application/vnd.powerbuilder75": {
                source: "iana"
            },
            "application/vnd.powerbuilder75-s": {
                source: "iana"
            },
            "application/vnd.preminet": {
                source: "iana"
            },
            "application/vnd.previewsystems.box": {
                source: "iana",
                extensions: ["box"]
            },
            "application/vnd.proteus.magazine": {
                source: "iana",
                extensions: ["mgz"]
            },
            "application/vnd.psfs": {
                source: "iana"
            },
            "application/vnd.publishare-delta-tree": {
                source: "iana",
                extensions: ["qps"]
            },
            "application/vnd.pvi.ptid1": {
                source: "iana",
                extensions: ["ptid"]
            },
            "application/vnd.pwg-multiplexed": {
                source: "iana"
            },
            "application/vnd.pwg-xhtml-print+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.qualcomm.brew-app-res": {
                source: "iana"
            },
            "application/vnd.quarantainenet": {
                source: "iana"
            },
            "application/vnd.quark.quarkxpress": {
                source: "iana",
                extensions: ["qxd", "qxt", "qwd", "qwt", "qxl", "qxb"]
            },
            "application/vnd.quobject-quoxdocument": {
                source: "iana"
            },
            "application/vnd.radisys.moml+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-audit+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-audit-conf+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-audit-conn+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-audit-dialog+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-audit-stream+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-conf+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-dialog+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-dialog-base+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-dialog-fax-detect+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-dialog-fax-sendrecv+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-dialog-group+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-dialog-speech+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.radisys.msml-dialog-transform+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.rainstor.data": {
                source: "iana"
            },
            "application/vnd.rapid": {
                source: "iana"
            },
            "application/vnd.rar": {
                source: "iana",
                extensions: ["rar"]
            },
            "application/vnd.realvnc.bed": {
                source: "iana",
                extensions: ["bed"]
            },
            "application/vnd.recordare.musicxml": {
                source: "iana",
                extensions: ["mxl"]
            },
            "application/vnd.recordare.musicxml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["musicxml"]
            },
            "application/vnd.renlearn.rlprint": {
                source: "iana"
            },
            "application/vnd.resilient.logic": {
                source: "iana"
            },
            "application/vnd.restful+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.rig.cryptonote": {
                source: "iana",
                extensions: ["cryptonote"]
            },
            "application/vnd.rim.cod": {
                source: "apache",
                extensions: ["cod"]
            },
            "application/vnd.rn-realmedia": {
                source: "apache",
                extensions: ["rm"]
            },
            "application/vnd.rn-realmedia-vbr": {
                source: "apache",
                extensions: ["rmvb"]
            },
            "application/vnd.route66.link66+xml": {
                source: "iana",
                compressible: true,
                extensions: ["link66"]
            },
            "application/vnd.rs-274x": {
                source: "iana"
            },
            "application/vnd.ruckus.download": {
                source: "iana"
            },
            "application/vnd.s3sms": {
                source: "iana"
            },
            "application/vnd.sailingtracker.track": {
                source: "iana",
                extensions: ["st"]
            },
            "application/vnd.sar": {
                source: "iana"
            },
            "application/vnd.sbm.cid": {
                source: "iana"
            },
            "application/vnd.sbm.mid2": {
                source: "iana"
            },
            "application/vnd.scribus": {
                source: "iana"
            },
            "application/vnd.sealed.3df": {
                source: "iana"
            },
            "application/vnd.sealed.csf": {
                source: "iana"
            },
            "application/vnd.sealed.doc": {
                source: "iana"
            },
            "application/vnd.sealed.eml": {
                source: "iana"
            },
            "application/vnd.sealed.mht": {
                source: "iana"
            },
            "application/vnd.sealed.net": {
                source: "iana"
            },
            "application/vnd.sealed.ppt": {
                source: "iana"
            },
            "application/vnd.sealed.tiff": {
                source: "iana"
            },
            "application/vnd.sealed.xls": {
                source: "iana"
            },
            "application/vnd.sealedmedia.softseal.html": {
                source: "iana"
            },
            "application/vnd.sealedmedia.softseal.pdf": {
                source: "iana"
            },
            "application/vnd.seemail": {
                source: "iana",
                extensions: ["see"]
            },
            "application/vnd.seis+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.sema": {
                source: "iana",
                extensions: ["sema"]
            },
            "application/vnd.semd": {
                source: "iana",
                extensions: ["semd"]
            },
            "application/vnd.semf": {
                source: "iana",
                extensions: ["semf"]
            },
            "application/vnd.shade-save-file": {
                source: "iana"
            },
            "application/vnd.shana.informed.formdata": {
                source: "iana",
                extensions: ["ifm"]
            },
            "application/vnd.shana.informed.formtemplate": {
                source: "iana",
                extensions: ["itp"]
            },
            "application/vnd.shana.informed.interchange": {
                source: "iana",
                extensions: ["iif"]
            },
            "application/vnd.shana.informed.package": {
                source: "iana",
                extensions: ["ipk"]
            },
            "application/vnd.shootproof+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.shopkick+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.shp": {
                source: "iana"
            },
            "application/vnd.shx": {
                source: "iana"
            },
            "application/vnd.sigrok.session": {
                source: "iana"
            },
            "application/vnd.simtech-mindmapper": {
                source: "iana",
                extensions: ["twd", "twds"]
            },
            "application/vnd.siren+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.smaf": {
                source: "iana",
                extensions: ["mmf"]
            },
            "application/vnd.smart.notebook": {
                source: "iana"
            },
            "application/vnd.smart.teacher": {
                source: "iana",
                extensions: ["teacher"]
            },
            "application/vnd.snesdev-page-table": {
                source: "iana"
            },
            "application/vnd.software602.filler.form+xml": {
                source: "iana",
                compressible: true,
                extensions: ["fo"]
            },
            "application/vnd.software602.filler.form-xml-zip": {
                source: "iana"
            },
            "application/vnd.solent.sdkm+xml": {
                source: "iana",
                compressible: true,
                extensions: ["sdkm", "sdkd"]
            },
            "application/vnd.spotfire.dxp": {
                source: "iana",
                extensions: ["dxp"]
            },
            "application/vnd.spotfire.sfs": {
                source: "iana",
                extensions: ["sfs"]
            },
            "application/vnd.sqlite3": {
                source: "iana"
            },
            "application/vnd.sss-cod": {
                source: "iana"
            },
            "application/vnd.sss-dtf": {
                source: "iana"
            },
            "application/vnd.sss-ntf": {
                source: "iana"
            },
            "application/vnd.stardivision.calc": {
                source: "apache",
                extensions: ["sdc"]
            },
            "application/vnd.stardivision.draw": {
                source: "apache",
                extensions: ["sda"]
            },
            "application/vnd.stardivision.impress": {
                source: "apache",
                extensions: ["sdd"]
            },
            "application/vnd.stardivision.math": {
                source: "apache",
                extensions: ["smf"]
            },
            "application/vnd.stardivision.writer": {
                source: "apache",
                extensions: ["sdw", "vor"]
            },
            "application/vnd.stardivision.writer-global": {
                source: "apache",
                extensions: ["sgl"]
            },
            "application/vnd.stepmania.package": {
                source: "iana",
                extensions: ["smzip"]
            },
            "application/vnd.stepmania.stepchart": {
                source: "iana",
                extensions: ["sm"]
            },
            "application/vnd.street-stream": {
                source: "iana"
            },
            "application/vnd.sun.wadl+xml": {
                source: "iana",
                compressible: true,
                extensions: ["wadl"]
            },
            "application/vnd.sun.xml.calc": {
                source: "apache",
                extensions: ["sxc"]
            },
            "application/vnd.sun.xml.calc.template": {
                source: "apache",
                extensions: ["stc"]
            },
            "application/vnd.sun.xml.draw": {
                source: "apache",
                extensions: ["sxd"]
            },
            "application/vnd.sun.xml.draw.template": {
                source: "apache",
                extensions: ["std"]
            },
            "application/vnd.sun.xml.impress": {
                source: "apache",
                extensions: ["sxi"]
            },
            "application/vnd.sun.xml.impress.template": {
                source: "apache",
                extensions: ["sti"]
            },
            "application/vnd.sun.xml.math": {
                source: "apache",
                extensions: ["sxm"]
            },
            "application/vnd.sun.xml.writer": {
                source: "apache",
                extensions: ["sxw"]
            },
            "application/vnd.sun.xml.writer.global": {
                source: "apache",
                extensions: ["sxg"]
            },
            "application/vnd.sun.xml.writer.template": {
                source: "apache",
                extensions: ["stw"]
            },
            "application/vnd.sus-calendar": {
                source: "iana",
                extensions: ["sus", "susp"]
            },
            "application/vnd.svd": {
                source: "iana",
                extensions: ["svd"]
            },
            "application/vnd.swiftview-ics": {
                source: "iana"
            },
            "application/vnd.sycle+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.syft+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.symbian.install": {
                source: "apache",
                extensions: ["sis", "sisx"]
            },
            "application/vnd.syncml+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true,
                extensions: ["xsm"]
            },
            "application/vnd.syncml.dm+wbxml": {
                source: "iana",
                charset: "UTF-8",
                extensions: ["bdm"]
            },
            "application/vnd.syncml.dm+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true,
                extensions: ["xdm"]
            },
            "application/vnd.syncml.dm.notification": {
                source: "iana"
            },
            "application/vnd.syncml.dmddf+wbxml": {
                source: "iana"
            },
            "application/vnd.syncml.dmddf+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true,
                extensions: ["ddf"]
            },
            "application/vnd.syncml.dmtnds+wbxml": {
                source: "iana"
            },
            "application/vnd.syncml.dmtnds+xml": {
                source: "iana",
                charset: "UTF-8",
                compressible: true
            },
            "application/vnd.syncml.ds.notification": {
                source: "iana"
            },
            "application/vnd.tableschema+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.tao.intent-module-archive": {
                source: "iana",
                extensions: ["tao"]
            },
            "application/vnd.tcpdump.pcap": {
                source: "iana",
                extensions: ["pcap", "cap", "dmp"]
            },
            "application/vnd.think-cell.ppttc+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.tmd.mediaflex.api+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.tml": {
                source: "iana"
            },
            "application/vnd.tmobile-livetv": {
                source: "iana",
                extensions: ["tmo"]
            },
            "application/vnd.tri.onesource": {
                source: "iana"
            },
            "application/vnd.trid.tpt": {
                source: "iana",
                extensions: ["tpt"]
            },
            "application/vnd.triscape.mxs": {
                source: "iana",
                extensions: ["mxs"]
            },
            "application/vnd.trueapp": {
                source: "iana",
                extensions: ["tra"]
            },
            "application/vnd.truedoc": {
                source: "iana"
            },
            "application/vnd.ubisoft.webplayer": {
                source: "iana"
            },
            "application/vnd.ufdl": {
                source: "iana",
                extensions: ["ufd", "ufdl"]
            },
            "application/vnd.uiq.theme": {
                source: "iana",
                extensions: ["utz"]
            },
            "application/vnd.umajin": {
                source: "iana",
                extensions: ["umj"]
            },
            "application/vnd.unity": {
                source: "iana",
                extensions: ["unityweb"]
            },
            "application/vnd.uoml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["uoml"]
            },
            "application/vnd.uplanet.alert": {
                source: "iana"
            },
            "application/vnd.uplanet.alert-wbxml": {
                source: "iana"
            },
            "application/vnd.uplanet.bearer-choice": {
                source: "iana"
            },
            "application/vnd.uplanet.bearer-choice-wbxml": {
                source: "iana"
            },
            "application/vnd.uplanet.cacheop": {
                source: "iana"
            },
            "application/vnd.uplanet.cacheop-wbxml": {
                source: "iana"
            },
            "application/vnd.uplanet.channel": {
                source: "iana"
            },
            "application/vnd.uplanet.channel-wbxml": {
                source: "iana"
            },
            "application/vnd.uplanet.list": {
                source: "iana"
            },
            "application/vnd.uplanet.list-wbxml": {
                source: "iana"
            },
            "application/vnd.uplanet.listcmd": {
                source: "iana"
            },
            "application/vnd.uplanet.listcmd-wbxml": {
                source: "iana"
            },
            "application/vnd.uplanet.signal": {
                source: "iana"
            },
            "application/vnd.uri-map": {
                source: "iana"
            },
            "application/vnd.valve.source.material": {
                source: "iana"
            },
            "application/vnd.vcx": {
                source: "iana",
                extensions: ["vcx"]
            },
            "application/vnd.vd-study": {
                source: "iana"
            },
            "application/vnd.vectorworks": {
                source: "iana"
            },
            "application/vnd.vel+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.verimatrix.vcas": {
                source: "iana"
            },
            "application/vnd.veritone.aion+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.veryant.thin": {
                source: "iana"
            },
            "application/vnd.ves.encrypted": {
                source: "iana"
            },
            "application/vnd.vidsoft.vidconference": {
                source: "iana"
            },
            "application/vnd.visio": {
                source: "iana",
                extensions: ["vsd", "vst", "vss", "vsw"]
            },
            "application/vnd.visionary": {
                source: "iana",
                extensions: ["vis"]
            },
            "application/vnd.vividence.scriptfile": {
                source: "iana"
            },
            "application/vnd.vsf": {
                source: "iana",
                extensions: ["vsf"]
            },
            "application/vnd.wap.sic": {
                source: "iana"
            },
            "application/vnd.wap.slc": {
                source: "iana"
            },
            "application/vnd.wap.wbxml": {
                source: "iana",
                charset: "UTF-8",
                extensions: ["wbxml"]
            },
            "application/vnd.wap.wmlc": {
                source: "iana",
                extensions: ["wmlc"]
            },
            "application/vnd.wap.wmlscriptc": {
                source: "iana",
                extensions: ["wmlsc"]
            },
            "application/vnd.webturbo": {
                source: "iana",
                extensions: ["wtb"]
            },
            "application/vnd.wfa.dpp": {
                source: "iana"
            },
            "application/vnd.wfa.p2p": {
                source: "iana"
            },
            "application/vnd.wfa.wsc": {
                source: "iana"
            },
            "application/vnd.windows.devicepairing": {
                source: "iana"
            },
            "application/vnd.wmc": {
                source: "iana"
            },
            "application/vnd.wmf.bootstrap": {
                source: "iana"
            },
            "application/vnd.wolfram.mathematica": {
                source: "iana"
            },
            "application/vnd.wolfram.mathematica.package": {
                source: "iana"
            },
            "application/vnd.wolfram.player": {
                source: "iana",
                extensions: ["nbp"]
            },
            "application/vnd.wordperfect": {
                source: "iana",
                extensions: ["wpd"]
            },
            "application/vnd.wqd": {
                source: "iana",
                extensions: ["wqd"]
            },
            "application/vnd.wrq-hp3000-labelled": {
                source: "iana"
            },
            "application/vnd.wt.stf": {
                source: "iana",
                extensions: ["stf"]
            },
            "application/vnd.wv.csp+wbxml": {
                source: "iana"
            },
            "application/vnd.wv.csp+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.wv.ssp+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.xacml+json": {
                source: "iana",
                compressible: true
            },
            "application/vnd.xara": {
                source: "iana",
                extensions: ["xar"]
            },
            "application/vnd.xfdl": {
                source: "iana",
                extensions: ["xfdl"]
            },
            "application/vnd.xfdl.webform": {
                source: "iana"
            },
            "application/vnd.xmi+xml": {
                source: "iana",
                compressible: true
            },
            "application/vnd.xmpie.cpkg": {
                source: "iana"
            },
            "application/vnd.xmpie.dpkg": {
                source: "iana"
            },
            "application/vnd.xmpie.plan": {
                source: "iana"
            },
            "application/vnd.xmpie.ppkg": {
                source: "iana"
            },
            "application/vnd.xmpie.xlim": {
                source: "iana"
            },
            "application/vnd.yamaha.hv-dic": {
                source: "iana",
                extensions: ["hvd"]
            },
            "application/vnd.yamaha.hv-script": {
                source: "iana",
                extensions: ["hvs"]
            },
            "application/vnd.yamaha.hv-voice": {
                source: "iana",
                extensions: ["hvp"]
            },
            "application/vnd.yamaha.openscoreformat": {
                source: "iana",
                extensions: ["osf"]
            },
            "application/vnd.yamaha.openscoreformat.osfpvg+xml": {
                source: "iana",
                compressible: true,
                extensions: ["osfpvg"]
            },
            "application/vnd.yamaha.remote-setup": {
                source: "iana"
            },
            "application/vnd.yamaha.smaf-audio": {
                source: "iana",
                extensions: ["saf"]
            },
            "application/vnd.yamaha.smaf-phrase": {
                source: "iana",
                extensions: ["spf"]
            },
            "application/vnd.yamaha.through-ngn": {
                source: "iana"
            },
            "application/vnd.yamaha.tunnel-udpencap": {
                source: "iana"
            },
            "application/vnd.yaoweme": {
                source: "iana"
            },
            "application/vnd.yellowriver-custom-menu": {
                source: "iana",
                extensions: ["cmp"]
            },
            "application/vnd.youtube.yt": {
                source: "iana"
            },
            "application/vnd.zul": {
                source: "iana",
                extensions: ["zir", "zirz"]
            },
            "application/vnd.zzazz.deck+xml": {
                source: "iana",
                compressible: true,
                extensions: ["zaz"]
            },
            "application/voicexml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["vxml"]
            },
            "application/voucher-cms+json": {
                source: "iana",
                compressible: true
            },
            "application/vq-rtcpxr": {
                source: "iana"
            },
            "application/wasm": {
                source: "iana",
                compressible: true,
                extensions: ["wasm"]
            },
            "application/watcherinfo+xml": {
                source: "iana",
                compressible: true,
                extensions: ["wif"]
            },
            "application/webpush-options+json": {
                source: "iana",
                compressible: true
            },
            "application/whoispp-query": {
                source: "iana"
            },
            "application/whoispp-response": {
                source: "iana"
            },
            "application/widget": {
                source: "iana",
                extensions: ["wgt"]
            },
            "application/winhlp": {
                source: "apache",
                extensions: ["hlp"]
            },
            "application/wita": {
                source: "iana"
            },
            "application/wordperfect5.1": {
                source: "iana"
            },
            "application/wsdl+xml": {
                source: "iana",
                compressible: true,
                extensions: ["wsdl"]
            },
            "application/wspolicy+xml": {
                source: "iana",
                compressible: true,
                extensions: ["wspolicy"]
            },
            "application/x-7z-compressed": {
                source: "apache",
                compressible: false,
                extensions: ["7z"]
            },
            "application/x-abiword": {
                source: "apache",
                extensions: ["abw"]
            },
            "application/x-ace-compressed": {
                source: "apache",
                extensions: ["ace"]
            },
            "application/x-amf": {
                source: "apache"
            },
            "application/x-apple-diskimage": {
                source: "apache",
                extensions: ["dmg"]
            },
            "application/x-arj": {
                compressible: false,
                extensions: ["arj"]
            },
            "application/x-authorware-bin": {
                source: "apache",
                extensions: ["aab", "x32", "u32", "vox"]
            },
            "application/x-authorware-map": {
                source: "apache",
                extensions: ["aam"]
            },
            "application/x-authorware-seg": {
                source: "apache",
                extensions: ["aas"]
            },
            "application/x-bcpio": {
                source: "apache",
                extensions: ["bcpio"]
            },
            "application/x-bdoc": {
                compressible: false,
                extensions: ["bdoc"]
            },
            "application/x-bittorrent": {
                source: "apache",
                extensions: ["torrent"]
            },
            "application/x-blorb": {
                source: "apache",
                extensions: ["blb", "blorb"]
            },
            "application/x-bzip": {
                source: "apache",
                compressible: false,
                extensions: ["bz"]
            },
            "application/x-bzip2": {
                source: "apache",
                compressible: false,
                extensions: ["bz2", "boz"]
            },
            "application/x-cbr": {
                source: "apache",
                extensions: ["cbr", "cba", "cbt", "cbz", "cb7"]
            },
            "application/x-cdlink": {
                source: "apache",
                extensions: ["vcd"]
            },
            "application/x-cfs-compressed": {
                source: "apache",
                extensions: ["cfs"]
            },
            "application/x-chat": {
                source: "apache",
                extensions: ["chat"]
            },
            "application/x-chess-pgn": {
                source: "apache",
                extensions: ["pgn"]
            },
            "application/x-chrome-extension": {
                extensions: ["crx"]
            },
            "application/x-cocoa": {
                source: "nginx",
                extensions: ["cco"]
            },
            "application/x-compress": {
                source: "apache"
            },
            "application/x-conference": {
                source: "apache",
                extensions: ["nsc"]
            },
            "application/x-cpio": {
                source: "apache",
                extensions: ["cpio"]
            },
            "application/x-csh": {
                source: "apache",
                extensions: ["csh"]
            },
            "application/x-deb": {
                compressible: false
            },
            "application/x-debian-package": {
                source: "apache",
                extensions: ["deb", "udeb"]
            },
            "application/x-dgc-compressed": {
                source: "apache",
                extensions: ["dgc"]
            },
            "application/x-director": {
                source: "apache",
                extensions: ["dir", "dcr", "dxr", "cst", "cct", "cxt", "w3d", "fgd", "swa"]
            },
            "application/x-doom": {
                source: "apache",
                extensions: ["wad"]
            },
            "application/x-dtbncx+xml": {
                source: "apache",
                compressible: true,
                extensions: ["ncx"]
            },
            "application/x-dtbook+xml": {
                source: "apache",
                compressible: true,
                extensions: ["dtb"]
            },
            "application/x-dtbresource+xml": {
                source: "apache",
                compressible: true,
                extensions: ["res"]
            },
            "application/x-dvi": {
                source: "apache",
                compressible: false,
                extensions: ["dvi"]
            },
            "application/x-envoy": {
                source: "apache",
                extensions: ["evy"]
            },
            "application/x-eva": {
                source: "apache",
                extensions: ["eva"]
            },
            "application/x-font-bdf": {
                source: "apache",
                extensions: ["bdf"]
            },
            "application/x-font-dos": {
                source: "apache"
            },
            "application/x-font-framemaker": {
                source: "apache"
            },
            "application/x-font-ghostscript": {
                source: "apache",
                extensions: ["gsf"]
            },
            "application/x-font-libgrx": {
                source: "apache"
            },
            "application/x-font-linux-psf": {
                source: "apache",
                extensions: ["psf"]
            },
            "application/x-font-pcf": {
                source: "apache",
                extensions: ["pcf"]
            },
            "application/x-font-snf": {
                source: "apache",
                extensions: ["snf"]
            },
            "application/x-font-speedo": {
                source: "apache"
            },
            "application/x-font-sunos-news": {
                source: "apache"
            },
            "application/x-font-type1": {
                source: "apache",
                extensions: ["pfa", "pfb", "pfm", "afm"]
            },
            "application/x-font-vfont": {
                source: "apache"
            },
            "application/x-freearc": {
                source: "apache",
                extensions: ["arc"]
            },
            "application/x-futuresplash": {
                source: "apache",
                extensions: ["spl"]
            },
            "application/x-gca-compressed": {
                source: "apache",
                extensions: ["gca"]
            },
            "application/x-glulx": {
                source: "apache",
                extensions: ["ulx"]
            },
            "application/x-gnumeric": {
                source: "apache",
                extensions: ["gnumeric"]
            },
            "application/x-gramps-xml": {
                source: "apache",
                extensions: ["gramps"]
            },
            "application/x-gtar": {
                source: "apache",
                extensions: ["gtar"]
            },
            "application/x-gzip": {
                source: "apache"
            },
            "application/x-hdf": {
                source: "apache",
                extensions: ["hdf"]
            },
            "application/x-httpd-php": {
                compressible: true,
                extensions: ["php"]
            },
            "application/x-install-instructions": {
                source: "apache",
                extensions: ["install"]
            },
            "application/x-iso9660-image": {
                source: "apache",
                extensions: ["iso"]
            },
            "application/x-iwork-keynote-sffkey": {
                extensions: ["key"]
            },
            "application/x-iwork-numbers-sffnumbers": {
                extensions: ["numbers"]
            },
            "application/x-iwork-pages-sffpages": {
                extensions: ["pages"]
            },
            "application/x-java-archive-diff": {
                source: "nginx",
                extensions: ["jardiff"]
            },
            "application/x-java-jnlp-file": {
                source: "apache",
                compressible: false,
                extensions: ["jnlp"]
            },
            "application/x-javascript": {
                compressible: true
            },
            "application/x-keepass2": {
                extensions: ["kdbx"]
            },
            "application/x-latex": {
                source: "apache",
                compressible: false,
                extensions: ["latex"]
            },
            "application/x-lua-bytecode": {
                extensions: ["luac"]
            },
            "application/x-lzh-compressed": {
                source: "apache",
                extensions: ["lzh", "lha"]
            },
            "application/x-makeself": {
                source: "nginx",
                extensions: ["run"]
            },
            "application/x-mie": {
                source: "apache",
                extensions: ["mie"]
            },
            "application/x-mobipocket-ebook": {
                source: "apache",
                extensions: ["prc", "mobi"]
            },
            "application/x-mpegurl": {
                compressible: false
            },
            "application/x-ms-application": {
                source: "apache",
                extensions: ["application"]
            },
            "application/x-ms-shortcut": {
                source: "apache",
                extensions: ["lnk"]
            },
            "application/x-ms-wmd": {
                source: "apache",
                extensions: ["wmd"]
            },
            "application/x-ms-wmz": {
                source: "apache",
                extensions: ["wmz"]
            },
            "application/x-ms-xbap": {
                source: "apache",
                extensions: ["xbap"]
            },
            "application/x-msaccess": {
                source: "apache",
                extensions: ["mdb"]
            },
            "application/x-msbinder": {
                source: "apache",
                extensions: ["obd"]
            },
            "application/x-mscardfile": {
                source: "apache",
                extensions: ["crd"]
            },
            "application/x-msclip": {
                source: "apache",
                extensions: ["clp"]
            },
            "application/x-msdos-program": {
                extensions: ["exe"]
            },
            "application/x-msdownload": {
                source: "apache",
                extensions: ["exe", "dll", "com", "bat", "msi"]
            },
            "application/x-msmediaview": {
                source: "apache",
                extensions: ["mvb", "m13", "m14"]
            },
            "application/x-msmetafile": {
                source: "apache",
                extensions: ["wmf", "wmz", "emf", "emz"]
            },
            "application/x-msmoney": {
                source: "apache",
                extensions: ["mny"]
            },
            "application/x-mspublisher": {
                source: "apache",
                extensions: ["pub"]
            },
            "application/x-msschedule": {
                source: "apache",
                extensions: ["scd"]
            },
            "application/x-msterminal": {
                source: "apache",
                extensions: ["trm"]
            },
            "application/x-mswrite": {
                source: "apache",
                extensions: ["wri"]
            },
            "application/x-netcdf": {
                source: "apache",
                extensions: ["nc", "cdf"]
            },
            "application/x-ns-proxy-autoconfig": {
                compressible: true,
                extensions: ["pac"]
            },
            "application/x-nzb": {
                source: "apache",
                extensions: ["nzb"]
            },
            "application/x-perl": {
                source: "nginx",
                extensions: ["pl", "pm"]
            },
            "application/x-pilot": {
                source: "nginx",
                extensions: ["prc", "pdb"]
            },
            "application/x-pkcs12": {
                source: "apache",
                compressible: false,
                extensions: ["p12", "pfx"]
            },
            "application/x-pkcs7-certificates": {
                source: "apache",
                extensions: ["p7b", "spc"]
            },
            "application/x-pkcs7-certreqresp": {
                source: "apache",
                extensions: ["p7r"]
            },
            "application/x-pki-message": {
                source: "iana"
            },
            "application/x-rar-compressed": {
                source: "apache",
                compressible: false,
                extensions: ["rar"]
            },
            "application/x-redhat-package-manager": {
                source: "nginx",
                extensions: ["rpm"]
            },
            "application/x-research-info-systems": {
                source: "apache",
                extensions: ["ris"]
            },
            "application/x-sea": {
                source: "nginx",
                extensions: ["sea"]
            },
            "application/x-sh": {
                source: "apache",
                compressible: true,
                extensions: ["sh"]
            },
            "application/x-shar": {
                source: "apache",
                extensions: ["shar"]
            },
            "application/x-shockwave-flash": {
                source: "apache",
                compressible: false,
                extensions: ["swf"]
            },
            "application/x-silverlight-app": {
                source: "apache",
                extensions: ["xap"]
            },
            "application/x-sql": {
                source: "apache",
                extensions: ["sql"]
            },
            "application/x-stuffit": {
                source: "apache",
                compressible: false,
                extensions: ["sit"]
            },
            "application/x-stuffitx": {
                source: "apache",
                extensions: ["sitx"]
            },
            "application/x-subrip": {
                source: "apache",
                extensions: ["srt"]
            },
            "application/x-sv4cpio": {
                source: "apache",
                extensions: ["sv4cpio"]
            },
            "application/x-sv4crc": {
                source: "apache",
                extensions: ["sv4crc"]
            },
            "application/x-t3vm-image": {
                source: "apache",
                extensions: ["t3"]
            },
            "application/x-tads": {
                source: "apache",
                extensions: ["gam"]
            },
            "application/x-tar": {
                source: "apache",
                compressible: true,
                extensions: ["tar"]
            },
            "application/x-tcl": {
                source: "apache",
                extensions: ["tcl", "tk"]
            },
            "application/x-tex": {
                source: "apache",
                extensions: ["tex"]
            },
            "application/x-tex-tfm": {
                source: "apache",
                extensions: ["tfm"]
            },
            "application/x-texinfo": {
                source: "apache",
                extensions: ["texinfo", "texi"]
            },
            "application/x-tgif": {
                source: "apache",
                extensions: ["obj"]
            },
            "application/x-ustar": {
                source: "apache",
                extensions: ["ustar"]
            },
            "application/x-virtualbox-hdd": {
                compressible: true,
                extensions: ["hdd"]
            },
            "application/x-virtualbox-ova": {
                compressible: true,
                extensions: ["ova"]
            },
            "application/x-virtualbox-ovf": {
                compressible: true,
                extensions: ["ovf"]
            },
            "application/x-virtualbox-vbox": {
                compressible: true,
                extensions: ["vbox"]
            },
            "application/x-virtualbox-vbox-extpack": {
                compressible: false,
                extensions: ["vbox-extpack"]
            },
            "application/x-virtualbox-vdi": {
                compressible: true,
                extensions: ["vdi"]
            },
            "application/x-virtualbox-vhd": {
                compressible: true,
                extensions: ["vhd"]
            },
            "application/x-virtualbox-vmdk": {
                compressible: true,
                extensions: ["vmdk"]
            },
            "application/x-wais-source": {
                source: "apache",
                extensions: ["src"]
            },
            "application/x-web-app-manifest+json": {
                compressible: true,
                extensions: ["webapp"]
            },
            "application/x-www-form-urlencoded": {
                source: "iana",
                compressible: true
            },
            "application/x-x509-ca-cert": {
                source: "iana",
                extensions: ["der", "crt", "pem"]
            },
            "application/x-x509-ca-ra-cert": {
                source: "iana"
            },
            "application/x-x509-next-ca-cert": {
                source: "iana"
            },
            "application/x-xfig": {
                source: "apache",
                extensions: ["fig"]
            },
            "application/x-xliff+xml": {
                source: "apache",
                compressible: true,
                extensions: ["xlf"]
            },
            "application/x-xpinstall": {
                source: "apache",
                compressible: false,
                extensions: ["xpi"]
            },
            "application/x-xz": {
                source: "apache",
                extensions: ["xz"]
            },
            "application/x-zmachine": {
                source: "apache",
                extensions: ["z1", "z2", "z3", "z4", "z5", "z6", "z7", "z8"]
            },
            "application/x400-bp": {
                source: "iana"
            },
            "application/xacml+xml": {
                source: "iana",
                compressible: true
            },
            "application/xaml+xml": {
                source: "apache",
                compressible: true,
                extensions: ["xaml"]
            },
            "application/xcap-att+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xav"]
            },
            "application/xcap-caps+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xca"]
            },
            "application/xcap-diff+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xdf"]
            },
            "application/xcap-el+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xel"]
            },
            "application/xcap-error+xml": {
                source: "iana",
                compressible: true
            },
            "application/xcap-ns+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xns"]
            },
            "application/xcon-conference-info+xml": {
                source: "iana",
                compressible: true
            },
            "application/xcon-conference-info-diff+xml": {
                source: "iana",
                compressible: true
            },
            "application/xenc+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xenc"]
            },
            "application/xhtml+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xhtml", "xht"]
            },
            "application/xhtml-voice+xml": {
                source: "apache",
                compressible: true
            },
            "application/xliff+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xlf"]
            },
            "application/xml": {
                source: "iana",
                compressible: true,
                extensions: ["xml", "xsl", "xsd", "rng"]
            },
            "application/xml-dtd": {
                source: "iana",
                compressible: true,
                extensions: ["dtd"]
            },
            "application/xml-external-parsed-entity": {
                source: "iana"
            },
            "application/xml-patch+xml": {
                source: "iana",
                compressible: true
            },
            "application/xmpp+xml": {
                source: "iana",
                compressible: true
            },
            "application/xop+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xop"]
            },
            "application/xproc+xml": {
                source: "apache",
                compressible: true,
                extensions: ["xpl"]
            },
            "application/xslt+xml": {
                source: "iana",
                compressible: true,
                extensions: ["xsl", "xslt"]
            },
            "application/xspf+xml": {
                source: "apache",
                compressible: true,
                extensions: ["xspf"]
            },
            "application/xv+xml": {
                source: "iana",
                compressible: true,
                extensions: ["mxml", "xhvml", "xvml", "xvm"]
            },
            "application/yang": {
                source: "iana",
                extensions: ["yang"]
            },
            "application/yang-data+json": {
                source: "iana",
                compressible: true
            },
            "application/yang-data+xml": {
                source: "iana",
                compressible: true
            },
            "application/yang-patch+json": {
                source: "iana",
                compressible: true
            },
            "application/yang-patch+xml": {
                source: "iana",
                compressible: true
            },
            "application/yin+xml": {
                source: "iana",
                compressible: true,
                extensions: ["yin"]
            },
            "application/zip": {
                source: "iana",
                compressible: false,
                extensions: ["zip"]
            },
            "application/zlib": {
                source: "iana"
            },
            "application/zstd": {
                source: "iana"
            },
            "audio/1d-interleaved-parityfec": {
                source: "iana"
            },
            "audio/32kadpcm": {
                source: "iana"
            },
            "audio/3gpp": {
                source: "iana",
                compressible: false,
                extensions: ["3gpp"]
            },
            "audio/3gpp2": {
                source: "iana"
            },
            "audio/aac": {
                source: "iana"
            },
            "audio/ac3": {
                source: "iana"
            },
            "audio/adpcm": {
                source: "apache",
                extensions: ["adp"]
            },
            "audio/amr": {
                source: "iana",
                extensions: ["amr"]
            },
            "audio/amr-wb": {
                source: "iana"
            },
            "audio/amr-wb+": {
                source: "iana"
            },
            "audio/aptx": {
                source: "iana"
            },
            "audio/asc": {
                source: "iana"
            },
            "audio/atrac-advanced-lossless": {
                source: "iana"
            },
            "audio/atrac-x": {
                source: "iana"
            },
            "audio/atrac3": {
                source: "iana"
            },
            "audio/basic": {
                source: "iana",
                compressible: false,
                extensions: ["au", "snd"]
            },
            "audio/bv16": {
                source: "iana"
            },
            "audio/bv32": {
                source: "iana"
            },
            "audio/clearmode": {
                source: "iana"
            },
            "audio/cn": {
                source: "iana"
            },
            "audio/dat12": {
                source: "iana"
            },
            "audio/dls": {
                source: "iana"
            },
            "audio/dsr-es201108": {
                source: "iana"
            },
            "audio/dsr-es202050": {
                source: "iana"
            },
            "audio/dsr-es202211": {
                source: "iana"
            },
            "audio/dsr-es202212": {
                source: "iana"
            },
            "audio/dv": {
                source: "iana"
            },
            "audio/dvi4": {
                source: "iana"
            },
            "audio/eac3": {
                source: "iana"
            },
            "audio/encaprtp": {
                source: "iana"
            },
            "audio/evrc": {
                source: "iana"
            },
            "audio/evrc-qcp": {
                source: "iana"
            },
            "audio/evrc0": {
                source: "iana"
            },
            "audio/evrc1": {
                source: "iana"
            },
            "audio/evrcb": {
                source: "iana"
            },
            "audio/evrcb0": {
                source: "iana"
            },
            "audio/evrcb1": {
                source: "iana"
            },
            "audio/evrcnw": {
                source: "iana"
            },
            "audio/evrcnw0": {
                source: "iana"
            },
            "audio/evrcnw1": {
                source: "iana"
            },
            "audio/evrcwb": {
                source: "iana"
            },
            "audio/evrcwb0": {
                source: "iana"
            },
            "audio/evrcwb1": {
                source: "iana"
            },
            "audio/evs": {
                source: "iana"
            },
            "audio/flexfec": {
                source: "iana"
            },
            "audio/fwdred": {
                source: "iana"
            },
            "audio/g711-0": {
                source: "iana"
            },
            "audio/g719": {
                source: "iana"
            },
            "audio/g722": {
                source: "iana"
            },
            "audio/g7221": {
                source: "iana"
            },
            "audio/g723": {
                source: "iana"
            },
            "audio/g726-16": {
                source: "iana"
            },
            "audio/g726-24": {
                source: "iana"
            },
            "audio/g726-32": {
                source: "iana"
            },
            "audio/g726-40": {
                source: "iana"
            },
            "audio/g728": {
                source: "iana"
            },
            "audio/g729": {
                source: "iana"
            },
            "audio/g7291": {
                source: "iana"
            },
            "audio/g729d": {
                source: "iana"
            },
            "audio/g729e": {
                source: "iana"
            },
            "audio/gsm": {
                source: "iana"
            },
            "audio/gsm-efr": {
                source: "iana"
            },
            "audio/gsm-hr-08": {
                source: "iana"
            },
            "audio/ilbc": {
                source: "iana"
            },
            "audio/ip-mr_v2.5": {
                source: "iana"
            },
            "audio/isac": {
                source: "apache"
            },
            "audio/l16": {
                source: "iana"
            },
            "audio/l20": {
                source: "iana"
            },
            "audio/l24": {
                source: "iana",
                compressible: false
            },
            "audio/l8": {
                source: "iana"
            },
            "audio/lpc": {
                source: "iana"
            },
            "audio/melp": {
                source: "iana"
            },
            "audio/melp1200": {
                source: "iana"
            },
            "audio/melp2400": {
                source: "iana"
            },
            "audio/melp600": {
                source: "iana"
            },
            "audio/mhas": {
                source: "iana"
            },
            "audio/midi": {
                source: "apache",
                extensions: ["mid", "midi", "kar", "rmi"]
            },
            "audio/mobile-xmf": {
                source: "iana",
                extensions: ["mxmf"]
            },
            "audio/mp3": {
                compressible: false,
                extensions: ["mp3"]
            },
            "audio/mp4": {
                source: "iana",
                compressible: false,
                extensions: ["m4a", "mp4a"]
            },
            "audio/mp4a-latm": {
                source: "iana"
            },
            "audio/mpa": {
                source: "iana"
            },
            "audio/mpa-robust": {
                source: "iana"
            },
            "audio/mpeg": {
                source: "iana",
                compressible: false,
                extensions: ["mpga", "mp2", "mp2a", "mp3", "m2a", "m3a"]
            },
            "audio/mpeg4-generic": {
                source: "iana"
            },
            "audio/musepack": {
                source: "apache"
            },
            "audio/ogg": {
                source: "iana",
                compressible: false,
                extensions: ["oga", "ogg", "spx", "opus"]
            },
            "audio/opus": {
                source: "iana"
            },
            "audio/parityfec": {
                source: "iana"
            },
            "audio/pcma": {
                source: "iana"
            },
            "audio/pcma-wb": {
                source: "iana"
            },
            "audio/pcmu": {
                source: "iana"
            },
            "audio/pcmu-wb": {
                source: "iana"
            },
            "audio/prs.sid": {
                source: "iana"
            },
            "audio/qcelp": {
                source: "iana"
            },
            "audio/raptorfec": {
                source: "iana"
            },
            "audio/red": {
                source: "iana"
            },
            "audio/rtp-enc-aescm128": {
                source: "iana"
            },
            "audio/rtp-midi": {
                source: "iana"
            },
            "audio/rtploopback": {
                source: "iana"
            },
            "audio/rtx": {
                source: "iana"
            },
            "audio/s3m": {
                source: "apache",
                extensions: ["s3m"]
            },
            "audio/scip": {
                source: "iana"
            },
            "audio/silk": {
                source: "apache",
                extensions: ["sil"]
            },
            "audio/smv": {
                source: "iana"
            },
            "audio/smv-qcp": {
                source: "iana"
            },
            "audio/smv0": {
                source: "iana"
            },
            "audio/sofa": {
                source: "iana"
            },
            "audio/sp-midi": {
                source: "iana"
            },
            "audio/speex": {
                source: "iana"
            },
            "audio/t140c": {
                source: "iana"
            },
            "audio/t38": {
                source: "iana"
            },
            "audio/telephone-event": {
                source: "iana"
            },
            "audio/tetra_acelp": {
                source: "iana"
            },
            "audio/tetra_acelp_bb": {
                source: "iana"
            },
            "audio/tone": {
                source: "iana"
            },
            "audio/tsvcis": {
                source: "iana"
            },
            "audio/uemclip": {
                source: "iana"
            },
            "audio/ulpfec": {
                source: "iana"
            },
            "audio/usac": {
                source: "iana"
            },
            "audio/vdvi": {
                source: "iana"
            },
            "audio/vmr-wb": {
                source: "iana"
            },
            "audio/vnd.3gpp.iufp": {
                source: "iana"
            },
            "audio/vnd.4sb": {
                source: "iana"
            },
            "audio/vnd.audiokoz": {
                source: "iana"
            },
            "audio/vnd.celp": {
                source: "iana"
            },
            "audio/vnd.cisco.nse": {
                source: "iana"
            },
            "audio/vnd.cmles.radio-events": {
                source: "iana"
            },
            "audio/vnd.cns.anp1": {
                source: "iana"
            },
            "audio/vnd.cns.inf1": {
                source: "iana"
            },
            "audio/vnd.dece.audio": {
                source: "iana",
                extensions: ["uva", "uvva"]
            },
            "audio/vnd.digital-winds": {
                source: "iana",
                extensions: ["eol"]
            },
            "audio/vnd.dlna.adts": {
                source: "iana"
            },
            "audio/vnd.dolby.heaac.1": {
                source: "iana"
            },
            "audio/vnd.dolby.heaac.2": {
                source: "iana"
            },
            "audio/vnd.dolby.mlp": {
                source: "iana"
            },
            "audio/vnd.dolby.mps": {
                source: "iana"
            },
            "audio/vnd.dolby.pl2": {
                source: "iana"
            },
            "audio/vnd.dolby.pl2x": {
                source: "iana"
            },
            "audio/vnd.dolby.pl2z": {
                source: "iana"
            },
            "audio/vnd.dolby.pulse.1": {
                source: "iana"
            },
            "audio/vnd.dra": {
                source: "iana",
                extensions: ["dra"]
            },
            "audio/vnd.dts": {
                source: "iana",
                extensions: ["dts"]
            },
            "audio/vnd.dts.hd": {
                source: "iana",
                extensions: ["dtshd"]
            },
            "audio/vnd.dts.uhd": {
                source: "iana"
            },
            "audio/vnd.dvb.file": {
                source: "iana"
            },
            "audio/vnd.everad.plj": {
                source: "iana"
            },
            "audio/vnd.hns.audio": {
                source: "iana"
            },
            "audio/vnd.lucent.voice": {
                source: "iana",
                extensions: ["lvp"]
            },
            "audio/vnd.ms-playready.media.pya": {
                source: "iana",
                extensions: ["pya"]
            },
            "audio/vnd.nokia.mobile-xmf": {
                source: "iana"
            },
            "audio/vnd.nortel.vbk": {
                source: "iana"
            },
            "audio/vnd.nuera.ecelp4800": {
                source: "iana",
                extensions: ["ecelp4800"]
            },
            "audio/vnd.nuera.ecelp7470": {
                source: "iana",
                extensions: ["ecelp7470"]
            },
            "audio/vnd.nuera.ecelp9600": {
                source: "iana",
                extensions: ["ecelp9600"]
            },
            "audio/vnd.octel.sbc": {
                source: "iana"
            },
            "audio/vnd.presonus.multitrack": {
                source: "iana"
            },
            "audio/vnd.qcelp": {
                source: "iana"
            },
            "audio/vnd.rhetorex.32kadpcm": {
                source: "iana"
            },
            "audio/vnd.rip": {
                source: "iana",
                extensions: ["rip"]
            },
            "audio/vnd.rn-realaudio": {
                compressible: false
            },
            "audio/vnd.sealedmedia.softseal.mpeg": {
                source: "iana"
            },
            "audio/vnd.vmx.cvsd": {
                source: "iana"
            },
            "audio/vnd.wave": {
                compressible: false
            },
            "audio/vorbis": {
                source: "iana",
                compressible: false
            },
            "audio/vorbis-config": {
                source: "iana"
            },
            "audio/wav": {
                compressible: false,
                extensions: ["wav"]
            },
            "audio/wave": {
                compressible: false,
                extensions: ["wav"]
            },
            "audio/webm": {
                source: "apache",
                compressible: false,
                extensions: ["weba"]
            },
            "audio/x-aac": {
                source: "apache",
                compressible: false,
                extensions: ["aac"]
            },
            "audio/x-aiff": {
                source: "apache",
                extensions: ["aif", "aiff", "aifc"]
            },
            "audio/x-caf": {
                source: "apache",
                compressible: false,
                extensions: ["caf"]
            },
            "audio/x-flac": {
                source: "apache",
                extensions: ["flac"]
            },
            "audio/x-m4a": {
                source: "nginx",
                extensions: ["m4a"]
            },
            "audio/x-matroska": {
                source: "apache",
                extensions: ["mka"]
            },
            "audio/x-mpegurl": {
                source: "apache",
                extensions: ["m3u"]
            },
            "audio/x-ms-wax": {
                source: "apache",
                extensions: ["wax"]
            },
            "audio/x-ms-wma": {
                source: "apache",
                extensions: ["wma"]
            },
            "audio/x-pn-realaudio": {
                source: "apache",
                extensions: ["ram", "ra"]
            },
            "audio/x-pn-realaudio-plugin": {
                source: "apache",
                extensions: ["rmp"]
            },
            "audio/x-realaudio": {
                source: "nginx",
                extensions: ["ra"]
            },
            "audio/x-tta": {
                source: "apache"
            },
            "audio/x-wav": {
                source: "apache",
                extensions: ["wav"]
            },
            "audio/xm": {
                source: "apache",
                extensions: ["xm"]
            },
            "chemical/x-cdx": {
                source: "apache",
                extensions: ["cdx"]
            },
            "chemical/x-cif": {
                source: "apache",
                extensions: ["cif"]
            },
            "chemical/x-cmdf": {
                source: "apache",
                extensions: ["cmdf"]
            },
            "chemical/x-cml": {
                source: "apache",
                extensions: ["cml"]
            },
            "chemical/x-csml": {
                source: "apache",
                extensions: ["csml"]
            },
            "chemical/x-pdb": {
                source: "apache"
            },
            "chemical/x-xyz": {
                source: "apache",
                extensions: ["xyz"]
            },
            "font/collection": {
                source: "iana",
                extensions: ["ttc"]
            },
            "font/otf": {
                source: "iana",
                compressible: true,
                extensions: ["otf"]
            },
            "font/sfnt": {
                source: "iana"
            },
            "font/ttf": {
                source: "iana",
                compressible: true,
                extensions: ["ttf"]
            },
            "font/woff": {
                source: "iana",
                extensions: ["woff"]
            },
            "font/woff2": {
                source: "iana",
                extensions: ["woff2"]
            },
            "image/aces": {
                source: "iana",
                extensions: ["exr"]
            },
            "image/apng": {
                compressible: false,
                extensions: ["apng"]
            },
            "image/avci": {
                source: "iana",
                extensions: ["avci"]
            },
            "image/avcs": {
                source: "iana",
                extensions: ["avcs"]
            },
            "image/avif": {
                source: "iana",
                compressible: false,
                extensions: ["avif"]
            },
            "image/bmp": {
                source: "iana",
                compressible: true,
                extensions: ["bmp"]
            },
            "image/cgm": {
                source: "iana",
                extensions: ["cgm"]
            },
            "image/dicom-rle": {
                source: "iana",
                extensions: ["drle"]
            },
            "image/emf": {
                source: "iana",
                extensions: ["emf"]
            },
            "image/fits": {
                source: "iana",
                extensions: ["fits"]
            },
            "image/g3fax": {
                source: "iana",
                extensions: ["g3"]
            },
            "image/gif": {
                source: "iana",
                compressible: false,
                extensions: ["gif"]
            },
            "image/heic": {
                source: "iana",
                extensions: ["heic"]
            },
            "image/heic-sequence": {
                source: "iana",
                extensions: ["heics"]
            },
            "image/heif": {
                source: "iana",
                extensions: ["heif"]
            },
            "image/heif-sequence": {
                source: "iana",
                extensions: ["heifs"]
            },
            "image/hej2k": {
                source: "iana",
                extensions: ["hej2"]
            },
            "image/hsj2": {
                source: "iana",
                extensions: ["hsj2"]
            },
            "image/ief": {
                source: "iana",
                extensions: ["ief"]
            },
            "image/jls": {
                source: "iana",
                extensions: ["jls"]
            },
            "image/jp2": {
                source: "iana",
                compressible: false,
                extensions: ["jp2", "jpg2"]
            },
            "image/jpeg": {
                source: "iana",
                compressible: false,
                extensions: ["jpeg", "jpg", "jpe"]
            },
            "image/jph": {
                source: "iana",
                extensions: ["jph"]
            },
            "image/jphc": {
                source: "iana",
                extensions: ["jhc"]
            },
            "image/jpm": {
                source: "iana",
                compressible: false,
                extensions: ["jpm"]
            },
            "image/jpx": {
                source: "iana",
                compressible: false,
                extensions: ["jpx", "jpf"]
            },
            "image/jxr": {
                source: "iana",
                extensions: ["jxr"]
            },
            "image/jxra": {
                source: "iana",
                extensions: ["jxra"]
            },
            "image/jxrs": {
                source: "iana",
                extensions: ["jxrs"]
            },
            "image/jxs": {
                source: "iana",
                extensions: ["jxs"]
            },
            "image/jxsc": {
                source: "iana",
                extensions: ["jxsc"]
            },
            "image/jxsi": {
                source: "iana",
                extensions: ["jxsi"]
            },
            "image/jxss": {
                source: "iana",
                extensions: ["jxss"]
            },
            "image/ktx": {
                source: "iana",
                extensions: ["ktx"]
            },
            "image/ktx2": {
                source: "iana",
                extensions: ["ktx2"]
            },
            "image/naplps": {
                source: "iana"
            },
            "image/pjpeg": {
                compressible: false
            },
            "image/png": {
                source: "iana",
                compressible: false,
                extensions: ["png"]
            },
            "image/prs.btif": {
                source: "iana",
                extensions: ["btif"]
            },
            "image/prs.pti": {
                source: "iana",
                extensions: ["pti"]
            },
            "image/pwg-raster": {
                source: "iana"
            },
            "image/sgi": {
                source: "apache",
                extensions: ["sgi"]
            },
            "image/svg+xml": {
                source: "iana",
                compressible: true,
                extensions: ["svg", "svgz"]
            },
            "image/t38": {
                source: "iana",
                extensions: ["t38"]
            },
            "image/tiff": {
                source: "iana",
                compressible: false,
                extensions: ["tif", "tiff"]
            },
            "image/tiff-fx": {
                source: "iana",
                extensions: ["tfx"]
            },
            "image/vnd.adobe.photoshop": {
                source: "iana",
                compressible: true,
                extensions: ["psd"]
            },
            "image/vnd.airzip.accelerator.azv": {
                source: "iana",
                extensions: ["azv"]
            },
            "image/vnd.cns.inf2": {
                source: "iana"
            },
            "image/vnd.dece.graphic": {
                source: "iana",
                extensions: ["uvi", "uvvi", "uvg", "uvvg"]
            },
            "image/vnd.djvu": {
                source: "iana",
                extensions: ["djvu", "djv"]
            },
            "image/vnd.dvb.subtitle": {
                source: "iana",
                extensions: ["sub"]
            },
            "image/vnd.dwg": {
                source: "iana",
                extensions: ["dwg"]
            },
            "image/vnd.dxf": {
                source: "iana",
                extensions: ["dxf"]
            },
            "image/vnd.fastbidsheet": {
                source: "iana",
                extensions: ["fbs"]
            },
            "image/vnd.fpx": {
                source: "iana",
                extensions: ["fpx"]
            },
            "image/vnd.fst": {
                source: "iana",
                extensions: ["fst"]
            },
            "image/vnd.fujixerox.edmics-mmr": {
                source: "iana",
                extensions: ["mmr"]
            },
            "image/vnd.fujixerox.edmics-rlc": {
                source: "iana",
                extensions: ["rlc"]
            },
            "image/vnd.globalgraphics.pgb": {
                source: "iana"
            },
            "image/vnd.microsoft.icon": {
                source: "iana",
                compressible: true,
                extensions: ["ico"]
            },
            "image/vnd.mix": {
                source: "iana"
            },
            "image/vnd.mozilla.apng": {
                source: "iana"
            },
            "image/vnd.ms-dds": {
                compressible: true,
                extensions: ["dds"]
            },
            "image/vnd.ms-modi": {
                source: "iana",
                extensions: ["mdi"]
            },
            "image/vnd.ms-photo": {
                source: "apache",
                extensions: ["wdp"]
            },
            "image/vnd.net-fpx": {
                source: "iana",
                extensions: ["npx"]
            },
            "image/vnd.pco.b16": {
                source: "iana",
                extensions: ["b16"]
            },
            "image/vnd.radiance": {
                source: "iana"
            },
            "image/vnd.sealed.png": {
                source: "iana"
            },
            "image/vnd.sealedmedia.softseal.gif": {
                source: "iana"
            },
            "image/vnd.sealedmedia.softseal.jpg": {
                source: "iana"
            },
            "image/vnd.svf": {
                source: "iana"
            },
            "image/vnd.tencent.tap": {
                source: "iana",
                extensions: ["tap"]
            },
            "image/vnd.valve.source.texture": {
                source: "iana",
                extensions: ["vtf"]
            },
            "image/vnd.wap.wbmp": {
                source: "iana",
                extensions: ["wbmp"]
            },
            "image/vnd.xiff": {
                source: "iana",
                extensions: ["xif"]
            },
            "image/vnd.zbrush.pcx": {
                source: "iana",
                extensions: ["pcx"]
            },
            "image/webp": {
                source: "apache",
                extensions: ["webp"]
            },
            "image/wmf": {
                source: "iana",
                extensions: ["wmf"]
            },
            "image/x-3ds": {
                source: "apache",
                extensions: ["3ds"]
            },
            "image/x-cmu-raster": {
                source: "apache",
                extensions: ["ras"]
            },
            "image/x-cmx": {
                source: "apache",
                extensions: ["cmx"]
            },
            "image/x-freehand": {
                source: "apache",
                extensions: ["fh", "fhc", "fh4", "fh5", "fh7"]
            },
            "image/x-icon": {
                source: "apache",
                compressible: true,
                extensions: ["ico"]
            },
            "image/x-jng": {
                source: "nginx",
                extensions: ["jng"]
            },
            "image/x-mrsid-image": {
                source: "apache",
                extensions: ["sid"]
            },
            "image/x-ms-bmp": {
                source: "nginx",
                compressible: true,
                extensions: ["bmp"]
            },
            "image/x-pcx": {
                source: "apache",
                extensions: ["pcx"]
            },
            "image/x-pict": {
                source: "apache",
                extensions: ["pic", "pct"]
            },
            "image/x-portable-anymap": {
                source: "apache",
                extensions: ["pnm"]
            },
            "image/x-portable-bitmap": {
                source: "apache",
                extensions: ["pbm"]
            },
            "image/x-portable-graymap": {
                source: "apache",
                extensions: ["pgm"]
            },
            "image/x-portable-pixmap": {
                source: "apache",
                extensions: ["ppm"]
            },
            "image/x-rgb": {
                source: "apache",
                extensions: ["rgb"]
            },
            "image/x-tga": {
                source: "apache",
                extensions: ["tga"]
            },
            "image/x-xbitmap": {
                source: "apache",
                extensions: ["xbm"]
            },
            "image/x-xcf": {
                compressible: false
            },
            "image/x-xpixmap": {
                source: "apache",
                extensions: ["xpm"]
            },
            "image/x-xwindowdump": {
                source: "apache",
                extensions: ["xwd"]
            },
            "message/cpim": {
                source: "iana"
            },
            "message/delivery-status": {
                source: "iana"
            },
            "message/disposition-notification": {
                source: "iana",
                extensions: ["disposition-notification"]
            },
            "message/external-body": {
                source: "iana"
            },
            "message/feedback-report": {
                source: "iana"
            },
            "message/global": {
                source: "iana",
                extensions: ["u8msg"]
            },
            "message/global-delivery-status": {
                source: "iana",
                extensions: ["u8dsn"]
            },
            "message/global-disposition-notification": {
                source: "iana",
                extensions: ["u8mdn"]
            },
            "message/global-headers": {
                source: "iana",
                extensions: ["u8hdr"]
            },
            "message/http": {
                source: "iana",
                compressible: false
            },
            "message/imdn+xml": {
                source: "iana",
                compressible: true
            },
            "message/news": {
                source: "iana"
            },
            "message/partial": {
                source: "iana",
                compressible: false
            },
            "message/rfc822": {
                source: "iana",
                compressible: true,
                extensions: ["eml", "mime"]
            },
            "message/s-http": {
                source: "iana"
            },
            "message/sip": {
                source: "iana"
            },
            "message/sipfrag": {
                source: "iana"
            },
            "message/tracking-status": {
                source: "iana"
            },
            "message/vnd.si.simp": {
                source: "iana"
            },
            "message/vnd.wfa.wsc": {
                source: "iana",
                extensions: ["wsc"]
            },
            "model/3mf": {
                source: "iana",
                extensions: ["3mf"]
            },
            "model/e57": {
                source: "iana"
            },
            "model/gltf+json": {
                source: "iana",
                compressible: true,
                extensions: ["gltf"]
            },
            "model/gltf-binary": {
                source: "iana",
                compressible: true,
                extensions: ["glb"]
            },
            "model/iges": {
                source: "iana",
                compressible: false,
                extensions: ["igs", "iges"]
            },
            "model/mesh": {
                source: "iana",
                compressible: false,
                extensions: ["msh", "mesh", "silo"]
            },
            "model/mtl": {
                source: "iana",
                extensions: ["mtl"]
            },
            "model/obj": {
                source: "iana",
                extensions: ["obj"]
            },
            "model/step": {
                source: "iana"
            },
            "model/step+xml": {
                source: "iana",
                compressible: true,
                extensions: ["stpx"]
            },
            "model/step+zip": {
                source: "iana",
                compressible: false,
                extensions: ["stpz"]
            },
            "model/step-xml+zip": {
                source: "iana",
                compressible: false,
                extensions: ["stpxz"]
            },
            "model/stl": {
                source: "iana",
                extensions: ["stl"]
            },
            "model/vnd.collada+xml": {
                source: "iana",
                compressible: true,
                extensions: ["dae"]
            },
            "model/vnd.dwf": {
                source: "iana",
                extensions: ["dwf"]
            },
            "model/vnd.flatland.3dml": {
                source: "iana"
            },
            "model/vnd.gdl": {
                source: "iana",
                extensions: ["gdl"]
            },
            "model/vnd.gs-gdl": {
                source: "apache"
            },
            "model/vnd.gs.gdl": {
                source: "iana"
            },
            "model/vnd.gtw": {
                source: "iana",
                extensions: ["gtw"]
            },
            "model/vnd.moml+xml": {
                source: "iana",
                compressible: true
            },
            "model/vnd.mts": {
                source: "iana",
                extensions: ["mts"]
            },
            "model/vnd.opengex": {
                source: "iana",
                extensions: ["ogex"]
            },
            "model/vnd.parasolid.transmit.binary": {
                source: "iana",
                extensions: ["x_b"]
            },
            "model/vnd.parasolid.transmit.text": {
                source: "iana",
                extensions: ["x_t"]
            },
            "model/vnd.pytha.pyox": {
                source: "iana"
            },
            "model/vnd.rosette.annotated-data-model": {
                source: "iana"
            },
            "model/vnd.sap.vds": {
                source: "iana",
                extensions: ["vds"]
            },
            "model/vnd.usdz+zip": {
                source: "iana",
                compressible: false,
                extensions: ["usdz"]
            },
            "model/vnd.valve.source.compiled-map": {
                source: "iana",
                extensions: ["bsp"]
            },
            "model/vnd.vtu": {
                source: "iana",
                extensions: ["vtu"]
            },
            "model/vrml": {
                source: "iana",
                compressible: false,
                extensions: ["wrl", "vrml"]
            },
            "model/x3d+binary": {
                source: "apache",
                compressible: false,
                extensions: ["x3db", "x3dbz"]
            },
            "model/x3d+fastinfoset": {
                source: "iana",
                extensions: ["x3db"]
            },
            "model/x3d+vrml": {
                source: "apache",
                compressible: false,
                extensions: ["x3dv", "x3dvz"]
            },
            "model/x3d+xml": {
                source: "iana",
                compressible: true,
                extensions: ["x3d", "x3dz"]
            },
            "model/x3d-vrml": {
                source: "iana",
                extensions: ["x3dv"]
            },
            "multipart/alternative": {
                source: "iana",
                compressible: false
            },
            "multipart/appledouble": {
                source: "iana"
            },
            "multipart/byteranges": {
                source: "iana"
            },
            "multipart/digest": {
                source: "iana"
            },
            "multipart/encrypted": {
                source: "iana",
                compressible: false
            },
            "multipart/form-data": {
                source: "iana",
                compressible: false
            },
            "multipart/header-set": {
                source: "iana"
            },
            "multipart/mixed": {
                source: "iana"
            },
            "multipart/multilingual": {
                source: "iana"
            },
            "multipart/parallel": {
                source: "iana"
            },
            "multipart/related": {
                source: "iana",
                compressible: false
            },
            "multipart/report": {
                source: "iana"
            },
            "multipart/signed": {
                source: "iana",
                compressible: false
            },
            "multipart/vnd.bint.med-plus": {
                source: "iana"
            },
            "multipart/voice-message": {
                source: "iana"
            },
            "multipart/x-mixed-replace": {
                source: "iana"
            },
            "text/1d-interleaved-parityfec": {
                source: "iana"
            },
            "text/cache-manifest": {
                source: "iana",
                compressible: true,
                extensions: ["appcache", "manifest"]
            },
            "text/calendar": {
                source: "iana",
                extensions: ["ics", "ifb"]
            },
            "text/calender": {
                compressible: true
            },
            "text/cmd": {
                compressible: true
            },
            "text/coffeescript": {
                extensions: ["coffee", "litcoffee"]
            },
            "text/cql": {
                source: "iana"
            },
            "text/cql-expression": {
                source: "iana"
            },
            "text/cql-identifier": {
                source: "iana"
            },
            "text/css": {
                source: "iana",
                charset: "UTF-8",
                compressible: true,
                extensions: ["css"]
            },
            "text/csv": {
                source: "iana",
                compressible: true,
                extensions: ["csv"]
            },
            "text/csv-schema": {
                source: "iana"
            },
            "text/directory": {
                source: "iana"
            },
            "text/dns": {
                source: "iana"
            },
            "text/ecmascript": {
                source: "iana"
            },
            "text/encaprtp": {
                source: "iana"
            },
            "text/enriched": {
                source: "iana"
            },
            "text/fhirpath": {
                source: "iana"
            },
            "text/flexfec": {
                source: "iana"
            },
            "text/fwdred": {
                source: "iana"
            },
            "text/gff3": {
                source: "iana"
            },
            "text/grammar-ref-list": {
                source: "iana"
            },
            "text/html": {
                source: "iana",
                compressible: true,
                extensions: ["html", "htm", "shtml"]
            },
            "text/jade": {
                extensions: ["jade"]
            },
            "text/javascript": {
                source: "iana",
                compressible: true
            },
            "text/jcr-cnd": {
                source: "iana"
            },
            "text/jsx": {
                compressible: true,
                extensions: ["jsx"]
            },
            "text/less": {
                compressible: true,
                extensions: ["less"]
            },
            "text/markdown": {
                source: "iana",
                compressible: true,
                extensions: ["markdown", "md"]
            },
            "text/mathml": {
                source: "nginx",
                extensions: ["mml"]
            },
            "text/mdx": {
                compressible: true,
                extensions: ["mdx"]
            },
            "text/mizar": {
                source: "iana"
            },
            "text/n3": {
                source: "iana",
                charset: "UTF-8",
                compressible: true,
                extensions: ["n3"]
            },
            "text/parameters": {
                source: "iana",
                charset: "UTF-8"
            },
            "text/parityfec": {
                source: "iana"
            },
            "text/plain": {
                source: "iana",
                compressible: true,
                extensions: ["txt", "text", "conf", "def", "list", "log", "in", "ini"]
            },
            "text/provenance-notation": {
                source: "iana",
                charset: "UTF-8"
            },
            "text/prs.fallenstein.rst": {
                source: "iana"
            },
            "text/prs.lines.tag": {
                source: "iana",
                extensions: ["dsc"]
            },
            "text/prs.prop.logic": {
                source: "iana"
            },
            "text/raptorfec": {
                source: "iana"
            },
            "text/red": {
                source: "iana"
            },
            "text/rfc822-headers": {
                source: "iana"
            },
            "text/richtext": {
                source: "iana",
                compressible: true,
                extensions: ["rtx"]
            },
            "text/rtf": {
                source: "iana",
                compressible: true,
                extensions: ["rtf"]
            },
            "text/rtp-enc-aescm128": {
                source: "iana"
            },
            "text/rtploopback": {
                source: "iana"
            },
            "text/rtx": {
                source: "iana"
            },
            "text/sgml": {
                source: "iana",
                extensions: ["sgml", "sgm"]
            },
            "text/shaclc": {
                source: "iana"
            },
            "text/shex": {
                source: "iana",
                extensions: ["shex"]
            },
            "text/slim": {
                extensions: ["slim", "slm"]
            },
            "text/spdx": {
                source: "iana",
                extensions: ["spdx"]
            },
            "text/strings": {
                source: "iana"
            },
            "text/stylus": {
                extensions: ["stylus", "styl"]
            },
            "text/t140": {
                source: "iana"
            },
            "text/tab-separated-values": {
                source: "iana",
                compressible: true,
                extensions: ["tsv"]
            },
            "text/troff": {
                source: "iana",
                extensions: ["t", "tr", "roff", "man", "me", "ms"]
            },
            "text/turtle": {
                source: "iana",
                charset: "UTF-8",
                extensions: ["ttl"]
            },
            "text/ulpfec": {
                source: "iana"
            },
            "text/uri-list": {
                source: "iana",
                compressible: true,
                extensions: ["uri", "uris", "urls"]
            },
            "text/vcard": {
                source: "iana",
                compressible: true,
                extensions: ["vcard"]
            },
            "text/vnd.a": {
                source: "iana"
            },
            "text/vnd.abc": {
                source: "iana"
            },
            "text/vnd.ascii-art": {
                source: "iana"
            },
            "text/vnd.curl": {
                source: "iana",
                extensions: ["curl"]
            },
            "text/vnd.curl.dcurl": {
                source: "apache",
                extensions: ["dcurl"]
            },
            "text/vnd.curl.mcurl": {
                source: "apache",
                extensions: ["mcurl"]
            },
            "text/vnd.curl.scurl": {
                source: "apache",
                extensions: ["scurl"]
            },
            "text/vnd.debian.copyright": {
                source: "iana",
                charset: "UTF-8"
            },
            "text/vnd.dmclientscript": {
                source: "iana"
            },
            "text/vnd.dvb.subtitle": {
                source: "iana",
                extensions: ["sub"]
            },
            "text/vnd.esmertec.theme-descriptor": {
                source: "iana",
                charset: "UTF-8"
            },
            "text/vnd.familysearch.gedcom": {
                source: "iana",
                extensions: ["ged"]
            },
            "text/vnd.ficlab.flt": {
                source: "iana"
            },
            "text/vnd.fly": {
                source: "iana",
                extensions: ["fly"]
            },
            "text/vnd.fmi.flexstor": {
                source: "iana",
                extensions: ["flx"]
            },
            "text/vnd.gml": {
                source: "iana"
            },
            "text/vnd.graphviz": {
                source: "iana",
                extensions: ["gv"]
            },
            "text/vnd.hans": {
                source: "iana"
            },
            "text/vnd.hgl": {
                source: "iana"
            },
            "text/vnd.in3d.3dml": {
                source: "iana",
                extensions: ["3dml"]
            },
            "text/vnd.in3d.spot": {
                source: "iana",
                extensions: ["spot"]
            },
            "text/vnd.iptc.newsml": {
                source: "iana"
            },
            "text/vnd.iptc.nitf": {
                source: "iana"
            },
            "text/vnd.latex-z": {
                source: "iana"
            },
            "text/vnd.motorola.reflex": {
                source: "iana"
            },
            "text/vnd.ms-mediapackage": {
                source: "iana"
            },
            "text/vnd.net2phone.commcenter.command": {
                source: "iana"
            },
            "text/vnd.radisys.msml-basic-layout": {
                source: "iana"
            },
            "text/vnd.senx.warpscript": {
                source: "iana"
            },
            "text/vnd.si.uricatalogue": {
                source: "iana"
            },
            "text/vnd.sosi": {
                source: "iana"
            },
            "text/vnd.sun.j2me.app-descriptor": {
                source: "iana",
                charset: "UTF-8",
                extensions: ["jad"]
            },
            "text/vnd.trolltech.linguist": {
                source: "iana",
                charset: "UTF-8"
            },
            "text/vnd.wap.si": {
                source: "iana"
            },
            "text/vnd.wap.sl": {
                source: "iana"
            },
            "text/vnd.wap.wml": {
                source: "iana",
                extensions: ["wml"]
            },
            "text/vnd.wap.wmlscript": {
                source: "iana",
                extensions: ["wmls"]
            },
            "text/vtt": {
                source: "iana",
                charset: "UTF-8",
                compressible: true,
                extensions: ["vtt"]
            },
            "text/x-asm": {
                source: "apache",
                extensions: ["s", "asm"]
            },
            "text/x-c": {
                source: "apache",
                extensions: ["c", "cc", "cxx", "cpp", "h", "hh", "dic"]
            },
            "text/x-component": {
                source: "nginx",
                extensions: ["htc"]
            },
            "text/x-fortran": {
                source: "apache",
                extensions: ["f", "for", "f77", "f90"]
            },
            "text/x-gwt-rpc": {
                compressible: true
            },
            "text/x-handlebars-template": {
                extensions: ["hbs"]
            },
            "text/x-java-source": {
                source: "apache",
                extensions: ["java"]
            },
            "text/x-jquery-tmpl": {
                compressible: true
            },
            "text/x-lua": {
                extensions: ["lua"]
            },
            "text/x-markdown": {
                compressible: true,
                extensions: ["mkd"]
            },
            "text/x-nfo": {
                source: "apache",
                extensions: ["nfo"]
            },
            "text/x-opml": {
                source: "apache",
                extensions: ["opml"]
            },
            "text/x-org": {
                compressible: true,
                extensions: ["org"]
            },
            "text/x-pascal": {
                source: "apache",
                extensions: ["p", "pas"]
            },
            "text/x-processing": {
                compressible: true,
                extensions: ["pde"]
            },
            "text/x-sass": {
                extensions: ["sass"]
            },
            "text/x-scss": {
                extensions: ["scss"]
            },
            "text/x-setext": {
                source: "apache",
                extensions: ["etx"]
            },
            "text/x-sfv": {
                source: "apache",
                extensions: ["sfv"]
            },
            "text/x-suse-ymp": {
                compressible: true,
                extensions: ["ymp"]
            },
            "text/x-uuencode": {
                source: "apache",
                extensions: ["uu"]
            },
            "text/x-vcalendar": {
                source: "apache",
                extensions: ["vcs"]
            },
            "text/x-vcard": {
                source: "apache",
                extensions: ["vcf"]
            },
            "text/xml": {
                source: "iana",
                compressible: true,
                extensions: ["xml"]
            },
            "text/xml-external-parsed-entity": {
                source: "iana"
            },
            "text/yaml": {
                compressible: true,
                extensions: ["yaml", "yml"]
            },
            "video/1d-interleaved-parityfec": {
                source: "iana"
            },
            "video/3gpp": {
                source: "iana",
                extensions: ["3gp", "3gpp"]
            },
            "video/3gpp-tt": {
                source: "iana"
            },
            "video/3gpp2": {
                source: "iana",
                extensions: ["3g2"]
            },
            "video/av1": {
                source: "iana"
            },
            "video/bmpeg": {
                source: "iana"
            },
            "video/bt656": {
                source: "iana"
            },
            "video/celb": {
                source: "iana"
            },
            "video/dv": {
                source: "iana"
            },
            "video/encaprtp": {
                source: "iana"
            },
            "video/ffv1": {
                source: "iana"
            },
            "video/flexfec": {
                source: "iana"
            },
            "video/h261": {
                source: "iana",
                extensions: ["h261"]
            },
            "video/h263": {
                source: "iana",
                extensions: ["h263"]
            },
            "video/h263-1998": {
                source: "iana"
            },
            "video/h263-2000": {
                source: "iana"
            },
            "video/h264": {
                source: "iana",
                extensions: ["h264"]
            },
            "video/h264-rcdo": {
                source: "iana"
            },
            "video/h264-svc": {
                source: "iana"
            },
            "video/h265": {
                source: "iana"
            },
            "video/iso.segment": {
                source: "iana",
                extensions: ["m4s"]
            },
            "video/jpeg": {
                source: "iana",
                extensions: ["jpgv"]
            },
            "video/jpeg2000": {
                source: "iana"
            },
            "video/jpm": {
                source: "apache",
                extensions: ["jpm", "jpgm"]
            },
            "video/jxsv": {
                source: "iana"
            },
            "video/mj2": {
                source: "iana",
                extensions: ["mj2", "mjp2"]
            },
            "video/mp1s": {
                source: "iana"
            },
            "video/mp2p": {
                source: "iana"
            },
            "video/mp2t": {
                source: "iana",
                extensions: ["ts"]
            },
            "video/mp4": {
                source: "iana",
                compressible: false,
                extensions: ["mp4", "mp4v", "mpg4"]
            },
            "video/mp4v-es": {
                source: "iana"
            },
            "video/mpeg": {
                source: "iana",
                compressible: false,
                extensions: ["mpeg", "mpg", "mpe", "m1v", "m2v"]
            },
            "video/mpeg4-generic": {
                source: "iana"
            },
            "video/mpv": {
                source: "iana"
            },
            "video/nv": {
                source: "iana"
            },
            "video/ogg": {
                source: "iana",
                compressible: false,
                extensions: ["ogv"]
            },
            "video/parityfec": {
                source: "iana"
            },
            "video/pointer": {
                source: "iana"
            },
            "video/quicktime": {
                source: "iana",
                compressible: false,
                extensions: ["qt", "mov"]
            },
            "video/raptorfec": {
                source: "iana"
            },
            "video/raw": {
                source: "iana"
            },
            "video/rtp-enc-aescm128": {
                source: "iana"
            },
            "video/rtploopback": {
                source: "iana"
            },
            "video/rtx": {
                source: "iana"
            },
            "video/scip": {
                source: "iana"
            },
            "video/smpte291": {
                source: "iana"
            },
            "video/smpte292m": {
                source: "iana"
            },
            "video/ulpfec": {
                source: "iana"
            },
            "video/vc1": {
                source: "iana"
            },
            "video/vc2": {
                source: "iana"
            },
            "video/vnd.cctv": {
                source: "iana"
            },
            "video/vnd.dece.hd": {
                source: "iana",
                extensions: ["uvh", "uvvh"]
            },
            "video/vnd.dece.mobile": {
                source: "iana",
                extensions: ["uvm", "uvvm"]
            },
            "video/vnd.dece.mp4": {
                source: "iana"
            },
            "video/vnd.dece.pd": {
                source: "iana",
                extensions: ["uvp", "uvvp"]
            },
            "video/vnd.dece.sd": {
                source: "iana",
                extensions: ["uvs", "uvvs"]
            },
            "video/vnd.dece.video": {
                source: "iana",
                extensions: ["uvv", "uvvv"]
            },
            "video/vnd.directv.mpeg": {
                source: "iana"
            },
            "video/vnd.directv.mpeg-tts": {
                source: "iana"
            },
            "video/vnd.dlna.mpeg-tts": {
                source: "iana"
            },
            "video/vnd.dvb.file": {
                source: "iana",
                extensions: ["dvb"]
            },
            "video/vnd.fvt": {
                source: "iana",
                extensions: ["fvt"]
            },
            "video/vnd.hns.video": {
                source: "iana"
            },
            "video/vnd.iptvforum.1dparityfec-1010": {
                source: "iana"
            },
            "video/vnd.iptvforum.1dparityfec-2005": {
                source: "iana"
            },
            "video/vnd.iptvforum.2dparityfec-1010": {
                source: "iana"
            },
            "video/vnd.iptvforum.2dparityfec-2005": {
                source: "iana"
            },
            "video/vnd.iptvforum.ttsavc": {
                source: "iana"
            },
            "video/vnd.iptvforum.ttsmpeg2": {
                source: "iana"
            },
            "video/vnd.motorola.video": {
                source: "iana"
            },
            "video/vnd.motorola.videop": {
                source: "iana"
            },
            "video/vnd.mpegurl": {
                source: "iana",
                extensions: ["mxu", "m4u"]
            },
            "video/vnd.ms-playready.media.pyv": {
                source: "iana",
                extensions: ["pyv"]
            },
            "video/vnd.nokia.interleaved-multimedia": {
                source: "iana"
            },
            "video/vnd.nokia.mp4vr": {
                source: "iana"
            },
            "video/vnd.nokia.videovoip": {
                source: "iana"
            },
            "video/vnd.objectvideo": {
                source: "iana"
            },
            "video/vnd.radgamettools.bink": {
                source: "iana"
            },
            "video/vnd.radgamettools.smacker": {
                source: "iana"
            },
            "video/vnd.sealed.mpeg1": {
                source: "iana"
            },
            "video/vnd.sealed.mpeg4": {
                source: "iana"
            },
            "video/vnd.sealed.swf": {
                source: "iana"
            },
            "video/vnd.sealedmedia.softseal.mov": {
                source: "iana"
            },
            "video/vnd.uvvu.mp4": {
                source: "iana",
                extensions: ["uvu", "uvvu"]
            },
            "video/vnd.vivo": {
                source: "iana",
                extensions: ["viv"]
            },
            "video/vnd.youtube.yt": {
                source: "iana"
            },
            "video/vp8": {
                source: "iana"
            },
            "video/vp9": {
                source: "iana"
            },
            "video/webm": {
                source: "apache",
                compressible: false,
                extensions: ["webm"]
            },
            "video/x-f4v": {
                source: "apache",
                extensions: ["f4v"]
            },
            "video/x-fli": {
                source: "apache",
                extensions: ["fli"]
            },
            "video/x-flv": {
                source: "apache",
                compressible: false,
                extensions: ["flv"]
            },
            "video/x-m4v": {
                source: "apache",
                extensions: ["m4v"]
            },
            "video/x-matroska": {
                source: "apache",
                compressible: false,
                extensions: ["mkv", "mk3d", "mks"]
            },
            "video/x-mng": {
                source: "apache",
                extensions: ["mng"]
            },
            "video/x-ms-asf": {
                source: "apache",
                extensions: ["asf", "asx"]
            },
            "video/x-ms-vob": {
                source: "apache",
                extensions: ["vob"]
            },
            "video/x-ms-wm": {
                source: "apache",
                extensions: ["wm"]
            },
            "video/x-ms-wmv": {
                source: "apache",
                compressible: false,
                extensions: ["wmv"]
            },
            "video/x-ms-wmx": {
                source: "apache",
                extensions: ["wmx"]
            },
            "video/x-ms-wvx": {
                source: "apache",
                extensions: ["wvx"]
            },
            "video/x-msvideo": {
                source: "apache",
                extensions: ["avi"]
            },
            "video/x-sgi-movie": {
                source: "apache",
                extensions: ["movie"]
            },
            "video/x-smv": {
                source: "apache",
                extensions: ["smv"]
            },
            "x-conference/x-cooltalk": {
                source: "apache",
                extensions: ["ice"]
            },
            "x-shader/x-fragment": {
                compressible: true
            },
            "x-shader/x-vertex": {
                compressible: true
            }
        };
    }
});

// node_modules/.pnpm/mime-db@1.52.0/node_modules/mime-db/index.js
var require_mime_db = __commonJS({
    "node_modules/.pnpm/mime-db@1.52.0/node_modules/mime-db/index.js"(exports2, module2) {
        module2.exports = require_db();
    }
});

// node_modules/.pnpm/mime-types@2.1.35/node_modules/mime-types/index.js
var require_mime_types = __commonJS({
    "node_modules/.pnpm/mime-types@2.1.35/node_modules/mime-types/index.js"(exports2) {
        "use strict";
        var db = require_mime_db();
        var extname = require("path").extname;
        var EXTRACT_TYPE_REGEXP = /^\s*([^;\s]*)(?:;|\s|$)/;
        var TEXT_TYPE_REGEXP = /^text\//i;
        exports2.charset = charset;
        exports2.charsets = { lookup: charset };
        exports2.contentType = contentType;
        exports2.extension = extension;
        exports2.extensions = /* @__PURE__ */ Object.create(null);
        exports2.lookup = lookup;
        exports2.types = /* @__PURE__ */ Object.create(null);
        populateMaps(exports2.extensions, exports2.types);
        function charset(type) {
            if (!type || typeof type !== "string") {
                return false;
            }
            var match = EXTRACT_TYPE_REGEXP.exec(type);
            var mime = match && db[match[1].toLowerCase()];
            if (mime && mime.charset) {
                return mime.charset;
            }
            if (match && TEXT_TYPE_REGEXP.test(match[1])) {
                return "UTF-8";
            }
            return false;
        }
        function contentType(str) {
            if (!str || typeof str !== "string") {
                return false;
            }
            var mime = str.indexOf("/") === -1 ? exports2.lookup(str) : str;
            if (!mime) {
                return false;
            }
            if (mime.indexOf("charset") === -1) {
                var charset2 = exports2.charset(mime);
                if (charset2) mime += "; charset=" + charset2.toLowerCase();
            }
            return mime;
        }
        function extension(type) {
            if (!type || typeof type !== "string") {
                return false;
            }
            var match = EXTRACT_TYPE_REGEXP.exec(type);
            var exts = match && exports2.extensions[match[1].toLowerCase()];
            if (!exts || !exts.length) {
                return false;
            }
            return exts[0];
        }
        function lookup(path) {
            if (!path || typeof path !== "string") {
                return false;
            }
            var extension2 = extname("x." + path)
                .toLowerCase()
                .substr(1);
            if (!extension2) {
                return false;
            }
            return exports2.types[extension2] || false;
        }
        function populateMaps(extensions, types) {
            var preference = ["nginx", "apache", void 0, "iana"];
            Object.keys(db).forEach(function forEachMimeType(type) {
                var mime = db[type];
                var exts = mime.extensions;
                if (!exts || !exts.length) {
                    return;
                }
                extensions[type] = exts;
                for (var i = 0; i < exts.length; i++) {
                    var extension2 = exts[i];
                    if (types[extension2]) {
                        var from = preference.indexOf(db[types[extension2]].source);
                        var to = preference.indexOf(mime.source);
                        if (types[extension2] !== "application/octet-stream" && (from > to || (from === to && types[extension2].substr(0, 12) === "application/"))) {
                            continue;
                        }
                    }
                    types[extension2] = type;
                }
            });
        }
    }
});

// node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/lib/defer.js
var require_defer = __commonJS({
    "node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/lib/defer.js"(exports2, module2) {
        module2.exports = defer;
        function defer(fn) {
            var nextTick = typeof setImmediate == "function" ? setImmediate : typeof process == "object" && typeof process.nextTick == "function" ? process.nextTick : null;
            if (nextTick) {
                nextTick(fn);
            } else {
                setTimeout(fn, 0);
            }
        }
    }
});

// node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/lib/async.js
var require_async = __commonJS({
    "node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/lib/async.js"(exports2, module2) {
        var defer = require_defer();
        module2.exports = async;
        function async(callback) {
            var isAsync = false;
            defer(function () {
                isAsync = true;
            });
            return function async_callback(err, result) {
                if (isAsync) {
                    callback(err, result);
                } else {
                    defer(function nextTick_callback() {
                        callback(err, result);
                    });
                }
            };
        }
    }
});

// node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/lib/abort.js
var require_abort = __commonJS({
    "node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/lib/abort.js"(exports2, module2) {
        module2.exports = abort;
        function abort(state) {
            Object.keys(state.jobs).forEach(clean.bind(state));
            state.jobs = {};
        }
        function clean(key) {
            if (typeof this.jobs[key] == "function") {
                this.jobs[key]();
            }
        }
    }
});

// node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/lib/iterate.js
var require_iterate = __commonJS({
    "node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/lib/iterate.js"(exports2, module2) {
        var async = require_async();
        var abort = require_abort();
        module2.exports = iterate;
        function iterate(list, iterator2, state, callback) {
            var key = state["keyedList"] ? state["keyedList"][state.index] : state.index;
            state.jobs[key] = runJob(iterator2, key, list[key], function (error, output) {
                if (!(key in state.jobs)) {
                    return;
                }
                delete state.jobs[key];
                if (error) {
                    abort(state);
                } else {
                    state.results[key] = output;
                }
                callback(error, state.results);
            });
        }
        function runJob(iterator2, key, item, callback) {
            var aborter;
            if (iterator2.length == 2) {
                aborter = iterator2(item, async(callback));
            } else {
                aborter = iterator2(item, key, async(callback));
            }
            return aborter;
        }
    }
});

// node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/lib/state.js
var require_state = __commonJS({
    "node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/lib/state.js"(exports2, module2) {
        module2.exports = state;
        function state(list, sortMethod) {
            var isNamedList = !Array.isArray(list),
                initState = {
                    index: 0,
                    keyedList: isNamedList || sortMethod ? Object.keys(list) : null,
                    jobs: {},
                    results: isNamedList ? {} : [],
                    size: isNamedList ? Object.keys(list).length : list.length
                };
            if (sortMethod) {
                initState.keyedList.sort(
                    isNamedList
                        ? sortMethod
                        : function (a, b) {
                              return sortMethod(list[a], list[b]);
                          }
                );
            }
            return initState;
        }
    }
});

// node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/lib/terminator.js
var require_terminator = __commonJS({
    "node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/lib/terminator.js"(exports2, module2) {
        var abort = require_abort();
        var async = require_async();
        module2.exports = terminator;
        function terminator(callback) {
            if (!Object.keys(this.jobs).length) {
                return;
            }
            this.index = this.size;
            abort(this);
            async(callback)(null, this.results);
        }
    }
});

// node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/parallel.js
var require_parallel = __commonJS({
    "node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/parallel.js"(exports2, module2) {
        var iterate = require_iterate();
        var initState = require_state();
        var terminator = require_terminator();
        module2.exports = parallel;
        function parallel(list, iterator2, callback) {
            var state = initState(list);
            while (state.index < (state["keyedList"] || list).length) {
                iterate(list, iterator2, state, function (error, result) {
                    if (error) {
                        callback(error, result);
                        return;
                    }
                    if (Object.keys(state.jobs).length === 0) {
                        callback(null, state.results);
                        return;
                    }
                });
                state.index++;
            }
            return terminator.bind(state, callback);
        }
    }
});

// node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/serialOrdered.js
var require_serialOrdered = __commonJS({
    "node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/serialOrdered.js"(exports2, module2) {
        var iterate = require_iterate();
        var initState = require_state();
        var terminator = require_terminator();
        module2.exports = serialOrdered;
        module2.exports.ascending = ascending;
        module2.exports.descending = descending;
        function serialOrdered(list, iterator2, sortMethod, callback) {
            var state = initState(list, sortMethod);
            iterate(list, iterator2, state, function iteratorHandler(error, result) {
                if (error) {
                    callback(error, result);
                    return;
                }
                state.index++;
                if (state.index < (state["keyedList"] || list).length) {
                    iterate(list, iterator2, state, iteratorHandler);
                    return;
                }
                callback(null, state.results);
            });
            return terminator.bind(state, callback);
        }
        function ascending(a, b) {
            return a < b ? -1 : a > b ? 1 : 0;
        }
        function descending(a, b) {
            return -1 * ascending(a, b);
        }
    }
});

// node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/serial.js
var require_serial = __commonJS({
    "node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/serial.js"(exports2, module2) {
        var serialOrdered = require_serialOrdered();
        module2.exports = serial;
        function serial(list, iterator2, callback) {
            return serialOrdered(list, iterator2, null, callback);
        }
    }
});

// node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/index.js
var require_asynckit = __commonJS({
    "node_modules/.pnpm/asynckit@0.4.0/node_modules/asynckit/index.js"(exports2, module2) {
        module2.exports = {
            parallel: require_parallel(),
            serial: require_serial(),
            serialOrdered: require_serialOrdered()
        };
    }
});

// node_modules/.pnpm/has-tostringtag@1.0.2/node_modules/has-tostringtag/shams.js
var require_shams2 = __commonJS({
    "node_modules/.pnpm/has-tostringtag@1.0.2/node_modules/has-tostringtag/shams.js"(exports2, module2) {
        "use strict";
        var hasSymbols = require_shams();
        module2.exports = function hasToStringTagShams() {
            return hasSymbols() && !!Symbol.toStringTag;
        };
    }
});

// node_modules/.pnpm/es-set-tostringtag@2.1.0/node_modules/es-set-tostringtag/index.js
var require_es_set_tostringtag = __commonJS({
    "node_modules/.pnpm/es-set-tostringtag@2.1.0/node_modules/es-set-tostringtag/index.js"(exports2, module2) {
        "use strict";
        var GetIntrinsic = require_get_intrinsic();
        var $defineProperty = GetIntrinsic("%Object.defineProperty%", true);
        var hasToStringTag = require_shams2()();
        var hasOwn = require_hasown();
        var $TypeError = require_type();
        var toStringTag2 = hasToStringTag ? Symbol.toStringTag : null;
        module2.exports = function setToStringTag(object, value) {
            var overrideIfSet = arguments.length > 2 && !!arguments[2] && arguments[2].force;
            var nonConfigurable = arguments.length > 2 && !!arguments[2] && arguments[2].nonConfigurable;
            if ((typeof overrideIfSet !== "undefined" && typeof overrideIfSet !== "boolean") || (typeof nonConfigurable !== "undefined" && typeof nonConfigurable !== "boolean")) {
                throw new $TypeError("if provided, the `overrideIfSet` and `nonConfigurable` options must be booleans");
            }
            if (toStringTag2 && (overrideIfSet || !hasOwn(object, toStringTag2))) {
                if ($defineProperty) {
                    $defineProperty(object, toStringTag2, {
                        configurable: !nonConfigurable,
                        enumerable: false,
                        value,
                        writable: false
                    });
                } else {
                    object[toStringTag2] = value;
                }
            }
        };
    }
});

// node_modules/.pnpm/form-data@4.0.6/node_modules/form-data/lib/populate.js
var require_populate = __commonJS({
    "node_modules/.pnpm/form-data@4.0.6/node_modules/form-data/lib/populate.js"(exports2, module2) {
        "use strict";
        module2.exports = function (dst, src) {
            Object.keys(src).forEach(function (prop) {
                dst[prop] = dst[prop] || src[prop];
            });
            return dst;
        };
    }
});

// node_modules/.pnpm/form-data@4.0.6/node_modules/form-data/lib/form_data.js
var require_form_data = __commonJS({
    "node_modules/.pnpm/form-data@4.0.6/node_modules/form-data/lib/form_data.js"(exports2, module2) {
        "use strict";
        var CombinedStream = require_combined_stream();
        var util4 = require("util");
        var path = require("path");
        var http4 = require("http");
        var https2 = require("https");
        var parseUrl2 = require("url").parse;
        var fs = require("fs");
        var Stream = require("stream").Stream;
        var crypto2 = require("crypto");
        var mime = require_mime_types();
        var asynckit = require_asynckit();
        var setToStringTag = require_es_set_tostringtag();
        var hasOwn = require_hasown();
        var populate = require_populate();
        function escapeHeaderParam(str) {
            return String(str).replace(/\r/g, "%0D").replace(/\n/g, "%0A").replace(/"/g, "%22");
        }
        function FormData3(options) {
            if (!(this instanceof FormData3)) {
                return new FormData3(options);
            }
            this._overheadLength = 0;
            this._valueLength = 0;
            this._valuesToMeasure = [];
            CombinedStream.call(this);
            options = options || {};
            for (var option in options) {
                this[option] = options[option];
            }
        }
        util4.inherits(FormData3, CombinedStream);
        FormData3.LINE_BREAK = "\r\n";
        FormData3.DEFAULT_CONTENT_TYPE = "application/octet-stream";
        FormData3.prototype.append = function (field, value, options) {
            options = options || {};
            if (typeof options === "string") {
                options = { filename: options };
            }
            var append2 = CombinedStream.prototype.append.bind(this);
            if (typeof value === "number" || value == null) {
                value = String(value);
            }
            if (Array.isArray(value)) {
                this._error(new Error("Arrays are not supported."));
                return;
            }
            var header = this._multiPartHeader(field, value, options);
            var footer = this._multiPartFooter();
            append2(header);
            append2(value);
            append2(footer);
            this._trackLength(header, value, options);
        };
        FormData3.prototype._trackLength = function (header, value, options) {
            var valueLength = 0;
            if (options.knownLength != null) {
                valueLength += Number(options.knownLength);
            } else if (Buffer.isBuffer(value)) {
                valueLength = value.length;
            } else if (typeof value === "string") {
                valueLength = Buffer.byteLength(value);
            }
            this._valueLength += valueLength;
            this._overheadLength += Buffer.byteLength(header) + FormData3.LINE_BREAK.length;
            if (!value || (!value.path && !(value.readable && hasOwn(value, "httpVersion")) && !(value instanceof Stream))) {
                return;
            }
            if (!options.knownLength) {
                this._valuesToMeasure.push(value);
            }
        };
        FormData3.prototype._lengthRetriever = function (value, callback) {
            if (hasOwn(value, "fd")) {
                if (value.end != void 0 && value.end != Infinity && value.start != void 0) {
                    callback(null, value.end + 1 - (value.start ? value.start : 0));
                } else {
                    fs.stat(value.path, function (err, stat) {
                        if (err) {
                            callback(err);
                            return;
                        }
                        var fileSize = stat.size - (value.start ? value.start : 0);
                        callback(null, fileSize);
                    });
                }
            } else if (hasOwn(value, "httpVersion")) {
                callback(null, Number(value.headers["content-length"]));
            } else if (hasOwn(value, "httpModule")) {
                value.on("response", function (response) {
                    value.pause();
                    callback(null, Number(response.headers["content-length"]));
                });
                value.resume();
            } else {
                callback("Unknown stream");
            }
        };
        FormData3.prototype._multiPartHeader = function (field, value, options) {
            if (typeof options.header === "string") {
                return options.header;
            }
            var contentDisposition = this._getContentDisposition(value, options);
            var contentType = this._getContentType(value, options);
            var contents = "";
            var headers = {
                // add custom disposition as third element or keep it two elements if not
                "Content-Disposition": ["form-data", 'name="' + escapeHeaderParam(field) + '"'].concat(contentDisposition || []),
                // if no content type. allow it to be empty array
                "Content-Type": [].concat(contentType || [])
            };
            if (typeof options.header === "object") {
                populate(headers, options.header);
            }
            var header;
            for (var prop in headers) {
                if (hasOwn(headers, prop)) {
                    header = headers[prop];
                    if (header == null) {
                        continue;
                    }
                    if (!Array.isArray(header)) {
                        header = [header];
                    }
                    if (header.length) {
                        contents += prop + ": " + header.join("; ") + FormData3.LINE_BREAK;
                    }
                }
            }
            return "--" + this.getBoundary() + FormData3.LINE_BREAK + contents + FormData3.LINE_BREAK;
        };
        FormData3.prototype._getContentDisposition = function (value, options) {
            var filename;
            if (typeof options.filepath === "string") {
                filename = path.normalize(options.filepath).replace(/\\/g, "/");
            } else if (options.filename || (value && (value.name || value.path))) {
                filename = path.basename(options.filename || (value && (value.name || value.path)));
            } else if (value && value.readable && hasOwn(value, "httpVersion")) {
                filename = path.basename(value.client._httpMessage.path || "");
            }
            if (filename) {
                return 'filename="' + escapeHeaderParam(filename) + '"';
            }
        };
        FormData3.prototype._getContentType = function (value, options) {
            var contentType = options.contentType;
            if (!contentType && value && value.name) {
                contentType = mime.lookup(value.name);
            }
            if (!contentType && value && value.path) {
                contentType = mime.lookup(value.path);
            }
            if (!contentType && value && value.readable && hasOwn(value, "httpVersion")) {
                contentType = value.headers["content-type"];
            }
            if (!contentType && (options.filepath || options.filename)) {
                contentType = mime.lookup(options.filepath || options.filename);
            }
            if (!contentType && value && typeof value === "object") {
                contentType = FormData3.DEFAULT_CONTENT_TYPE;
            }
            return contentType;
        };
        FormData3.prototype._multiPartFooter = function () {
            return function (next) {
                var footer = FormData3.LINE_BREAK;
                var lastPart = this._streams.length === 0;
                if (lastPart) {
                    footer += this._lastBoundary();
                }
                next(footer);
            }.bind(this);
        };
        FormData3.prototype._lastBoundary = function () {
            return "--" + this.getBoundary() + "--" + FormData3.LINE_BREAK;
        };
        FormData3.prototype.getHeaders = function (userHeaders) {
            var header;
            var formHeaders = {
                "content-type": "multipart/form-data; boundary=" + this.getBoundary()
            };
            for (header in userHeaders) {
                if (hasOwn(userHeaders, header)) {
                    formHeaders[header.toLowerCase()] = userHeaders[header];
                }
            }
            return formHeaders;
        };
        FormData3.prototype.setBoundary = function (boundary) {
            if (typeof boundary !== "string") {
                throw new TypeError("FormData boundary must be a string");
            }
            this._boundary = boundary;
        };
        FormData3.prototype.getBoundary = function () {
            if (!this._boundary) {
                this._generateBoundary();
            }
            return this._boundary;
        };
        FormData3.prototype.getBuffer = function () {
            var dataBuffer = new Buffer.alloc(0);
            var boundary = this.getBoundary();
            for (var i = 0, len = this._streams.length; i < len; i++) {
                if (typeof this._streams[i] !== "function") {
                    if (Buffer.isBuffer(this._streams[i])) {
                        dataBuffer = Buffer.concat([dataBuffer, this._streams[i]]);
                    } else {
                        dataBuffer = Buffer.concat([dataBuffer, Buffer.from(this._streams[i])]);
                    }
                    if (typeof this._streams[i] !== "string" || this._streams[i].substring(2, boundary.length + 2) !== boundary) {
                        dataBuffer = Buffer.concat([dataBuffer, Buffer.from(FormData3.LINE_BREAK)]);
                    }
                }
            }
            return Buffer.concat([dataBuffer, Buffer.from(this._lastBoundary())]);
        };
        FormData3.prototype._generateBoundary = function () {
            this._boundary = "--------------------------" + crypto2.randomBytes(12).toString("hex");
        };
        FormData3.prototype.getLengthSync = function () {
            var knownLength = this._overheadLength + this._valueLength;
            if (this._streams.length) {
                knownLength += this._lastBoundary().length;
            }
            if (!this.hasKnownLength()) {
                this._error(new Error("Cannot calculate proper length in synchronous way."));
            }
            return knownLength;
        };
        FormData3.prototype.hasKnownLength = function () {
            var hasKnownLength = true;
            if (this._valuesToMeasure.length) {
                hasKnownLength = false;
            }
            return hasKnownLength;
        };
        FormData3.prototype.getLength = function (cb) {
            var knownLength = this._overheadLength + this._valueLength;
            if (this._streams.length) {
                knownLength += this._lastBoundary().length;
            }
            if (!this._valuesToMeasure.length) {
                process.nextTick(cb.bind(this, null, knownLength));
                return;
            }
            asynckit.parallel(this._valuesToMeasure, this._lengthRetriever, function (err, values) {
                if (err) {
                    cb(err);
                    return;
                }
                values.forEach(function (length) {
                    knownLength += length;
                });
                cb(null, knownLength);
            });
        };
        FormData3.prototype.submit = function (params, cb) {
            var request2;
            var options;
            var defaults2 = { method: "post" };
            if (typeof params === "string") {
                params = parseUrl2(params);
                options = populate(
                    {
                        port: params.port,
                        path: params.pathname,
                        host: params.hostname,
                        protocol: params.protocol
                    },
                    defaults2
                );
            } else {
                options = populate(params, defaults2);
                if (!options.port) {
                    options.port = options.protocol === "https:" ? 443 : 80;
                }
            }
            options.headers = this.getHeaders(params.headers);
            if (options.protocol === "https:") {
                request2 = https2.request(options);
            } else {
                request2 = http4.request(options);
            }
            this.getLength(
                function (err, length) {
                    if (err && err !== "Unknown stream") {
                        this._error(err);
                        return;
                    }
                    if (length) {
                        request2.setHeader("Content-Length", length);
                    }
                    this.pipe(request2);
                    if (cb) {
                        var onResponse;
                        var callback = function (error, responce) {
                            request2.removeListener("error", callback);
                            request2.removeListener("response", onResponse);
                            return cb.call(this, error, responce);
                        };
                        onResponse = callback.bind(this, null);
                        request2.on("error", callback);
                        request2.on("response", onResponse);
                    }
                }.bind(this)
            );
            return request2;
        };
        FormData3.prototype._error = function (err) {
            if (!this.error) {
                this.error = err;
                this.pause();
                this.emit("error", err);
            }
        };
        FormData3.prototype.toString = function () {
            return "[object FormData]";
        };
        setToStringTag(FormData3.prototype, "FormData");
        module2.exports = FormData3;
    }
});

// node_modules/.pnpm/ms@2.1.3/node_modules/ms/index.js
var require_ms = __commonJS({
    "node_modules/.pnpm/ms@2.1.3/node_modules/ms/index.js"(exports2, module2) {
        var s = 1e3;
        var m = s * 60;
        var h = m * 60;
        var d = h * 24;
        var w = d * 7;
        var y = d * 365.25;
        module2.exports = function (val, options) {
            options = options || {};
            var type = typeof val;
            if (type === "string" && val.length > 0) {
                return parse(val);
            } else if (type === "number" && isFinite(val)) {
                return options.long ? fmtLong(val) : fmtShort(val);
            }
            throw new Error("val is not a non-empty string or a valid number. val=" + JSON.stringify(val));
        };
        function parse(str) {
            str = String(str);
            if (str.length > 100) {
                return;
            }
            var match = /^(-?(?:\d+)?\.?\d+) *(milliseconds?|msecs?|ms|seconds?|secs?|s|minutes?|mins?|m|hours?|hrs?|h|days?|d|weeks?|w|years?|yrs?|y)?$/i.exec(str);
            if (!match) {
                return;
            }
            var n = parseFloat(match[1]);
            var type = (match[2] || "ms").toLowerCase();
            switch (type) {
                case "years":
                case "year":
                case "yrs":
                case "yr":
                case "y":
                    return n * y;
                case "weeks":
                case "week":
                case "w":
                    return n * w;
                case "days":
                case "day":
                case "d":
                    return n * d;
                case "hours":
                case "hour":
                case "hrs":
                case "hr":
                case "h":
                    return n * h;
                case "minutes":
                case "minute":
                case "mins":
                case "min":
                case "m":
                    return n * m;
                case "seconds":
                case "second":
                case "secs":
                case "sec":
                case "s":
                    return n * s;
                case "milliseconds":
                case "millisecond":
                case "msecs":
                case "msec":
                case "ms":
                    return n;
                default:
                    return void 0;
            }
        }
        function fmtShort(ms) {
            var msAbs = Math.abs(ms);
            if (msAbs >= d) {
                return Math.round(ms / d) + "d";
            }
            if (msAbs >= h) {
                return Math.round(ms / h) + "h";
            }
            if (msAbs >= m) {
                return Math.round(ms / m) + "m";
            }
            if (msAbs >= s) {
                return Math.round(ms / s) + "s";
            }
            return ms + "ms";
        }
        function fmtLong(ms) {
            var msAbs = Math.abs(ms);
            if (msAbs >= d) {
                return plural(ms, msAbs, d, "day");
            }
            if (msAbs >= h) {
                return plural(ms, msAbs, h, "hour");
            }
            if (msAbs >= m) {
                return plural(ms, msAbs, m, "minute");
            }
            if (msAbs >= s) {
                return plural(ms, msAbs, s, "second");
            }
            return ms + " ms";
        }
        function plural(ms, msAbs, n, name) {
            var isPlural = msAbs >= n * 1.5;
            return Math.round(ms / n) + " " + name + (isPlural ? "s" : "");
        }
    }
});

// node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/common.js
var require_common = __commonJS({
    "node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/common.js"(exports2, module2) {
        function setup(env) {
            createDebug.debug = createDebug;
            createDebug.default = createDebug;
            createDebug.coerce = coerce;
            createDebug.disable = disable;
            createDebug.enable = enable;
            createDebug.enabled = enabled;
            createDebug.humanize = require_ms();
            createDebug.destroy = destroy;
            Object.keys(env).forEach(key => {
                createDebug[key] = env[key];
            });
            createDebug.names = [];
            createDebug.skips = [];
            createDebug.formatters = {};
            function selectColor(namespace) {
                let hash = 0;
                for (let i = 0; i < namespace.length; i++) {
                    hash = (hash << 5) - hash + namespace.charCodeAt(i);
                    hash |= 0;
                }
                return createDebug.colors[Math.abs(hash) % createDebug.colors.length];
            }
            createDebug.selectColor = selectColor;
            function createDebug(namespace) {
                let prevTime;
                let enableOverride = null;
                let namespacesCache;
                let enabledCache;
                function debug(...args) {
                    if (!debug.enabled) {
                        return;
                    }
                    const self2 = debug;
                    const curr = Number(/* @__PURE__ */ new Date());
                    const ms = curr - (prevTime || curr);
                    self2.diff = ms;
                    self2.prev = prevTime;
                    self2.curr = curr;
                    prevTime = curr;
                    args[0] = createDebug.coerce(args[0]);
                    if (typeof args[0] !== "string") {
                        args.unshift("%O");
                    }
                    let index2 = 0;
                    args[0] = args[0].replace(/%([a-zA-Z%])/g, (match, format) => {
                        if (match === "%%") {
                            return "%";
                        }
                        index2++;
                        const formatter = createDebug.formatters[format];
                        if (typeof formatter === "function") {
                            const val = args[index2];
                            match = formatter.call(self2, val);
                            args.splice(index2, 1);
                            index2--;
                        }
                        return match;
                    });
                    createDebug.formatArgs.call(self2, args);
                    const logFn = self2.log || createDebug.log;
                    logFn.apply(self2, args);
                }
                debug.namespace = namespace;
                debug.useColors = createDebug.useColors();
                debug.color = createDebug.selectColor(namespace);
                debug.extend = extend2;
                debug.destroy = createDebug.destroy;
                Object.defineProperty(debug, "enabled", {
                    enumerable: true,
                    configurable: false,
                    get: () => {
                        if (enableOverride !== null) {
                            return enableOverride;
                        }
                        if (namespacesCache !== createDebug.namespaces) {
                            namespacesCache = createDebug.namespaces;
                            enabledCache = createDebug.enabled(namespace);
                        }
                        return enabledCache;
                    },
                    set: v => {
                        enableOverride = v;
                    }
                });
                if (typeof createDebug.init === "function") {
                    createDebug.init(debug);
                }
                return debug;
            }
            function extend2(namespace, delimiter) {
                const newDebug = createDebug(this.namespace + (typeof delimiter === "undefined" ? ":" : delimiter) + namespace);
                newDebug.log = this.log;
                return newDebug;
            }
            function enable(namespaces) {
                createDebug.save(namespaces);
                createDebug.namespaces = namespaces;
                createDebug.names = [];
                createDebug.skips = [];
                const split = (typeof namespaces === "string" ? namespaces : "").trim().replace(/\s+/g, ",").split(",").filter(Boolean);
                for (const ns of split) {
                    if (ns[0] === "-") {
                        createDebug.skips.push(ns.slice(1));
                    } else {
                        createDebug.names.push(ns);
                    }
                }
            }
            function matchesTemplate(search, template) {
                let searchIndex = 0;
                let templateIndex = 0;
                let starIndex = -1;
                let matchIndex = 0;
                while (searchIndex < search.length) {
                    if (templateIndex < template.length && (template[templateIndex] === search[searchIndex] || template[templateIndex] === "*")) {
                        if (template[templateIndex] === "*") {
                            starIndex = templateIndex;
                            matchIndex = searchIndex;
                            templateIndex++;
                        } else {
                            searchIndex++;
                            templateIndex++;
                        }
                    } else if (starIndex !== -1) {
                        templateIndex = starIndex + 1;
                        matchIndex++;
                        searchIndex = matchIndex;
                    } else {
                        return false;
                    }
                }
                while (templateIndex < template.length && template[templateIndex] === "*") {
                    templateIndex++;
                }
                return templateIndex === template.length;
            }
            function disable() {
                const namespaces = [...createDebug.names, ...createDebug.skips.map(namespace => "-" + namespace)].join(",");
                createDebug.enable("");
                return namespaces;
            }
            function enabled(name) {
                for (const skip of createDebug.skips) {
                    if (matchesTemplate(name, skip)) {
                        return false;
                    }
                }
                for (const ns of createDebug.names) {
                    if (matchesTemplate(name, ns)) {
                        return true;
                    }
                }
                return false;
            }
            function coerce(val) {
                if (val instanceof Error) {
                    return val.stack || val.message;
                }
                return val;
            }
            function destroy() {
                console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
            }
            createDebug.enable(createDebug.load());
            return createDebug;
        }
        module2.exports = setup;
    }
});

// node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/browser.js
var require_browser = __commonJS({
    "node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/browser.js"(exports2, module2) {
        exports2.formatArgs = formatArgs;
        exports2.save = save;
        exports2.load = load;
        exports2.useColors = useColors;
        exports2.storage = localstorage();
        exports2.destroy = /* @__PURE__ */ (() => {
            let warned = false;
            return () => {
                if (!warned) {
                    warned = true;
                    console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
                }
            };
        })();
        exports2.colors = [
            "#0000CC",
            "#0000FF",
            "#0033CC",
            "#0033FF",
            "#0066CC",
            "#0066FF",
            "#0099CC",
            "#0099FF",
            "#00CC00",
            "#00CC33",
            "#00CC66",
            "#00CC99",
            "#00CCCC",
            "#00CCFF",
            "#3300CC",
            "#3300FF",
            "#3333CC",
            "#3333FF",
            "#3366CC",
            "#3366FF",
            "#3399CC",
            "#3399FF",
            "#33CC00",
            "#33CC33",
            "#33CC66",
            "#33CC99",
            "#33CCCC",
            "#33CCFF",
            "#6600CC",
            "#6600FF",
            "#6633CC",
            "#6633FF",
            "#66CC00",
            "#66CC33",
            "#9900CC",
            "#9900FF",
            "#9933CC",
            "#9933FF",
            "#99CC00",
            "#99CC33",
            "#CC0000",
            "#CC0033",
            "#CC0066",
            "#CC0099",
            "#CC00CC",
            "#CC00FF",
            "#CC3300",
            "#CC3333",
            "#CC3366",
            "#CC3399",
            "#CC33CC",
            "#CC33FF",
            "#CC6600",
            "#CC6633",
            "#CC9900",
            "#CC9933",
            "#CCCC00",
            "#CCCC33",
            "#FF0000",
            "#FF0033",
            "#FF0066",
            "#FF0099",
            "#FF00CC",
            "#FF00FF",
            "#FF3300",
            "#FF3333",
            "#FF3366",
            "#FF3399",
            "#FF33CC",
            "#FF33FF",
            "#FF6600",
            "#FF6633",
            "#FF9900",
            "#FF9933",
            "#FFCC00",
            "#FFCC33"
        ];
        function useColors() {
            if (typeof window !== "undefined" && window.process && (window.process.type === "renderer" || window.process.__nwjs)) {
                return true;
            }
            if (typeof navigator !== "undefined" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/(edge|trident)\/(\d+)/)) {
                return false;
            }
            let m;
            return (
                (typeof document !== "undefined" && document.documentElement && document.documentElement.style && document.documentElement.style.WebkitAppearance) || // Is firebug? http://stackoverflow.com/a/398120/376773
                (typeof window !== "undefined" && window.console && (window.console.firebug || (window.console.exception && window.console.table))) || // Is firefox >= v31?
                // https://developer.mozilla.org/en-US/docs/Tools/Web_Console#Styling_messages
                (typeof navigator !== "undefined" && navigator.userAgent && (m = navigator.userAgent.toLowerCase().match(/firefox\/(\d+)/)) && parseInt(m[1], 10) >= 31) || // Double check webkit in userAgent just in case we are in a worker
                (typeof navigator !== "undefined" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/applewebkit\/(\d+)/))
            );
        }
        function formatArgs(args) {
            args[0] = (this.useColors ? "%c" : "") + this.namespace + (this.useColors ? " %c" : " ") + args[0] + (this.useColors ? "%c " : " ") + "+" + module2.exports.humanize(this.diff);
            if (!this.useColors) {
                return;
            }
            const c = "color: " + this.color;
            args.splice(1, 0, c, "color: inherit");
            let index2 = 0;
            let lastC = 0;
            args[0].replace(/%[a-zA-Z%]/g, match => {
                if (match === "%%") {
                    return;
                }
                index2++;
                if (match === "%c") {
                    lastC = index2;
                }
            });
            args.splice(lastC, 0, c);
        }
        exports2.log = console.debug || console.log || (() => {});
        function save(namespaces) {
            try {
                if (namespaces) {
                    exports2.storage.setItem("debug", namespaces);
                } else {
                    exports2.storage.removeItem("debug");
                }
            } catch (error) {}
        }
        function load() {
            let r;
            try {
                r = exports2.storage.getItem("debug") || exports2.storage.getItem("DEBUG");
            } catch (error) {}
            if (!r && typeof process !== "undefined" && "env" in process) {
                r = process.env.DEBUG;
            }
            return r;
        }
        function localstorage() {
            try {
                return localStorage;
            } catch (error) {}
        }
        module2.exports = require_common()(exports2);
        var { formatters } = module2.exports;
        formatters.j = function (v) {
            try {
                return JSON.stringify(v);
            } catch (error) {
                return "[UnexpectedJSONParseError]: " + error.message;
            }
        };
    }
});

// node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/node.js
var require_node = __commonJS({
    "node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/node.js"(exports2, module2) {
        var tty = require("tty");
        var util4 = require("util");
        exports2.init = init;
        exports2.log = log;
        exports2.formatArgs = formatArgs;
        exports2.save = save;
        exports2.load = load;
        exports2.useColors = useColors;
        exports2.destroy = util4.deprecate(() => {}, "Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
        exports2.colors = [6, 2, 3, 4, 5, 1];
        try {
            const supportsColor = require("supports-color");
            if (supportsColor && (supportsColor.stderr || supportsColor).level >= 2) {
                exports2.colors = [
                    20, 21, 26, 27, 32, 33, 38, 39, 40, 41, 42, 43, 44, 45, 56, 57, 62, 63, 68, 69, 74, 75, 76, 77, 78, 79, 80, 81, 92, 93, 98, 99, 112, 113, 128, 129, 134, 135, 148, 149, 160, 161, 162, 163, 164, 165, 166, 167, 168, 169, 170, 171, 172, 173, 178, 179, 184, 185, 196, 197, 198, 199,
                    200, 201, 202, 203, 204, 205, 206, 207, 208, 209, 214, 215, 220, 221
                ];
            }
        } catch (error) {}
        exports2.inspectOpts = Object.keys(process.env)
            .filter(key => {
                return /^debug_/i.test(key);
            })
            .reduce((obj, key) => {
                const prop = key
                    .substring(6)
                    .toLowerCase()
                    .replace(/_([a-z])/g, (_, k) => {
                        return k.toUpperCase();
                    });
                let val = process.env[key];
                if (/^(yes|on|true|enabled)$/i.test(val)) {
                    val = true;
                } else if (/^(no|off|false|disabled)$/i.test(val)) {
                    val = false;
                } else if (val === "null") {
                    val = null;
                } else {
                    val = Number(val);
                }
                obj[prop] = val;
                return obj;
            }, {});
        function useColors() {
            return "colors" in exports2.inspectOpts ? Boolean(exports2.inspectOpts.colors) : tty.isatty(process.stderr.fd);
        }
        function formatArgs(args) {
            const { namespace: name, useColors: useColors2 } = this;
            if (useColors2) {
                const c = this.color;
                const colorCode = "\x1B[3" + (c < 8 ? c : "8;5;" + c);
                const prefix = `  ${colorCode};1m${name} \x1B[0m`;
                args[0] = prefix + args[0].split("\n").join("\n" + prefix);
                args.push(colorCode + "m+" + module2.exports.humanize(this.diff) + "\x1B[0m");
            } else {
                args[0] = getDate() + name + " " + args[0];
            }
        }
        function getDate() {
            if (exports2.inspectOpts.hideDate) {
                return "";
            }
            return /* @__PURE__ */ new Date().toISOString() + " ";
        }
        function log(...args) {
            return process.stderr.write(util4.formatWithOptions(exports2.inspectOpts, ...args) + "\n");
        }
        function save(namespaces) {
            if (namespaces) {
                process.env.DEBUG = namespaces;
            } else {
                delete process.env.DEBUG;
            }
        }
        function load() {
            return process.env.DEBUG;
        }
        function init(debug) {
            debug.inspectOpts = {};
            const keys = Object.keys(exports2.inspectOpts);
            for (let i = 0; i < keys.length; i++) {
                debug.inspectOpts[keys[i]] = exports2.inspectOpts[keys[i]];
            }
        }
        module2.exports = require_common()(exports2);
        var { formatters } = module2.exports;
        formatters.o = function (v) {
            this.inspectOpts.colors = this.useColors;
            return util4
                .inspect(v, this.inspectOpts)
                .split("\n")
                .map(str => str.trim())
                .join(" ");
        };
        formatters.O = function (v) {
            this.inspectOpts.colors = this.useColors;
            return util4.inspect(v, this.inspectOpts);
        };
    }
});

// node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/index.js
var require_src = __commonJS({
    "node_modules/.pnpm/debug@4.4.3/node_modules/debug/src/index.js"(exports2, module2) {
        if (typeof process === "undefined" || process.type === "renderer" || process.browser === true || process.__nwjs) {
            module2.exports = require_browser();
        } else {
            module2.exports = require_node();
        }
    }
});

// node_modules/.pnpm/agent-base@6.0.2/node_modules/agent-base/dist/src/promisify.js
var require_promisify = __commonJS({
    "node_modules/.pnpm/agent-base@6.0.2/node_modules/agent-base/dist/src/promisify.js"(exports2) {
        "use strict";
        Object.defineProperty(exports2, "__esModule", { value: true });
        function promisify(fn) {
            return function (req, opts) {
                return new Promise((resolve, reject) => {
                    fn.call(this, req, opts, (err, rtn) => {
                        if (err) {
                            reject(err);
                        } else {
                            resolve(rtn);
                        }
                    });
                });
            };
        }
        exports2.default = promisify;
    }
});

// node_modules/.pnpm/agent-base@6.0.2/node_modules/agent-base/dist/src/index.js
var require_src2 = __commonJS({
    "node_modules/.pnpm/agent-base@6.0.2/node_modules/agent-base/dist/src/index.js"(exports2, module2) {
        "use strict";
        var __importDefault =
            (exports2 && exports2.__importDefault) ||
            function (mod) {
                return mod && mod.__esModule ? mod : { default: mod };
            };
        var events_1 = require("events");
        var debug_1 = __importDefault(require_src());
        var promisify_1 = __importDefault(require_promisify());
        var debug = debug_1.default("agent-base");
        function isAgent(v) {
            return Boolean(v) && typeof v.addRequest === "function";
        }
        function isSecureEndpoint() {
            const { stack } = new Error();
            if (typeof stack !== "string") return false;
            return stack.split("\n").some(l => l.indexOf("(https.js:") !== -1 || l.indexOf("node:https:") !== -1);
        }
        function createAgent(callback, opts) {
            return new createAgent.Agent(callback, opts);
        }
        (function (createAgent2) {
            class Agent extends events_1.EventEmitter {
                constructor(callback, _opts) {
                    super();
                    let opts = _opts;
                    if (typeof callback === "function") {
                        this.callback = callback;
                    } else if (callback) {
                        opts = callback;
                    }
                    this.timeout = null;
                    if (opts && typeof opts.timeout === "number") {
                        this.timeout = opts.timeout;
                    }
                    this.maxFreeSockets = 1;
                    this.maxSockets = 1;
                    this.maxTotalSockets = Infinity;
                    this.sockets = {};
                    this.freeSockets = {};
                    this.requests = {};
                    this.options = {};
                }
                get defaultPort() {
                    if (typeof this.explicitDefaultPort === "number") {
                        return this.explicitDefaultPort;
                    }
                    return isSecureEndpoint() ? 443 : 80;
                }
                set defaultPort(v) {
                    this.explicitDefaultPort = v;
                }
                get protocol() {
                    if (typeof this.explicitProtocol === "string") {
                        return this.explicitProtocol;
                    }
                    return isSecureEndpoint() ? "https:" : "http:";
                }
                set protocol(v) {
                    this.explicitProtocol = v;
                }
                callback(req, opts, fn) {
                    throw new Error('"agent-base" has no default implementation, you must subclass and override `callback()`');
                }
                /**
                 * Called by node-core's "_http_client.js" module when creating
                 * a new HTTP request with this Agent instance.
                 *
                 * @api public
                 */
                addRequest(req, _opts) {
                    const opts = Object.assign({}, _opts);
                    if (typeof opts.secureEndpoint !== "boolean") {
                        opts.secureEndpoint = isSecureEndpoint();
                    }
                    if (opts.host == null) {
                        opts.host = "localhost";
                    }
                    if (opts.port == null) {
                        opts.port = opts.secureEndpoint ? 443 : 80;
                    }
                    if (opts.protocol == null) {
                        opts.protocol = opts.secureEndpoint ? "https:" : "http:";
                    }
                    if (opts.host && opts.path) {
                        delete opts.path;
                    }
                    delete opts.agent;
                    delete opts.hostname;
                    delete opts._defaultAgent;
                    delete opts.defaultPort;
                    delete opts.createConnection;
                    req._last = true;
                    req.shouldKeepAlive = false;
                    let timedOut = false;
                    let timeoutId = null;
                    const timeoutMs = opts.timeout || this.timeout;
                    const onerror = err => {
                        if (req._hadError) return;
                        req.emit("error", err);
                        req._hadError = true;
                    };
                    const ontimeout = () => {
                        timeoutId = null;
                        timedOut = true;
                        const err = new Error(`A "socket" was not created for HTTP request before ${timeoutMs}ms`);
                        err.code = "ETIMEOUT";
                        onerror(err);
                    };
                    const callbackError = err => {
                        if (timedOut) return;
                        if (timeoutId !== null) {
                            clearTimeout(timeoutId);
                            timeoutId = null;
                        }
                        onerror(err);
                    };
                    const onsocket = socket => {
                        if (timedOut) return;
                        if (timeoutId != null) {
                            clearTimeout(timeoutId);
                            timeoutId = null;
                        }
                        if (isAgent(socket)) {
                            debug("Callback returned another Agent instance %o", socket.constructor.name);
                            socket.addRequest(req, opts);
                            return;
                        }
                        if (socket) {
                            socket.once("free", () => {
                                this.freeSocket(socket, opts);
                            });
                            req.onSocket(socket);
                            return;
                        }
                        const err = new Error(`no Duplex stream was returned to agent-base for \`${req.method} ${req.path}\``);
                        onerror(err);
                    };
                    if (typeof this.callback !== "function") {
                        onerror(new Error("`callback` is not defined"));
                        return;
                    }
                    if (!this.promisifiedCallback) {
                        if (this.callback.length >= 3) {
                            debug("Converting legacy callback function to promise");
                            this.promisifiedCallback = promisify_1.default(this.callback);
                        } else {
                            this.promisifiedCallback = this.callback;
                        }
                    }
                    if (typeof timeoutMs === "number" && timeoutMs > 0) {
                        timeoutId = setTimeout(ontimeout, timeoutMs);
                    }
                    if ("port" in opts && typeof opts.port !== "number") {
                        opts.port = Number(opts.port);
                    }
                    try {
                        debug("Resolving socket for %o request: %o", opts.protocol, `${req.method} ${req.path}`);
                        Promise.resolve(this.promisifiedCallback(req, opts)).then(onsocket, callbackError);
                    } catch (err) {
                        Promise.reject(err).catch(callbackError);
                    }
                }
                freeSocket(socket, opts) {
                    debug("Freeing socket %o %o", socket.constructor.name, opts);
                    socket.destroy();
                }
                destroy() {
                    debug("Destroying agent %o", this.constructor.name);
                }
            }
            createAgent2.Agent = Agent;
            createAgent2.prototype = createAgent2.Agent.prototype;
        })(createAgent || (createAgent = {}));
        module2.exports = createAgent;
    }
});

// node_modules/.pnpm/https-proxy-agent@5.0.1/node_modules/https-proxy-agent/dist/parse-proxy-response.js
var require_parse_proxy_response = __commonJS({
    "node_modules/.pnpm/https-proxy-agent@5.0.1/node_modules/https-proxy-agent/dist/parse-proxy-response.js"(exports2) {
        "use strict";
        var __importDefault =
            (exports2 && exports2.__importDefault) ||
            function (mod) {
                return mod && mod.__esModule ? mod : { default: mod };
            };
        Object.defineProperty(exports2, "__esModule", { value: true });
        var debug_1 = __importDefault(require_src());
        var debug = debug_1.default("https-proxy-agent:parse-proxy-response");
        function parseProxyResponse(socket) {
            return new Promise((resolve, reject) => {
                let buffersLength = 0;
                const buffers = [];
                function read() {
                    const b = socket.read();
                    if (b) ondata(b);
                    else socket.once("readable", read);
                }
                function cleanup() {
                    socket.removeListener("end", onend);
                    socket.removeListener("error", onerror);
                    socket.removeListener("close", onclose);
                    socket.removeListener("readable", read);
                }
                function onclose(err) {
                    debug("onclose had error %o", err);
                }
                function onend() {
                    debug("onend");
                }
                function onerror(err) {
                    cleanup();
                    debug("onerror %o", err);
                    reject(err);
                }
                function ondata(b) {
                    buffers.push(b);
                    buffersLength += b.length;
                    const buffered = Buffer.concat(buffers, buffersLength);
                    const endOfHeaders = buffered.indexOf("\r\n\r\n");
                    if (endOfHeaders === -1) {
                        debug("have not received end of HTTP headers yet...");
                        read();
                        return;
                    }
                    const firstLine = buffered.toString("ascii", 0, buffered.indexOf("\r\n"));
                    const statusCode = +firstLine.split(" ")[1];
                    debug("got proxy server response: %o", firstLine);
                    resolve({
                        statusCode,
                        buffered
                    });
                }
                socket.on("error", onerror);
                socket.on("close", onclose);
                socket.on("end", onend);
                read();
            });
        }
        exports2.default = parseProxyResponse;
    }
});

// node_modules/.pnpm/https-proxy-agent@5.0.1/node_modules/https-proxy-agent/dist/agent.js
var require_agent = __commonJS({
    "node_modules/.pnpm/https-proxy-agent@5.0.1/node_modules/https-proxy-agent/dist/agent.js"(exports2) {
        "use strict";
        var __awaiter =
            (exports2 && exports2.__awaiter) ||
            function (thisArg, _arguments, P, generator) {
                function adopt(value) {
                    return value instanceof P
                        ? value
                        : new P(function (resolve) {
                              resolve(value);
                          });
                }
                return new (P || (P = Promise))(function (resolve, reject) {
                    function fulfilled(value) {
                        try {
                            step(generator.next(value));
                        } catch (e) {
                            reject(e);
                        }
                    }
                    function rejected(value) {
                        try {
                            step(generator["throw"](value));
                        } catch (e) {
                            reject(e);
                        }
                    }
                    function step(result) {
                        result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
                    }
                    step((generator = generator.apply(thisArg, _arguments || [])).next());
                });
            };
        var __importDefault =
            (exports2 && exports2.__importDefault) ||
            function (mod) {
                return mod && mod.__esModule ? mod : { default: mod };
            };
        Object.defineProperty(exports2, "__esModule", { value: true });
        var net_1 = __importDefault(require("net"));
        var tls_1 = __importDefault(require("tls"));
        var url_1 = __importDefault(require("url"));
        var assert_1 = __importDefault(require("assert"));
        var debug_1 = __importDefault(require_src());
        var agent_base_1 = require_src2();
        var parse_proxy_response_1 = __importDefault(require_parse_proxy_response());
        var debug = debug_1.default("https-proxy-agent:agent");
        var HttpsProxyAgent2 = class extends agent_base_1.Agent {
            constructor(_opts) {
                let opts;
                if (typeof _opts === "string") {
                    opts = url_1.default.parse(_opts);
                } else {
                    opts = _opts;
                }
                if (!opts) {
                    throw new Error("an HTTP(S) proxy server `host` and `port` must be specified!");
                }
                debug("creating new HttpsProxyAgent instance: %o", opts);
                super(opts);
                const proxy = Object.assign({}, opts);
                this.secureProxy = opts.secureProxy || isHTTPS(proxy.protocol);
                proxy.host = proxy.hostname || proxy.host;
                if (typeof proxy.port === "string") {
                    proxy.port = parseInt(proxy.port, 10);
                }
                if (!proxy.port && proxy.host) {
                    proxy.port = this.secureProxy ? 443 : 80;
                }
                if (this.secureProxy && !("ALPNProtocols" in proxy)) {
                    proxy.ALPNProtocols = ["http 1.1"];
                }
                if (proxy.host && proxy.path) {
                    delete proxy.path;
                    delete proxy.pathname;
                }
                this.proxy = proxy;
            }
            /**
             * Called when the node-core HTTP client library is creating a
             * new HTTP request.
             *
             * @api protected
             */
            callback(req, opts) {
                return __awaiter(this, void 0, void 0, function* () {
                    const { proxy, secureProxy } = this;
                    let socket;
                    if (secureProxy) {
                        debug("Creating `tls.Socket`: %o", proxy);
                        socket = tls_1.default.connect(proxy);
                    } else {
                        debug("Creating `net.Socket`: %o", proxy);
                        socket = net_1.default.connect(proxy);
                    }
                    const headers = Object.assign({}, proxy.headers);
                    const hostname = `${opts.host}:${opts.port}`;
                    let payload = `CONNECT ${hostname} HTTP/1.1\r
`;
                    if (proxy.auth) {
                        headers["Proxy-Authorization"] = `Basic ${Buffer.from(proxy.auth).toString("base64")}`;
                    }
                    let { host, port, secureEndpoint } = opts;
                    if (!isDefaultPort(port, secureEndpoint)) {
                        host += `:${port}`;
                    }
                    headers.Host = host;
                    headers.Connection = "close";
                    for (const name of Object.keys(headers)) {
                        payload += `${name}: ${headers[name]}\r
`;
                    }
                    const proxyResponsePromise = parse_proxy_response_1.default(socket);
                    socket.write(`${payload}\r
`);
                    const { statusCode, buffered } = yield proxyResponsePromise;
                    if (statusCode === 200) {
                        req.once("socket", resume);
                        if (opts.secureEndpoint) {
                            debug("Upgrading socket connection to TLS");
                            const servername = opts.servername || opts.host;
                            return tls_1.default.connect(
                                Object.assign(Object.assign({}, omit(opts, "host", "hostname", "path", "port")), {
                                    socket,
                                    servername
                                })
                            );
                        }
                        return socket;
                    }
                    socket.destroy();
                    const fakeSocket = new net_1.default.Socket({ writable: false });
                    fakeSocket.readable = true;
                    req.once("socket", s => {
                        debug("replaying proxy buffer for failed request");
                        assert_1.default(s.listenerCount("data") > 0);
                        s.push(buffered);
                        s.push(null);
                    });
                    return fakeSocket;
                });
            }
        };
        exports2.default = HttpsProxyAgent2;
        function resume(socket) {
            socket.resume();
        }
        function isDefaultPort(port, secure) {
            return Boolean((!secure && port === 80) || (secure && port === 443));
        }
        function isHTTPS(protocol) {
            return typeof protocol === "string" ? /^https:?$/i.test(protocol) : false;
        }
        function omit(obj, ...keys) {
            const ret = {};
            let key;
            for (key in obj) {
                if (!keys.includes(key)) {
                    ret[key] = obj[key];
                }
            }
            return ret;
        }
    }
});

// node_modules/.pnpm/https-proxy-agent@5.0.1/node_modules/https-proxy-agent/dist/index.js
var require_dist = __commonJS({
    "node_modules/.pnpm/https-proxy-agent@5.0.1/node_modules/https-proxy-agent/dist/index.js"(exports2, module2) {
        "use strict";
        var __importDefault =
            (exports2 && exports2.__importDefault) ||
            function (mod) {
                return mod && mod.__esModule ? mod : { default: mod };
            };
        var agent_1 = __importDefault(require_agent());
        function createHttpsProxyAgent(opts) {
            return new agent_1.default(opts);
        }
        (function (createHttpsProxyAgent2) {
            createHttpsProxyAgent2.HttpsProxyAgent = agent_1.default;
            createHttpsProxyAgent2.prototype = agent_1.default.prototype;
        })(createHttpsProxyAgent || (createHttpsProxyAgent = {}));
        module2.exports = createHttpsProxyAgent;
    }
});

// node_modules/.pnpm/follow-redirects@1.16.0/node_modules/follow-redirects/debug.js
var require_debug = __commonJS({
    "node_modules/.pnpm/follow-redirects@1.16.0/node_modules/follow-redirects/debug.js"(exports2, module2) {
        var debug;
        module2.exports = function () {
            if (!debug) {
                try {
                    debug = require_src()("follow-redirects");
                } catch (error) {}
                if (typeof debug !== "function") {
                    debug = function () {};
                }
            }
            debug.apply(null, arguments);
        };
    }
});

// node_modules/.pnpm/follow-redirects@1.16.0/node_modules/follow-redirects/index.js
var require_follow_redirects = __commonJS({
    "node_modules/.pnpm/follow-redirects@1.16.0/node_modules/follow-redirects/index.js"(exports2, module2) {
        var url2 = require("url");
        var URL2 = url2.URL;
        var http4 = require("http");
        var https2 = require("https");
        var Writable = require("stream").Writable;
        var assert = require("assert");
        var debug = require_debug();
        (function detectUnsupportedEnvironment() {
            var looksLikeNode = typeof process !== "undefined";
            var looksLikeBrowser = typeof window !== "undefined" && typeof document !== "undefined";
            var looksLikeV8 = isFunction3(Error.captureStackTrace);
            if (!looksLikeNode && (looksLikeBrowser || !looksLikeV8)) {
                console.warn("The follow-redirects package should be excluded from browser builds.");
            }
        })();
        var useNativeURL = false;
        try {
            assert(new URL2(""));
        } catch (error) {
            useNativeURL = error.code === "ERR_INVALID_URL";
        }
        var sensitiveHeaders = ["Authorization", "Proxy-Authorization", "Cookie"];
        var preservedUrlFields = ["auth", "host", "hostname", "href", "path", "pathname", "port", "protocol", "query", "search", "hash"];
        var events = ["abort", "aborted", "connect", "error", "socket", "timeout"];
        var eventHandlers = /* @__PURE__ */ Object.create(null);
        events.forEach(function (event) {
            eventHandlers[event] = function (arg1, arg2, arg3) {
                this._redirectable.emit(event, arg1, arg2, arg3);
            };
        });
        var InvalidUrlError = createErrorType("ERR_INVALID_URL", "Invalid URL", TypeError);
        var RedirectionError = createErrorType("ERR_FR_REDIRECTION_FAILURE", "Redirected request failed");
        var TooManyRedirectsError = createErrorType("ERR_FR_TOO_MANY_REDIRECTS", "Maximum number of redirects exceeded", RedirectionError);
        var MaxBodyLengthExceededError = createErrorType("ERR_FR_MAX_BODY_LENGTH_EXCEEDED", "Request body larger than maxBodyLength limit");
        var WriteAfterEndError = createErrorType("ERR_STREAM_WRITE_AFTER_END", "write after end");
        var destroy = Writable.prototype.destroy || noop2;
        function RedirectableRequest(options, responseCallback) {
            Writable.call(this);
            this._sanitizeOptions(options);
            this._options = options;
            this._ended = false;
            this._ending = false;
            this._redirectCount = 0;
            this._redirects = [];
            this._requestBodyLength = 0;
            this._requestBodyBuffers = [];
            if (responseCallback) {
                this.on("response", responseCallback);
            }
            var self2 = this;
            this._onNativeResponse = function (response) {
                try {
                    self2._processResponse(response);
                } catch (cause) {
                    self2.emit("error", cause instanceof RedirectionError ? cause : new RedirectionError({ cause }));
                }
            };
            this._headerFilter = new RegExp("^(?:" + sensitiveHeaders.concat(options.sensitiveHeaders).map(escapeRegex).join("|") + ")$", "i");
            this._performRequest();
        }
        RedirectableRequest.prototype = Object.create(Writable.prototype);
        RedirectableRequest.prototype.abort = function () {
            destroyRequest(this._currentRequest);
            this._currentRequest.abort();
            this.emit("abort");
        };
        RedirectableRequest.prototype.destroy = function (error) {
            destroyRequest(this._currentRequest, error);
            destroy.call(this, error);
            return this;
        };
        RedirectableRequest.prototype.write = function (data, encoding, callback) {
            if (this._ending) {
                throw new WriteAfterEndError();
            }
            if (!isString2(data) && !isBuffer2(data)) {
                throw new TypeError("data should be a string, Buffer or Uint8Array");
            }
            if (isFunction3(encoding)) {
                callback = encoding;
                encoding = null;
            }
            if (data.length === 0) {
                if (callback) {
                    callback();
                }
                return;
            }
            if (this._requestBodyLength + data.length <= this._options.maxBodyLength) {
                this._requestBodyLength += data.length;
                this._requestBodyBuffers.push({ data, encoding });
                this._currentRequest.write(data, encoding, callback);
            } else {
                this.emit("error", new MaxBodyLengthExceededError());
                this.abort();
            }
        };
        RedirectableRequest.prototype.end = function (data, encoding, callback) {
            if (isFunction3(data)) {
                callback = data;
                data = encoding = null;
            } else if (isFunction3(encoding)) {
                callback = encoding;
                encoding = null;
            }
            if (!data) {
                this._ended = this._ending = true;
                this._currentRequest.end(null, null, callback);
            } else {
                var self2 = this;
                var currentRequest = this._currentRequest;
                this.write(data, encoding, function () {
                    self2._ended = true;
                    currentRequest.end(null, null, callback);
                });
                this._ending = true;
            }
        };
        RedirectableRequest.prototype.setHeader = function (name, value) {
            this._options.headers[name] = value;
            this._currentRequest.setHeader(name, value);
        };
        RedirectableRequest.prototype.removeHeader = function (name) {
            delete this._options.headers[name];
            this._currentRequest.removeHeader(name);
        };
        RedirectableRequest.prototype.setTimeout = function (msecs, callback) {
            var self2 = this;
            function destroyOnTimeout(socket) {
                socket.setTimeout(msecs);
                socket.removeListener("timeout", socket.destroy);
                socket.addListener("timeout", socket.destroy);
            }
            function startTimer(socket) {
                if (self2._timeout) {
                    clearTimeout(self2._timeout);
                }
                self2._timeout = setTimeout(function () {
                    self2.emit("timeout");
                    clearTimer();
                }, msecs);
                destroyOnTimeout(socket);
            }
            function clearTimer() {
                if (self2._timeout) {
                    clearTimeout(self2._timeout);
                    self2._timeout = null;
                }
                self2.removeListener("abort", clearTimer);
                self2.removeListener("error", clearTimer);
                self2.removeListener("response", clearTimer);
                self2.removeListener("close", clearTimer);
                if (callback) {
                    self2.removeListener("timeout", callback);
                }
                if (!self2.socket) {
                    self2._currentRequest.removeListener("socket", startTimer);
                }
            }
            if (callback) {
                this.on("timeout", callback);
            }
            if (this.socket) {
                startTimer(this.socket);
            } else {
                this._currentRequest.once("socket", startTimer);
            }
            this.on("socket", destroyOnTimeout);
            this.on("abort", clearTimer);
            this.on("error", clearTimer);
            this.on("response", clearTimer);
            this.on("close", clearTimer);
            return this;
        };
        ["flushHeaders", "getHeader", "setNoDelay", "setSocketKeepAlive"].forEach(function (method) {
            RedirectableRequest.prototype[method] = function (a, b) {
                return this._currentRequest[method](a, b);
            };
        });
        ["aborted", "connection", "socket"].forEach(function (property) {
            Object.defineProperty(RedirectableRequest.prototype, property, {
                get: function () {
                    return this._currentRequest[property];
                }
            });
        });
        RedirectableRequest.prototype._sanitizeOptions = function (options) {
            if (!options.headers) {
                options.headers = {};
            }
            if (!isArray3(options.sensitiveHeaders)) {
                options.sensitiveHeaders = [];
            }
            if (options.host) {
                if (!options.hostname) {
                    options.hostname = options.host;
                }
                delete options.host;
            }
            if (!options.pathname && options.path) {
                var searchPos = options.path.indexOf("?");
                if (searchPos < 0) {
                    options.pathname = options.path;
                } else {
                    options.pathname = options.path.substring(0, searchPos);
                    options.search = options.path.substring(searchPos);
                }
            }
        };
        RedirectableRequest.prototype._performRequest = function () {
            var protocol = this._options.protocol;
            var nativeProtocol = this._options.nativeProtocols[protocol];
            if (!nativeProtocol) {
                throw new TypeError("Unsupported protocol " + protocol);
            }
            if (this._options.agents) {
                var scheme = protocol.slice(0, -1);
                this._options.agent = this._options.agents[scheme];
            }
            var request2 = (this._currentRequest = nativeProtocol.request(this._options, this._onNativeResponse));
            request2._redirectable = this;
            for (var event of events) {
                request2.on(event, eventHandlers[event]);
            }
            this._currentUrl = /^\//.test(this._options.path)
                ? url2.format(this._options)
                : // When making a request to a proxy, […]
                  // a client MUST send the target URI in absolute-form […].
                  this._options.path;
            if (this._isRedirect) {
                var i = 0;
                var self2 = this;
                var buffers = this._requestBodyBuffers;
                (function writeNext(error) {
                    if (request2 === self2._currentRequest) {
                        if (error) {
                            self2.emit("error", error);
                        } else if (i < buffers.length) {
                            var buffer = buffers[i++];
                            if (!request2.finished) {
                                request2.write(buffer.data, buffer.encoding, writeNext);
                            }
                        } else if (self2._ended) {
                            request2.end();
                        }
                    }
                })();
            }
        };
        RedirectableRequest.prototype._processResponse = function (response) {
            var statusCode = response.statusCode;
            if (this._options.trackRedirects) {
                this._redirects.push({
                    url: this._currentUrl,
                    headers: response.headers,
                    statusCode
                });
            }
            var location = response.headers.location;
            if (!location || this._options.followRedirects === false || statusCode < 300 || statusCode >= 400) {
                response.responseUrl = this._currentUrl;
                response.redirects = this._redirects;
                this.emit("response", response);
                this._requestBodyBuffers = [];
                return;
            }
            destroyRequest(this._currentRequest);
            response.destroy();
            if (++this._redirectCount > this._options.maxRedirects) {
                throw new TooManyRedirectsError();
            }
            var requestHeaders;
            var beforeRedirect = this._options.beforeRedirect;
            if (beforeRedirect) {
                requestHeaders = Object.assign(
                    {
                        // The Host header was set by nativeProtocol.request
                        Host: response.req.getHeader("host")
                    },
                    this._options.headers
                );
            }
            var method = this._options.method;
            if (
                ((statusCode === 301 || statusCode === 302) && this._options.method === "POST") || // RFC7231§6.4.4: The 303 (See Other) status code indicates that
                // the server is redirecting the user agent to a different resource […]
                // A user agent can perform a retrieval request targeting that URI
                // (a GET or HEAD request if using HTTP) […]
                (statusCode === 303 && !/^(?:GET|HEAD)$/.test(this._options.method))
            ) {
                this._options.method = "GET";
                this._requestBodyBuffers = [];
                removeMatchingHeaders(/^content-/i, this._options.headers);
            }
            var currentHostHeader = removeMatchingHeaders(/^host$/i, this._options.headers);
            var currentUrlParts = parseUrl2(this._currentUrl);
            var currentHost = currentHostHeader || currentUrlParts.host;
            var currentUrl = /^\w+:/.test(location) ? this._currentUrl : url2.format(Object.assign(currentUrlParts, { host: currentHost }));
            var redirectUrl = resolveUrl(location, currentUrl);
            debug("redirecting to", redirectUrl.href);
            this._isRedirect = true;
            spreadUrlObject(redirectUrl, this._options);
            if ((redirectUrl.protocol !== currentUrlParts.protocol && redirectUrl.protocol !== "https:") || (redirectUrl.host !== currentHost && !isSubdomain(redirectUrl.host, currentHost))) {
                removeMatchingHeaders(this._headerFilter, this._options.headers);
            }
            if (isFunction3(beforeRedirect)) {
                var responseDetails = {
                    headers: response.headers,
                    statusCode
                };
                var requestDetails = {
                    url: currentUrl,
                    method,
                    headers: requestHeaders
                };
                beforeRedirect(this._options, responseDetails, requestDetails);
                this._sanitizeOptions(this._options);
            }
            this._performRequest();
        };
        function wrap2(protocols) {
            var exports3 = {
                maxRedirects: 21,
                maxBodyLength: 10 * 1024 * 1024
            };
            var nativeProtocols = {};
            Object.keys(protocols).forEach(function (scheme) {
                var protocol = scheme + ":";
                var nativeProtocol = (nativeProtocols[protocol] = protocols[scheme]);
                var wrappedProtocol = (exports3[scheme] = Object.create(nativeProtocol));
                function request2(input, options, callback) {
                    if (isURL(input)) {
                        input = spreadUrlObject(input);
                    } else if (isString2(input)) {
                        input = spreadUrlObject(parseUrl2(input));
                    } else {
                        callback = options;
                        options = validateUrl(input);
                        input = { protocol };
                    }
                    if (isFunction3(options)) {
                        callback = options;
                        options = null;
                    }
                    options = Object.assign(
                        {
                            maxRedirects: exports3.maxRedirects,
                            maxBodyLength: exports3.maxBodyLength
                        },
                        input,
                        options
                    );
                    options.nativeProtocols = nativeProtocols;
                    if (!isString2(options.host) && !isString2(options.hostname)) {
                        options.hostname = "::1";
                    }
                    assert.equal(options.protocol, protocol, "protocol mismatch");
                    debug("options", options);
                    return new RedirectableRequest(options, callback);
                }
                function get70(input, options, callback) {
                    var wrappedRequest = wrappedProtocol.request(input, options, callback);
                    wrappedRequest.end();
                    return wrappedRequest;
                }
                Object.defineProperties(wrappedProtocol, {
                    request: { value: request2, configurable: true, enumerable: true, writable: true },
                    get: { value: get70, configurable: true, enumerable: true, writable: true }
                });
            });
            return exports3;
        }
        function noop2() {}
        function parseUrl2(input) {
            var parsed;
            if (useNativeURL) {
                parsed = new URL2(input);
            } else {
                parsed = validateUrl(url2.parse(input));
                if (!isString2(parsed.protocol)) {
                    throw new InvalidUrlError({ input });
                }
            }
            return parsed;
        }
        function resolveUrl(relative, base) {
            return useNativeURL ? new URL2(relative, base) : parseUrl2(url2.resolve(base, relative));
        }
        function validateUrl(input) {
            if (/^\[/.test(input.hostname) && !/^\[[:0-9a-f]+\]$/i.test(input.hostname)) {
                throw new InvalidUrlError({ input: input.href || input });
            }
            if (/^\[/.test(input.host) && !/^\[[:0-9a-f]+\](:\d+)?$/i.test(input.host)) {
                throw new InvalidUrlError({ input: input.href || input });
            }
            return input;
        }
        function spreadUrlObject(urlObject, target) {
            var spread3 = target || {};
            for (var key of preservedUrlFields) {
                spread3[key] = urlObject[key];
            }
            if (spread3.hostname.startsWith("[")) {
                spread3.hostname = spread3.hostname.slice(1, -1);
            }
            if (spread3.port !== "") {
                spread3.port = Number(spread3.port);
            }
            spread3.path = spread3.search ? spread3.pathname + spread3.search : spread3.pathname;
            return spread3;
        }
        function removeMatchingHeaders(regex, headers) {
            var lastValue;
            for (var header in headers) {
                if (regex.test(header)) {
                    lastValue = headers[header];
                    delete headers[header];
                }
            }
            return lastValue === null || typeof lastValue === "undefined" ? void 0 : String(lastValue).trim();
        }
        function createErrorType(code, message, baseClass) {
            function CustomError(properties) {
                if (isFunction3(Error.captureStackTrace)) {
                    Error.captureStackTrace(this, this.constructor);
                }
                Object.assign(this, properties || {});
                this.code = code;
                this.message = this.cause ? message + ": " + this.cause.message : message;
            }
            CustomError.prototype = new (baseClass || Error)();
            Object.defineProperties(CustomError.prototype, {
                constructor: {
                    value: CustomError,
                    enumerable: false
                },
                name: {
                    value: "Error [" + code + "]",
                    enumerable: false
                }
            });
            return CustomError;
        }
        function destroyRequest(request2, error) {
            for (var event of events) {
                request2.removeListener(event, eventHandlers[event]);
            }
            request2.on("error", noop2);
            request2.destroy(error);
        }
        function isSubdomain(subdomain, domain) {
            assert(isString2(subdomain) && isString2(domain));
            var dot = subdomain.length - domain.length - 1;
            return dot > 0 && subdomain[dot] === "." && subdomain.endsWith(domain);
        }
        function isArray3(value) {
            return value instanceof Array;
        }
        function isString2(value) {
            return typeof value === "string" || value instanceof String;
        }
        function isFunction3(value) {
            return typeof value === "function";
        }
        function isBuffer2(value) {
            return typeof value === "object" && "length" in value;
        }
        function isURL(value) {
            return URL2 && value instanceof URL2;
        }
        function escapeRegex(regex) {
            return regex.replace(/[\]\\/()*+?.$]/g, "\\$&");
        }
        module2.exports = wrap2({ http: http4, https: https2 });
        module2.exports.wrap = wrap2;
    }
});

// src/publish-worker.ts
var publish_worker_exports = {};
__export(publish_worker_exports, {
    parsePayload: () => parsePayload
});
module.exports = __toCommonJS(publish_worker_exports);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/index.mjs
var import_contentful_sdk_core82 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/bind.js
function bind(fn, thisArg) {
    return function wrap2() {
        return fn.apply(thisArg, arguments);
    };
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/utils.js
var { toString } = Object.prototype;
var { getPrototypeOf } = Object;
var { iterator, toStringTag } = Symbol;
var hasOwnProperty = (
    ({ hasOwnProperty: hasOwnProperty3 }) =>
    (obj, prop) =>
        hasOwnProperty3.call(obj, prop)
)(Object.prototype);
var hasOwnInPrototypeChain = (thing, prop) => {
    let obj = thing;
    const seen = [];
    while (obj != null && obj !== Object.prototype) {
        if (seen.indexOf(obj) !== -1) {
            return false;
        }
        seen.push(obj);
        if (hasOwnProperty(obj, prop)) {
            return true;
        }
        obj = getPrototypeOf(obj);
    }
    return false;
};
var getSafeProp = (obj, prop) => (obj != null && hasOwnInPrototypeChain(obj, prop) ? obj[prop] : void 0);
var kindOf = /* @__PURE__ */ (cache => thing => {
    const str = toString.call(thing);
    return cache[str] || (cache[str] = str.slice(8, -1).toLowerCase());
})(/* @__PURE__ */ Object.create(null));
var kindOfTest = type => {
    type = type.toLowerCase();
    return thing => kindOf(thing) === type;
};
var typeOfTest = type => thing => typeof thing === type;
var { isArray } = Array;
var isUndefined = typeOfTest("undefined");
function isBuffer(val) {
    return val !== null && !isUndefined(val) && val.constructor !== null && !isUndefined(val.constructor) && isFunction(val.constructor.isBuffer) && val.constructor.isBuffer(val);
}
var isArrayBuffer = kindOfTest("ArrayBuffer");
function isArrayBufferView(val) {
    let result;
    if (typeof ArrayBuffer !== "undefined" && ArrayBuffer.isView) {
        result = ArrayBuffer.isView(val);
    } else {
        result = val && val.buffer && isArrayBuffer(val.buffer);
    }
    return result;
}
var isString = typeOfTest("string");
var isFunction = typeOfTest("function");
var isNumber = typeOfTest("number");
var isObject = thing => thing !== null && typeof thing === "object";
var isBoolean = thing => thing === true || thing === false;
var isPlainObject = val => {
    if (!isObject(val)) {
        return false;
    }
    const prototype2 = getPrototypeOf(val);
    return (
        (prototype2 === null || prototype2 === Object.prototype || getPrototypeOf(prototype2) === null) && // Treat any genuine (non-Object.prototype-polluted) Symbol.toStringTag or
        // Symbol.iterator as evidence the value is a tagged/iterable type rather
        // than a plain object, while ignoring keys injected onto Object.prototype.
        !hasOwnInPrototypeChain(val, toStringTag) &&
        !hasOwnInPrototypeChain(val, iterator)
    );
};
var isEmptyObject = val => {
    if (!isObject(val) || isBuffer(val)) {
        return false;
    }
    try {
        return Object.keys(val).length === 0 && Object.getPrototypeOf(val) === Object.prototype;
    } catch (e) {
        return false;
    }
};
var isDate = kindOfTest("Date");
var isFile = kindOfTest("File");
var isReactNativeBlob = value => {
    return !!(value && typeof value.uri !== "undefined");
};
var isReactNative = formData => formData && typeof formData.getParts !== "undefined";
var isBlob = kindOfTest("Blob");
var isFileList = kindOfTest("FileList");
var isStream = val => isObject(val) && isFunction(val.pipe);
function getGlobal() {
    if (typeof globalThis !== "undefined") return globalThis;
    if (typeof self !== "undefined") return self;
    if (typeof window !== "undefined") return window;
    if (typeof global !== "undefined") return global;
    return {};
}
var G = getGlobal();
var FormDataCtor = typeof G.FormData !== "undefined" ? G.FormData : void 0;
var isFormData = thing => {
    if (!thing) return false;
    if (FormDataCtor && thing instanceof FormDataCtor) return true;
    const proto = getPrototypeOf(thing);
    if (!proto || proto === Object.prototype) return false;
    if (!isFunction(thing.append)) return false;
    const kind = kindOf(thing);
    return (
        kind === "formdata" || // detect form-data instance
        (kind === "object" && isFunction(thing.toString) && thing.toString() === "[object FormData]")
    );
};
var isURLSearchParams = kindOfTest("URLSearchParams");
var [isReadableStream, isRequest, isResponse, isHeaders] = ["ReadableStream", "Request", "Response", "Headers"].map(kindOfTest);
var trim = str => {
    return str.trim ? str.trim() : str.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, "");
};
function forEach(obj, fn, { allOwnKeys = false } = {}) {
    if (obj === null || typeof obj === "undefined") {
        return;
    }
    let i;
    let l;
    if (typeof obj !== "object") {
        obj = [obj];
    }
    if (isArray(obj)) {
        for (i = 0, l = obj.length; i < l; i++) {
            fn.call(null, obj[i], i, obj);
        }
    } else {
        if (isBuffer(obj)) {
            return;
        }
        const keys = allOwnKeys ? Object.getOwnPropertyNames(obj) : Object.keys(obj);
        const len = keys.length;
        let key;
        for (i = 0; i < len; i++) {
            key = keys[i];
            fn.call(null, obj[key], key, obj);
        }
    }
}
function findKey(obj, key) {
    if (isBuffer(obj)) {
        return null;
    }
    key = key.toLowerCase();
    const keys = Object.keys(obj);
    let i = keys.length;
    let _key;
    while (i-- > 0) {
        _key = keys[i];
        if (key === _key.toLowerCase()) {
            return _key;
        }
    }
    return null;
}
var _global = (() => {
    if (typeof globalThis !== "undefined") return globalThis;
    return typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : global;
})();
var isContextDefined = context => !isUndefined(context) && context !== _global;
function merge(...objs) {
    const { caseless, skipUndefined } = (isContextDefined(this) && this) || {};
    const result = {};
    const assignValue = (val, key) => {
        if (key === "__proto__" || key === "constructor" || key === "prototype") {
            return;
        }
        const targetKey = (caseless && typeof key === "string" && findKey(result, key)) || key;
        const existing = hasOwnProperty(result, targetKey) ? result[targetKey] : void 0;
        if (isPlainObject(existing) && isPlainObject(val)) {
            result[targetKey] = merge(existing, val);
        } else if (isPlainObject(val)) {
            result[targetKey] = merge({}, val);
        } else if (isArray(val)) {
            result[targetKey] = val.slice();
        } else if (!skipUndefined || !isUndefined(val)) {
            result[targetKey] = val;
        }
    };
    for (let i = 0, l = objs.length; i < l; i++) {
        const source = objs[i];
        if (!source || isBuffer(source)) {
            continue;
        }
        forEach(source, assignValue);
        if (typeof source !== "object" || isArray(source)) {
            continue;
        }
        const symbols = Object.getOwnPropertySymbols(source);
        for (let j = 0; j < symbols.length; j++) {
            const symbol = symbols[j];
            if (propertyIsEnumerable.call(source, symbol)) {
                assignValue(source[symbol], symbol);
            }
        }
    }
    return result;
}
var extend = (a, b, thisArg, { allOwnKeys } = {}) => {
    forEach(
        b,
        (val, key) => {
            if (thisArg && isFunction(val)) {
                Object.defineProperty(a, key, {
                    // Null-proto descriptor so a polluted Object.prototype.get cannot
                    // hijack defineProperty's accessor-vs-data resolution.
                    __proto__: null,
                    value: bind(val, thisArg),
                    writable: true,
                    enumerable: true,
                    configurable: true
                });
            } else {
                Object.defineProperty(a, key, {
                    __proto__: null,
                    value: val,
                    writable: true,
                    enumerable: true,
                    configurable: true
                });
            }
        },
        { allOwnKeys }
    );
    return a;
};
var stripBOM = content => {
    if (content.charCodeAt(0) === 65279) {
        content = content.slice(1);
    }
    return content;
};
var inherits = (constructor, superConstructor, props, descriptors) => {
    constructor.prototype = Object.create(superConstructor.prototype, descriptors);
    Object.defineProperty(constructor.prototype, "constructor", {
        __proto__: null,
        value: constructor,
        writable: true,
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(constructor, "super", {
        __proto__: null,
        value: superConstructor.prototype
    });
    props && Object.assign(constructor.prototype, props);
};
var toFlatObject = (sourceObj, destObj, filter2, propFilter) => {
    let props;
    let i;
    let prop;
    const merged = {};
    destObj = destObj || {};
    if (sourceObj == null) return destObj;
    do {
        props = Object.getOwnPropertyNames(sourceObj);
        i = props.length;
        while (i-- > 0) {
            prop = props[i];
            if ((!propFilter || propFilter(prop, sourceObj, destObj)) && !merged[prop]) {
                destObj[prop] = sourceObj[prop];
                merged[prop] = true;
            }
        }
        sourceObj = filter2 !== false && getPrototypeOf(sourceObj);
    } while (sourceObj && (!filter2 || filter2(sourceObj, destObj)) && sourceObj !== Object.prototype);
    return destObj;
};
var endsWith = (str, searchString, position) => {
    str = String(str);
    if (position === void 0 || position > str.length) {
        position = str.length;
    }
    position -= searchString.length;
    const lastIndex = str.indexOf(searchString, position);
    return lastIndex !== -1 && lastIndex === position;
};
var toArray = thing => {
    if (!thing) return null;
    if (isArray(thing)) return thing;
    let i = thing.length;
    if (!isNumber(i)) return null;
    const arr = new Array(i);
    while (i-- > 0) {
        arr[i] = thing[i];
    }
    return arr;
};
var isTypedArray = /* @__PURE__ */ (TypedArray => {
    return thing => {
        return TypedArray && thing instanceof TypedArray;
    };
})(typeof Uint8Array !== "undefined" && getPrototypeOf(Uint8Array));
var forEachEntry = (obj, fn) => {
    const generator = obj && obj[iterator];
    const _iterator = generator.call(obj);
    let result;
    while ((result = _iterator.next()) && !result.done) {
        const pair = result.value;
        fn.call(obj, pair[0], pair[1]);
    }
};
var matchAll = (regExp, str) => {
    let matches;
    const arr = [];
    while ((matches = regExp.exec(str)) !== null) {
        arr.push(matches);
    }
    return arr;
};
var isHTMLForm = kindOfTest("HTMLFormElement");
var toCamelCase = str => {
    return str.toLowerCase().replace(/[-_\s]([a-z\d])(\w*)/g, function replacer(m, p1, p2) {
        return p1.toUpperCase() + p2;
    });
};
var { propertyIsEnumerable } = Object.prototype;
var isRegExp = kindOfTest("RegExp");
var reduceDescriptors = (obj, reducer) => {
    const descriptors = Object.getOwnPropertyDescriptors(obj);
    const reducedDescriptors = {};
    forEach(descriptors, (descriptor, name) => {
        let ret;
        if ((ret = reducer(descriptor, name, obj)) !== false) {
            reducedDescriptors[name] = ret || descriptor;
        }
    });
    Object.defineProperties(obj, reducedDescriptors);
};
var freezeMethods = obj => {
    reduceDescriptors(obj, (descriptor, name) => {
        if (isFunction(obj) && ["arguments", "caller", "callee"].includes(name)) {
            return false;
        }
        const value = obj[name];
        if (!isFunction(value)) return;
        descriptor.enumerable = false;
        if ("writable" in descriptor) {
            descriptor.writable = false;
            return;
        }
        if (!descriptor.set) {
            descriptor.set = () => {
                throw Error("Can not rewrite read-only method '" + name + "'");
            };
        }
    });
};
var toObjectSet = (arrayOrString, delimiter) => {
    const obj = {};
    const define = arr => {
        arr.forEach(value => {
            obj[value] = true;
        });
    };
    isArray(arrayOrString) ? define(arrayOrString) : define(String(arrayOrString).split(delimiter));
    return obj;
};
var noop = () => {};
var toFiniteNumber = (value, defaultValue) => {
    return value != null && Number.isFinite((value = +value)) ? value : defaultValue;
};
function isSpecCompliantForm(thing) {
    return !!(thing && isFunction(thing.append) && thing[toStringTag] === "FormData" && thing[iterator]);
}
var toJSONObject = obj => {
    const visited = /* @__PURE__ */ new WeakSet();
    const visit = source => {
        if (isObject(source)) {
            if (visited.has(source)) {
                return;
            }
            if (isBuffer(source)) {
                return source;
            }
            if (!("toJSON" in source)) {
                visited.add(source);
                const target = isArray(source) ? [] : {};
                forEach(source, (value, key) => {
                    const reducedValue = visit(value);
                    !isUndefined(reducedValue) && (target[key] = reducedValue);
                });
                visited.delete(source);
                return target;
            }
        }
        return source;
    };
    return visit(obj);
};
var isAsyncFn = kindOfTest("AsyncFunction");
var isThenable = thing => thing && (isObject(thing) || isFunction(thing)) && isFunction(thing.then) && isFunction(thing.catch);
var _setImmediate = ((setImmediateSupported, postMessageSupported) => {
    if (setImmediateSupported) {
        return setImmediate;
    }
    return postMessageSupported
        ? ((token, callbacks) => {
              _global.addEventListener(
                  "message",
                  ({ source, data }) => {
                      if (source === _global && data === token) {
                          callbacks.length && callbacks.shift()();
                      }
                  },
                  false
              );
              return cb => {
                  callbacks.push(cb);
                  _global.postMessage(token, "*");
              };
          })(`axios@${Math.random()}`, [])
        : cb => setTimeout(cb);
})(typeof setImmediate === "function", isFunction(_global.postMessage));
var asap = typeof queueMicrotask !== "undefined" ? queueMicrotask.bind(_global) : (typeof process !== "undefined" && process.nextTick) || _setImmediate;
var isIterable = thing => thing != null && isFunction(thing[iterator]);
var isSafeIterable = thing => thing != null && hasOwnInPrototypeChain(thing, iterator) && isIterable(thing);
var utils_default = {
    isArray,
    isArrayBuffer,
    isBuffer,
    isFormData,
    isArrayBufferView,
    isString,
    isNumber,
    isBoolean,
    isObject,
    isPlainObject,
    isEmptyObject,
    isReadableStream,
    isRequest,
    isResponse,
    isHeaders,
    isUndefined,
    isDate,
    isFile,
    isReactNativeBlob,
    isReactNative,
    isBlob,
    isRegExp,
    isFunction,
    isStream,
    isURLSearchParams,
    isTypedArray,
    isFileList,
    forEach,
    merge,
    extend,
    trim,
    stripBOM,
    inherits,
    toFlatObject,
    kindOf,
    kindOfTest,
    endsWith,
    toArray,
    forEachEntry,
    matchAll,
    isHTMLForm,
    hasOwnProperty,
    hasOwnProp: hasOwnProperty,
    // an alias to avoid ESLint no-prototype-builtins detection
    hasOwnInPrototypeChain,
    getSafeProp,
    reduceDescriptors,
    freezeMethods,
    toObjectSet,
    toCamelCase,
    noop,
    toFiniteNumber,
    findKey,
    global: _global,
    isContextDefined,
    isSpecCompliantForm,
    toJSONObject,
    isAsyncFn,
    isThenable,
    setImmediate: _setImmediate,
    asap,
    isIterable,
    isSafeIterable
};

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/parseHeaders.js
var ignoreDuplicateOf = utils_default.toObjectSet(["age", "authorization", "content-length", "content-type", "etag", "expires", "from", "host", "if-modified-since", "if-unmodified-since", "last-modified", "location", "max-forwards", "proxy-authorization", "referer", "retry-after", "user-agent"]);
var parseHeaders_default = rawHeaders => {
    const parsed = {};
    let key;
    let val;
    let i;
    rawHeaders &&
        rawHeaders.split("\n").forEach(function parser(line) {
            i = line.indexOf(":");
            key = line.substring(0, i).trim().toLowerCase();
            val = line.substring(i + 1).trim();
            if (!key || (parsed[key] && ignoreDuplicateOf[key])) {
                return;
            }
            if (key === "set-cookie") {
                if (parsed[key]) {
                    parsed[key].push(val);
                } else {
                    parsed[key] = [val];
                }
            } else {
                parsed[key] = parsed[key] ? parsed[key] + ", " + val : val;
            }
        });
    return parsed;
};

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/sanitizeHeaderValue.js
function trimSPorHTAB(str) {
    let start = 0;
    let end = str.length;
    while (start < end) {
        const code = str.charCodeAt(start);
        if (code !== 9 && code !== 32) {
            break;
        }
        start += 1;
    }
    while (end > start) {
        const code = str.charCodeAt(end - 1);
        if (code !== 9 && code !== 32) {
            break;
        }
        end -= 1;
    }
    return start === 0 && end === str.length ? str : str.slice(start, end);
}
var INVALID_UNICODE_HEADER_VALUE_CHARS = new RegExp("[\\u0000-\\u0008\\u000a-\\u001f\\u007f]+", "g");
var INVALID_BYTE_STRING_HEADER_VALUE_CHARS = new RegExp("[^\\u0009\\u0020-\\u007e\\u0080-\\u00ff]+", "g");
function sanitizeValue(value, invalidChars) {
    if (utils_default.isArray(value)) {
        return value.map(item => sanitizeValue(item, invalidChars));
    }
    return trimSPorHTAB(String(value).replace(invalidChars, ""));
}
var sanitizeHeaderValue = value => sanitizeValue(value, INVALID_UNICODE_HEADER_VALUE_CHARS);
var sanitizeByteStringHeaderValue = value => sanitizeValue(value, INVALID_BYTE_STRING_HEADER_VALUE_CHARS);
function toByteStringHeaderObject(headers) {
    const byteStringHeaders = /* @__PURE__ */ Object.create(null);
    utils_default.forEach(headers.toJSON(), (value, header) => {
        byteStringHeaders[header] = sanitizeByteStringHeaderValue(value);
    });
    return byteStringHeaders;
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/core/AxiosHeaders.js
var $internals = /* @__PURE__ */ Symbol("internals");
function normalizeHeader(header) {
    return header && String(header).trim().toLowerCase();
}
function normalizeValue(value) {
    if (value === false || value == null) {
        return value;
    }
    return utils_default.isArray(value) ? value.map(normalizeValue) : sanitizeHeaderValue(String(value));
}
function parseTokens(str) {
    const tokens = /* @__PURE__ */ Object.create(null);
    const tokensRE = /([^\s,;=]+)\s*(?:=\s*([^,;]+))?/g;
    let match;
    while ((match = tokensRE.exec(str))) {
        tokens[match[1]] = match[2];
    }
    return tokens;
}
var isValidHeaderName = str => /^[-_a-zA-Z0-9^`|~,!#$%&'*+.]+$/.test(str.trim());
function matchHeaderValue(context, value, header, filter2, isHeaderNameFilter) {
    if (utils_default.isFunction(filter2)) {
        return filter2.call(this, value, header);
    }
    if (isHeaderNameFilter) {
        value = header;
    }
    if (!utils_default.isString(value)) return;
    if (utils_default.isString(filter2)) {
        return value.indexOf(filter2) !== -1;
    }
    if (utils_default.isRegExp(filter2)) {
        return filter2.test(value);
    }
}
function formatHeader(header) {
    return header
        .trim()
        .toLowerCase()
        .replace(/([a-z\d])(\w*)/g, (w, char, str) => {
            return char.toUpperCase() + str;
        });
}
function buildAccessors(obj, header) {
    const accessorName = utils_default.toCamelCase(" " + header);
    ["get", "set", "has"].forEach(methodName => {
        Object.defineProperty(obj, methodName + accessorName, {
            // Null-proto descriptor so a polluted Object.prototype.get cannot turn
            // this data descriptor into an accessor descriptor on the way in.
            __proto__: null,
            value: function (arg1, arg2, arg3) {
                return this[methodName].call(this, header, arg1, arg2, arg3);
            },
            configurable: true
        });
    });
}
var AxiosHeaders = class {
    constructor(headers) {
        headers && this.set(headers);
    }
    set(header, valueOrRewrite, rewrite) {
        const self2 = this;
        function setHeader(_value, _header, _rewrite) {
            const lHeader = normalizeHeader(_header);
            if (!lHeader) {
                return;
            }
            const key = utils_default.findKey(self2, lHeader);
            if (!key || self2[key] === void 0 || _rewrite === true || (_rewrite === void 0 && self2[key] !== false)) {
                self2[key || _header] = normalizeValue(_value);
            }
        }
        const setHeaders = (headers, _rewrite) => utils_default.forEach(headers, (_value, _header) => setHeader(_value, _header, _rewrite));
        if (utils_default.isPlainObject(header) || header instanceof this.constructor) {
            setHeaders(header, valueOrRewrite);
        } else if (utils_default.isString(header) && (header = header.trim()) && !isValidHeaderName(header)) {
            setHeaders(parseHeaders_default(header), valueOrRewrite);
        } else if (utils_default.isObject(header) && utils_default.isSafeIterable(header)) {
            let obj = /* @__PURE__ */ Object.create(null),
                dest,
                key;
            for (const entry of header) {
                if (!utils_default.isArray(entry)) {
                    throw new TypeError("Object iterator must return a key-value pair");
                }
                key = entry[0];
                if (utils_default.hasOwnProp(obj, key)) {
                    dest = obj[key];
                    obj[key] = utils_default.isArray(dest) ? [...dest, entry[1]] : [dest, entry[1]];
                } else {
                    obj[key] = entry[1];
                }
            }
            setHeaders(obj, valueOrRewrite);
        } else {
            header != null && setHeader(valueOrRewrite, header, rewrite);
        }
        return this;
    }
    get(header, parser) {
        header = normalizeHeader(header);
        if (header) {
            const key = utils_default.findKey(this, header);
            if (key) {
                const value = this[key];
                if (!parser) {
                    return value;
                }
                if (parser === true) {
                    return parseTokens(value);
                }
                if (utils_default.isFunction(parser)) {
                    return parser.call(this, value, key);
                }
                if (utils_default.isRegExp(parser)) {
                    return parser.exec(value);
                }
                throw new TypeError("parser must be boolean|regexp|function");
            }
        }
    }
    has(header, matcher) {
        header = normalizeHeader(header);
        if (header) {
            const key = utils_default.findKey(this, header);
            return !!(key && this[key] !== void 0 && (!matcher || matchHeaderValue(this, this[key], key, matcher)));
        }
        return false;
    }
    delete(header, matcher) {
        const self2 = this;
        let deleted = false;
        function deleteHeader(_header) {
            _header = normalizeHeader(_header);
            if (_header) {
                const key = utils_default.findKey(self2, _header);
                if (key && (!matcher || matchHeaderValue(self2, self2[key], key, matcher))) {
                    delete self2[key];
                    deleted = true;
                }
            }
        }
        if (utils_default.isArray(header)) {
            header.forEach(deleteHeader);
        } else {
            deleteHeader(header);
        }
        return deleted;
    }
    clear(matcher) {
        const keys = Object.keys(this);
        let i = keys.length;
        let deleted = false;
        while (i--) {
            const key = keys[i];
            if (!matcher || matchHeaderValue(this, this[key], key, matcher, true)) {
                delete this[key];
                deleted = true;
            }
        }
        return deleted;
    }
    normalize(format) {
        const self2 = this;
        const headers = {};
        utils_default.forEach(this, (value, header) => {
            const key = utils_default.findKey(headers, header);
            if (key) {
                self2[key] = normalizeValue(value);
                delete self2[header];
                return;
            }
            const normalized = format ? formatHeader(header) : String(header).trim();
            if (normalized !== header) {
                delete self2[header];
            }
            self2[normalized] = normalizeValue(value);
            headers[normalized] = true;
        });
        return this;
    }
    concat(...targets) {
        return this.constructor.concat(this, ...targets);
    }
    toJSON(asStrings) {
        const obj = /* @__PURE__ */ Object.create(null);
        utils_default.forEach(this, (value, header) => {
            value != null && value !== false && (obj[header] = asStrings && utils_default.isArray(value) ? value.join(", ") : value);
        });
        return obj;
    }
    [Symbol.iterator]() {
        return Object.entries(this.toJSON())[Symbol.iterator]();
    }
    toString() {
        return Object.entries(this.toJSON())
            .map(([header, value]) => header + ": " + value)
            .join("\n");
    }
    getSetCookie() {
        return this.get("set-cookie") || [];
    }
    get [Symbol.toStringTag]() {
        return "AxiosHeaders";
    }
    static from(thing) {
        return thing instanceof this ? thing : new this(thing);
    }
    static concat(first, ...targets) {
        const computed = new this(first);
        targets.forEach(target => computed.set(target));
        return computed;
    }
    static accessor(header) {
        const internals =
            (this[$internals] =
            this[$internals] =
                {
                    accessors: {}
                });
        const accessors = internals.accessors;
        const prototype2 = this.prototype;
        function defineAccessor(_header) {
            const lHeader = normalizeHeader(_header);
            if (!accessors[lHeader]) {
                buildAccessors(prototype2, _header);
                accessors[lHeader] = true;
            }
        }
        utils_default.isArray(header) ? header.forEach(defineAccessor) : defineAccessor(header);
        return this;
    }
};
AxiosHeaders.accessor(["Content-Type", "Content-Length", "Accept", "Accept-Encoding", "User-Agent", "Authorization"]);
utils_default.reduceDescriptors(AxiosHeaders.prototype, ({ value }, key) => {
    let mapped = key[0].toUpperCase() + key.slice(1);
    return {
        get: () => value,
        set(headerValue) {
            this[mapped] = headerValue;
        }
    };
});
utils_default.freezeMethods(AxiosHeaders);
var AxiosHeaders_default = AxiosHeaders;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/core/AxiosError.js
var REDACTED = "[REDACTED ****]";
function hasOwnOrPrototypeToJSON(source) {
    if (utils_default.hasOwnProp(source, "toJSON")) {
        return true;
    }
    let prototype2 = Object.getPrototypeOf(source);
    while (prototype2 && prototype2 !== Object.prototype) {
        if (utils_default.hasOwnProp(prototype2, "toJSON")) {
            return true;
        }
        prototype2 = Object.getPrototypeOf(prototype2);
    }
    return false;
}
function redactConfig(config, redactKeys) {
    const lowerKeys = new Set(redactKeys.map(k => String(k).toLowerCase()));
    const seen = [];
    const visit = source => {
        if (source === null || typeof source !== "object") return source;
        if (utils_default.isBuffer(source)) return source;
        if (seen.indexOf(source) !== -1) return void 0;
        if (source instanceof AxiosHeaders_default) {
            source = source.toJSON();
        }
        seen.push(source);
        let result;
        if (utils_default.isArray(source)) {
            result = [];
            source.forEach((v, i) => {
                const reducedValue = visit(v);
                if (!utils_default.isUndefined(reducedValue)) {
                    result[i] = reducedValue;
                }
            });
        } else {
            if (!utils_default.isPlainObject(source) && hasOwnOrPrototypeToJSON(source)) {
                seen.pop();
                return source;
            }
            result = /* @__PURE__ */ Object.create(null);
            for (const [key, value] of Object.entries(source)) {
                const reducedValue = lowerKeys.has(key.toLowerCase()) ? REDACTED : visit(value);
                if (!utils_default.isUndefined(reducedValue)) {
                    result[key] = reducedValue;
                }
            }
        }
        seen.pop();
        return result;
    };
    return visit(config);
}
var AxiosError = class _AxiosError extends Error {
    static from(error, code, config, request2, response, customProps) {
        const axiosError = new _AxiosError(error.message, code || error.code, config, request2, response);
        Object.defineProperty(axiosError, "cause", {
            __proto__: null,
            value: error,
            writable: true,
            enumerable: false,
            configurable: true
        });
        axiosError.name = error.name;
        if (error.status != null && axiosError.status == null) {
            axiosError.status = error.status;
        }
        customProps && Object.assign(axiosError, customProps);
        return axiosError;
    }
    /**
     * Create an Error with the specified message, config, error code, request and response.
     *
     * @param {string} message The error message.
     * @param {string} [code] The error code (for example, 'ECONNABORTED').
     * @param {Object} [config] The config.
     * @param {Object} [request] The request.
     * @param {Object} [response] The response.
     *
     * @returns {Error} The created error.
     */
    constructor(message, code, config, request2, response) {
        super(message);
        Object.defineProperty(this, "message", {
            // Null-proto descriptor so a polluted Object.prototype.get cannot turn
            // this data descriptor into an accessor descriptor on the way in.
            __proto__: null,
            value: message,
            enumerable: true,
            writable: true,
            configurable: true
        });
        this.name = "AxiosError";
        this.isAxiosError = true;
        code && (this.code = code);
        config && (this.config = config);
        request2 && (this.request = request2);
        if (response) {
            this.response = response;
            this.status = response.status;
        }
    }
    toJSON() {
        const config = this.config;
        const redactKeys = config && utils_default.hasOwnProp(config, "redact") ? config.redact : void 0;
        const serializedConfig = utils_default.isArray(redactKeys) && redactKeys.length > 0 ? redactConfig(config, redactKeys) : utils_default.toJSONObject(config);
        return {
            // Standard
            message: this.message,
            name: this.name,
            // Microsoft
            description: this.description,
            number: this.number,
            // Mozilla
            fileName: this.fileName,
            lineNumber: this.lineNumber,
            columnNumber: this.columnNumber,
            stack: this.stack,
            // Axios
            config: serializedConfig,
            code: this.code,
            status: this.status
        };
    }
};
AxiosError.ERR_BAD_OPTION_VALUE = "ERR_BAD_OPTION_VALUE";
AxiosError.ERR_BAD_OPTION = "ERR_BAD_OPTION";
AxiosError.ECONNABORTED = "ECONNABORTED";
AxiosError.ETIMEDOUT = "ETIMEDOUT";
AxiosError.ECONNREFUSED = "ECONNREFUSED";
AxiosError.ERR_NETWORK = "ERR_NETWORK";
AxiosError.ERR_FR_TOO_MANY_REDIRECTS = "ERR_FR_TOO_MANY_REDIRECTS";
AxiosError.ERR_DEPRECATED = "ERR_DEPRECATED";
AxiosError.ERR_BAD_RESPONSE = "ERR_BAD_RESPONSE";
AxiosError.ERR_BAD_REQUEST = "ERR_BAD_REQUEST";
AxiosError.ERR_CANCELED = "ERR_CANCELED";
AxiosError.ERR_NOT_SUPPORT = "ERR_NOT_SUPPORT";
AxiosError.ERR_INVALID_URL = "ERR_INVALID_URL";
AxiosError.ERR_FORM_DATA_DEPTH_EXCEEDED = "ERR_FORM_DATA_DEPTH_EXCEEDED";
var AxiosError_default = AxiosError;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/platform/node/classes/FormData.js
var import_form_data = __toESM(require_form_data(), 1);
var FormData_default = import_form_data.default;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/toFormData.js
var DEFAULT_FORM_DATA_MAX_DEPTH = 100;
function isVisitable(thing) {
    return utils_default.isPlainObject(thing) || utils_default.isArray(thing);
}
function removeBrackets(key) {
    return utils_default.endsWith(key, "[]") ? key.slice(0, -2) : key;
}
function renderKey(path, key, dots) {
    if (!path) return key;
    return path
        .concat(key)
        .map(function each(token, i) {
            token = removeBrackets(token);
            return !dots && i ? "[" + token + "]" : token;
        })
        .join(dots ? "." : "");
}
function isFlatArray(arr) {
    return utils_default.isArray(arr) && !arr.some(isVisitable);
}
var predicates = utils_default.toFlatObject(utils_default, {}, null, function filter(prop) {
    return /^is[A-Z]/.test(prop);
});
function toFormData(obj, formData, options) {
    if (!utils_default.isObject(obj)) {
        throw new TypeError("target must be an object");
    }
    formData = formData || new (FormData_default || FormData)();
    options = utils_default.toFlatObject(
        options,
        {
            metaTokens: true,
            dots: false,
            indexes: false
        },
        false,
        function defined(option, source) {
            return !utils_default.isUndefined(source[option]);
        }
    );
    const metaTokens = options.metaTokens;
    const visitor = options.visitor || defaultVisitor;
    const dots = options.dots;
    const indexes = options.indexes;
    const _Blob = options.Blob || (typeof Blob !== "undefined" && Blob);
    const maxDepth = options.maxDepth === void 0 ? DEFAULT_FORM_DATA_MAX_DEPTH : options.maxDepth;
    const useBlob = _Blob && utils_default.isSpecCompliantForm(formData);
    const stack = [];
    if (!utils_default.isFunction(visitor)) {
        throw new TypeError("visitor must be a function");
    }
    function convertValue(value) {
        if (value === null) return "";
        if (utils_default.isDate(value)) {
            return value.toISOString();
        }
        if (utils_default.isBoolean(value)) {
            return value.toString();
        }
        if (!useBlob && utils_default.isBlob(value)) {
            throw new AxiosError_default("Blob is not supported. Use a Buffer instead.");
        }
        if (utils_default.isArrayBuffer(value) || utils_default.isTypedArray(value)) {
            if (useBlob && typeof _Blob === "function") {
                return new _Blob([value]);
            }
            if (typeof Buffer !== "undefined") {
                return Buffer.from(value);
            }
            throw new AxiosError_default("Blob is not supported. Use a Buffer instead.", AxiosError_default.ERR_NOT_SUPPORT);
        }
        return value;
    }
    function throwIfMaxDepthExceeded(depth) {
        if (depth > maxDepth) {
            throw new AxiosError_default("Object is too deeply nested (" + depth + " levels). Max depth: " + maxDepth, AxiosError_default.ERR_FORM_DATA_DEPTH_EXCEEDED);
        }
    }
    function stringifyWithDepthLimit(value, depth) {
        if (maxDepth === Infinity) {
            return JSON.stringify(value);
        }
        const ancestors = [];
        return JSON.stringify(value, function limitDepth(_key, currentValue) {
            if (!utils_default.isObject(currentValue)) {
                return currentValue;
            }
            while (ancestors.length && ancestors[ancestors.length - 1] !== this) {
                ancestors.pop();
            }
            ancestors.push(currentValue);
            throwIfMaxDepthExceeded(depth + ancestors.length - 1);
            return currentValue;
        });
    }
    function defaultVisitor(value, key, path) {
        let arr = value;
        if (utils_default.isReactNative(formData) && utils_default.isReactNativeBlob(value)) {
            formData.append(renderKey(path, key, dots), convertValue(value));
            return false;
        }
        if (value && !path && typeof value === "object") {
            if (utils_default.endsWith(key, "{}")) {
                key = metaTokens ? key : key.slice(0, -2);
                value = stringifyWithDepthLimit(value, 1);
            } else if ((utils_default.isArray(value) && isFlatArray(value)) || ((utils_default.isFileList(value) || utils_default.endsWith(key, "[]")) && (arr = utils_default.toArray(value)))) {
                key = removeBrackets(key);
                arr.forEach(function each(el, index2) {
                    !(utils_default.isUndefined(el) || el === null) &&
                        formData.append(
                            // eslint-disable-next-line no-nested-ternary
                            indexes === true ? renderKey([key], index2, dots) : indexes === null ? key : key + "[]",
                            convertValue(el)
                        );
                });
                return false;
            }
        }
        if (isVisitable(value)) {
            return true;
        }
        formData.append(renderKey(path, key, dots), convertValue(value));
        return false;
    }
    const exposedHelpers = Object.assign(predicates, {
        defaultVisitor,
        convertValue,
        isVisitable
    });
    function build(value, path, depth = 0) {
        if (utils_default.isUndefined(value)) return;
        throwIfMaxDepthExceeded(depth);
        if (stack.indexOf(value) !== -1) {
            throw new Error("Circular reference detected in " + path.join("."));
        }
        stack.push(value);
        utils_default.forEach(value, function each(el, key) {
            const result = !(utils_default.isUndefined(el) || el === null) && visitor.call(formData, el, utils_default.isString(key) ? key.trim() : key, path, exposedHelpers);
            if (result === true) {
                build(el, path ? path.concat(key) : [key], depth + 1);
            }
        });
        stack.pop();
    }
    if (!utils_default.isObject(obj)) {
        throw new TypeError("data must be an object");
    }
    build(obj);
    return formData;
}
var toFormData_default = toFormData;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/AxiosURLSearchParams.js
function encode(str) {
    const charMap = {
        "!": "%21",
        "'": "%27",
        "(": "%28",
        ")": "%29",
        "~": "%7E",
        "%20": "+"
    };
    return encodeURIComponent(str).replace(/[!'()~]|%20/g, function replacer(match) {
        return charMap[match];
    });
}
function AxiosURLSearchParams(params, options) {
    this._pairs = [];
    params && toFormData_default(params, this, options);
}
var prototype = AxiosURLSearchParams.prototype;
prototype.append = function append(name, value) {
    this._pairs.push([name, value]);
};
prototype.toString = function toString2(encoder) {
    const _encode = encoder ? value => encoder.call(this, value, encode) : encode;
    return this._pairs
        .map(function each(pair) {
            return _encode(pair[0]) + "=" + _encode(pair[1]);
        }, "")
        .join("&");
};
var AxiosURLSearchParams_default = AxiosURLSearchParams;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/buildURL.js
function encode2(val) {
    return encodeURIComponent(val).replace(/%3A/gi, ":").replace(/%24/g, "$").replace(/%2C/gi, ",").replace(/%20/g, "+");
}
function buildURL(url2, params, options) {
    if (!params) {
        return url2;
    }
    url2 = url2 || "";
    const _options = utils_default.isFunction(options)
        ? {
              serialize: options
          }
        : options;
    const _encode = utils_default.getSafeProp(_options, "encode") || encode2;
    const serializeFn = utils_default.getSafeProp(_options, "serialize");
    let serializedParams;
    if (serializeFn) {
        serializedParams = serializeFn(params, _options);
    } else {
        serializedParams = utils_default.isURLSearchParams(params) ? params.toString() : new AxiosURLSearchParams_default(params, _options).toString(_encode);
    }
    if (serializedParams) {
        const hashmarkIndex = url2.indexOf("#");
        if (hashmarkIndex !== -1) {
            url2 = url2.slice(0, hashmarkIndex);
        }
        url2 += (url2.indexOf("?") === -1 ? "?" : "&") + serializedParams;
    }
    return url2;
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/core/InterceptorManager.js
var InterceptorManager = class {
    constructor() {
        this.handlers = [];
    }
    /**
     * Add a new interceptor to the stack
     *
     * @param {Function} fulfilled The function to handle `then` for a `Promise`
     * @param {Function} rejected The function to handle `reject` for a `Promise`
     * @param {Object} options The options for the interceptor, synchronous and runWhen
     *
     * @return {Number} An ID used to remove interceptor later
     */
    use(fulfilled, rejected, options) {
        this.handlers.push({
            fulfilled,
            rejected,
            synchronous: options ? options.synchronous : false,
            runWhen: options ? options.runWhen : null
        });
        return this.handlers.length - 1;
    }
    /**
     * Remove an interceptor from the stack
     *
     * @param {Number} id The ID that was returned by `use`
     *
     * @returns {void}
     */
    eject(id) {
        if (this.handlers[id]) {
            this.handlers[id] = null;
        }
    }
    /**
     * Clear all interceptors from the stack
     *
     * @returns {void}
     */
    clear() {
        if (this.handlers) {
            this.handlers = [];
        }
    }
    /**
     * Iterate over all the registered interceptors
     *
     * This method is particularly useful for skipping over any
     * interceptors that may have become `null` calling `eject`.
     *
     * @param {Function} fn The function to call for each interceptor
     *
     * @returns {void}
     */
    forEach(fn) {
        utils_default.forEach(this.handlers, function forEachHandler(h) {
            if (h !== null) {
                fn(h);
            }
        });
    }
};
var InterceptorManager_default = InterceptorManager;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/defaults/transitional.js
var transitional_default = {
    silentJSONParsing: true,
    forcedJSONParsing: true,
    clarifyTimeoutError: false,
    legacyInterceptorReqResOrdering: true,
    advertiseZstdAcceptEncoding: false,
    validateStatusUndefinedResolves: true
};

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/platform/node/index.js
var import_crypto = __toESM(require("crypto"), 1);

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/platform/node/classes/URLSearchParams.js
var import_url = __toESM(require("url"), 1);
var URLSearchParams_default = import_url.default.URLSearchParams;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/platform/node/index.js
var ALPHA = "abcdefghijklmnopqrstuvwxyz";
var DIGIT = "0123456789";
var ALPHABET = {
    DIGIT,
    ALPHA,
    ALPHA_DIGIT: ALPHA + ALPHA.toUpperCase() + DIGIT
};
var generateString = (size = 16, alphabet = ALPHABET.ALPHA_DIGIT) => {
    let str = "";
    const { length } = alphabet;
    const randomValues = new Uint32Array(size);
    import_crypto.default.randomFillSync(randomValues);
    for (let i = 0; i < size; i++) {
        str += alphabet[randomValues[i] % length];
    }
    return str;
};
var node_default = {
    isNode: true,
    classes: {
        URLSearchParams: URLSearchParams_default,
        FormData: FormData_default,
        Blob: (typeof Blob !== "undefined" && Blob) || null
    },
    ALPHABET,
    generateString,
    protocols: ["http", "https", "file", "data"]
};

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/platform/common/utils.js
var utils_exports = {};
__export(utils_exports, {
    hasBrowserEnv: () => hasBrowserEnv,
    hasStandardBrowserEnv: () => hasStandardBrowserEnv,
    hasStandardBrowserWebWorkerEnv: () => hasStandardBrowserWebWorkerEnv,
    navigator: () => _navigator,
    origin: () => origin
});
var hasBrowserEnv = typeof window !== "undefined" && typeof document !== "undefined";
var _navigator = (typeof navigator === "object" && navigator) || void 0;
var hasStandardBrowserEnv = hasBrowserEnv && (!_navigator || ["ReactNative", "NativeScript", "NS"].indexOf(_navigator.product) < 0);
var hasStandardBrowserWebWorkerEnv = (() => {
    return (
        typeof WorkerGlobalScope !== "undefined" && // eslint-disable-next-line no-undef
        self instanceof WorkerGlobalScope &&
        typeof self.importScripts === "function"
    );
})();
var origin = (hasBrowserEnv && window.location.href) || "http://localhost";

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/platform/index.js
var platform_default = {
    ...utils_exports,
    ...node_default
};

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/toURLEncodedForm.js
function toURLEncodedForm(data, options) {
    return toFormData_default(data, new platform_default.classes.URLSearchParams(), {
        visitor: function (value, key, path, helpers) {
            if (platform_default.isNode && utils_default.isBuffer(value)) {
                this.append(key, value.toString("base64"));
                return false;
            }
            return helpers.defaultVisitor.apply(this, arguments);
        },
        ...options
    });
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/formDataToJSON.js
var MAX_DEPTH = DEFAULT_FORM_DATA_MAX_DEPTH;
function throwIfDepthExceeded(index2) {
    if (index2 > MAX_DEPTH) {
        throw new AxiosError_default("FormData field is too deeply nested (" + index2 + " levels). Max depth: " + MAX_DEPTH, AxiosError_default.ERR_FORM_DATA_DEPTH_EXCEEDED);
    }
}
function parsePropPath(name) {
    const path = [];
    const pattern = /\w+|\[(\w*)]/g;
    let match;
    while ((match = pattern.exec(name)) !== null) {
        throwIfDepthExceeded(path.length);
        path.push(match[0] === "[]" ? "" : match[1] || match[0]);
    }
    return path;
}
function arrayToObject(arr) {
    const obj = {};
    const keys = Object.keys(arr);
    let i;
    const len = keys.length;
    let key;
    for (i = 0; i < len; i++) {
        key = keys[i];
        obj[key] = arr[key];
    }
    return obj;
}
function formDataToJSON(formData) {
    function buildPath(path, value, target, index2) {
        throwIfDepthExceeded(index2);
        let name = path[index2++];
        if (name === "__proto__") return true;
        const isNumericKey = Number.isFinite(+name);
        const isLast = index2 >= path.length;
        name = !name && utils_default.isArray(target) ? target.length : name;
        if (isLast) {
            if (utils_default.hasOwnProp(target, name)) {
                target[name] = utils_default.isArray(target[name]) ? target[name].concat(value) : [target[name], value];
            } else {
                target[name] = value;
            }
            return !isNumericKey;
        }
        if (!utils_default.hasOwnProp(target, name) || !utils_default.isObject(target[name])) {
            target[name] = [];
        }
        const result = buildPath(path, value, target[name], index2);
        if (result && utils_default.isArray(target[name])) {
            target[name] = arrayToObject(target[name]);
        }
        return !isNumericKey;
    }
    if (utils_default.isFormData(formData) && utils_default.isFunction(formData.entries)) {
        const obj = {};
        utils_default.forEachEntry(formData, (name, value) => {
            buildPath(parsePropPath(name), value, obj, 0);
        });
        return obj;
    }
    return null;
}
var formDataToJSON_default = formDataToJSON;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/defaults/index.js
var own = (obj, key) => (obj != null && utils_default.hasOwnProp(obj, key) ? obj[key] : void 0);
function stringifySafely(rawValue, parser, encoder) {
    if (utils_default.isString(rawValue)) {
        try {
            (parser || JSON.parse)(rawValue);
            return utils_default.trim(rawValue);
        } catch (e) {
            if (e.name !== "SyntaxError") {
                throw e;
            }
        }
    }
    return (encoder || JSON.stringify)(rawValue);
}
var defaults = {
    transitional: transitional_default,
    adapter: ["xhr", "http", "fetch"],
    transformRequest: [
        function transformRequest(data, headers) {
            const contentType = headers.getContentType() || "";
            const hasJSONContentType = contentType.indexOf("application/json") > -1;
            const isObjectPayload = utils_default.isObject(data);
            if (isObjectPayload && utils_default.isHTMLForm(data)) {
                data = new FormData(data);
            }
            const isFormData2 = utils_default.isFormData(data);
            if (isFormData2) {
                return hasJSONContentType ? JSON.stringify(formDataToJSON_default(data)) : data;
            }
            if (utils_default.isArrayBuffer(data) || utils_default.isBuffer(data) || utils_default.isStream(data) || utils_default.isFile(data) || utils_default.isBlob(data) || utils_default.isReadableStream(data)) {
                return data;
            }
            if (utils_default.isArrayBufferView(data)) {
                return data.buffer;
            }
            if (utils_default.isURLSearchParams(data)) {
                headers.setContentType("application/x-www-form-urlencoded;charset=utf-8", false);
                return data.toString();
            }
            let isFileList2;
            if (isObjectPayload) {
                const formSerializer = own(this, "formSerializer");
                if (contentType.indexOf("application/x-www-form-urlencoded") > -1) {
                    return toURLEncodedForm(data, formSerializer).toString();
                }
                if ((isFileList2 = utils_default.isFileList(data)) || contentType.indexOf("multipart/form-data") > -1) {
                    const env = own(this, "env");
                    const _FormData = env && env.FormData;
                    return toFormData_default(isFileList2 ? { "files[]": data } : data, _FormData && new _FormData(), formSerializer);
                }
            }
            if (isObjectPayload || hasJSONContentType) {
                headers.setContentType("application/json", false);
                return stringifySafely(data);
            }
            return data;
        }
    ],
    transformResponse: [
        function transformResponse(data) {
            const transitional2 = own(this, "transitional") || defaults.transitional;
            const forcedJSONParsing = transitional2 && transitional2.forcedJSONParsing;
            const responseType = own(this, "responseType");
            const JSONRequested = responseType === "json";
            if (utils_default.isResponse(data) || utils_default.isReadableStream(data)) {
                return data;
            }
            if (data && utils_default.isString(data) && ((forcedJSONParsing && !responseType) || JSONRequested)) {
                const silentJSONParsing = transitional2 && transitional2.silentJSONParsing;
                const strictJSONParsing = !silentJSONParsing && JSONRequested;
                try {
                    return JSON.parse(data, own(this, "parseReviver"));
                } catch (e) {
                    if (strictJSONParsing) {
                        if (e.name === "SyntaxError") {
                            throw AxiosError_default.from(e, AxiosError_default.ERR_BAD_RESPONSE, this, null, own(this, "response"));
                        }
                        throw e;
                    }
                }
            }
            return data;
        }
    ],
    /**
     * A timeout in milliseconds to abort a request. If set to 0 (default) a
     * timeout is not created.
     */
    timeout: 0,
    xsrfCookieName: "XSRF-TOKEN",
    xsrfHeaderName: "X-XSRF-TOKEN",
    maxContentLength: -1,
    maxBodyLength: -1,
    env: {
        FormData: platform_default.classes.FormData,
        Blob: platform_default.classes.Blob
    },
    validateStatus: function validateStatus(status) {
        return status >= 200 && status < 300;
    },
    headers: {
        common: {
            Accept: "application/json, text/plain, */*",
            "Content-Type": void 0
        }
    }
};
utils_default.forEach(["delete", "get", "head", "post", "put", "patch", "query"], method => {
    defaults.headers[method] = {};
});
var defaults_default = defaults;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/core/transformData.js
function transformData(fns, response) {
    const config = this || defaults_default;
    const context = response || config;
    const headers = AxiosHeaders_default.from(context.headers);
    let data = context.data;
    utils_default.forEach(fns, function transform(fn) {
        data = fn.call(config, data, headers.normalize(), response ? response.status : void 0);
    });
    headers.normalize();
    return data;
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/cancel/isCancel.js
function isCancel(value) {
    return !!(value && value.__CANCEL__);
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/cancel/CanceledError.js
var CanceledError = class extends AxiosError_default {
    /**
     * A `CanceledError` is an object that is thrown when an operation is canceled.
     *
     * @param {string=} message The message.
     * @param {Object=} config The config.
     * @param {Object=} request The request.
     *
     * @returns {CanceledError} The created error.
     */
    constructor(message, config, request2) {
        super(message == null ? "canceled" : message, AxiosError_default.ERR_CANCELED, config, request2);
        this.name = "CanceledError";
        this.__CANCEL__ = true;
    }
};
var CanceledError_default = CanceledError;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/core/settle.js
function settle(resolve, reject, response) {
    const validateStatus2 = response.config.validateStatus;
    if (!response.status || !validateStatus2 || validateStatus2(response.status)) {
        resolve(response);
    } else {
        reject(new AxiosError_default("Request failed with status code " + response.status, response.status >= 400 && response.status < 500 ? AxiosError_default.ERR_BAD_REQUEST : AxiosError_default.ERR_BAD_RESPONSE, response.config, response.request, response));
    }
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/isAbsoluteURL.js
function isAbsoluteURL(url2) {
    if (typeof url2 !== "string") {
        return false;
    }
    return /^([a-z][a-z\d+\-.]*:)?\/\//i.test(url2);
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/combineURLs.js
function combineURLs(baseURL2, relativeURL) {
    return relativeURL ? baseURL2.replace(/\/?\/$/, "") + "/" + relativeURL.replace(/^\/+/, "") : baseURL2;
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/core/buildFullPath.js
var malformedHttpProtocol = /^https?:(?!\/\/)/i;
var httpProtocolControlCharacters = /[\t\n\r]/g;
function stripLeadingC0ControlOrSpace(url2) {
    let i = 0;
    while (i < url2.length && url2.charCodeAt(i) <= 32) {
        i++;
    }
    return url2.slice(i);
}
function normalizeURLForProtocolCheck(url2) {
    return stripLeadingC0ControlOrSpace(url2).replace(httpProtocolControlCharacters, "");
}
function assertValidHttpProtocolURL(url2, config) {
    if (typeof url2 === "string" && malformedHttpProtocol.test(normalizeURLForProtocolCheck(url2))) {
        throw new AxiosError_default('Invalid URL: missing "//" after protocol', AxiosError_default.ERR_INVALID_URL, config);
    }
}
function buildFullPath(baseURL2, requestedURL, allowAbsoluteUrls, config) {
    assertValidHttpProtocolURL(requestedURL, config);
    let isRelativeUrl = !isAbsoluteURL(requestedURL);
    if (baseURL2 && (isRelativeUrl || allowAbsoluteUrls === false)) {
        assertValidHttpProtocolURL(baseURL2, config);
        return combineURLs(baseURL2, requestedURL);
    }
    return requestedURL;
}

// node_modules/.pnpm/proxy-from-env@2.1.0/node_modules/proxy-from-env/index.js
var DEFAULT_PORTS = {
    ftp: 21,
    gopher: 70,
    http: 80,
    https: 443,
    ws: 80,
    wss: 443
};
function parseUrl(urlString) {
    try {
        return new URL(urlString);
    } catch {
        return null;
    }
}
function getProxyForUrl(url2) {
    var parsedUrl = (typeof url2 === "string" ? parseUrl(url2) : url2) || {};
    var proto = parsedUrl.protocol;
    var hostname = parsedUrl.host;
    var port = parsedUrl.port;
    if (typeof hostname !== "string" || !hostname || typeof proto !== "string") {
        return "";
    }
    proto = proto.split(":", 1)[0];
    hostname = hostname.replace(/:\d*$/, "");
    port = parseInt(port) || DEFAULT_PORTS[proto] || 0;
    if (!shouldProxy(hostname, port)) {
        return "";
    }
    var proxy = getEnv(proto + "_proxy") || getEnv("all_proxy");
    if (proxy && proxy.indexOf("://") === -1) {
        proxy = proto + "://" + proxy;
    }
    return proxy;
}
function shouldProxy(hostname, port) {
    var NO_PROXY = getEnv("no_proxy").toLowerCase();
    if (!NO_PROXY) {
        return true;
    }
    if (NO_PROXY === "*") {
        return false;
    }
    return NO_PROXY.split(/[,\s]/).every(function (proxy) {
        if (!proxy) {
            return true;
        }
        var parsedProxy = proxy.match(/^(.+):(\d+)$/);
        var parsedProxyHostname = parsedProxy ? parsedProxy[1] : proxy;
        var parsedProxyPort = parsedProxy ? parseInt(parsedProxy[2]) : 0;
        if (parsedProxyPort && parsedProxyPort !== port) {
            return true;
        }
        if (!/^[.*]/.test(parsedProxyHostname)) {
            return hostname !== parsedProxyHostname;
        }
        if (parsedProxyHostname.charAt(0) === "*") {
            parsedProxyHostname = parsedProxyHostname.slice(1);
        }
        return !hostname.endsWith(parsedProxyHostname);
    });
}
function getEnv(key) {
    return process.env[key.toLowerCase()] || process.env[key.toUpperCase()] || "";
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/adapters/http.js
var import_https_proxy_agent = __toESM(require_dist(), 1);
var import_http = __toESM(require("http"), 1);
var import_https = __toESM(require("https"), 1);
var import_http22 = __toESM(require("http2"), 1);
var import_util3 = __toESM(require("util"), 1);
var import_path = require("path");
var import_follow_redirects = __toESM(require_follow_redirects(), 1);
var import_zlib = __toESM(require("zlib"), 1);

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/env/data.js
var VERSION = "1.18.1";

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/parseProtocol.js
function parseProtocol(url2) {
    const match = /^([-+\w]{1,25}):(?:\/\/)?/.exec(url2);
    return (match && match[1]) || "";
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/fromDataURI.js
var DATA_URL_PATTERN = /^([^,;]+\/[^,;]+)?((?:;[^,;=]+=[^,;]+)*)(;base64)?,([\s\S]*)$/;
function fromDataURI(uri, asBlob, options) {
    const _Blob = (options && options.Blob) || platform_default.classes.Blob;
    const protocol = parseProtocol(uri);
    if (asBlob === void 0 && _Blob) {
        asBlob = true;
    }
    if (protocol === "data") {
        uri = protocol.length ? uri.slice(protocol.length + 1) : uri;
        const match = DATA_URL_PATTERN.exec(uri);
        if (!match) {
            throw new AxiosError_default("Invalid URL", AxiosError_default.ERR_INVALID_URL);
        }
        const type = match[1];
        const params = match[2];
        const encoding = match[3] ? "base64" : "utf8";
        const body = match[4];
        let mime = "";
        if (type) {
            mime = params ? type + params : type;
        } else if (params) {
            mime = "text/plain" + params;
        }
        const buffer = encoding === "base64" ? Buffer.from(body, "base64") : Buffer.from(decodeURIComponent(body), encoding);
        if (asBlob) {
            if (!_Blob) {
                throw new AxiosError_default("Blob is not supported", AxiosError_default.ERR_NOT_SUPPORT);
            }
            return new _Blob([buffer], { type: mime });
        }
        return buffer;
    }
    throw new AxiosError_default("Unsupported protocol " + protocol, AxiosError_default.ERR_NOT_SUPPORT);
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/adapters/http.js
var import_stream4 = __toESM(require("stream"), 1);

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/AxiosTransformStream.js
var import_stream = __toESM(require("stream"), 1);
var kInternals = /* @__PURE__ */ Symbol("internals");
var AxiosTransformStream = class extends import_stream.default.Transform {
    constructor(options) {
        options = utils_default.toFlatObject(
            options,
            {
                maxRate: 0,
                chunkSize: 64 * 1024,
                minChunkSize: 100,
                timeWindow: 500,
                ticksRate: 2,
                samplesCount: 15
            },
            null,
            (prop, source) => {
                return !utils_default.isUndefined(source[prop]);
            }
        );
        super({
            readableHighWaterMark: options.chunkSize
        });
        const internals = (this[kInternals] = {
            timeWindow: options.timeWindow,
            chunkSize: options.chunkSize,
            maxRate: options.maxRate,
            minChunkSize: options.minChunkSize,
            bytesSeen: 0,
            isCaptured: false,
            notifiedBytesLoaded: 0,
            ts: Date.now(),
            bytes: 0,
            onReadCallback: null
        });
        this.on("newListener", event => {
            if (event === "progress") {
                if (!internals.isCaptured) {
                    internals.isCaptured = true;
                }
            }
        });
    }
    _read(size) {
        const internals = this[kInternals];
        if (internals.onReadCallback) {
            internals.onReadCallback();
        }
        return super._read(size);
    }
    _transform(chunk, encoding, callback) {
        const internals = this[kInternals];
        const maxRate = internals.maxRate;
        const readableHighWaterMark = this.readableHighWaterMark;
        const timeWindow = internals.timeWindow;
        const divider = 1e3 / timeWindow;
        const bytesThreshold = maxRate / divider;
        const minChunkSize = internals.minChunkSize !== false ? Math.max(internals.minChunkSize, bytesThreshold * 0.01) : 0;
        const pushChunk = (_chunk, _callback) => {
            const bytes = Buffer.byteLength(_chunk);
            internals.bytesSeen += bytes;
            internals.bytes += bytes;
            internals.isCaptured && this.emit("progress", internals.bytesSeen);
            if (this.push(_chunk)) {
                process.nextTick(_callback);
            } else {
                internals.onReadCallback = () => {
                    internals.onReadCallback = null;
                    process.nextTick(_callback);
                };
            }
        };
        const transformChunk = (_chunk, _callback) => {
            const chunkSize = Buffer.byteLength(_chunk);
            let chunkRemainder = null;
            let maxChunkSize = readableHighWaterMark;
            let bytesLeft;
            let passed = 0;
            if (maxRate) {
                const now = Date.now();
                if (!internals.ts || (passed = now - internals.ts) >= timeWindow) {
                    internals.ts = now;
                    bytesLeft = bytesThreshold - internals.bytes;
                    internals.bytes = bytesLeft < 0 ? -bytesLeft : 0;
                    passed = 0;
                }
                bytesLeft = bytesThreshold - internals.bytes;
            }
            if (maxRate) {
                if (bytesLeft <= 0) {
                    return setTimeout(() => {
                        _callback(null, _chunk);
                    }, timeWindow - passed);
                }
                if (bytesLeft < maxChunkSize) {
                    maxChunkSize = bytesLeft;
                }
            }
            if (maxChunkSize && chunkSize > maxChunkSize && chunkSize - maxChunkSize > minChunkSize) {
                chunkRemainder = _chunk.subarray(maxChunkSize);
                _chunk = _chunk.subarray(0, maxChunkSize);
            }
            pushChunk(
                _chunk,
                chunkRemainder
                    ? () => {
                          process.nextTick(_callback, null, chunkRemainder);
                      }
                    : _callback
            );
        };
        transformChunk(chunk, function transformNextChunk(err, _chunk) {
            if (err) {
                return callback(err);
            }
            if (_chunk) {
                transformChunk(_chunk, transformNextChunk);
            } else {
                callback(null);
            }
        });
    }
};
var AxiosTransformStream_default = AxiosTransformStream;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/adapters/http.js
var import_events = require("events");

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/formDataToStream.js
var import_util = __toESM(require("util"), 1);
var import_stream2 = require("stream");

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/readBlob.js
var { asyncIterator } = Symbol;
var readBlob = async function* (blob) {
    if (blob.stream) {
        yield* blob.stream();
    } else if (blob.arrayBuffer) {
        yield await blob.arrayBuffer();
    } else if (blob[asyncIterator]) {
        yield* blob[asyncIterator]();
    } else {
        yield blob;
    }
};
var readBlob_default = readBlob;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/formDataToStream.js
var BOUNDARY_ALPHABET = platform_default.ALPHABET.ALPHA_DIGIT + "-_";
var textEncoder = typeof TextEncoder === "function" ? new TextEncoder() : new import_util.default.TextEncoder();
var CRLF = "\r\n";
var CRLF_BYTES = textEncoder.encode(CRLF);
var CRLF_BYTES_COUNT = 2;
var FormDataPart = class {
    constructor(name, value) {
        const { escapeName } = this.constructor;
        const isStringValue = utils_default.isString(value);
        let headers = `Content-Disposition: form-data; name="${escapeName(name)}"${!isStringValue && value.name ? `; filename="${escapeName(value.name)}"` : ""}${CRLF}`;
        if (isStringValue) {
            value = textEncoder.encode(String(value).replace(/\r?\n|\r\n?/g, CRLF));
        } else {
            const safeType = String(value.type || "application/octet-stream").replace(/[\r\n]/g, "");
            headers += `Content-Type: ${safeType}${CRLF}`;
        }
        this.headers = textEncoder.encode(headers + CRLF);
        this.contentLength = isStringValue ? value.byteLength : value.size;
        this.size = this.headers.byteLength + this.contentLength + CRLF_BYTES_COUNT;
        this.name = name;
        this.value = value;
    }
    async *encode() {
        yield this.headers;
        const { value } = this;
        if (utils_default.isTypedArray(value)) {
            yield value;
        } else {
            yield* readBlob_default(value);
        }
        yield CRLF_BYTES;
    }
    static escapeName(name) {
        return String(name).replace(
            /[\r\n"]/g,
            match =>
                ({
                    "\r": "%0D",
                    "\n": "%0A",
                    '"': "%22"
                })[match]
        );
    }
};
var formDataToStream = (form, headersHandler, options) => {
    const { tag = "form-data-boundary", size = 25, boundary = tag + "-" + platform_default.generateString(size, BOUNDARY_ALPHABET) } = options || {};
    if (!utils_default.isFormData(form)) {
        throw new TypeError("FormData instance required");
    }
    if (boundary.length < 1 || boundary.length > 70) {
        throw new Error("boundary must be 1-70 characters long");
    }
    const boundaryBytes = textEncoder.encode("--" + boundary + CRLF);
    const footerBytes = textEncoder.encode("--" + boundary + "--" + CRLF);
    let contentLength = footerBytes.byteLength;
    const parts = Array.from(form.entries()).map(([name, value]) => {
        const part = new FormDataPart(name, value);
        contentLength += part.size;
        return part;
    });
    contentLength += boundaryBytes.byteLength * parts.length;
    contentLength = utils_default.toFiniteNumber(contentLength);
    const computedHeaders = {
        "Content-Type": `multipart/form-data; boundary=${boundary}`
    };
    if (Number.isFinite(contentLength)) {
        computedHeaders["Content-Length"] = contentLength;
    }
    headersHandler && headersHandler(computedHeaders);
    return import_stream2.Readable.from(
        (async function* () {
            for (const part of parts) {
                yield boundaryBytes;
                yield* part.encode();
            }
            yield footerBytes;
        })()
    );
};
var formDataToStream_default = formDataToStream;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/ZlibHeaderTransformStream.js
var import_stream3 = __toESM(require("stream"), 1);
var ZlibHeaderTransformStream = class extends import_stream3.default.Transform {
    __transform(chunk, encoding, callback) {
        this.push(chunk);
        callback();
    }
    _transform(chunk, encoding, callback) {
        if (chunk.length !== 0) {
            this._transform = this.__transform;
            if (chunk[0] !== 120) {
                const header = Buffer.alloc(2);
                header[0] = 120;
                header[1] = 156;
                this.push(header, encoding);
            }
        }
        this.__transform(chunk, encoding, callback);
    }
};
var ZlibHeaderTransformStream_default = ZlibHeaderTransformStream;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/Http2Sessions.js
var import_http2 = __toESM(require("http2"), 1);
var import_util2 = __toESM(require("util"), 1);
var Http2Sessions = class {
    constructor() {
        this.sessions = /* @__PURE__ */ Object.create(null);
    }
    getSession(authority, options) {
        options = Object.assign(
            {
                sessionTimeout: 1e3
            },
            options
        );
        let authoritySessions = this.sessions[authority];
        if (authoritySessions) {
            let len = authoritySessions.length;
            for (let i = 0; i < len; i++) {
                const [sessionHandle, sessionOptions] = authoritySessions[i];
                if (!sessionHandle.destroyed && !sessionHandle.closed && import_util2.default.isDeepStrictEqual(sessionOptions, options)) {
                    return sessionHandle;
                }
            }
        }
        const session = import_http2.default.connect(authority, options);
        let removed;
        let timer;
        const removeSession = () => {
            if (removed) {
                return;
            }
            removed = true;
            if (timer) {
                clearTimeout(timer);
                timer = null;
            }
            let entries = authoritySessions,
                len = entries.length,
                i = len;
            while (i--) {
                if (entries[i][0] === session) {
                    if (len === 1) {
                        delete this.sessions[authority];
                    } else {
                        entries.splice(i, 1);
                    }
                    if (!session.closed) {
                        session.close();
                    }
                    return;
                }
            }
        };
        const originalRequestFn = session.request;
        const { sessionTimeout } = options;
        if (sessionTimeout != null) {
            let streamsCount = 0;
            session.request = function () {
                const stream4 = originalRequestFn.apply(this, arguments);
                streamsCount++;
                if (timer) {
                    clearTimeout(timer);
                    timer = null;
                }
                stream4.once("close", () => {
                    if (!--streamsCount) {
                        timer = setTimeout(() => {
                            timer = null;
                            removeSession();
                        }, sessionTimeout);
                    }
                });
                return stream4;
            };
        }
        session.once("close", removeSession);
        let entry = [session, options];
        authoritySessions ? authoritySessions.push(entry) : (authoritySessions = this.sessions[authority] = [entry]);
        return session;
    }
};
var Http2Sessions_default = Http2Sessions;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/callbackify.js
var callbackify = (fn, reducer) => {
    return utils_default.isAsyncFn(fn)
        ? function (...args) {
              const cb = args.pop();
              fn.apply(this, args).then(value => {
                  try {
                      reducer ? cb(null, ...reducer(value)) : cb(null, value);
                  } catch (err) {
                      cb(err);
                  }
              }, cb);
          }
        : fn;
};
var callbackify_default = callbackify;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/shouldBypassProxy.js
var LOOPBACK_HOSTNAMES = /* @__PURE__ */ new Set(["localhost", "0.0.0.0"]);
var isIPv4Loopback = host => {
    const parts = host.split(".");
    if (parts.length !== 4) return false;
    if (parts[0] !== "127") return false;
    return parts.every(p => /^\d+$/.test(p) && Number(p) >= 0 && Number(p) <= 255);
};
var isIPv6ZeroGroup = group => /^0{1,4}$/.test(group);
var isIPv6Unspecified = host => {
    if (host === "::") return true;
    const compressionIndex = host.indexOf("::");
    if (compressionIndex !== -1) {
        if (compressionIndex !== host.lastIndexOf("::")) return false;
        const left = host.slice(0, compressionIndex);
        const right = host.slice(compressionIndex + 2);
        const leftGroups = left ? left.split(":") : [];
        const rightGroups = right ? right.split(":") : [];
        const explicitGroups = leftGroups.length + rightGroups.length;
        return explicitGroups < 8 && leftGroups.every(isIPv6ZeroGroup) && rightGroups.every(isIPv6ZeroGroup);
    }
    const groups = host.split(":");
    return groups.length === 8 && groups.every(isIPv6ZeroGroup);
};
var isIPv6Loopback = host => {
    if (host === "::1") return true;
    const v4MappedDotted = host.match(/^::ffff:(\d+\.\d+\.\d+\.\d+)$/i);
    if (v4MappedDotted) return isIPv4Loopback(v4MappedDotted[1]);
    const v4MappedHex = host.match(/^::ffff:([0-9a-f]{1,4}):([0-9a-f]{1,4})$/i);
    if (v4MappedHex) {
        const high = parseInt(v4MappedHex[1], 16);
        return high >= 32512 && high <= 32767;
    }
    const groups = host.split(":");
    if (groups.length === 8) {
        for (let i = 0; i < 7; i++) {
            if (!/^0+$/.test(groups[i])) return false;
        }
        return /^0*1$/.test(groups[7]);
    }
    return false;
};
var isLoopback = host => {
    if (!host) return false;
    if (LOOPBACK_HOSTNAMES.has(host)) return true;
    if (isIPv4Loopback(host)) return true;
    if (isIPv6Unspecified(host)) return true;
    return isIPv6Loopback(host);
};
var DEFAULT_PORTS2 = {
    http: 80,
    https: 443,
    ws: 80,
    wss: 443,
    ftp: 21
};
var parseNoProxyEntry = entry => {
    let entryHost = entry;
    let entryPort = 0;
    if (entryHost.charAt(0) === "[") {
        const bracketIndex = entryHost.indexOf("]");
        if (bracketIndex !== -1) {
            const host = entryHost.slice(1, bracketIndex);
            const rest = entryHost.slice(bracketIndex + 1);
            if (rest.charAt(0) === ":" && /^\d+$/.test(rest.slice(1))) {
                entryPort = Number.parseInt(rest.slice(1), 10);
            }
            return [host, entryPort];
        }
    }
    const firstColon = entryHost.indexOf(":");
    const lastColon = entryHost.lastIndexOf(":");
    if (firstColon !== -1 && firstColon === lastColon && /^\d+$/.test(entryHost.slice(lastColon + 1))) {
        entryPort = Number.parseInt(entryHost.slice(lastColon + 1), 10);
        entryHost = entryHost.slice(0, lastColon);
    }
    return [entryHost, entryPort];
};
var IPV4_MAPPED_DOTTED_RE = /^(?:::|(?:0{1,4}:){1,4}:|(?:0{1,4}:){5})ffff:(\d+\.\d+\.\d+\.\d+)$/i;
var IPV4_MAPPED_HEX_RE = /^(?:::|(?:0{1,4}:){1,4}:|(?:0{1,4}:){5})ffff:([0-9a-f]{1,4}):([0-9a-f]{1,4})$/i;
var unmapIPv4MappedIPv6 = host => {
    if (typeof host !== "string" || host.indexOf(":") === -1) return host;
    const dotted = host.match(IPV4_MAPPED_DOTTED_RE);
    if (dotted) return dotted[1];
    const hex = host.match(IPV4_MAPPED_HEX_RE);
    if (hex) {
        const high = parseInt(hex[1], 16);
        const low = parseInt(hex[2], 16);
        return `${high >> 8}.${high & 255}.${low >> 8}.${low & 255}`;
    }
    return host;
};
var normalizeNoProxyHost = hostname => {
    if (!hostname) {
        return hostname;
    }
    if (hostname.charAt(0) === "[" && hostname.charAt(hostname.length - 1) === "]") {
        hostname = hostname.slice(1, -1);
    }
    return unmapIPv4MappedIPv6(hostname.replace(/\.+$/, ""));
};
function shouldBypassProxy(location) {
    let parsed;
    try {
        parsed = new URL(location);
    } catch (_err) {
        return false;
    }
    const noProxy = (process.env.no_proxy || process.env.NO_PROXY || "").toLowerCase();
    if (!noProxy) {
        return false;
    }
    if (noProxy === "*") {
        return true;
    }
    const port = Number.parseInt(parsed.port, 10) || DEFAULT_PORTS2[parsed.protocol.split(":", 1)[0]] || 0;
    const hostname = normalizeNoProxyHost(parsed.hostname.toLowerCase());
    return noProxy.split(/[\s,]+/).some(entry => {
        if (!entry) {
            return false;
        }
        let [entryHost, entryPort] = parseNoProxyEntry(entry);
        entryHost = normalizeNoProxyHost(entryHost);
        if (!entryHost) {
            return false;
        }
        if (entryPort && entryPort !== port) {
            return false;
        }
        if (entryHost.charAt(0) === "*") {
            entryHost = entryHost.slice(1);
        }
        if (entryHost.charAt(0) === ".") {
            return hostname.endsWith(entryHost);
        }
        return hostname === entryHost || (isLoopback(hostname) && isLoopback(entryHost));
    });
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/speedometer.js
function speedometer(samplesCount, min) {
    samplesCount = samplesCount || 10;
    const bytes = new Array(samplesCount);
    const timestamps = new Array(samplesCount);
    let head = 0;
    let tail = 0;
    let firstSampleTS;
    min = min !== void 0 ? min : 1e3;
    return function push(chunkLength) {
        const now = Date.now();
        const startedAt = timestamps[tail];
        if (!firstSampleTS) {
            firstSampleTS = now;
        }
        bytes[head] = chunkLength;
        timestamps[head] = now;
        let i = tail;
        let bytesCount = 0;
        while (i !== head) {
            bytesCount += bytes[i++];
            i = i % samplesCount;
        }
        head = (head + 1) % samplesCount;
        if (head === tail) {
            tail = (tail + 1) % samplesCount;
        }
        if (now - firstSampleTS < min) {
            return;
        }
        const passed = startedAt && now - startedAt;
        return passed ? Math.round((bytesCount * 1e3) / passed) : void 0;
    };
}
var speedometer_default = speedometer;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/throttle.js
function throttle(fn, freq) {
    let timestamp = 0;
    let threshold = 1e3 / freq;
    let lastArgs;
    let timer;
    const invoke2 = (args, now = Date.now()) => {
        timestamp = now;
        lastArgs = null;
        if (timer) {
            clearTimeout(timer);
            timer = null;
        }
        fn(...args);
    };
    const throttled = (...args) => {
        const now = Date.now();
        const passed = now - timestamp;
        if (passed >= threshold) {
            invoke2(args, now);
        } else {
            lastArgs = args;
            if (!timer) {
                timer = setTimeout(() => {
                    timer = null;
                    invoke2(lastArgs);
                }, threshold - passed);
            }
        }
    };
    const flush = () => lastArgs && invoke2(lastArgs);
    return [throttled, flush];
}
var throttle_default = throttle;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/progressEventReducer.js
var progressEventReducer = (listener, isDownloadStream, freq = 3) => {
    let bytesNotified = 0;
    const _speedometer = speedometer_default(50, 250);
    return throttle_default(e => {
        if (!e || typeof e.loaded !== "number") {
            return;
        }
        const rawLoaded = e.loaded;
        const total = e.lengthComputable ? e.total : void 0;
        const loaded = total != null ? Math.min(rawLoaded, total) : rawLoaded;
        const progressBytes = Math.max(0, loaded - bytesNotified);
        const rate = _speedometer(progressBytes);
        bytesNotified = Math.max(bytesNotified, loaded);
        const data = {
            loaded,
            total,
            progress: total ? loaded / total : void 0,
            bytes: progressBytes,
            rate: rate ? rate : void 0,
            estimated: rate && total ? (total - loaded) / rate : void 0,
            event: e,
            lengthComputable: total != null,
            [isDownloadStream ? "download" : "upload"]: true
        };
        listener(data);
    }, freq);
};
var progressEventDecorator = (total, throttled) => {
    const lengthComputable = total != null;
    return [
        loaded =>
            throttled[0]({
                lengthComputable,
                total,
                loaded
            }),
        throttled[1]
    ];
};
var asyncDecorator =
    fn =>
    (...args) =>
        utils_default.asap(() => fn(...args));

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/estimateDataURLDecodedBytes.js
var isHexDigit = charCode => (charCode >= 48 && charCode <= 57) || (charCode >= 65 && charCode <= 70) || (charCode >= 97 && charCode <= 102);
var isPercentEncodedByte = (str, i, len) => i + 2 < len && isHexDigit(str.charCodeAt(i + 1)) && isHexDigit(str.charCodeAt(i + 2));
function estimateDataURLDecodedBytes(url2) {
    if (!url2 || typeof url2 !== "string") return 0;
    if (!url2.startsWith("data:")) return 0;
    const comma = url2.indexOf(",");
    if (comma < 0) return 0;
    const meta = url2.slice(5, comma);
    const body = url2.slice(comma + 1);
    const isBase64 = /;base64/i.test(meta);
    if (isBase64) {
        let effectiveLen = body.length;
        const len = body.length;
        for (let i = 0; i < len; i++) {
            if (body.charCodeAt(i) === 37 && i + 2 < len) {
                const a = body.charCodeAt(i + 1);
                const b = body.charCodeAt(i + 2);
                const isHex = isHexDigit(a) && isHexDigit(b);
                if (isHex) {
                    effectiveLen -= 2;
                    i += 2;
                }
            }
        }
        let pad = 0;
        let idx = len - 1;
        const tailIsPct3D = j =>
            j >= 2 &&
            body.charCodeAt(j - 2) === 37 && // '%'
            body.charCodeAt(j - 1) === 51 && // '3'
            (body.charCodeAt(j) === 68 || body.charCodeAt(j) === 100);
        if (idx >= 0) {
            if (body.charCodeAt(idx) === 61) {
                pad++;
                idx--;
            } else if (tailIsPct3D(idx)) {
                pad++;
                idx -= 3;
            }
        }
        if (pad === 1 && idx >= 0) {
            if (body.charCodeAt(idx) === 61) {
                pad++;
            } else if (tailIsPct3D(idx)) {
                pad++;
            }
        }
        const groups = Math.floor(effectiveLen / 4);
        const bytes2 = groups * 3 - (pad || 0);
        return bytes2 > 0 ? bytes2 : 0;
    }
    let bytes = 0;
    for (let i = 0, len = body.length; i < len; i++) {
        const c = body.charCodeAt(i);
        if (c === 37 && isPercentEncodedByte(body, i, len)) {
            bytes += 1;
            i += 2;
        } else if (c < 128) {
            bytes += 1;
        } else if (c < 2048) {
            bytes += 2;
        } else if (c >= 55296 && c <= 56319 && i + 1 < len) {
            const next = body.charCodeAt(i + 1);
            if (next >= 56320 && next <= 57343) {
                bytes += 4;
                i++;
            } else {
                bytes += 3;
            }
        } else {
            bytes += 3;
        }
    }
    return bytes;
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/adapters/http.js
var zlibOptions = {
    flush: import_zlib.default.constants.Z_SYNC_FLUSH,
    finishFlush: import_zlib.default.constants.Z_SYNC_FLUSH
};
var brotliOptions = {
    flush: import_zlib.default.constants.BROTLI_OPERATION_FLUSH,
    finishFlush: import_zlib.default.constants.BROTLI_OPERATION_FLUSH
};
var zstdOptions = {
    flush: import_zlib.default.constants.ZSTD_e_flush,
    finishFlush: import_zlib.default.constants.ZSTD_e_flush
};
var isBrotliSupported = utils_default.isFunction(import_zlib.default.createBrotliDecompress);
var isZstdSupported = utils_default.isFunction(import_zlib.default.createZstdDecompress);
var ACCEPT_ENCODING = "gzip, compress, deflate" + (isBrotliSupported ? ", br" : "");
var ACCEPT_ENCODING_WITH_ZSTD = ACCEPT_ENCODING + (isZstdSupported ? ", zstd" : "");
var { http: httpFollow, https: httpsFollow } = import_follow_redirects.default;
var isHttps = /https:?/;
var FORM_DATA_CONTENT_HEADERS = ["content-type", "content-length"];
function setFormDataHeaders(headers, formHeaders, policy) {
    if (policy !== "content-only") {
        headers.set(formHeaders);
        return;
    }
    Object.entries(formHeaders).forEach(([key, val]) => {
        if (FORM_DATA_CONTENT_HEADERS.includes(key.toLowerCase())) {
            headers.set(key, val);
        }
    });
}
var kAxiosSocketListener = /* @__PURE__ */ Symbol("axios.http.socketListener");
var kAxiosCurrentReq = /* @__PURE__ */ Symbol("axios.http.currentReq");
var kAxiosInstalledTunnel = /* @__PURE__ */ Symbol("axios.http.installedTunnel");
var tunnelingAgentCache = /* @__PURE__ */ new Map();
var tunnelingAgentCacheUser = /* @__PURE__ */ new WeakMap();
var NODE_NATIVE_ENV_PROXY_SUPPORT = {
    22: 21,
    24: 5
};
function isNodeNativeEnvProxySupported(nodeVersion = process.versions && process.versions.node) {
    if (!nodeVersion) {
        return false;
    }
    const [major, minor] = nodeVersion.split(".").map(part => Number(part));
    if (!Number.isInteger(major) || !Number.isInteger(minor)) {
        return false;
    }
    if (major > 24) {
        return true;
    }
    return NODE_NATIVE_ENV_PROXY_SUPPORT[major] != null && minor >= NODE_NATIVE_ENV_PROXY_SUPPORT[major];
}
function isNodeEnvProxyEnabled(agent, nodeVersion = process.versions && process.versions.node) {
    if (!isNodeNativeEnvProxySupported(nodeVersion)) {
        return false;
    }
    const agentOptions = agent && agent.options;
    return Boolean(agentOptions && utils_default.hasOwnProp(agentOptions, "proxyEnv") && agentOptions.proxyEnv != null);
}
function getProxyEnvAgent(options, configHttpAgent, configHttpsAgent) {
    return isHttps.test(options.protocol) ? configHttpsAgent || import_https.default.globalAgent : configHttpAgent || import_http.default.globalAgent;
}
function getTunnelingAgent(agentOptions, userHttpsAgent) {
    const key = agentOptions.protocol + "//" + agentOptions.hostname + ":" + (agentOptions.port || "") + "#" + (agentOptions.auth || "");
    const cache = userHttpsAgent ? tunnelingAgentCacheUser.get(userHttpsAgent) || tunnelingAgentCacheUser.set(userHttpsAgent, /* @__PURE__ */ new Map()).get(userHttpsAgent) : tunnelingAgentCache;
    let agent = cache.get(key);
    if (agent) return agent;
    const merged = userHttpsAgent && userHttpsAgent.options ? { ...userHttpsAgent.options, ...agentOptions } : agentOptions;
    agent = new import_https_proxy_agent.default(merged);
    if (userHttpsAgent && userHttpsAgent.options) {
        const originTLSOptions = { ...userHttpsAgent.options };
        const callback = agent.callback;
        agent.callback = function axiosTunnelingAgentCallback(req, opts) {
            return callback.call(this, req, { ...originTLSOptions, ...opts });
        };
    }
    agent[kAxiosInstalledTunnel] = true;
    cache.set(key, agent);
    return agent;
}
var supportedProtocols = platform_default.protocols.map(protocol => {
    return protocol + ":";
});
var decodeURIComponentSafe = value => {
    if (!utils_default.isString(value)) {
        return value;
    }
    try {
        return decodeURIComponent(value);
    } catch (error) {
        return value;
    }
};
var flushOnFinish = (stream4, [throttled, flush]) => {
    stream4.on("end", flush).on("error", flush);
    return throttled;
};
var http2Sessions = new Http2Sessions_default();
function dispatchBeforeRedirect(options, responseDetails, requestDetails) {
    if (options.beforeRedirects.proxy) {
        options.beforeRedirects.proxy(options);
    }
    if (options.beforeRedirects.auth) {
        options.beforeRedirects.auth(options);
    }
    if (options.beforeRedirects.sensitiveHeaders) {
        options.beforeRedirects.sensitiveHeaders(options, requestDetails);
    }
    if (options.beforeRedirects.config) {
        options.beforeRedirects.config(options, responseDetails, requestDetails);
    }
}
function stripMatchingHeaders(headers, sensitiveSet) {
    if (!headers) {
        return;
    }
    Object.keys(headers).forEach(header => {
        if (sensitiveSet.has(header.toLowerCase())) {
            delete headers[header];
        }
    });
}
function isSameOriginRedirect(redirectOptions, requestDetails) {
    if (!requestDetails) {
        return false;
    }
    try {
        return new URL(requestDetails.url).origin === new URL(redirectOptions.href).origin;
    } catch (e) {
        return false;
    }
}
function setProxy(options, configProxy, location, isRedirect, configHttpsAgent, configHttpAgent) {
    let proxy = configProxy;
    const proxyEnvAgent = getProxyEnvAgent(options, configHttpAgent, configHttpsAgent);
    if (!proxy && proxy !== false && !isNodeEnvProxyEnabled(proxyEnvAgent)) {
        const proxyUrl = getProxyForUrl(location);
        if (proxyUrl) {
            if (!shouldBypassProxy(location)) {
                proxy = new URL(proxyUrl);
            }
        }
    }
    if (isRedirect && options.headers) {
        for (const name of Object.keys(options.headers)) {
            if (name.toLowerCase() === "proxy-authorization") {
                delete options.headers[name];
            }
        }
    }
    if (isRedirect && options.agent && options.agent[kAxiosInstalledTunnel]) {
        options.agent = void 0;
    }
    if (proxy) {
        const isProxyURL = proxy instanceof URL;
        const readProxyField = key => (isProxyURL || utils_default.hasOwnProp(proxy, key) ? proxy[key] : void 0);
        const proxyUsername = readProxyField("username");
        const proxyPassword = readProxyField("password");
        let proxyAuth = utils_default.hasOwnProp(proxy, "auth") ? proxy.auth : void 0;
        if (proxyUsername) {
            proxyAuth = (proxyUsername || "") + ":" + (proxyPassword || "");
        }
        if (proxyAuth) {
            const authIsObject = typeof proxyAuth === "object";
            const authUsername = authIsObject && utils_default.hasOwnProp(proxyAuth, "username") ? proxyAuth.username : void 0;
            const authPassword = authIsObject && utils_default.hasOwnProp(proxyAuth, "password") ? proxyAuth.password : void 0;
            const validProxyAuth = Boolean(authUsername || authPassword);
            if (validProxyAuth) {
                proxyAuth = (authUsername || "") + ":" + (authPassword || "");
            } else if (authIsObject) {
                throw new AxiosError_default("Invalid proxy authorization", AxiosError_default.ERR_BAD_OPTION, { proxy });
            }
        }
        const targetIsHttps = isHttps.test(options.protocol);
        if (targetIsHttps) {
            if (!(configHttpsAgent instanceof import_https_proxy_agent.default)) {
                const proxyHost = readProxyField("hostname") || readProxyField("host");
                const proxyPort = readProxyField("port");
                const rawProxyProtocol = readProxyField("protocol");
                const normalizedProtocol = rawProxyProtocol ? (rawProxyProtocol.includes(":") ? rawProxyProtocol : `${rawProxyProtocol}:`) : "http:";
                const proxyHostForURL = proxyHost && proxyHost.includes(":") && !proxyHost.startsWith("[") ? `[${proxyHost}]` : proxyHost;
                const proxyURL = new URL(`${normalizedProtocol}//${proxyHostForURL}${proxyPort ? ":" + proxyPort : ""}`);
                const agentOptions = {
                    protocol: proxyURL.protocol,
                    hostname: proxyURL.hostname.replace(/^\[|\]$/g, ""),
                    port: proxyURL.port,
                    auth: proxyAuth && typeof proxyAuth === "string" ? proxyAuth : void 0
                };
                if (proxyURL.protocol === "https:") {
                    agentOptions.ALPNProtocols = ["http/1.1"];
                }
                const tunnelingAgent = getTunnelingAgent(agentOptions, configHttpsAgent);
                options.agent = tunnelingAgent;
                if (options.agents) {
                    options.agents.https = tunnelingAgent;
                }
            }
        } else {
            if (proxyAuth) {
                const base64 = Buffer.from(proxyAuth, "utf8").toString("base64");
                options.headers["Proxy-Authorization"] = "Basic " + base64;
            }
            let hasUserHostHeader = false;
            for (const name of Object.keys(options.headers)) {
                if (name.toLowerCase() === "host") {
                    hasUserHostHeader = true;
                    break;
                }
            }
            if (!hasUserHostHeader) {
                options.headers.host = options.hostname + (options.port ? ":" + options.port : "");
            }
            const proxyHost = readProxyField("hostname") || readProxyField("host");
            options.hostname = proxyHost;
            options.host = proxyHost;
            options.port = readProxyField("port");
            options.path = location;
            const proxyProtocol = readProxyField("protocol");
            if (proxyProtocol) {
                options.protocol = proxyProtocol.includes(":") ? proxyProtocol : `${proxyProtocol}:`;
            }
        }
    }
    options.beforeRedirects.proxy = function beforeRedirect(redirectOptions) {
        setProxy(redirectOptions, configProxy, redirectOptions.href, true, configHttpsAgent, configHttpAgent);
    };
}
var isHttpAdapterSupported = typeof process !== "undefined" && utils_default.kindOf(process) === "process";
var wrapAsync = asyncExecutor => {
    return new Promise((resolve, reject) => {
        let onDone;
        let isDone;
        const done = (value, isRejected) => {
            if (isDone) return;
            isDone = true;
            onDone && onDone(value, isRejected);
        };
        const _resolve = value => {
            done(value);
            resolve(value);
        };
        const _reject = reason => {
            done(reason, true);
            reject(reason);
        };
        asyncExecutor(_resolve, _reject, onDoneHandler => (onDone = onDoneHandler)).catch(_reject);
    });
};
var resolveFamily = ({ address, family }) => {
    if (!utils_default.isString(address)) {
        throw TypeError("address must be a string");
    }
    return {
        address,
        family: family || (address.indexOf(".") < 0 ? 6 : 4)
    };
};
var buildAddressEntry = (address, family) => resolveFamily(utils_default.isObject(address) ? address : { address, family });
var http2Transport = {
    request(options, cb) {
        const authority = options.protocol + "//" + options.hostname + ":" + (options.port || (options.protocol === "https:" ? 443 : 80));
        const { http2Options, headers } = options;
        const session = http2Sessions.getSession(authority, http2Options);
        const { HTTP2_HEADER_SCHEME, HTTP2_HEADER_METHOD, HTTP2_HEADER_PATH, HTTP2_HEADER_STATUS } = import_http22.default.constants;
        const http2Headers = {
            [HTTP2_HEADER_SCHEME]: options.protocol.replace(":", ""),
            [HTTP2_HEADER_METHOD]: options.method,
            [HTTP2_HEADER_PATH]: options.path
        };
        utils_default.forEach(headers, (header, name) => {
            name.charAt(0) !== ":" && (http2Headers[name] = header);
        });
        const req = session.request(http2Headers);
        req.once("response", responseHeaders => {
            const response = req;
            responseHeaders = Object.assign({}, responseHeaders);
            const status = responseHeaders[HTTP2_HEADER_STATUS];
            delete responseHeaders[HTTP2_HEADER_STATUS];
            response.headers = responseHeaders;
            response.statusCode = +status;
            cb(response);
        });
        return req;
    }
};
var http_default =
    isHttpAdapterSupported &&
    function httpAdapter(config) {
        return wrapAsync(async function dispatchHttpRequest(resolve, reject, onDone) {
            const own2 = key => utils_default.getSafeProp(config, key);
            const transitional2 = own2("transitional") || transitional_default;
            let data = own2("data");
            let lookup = own2("lookup");
            let family = own2("family");
            let httpVersion = own2("httpVersion");
            if (httpVersion === void 0) httpVersion = 1;
            let http2Options = own2("http2Options");
            const httpAgent = own2("httpAgent");
            const httpsAgent = own2("httpsAgent");
            const configProxy = own2("proxy");
            const responseType = own2("responseType");
            const responseEncoding = own2("responseEncoding");
            const socketPath = own2("socketPath");
            const method = own2("method").toUpperCase();
            const maxRedirects = own2("maxRedirects");
            const maxBodyLength = own2("maxBodyLength");
            const maxContentLength = own2("maxContentLength");
            const decompress = own2("decompress");
            let isDone;
            let rejected = false;
            let req;
            let connectPhaseTimer;
            httpVersion = +httpVersion;
            if (Number.isNaN(httpVersion)) {
                throw TypeError(`Invalid protocol version: '${config.httpVersion}' is not a number`);
            }
            if (httpVersion !== 1 && httpVersion !== 2) {
                throw TypeError(`Unsupported protocol version '${httpVersion}'`);
            }
            const isHttp2 = httpVersion === 2;
            if (lookup) {
                const _lookup = callbackify_default(lookup, value => (utils_default.isArray(value) ? value : [value]));
                lookup = (hostname, opt, cb) => {
                    _lookup(hostname, opt, (err, arg0, arg1) => {
                        if (err) {
                            return cb(err);
                        }
                        const addresses = utils_default.isArray(arg0) ? arg0.map(addr => buildAddressEntry(addr)) : [buildAddressEntry(arg0, arg1)];
                        opt.all ? cb(err, addresses) : cb(err, addresses[0].address, addresses[0].family);
                    });
                };
            }
            const abortEmitter = new import_events.EventEmitter();
            function abort(reason) {
                try {
                    abortEmitter.emit("abort", !reason || reason.type ? new CanceledError_default(null, config, req) : reason);
                } catch (err) {}
            }
            function clearConnectPhaseTimer() {
                if (connectPhaseTimer) {
                    clearTimeout(connectPhaseTimer);
                    connectPhaseTimer = null;
                }
            }
            function createTimeoutError() {
                const configTimeout = own2("timeout");
                let timeoutErrorMessage = configTimeout ? "timeout of " + configTimeout + "ms exceeded" : "timeout exceeded";
                const configTimeoutErrorMessage = own2("timeoutErrorMessage");
                if (configTimeoutErrorMessage) {
                    timeoutErrorMessage = configTimeoutErrorMessage;
                }
                return new AxiosError_default(timeoutErrorMessage, transitional2.clarifyTimeoutError ? AxiosError_default.ETIMEDOUT : AxiosError_default.ECONNABORTED, config, req);
            }
            abortEmitter.once("abort", reject);
            const onFinished = () => {
                clearConnectPhaseTimer();
                if (config.cancelToken) {
                    config.cancelToken.unsubscribe(abort);
                }
                if (config.signal) {
                    config.signal.removeEventListener("abort", abort);
                }
                abortEmitter.removeAllListeners();
            };
            if (config.cancelToken || config.signal) {
                config.cancelToken && config.cancelToken.subscribe(abort);
                if (config.signal) {
                    config.signal.aborted ? abort() : config.signal.addEventListener("abort", abort);
                }
            }
            onDone((response, isRejected) => {
                isDone = true;
                clearConnectPhaseTimer();
                if (isRejected) {
                    rejected = true;
                    onFinished();
                    return;
                }
                const { data: data2 } = response;
                if (data2 instanceof import_stream4.default.Readable || data2 instanceof import_stream4.default.Duplex) {
                    const offListeners = import_stream4.default.finished(data2, () => {
                        offListeners();
                        onFinished();
                    });
                } else {
                    onFinished();
                }
            });
            const fullPath = buildFullPath(own2("baseURL"), own2("url"), own2("allowAbsoluteUrls"), config);
            const urlBase = socketPath ? "http://localhost" : platform_default.hasBrowserEnv ? platform_default.origin : void 0;
            const parsed = new URL(fullPath, urlBase);
            const protocol = parsed.protocol || supportedProtocols[0];
            if (protocol === "data:") {
                if (maxContentLength > -1) {
                    const dataUrl = String(own2("url") || fullPath || "");
                    const estimated = estimateDataURLDecodedBytes(dataUrl);
                    if (estimated > maxContentLength) {
                        return reject(new AxiosError_default("maxContentLength size of " + maxContentLength + " exceeded", AxiosError_default.ERR_BAD_RESPONSE, config));
                    }
                }
                let convertedData;
                if (method !== "GET") {
                    return settle(resolve, reject, {
                        status: 405,
                        statusText: "method not allowed",
                        headers: {},
                        config
                    });
                }
                try {
                    convertedData = fromDataURI(own2("url"), responseType === "blob", {
                        Blob: config.env && config.env.Blob
                    });
                } catch (err) {
                    throw AxiosError_default.from(err, AxiosError_default.ERR_BAD_REQUEST, config);
                }
                if (responseType === "text") {
                    convertedData = convertedData.toString(responseEncoding);
                    if (!responseEncoding || responseEncoding === "utf8") {
                        convertedData = utils_default.stripBOM(convertedData);
                    }
                } else if (responseType === "stream") {
                    convertedData = import_stream4.default.Readable.from(convertedData);
                }
                return settle(resolve, reject, {
                    data: convertedData,
                    status: 200,
                    statusText: "OK",
                    headers: new AxiosHeaders_default(),
                    config
                });
            }
            if (supportedProtocols.indexOf(protocol) === -1) {
                return reject(new AxiosError_default("Unsupported protocol " + protocol, AxiosError_default.ERR_BAD_REQUEST, config));
            }
            const headers = AxiosHeaders_default.from(config.headers).normalize();
            headers.set("User-Agent", "axios/" + VERSION, false);
            const { onUploadProgress, onDownloadProgress } = config;
            const maxRate = config.maxRate;
            let maxUploadRate = void 0;
            let maxDownloadRate = void 0;
            if (utils_default.isSpecCompliantForm(data)) {
                const userBoundary = headers.getContentType(/boundary=([-_\w\d]{10,70})/i);
                data = formDataToStream_default(
                    data,
                    formHeaders => {
                        headers.set(formHeaders);
                    },
                    {
                        tag: `axios-${VERSION}-boundary`,
                        boundary: (userBoundary && userBoundary[1]) || void 0
                    }
                );
            } else if (utils_default.isFormData(data) && utils_default.isFunction(data.getHeaders) && data.getHeaders !== Object.prototype.getHeaders) {
                setFormDataHeaders(headers, data.getHeaders(), own2("formDataHeaderPolicy"));
                if (!headers.hasContentLength()) {
                    try {
                        const knownLength = await import_util3.default.promisify(data.getLength).call(data);
                        Number.isFinite(knownLength) && knownLength >= 0 && headers.setContentLength(knownLength);
                    } catch (e) {}
                }
            } else if (utils_default.isBlob(data) || utils_default.isFile(data)) {
                data.size && headers.setContentType(data.type || "application/octet-stream");
                headers.setContentLength(data.size || 0);
                data = import_stream4.default.Readable.from(readBlob_default(data));
            } else if (data && !utils_default.isStream(data)) {
                if (Buffer.isBuffer(data)) {
                } else if (utils_default.isArrayBuffer(data)) {
                    data = Buffer.from(new Uint8Array(data));
                } else if (utils_default.isString(data)) {
                    data = Buffer.from(data, "utf-8");
                } else {
                    return reject(new AxiosError_default("Data after transformation must be a string, an ArrayBuffer, a Buffer, or a Stream", AxiosError_default.ERR_BAD_REQUEST, config));
                }
                headers.setContentLength(data.length, false);
                if (maxBodyLength > -1 && data.length > maxBodyLength) {
                    return reject(new AxiosError_default("Request body larger than maxBodyLength limit", AxiosError_default.ERR_BAD_REQUEST, config));
                }
            }
            const contentLength = utils_default.toFiniteNumber(headers.getContentLength());
            if (utils_default.isArray(maxRate)) {
                maxUploadRate = maxRate[0];
                maxDownloadRate = maxRate[1];
            } else {
                maxUploadRate = maxDownloadRate = maxRate;
            }
            if (data && (onUploadProgress || maxUploadRate)) {
                if (!utils_default.isStream(data)) {
                    data = import_stream4.default.Readable.from(data, { objectMode: false });
                }
                data = import_stream4.default.pipeline(
                    [
                        data,
                        new AxiosTransformStream_default({
                            maxRate: utils_default.toFiniteNumber(maxUploadRate)
                        })
                    ],
                    utils_default.noop
                );
                onUploadProgress && data.on("progress", flushOnFinish(data, progressEventDecorator(contentLength, progressEventReducer(asyncDecorator(onUploadProgress), false, 3))));
            }
            let auth = void 0;
            const configAuth = own2("auth");
            if (configAuth) {
                const username = utils_default.getSafeProp(configAuth, "username") || "";
                const password = utils_default.getSafeProp(configAuth, "password") || "";
                auth = username + ":" + password;
            }
            if (!auth && (parsed.username || parsed.password)) {
                const urlUsername = decodeURIComponentSafe(parsed.username);
                const urlPassword = decodeURIComponentSafe(parsed.password);
                auth = urlUsername + ":" + urlPassword;
            }
            auth && headers.delete("authorization");
            let path;
            try {
                path = buildURL(parsed.pathname + parsed.search, own2("params"), own2("paramsSerializer")).replace(/^\?/, "");
            } catch (err) {
                return reject(
                    AxiosError_default.from(err, AxiosError_default.ERR_BAD_REQUEST, config, null, null, {
                        url: own2("url"),
                        exists: true
                    })
                );
            }
            headers.set("Accept-Encoding", utils_default.hasOwnProp(transitional2, "advertiseZstdAcceptEncoding") && transitional2.advertiseZstdAcceptEncoding === true ? ACCEPT_ENCODING_WITH_ZSTD : ACCEPT_ENCODING, false);
            const options = Object.assign(/* @__PURE__ */ Object.create(null), {
                path,
                method,
                headers: toByteStringHeaderObject(headers),
                agents: { http: httpAgent, https: httpsAgent },
                auth,
                protocol,
                family,
                beforeRedirect: dispatchBeforeRedirect,
                beforeRedirects: /* @__PURE__ */ Object.create(null),
                http2Options
            });
            !utils_default.isUndefined(lookup) && (options.lookup = lookup);
            if (socketPath) {
                if (typeof socketPath !== "string") {
                    return reject(new AxiosError_default("socketPath must be a string", AxiosError_default.ERR_BAD_OPTION_VALUE, config));
                }
                const allowedSocketPaths = own2("allowedSocketPaths");
                if (allowedSocketPaths != null) {
                    const allowed = Array.isArray(allowedSocketPaths) ? allowedSocketPaths : [allowedSocketPaths];
                    const resolvedSocket = (0, import_path.resolve)(socketPath);
                    const isAllowed = allowed.some(entry => typeof entry === "string" && (0, import_path.resolve)(entry) === resolvedSocket);
                    if (!isAllowed) {
                        return reject(new AxiosError_default(`socketPath "${socketPath}" is not permitted by allowedSocketPaths`, AxiosError_default.ERR_BAD_OPTION_VALUE, config));
                    }
                }
                options.socketPath = socketPath;
            } else {
                options.hostname = parsed.hostname.startsWith("[") ? parsed.hostname.slice(1, -1) : parsed.hostname;
                options.port = parsed.port;
                setProxy(options, configProxy, protocol + "//" + parsed.hostname + (parsed.port ? ":" + parsed.port : "") + options.path, false, httpsAgent, httpAgent);
            }
            let transport;
            let isNativeTransport = false;
            let transportEnforcesMaxBodyLength = false;
            const isHttpsRequest = isHttps.test(options.protocol);
            if (options.agent == null) {
                options.agent = isHttpsRequest ? httpsAgent : httpAgent;
            }
            if (isHttp2) {
                transport = http2Transport;
            } else {
                const configTransport = own2("transport");
                if (configTransport) {
                    transport = configTransport;
                } else if (maxRedirects === 0) {
                    transport = isHttpsRequest ? import_https.default : import_http.default;
                    isNativeTransport = true;
                } else {
                    transportEnforcesMaxBodyLength = true;
                    options.sensitiveHeaders = [];
                    if (maxRedirects) {
                        options.maxRedirects = maxRedirects;
                    }
                    const configBeforeRedirect = own2("beforeRedirect");
                    if (configBeforeRedirect) {
                        options.beforeRedirects.config = configBeforeRedirect;
                    }
                    if (auth) {
                        const requestOrigin = parsed.origin;
                        const authToRestore = auth;
                        options.beforeRedirects.auth = function beforeRedirectAuth(redirectOptions) {
                            try {
                                if (new URL(redirectOptions.href).origin === requestOrigin) {
                                    redirectOptions.auth = authToRestore;
                                }
                            } catch (e) {}
                        };
                    }
                    const sensitiveHeaders = own2("sensitiveHeaders");
                    if (sensitiveHeaders != null) {
                        if (!utils_default.isArray(sensitiveHeaders)) {
                            return reject(new AxiosError_default("sensitiveHeaders must be an array of strings", AxiosError_default.ERR_BAD_OPTION_VALUE, config));
                        }
                        const sensitiveSet = /* @__PURE__ */ new Set();
                        for (const header of sensitiveHeaders) {
                            if (!utils_default.isString(header)) {
                                return reject(new AxiosError_default("sensitiveHeaders must be an array of strings", AxiosError_default.ERR_BAD_OPTION_VALUE, config));
                            }
                            sensitiveSet.add(header.toLowerCase());
                        }
                        if (sensitiveSet.size) {
                            options.sensitiveHeaders = Array.from(sensitiveSet);
                            options.beforeRedirects.sensitiveHeaders = function beforeRedirectSensitiveHeaders(redirectOptions, requestDetails) {
                                if (!isSameOriginRedirect(redirectOptions, requestDetails)) {
                                    stripMatchingHeaders(redirectOptions.headers, sensitiveSet);
                                }
                            };
                        }
                    }
                    transport = isHttpsRequest ? httpsFollow : httpFollow;
                }
            }
            if (maxBodyLength > -1) {
                options.maxBodyLength = maxBodyLength;
            } else {
                options.maxBodyLength = Infinity;
            }
            options.insecureHTTPParser = Boolean(own2("insecureHTTPParser"));
            req = transport.request(options, function handleResponse(res) {
                clearConnectPhaseTimer();
                if (req.destroyed) return;
                const streams = [res];
                const responseLength = utils_default.toFiniteNumber(res.headers["content-length"]);
                if (onDownloadProgress || maxDownloadRate) {
                    const transformStream = new AxiosTransformStream_default({
                        maxRate: utils_default.toFiniteNumber(maxDownloadRate)
                    });
                    onDownloadProgress && transformStream.on("progress", flushOnFinish(transformStream, progressEventDecorator(responseLength, progressEventReducer(asyncDecorator(onDownloadProgress), true, 3))));
                    streams.push(transformStream);
                }
                let responseStream = res;
                const lastRequest = res.req || req;
                if (decompress !== false && res.headers["content-encoding"]) {
                    if (method === "HEAD" || res.statusCode === 204) {
                        delete res.headers["content-encoding"];
                    }
                    switch ((res.headers["content-encoding"] || "").toLowerCase()) {
                        /*eslint default-case:0*/
                        case "gzip":
                        case "x-gzip":
                        case "compress":
                        case "x-compress":
                            streams.push(import_zlib.default.createUnzip(zlibOptions));
                            delete res.headers["content-encoding"];
                            break;
                        case "deflate":
                            streams.push(new ZlibHeaderTransformStream_default());
                            streams.push(import_zlib.default.createUnzip(zlibOptions));
                            delete res.headers["content-encoding"];
                            break;
                        case "br":
                            if (isBrotliSupported) {
                                streams.push(import_zlib.default.createBrotliDecompress(brotliOptions));
                                delete res.headers["content-encoding"];
                            }
                            break;
                        case "zstd":
                            if (isZstdSupported) {
                                streams.push(import_zlib.default.createZstdDecompress(zstdOptions));
                                delete res.headers["content-encoding"];
                            }
                            break;
                    }
                }
                responseStream = streams.length > 1 ? import_stream4.default.pipeline(streams, utils_default.noop) : streams[0];
                const response = {
                    status: res.statusCode,
                    statusText: res.statusMessage,
                    headers: new AxiosHeaders_default(res.headers),
                    config,
                    request: lastRequest
                };
                if (responseType === "stream") {
                    if (maxContentLength > -1) {
                        const limit = maxContentLength;
                        const source = responseStream;
                        async function* enforceMaxContentLength() {
                            let totalResponseBytes = 0;
                            for await (const chunk of source) {
                                totalResponseBytes += chunk.length;
                                if (totalResponseBytes > limit) {
                                    throw new AxiosError_default("maxContentLength size of " + limit + " exceeded", AxiosError_default.ERR_BAD_RESPONSE, config, lastRequest);
                                }
                                yield chunk;
                            }
                        }
                        responseStream = import_stream4.default.Readable.from(enforceMaxContentLength(), {
                            objectMode: false
                        });
                    }
                    response.data = responseStream;
                    settle(resolve, reject, response);
                } else {
                    const responseBuffer = [];
                    let totalResponseBytes = 0;
                    responseStream.on("data", function handleStreamData(chunk) {
                        responseBuffer.push(chunk);
                        totalResponseBytes += chunk.length;
                        if (maxContentLength > -1 && totalResponseBytes > maxContentLength) {
                            rejected = true;
                            responseStream.destroy();
                            abort(new AxiosError_default("maxContentLength size of " + maxContentLength + " exceeded", AxiosError_default.ERR_BAD_RESPONSE, config, lastRequest));
                        }
                    });
                    responseStream.on("aborted", function handlerStreamAborted() {
                        if (rejected) {
                            return;
                        }
                        const err = new AxiosError_default("stream has been aborted", AxiosError_default.ERR_BAD_RESPONSE, config, lastRequest, response);
                        responseStream.destroy(err);
                        reject(err);
                    });
                    responseStream.on("error", function handleStreamError(err) {
                        if (rejected) return;
                        reject(AxiosError_default.from(err, null, config, lastRequest, response));
                    });
                    responseStream.on("end", function handleStreamEnd() {
                        try {
                            let responseData = responseBuffer.length === 1 ? responseBuffer[0] : Buffer.concat(responseBuffer);
                            if (responseType !== "arraybuffer") {
                                responseData = responseData.toString(responseEncoding);
                                if (!responseEncoding || responseEncoding === "utf8") {
                                    responseData = utils_default.stripBOM(responseData);
                                }
                            }
                            response.data = responseData;
                        } catch (err) {
                            return reject(AxiosError_default.from(err, null, config, response.request, response));
                        }
                        settle(resolve, reject, response);
                    });
                }
                abortEmitter.once("abort", err => {
                    if (!responseStream.destroyed) {
                        responseStream.emit("error", err);
                        responseStream.destroy();
                    }
                });
            });
            abortEmitter.once("abort", err => {
                if (req.close) {
                    req.close();
                } else {
                    req.destroy(err);
                }
            });
            req.on("error", function handleRequestError(err) {
                reject(AxiosError_default.from(err, null, config, req));
            });
            const boundSockets = /* @__PURE__ */ new Set();
            req.on("socket", function handleRequestSocket(socket) {
                if (typeof socket.setKeepAlive === "function") {
                    socket.setKeepAlive(true, 1e3 * 60);
                }
                if (!socket[kAxiosSocketListener]) {
                    socket.on("error", function handleSocketError(err) {
                        const current = socket[kAxiosCurrentReq];
                        if (current && !current.destroyed) {
                            current.destroy(err);
                        }
                    });
                    socket[kAxiosSocketListener] = true;
                }
                socket[kAxiosCurrentReq] = req;
                boundSockets.add(socket);
            });
            req.once("close", function clearCurrentReq() {
                clearConnectPhaseTimer();
                for (const socket of boundSockets) {
                    if (socket[kAxiosCurrentReq] === req) {
                        socket[kAxiosCurrentReq] = null;
                    }
                }
                boundSockets.clear();
            });
            if (own2("timeout")) {
                const timeout = parseInt(own2("timeout"), 10);
                if (Number.isNaN(timeout)) {
                    abort(new AxiosError_default("error trying to parse `config.timeout` to int", AxiosError_default.ERR_BAD_OPTION_VALUE, config, req));
                    return;
                }
                const handleTimeout = function handleTimeout2() {
                    if (isDone) return;
                    abort(createTimeoutError());
                };
                if (isNativeTransport && timeout > 0) {
                    connectPhaseTimer = setTimeout(handleTimeout, timeout);
                }
                req.setTimeout(timeout, handleTimeout);
            } else {
                req.setTimeout(0);
            }
            if (utils_default.isStream(data)) {
                let ended = false;
                let errored = false;
                data.on("end", () => {
                    ended = true;
                });
                data.once("error", err => {
                    errored = true;
                    req.destroy(err);
                });
                data.on("close", () => {
                    if (!ended && !errored) {
                        abort(new CanceledError_default("Request stream has been aborted", config, req));
                    }
                });
                let uploadStream = data;
                if (maxBodyLength > -1 && !transportEnforcesMaxBodyLength) {
                    const limit = maxBodyLength;
                    let bytesSent = 0;
                    uploadStream = import_stream4.default.pipeline(
                        [
                            data,
                            new import_stream4.default.Transform({
                                transform(chunk, _enc, cb) {
                                    bytesSent += chunk.length;
                                    if (bytesSent > limit) {
                                        return cb(new AxiosError_default("Request body larger than maxBodyLength limit", AxiosError_default.ERR_BAD_REQUEST, config, req));
                                    }
                                    cb(null, chunk);
                                }
                            })
                        ],
                        utils_default.noop
                    );
                    uploadStream.on("error", err => {
                        if (!req.destroyed) req.destroy(err);
                    });
                }
                uploadStream.pipe(req);
            } else {
                data && req.write(data);
                req.end();
            }
        });
    };

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/isURLSameOrigin.js
var isURLSameOrigin_default = platform_default.hasStandardBrowserEnv
    ? /* @__PURE__ */ ((origin2, isMSIE) => url2 => {
          url2 = new URL(url2, platform_default.origin);
          return origin2.protocol === url2.protocol && origin2.host === url2.host && (isMSIE || origin2.port === url2.port);
      })(new URL(platform_default.origin), platform_default.navigator && /(msie|trident)/i.test(platform_default.navigator.userAgent))
    : () => true;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/cookies.js
var cookies_default = platform_default.hasStandardBrowserEnv
    ? // Standard browser envs support document.cookie
      {
          write(name, value, expires, path, domain, secure, sameSite) {
              if (typeof document === "undefined") return;
              const cookie = [`${name}=${encodeURIComponent(value)}`];
              if (utils_default.isNumber(expires)) {
                  cookie.push(`expires=${new Date(expires).toUTCString()}`);
              }
              if (utils_default.isString(path)) {
                  cookie.push(`path=${path}`);
              }
              if (utils_default.isString(domain)) {
                  cookie.push(`domain=${domain}`);
              }
              if (secure === true) {
                  cookie.push("secure");
              }
              if (utils_default.isString(sameSite)) {
                  cookie.push(`SameSite=${sameSite}`);
              }
              document.cookie = cookie.join("; ");
          },
          read(name) {
              if (typeof document === "undefined") return null;
              const cookies = document.cookie.split(";");
              for (let i = 0; i < cookies.length; i++) {
                  const cookie = cookies[i].replace(/^\s+/, "");
                  const eq = cookie.indexOf("=");
                  if (eq !== -1 && cookie.slice(0, eq) === name) {
                      try {
                          return decodeURIComponent(cookie.slice(eq + 1));
                      } catch (e) {
                          return cookie.slice(eq + 1);
                      }
                  }
              }
              return null;
          },
          remove(name) {
              this.write(name, "", Date.now() - 864e5, "/");
          }
      }
    : // Non-standard browser env (web workers, react-native) lack needed support.
      {
          write() {},
          read() {
              return null;
          },
          remove() {}
      };

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/core/mergeConfig.js
var headersToObject = thing => (thing instanceof AxiosHeaders_default ? { ...thing } : thing);
function mergeConfig(config1, config2) {
    config1 = config1 || {};
    config2 = config2 || {};
    const config = /* @__PURE__ */ Object.create(null);
    Object.defineProperty(config, "hasOwnProperty", {
        // Null-proto descriptor so a polluted Object.prototype.get cannot turn
        // this data descriptor into an accessor descriptor on the way in.
        __proto__: null,
        value: Object.prototype.hasOwnProperty,
        enumerable: false,
        writable: true,
        configurable: true
    });
    function getMergedValue(target, source, prop, caseless) {
        if (utils_default.isPlainObject(target) && utils_default.isPlainObject(source)) {
            return utils_default.merge.call({ caseless }, target, source);
        } else if (utils_default.isPlainObject(source)) {
            return utils_default.merge({}, source);
        } else if (utils_default.isArray(source)) {
            return source.slice();
        }
        return source;
    }
    function mergeDeepProperties(a, b, prop, caseless) {
        if (!utils_default.isUndefined(b)) {
            return getMergedValue(a, b, prop, caseless);
        } else if (!utils_default.isUndefined(a)) {
            return getMergedValue(void 0, a, prop, caseless);
        }
    }
    function valueFromConfig2(a, b) {
        if (!utils_default.isUndefined(b)) {
            return getMergedValue(void 0, b);
        }
    }
    function defaultToConfig2(a, b) {
        if (!utils_default.isUndefined(b)) {
            return getMergedValue(void 0, b);
        } else if (!utils_default.isUndefined(a)) {
            return getMergedValue(void 0, a);
        }
    }
    function getMergedTransitionalOption(prop) {
        const transitional2 = utils_default.hasOwnProp(config2, "transitional") ? config2.transitional : void 0;
        if (!utils_default.isUndefined(transitional2)) {
            if (utils_default.isPlainObject(transitional2)) {
                if (utils_default.hasOwnProp(transitional2, prop)) {
                    return transitional2[prop];
                }
            } else {
                return void 0;
            }
        }
        const transitional1 = utils_default.hasOwnProp(config1, "transitional") ? config1.transitional : void 0;
        if (utils_default.isPlainObject(transitional1) && utils_default.hasOwnProp(transitional1, prop)) {
            return transitional1[prop];
        }
        return void 0;
    }
    function mergeDirectKeys(a, b, prop) {
        if (utils_default.hasOwnProp(config2, prop)) {
            return getMergedValue(a, b);
        } else if (utils_default.hasOwnProp(config1, prop)) {
            return getMergedValue(void 0, a);
        }
    }
    const mergeMap = {
        url: valueFromConfig2,
        method: valueFromConfig2,
        data: valueFromConfig2,
        baseURL: defaultToConfig2,
        transformRequest: defaultToConfig2,
        transformResponse: defaultToConfig2,
        paramsSerializer: defaultToConfig2,
        timeout: defaultToConfig2,
        timeoutMessage: defaultToConfig2,
        withCredentials: defaultToConfig2,
        withXSRFToken: defaultToConfig2,
        adapter: defaultToConfig2,
        responseType: defaultToConfig2,
        xsrfCookieName: defaultToConfig2,
        xsrfHeaderName: defaultToConfig2,
        onUploadProgress: defaultToConfig2,
        onDownloadProgress: defaultToConfig2,
        decompress: defaultToConfig2,
        maxContentLength: defaultToConfig2,
        maxBodyLength: defaultToConfig2,
        beforeRedirect: defaultToConfig2,
        transport: defaultToConfig2,
        httpAgent: defaultToConfig2,
        httpsAgent: defaultToConfig2,
        cancelToken: defaultToConfig2,
        socketPath: defaultToConfig2,
        allowedSocketPaths: defaultToConfig2,
        responseEncoding: defaultToConfig2,
        validateStatus: mergeDirectKeys,
        headers: (a, b, prop) => mergeDeepProperties(headersToObject(a), headersToObject(b), prop, true)
    };
    utils_default.forEach(Object.keys({ ...config1, ...config2 }), function computeConfigValue(prop) {
        if (prop === "__proto__" || prop === "constructor" || prop === "prototype") return;
        const merge2 = utils_default.hasOwnProp(mergeMap, prop) ? mergeMap[prop] : mergeDeepProperties;
        const a = utils_default.hasOwnProp(config1, prop) ? config1[prop] : void 0;
        const b = utils_default.hasOwnProp(config2, prop) ? config2[prop] : void 0;
        const configValue = merge2(a, b, prop);
        (utils_default.isUndefined(configValue) && merge2 !== mergeDirectKeys) || (config[prop] = configValue);
    });
    if (utils_default.hasOwnProp(config2, "validateStatus") && utils_default.isUndefined(config2.validateStatus) && getMergedTransitionalOption("validateStatusUndefinedResolves") === false) {
        if (utils_default.hasOwnProp(config1, "validateStatus")) {
            config.validateStatus = getMergedValue(void 0, config1.validateStatus);
        } else {
            delete config.validateStatus;
        }
    }
    return config;
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/resolveConfig.js
var FORM_DATA_CONTENT_HEADERS2 = ["content-type", "content-length"];
function setFormDataHeaders2(headers, formHeaders, policy) {
    if (policy !== "content-only") {
        headers.set(formHeaders);
        return;
    }
    Object.entries(formHeaders || {}).forEach(([key, val]) => {
        if (FORM_DATA_CONTENT_HEADERS2.includes(key.toLowerCase())) {
            headers.set(key, val);
        }
    });
}
var encodeUTF8 = str => encodeURIComponent(str).replace(/%([0-9A-F]{2})/gi, (_, hex) => String.fromCharCode(parseInt(hex, 16)));
function resolveConfig(config) {
    const newConfig = mergeConfig({}, config);
    const own2 = key => (utils_default.hasOwnProp(newConfig, key) ? newConfig[key] : void 0);
    const data = own2("data");
    let withXSRFToken = own2("withXSRFToken");
    const xsrfHeaderName = own2("xsrfHeaderName");
    const xsrfCookieName = own2("xsrfCookieName");
    let headers = own2("headers");
    const auth = own2("auth");
    const baseURL2 = own2("baseURL");
    const allowAbsoluteUrls = own2("allowAbsoluteUrls");
    const url2 = own2("url");
    newConfig.headers = headers = AxiosHeaders_default.from(headers);
    newConfig.url = buildURL(buildFullPath(baseURL2, url2, allowAbsoluteUrls, newConfig), own2("params"), own2("paramsSerializer"));
    if (auth) {
        const username = utils_default.getSafeProp(auth, "username") || "";
        const password = utils_default.getSafeProp(auth, "password") || "";
        try {
            headers.set("Authorization", "Basic " + btoa(username + ":" + (password ? encodeUTF8(password) : "")));
        } catch (e) {
            throw AxiosError_default.from(e, AxiosError_default.ERR_BAD_OPTION_VALUE, config);
        }
    }
    if (utils_default.isFormData(data)) {
        if (platform_default.hasStandardBrowserEnv || platform_default.hasStandardBrowserWebWorkerEnv || utils_default.isReactNative(data)) {
            headers.setContentType(void 0);
        } else if (utils_default.isFunction(data.getHeaders)) {
            setFormDataHeaders2(headers, data.getHeaders(), own2("formDataHeaderPolicy"));
        }
    }
    if (platform_default.hasStandardBrowserEnv) {
        if (utils_default.isFunction(withXSRFToken)) {
            withXSRFToken = withXSRFToken(newConfig);
        }
        const shouldSendXSRF = withXSRFToken === true || (withXSRFToken == null && isURLSameOrigin_default(newConfig.url));
        if (shouldSendXSRF) {
            const xsrfValue = xsrfHeaderName && xsrfCookieName && cookies_default.read(xsrfCookieName);
            if (xsrfValue) {
                headers.set(xsrfHeaderName, xsrfValue);
            }
        }
    }
    return newConfig;
}
var resolveConfig_default = resolveConfig;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/adapters/xhr.js
var isXHRAdapterSupported = typeof XMLHttpRequest !== "undefined";
var xhr_default =
    isXHRAdapterSupported &&
    function (config) {
        return new Promise(function dispatchXhrRequest(resolve, reject) {
            const _config = resolveConfig_default(config);
            let requestData = _config.data;
            const requestHeaders = AxiosHeaders_default.from(_config.headers).normalize();
            let { responseType, onUploadProgress, onDownloadProgress } = _config;
            let onCanceled;
            let uploadThrottled, downloadThrottled;
            let flushUpload, flushDownload;
            function done() {
                flushUpload && flushUpload();
                flushDownload && flushDownload();
                _config.cancelToken && _config.cancelToken.unsubscribe(onCanceled);
                _config.signal && _config.signal.removeEventListener("abort", onCanceled);
            }
            let request2 = new XMLHttpRequest();
            request2.open(_config.method.toUpperCase(), _config.url, true);
            request2.timeout = _config.timeout;
            function onloadend() {
                if (!request2) {
                    return;
                }
                const responseHeaders = AxiosHeaders_default.from("getAllResponseHeaders" in request2 && request2.getAllResponseHeaders());
                const responseData = !responseType || responseType === "text" || responseType === "json" ? request2.responseText : request2.response;
                const response = {
                    data: responseData,
                    status: request2.status,
                    statusText: request2.statusText,
                    headers: responseHeaders,
                    config,
                    request: request2
                };
                settle(
                    function _resolve(value) {
                        resolve(value);
                        done();
                    },
                    function _reject(err) {
                        reject(err);
                        done();
                    },
                    response
                );
                request2 = null;
            }
            if ("onloadend" in request2) {
                request2.onloadend = onloadend;
            } else {
                request2.onreadystatechange = function handleLoad() {
                    if (!request2 || request2.readyState !== 4) {
                        return;
                    }
                    if (request2.status === 0 && !(request2.responseURL && request2.responseURL.startsWith("file:"))) {
                        return;
                    }
                    setTimeout(onloadend);
                };
            }
            request2.onabort = function handleAbort() {
                if (!request2) {
                    return;
                }
                reject(new AxiosError_default("Request aborted", AxiosError_default.ECONNABORTED, config, request2));
                done();
                request2 = null;
            };
            request2.onerror = function handleError(event) {
                const msg = event && event.message ? event.message : "Network Error";
                const err = new AxiosError_default(msg, AxiosError_default.ERR_NETWORK, config, request2);
                err.event = event || null;
                reject(err);
                done();
                request2 = null;
            };
            request2.ontimeout = function handleTimeout() {
                let timeoutErrorMessage = _config.timeout ? "timeout of " + _config.timeout + "ms exceeded" : "timeout exceeded";
                const transitional2 = _config.transitional || transitional_default;
                if (_config.timeoutErrorMessage) {
                    timeoutErrorMessage = _config.timeoutErrorMessage;
                }
                reject(new AxiosError_default(timeoutErrorMessage, transitional2.clarifyTimeoutError ? AxiosError_default.ETIMEDOUT : AxiosError_default.ECONNABORTED, config, request2));
                done();
                request2 = null;
            };
            requestData === void 0 && requestHeaders.setContentType(null);
            if ("setRequestHeader" in request2) {
                utils_default.forEach(toByteStringHeaderObject(requestHeaders), function setRequestHeader(val, key) {
                    request2.setRequestHeader(key, val);
                });
            }
            if (!utils_default.isUndefined(_config.withCredentials)) {
                request2.withCredentials = !!_config.withCredentials;
            }
            if (responseType && responseType !== "json") {
                request2.responseType = _config.responseType;
            }
            if (onDownloadProgress) {
                [downloadThrottled, flushDownload] = progressEventReducer(onDownloadProgress, true);
                request2.addEventListener("progress", downloadThrottled);
            }
            if (onUploadProgress && request2.upload) {
                [uploadThrottled, flushUpload] = progressEventReducer(onUploadProgress);
                request2.upload.addEventListener("progress", uploadThrottled);
                request2.upload.addEventListener("loadend", flushUpload);
            }
            if (_config.cancelToken || _config.signal) {
                onCanceled = cancel => {
                    if (!request2) {
                        return;
                    }
                    reject(!cancel || cancel.type ? new CanceledError_default(null, config, request2) : cancel);
                    request2.abort();
                    done();
                    request2 = null;
                };
                _config.cancelToken && _config.cancelToken.subscribe(onCanceled);
                if (_config.signal) {
                    _config.signal.aborted ? onCanceled() : _config.signal.addEventListener("abort", onCanceled);
                }
            }
            const protocol = parseProtocol(_config.url);
            if (protocol && !platform_default.protocols.includes(protocol)) {
                reject(new AxiosError_default("Unsupported protocol " + protocol + ":", AxiosError_default.ERR_BAD_REQUEST, config));
                done();
                return;
            }
            request2.send(requestData || null);
        });
    };

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/composeSignals.js
var composeSignals = (signals, timeout) => {
    signals = signals ? signals.filter(Boolean) : [];
    if (!timeout && !signals.length) {
        return;
    }
    const controller = new AbortController();
    let aborted = false;
    const onabort = function (reason) {
        if (!aborted) {
            aborted = true;
            unsubscribe();
            const err = reason instanceof Error ? reason : this.reason;
            controller.abort(err instanceof AxiosError_default ? err : new CanceledError_default(err instanceof Error ? err.message : err));
        }
    };
    let timer =
        timeout &&
        setTimeout(() => {
            timer = null;
            onabort(new AxiosError_default(`timeout of ${timeout}ms exceeded`, AxiosError_default.ETIMEDOUT));
        }, timeout);
    const unsubscribe = () => {
        if (!signals) {
            return;
        }
        timer && clearTimeout(timer);
        timer = null;
        signals.forEach(signal2 => {
            signal2.unsubscribe ? signal2.unsubscribe(onabort) : signal2.removeEventListener("abort", onabort);
        });
        signals = null;
    };
    signals.forEach(signal2 => signal2.addEventListener("abort", onabort, { once: true }));
    const { signal } = controller;
    signal.unsubscribe = () => utils_default.asap(unsubscribe);
    return signal;
};
var composeSignals_default = composeSignals;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/trackStream.js
var streamChunk = function* (chunk, chunkSize) {
    let len = chunk.byteLength;
    if (!chunkSize || len < chunkSize) {
        yield chunk;
        return;
    }
    let pos = 0;
    let end;
    while (pos < len) {
        end = pos + chunkSize;
        yield chunk.slice(pos, end);
        pos = end;
    }
};
var readBytes = async function* (iterable, chunkSize) {
    for await (const chunk of readStream(iterable)) {
        yield* streamChunk(chunk, chunkSize);
    }
};
var readStream = async function* (stream4) {
    if (stream4[Symbol.asyncIterator]) {
        yield* stream4;
        return;
    }
    const reader = stream4.getReader();
    try {
        for (;;) {
            const { done, value } = await reader.read();
            if (done) {
                break;
            }
            yield value;
        }
    } finally {
        await reader.cancel();
    }
};
var trackStream = (stream4, chunkSize, onProgress, onFinish) => {
    const iterator2 = readBytes(stream4, chunkSize);
    let bytes = 0;
    let done;
    let _onFinish = e => {
        if (!done) {
            done = true;
            onFinish && onFinish(e);
        }
    };
    return new ReadableStream(
        {
            async pull(controller) {
                try {
                    const { done: done2, value } = await iterator2.next();
                    if (done2) {
                        _onFinish();
                        controller.close();
                        return;
                    }
                    let len = value.byteLength;
                    if (onProgress) {
                        let loadedBytes = (bytes += len);
                        onProgress(loadedBytes);
                    }
                    controller.enqueue(new Uint8Array(value));
                } catch (err) {
                    _onFinish(err);
                    throw err;
                }
            },
            cancel(reason) {
                _onFinish(reason);
                return iterator2.return();
            }
        },
        {
            highWaterMark: 2
        }
    );
};

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/adapters/fetch.js
var DEFAULT_CHUNK_SIZE = 64 * 1024;
var { isFunction: isFunction2 } = utils_default;
var encodeUTF82 = str => encodeURIComponent(str).replace(/%([0-9A-F]{2})/gi, (_, hex) => String.fromCharCode(parseInt(hex, 16)));
var decodeURIComponentSafe2 = value => {
    if (!utils_default.isString(value)) {
        return value;
    }
    try {
        return decodeURIComponent(value);
    } catch (error) {
        return value;
    }
};
var test = (fn, ...args) => {
    try {
        return !!fn(...args);
    } catch (e) {
        return false;
    }
};
var maybeWithAuthCredentials = url2 => {
    const protocolIndex = url2.indexOf("://");
    let urlToCheck = url2;
    if (protocolIndex !== -1) {
        urlToCheck = urlToCheck.slice(protocolIndex + 3);
    }
    return urlToCheck.includes("@") || urlToCheck.includes(":");
};
var factory = env => {
    const globalObject = utils_default.global !== void 0 && utils_default.global !== null ? utils_default.global : globalThis;
    const { ReadableStream: ReadableStream2, TextEncoder: TextEncoder2 } = globalObject;
    env = utils_default.merge.call(
        {
            skipUndefined: true
        },
        {
            Request: globalObject.Request,
            Response: globalObject.Response
        },
        env
    );
    const { fetch: envFetch, Request, Response } = env;
    const isFetchSupported = envFetch ? isFunction2(envFetch) : typeof fetch === "function";
    const isRequestSupported = isFunction2(Request);
    const isResponseSupported = isFunction2(Response);
    if (!isFetchSupported) {
        return false;
    }
    const isReadableStreamSupported = isFetchSupported && isFunction2(ReadableStream2);
    const encodeText =
        isFetchSupported &&
        (typeof TextEncoder2 === "function"
            ? /* @__PURE__ */ (
                  encoder => str =>
                      encoder.encode(str)
              )(new TextEncoder2())
            : async str => new Uint8Array(await new Request(str).arrayBuffer()));
    const supportsRequestStream =
        isRequestSupported &&
        isReadableStreamSupported &&
        test(() => {
            let duplexAccessed = false;
            const request2 = new Request(platform_default.origin, {
                body: new ReadableStream2(),
                method: "POST",
                get duplex() {
                    duplexAccessed = true;
                    return "half";
                }
            });
            const hasContentType = request2.headers.has("Content-Type");
            if (request2.body != null) {
                request2.body.cancel();
            }
            return duplexAccessed && !hasContentType;
        });
    const supportsResponseStream = isResponseSupported && isReadableStreamSupported && test(() => utils_default.isReadableStream(new Response("").body));
    const resolvers = {
        stream: supportsResponseStream && (res => res.body)
    };
    isFetchSupported &&
        (() => {
            ["text", "arrayBuffer", "blob", "formData", "stream"].forEach(type => {
                !resolvers[type] &&
                    (resolvers[type] = (res, config) => {
                        let method = res && res[type];
                        if (method) {
                            return method.call(res);
                        }
                        throw new AxiosError_default(`Response type '${type}' is not supported`, AxiosError_default.ERR_NOT_SUPPORT, config);
                    });
            });
        })();
    const getBodyLength = async body => {
        if (body == null) {
            return 0;
        }
        if (utils_default.isBlob(body)) {
            return body.size;
        }
        if (utils_default.isSpecCompliantForm(body)) {
            const _request = new Request(platform_default.origin, {
                method: "POST",
                body
            });
            return (await _request.arrayBuffer()).byteLength;
        }
        if (utils_default.isArrayBufferView(body) || utils_default.isArrayBuffer(body)) {
            return body.byteLength;
        }
        if (utils_default.isURLSearchParams(body)) {
            body = body + "";
        }
        if (utils_default.isString(body)) {
            return (await encodeText(body)).byteLength;
        }
    };
    const resolveBodyLength = async (headers, body) => {
        const length = utils_default.toFiniteNumber(headers.getContentLength());
        return length == null ? getBodyLength(body) : length;
    };
    return async config => {
        let { url: url2, method, data, signal, cancelToken, timeout, onDownloadProgress, onUploadProgress, responseType, headers, withCredentials = "same-origin", fetchOptions, maxContentLength, maxBodyLength } = resolveConfig_default(config);
        const hasMaxContentLength = utils_default.isNumber(maxContentLength) && maxContentLength > -1;
        const hasMaxBodyLength = utils_default.isNumber(maxBodyLength) && maxBodyLength > -1;
        const own2 = key => (utils_default.hasOwnProp(config, key) ? config[key] : void 0);
        let _fetch = envFetch || fetch;
        responseType = responseType ? (responseType + "").toLowerCase() : "text";
        let composedSignal = composeSignals_default([signal, cancelToken && cancelToken.toAbortSignal()], timeout);
        let request2 = null;
        const unsubscribe =
            composedSignal &&
            composedSignal.unsubscribe &&
            (() => {
                composedSignal.unsubscribe();
            });
        let requestContentLength;
        let pendingBodyError = null;
        const maxBodyLengthError = () => new AxiosError_default("Request body larger than maxBodyLength limit", AxiosError_default.ERR_BAD_REQUEST, config, request2);
        try {
            let auth = void 0;
            const configAuth = own2("auth");
            if (configAuth) {
                const username = utils_default.getSafeProp(configAuth, "username") || "";
                const password = utils_default.getSafeProp(configAuth, "password") || "";
                auth = {
                    username,
                    password
                };
            }
            if (maybeWithAuthCredentials(url2)) {
                const parsedURL = new URL(url2, platform_default.origin);
                if (!auth && (parsedURL.username || parsedURL.password)) {
                    const urlUsername = decodeURIComponentSafe2(parsedURL.username);
                    const urlPassword = decodeURIComponentSafe2(parsedURL.password);
                    auth = {
                        username: urlUsername,
                        password: urlPassword
                    };
                }
                if (parsedURL.username || parsedURL.password) {
                    parsedURL.username = "";
                    parsedURL.password = "";
                    url2 = parsedURL.href;
                }
            }
            if (auth) {
                headers.delete("authorization");
                headers.set("Authorization", "Basic " + btoa(encodeUTF82((auth.username || "") + ":" + (auth.password || ""))));
            }
            if (hasMaxContentLength && typeof url2 === "string" && url2.startsWith("data:")) {
                const estimated = estimateDataURLDecodedBytes(url2);
                if (estimated > maxContentLength) {
                    throw new AxiosError_default("maxContentLength size of " + maxContentLength + " exceeded", AxiosError_default.ERR_BAD_RESPONSE, config, request2);
                }
            }
            if (hasMaxBodyLength && method !== "get" && method !== "head") {
                const outboundLength = await getBodyLength(data);
                if (typeof outboundLength === "number" && isFinite(outboundLength)) {
                    requestContentLength = outboundLength;
                    if (outboundLength > maxBodyLength) {
                        throw maxBodyLengthError();
                    }
                }
            }
            const mustEnforceStreamBody = hasMaxBodyLength && (utils_default.isReadableStream(data) || utils_default.isStream(data));
            const trackRequestStream = (stream4, onProgress, flush) =>
                trackStream(
                    stream4,
                    DEFAULT_CHUNK_SIZE,
                    loadedBytes => {
                        if (hasMaxBodyLength && loadedBytes > maxBodyLength) {
                            throw (pendingBodyError = maxBodyLengthError());
                        }
                        onProgress && onProgress(loadedBytes);
                    },
                    flush
                );
            if (supportsRequestStream && method !== "get" && method !== "head" && (onUploadProgress || mustEnforceStreamBody)) {
                requestContentLength = requestContentLength == null ? await resolveBodyLength(headers, data) : requestContentLength;
                if (requestContentLength !== 0 || mustEnforceStreamBody) {
                    let _request = new Request(url2, {
                        method: "POST",
                        body: data,
                        duplex: "half"
                    });
                    let contentTypeHeader;
                    if (utils_default.isFormData(data) && (contentTypeHeader = _request.headers.get("content-type"))) {
                        headers.setContentType(contentTypeHeader);
                    }
                    if (_request.body) {
                        const [onProgress, flush] = (onUploadProgress && progressEventDecorator(requestContentLength, progressEventReducer(asyncDecorator(onUploadProgress)))) || [];
                        data = trackRequestStream(_request.body, onProgress, flush);
                    }
                }
            } else if (mustEnforceStreamBody && !isRequestSupported && isReadableStreamSupported && method !== "get" && method !== "head") {
                data = trackRequestStream(data);
            } else if (mustEnforceStreamBody && isRequestSupported && !supportsRequestStream && method !== "get" && method !== "head") {
                throw new AxiosError_default("Stream request bodies are not supported by the current fetch implementation", AxiosError_default.ERR_NOT_SUPPORT, config, request2);
            }
            if (!utils_default.isString(withCredentials)) {
                withCredentials = withCredentials ? "include" : "omit";
            }
            const isCredentialsSupported = isRequestSupported && "credentials" in Request.prototype;
            if (utils_default.isFormData(data)) {
                const contentType = headers.getContentType();
                if (contentType && /^multipart\/form-data/i.test(contentType) && !/boundary=/i.test(contentType)) {
                    headers.delete("content-type");
                }
            }
            headers.set("User-Agent", "axios/" + VERSION, false);
            const resolvedOptions = {
                ...fetchOptions,
                signal: composedSignal,
                method: method.toUpperCase(),
                headers: toByteStringHeaderObject(headers.normalize()),
                body: data,
                duplex: "half",
                credentials: isCredentialsSupported ? withCredentials : void 0
            };
            request2 = isRequestSupported && new Request(url2, resolvedOptions);
            let response = await (isRequestSupported ? _fetch(request2, fetchOptions) : _fetch(url2, resolvedOptions));
            const responseHeaders = AxiosHeaders_default.from(response.headers);
            if (hasMaxContentLength) {
                const declaredLength = utils_default.toFiniteNumber(responseHeaders.getContentLength());
                if (declaredLength != null && declaredLength > maxContentLength) {
                    throw new AxiosError_default("maxContentLength size of " + maxContentLength + " exceeded", AxiosError_default.ERR_BAD_RESPONSE, config, request2);
                }
            }
            const isStreamResponse = supportsResponseStream && (responseType === "stream" || responseType === "response");
            if (supportsResponseStream && response.body && (onDownloadProgress || hasMaxContentLength || (isStreamResponse && unsubscribe))) {
                const options = {};
                ["status", "statusText", "headers"].forEach(prop => {
                    options[prop] = response[prop];
                });
                const responseContentLength = utils_default.toFiniteNumber(responseHeaders.getContentLength());
                const [onProgress, flush] = (onDownloadProgress && progressEventDecorator(responseContentLength, progressEventReducer(asyncDecorator(onDownloadProgress), true))) || [];
                let bytesRead = 0;
                const onChunkProgress = loadedBytes => {
                    if (hasMaxContentLength) {
                        bytesRead = loadedBytes;
                        if (bytesRead > maxContentLength) {
                            throw new AxiosError_default("maxContentLength size of " + maxContentLength + " exceeded", AxiosError_default.ERR_BAD_RESPONSE, config, request2);
                        }
                    }
                    onProgress && onProgress(loadedBytes);
                };
                response = new Response(
                    trackStream(response.body, DEFAULT_CHUNK_SIZE, onChunkProgress, () => {
                        flush && flush();
                        unsubscribe && unsubscribe();
                    }),
                    options
                );
            }
            responseType = responseType || "text";
            let responseData = await resolvers[utils_default.findKey(resolvers, responseType) || "text"](response, config);
            if (hasMaxContentLength && !supportsResponseStream && !isStreamResponse) {
                let materializedSize;
                if (responseData != null) {
                    if (typeof responseData.byteLength === "number") {
                        materializedSize = responseData.byteLength;
                    } else if (typeof responseData.size === "number") {
                        materializedSize = responseData.size;
                    } else if (typeof responseData === "string") {
                        materializedSize = typeof TextEncoder2 === "function" ? new TextEncoder2().encode(responseData).byteLength : responseData.length;
                    }
                }
                if (typeof materializedSize === "number" && materializedSize > maxContentLength) {
                    throw new AxiosError_default("maxContentLength size of " + maxContentLength + " exceeded", AxiosError_default.ERR_BAD_RESPONSE, config, request2);
                }
            }
            !isStreamResponse && unsubscribe && unsubscribe();
            return await new Promise((resolve, reject) => {
                settle(resolve, reject, {
                    data: responseData,
                    headers: AxiosHeaders_default.from(response.headers),
                    status: response.status,
                    statusText: response.statusText,
                    config,
                    request: request2
                });
            });
        } catch (err) {
            unsubscribe && unsubscribe();
            if (composedSignal && composedSignal.aborted && composedSignal.reason instanceof AxiosError_default) {
                const canceledError = composedSignal.reason;
                canceledError.config = config;
                request2 && (canceledError.request = request2);
                if (err !== canceledError) {
                    Object.defineProperty(canceledError, "cause", {
                        __proto__: null,
                        value: err,
                        writable: true,
                        enumerable: false,
                        configurable: true
                    });
                }
                throw canceledError;
            }
            if (pendingBodyError) {
                request2 && !pendingBodyError.request && (pendingBodyError.request = request2);
                throw pendingBodyError;
            }
            if (err instanceof AxiosError_default) {
                request2 && !err.request && (err.request = request2);
                throw err;
            }
            if (err && err.name === "TypeError" && /Load failed|fetch/i.test(err.message)) {
                const networkError = new AxiosError_default("Network Error", AxiosError_default.ERR_NETWORK, config, request2, err && err.response);
                Object.defineProperty(networkError, "cause", {
                    __proto__: null,
                    value: err.cause || err,
                    writable: true,
                    enumerable: false,
                    configurable: true
                });
                throw networkError;
            }
            throw AxiosError_default.from(err, err && err.code, config, request2, err && err.response);
        }
    };
};
var seedCache = /* @__PURE__ */ new Map();
var getFetch = config => {
    let env = (config && config.env) || {};
    const { fetch: fetch2, Request, Response } = env;
    const seeds = [Request, Response, fetch2];
    let len = seeds.length,
        i = len,
        seed,
        target,
        map = seedCache;
    while (i--) {
        seed = seeds[i];
        target = map.get(seed);
        target === void 0 && map.set(seed, (target = i ? /* @__PURE__ */ new Map() : factory(env)));
        map = target;
    }
    return target;
};
var adapter = getFetch();

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/adapters/adapters.js
var knownAdapters = {
    http: http_default,
    xhr: xhr_default,
    fetch: {
        get: getFetch
    }
};
utils_default.forEach(knownAdapters, (fn, value) => {
    if (fn) {
        try {
            Object.defineProperty(fn, "name", { __proto__: null, value });
        } catch (e) {}
        Object.defineProperty(fn, "adapterName", { __proto__: null, value });
    }
});
var renderReason = reason => `- ${reason}`;
var isResolvedHandle = adapter2 => utils_default.isFunction(adapter2) || adapter2 === null || adapter2 === false;
function getAdapter(adapters, config) {
    adapters = utils_default.isArray(adapters) ? adapters : [adapters];
    const { length } = adapters;
    let nameOrAdapter;
    let adapter2;
    const rejectedReasons = {};
    for (let i = 0; i < length; i++) {
        nameOrAdapter = adapters[i];
        let id;
        adapter2 = nameOrAdapter;
        if (!isResolvedHandle(nameOrAdapter)) {
            adapter2 = knownAdapters[(id = String(nameOrAdapter)).toLowerCase()];
            if (adapter2 === void 0) {
                throw new AxiosError_default(`Unknown adapter '${id}'`);
            }
        }
        if (adapter2 && (utils_default.isFunction(adapter2) || (adapter2 = adapter2.get(config)))) {
            break;
        }
        rejectedReasons[id || "#" + i] = adapter2;
    }
    if (!adapter2) {
        const reasons = Object.entries(rejectedReasons).map(([id, state]) => `adapter ${id} ` + (state === false ? "is not supported by the environment" : "is not available in the build"));
        let s = length ? (reasons.length > 1 ? "since :\n" + reasons.map(renderReason).join("\n") : " " + renderReason(reasons[0])) : "as no adapter specified";
        throw new AxiosError_default(`There is no suitable adapter to dispatch the request ` + s, AxiosError_default.ERR_NOT_SUPPORT);
    }
    return adapter2;
}
var adapters_default = {
    /**
     * Resolve an adapter from a list of adapter names or functions.
     * @type {Function}
     */
    getAdapter,
    /**
     * Exposes all known adapters
     * @type {Object<string, Function|Object>}
     */
    adapters: knownAdapters
};

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/core/dispatchRequest.js
function throwIfCancellationRequested(config) {
    if (config.cancelToken) {
        config.cancelToken.throwIfRequested();
    }
    if (config.signal && config.signal.aborted) {
        throw new CanceledError_default(null, config);
    }
}
function dispatchRequest(config) {
    throwIfCancellationRequested(config);
    config.headers = AxiosHeaders_default.from(config.headers);
    config.data = transformData.call(config, config.transformRequest);
    if (["post", "put", "patch"].indexOf(config.method) !== -1) {
        config.headers.setContentType("application/x-www-form-urlencoded", false);
    }
    const adapter2 = adapters_default.getAdapter(config.adapter || defaults_default.adapter, config);
    return adapter2(config).then(
        function onAdapterResolution(response) {
            throwIfCancellationRequested(config);
            config.response = response;
            try {
                response.data = transformData.call(config, config.transformResponse, response);
            } finally {
                delete config.response;
            }
            response.headers = AxiosHeaders_default.from(response.headers);
            return response;
        },
        function onAdapterRejection(reason) {
            if (!isCancel(reason)) {
                throwIfCancellationRequested(config);
                if (reason && reason.response) {
                    config.response = reason.response;
                    try {
                        reason.response.data = transformData.call(config, config.transformResponse, reason.response);
                    } finally {
                        delete config.response;
                    }
                    reason.response.headers = AxiosHeaders_default.from(reason.response.headers);
                }
            }
            return Promise.reject(reason);
        }
    );
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/validator.js
var validators = {};
["object", "boolean", "number", "function", "string", "symbol"].forEach((type, i) => {
    validators[type] = function validator(thing) {
        return typeof thing === type || "a" + (i < 1 ? "n " : " ") + type;
    };
});
var deprecatedWarnings = {};
validators.transitional = function transitional(validator, version, message) {
    function formatMessage(opt, desc) {
        return "[Axios v" + VERSION + "] Transitional option '" + opt + "'" + desc + (message ? ". " + message : "");
    }
    return (value, opt, opts) => {
        if (validator === false) {
            throw new AxiosError_default(formatMessage(opt, " has been removed" + (version ? " in " + version : "")), AxiosError_default.ERR_DEPRECATED);
        }
        if (version && !deprecatedWarnings[opt]) {
            deprecatedWarnings[opt] = true;
            console.warn(formatMessage(opt, " has been deprecated since v" + version + " and will be removed in the near future"));
        }
        return validator ? validator(value, opt, opts) : true;
    };
};
validators.spelling = function spelling(correctSpelling) {
    return (value, opt) => {
        console.warn(`${opt} is likely a misspelling of ${correctSpelling}`);
        return true;
    };
};
function assertOptions(options, schema, allowUnknown) {
    if (typeof options !== "object" || options === null) {
        throw new AxiosError_default("options must be an object", AxiosError_default.ERR_BAD_OPTION_VALUE);
    }
    const keys = Object.keys(options);
    let i = keys.length;
    while (i-- > 0) {
        const opt = keys[i];
        const validator = Object.prototype.hasOwnProperty.call(schema, opt) ? schema[opt] : void 0;
        if (validator) {
            const value = options[opt];
            const result = value === void 0 || validator(value, opt, options);
            if (result !== true) {
                throw new AxiosError_default("option " + opt + " must be " + result, AxiosError_default.ERR_BAD_OPTION_VALUE);
            }
            continue;
        }
        if (allowUnknown !== true) {
            throw new AxiosError_default("Unknown option " + opt, AxiosError_default.ERR_BAD_OPTION);
        }
    }
}
var validator_default = {
    assertOptions,
    validators
};

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/core/Axios.js
var validators2 = validator_default.validators;
var Axios = class {
    constructor(instanceConfig) {
        this.defaults = instanceConfig || {};
        this.interceptors = {
            request: new InterceptorManager_default(),
            response: new InterceptorManager_default()
        };
    }
    /**
     * Dispatch a request
     *
     * @param {String|Object} configOrUrl The config specific for this request (merged with this.defaults)
     * @param {?Object} config
     *
     * @returns {Promise} The Promise to be fulfilled
     */
    async request(configOrUrl, config) {
        try {
            return await this._request(configOrUrl, config);
        } catch (err) {
            if (err instanceof Error) {
                let dummy = {};
                Error.captureStackTrace ? Error.captureStackTrace(dummy) : (dummy = new Error());
                const stack = (() => {
                    if (!dummy.stack) {
                        return "";
                    }
                    const firstNewlineIndex = dummy.stack.indexOf("\n");
                    return firstNewlineIndex === -1 ? "" : dummy.stack.slice(firstNewlineIndex + 1);
                })();
                try {
                    if (!err.stack) {
                        err.stack = stack;
                    } else if (stack) {
                        const firstNewlineIndex = stack.indexOf("\n");
                        const secondNewlineIndex = firstNewlineIndex === -1 ? -1 : stack.indexOf("\n", firstNewlineIndex + 1);
                        const stackWithoutTwoTopLines = secondNewlineIndex === -1 ? "" : stack.slice(secondNewlineIndex + 1);
                        if (!String(err.stack).endsWith(stackWithoutTwoTopLines)) {
                            err.stack += "\n" + stack;
                        }
                    }
                } catch (e) {}
            }
            throw err;
        }
    }
    _request(configOrUrl, config) {
        if (typeof configOrUrl === "string") {
            config = config || {};
            config.url = configOrUrl;
        } else {
            config = configOrUrl || {};
        }
        config = mergeConfig(this.defaults, config);
        const { transitional: transitional2, paramsSerializer, headers } = config;
        if (transitional2 !== void 0) {
            validator_default.assertOptions(
                transitional2,
                {
                    silentJSONParsing: validators2.transitional(validators2.boolean),
                    forcedJSONParsing: validators2.transitional(validators2.boolean),
                    clarifyTimeoutError: validators2.transitional(validators2.boolean),
                    legacyInterceptorReqResOrdering: validators2.transitional(validators2.boolean),
                    advertiseZstdAcceptEncoding: validators2.transitional(validators2.boolean),
                    validateStatusUndefinedResolves: validators2.transitional(validators2.boolean)
                },
                false
            );
        }
        if (paramsSerializer != null) {
            if (utils_default.isFunction(paramsSerializer)) {
                config.paramsSerializer = {
                    serialize: paramsSerializer
                };
            } else {
                validator_default.assertOptions(
                    paramsSerializer,
                    {
                        encode: validators2.function,
                        serialize: validators2.function
                    },
                    true
                );
            }
        }
        if (config.allowAbsoluteUrls !== void 0) {
        } else if (this.defaults.allowAbsoluteUrls !== void 0) {
            config.allowAbsoluteUrls = this.defaults.allowAbsoluteUrls;
        } else {
            config.allowAbsoluteUrls = true;
        }
        validator_default.assertOptions(
            config,
            {
                baseUrl: validators2.spelling("baseURL"),
                withXsrfToken: validators2.spelling("withXSRFToken")
            },
            true
        );
        config.method = (config.method || this.defaults.method || "get").toLowerCase();
        let contextHeaders = headers && utils_default.merge(headers.common, headers[config.method]);
        headers &&
            utils_default.forEach(["delete", "get", "head", "post", "put", "patch", "query", "common"], method => {
                delete headers[method];
            });
        config.headers = AxiosHeaders_default.concat(contextHeaders, headers);
        const requestInterceptorChain = [];
        let synchronousRequestInterceptors = true;
        this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {
            if (typeof interceptor.runWhen === "function" && interceptor.runWhen(config) === false) {
                return;
            }
            synchronousRequestInterceptors = synchronousRequestInterceptors && interceptor.synchronous;
            const transitional3 = config.transitional || transitional_default;
            const legacyInterceptorReqResOrdering = transitional3 && transitional3.legacyInterceptorReqResOrdering;
            if (legacyInterceptorReqResOrdering) {
                requestInterceptorChain.unshift(interceptor.fulfilled, interceptor.rejected);
            } else {
                requestInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
            }
        });
        const responseInterceptorChain = [];
        this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {
            responseInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
        });
        let promise;
        let i = 0;
        let len;
        if (!synchronousRequestInterceptors) {
            const chain = [dispatchRequest.bind(this), void 0];
            chain.unshift(...requestInterceptorChain);
            chain.push(...responseInterceptorChain);
            len = chain.length;
            promise = Promise.resolve(config);
            while (i < len) {
                promise = promise.then(chain[i++], chain[i++]);
            }
            return promise;
        }
        len = requestInterceptorChain.length;
        let newConfig = config;
        while (i < len) {
            const onFulfilled = requestInterceptorChain[i++];
            const onRejected = requestInterceptorChain[i++];
            try {
                newConfig = onFulfilled(newConfig);
            } catch (error) {
                onRejected.call(this, error);
                break;
            }
        }
        try {
            promise = dispatchRequest.call(this, newConfig);
        } catch (error) {
            return Promise.reject(error);
        }
        i = 0;
        len = responseInterceptorChain.length;
        while (i < len) {
            promise = promise.then(responseInterceptorChain[i++], responseInterceptorChain[i++]);
        }
        return promise;
    }
    getUri(config) {
        config = mergeConfig(this.defaults, config);
        const fullPath = buildFullPath(config.baseURL, config.url, config.allowAbsoluteUrls, config);
        return buildURL(fullPath, config.params, config.paramsSerializer);
    }
};
utils_default.forEach(["delete", "get", "head", "options"], function forEachMethodNoData(method) {
    Axios.prototype[method] = function (url2, config) {
        return this.request(
            mergeConfig(config || {}, {
                method,
                url: url2,
                data: config && utils_default.hasOwnProp(config, "data") ? config.data : void 0
            })
        );
    };
});
utils_default.forEach(["post", "put", "patch", "query"], function forEachMethodWithData(method) {
    function generateHTTPMethod(isForm) {
        return function httpMethod(url2, data, config) {
            return this.request(
                mergeConfig(config || {}, {
                    method,
                    headers: isForm
                        ? {
                              "Content-Type": "multipart/form-data"
                          }
                        : {},
                    url: url2,
                    data
                })
            );
        };
    }
    Axios.prototype[method] = generateHTTPMethod();
    if (method !== "query") {
        Axios.prototype[method + "Form"] = generateHTTPMethod(true);
    }
});
var Axios_default = Axios;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/cancel/CancelToken.js
var CancelToken = class _CancelToken {
    constructor(executor) {
        if (typeof executor !== "function") {
            throw new TypeError("executor must be a function.");
        }
        let resolvePromise;
        this.promise = new Promise(function promiseExecutor(resolve) {
            resolvePromise = resolve;
        });
        const token = this;
        this.promise.then(cancel => {
            if (!token._listeners) return;
            let i = token._listeners.length;
            while (i-- > 0) {
                token._listeners[i](cancel);
            }
            token._listeners = null;
        });
        this.promise.then = onfulfilled => {
            let _resolve;
            const promise = new Promise(resolve => {
                token.subscribe(resolve);
                _resolve = resolve;
            }).then(onfulfilled);
            promise.cancel = function reject() {
                token.unsubscribe(_resolve);
            };
            return promise;
        };
        executor(function cancel(message, config, request2) {
            if (token.reason) {
                return;
            }
            token.reason = new CanceledError_default(message, config, request2);
            resolvePromise(token.reason);
        });
    }
    /**
     * Throws a `CanceledError` if cancellation has been requested.
     */
    throwIfRequested() {
        if (this.reason) {
            throw this.reason;
        }
    }
    /**
     * Subscribe to the cancel signal
     */
    subscribe(listener) {
        if (this.reason) {
            listener(this.reason);
            return;
        }
        if (this._listeners) {
            this._listeners.push(listener);
        } else {
            this._listeners = [listener];
        }
    }
    /**
     * Unsubscribe from the cancel signal
     */
    unsubscribe(listener) {
        if (!this._listeners) {
            return;
        }
        const index2 = this._listeners.indexOf(listener);
        if (index2 !== -1) {
            this._listeners.splice(index2, 1);
        }
    }
    toAbortSignal() {
        const controller = new AbortController();
        const abort = err => {
            controller.abort(err);
        };
        this.subscribe(abort);
        controller.signal.unsubscribe = () => this.unsubscribe(abort);
        return controller.signal;
    }
    /**
     * Returns an object that contains a new `CancelToken` and a function that, when called,
     * cancels the `CancelToken`.
     */
    static source() {
        let cancel;
        const token = new _CancelToken(function executor(c) {
            cancel = c;
        });
        return {
            token,
            cancel
        };
    }
};
var CancelToken_default = CancelToken;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/spread.js
function spread(callback) {
    return function wrap2(arr) {
        return callback.apply(null, arr);
    };
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/isAxiosError.js
function isAxiosError(payload) {
    return utils_default.isObject(payload) && payload.isAxiosError === true;
}

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/helpers/HttpStatusCode.js
var HttpStatusCode = {
    Continue: 100,
    SwitchingProtocols: 101,
    Processing: 102,
    EarlyHints: 103,
    Ok: 200,
    Created: 201,
    Accepted: 202,
    NonAuthoritativeInformation: 203,
    NoContent: 204,
    ResetContent: 205,
    PartialContent: 206,
    MultiStatus: 207,
    AlreadyReported: 208,
    ImUsed: 226,
    MultipleChoices: 300,
    MovedPermanently: 301,
    Found: 302,
    SeeOther: 303,
    NotModified: 304,
    UseProxy: 305,
    Unused: 306,
    TemporaryRedirect: 307,
    PermanentRedirect: 308,
    BadRequest: 400,
    Unauthorized: 401,
    PaymentRequired: 402,
    Forbidden: 403,
    NotFound: 404,
    MethodNotAllowed: 405,
    NotAcceptable: 406,
    ProxyAuthenticationRequired: 407,
    RequestTimeout: 408,
    Conflict: 409,
    Gone: 410,
    LengthRequired: 411,
    PreconditionFailed: 412,
    PayloadTooLarge: 413,
    UriTooLong: 414,
    UnsupportedMediaType: 415,
    RangeNotSatisfiable: 416,
    ExpectationFailed: 417,
    ImATeapot: 418,
    MisdirectedRequest: 421,
    UnprocessableEntity: 422,
    Locked: 423,
    FailedDependency: 424,
    TooEarly: 425,
    UpgradeRequired: 426,
    PreconditionRequired: 428,
    TooManyRequests: 429,
    RequestHeaderFieldsTooLarge: 431,
    UnavailableForLegalReasons: 451,
    InternalServerError: 500,
    NotImplemented: 501,
    BadGateway: 502,
    ServiceUnavailable: 503,
    GatewayTimeout: 504,
    HttpVersionNotSupported: 505,
    VariantAlsoNegotiates: 506,
    InsufficientStorage: 507,
    LoopDetected: 508,
    NotExtended: 510,
    NetworkAuthenticationRequired: 511,
    WebServerIsDown: 521,
    ConnectionTimedOut: 522,
    OriginIsUnreachable: 523,
    TimeoutOccurred: 524,
    SslHandshakeFailed: 525,
    InvalidSslCertificate: 526
};
Object.entries(HttpStatusCode).forEach(([key, value]) => {
    HttpStatusCode[value] = key;
});
var HttpStatusCode_default = HttpStatusCode;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/lib/axios.js
function createInstance(defaultConfig) {
    const context = new Axios_default(defaultConfig);
    const instance = bind(Axios_default.prototype.request, context);
    utils_default.extend(instance, Axios_default.prototype, context, { allOwnKeys: true });
    utils_default.extend(instance, context, null, { allOwnKeys: true });
    instance.create = function create45(instanceConfig) {
        return createInstance(mergeConfig(defaultConfig, instanceConfig));
    };
    return instance;
}
var axios = createInstance(defaults_default);
axios.Axios = Axios_default;
axios.CanceledError = CanceledError_default;
axios.CancelToken = CancelToken_default;
axios.isCancel = isCancel;
axios.VERSION = VERSION;
axios.toFormData = toFormData_default;
axios.AxiosError = AxiosError_default;
axios.Cancel = axios.CanceledError;
axios.all = function all(promises) {
    return Promise.all(promises);
};
axios.spread = spread;
axios.isAxiosError = isAxiosError;
axios.mergeConfig = mergeConfig;
axios.AxiosHeaders = AxiosHeaders_default;
axios.formToJSON = thing => formDataToJSON_default(utils_default.isHTMLForm(thing) ? new FormData(thing) : thing);
axios.getAdapter = adapters_default.getAdapter;
axios.HttpStatusCode = HttpStatusCode_default;
axios.default = axios;
var axios_default = axios;

// node_modules/.pnpm/axios@1.18.1/node_modules/axios/index.js
var {
    Axios: Axios2,
    AxiosError: AxiosError2,
    CanceledError: CanceledError2,
    isCancel: isCancel2,
    CancelToken: CancelToken2,
    VERSION: VERSION2,
    all: all2,
    Cancel,
    isAxiosError: isAxiosError2,
    spread: spread2,
    toFormData: toFormData2,
    AxiosHeaders: AxiosHeaders2,
    HttpStatusCode: HttpStatusCode2,
    formToJSON,
    getAdapter: getAdapter2,
    mergeConfig: mergeConfig2,
    create
} = axios_default;

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/rest-adapter.mjs
var import_contentful_sdk_core5 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/fast-copy@3.0.2/node_modules/fast-copy/dist/esm/index.mjs
var toStringFunction = Function.prototype.toString;
var create2 = Object.create;
var toStringObject = Object.prototype.toString;
var LegacyCache =
    /** @class */
    (function () {
        function LegacyCache2() {
            this._keys = [];
            this._values = [];
        }
        LegacyCache2.prototype.has = function (key) {
            return !!~this._keys.indexOf(key);
        };
        LegacyCache2.prototype.get = function (key) {
            return this._values[this._keys.indexOf(key)];
        };
        LegacyCache2.prototype.set = function (key, value) {
            this._keys.push(key);
            this._values.push(value);
        };
        return LegacyCache2;
    })();
function createCacheLegacy() {
    return new LegacyCache();
}
function createCacheModern() {
    return /* @__PURE__ */ new WeakMap();
}
var createCache = typeof WeakMap !== "undefined" ? createCacheModern : createCacheLegacy;
function getCleanClone(prototype2) {
    if (!prototype2) {
        return create2(null);
    }
    var Constructor = prototype2.constructor;
    if (Constructor === Object) {
        return prototype2 === Object.prototype ? {} : create2(prototype2);
    }
    if (Constructor && ~toStringFunction.call(Constructor).indexOf("[native code]")) {
        try {
            return new Constructor();
        } catch (_a2) {}
    }
    return create2(prototype2);
}
function getRegExpFlagsLegacy(regExp) {
    var flags = "";
    if (regExp.global) {
        flags += "g";
    }
    if (regExp.ignoreCase) {
        flags += "i";
    }
    if (regExp.multiline) {
        flags += "m";
    }
    if (regExp.unicode) {
        flags += "u";
    }
    if (regExp.sticky) {
        flags += "y";
    }
    return flags;
}
function getRegExpFlagsModern(regExp) {
    return regExp.flags;
}
var getRegExpFlags = /test/g.flags === "g" ? getRegExpFlagsModern : getRegExpFlagsLegacy;
function getTagLegacy(value) {
    var type = toStringObject.call(value);
    return type.substring(8, type.length - 1);
}
function getTagModern(value) {
    return value[Symbol.toStringTag] || getTagLegacy(value);
}
var getTag = typeof Symbol !== "undefined" ? getTagModern : getTagLegacy;
var defineProperty = Object.defineProperty;
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
var getOwnPropertyNames = Object.getOwnPropertyNames;
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var _a = Object.prototype;
var hasOwnProperty2 = _a.hasOwnProperty;
var propertyIsEnumerable2 = _a.propertyIsEnumerable;
var SUPPORTS_SYMBOL = typeof getOwnPropertySymbols === "function";
function getStrictPropertiesModern(object) {
    return getOwnPropertyNames(object).concat(getOwnPropertySymbols(object));
}
var getStrictProperties = SUPPORTS_SYMBOL ? getStrictPropertiesModern : getOwnPropertyNames;
function copyOwnPropertiesStrict(value, clone, state) {
    var properties = getStrictProperties(value);
    for (var index2 = 0, length_1 = properties.length, property = void 0, descriptor = void 0; index2 < length_1; ++index2) {
        property = properties[index2];
        if (property === "callee" || property === "caller") {
            continue;
        }
        descriptor = getOwnPropertyDescriptor(value, property);
        if (!descriptor) {
            clone[property] = state.copier(value[property], state);
            continue;
        }
        if (!descriptor.get && !descriptor.set) {
            descriptor.value = state.copier(descriptor.value, state);
        }
        try {
            defineProperty(clone, property, descriptor);
        } catch (error) {
            clone[property] = descriptor.value;
        }
    }
    return clone;
}
function copyArrayLoose(array, state) {
    var clone = new state.Constructor();
    state.cache.set(array, clone);
    for (var index2 = 0, length_2 = array.length; index2 < length_2; ++index2) {
        clone[index2] = state.copier(array[index2], state);
    }
    return clone;
}
function copyArrayStrict(array, state) {
    var clone = new state.Constructor();
    state.cache.set(array, clone);
    return copyOwnPropertiesStrict(array, clone, state);
}
function copyArrayBuffer(arrayBuffer, _state) {
    return arrayBuffer.slice(0);
}
function copyBlob(blob, _state) {
    return blob.slice(0, blob.size, blob.type);
}
function copyDataView(dataView, state) {
    return new state.Constructor(copyArrayBuffer(dataView.buffer));
}
function copyDate(date, state) {
    return new state.Constructor(date.getTime());
}
function copyMapLoose(map, state) {
    var clone = new state.Constructor();
    state.cache.set(map, clone);
    map.forEach(function (value, key) {
        clone.set(key, state.copier(value, state));
    });
    return clone;
}
function copyMapStrict(map, state) {
    return copyOwnPropertiesStrict(map, copyMapLoose(map, state), state);
}
function copyObjectLooseLegacy(object, state) {
    var clone = getCleanClone(state.prototype);
    state.cache.set(object, clone);
    for (var key in object) {
        if (hasOwnProperty2.call(object, key)) {
            clone[key] = state.copier(object[key], state);
        }
    }
    return clone;
}
function copyObjectLooseModern(object, state) {
    var clone = getCleanClone(state.prototype);
    state.cache.set(object, clone);
    for (var key in object) {
        if (hasOwnProperty2.call(object, key)) {
            clone[key] = state.copier(object[key], state);
        }
    }
    var symbols = getOwnPropertySymbols(object);
    for (var index2 = 0, length_3 = symbols.length, symbol = void 0; index2 < length_3; ++index2) {
        symbol = symbols[index2];
        if (propertyIsEnumerable2.call(object, symbol)) {
            clone[symbol] = state.copier(object[symbol], state);
        }
    }
    return clone;
}
var copyObjectLoose = SUPPORTS_SYMBOL ? copyObjectLooseModern : copyObjectLooseLegacy;
function copyObjectStrict(object, state) {
    var clone = getCleanClone(state.prototype);
    state.cache.set(object, clone);
    return copyOwnPropertiesStrict(object, clone, state);
}
function copyPrimitiveWrapper(primitiveObject, state) {
    return new state.Constructor(primitiveObject.valueOf());
}
function copyRegExp(regExp, state) {
    var clone = new state.Constructor(regExp.source, getRegExpFlags(regExp));
    clone.lastIndex = regExp.lastIndex;
    return clone;
}
function copySelf(value, _state) {
    return value;
}
function copySetLoose(set, state) {
    var clone = new state.Constructor();
    state.cache.set(set, clone);
    set.forEach(function (value) {
        clone.add(state.copier(value, state));
    });
    return clone;
}
function copySetStrict(set, state) {
    return copyOwnPropertiesStrict(set, copySetLoose(set, state), state);
}
var isArray2 = Array.isArray;
var assign = Object.assign;
var getPrototypeOf2 =
    Object.getPrototypeOf ||
    function (obj) {
        return obj.__proto__;
    };
var DEFAULT_LOOSE_OPTIONS = {
    array: copyArrayLoose,
    arrayBuffer: copyArrayBuffer,
    blob: copyBlob,
    dataView: copyDataView,
    date: copyDate,
    error: copySelf,
    map: copyMapLoose,
    object: copyObjectLoose,
    regExp: copyRegExp,
    set: copySetLoose
};
var DEFAULT_STRICT_OPTIONS = assign({}, DEFAULT_LOOSE_OPTIONS, {
    array: copyArrayStrict,
    map: copyMapStrict,
    object: copyObjectStrict,
    set: copySetStrict
});
function getTagSpecificCopiers(options) {
    return {
        Arguments: options.object,
        Array: options.array,
        ArrayBuffer: options.arrayBuffer,
        Blob: options.blob,
        Boolean: copyPrimitiveWrapper,
        DataView: options.dataView,
        Date: options.date,
        Error: options.error,
        Float32Array: options.arrayBuffer,
        Float64Array: options.arrayBuffer,
        Int8Array: options.arrayBuffer,
        Int16Array: options.arrayBuffer,
        Int32Array: options.arrayBuffer,
        Map: options.map,
        Number: copyPrimitiveWrapper,
        Object: options.object,
        Promise: copySelf,
        RegExp: options.regExp,
        Set: options.set,
        String: copyPrimitiveWrapper,
        WeakMap: copySelf,
        WeakSet: copySelf,
        Uint8Array: options.arrayBuffer,
        Uint8ClampedArray: options.arrayBuffer,
        Uint16Array: options.arrayBuffer,
        Uint32Array: options.arrayBuffer,
        Uint64Array: options.arrayBuffer
    };
}
function createCopier(options) {
    var normalizedOptions = assign({}, DEFAULT_LOOSE_OPTIONS, options);
    var tagSpecificCopiers = getTagSpecificCopiers(normalizedOptions);
    var array = tagSpecificCopiers.Array,
        object = tagSpecificCopiers.Object;
    function copier(value, state) {
        state.prototype = state.Constructor = void 0;
        if (!value || typeof value !== "object") {
            return value;
        }
        if (state.cache.has(value)) {
            return state.cache.get(value);
        }
        state.prototype = getPrototypeOf2(value);
        state.Constructor = state.prototype && state.prototype.constructor;
        if (!state.Constructor || state.Constructor === Object) {
            return object(value, state);
        }
        if (isArray2(value)) {
            return array(value, state);
        }
        var tagSpecificCopier = tagSpecificCopiers[getTag(value)];
        if (tagSpecificCopier) {
            return tagSpecificCopier(value, state);
        }
        return typeof value.then === "function" ? value : object(value, state);
    }
    return function copy(value) {
        return copier(value, {
            Constructor: void 0,
            cache: createCache(),
            copier,
            prototype: void 0
        });
    };
}
function createStrictCopier(options) {
    return createCopier(assign({}, DEFAULT_STRICT_OPTIONS, options));
}
var copyStrict = createStrictCopier({});
var index = createCopier({});

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/ai-action.mjs
var ai_action_exports = {};
__export(ai_action_exports, {
    create: () => create3,
    del: () => del2,
    get: () => get2,
    getMany: () => getMany,
    invoke: () => invoke,
    publish: () => publish,
    unpublish: () => unpublish,
    update: () => update
});

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/raw.mjs
var import_contentful_sdk_core = __toESM(require_cjs2(), 1);
function getBaseUrl(http4) {
    return http4.defaults.baseURL?.split("/spaces")[0];
}
function get(http4, url2, config) {
    return http4
        .get(url2, {
            baseURL: getBaseUrl(http4),
            ...config
        })
        .then(response => response.data, import_contentful_sdk_core.errorHandler);
}
function patch(http4, url2, payload, config) {
    return http4
        .patch(url2, payload, {
            baseURL: getBaseUrl(http4),
            ...config
        })
        .then(response => response.data, import_contentful_sdk_core.errorHandler);
}
function post(http4, url2, payload, config) {
    return http4
        .post(url2, payload, {
            baseURL: getBaseUrl(http4),
            ...config
        })
        .then(response => response.data, import_contentful_sdk_core.errorHandler);
}
function put(http4, url2, payload, config) {
    return http4
        .put(url2, payload, {
            baseURL: getBaseUrl(http4),
            ...config
        })
        .then(response => response.data, import_contentful_sdk_core.errorHandler);
}
function del(http4, url2, config) {
    return http4
        .delete(url2, {
            baseURL: getBaseUrl(http4),
            ...config
        })
        .then(response => response.data, import_contentful_sdk_core.errorHandler);
}
function http3(http4, url2, config) {
    return http4(url2, {
        baseURL: getBaseUrl(http4),
        ...config
    }).then(response => response.data, import_contentful_sdk_core.errorHandler);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/ai-action.mjs
var get2 = (http4, params, headers) => {
    return get(http4, `/spaces/${params.spaceId}/ai/actions/${params.aiActionId}`, {
        headers
    });
};
var getMany = (http4, params, headers) => {
    return get(http4, `/spaces/${params.spaceId}/ai/actions`, {
        params: params.query,
        headers
    });
};
var create3 = (http4, params, data, headers) => {
    return post(http4, `/spaces/${params.spaceId}/ai/actions`, data, { headers });
};
var update = (http4, params, rawData, headers) => {
    const data = index(rawData);
    const { sys, ...payload } = data;
    return put(http4, `/spaces/${params.spaceId}/ai/actions/${params.aiActionId}`, payload, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del2 = (http4, params, headers) => {
    return del(http4, `/spaces/${params.spaceId}/ai/actions/${params.aiActionId}`, { headers });
};
var publish = (http4, params, rawData, headers) => {
    return put(http4, `/spaces/${params.spaceId}/ai/actions/${params.aiActionId}/published`, null, {
        headers: {
            "X-Contentful-Version": params.version,
            ...headers
        }
    });
};
var unpublish = (http4, params, headers) => {
    return del(http4, `/spaces/${params.spaceId}/ai/actions/${params.aiActionId}/published`, {
        headers
    });
};
var invoke = (http4, params, data, headers) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/actions/${params.aiActionId}/invoke`, data, { headers, params: params.query });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/ai-action-invocation.mjs
var ai_action_invocation_exports = {};
__export(ai_action_invocation_exports, {
    get: () => get3
});
var get3 = (http4, params, headers) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/actions/${params.aiActionId}/invocations/${params.invocationId}`, { headers });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/agent.mjs
var agent_exports = {};
__export(agent_exports, {
    generate: () => generate,
    get: () => get4,
    getMany: () => getMany2
});
var AgentAlphaHeaders = {
    "x-contentful-enable-alpha-feature": "agents-api"
};
var get4 = (http4, params, headers) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/agents/${params.agentId}`, {
        headers: {
            ...AgentAlphaHeaders,
            ...headers
        }
    });
};
var getMany2 = (http4, params, headers) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/agents`, {
        headers: {
            ...AgentAlphaHeaders,
            ...headers
        }
    });
};
var generate = (http4, params, data, headers) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/agents/${params.agentId}/generate`, data, {
        headers: {
            ...AgentAlphaHeaders,
            ...headers
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/agent-run.mjs
var agent_run_exports = {};
__export(agent_run_exports, {
    get: () => get5,
    getMany: () => getMany3,
    resumeRun: () => resumeRun
});
var AgentRunAlphaHeaders = {
    "x-contentful-enable-alpha-feature": "agents-api"
};
var get5 = (http4, params, headers) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/agents/runs/${params.runId}`, {
        headers: {
            ...AgentRunAlphaHeaders,
            ...headers
        }
    });
};
var getMany3 = (http4, params, headers) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/agents/runs`, {
        params: params.query,
        headers: {
            ...AgentRunAlphaHeaders,
            ...headers
        }
    });
};
var resumeRun = (http4, params, data, headers) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/agents/runs/${params.runId}/resume`, data, {
        headers: {
            ...AgentRunAlphaHeaders,
            ...headers
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/access-token.mjs
var access_token_exports = {};
__export(access_token_exports, {
    createPersonalAccessToken: () => createPersonalAccessToken,
    get: () => get6,
    getMany: () => getMany4,
    getManyForOrganization: () => getManyForOrganization,
    revoke: () => revoke
});
var get6 = (http4, params) => {
    return get(http4, `/users/me/access_tokens/${params.tokenId}`);
};
var getMany4 = (http4, params) => {
    return get(http4, "/users/me/access_tokens", {
        params: params.query
    });
};
var createPersonalAccessToken = (http4, _params, rawData, headers) => {
    return post(http4, "/users/me/access_tokens", rawData, {
        headers
    });
};
var revoke = (http4, params) => {
    return put(http4, `/users/me/access_tokens/${params.tokenId}/revoked`, null);
};
var getManyForOrganization = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/access_tokens`, {
        params: params.query
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/automation-definition.mjs
var automation_definition_exports = {};
__export(automation_definition_exports, {
    create: () => create4,
    del: () => del3,
    get: () => get7,
    getMany: () => getMany5,
    update: () => update2
});
var getBaseUrl2 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/automation_definitions`;
var getAutomationDefinitionUrl = params => `${getBaseUrl2(params)}/${params.automationDefinitionId}`;
var get7 = (http4, params, headers) =>
    get(http4, getAutomationDefinitionUrl(params), {
        headers
    });
var getMany5 = (http4, params, headers) =>
    get(http4, getBaseUrl2(params), {
        headers,
        params: params.query
    });
var create4 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return post(http4, getBaseUrl2(params), data, {
        headers
    });
};
var update2 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getAutomationDefinitionUrl(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del3 = (http4, params, headers) => {
    return del(http4, getAutomationDefinitionUrl(params), {
        headers: { "X-Contentful-Version": params.version, ...headers }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/automation-execution.mjs
var automation_execution_exports = {};
__export(automation_execution_exports, {
    get: () => get8,
    getForAutomationDefinition: () => getForAutomationDefinition,
    getMany: () => getMany6
});
var getBaseUrl3 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/automation_executions`;
var getAutomationExecutionUrl = params => `${getBaseUrl3(params)}/${params.automationExecutionId}`;
var getExecutionsByDefinitionUrl = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/automation_definitions/${params.automationDefinitionId}/automation_executions`;
var get8 = (http4, params, headers) =>
    get(http4, getAutomationExecutionUrl(params), {
        headers
    });
var getMany6 = (http4, params, headers) =>
    get(http4, getBaseUrl3(params), {
        headers,
        params: params.query
    });
var getForAutomationDefinition = (http4, params, headers) =>
    get(http4, getExecutionsByDefinitionUrl(params), {
        headers,
        params: params.query
    });

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/api-key.mjs
var api_key_exports = {};
__export(api_key_exports, {
    create: () => create5,
    createWithId: () => createWithId,
    del: () => del4,
    get: () => get9,
    getMany: () => getMany7,
    update: () => update3
});
var get9 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/api_keys/${params.apiKeyId}`);
};
var getMany7 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/api_keys`, {
        params: params.query
    });
};
var create5 = (http4, params, data, headers) => {
    return post(http4, `/spaces/${params.spaceId}/api_keys`, data, { headers });
};
var createWithId = (http4, params, data, headers) => {
    return put(http4, `/spaces/${params.spaceId}/api_keys/${params.apiKeyId}`, data, {
        headers
    });
};
var update3 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    if ("accessToken" in data) {
        delete data.accessToken;
    }
    if ("preview_api_key" in data) {
        delete data.preview_api_key;
    }
    if ("policies" in data) {
        delete data.policies;
    }
    delete data.sys;
    return put(http4, `/spaces/${params.spaceId}/api_keys/${params.apiKeyId}`, data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del4 = (http4, params) => {
    return del(http4, `/spaces/${params.spaceId}/api_keys/${params.apiKeyId}`);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-access-token.mjs
var app_access_token_exports = {};
__export(app_access_token_exports, {
    create: () => create6
});
var create6 = (http4, params, data) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/app_installations/${params.appDefinitionId}/access_tokens`, void 0, { headers: { Authorization: `Bearer ${data.jwt}` } });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-action.mjs
var app_action_exports = {};
__export(app_action_exports, {
    create: () => create7,
    del: () => del5,
    get: () => get10,
    getMany: () => getMany8,
    getManyForEnvironment: () => getManyForEnvironment,
    update: () => update4
});

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/utils.mjs
function normalizeSelect(query2) {
    if (query2 && query2.select && !/sys/i.test(query2.select)) {
        return {
            ...query2,
            select: query2.select + ",sys"
        };
    }
    return query2;
}
function normalizeSpaceId(query2) {
    if (query2 && query2.spaceId) {
        const { spaceId, ...rest } = query2;
        return {
            ...rest,
            "sys.space.sys.id[in]": spaceId
        };
    }
    return query2;
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-action.mjs
var getBaseUrl4 = params => `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/actions`;
var getAppActionUrl = params => `${getBaseUrl4(params)}/${params.appActionId}`;
var getAppActionsEnvUrl = params => {
    if (params.environmentId) {
        return `/spaces/${params.spaceId}/environments/${params.environmentId}/actions`;
    }
    return `/spaces/${params.spaceId}/actions`;
};
var get10 = (http4, params) => {
    return get(http4, getAppActionUrl(params));
};
var getMany8 = (http4, params) => {
    return get(http4, getBaseUrl4(params), {
        params: normalizeSelect(params.query)
    });
};
var getManyForEnvironment = (http4, params) => {
    return get(http4, getAppActionsEnvUrl(params), {
        params: normalizeSelect(params.query)
    });
};
var del5 = (http4, params) => {
    return del(http4, getAppActionUrl(params));
};
var create7 = (http4, params, data) => {
    return post(http4, getBaseUrl4(params), data);
};
var update4 = (http4, params, data) => {
    return put(http4, getAppActionUrl(params), data);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-action-call.mjs
var app_action_call_exports = {};
__export(app_action_call_exports, {
    create: () => create8,
    createWithResponse: () => createWithResponse,
    createWithResult: () => createWithResult,
    get: () => get11,
    getCallDetails: () => getCallDetails,
    getResponse: () => getResponse
});

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/common-utils.mjs
var import_contentful_sdk_core2 = __toESM(require_cjs2(), 1);
var wrapCollection =
    fn =>
    (makeRequest2, data, ...rest) => {
        const collectionData = (0, import_contentful_sdk_core2.toPlainObject)(index(data));
        collectionData.items = collectionData.items.map(entity => fn(makeRequest2, entity, ...rest));
        return collectionData;
    };
var wrapCursorPaginatedCollection =
    fn =>
    (makeRequest2, data, ...rest) => {
        const collectionData = (0, import_contentful_sdk_core2.toPlainObject)(index(data));
        collectionData.items = collectionData.items.map(entity => fn(makeRequest2, entity, ...rest));
        return collectionData;
    };
function isSuccessful(statusCode) {
    return statusCode < 300;
}
function shouldRePoll(statusCode) {
    return [404, 422, 429, 400].includes(statusCode);
}
async function waitFor(ms = 1e3) {
    return new Promise(resolve => setTimeout(resolve, ms));
}
function normalizeCursorPaginationParameters(query2) {
    const { pagePrev, pageNext, ...rest } = query2;
    return {
        ...rest,
        cursor: true,
        // omit pagePrev and pageNext if the value is falsy
        ...(pagePrev ? { pagePrev } : null),
        ...(pageNext ? { pageNext } : null)
    };
}
function extractQueryParam(key, url2) {
    if (!url2) return;
    const queryIndex = url2.indexOf("?");
    if (queryIndex === -1) return;
    const queryString = url2.slice(queryIndex + 1);
    return new URLSearchParams(queryString).get(key) ?? void 0;
}
var Pages = {
    prev: "pagePrev",
    next: "pageNext"
};
var PAGE_KEYS = ["prev", "next"];
function normalizeCursorPaginationResponse(data) {
    const pages = {};
    for (const key of PAGE_KEYS) {
        const token = extractQueryParam(Pages[key], data.pages?.[key]);
        if (token) pages[key] = token;
    }
    return {
        ...data,
        pages
    };
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-action-call.mjs
var create8 = (http4, params, data) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/app_installations/${params.appDefinitionId}/actions/${params.appActionId}/calls`, data);
};
var getCallDetails = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/actions/${params.appActionId}/calls/${params.callId}`);
};
var APP_ACTION_CALL_RETRY_INTERVAL = 2e3;
var APP_ACTION_CALL_RETRIES = 15;
async function callAppActionResult(http4, params, { callId }) {
    let checkCount = 1;
    const retryInterval = params.retryInterval || APP_ACTION_CALL_RETRY_INTERVAL;
    const retries = params.retries || APP_ACTION_CALL_RETRIES;
    return new Promise((resolve, reject) => {
        const poll = async () => {
            try {
                const result = await getCallDetails(http4, { ...params, callId });
                if (result?.response?.statusCode && !isSuccessful(result?.response?.statusCode)) {
                    const error = new Error("App action not found or lambda fails");
                    reject(error);
                } else if (isSuccessful(result.statusCode)) {
                    resolve(result);
                } else if (shouldRePoll(result.statusCode) && checkCount < retries) {
                    checkCount++;
                    await waitFor(retryInterval);
                    poll();
                } else {
                    const error = new Error("The app action response is taking longer than expected to process.");
                    reject(error);
                }
            } catch (error) {
                checkCount++;
                if (checkCount > retries) {
                    reject(new Error("The app action response is taking longer than expected to process."));
                    return;
                }
                await waitFor(retryInterval);
                poll();
            }
        };
        poll();
    });
}
var createWithResponse = async (http4, params, data) => {
    const createResponse = await post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/app_installations/${params.appDefinitionId}/actions/${params.appActionId}/calls`, data);
    const callId = createResponse.sys.id;
    return callAppActionResult(http4, params, { callId });
};
var get11 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/app_installations/${params.appDefinitionId}/actions/${params.appActionId}/calls/${params.callId}`);
};
var getResponse = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/app_installations/${params.appDefinitionId}/actions/${params.appActionId}/calls/${params.callId}/response`);
};
async function pollStructuredAppActionCall(http4, params, { callId }) {
    let checkCount = 1;
    const retryInterval = params.retryInterval || APP_ACTION_CALL_RETRY_INTERVAL;
    const retries = params.retries || APP_ACTION_CALL_RETRIES;
    return new Promise((resolve, reject) => {
        const poll = async () => {
            try {
                const result = await get11(http4, { ...params, callId });
                if (result?.sys.status === "succeeded" || result?.sys.status === "failed") {
                    resolve(result);
                } else if (result?.sys.status === "processing" && checkCount < retries) {
                    checkCount++;
                    await waitFor(retryInterval);
                    poll();
                } else {
                    reject(new Error("The app action result is taking longer than expected to process."));
                }
            } catch (error) {
                checkCount++;
                if (checkCount > retries) {
                    reject(new Error("The app action result is taking longer than expected to process."));
                    return;
                }
                await waitFor(retryInterval);
                poll();
            }
        };
        poll();
    });
}
var createWithResult = async (http4, params, data) => {
    const createResponse = await post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/app_installations/${params.appDefinitionId}/actions/${params.appActionId}/calls`, data);
    const callId = createResponse.sys.id;
    return pollStructuredAppActionCall(http4, params, { callId });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-bundle.mjs
var app_bundle_exports = {};
__export(app_bundle_exports, {
    create: () => create9,
    del: () => del6,
    get: () => get12,
    getMany: () => getMany9
});
var getBaseUrl5 = params => `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/app_bundles`;
var getAppBundleUrl = params => `${getBaseUrl5(params)}/${params.appBundleId}`;
var get12 = (http4, params) => {
    return get(http4, getAppBundleUrl(params));
};
var getMany9 = (http4, params) => {
    return get(http4, getBaseUrl5(params), {
        params: normalizeSelect(params.query)
    });
};
var del6 = (http4, params) => {
    return del(http4, getAppBundleUrl(params));
};
var create9 = (http4, params, payload) => {
    const { appUploadId, comment, actions, functions } = payload;
    const data = {
        upload: {
            sys: {
                type: "Link",
                linkType: "AppUpload",
                id: appUploadId
            }
        },
        comment,
        actions,
        functions
    };
    return post(http4, getBaseUrl5(params), data);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-definition.mjs
var app_definition_exports = {};
__export(app_definition_exports, {
    create: () => create10,
    del: () => del7,
    get: () => get13,
    getAppDefinitionUrl: () => getAppDefinitionUrl,
    getInstallationsForOrg: () => getInstallationsForOrg,
    getMany: () => getMany10,
    update: () => update5
});
var getBaseUrl6 = params => `/organizations/${params.organizationId}/app_definitions`;
var getAppDefinitionUrl = params => getBaseUrl6(params) + `/${params.appDefinitionId}`;
var getBaseUrlForOrgInstallations = params => `/app_definitions/${params.appDefinitionId}/app_installations`;
var get13 = (http4, params) => {
    return get(http4, getAppDefinitionUrl(params), {
        params: normalizeSelect(params.query)
    });
};
var getMany10 = (http4, params) => {
    return get(http4, getBaseUrl6(params), {
        params: params.query
    });
};
var create10 = (http4, params, rawData) => {
    const data = index(rawData);
    return post(http4, getBaseUrl6(params), data);
};
var update5 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getAppDefinitionUrl(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del7 = (http4, params) => {
    return del(http4, getAppDefinitionUrl(params));
};
var getInstallationsForOrg = (http4, params) => {
    return get(http4, getBaseUrlForOrgInstallations(params), {
        params: {
            ...normalizeSpaceId(normalizeSelect(params.query)),
            "sys.organization.sys.id[in]": params.organizationId
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-details.mjs
var app_details_exports = {};
__export(app_details_exports, {
    del: () => del8,
    get: () => get14,
    upsert: () => upsert
});
var get14 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/details`);
};
var upsert = (http4, params, data) => {
    return put(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/details`, data);
};
var del8 = (http4, params) => {
    return del(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/details`);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-event-subscription.mjs
var app_event_subscription_exports = {};
__export(app_event_subscription_exports, {
    del: () => del9,
    get: () => get15,
    upsert: () => upsert2
});
var get15 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/event_subscription`);
};
var upsert2 = (http4, params, data) => {
    return put(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/event_subscription`, data);
};
var del9 = (http4, params) => {
    return del(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/event_subscription`);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-installation.mjs
var app_installation_exports = {};
__export(app_installation_exports, {
    del: () => del10,
    get: () => get16,
    getAppInstallationUrl: () => getAppInstallationUrl,
    getForOrganization: () => getForOrganization,
    getMany: () => getMany11,
    upsert: () => upsert3
});
var getBaseUrl7 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/app_installations`;
var getBaseUrlForOrgInstallations2 = params => `/app_definitions/${params.appDefinitionId}/app_installations`;
var getAppInstallationUrl = params => getBaseUrl7(params) + `/${params.appDefinitionId}`;
var get16 = (http4, params) => {
    return get(http4, getAppInstallationUrl(params), {
        params: normalizeSelect(params.query)
    });
};
var getMany11 = (http4, params) => {
    return get(http4, getBaseUrl7(params), {
        params: normalizeSelect(params.query)
    });
};
var upsert3 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return put(http4, getAppInstallationUrl(params), data, {
        headers: {
            ...headers,
            ...(params.acceptAllTerms && {
                "X-Contentful-Marketplace": "i-accept-end-user-license-agreement,i-accept-marketplace-terms-of-service,i-accept-privacy-policy"
            })
        }
    });
};
var del10 = (http4, params) => {
    return del(http4, getAppInstallationUrl(params));
};
var getForOrganization = (http4, params) => {
    return get(http4, getBaseUrlForOrgInstallations2(params), {
        params: {
            ...normalizeSpaceId(normalizeSelect(params.query)),
            "sys.organization.sys.id[in]": params.organizationId
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-key.mjs
var app_key_exports = {};
__export(app_key_exports, {
    create: () => create11,
    del: () => del11,
    get: () => get17,
    getMany: () => getMany12
});
var get17 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/keys/${params.fingerprint}`);
};
var getMany12 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/keys`);
};
var create11 = (http4, params, data) => {
    return post(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/keys`, data);
};
var del11 = (http4, params) => {
    return del(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/keys/${params.fingerprint}`);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-signed-request.mjs
var app_signed_request_exports = {};
__export(app_signed_request_exports, {
    create: () => create12
});
var create12 = (http4, params, data) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/app_installations/${params.appDefinitionId}/signed_requests`, data);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-signing-secret.mjs
var app_signing_secret_exports = {};
__export(app_signing_secret_exports, {
    del: () => del12,
    get: () => get18,
    upsert: () => upsert4
});
var get18 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/signing_secret`);
};
var upsert4 = (http4, params, data) => {
    return put(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/signing_secret`, data);
};
var del12 = (http4, params) => {
    return del(http4, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/signing_secret`);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-upload.mjs
var app_upload_exports = {};
__export(app_upload_exports, {
    create: () => create13,
    del: () => del13,
    get: () => get19
});

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/upload-http-client.mjs
function getUploadHttpClient(http4, options) {
    const { hostUpload, defaultHostnameUpload, timeout } = http4.httpClientParams;
    const uploadHttp = http4.cloneWithNewParams({
        host: hostUpload || defaultHostnameUpload,
        // Using client presets, options or 5 minute default timeout
        timeout: timeout ?? options?.uploadTimeout ?? 3e5
    });
    return uploadHttp;
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/app-upload.mjs
var getBaseUrl8 = params => `/organizations/${params.organizationId}/app_uploads`;
var getAppUploadUrl = params => `${getBaseUrl8(params)}/${params.appUploadId}`;
var get19 = (http4, params) => {
    const httpUpload = getUploadHttpClient(http4);
    return get(httpUpload, getAppUploadUrl(params));
};
var del13 = (http4, params) => {
    const httpUpload = getUploadHttpClient(http4);
    return del(httpUpload, getAppUploadUrl(params));
};
var create13 = (http4, params, payload) => {
    const httpUpload = getUploadHttpClient(http4);
    const { file } = payload;
    return post(httpUpload, getBaseUrl8(params), file, {
        headers: {
            "Content-Type": "application/octet-stream"
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/asset.mjs
var asset_exports = {};
__export(asset_exports, {
    archive: () => archive,
    create: () => create16,
    createFromFiles: () => createFromFiles2,
    createWithId: () => createWithId3,
    del: () => del15,
    get: () => get22,
    getMany: () => getMany14,
    getManyWithCursor: () => getManyWithCursor,
    getPublished: () => getPublished,
    processForAllLocales: () => processForAllLocales2,
    processForLocale: () => processForLocale2,
    publish: () => publish2,
    unarchive: () => unarchive,
    unpublish: () => unpublish2,
    update: () => update7
});
var import_contentful_sdk_core4 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/upload.mjs
var upload_exports = {};
__export(upload_exports, {
    create: () => create14,
    del: () => del14,
    get: () => get20
});
var getBaseUploadUrl = params => {
    const spacePath = `/spaces/${params.spaceId}/uploads`;
    const environmentPath = `/spaces/${params.spaceId}/environments/${params.environmentId}/uploads`;
    const path = params.environmentId ? environmentPath : spacePath;
    return path;
};
var getEntityUploadUrl = params => {
    const path = getBaseUploadUrl(params);
    return path + `/${params.uploadId}`;
};
var create14 = (http4, params, data) => {
    const httpUpload = getUploadHttpClient(http4);
    const { file } = data;
    if (!file) {
        return Promise.reject(new Error("Unable to locate a file to upload."));
    }
    const path = getBaseUploadUrl(params);
    return post(httpUpload, path, file, {
        headers: {
            "Content-Type": "application/octet-stream"
        }
    });
};
var del14 = (http4, params) => {
    const httpUpload = getUploadHttpClient(http4);
    const path = getEntityUploadUrl(params);
    return del(httpUpload, path);
};
var get20 = (http4, params) => {
    const httpUpload = getUploadHttpClient(http4);
    const path = getEntityUploadUrl(params);
    return get(httpUpload, path);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/release-asset.mjs
var release_asset_exports = {};
__export(release_asset_exports, {
    create: () => create15,
    createFromFiles: () => createFromFiles,
    createWithId: () => createWithId2,
    get: () => get21,
    getMany: () => getMany13,
    processForAllLocales: () => processForAllLocales,
    processForLocale: () => processForLocale,
    update: () => update6
});
var import_contentful_sdk_core3 = __toESM(require_cjs2(), 1);
var get21 = (http4, params, rawData, headers) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/assets/${params.assetId}`, {
        params: normalizeSelect(params.query),
        headers: headers ? { ...headers } : void 0
    });
};
var getMany13 = (http4, params, rawData, headers) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/assets`, {
        params: normalizeSelect(params.query),
        headers: headers ? { ...headers } : void 0
    });
};
var update6 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/assets/${params.assetId}`, data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var create15 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/assets`, data, {
        headers
    });
};
var createWithId2 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/assets/${params.assetId}`, data, {
        headers
    });
};
var createFromFiles = async (http4, params, data) => {
    const httpUpload = getUploadHttpClient(http4, { uploadTimeout: params.uploadTimeout });
    const { file } = data.fields;
    return Promise.all(
        Object.keys(file).map(async locale => {
            const { contentType, fileName } = file[locale];
            return create14(httpUpload, params, file[locale]).then(upload => {
                return {
                    [locale]: {
                        contentType,
                        fileName,
                        uploadFrom: {
                            sys: {
                                type: "Link",
                                linkType: "Upload",
                                id: upload.sys.id
                            }
                        }
                    }
                };
            });
        })
    )
        .then(uploads => {
            const file2 = uploads.reduce((fieldsData, upload) => ({ ...fieldsData, ...upload }), {});
            const asset = {
                ...data,
                fields: {
                    ...data.fields,
                    file: file2
                }
            };
            return create15(http4, params, asset, {});
        })
        .catch(import_contentful_sdk_core3.errorHandler);
};
var ASSET_PROCESSING_CHECK_WAIT = 3e3;
var ASSET_PROCESSING_CHECK_RETRIES = 10;
async function checkIfAssetHasUrl(http4, params, { resolve, reject, locale, processingCheckWait = ASSET_PROCESSING_CHECK_WAIT, processingCheckRetries = ASSET_PROCESSING_CHECK_RETRIES, checkCount = 0 }) {
    return get21(http4, params).then(asset => {
        if (asset.fields.file[locale].url) {
            resolve(asset);
        } else if (checkCount === processingCheckRetries) {
            const error = new Error();
            error.name = "AssetProcessingTimeout";
            error.message = "Asset is taking longer then expected to process.";
            reject(error);
        } else {
            checkCount++;
            setTimeout(
                () =>
                    checkIfAssetHasUrl(http4, params, {
                        resolve,
                        reject,
                        locale,
                        checkCount,
                        processingCheckWait,
                        processingCheckRetries
                    }),
                processingCheckWait
            );
        }
    });
}
var processForLocale = async (http4, { asset, locale, options: { processingCheckRetries, processingCheckWait } = {}, ...params }) => {
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${asset.sys.release.sys.id}/assets/${asset.sys.id}/files/${locale}/process`, null, {
        headers: {
            "X-Contentful-Version": asset.sys.version
        }
    }).then(() => {
        return new Promise((resolve, reject) =>
            checkIfAssetHasUrl(
                http4,
                {
                    spaceId: params.spaceId,
                    environmentId: params.environmentId,
                    assetId: asset.sys.id,
                    releaseId: asset.sys.release.sys.id
                },
                {
                    resolve,
                    reject,
                    locale,
                    processingCheckWait,
                    processingCheckRetries
                }
            )
        );
    });
};
var processForAllLocales = async (http4, { asset, options = {}, ...params }) => {
    const locales = Object.keys(asset.fields.file || {});
    let mostUpToDateAssetVersion = asset;
    const allProcessingLocales = locales.map(locale =>
        processForLocale(http4, { ...params, asset, locale, options }).then(result => {
            mostUpToDateAssetVersion = result;
        })
    );
    return Promise.all(allProcessingLocales).then(() => mostUpToDateAssetVersion);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/asset.mjs
var get22 = (http4, params, rawData, headers) => {
    if (params.releaseId) {
        return get21(http4, params);
    }
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets/${params.assetId}`, {
        params: normalizeSelect(params.query),
        headers: headers ? { ...headers } : void 0
    });
};
var getPublished = (http4, params, rawData, headers) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/public/assets`, {
        params: normalizeSelect(params.query),
        headers: headers ? { ...headers } : void 0
    });
};
var getMany14 = (http4, params, rawData, headers) => {
    if (params.releaseId) {
        return getMany13(http4, params);
    }
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets`, {
        params: normalizeSelect(params.query),
        headers: headers ? { ...headers } : void 0
    });
};
var getManyWithCursor = (http4, params, rawData, headers) => {
    if (params.releaseId) {
        throw new Error("getManyWithCursor is not supported for release-scoped assets");
    }
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets`, {
        params: { cursor: true, ...(params.query ?? {}) },
        headers: headers ? { ...headers } : void 0
    }).then(normalizeCursorPaginationResponse);
};
var update7 = (http4, params, rawData, headers) => {
    if (params.releaseId) {
        return update6(http4, params, rawData, headers ?? {});
    }
    const data = index(rawData);
    delete data.sys;
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets/${params.assetId}`, data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del15 = (http4, params) => {
    return del(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets/${params.assetId}`);
};
var publish2 = (http4, params, rawData) => {
    const payload = params.locales?.length ? { add: { fields: { "*": params.locales } } } : null;
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets/${params.assetId}/published`, payload, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0
        }
    });
};
var unpublish2 = (http4, params, rawData) => {
    if (params.locales?.length) {
        const payload = { remove: { fields: { "*": params.locales } } };
        return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets/${params.assetId}/published`, payload, {
            headers: {
                "X-Contentful-Version": rawData?.sys.version
            }
        });
    } else {
        return del(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets/${params.assetId}/published`);
    }
};
var archive = (http4, params) => {
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets/${params.assetId}/archived`);
};
var unarchive = (http4, params) => {
    return del(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets/${params.assetId}/archived`);
};
var create16 = (http4, params, rawData) => {
    if (params.releaseId) {
        return create15(http4, params, rawData, {});
    }
    const data = index(rawData);
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets`, data);
};
var createWithId3 = (http4, params, rawData) => {
    if (params.releaseId) {
        return createWithId2(http4, params, rawData, {});
    }
    const data = index(rawData);
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets/${params.assetId}`, data);
};
var createFromFiles2 = async (http4, params, data) => {
    if (params.releaseId) {
        return createFromFiles(http4, params, data);
    }
    const httpUpload = getUploadHttpClient(http4, { uploadTimeout: params.uploadTimeout });
    const { file } = data.fields;
    return Promise.all(
        Object.keys(file).map(async locale => {
            const { contentType, fileName } = file[locale];
            return create14(httpUpload, params, file[locale]).then(upload => {
                return {
                    [locale]: {
                        contentType,
                        fileName,
                        uploadFrom: {
                            sys: {
                                type: "Link",
                                linkType: "Upload",
                                id: upload.sys.id
                            }
                        }
                    }
                };
            });
        })
    )
        .then(uploads => {
            const file2 = uploads.reduce((fieldsData, upload) => ({ ...fieldsData, ...upload }), {});
            const asset = {
                ...data,
                fields: {
                    ...data.fields,
                    file: file2
                }
            };
            return create16(http4, params, asset);
        })
        .catch(import_contentful_sdk_core4.errorHandler);
};
var ASSET_PROCESSING_CHECK_WAIT2 = 3e3;
var ASSET_PROCESSING_CHECK_RETRIES2 = 10;
async function checkIfAssetHasUrl2(http4, params, { resolve, reject, locale, processingCheckWait = ASSET_PROCESSING_CHECK_WAIT2, processingCheckRetries = ASSET_PROCESSING_CHECK_RETRIES2, checkCount = 0 }) {
    return get22(http4, params).then(asset => {
        if (asset.fields.file[locale].url) {
            resolve(asset);
        } else if (checkCount === processingCheckRetries) {
            const error = new Error();
            error.name = "AssetProcessingTimeout";
            error.message = "Asset is taking longer then expected to process.";
            reject(error);
        } else {
            checkCount++;
            setTimeout(
                () =>
                    checkIfAssetHasUrl2(http4, params, {
                        resolve,
                        reject,
                        locale,
                        checkCount,
                        processingCheckWait,
                        processingCheckRetries
                    }),
                processingCheckWait
            );
        }
    });
}
var processForLocale2 = async (http4, { asset, locale, options: { processingCheckRetries, processingCheckWait } = {}, ...params }) => {
    if (asset.sys.release) {
        return processForLocale(http4, {
            asset,
            locale,
            options: { processingCheckRetries, processingCheckWait },
            ...params
        });
    }
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/assets/${asset.sys.id}/files/${locale}/process`, null, {
        headers: {
            "X-Contentful-Version": asset.sys.version
        }
    }).then(() => {
        return new Promise((resolve, reject) =>
            checkIfAssetHasUrl2(
                http4,
                {
                    spaceId: params.spaceId,
                    environmentId: params.environmentId,
                    assetId: asset.sys.id
                },
                {
                    resolve,
                    reject,
                    locale,
                    processingCheckWait,
                    processingCheckRetries
                }
            )
        );
    });
};
var processForAllLocales2 = async (http4, { asset, options = {}, ...params }) => {
    if (asset.sys.release) {
        return processForAllLocales(http4, {
            asset,
            options,
            ...params
        });
    }
    const locales = Object.keys(asset.fields.file || {});
    let mostUpToDateAssetVersion = asset;
    const allProcessingLocales = locales.map(locale =>
        processForLocale2(http4, { ...params, asset, locale, options }).then(result => {
            mostUpToDateAssetVersion = result;
        })
    );
    return Promise.all(allProcessingLocales).then(() => mostUpToDateAssetVersion);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/asset-key.mjs
var asset_key_exports = {};
__export(asset_key_exports, {
    ValidationError: () => ValidationError,
    create: () => create17
});
var ASSET_KEY_MAX_LIFETIME = 48 * 60 * 60;
var ValidationError = class extends Error {
    constructor(name, message) {
        super(`Invalid "${name}" provided, ` + message);
        this.name = "ValidationError";
    }
};
var validateTimestamp = (name, timestamp, options) => {
    options = options || {};
    if (typeof timestamp !== "number") {
        throw new ValidationError(name, `only numeric values are allowed for timestamps, provided type was "${typeof timestamp}"`);
    }
    if (options.maximum && timestamp > options.maximum) {
        throw new ValidationError(name, `value (${timestamp}) cannot be further in the future than expected maximum (${options.maximum})`);
    }
    if (options.now && timestamp < options.now) {
        throw new ValidationError(name, `value (${timestamp}) cannot be in the past, current time was ${options.now}`);
    }
};
var create17 = (http4, params, data) => {
    const expiresAt = data.expiresAt;
    const now = Math.floor(Date.now() / 1e3);
    const currentMaxLifetime = now + ASSET_KEY_MAX_LIFETIME;
    validateTimestamp("expiresAt", expiresAt, { maximum: currentMaxLifetime, now });
    const postParams = { expiresAt };
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/asset_keys`, postParams);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/available-license.mjs
var available_license_exports = {};
__export(available_license_exports, {
    getMany: () => getMany15
});
var getBaseUrl9 = params => `/organizations/${params.organizationId}/available_licenses`;
var getMany15 = (http4, params) => {
    return get(http4, getBaseUrl9(params), {
        params: params.query
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/bulk-action.mjs
var bulk_action_exports = {};
__export(bulk_action_exports, {
    get: () => get23,
    getV2: () => getV2,
    publish: () => publish3,
    publishV2: () => publishV2,
    unpublish: () => unpublish3,
    unpublishV2: () => unpublishV2,
    validate: () => validate,
    validateV2: () => validateV2
});
var get23 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/bulk_actions/actions/${params.bulkActionId}`);
};
var publish3 = (http4, params, payload) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/bulk_actions/publish`, payload);
};
var unpublish3 = (http4, params, payload) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/bulk_actions/unpublish`, payload);
};
var validate = (http4, params, payload) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/bulk_actions/validate`, payload);
};
var getV2 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/bulk_actions/${params.bulkActionId}`);
};
var publishV2 = (http4, params, payload) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/bulk_actions`, payload);
};
var unpublishV2 = (http4, params, payload) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/bulk_actions`, payload);
};
var validateV2 = (http4, params, payload) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/bulk_actions`, payload);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/comment.mjs
var comment_exports = {};
__export(comment_exports, {
    create: () => create18,
    del: () => del16,
    get: () => get24,
    getAll: () => getAll,
    getMany: () => getMany16,
    update: () => update8
});
var VERSION_HEADER = "X-Contentful-Version";
var BODY_FORMAT_HEADER = "x-contentful-comment-body-format";
var PARENT_ENTITY_REFERENCE_HEADER = "x-contentful-parent-entity-reference";
var PARENT_COMMENT_ID_HEADER = "x-contentful-parent-id";
var getSpaceEnvBaseUrl = params => `/spaces/${params.spaceId}/environments/${params.environmentId}`;
var getEntityCommentUrl = params => `${getEntityBaseUrl(params)}/${params.commentId}`;
function getParentPlural(parentEntityType) {
    switch (parentEntityType) {
        case "ContentType":
            return "content_types";
        case "Entry":
            return "entries";
        case "Workflow":
            return "workflows";
        case "Experience":
            return "experiences";
        case "Fragment":
            return "fragments";
        case "ComponentType":
            return "component_types";
        case "Template":
            return "templates";
    }
}
var getEntityBaseUrl = paramsOrg => {
    const params =
        "entryId" in paramsOrg
            ? {
                  spaceId: paramsOrg.spaceId,
                  environmentId: paramsOrg.environmentId,
                  parentEntityType: "Entry",
                  parentEntityId: paramsOrg.entryId
              }
            : paramsOrg;
    const { parentEntityId, parentEntityType } = params;
    const parentPlural = getParentPlural(parentEntityType);
    const versionPath = "parentEntityVersion" in params ? `/versions/${params.parentEntityVersion}` : "";
    return `${getSpaceEnvBaseUrl(params)}/${parentPlural}/${parentEntityId}${versionPath}/comments`;
};
var get24 = (http4, params) =>
    get(http4, getEntityCommentUrl(params), {
        headers:
            params.bodyFormat === "rich-text"
                ? {
                      [BODY_FORMAT_HEADER]: params.bodyFormat
                  }
                : {}
    });
var getMany16 = (http4, params) =>
    get(http4, getEntityBaseUrl(params), {
        params: normalizeSelect(params.query),
        headers:
            params.bodyFormat === "rich-text"
                ? {
                      [BODY_FORMAT_HEADER]: params.bodyFormat
                  }
                : {}
    });
var create18 = (http4, params, rawData) => {
    const data = index(rawData);
    return post(http4, getEntityBaseUrl(params), data, {
        headers: {
            ...(typeof rawData.body !== "string" ? { [BODY_FORMAT_HEADER]: "rich-text" } : {}),
            ...("parentEntityReference" in params && params.parentEntityReference ? { [PARENT_ENTITY_REFERENCE_HEADER]: params.parentEntityReference } : {}),
            ...(params.parentCommentId ? { [PARENT_COMMENT_ID_HEADER]: params.parentCommentId } : {})
        }
    });
};
var update8 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getEntityCommentUrl(params), data, {
        headers: {
            [VERSION_HEADER]: rawData.sys.version ?? 0,
            ...(typeof rawData.body !== "string" ? { [BODY_FORMAT_HEADER]: "rich-text" } : {}),
            ...headers
        }
    });
};
var del16 = (http4, { version, ...params }) => {
    return del(http4, getEntityCommentUrl(params), {
        headers: { [VERSION_HEADER]: version }
    });
};
var getAll = getMany16;

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/component-type.mjs
var component_type_exports = {};
__export(component_type_exports, {
    getMany: () => getMany17
});
var getBaseUrl10 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/component_types`;
var getMany17 = (http4, params, headers) => {
    return get(http4, getBaseUrl10(params), {
        params: params.query,
        headers
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/concept.mjs
var concept_exports = {};
__export(concept_exports, {
    create: () => create19,
    createWithId: () => createWithId4,
    del: () => del17,
    get: () => get25,
    getAncestors: () => getAncestors,
    getDescendants: () => getDescendants,
    getMany: () => getMany18,
    getTotal: () => getTotal,
    patch: () => patch2,
    update: () => update9
});
function basePath(organizationId) {
    return `/organizations/${organizationId}/taxonomy/concepts`;
}
var create19 = (http4, params, data) => {
    return post(http4, basePath(params.organizationId), data);
};
var createWithId4 = (http4, params, data) => {
    return put(http4, `${basePath(params.organizationId)}/${params.conceptId}`, data);
};
var patch2 = (http4, params, data, headers) => {
    return patch(http4, `${basePath(params.organizationId)}/${params.conceptId}`, data, {
        headers: {
            "X-Contentful-Version": params.version,
            "Content-Type": "application/json-patch+json",
            ...headers
        }
    });
};
var update9 = (http4, params, data, headers) => {
    return put(http4, `${basePath(params.organizationId)}/${params.conceptId}`, data, {
        headers: {
            "X-Contentful-Version": params.version,
            ...headers
        }
    });
};
var get25 = (http4, params) => get(http4, `${basePath(params.organizationId)}/${params.conceptId}`);
var del17 = (http4, params, headers) =>
    del(http4, `${basePath(params.organizationId)}/${params.conceptId}`, {
        headers: {
            "X-Contentful-Version": params.version ?? 0,
            ...headers
        }
    });
var getMany18 = (http4, params) => {
    const { url: url2, queryParams } = cursorBasedCollection("", params);
    return get(http4, url2, {
        params: queryParams
    });
};
var getDescendants = (http4, params) => {
    const { url: url2, queryParams } = cursorBasedCollection(`/${params.conceptId}/descendants`, params);
    return get(http4, url2, { params: queryParams });
};
var getAncestors = (http4, params) => {
    const { url: url2, queryParams } = cursorBasedCollection(`/${params.conceptId}/ancestors`, params);
    return get(http4, url2, { params: queryParams });
};
var getTotal = (http4, params) => get(http4, `${basePath(params.organizationId)}/total`);
function cursorBasedCollection(path, params) {
    return params.query?.pageUrl
        ? { url: params.query?.pageUrl }
        : {
              url: `${basePath(params.organizationId)}${path}`,
              queryParams: params.query
          };
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/concept-scheme.mjs
var concept_scheme_exports = {};
__export(concept_scheme_exports, {
    create: () => create20,
    createWithId: () => createWithId5,
    del: () => del18,
    get: () => get26,
    getMany: () => getMany19,
    getTotal: () => getTotal2,
    patch: () => patch3,
    update: () => update10
});
function basePath2(orgId) {
    return `/organizations/${orgId}/taxonomy/concept-schemes`;
}
var get26 = (http4, params) => get(http4, `${basePath2(params.organizationId)}/${params.conceptSchemeId}`);
var del18 = (http4, params, headers) =>
    del(http4, `${basePath2(params.organizationId)}/${params.conceptSchemeId}`, {
        headers: {
            "X-Contentful-Version": params.version,
            ...headers
        }
    });
var getMany19 = (http4, params) => {
    const url2 = params.query?.pageUrl ?? basePath2(params.organizationId);
    return get(http4, url2, {
        params: params.query?.pageUrl ? void 0 : params.query
    });
};
var getTotal2 = (http4, params) => get(http4, `${basePath2(params.organizationId)}/total`);
var create20 = (http4, params, data) => {
    return post(http4, basePath2(params.organizationId), data);
};
var createWithId5 = (http4, params, data) => {
    return put(http4, `${basePath2(params.organizationId)}/${params.conceptSchemeId}`, data);
};
var patch3 = (http4, params, data, headers) => {
    return patch(http4, `${basePath2(params.organizationId)}/${params.conceptSchemeId}`, data, {
        headers: {
            "X-Contentful-Version": params.version,
            "Content-Type": "application/json-patch+json",
            ...headers
        }
    });
};
var update10 = (http4, params, data, headers) => {
    return put(http4, `${basePath2(params.organizationId)}/${params.conceptSchemeId}`, data, {
        headers: {
            "X-Contentful-Version": params.version,
            ...headers
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/content-type.mjs
var content_type_exports = {};
__export(content_type_exports, {
    create: () => create21,
    createWithId: () => createWithId6,
    del: () => del19,
    get: () => get27,
    getMany: () => getMany20,
    getManyWithCursor: () => getManyWithCursor2,
    publish: () => publish4,
    unpublish: () => unpublish4,
    update: () => update11
});
var getBaseUrl11 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/content_types`;
var getContentTypeUrl = params => getBaseUrl11(params) + `/${params.contentTypeId}`;
var get27 = (http4, params, headers) => {
    return get(http4, getContentTypeUrl(params), {
        params: normalizeSelect(params.query),
        headers
    });
};
var getMany20 = (http4, params, headers) => {
    return get(http4, getBaseUrl11(params), {
        params: params.query,
        headers
    });
};
var getManyWithCursor2 = (http4, params, headers) => {
    return get(http4, getBaseUrl11(params), {
        params: { cursor: true, ...(params.query ?? {}) },
        headers
    }).then(normalizeCursorPaginationResponse);
};
var create21 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return post(http4, getBaseUrl11(params), data, { headers });
};
var createWithId6 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return put(http4, getContentTypeUrl(params), data, { headers });
};
var update11 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getContentTypeUrl(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del19 = (http4, params, headers) => {
    return del(http4, getContentTypeUrl(params), { headers });
};
var publish4 = (http4, params, rawData, headers) => {
    return put(http4, getContentTypeUrl(params) + "/published", null, {
        headers: {
            "X-Contentful-Version": rawData.sys.version,
            ...headers
        }
    });
};
var unpublish4 = (http4, params, headers) => {
    return del(http4, getContentTypeUrl(params) + "/published", { headers });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/editor-interface.mjs
var editor_interface_exports = {};
__export(editor_interface_exports, {
    get: () => get28,
    getMany: () => getMany21,
    update: () => update12
});
var getBaseUrl12 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/content_types/${params.contentTypeId}/editor_interface`;
var get28 = (http4, params) => {
    return get(http4, getBaseUrl12(params));
};
var getMany21 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/editor_interfaces`);
};
var update12 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getBaseUrl12(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/eligible-license.mjs
var eligible_license_exports = {};
__export(eligible_license_exports, {
    getMany: () => getMany22
});
var getBaseUrl13 = params => `/spaces/${params.spaceId}/eligible_licenses`;
var getMany22 = (http4, params) => {
    return get(http4, getBaseUrl13(params), {
        params: params.query
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/entry.mjs
var entry_exports = {};
__export(entry_exports, {
    archive: () => archive2,
    create: () => create23,
    createWithId: () => createWithId8,
    del: () => del20,
    get: () => get30,
    getMany: () => getMany24,
    getManyWithCursor: () => getManyWithCursor3,
    getPublished: () => getPublished2,
    patch: () => patch5,
    publish: () => publish5,
    references: () => references,
    unarchive: () => unarchive2,
    unpublish: () => unpublish5,
    update: () => update14
});

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/release-entry.mjs
var release_entry_exports = {};
__export(release_entry_exports, {
    create: () => create22,
    createWithId: () => createWithId7,
    get: () => get29,
    getMany: () => getMany23,
    patch: () => patch4,
    update: () => update13
});
var get29 = (http4, params, rawData, headers) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/entries/${params.entryId}`, {
        params: normalizeSelect(params.query),
        headers: { ...headers }
    });
};
var getMany23 = (http4, params, rawData, headers) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/entries`, {
        params: normalizeSelect(params.query),
        headers: { ...headers }
    });
};
var update13 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/entries/${params.entryId}`, data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var patch4 = (http4, params, data, headers) => {
    return patch(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/entries/${params.entryId}`, data, {
        headers: {
            "X-Contentful-Version": params.version,
            "Content-Type": "application/json-patch+json",
            ...headers
        }
    });
};
var create22 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/entries`, data, {
        headers: {
            "X-Contentful-Content-Type": params.contentTypeId,
            ...headers
        }
    });
};
var createWithId7 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/entries/${params.entryId}`, data, {
        headers: {
            "X-Contentful-Content-Type": params.contentTypeId,
            ...headers
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/entry.mjs
var get30 = (http4, params, rawData, headers) => {
    if (params.releaseId) {
        return get29(http4, params);
    }
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries/${params.entryId}`, {
        params: normalizeSelect(params.query),
        headers: { ...headers }
    });
};
var getPublished2 = (http4, params, rawData, headers) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/public/entries`, {
        params: normalizeSelect(params.query),
        headers: { ...headers }
    });
};
var getMany24 = (http4, params, rawData, headers) => {
    if (params.releaseId) {
        return getMany23(http4, params);
    }
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries`, {
        params: normalizeSelect(params.query),
        headers: { ...headers }
    });
};
var getManyWithCursor3 = (http4, params, rawData, headers) => {
    if (params.releaseId) {
        throw new Error("getManyWithCursor is not supported for release-scoped entries");
    }
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries`, {
        params: { cursor: true, ...(params.query ?? {}) },
        headers: { ...headers }
    }).then(normalizeCursorPaginationResponse);
};
var patch5 = (http4, params, data, headers) => {
    if (params.releaseId) {
        return patch4(http4, params, data, headers ?? {});
    }
    return patch(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries/${params.entryId}`, data, {
        headers: {
            "X-Contentful-Version": params.version,
            "Content-Type": "application/json-patch+json",
            ...headers
        }
    });
};
var update14 = (http4, params, rawData, headers) => {
    if (params.releaseId) {
        return update13(http4, params, rawData, headers ?? {});
    }
    const data = index(rawData);
    delete data.sys;
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries/${params.entryId}`, data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del20 = (http4, params) => {
    return del(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries/${params.entryId}`);
};
var publish5 = (http4, params, rawData) => {
    const payload = params.locales?.length ? { add: { fields: { "*": params.locales } } } : null;
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries/${params.entryId}/published`, payload, {
        headers: {
            "X-Contentful-Version": rawData.sys.version
        }
    });
};
var unpublish5 = (http4, params, rawData) => {
    if (params.locales?.length) {
        const payload = { remove: { fields: { "*": params.locales } } };
        return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries/${params.entryId}/published`, payload, {
            headers: {
                "X-Contentful-Version": rawData?.sys.version
            }
        });
    } else {
        return del(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries/${params.entryId}/published`);
    }
};
var archive2 = (http4, params) => {
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries/${params.entryId}/archived`);
};
var unarchive2 = (http4, params) => {
    return del(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries/${params.entryId}/archived`);
};
var create23 = (http4, params, rawData) => {
    if (params.releaseId) {
        return create22(http4, params, rawData, {});
    }
    const data = index(rawData);
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries`, data, {
        headers: {
            "X-Contentful-Content-Type": params.contentTypeId
        }
    });
};
var createWithId8 = (http4, params, rawData) => {
    if (params.releaseId) {
        return createWithId7(http4, params, rawData, {});
    }
    const data = index(rawData);
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/entries/${params.entryId}`, data, {
        headers: {
            "X-Contentful-Content-Type": params.contentTypeId
        }
    });
};
var references = (http4, params) => {
    const { spaceId, environmentId, entryId, include } = params;
    const level = include || 2;
    return get(http4, `/spaces/${spaceId}/environments/${environmentId}/entries/${entryId}/references?include=${level}`);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/environment.mjs
var environment_exports = {};
__export(environment_exports, {
    create: () => create24,
    createWithId: () => createWithId9,
    del: () => del21,
    get: () => get31,
    getMany: () => getMany25,
    update: () => update15
});
var get31 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}`);
};
var getMany25 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments`, {
        params: params.query
    });
};
var update15 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}`, data, {
        headers: {
            ...headers,
            "X-Contentful-Version": rawData.sys.version ?? 0
        }
    });
};
var del21 = (http4, params) => {
    return del(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}`);
};
var create24 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return post(http4, `/spaces/${params.spaceId}/environments`, data, {
        headers
    });
};
var createWithId9 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}`, data, {
        headers: {
            ...headers,
            ...(params.sourceEnvironmentId
                ? {
                      "X-Contentful-Source-Environment": params.sourceEnvironmentId
                  }
                : {})
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/environment-alias.mjs
var environment_alias_exports = {};
__export(environment_alias_exports, {
    createWithId: () => createWithId10,
    del: () => del22,
    get: () => get32,
    getMany: () => getMany26,
    update: () => update16
});
var getBaseUrl14 = params => `/spaces/${params.spaceId}/environment_aliases`;
var getEnvironmentAliasUrl = params => getBaseUrl14(params) + `/${params.environmentAliasId}`;
var get32 = (http4, params) => {
    return get(http4, getEnvironmentAliasUrl(params));
};
var getMany26 = (http4, params) => {
    return get(http4, getBaseUrl14(params), {
        params: params.query
    });
};
var createWithId10 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return put(http4, getEnvironmentAliasUrl(params), data, {
        headers
    });
};
var update16 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getEnvironmentAliasUrl(params), data, {
        headers: {
            ...headers,
            "X-Contentful-Version": rawData.sys.version ?? 0
        }
    });
};
var del22 = (http4, params) => {
    return del(http4, getEnvironmentAliasUrl(params));
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/environment-template.mjs
var environment_template_exports = {};
__export(environment_template_exports, {
    create: () => create25,
    del: () => del23,
    disconnect: () => disconnect,
    get: () => get33,
    getMany: () => getMany27,
    install: () => install,
    update: () => update17,
    validate: () => validate2,
    versionUpdate: () => versionUpdate,
    versions: () => versions
});
var apiPath = (organizationId, ...pathSegments) => `/organizations/${organizationId}/environment_templates/` + pathSegments.join("/");
var get33 = (http4, { organizationId, environmentTemplateId, version, query: query2 = {} }, headers) =>
    version
        ? get(http4, apiPath(organizationId, environmentTemplateId, "versions", version), {
              params: query2,
              headers
          })
        : get(http4, apiPath(organizationId, environmentTemplateId), {
              params: query2,
              headers
          });
var getMany27 = (http4, { organizationId, query: query2 = {} }, headers) => get(http4, apiPath(organizationId), { params: query2, headers });
var create25 = (http4, { organizationId }, payload, headers) => post(http4, apiPath(organizationId), payload, { headers });
var update17 = (http4, { organizationId, environmentTemplateId }, payload, headers) => {
    const data = index(payload);
    delete data.sys;
    return put(http4, apiPath(organizationId, environmentTemplateId), data, {
        headers: {
            "X-Contentful-Version": payload.sys.version ?? 0,
            ...headers
        }
    });
};
var versionUpdate = (http4, { organizationId, version, environmentTemplateId }, payload, headers) =>
    patch(http4, apiPath(organizationId, environmentTemplateId, "versions", version), payload, {
        headers
    });
var del23 = (http4, { organizationId, environmentTemplateId }, headers) => del(http4, apiPath(organizationId, environmentTemplateId), { headers });
var versions = (http4, { organizationId, environmentTemplateId, query: query2 = {} }, headers) =>
    get(http4, apiPath(organizationId, environmentTemplateId, "versions"), {
        params: query2,
        headers
    });
var validate2 = (http4, { spaceId, environmentId, environmentTemplateId, version }, payload, headers) =>
    put(http4, version ? `/spaces/${spaceId}/environments/${environmentId}/template_installations/${environmentTemplateId}/versions/${version}/validated` : `/spaces/${spaceId}/environments/${environmentId}/template_installations/${environmentTemplateId}/validated`, payload, { headers });
var install = (http4, { spaceId, environmentId, environmentTemplateId }, payload, headers) => post(http4, `/spaces/${spaceId}/environments/${environmentId}/template_installations/${environmentTemplateId}/versions`, payload, { headers });
var disconnect = (http4, { spaceId, environmentId, environmentTemplateId }, headers) => del(http4, `/spaces/${spaceId}/environments/${environmentId}/template_installations/${environmentTemplateId}`, { headers });

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/environment-template-installation.mjs
var environment_template_installation_exports = {};
__export(environment_template_installation_exports, {
    getForEnvironment: () => getForEnvironment,
    getMany: () => getMany28
});
var apiPath2 = (organizationId, ...pathSegments) => `/organizations/${organizationId}/environment_templates/` + pathSegments.join("/");
var getMany28 = (http4, { organizationId, environmentTemplateId, spaceId, environmentId, ...otherProps }, headers) =>
    get(http4, apiPath2(organizationId, environmentTemplateId, "template_installations"), {
        params: {
            ...otherProps,
            ...(environmentId && { "environment.sys.id": environmentId }),
            ...(spaceId && { "space.sys.id": spaceId })
        },
        headers
    });
var getForEnvironment = (http4, { spaceId, environmentId, environmentTemplateId, installationId, ...paginationProps }, headers) =>
    get(http4, `/spaces/${spaceId}/environments/${environmentId}/template_installations/${environmentTemplateId}`, {
        params: {
            ...(installationId && { "sys.id": installationId }),
            ...paginationProps
        },
        headers
    });

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/extension.mjs
var extension_exports = {};
__export(extension_exports, {
    create: () => create26,
    createWithId: () => createWithId11,
    del: () => del24,
    get: () => get34,
    getExtensionUrl: () => getExtensionUrl,
    getMany: () => getMany29,
    update: () => update18
});
var getBaseUrl15 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/extensions`;
var getExtensionUrl = params => getBaseUrl15(params) + `/${params.extensionId}`;
var get34 = (http4, params) => {
    return get(http4, getExtensionUrl(params), {
        params: normalizeSelect(params.query)
    });
};
var getMany29 = (http4, params) => {
    return get(http4, getBaseUrl15(params), {
        params: normalizeSelect(params.query)
    });
};
var create26 = (http4, params, rawData, headers) => {
    return post(http4, getBaseUrl15(params), rawData, { headers });
};
var createWithId11 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return put(http4, getExtensionUrl(params), data, { headers });
};
var update18 = async (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getExtensionUrl(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del24 = (http4, params) => {
    return del(http4, getExtensionUrl(params));
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/function.mjs
var function_exports = {};
__export(function_exports, {
    get: () => get35,
    getMany: () => getMany30,
    getManyForEnvironment: () => getManyForEnvironment2
});
var getManyUrl = params => `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/functions`;
var getFunctionUrl = params => `${getManyUrl(params)}/${params.functionId}`;
var getFunctionsEnvURL = params => {
    return `/spaces/${params.spaceId}/environments/${params.environmentId}/app_installations/${params.appInstallationId}/functions`;
};
var get35 = (http4, params) => {
    return get(http4, getFunctionUrl(params));
};
var getMany30 = (http4, params) => {
    return get(http4, getManyUrl(params), { params: params.query });
};
var getManyForEnvironment2 = (http4, params) => {
    return get(http4, getFunctionsEnvURL(params), {
        params: params.query
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/function-log.mjs
var function_log_exports = {};
__export(function_log_exports, {
    get: () => get36,
    getMany: () => getMany31
});
var FunctionLogAlphaHeaders = {
    "x-contentful-enable-alpha-feature": "function-logs"
};
var baseURL = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/app_installations/${params.appInstallationId}/functions/${params.functionId}/logs`;
var getURL = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/app_installations/${params.appInstallationId}/functions/${params.functionId}/logs/${params.logId}`;
var get36 = (http4, params) => {
    return get(http4, getURL(params), {
        headers: {
            ...FunctionLogAlphaHeaders
        }
    });
};
var getMany31 = (http4, params) => {
    return get(http4, baseURL(params), {
        params: params.query,
        headers: {
            ...FunctionLogAlphaHeaders
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/http.mjs
var http_exports = {};
__export(http_exports, {
    del: () => del25,
    get: () => get37,
    patch: () => patch6,
    post: () => post2,
    put: () => put2,
    request: () => request
});
var get37 = (http4, { url: url2, config }) => {
    return get(http4, url2, config);
};
var post2 = (http4, { url: url2, config }, payload) => {
    return post(http4, url2, payload, config);
};
var put2 = (http4, { url: url2, config }, payload) => {
    return put(http4, url2, payload, config);
};
var patch6 = (http4, { url: url2, config }, payload) => {
    return patch(http4, url2, payload, config);
};
var del25 = (http4, { url: url2, config }) => {
    return del(http4, url2, config);
};
var request = (http$1, { url: url2, config }) => {
    return http3(http$1, url2, config);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/locale.mjs
var locale_exports = {};
__export(locale_exports, {
    create: () => create27,
    del: () => del26,
    get: () => get38,
    getMany: () => getMany32,
    update: () => update19
});
var get38 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/locales/${params.localeId}`);
};
var getMany32 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/locales`, {
        params: normalizeSelect(params.query)
    });
};
var create27 = (http4, params, data, headers) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/locales`, data, {
        headers
    });
};
var update19 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    delete data.default;
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/locales/${params.localeId}`, data, {
        headers: {
            ...headers,
            "X-Contentful-Version": rawData.sys.version ?? 0
        }
    });
};
var del26 = (http4, params) => {
    return del(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/locales/${params.localeId}`);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/organization.mjs
var organization_exports = {};
__export(organization_exports, {
    get: () => get39,
    getMany: () => getMany33
});
var getMany33 = (http4, params) => {
    return get(http4, `/organizations`, {
        params: params?.query
    });
};
var get39 = (http4, params) => {
    return getMany33(http4, { query: { limit: 100 } }).then(data => {
        const org = data.items.find(org2 => org2.sys.id === params.organizationId);
        if (!org) {
            const error = new Error(`No organization was found with the ID ${params.organizationId} instead got ${JSON.stringify(data)}`);
            error.status = 404;
            error.statusText = "Not Found";
            return Promise.reject(error);
        }
        return org;
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/organization-invitation.mjs
var organization_invitation_exports = {};
__export(organization_invitation_exports, {
    create: () => create28,
    get: () => get40
});
var OrganizationUserManagementAlphaHeaders = {
    "x-contentful-enable-alpha-feature": "organization-user-management-api"
};
var InvitationAlphaHeaders = {
    "x-contentful-enable-alpha-feature": "pending-org-membership"
};
var create28 = (http4, params, data, headers) => {
    return post(http4, `/organizations/${params.organizationId}/invitations`, data, {
        headers: {
            ...InvitationAlphaHeaders,
            ...headers
        }
    });
};
var get40 = (http4, params, headers) => {
    return get(http4, `/organizations/${params.organizationId}/invitations/${params.invitationId}`, {
        headers: {
            ...OrganizationUserManagementAlphaHeaders,
            ...headers
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/organization-membership.mjs
var organization_membership_exports = {};
__export(organization_membership_exports, {
    del: () => del27,
    get: () => get41,
    getMany: () => getMany34,
    update: () => update20
});
var getBaseUrl16 = params => `/organizations/${params.organizationId}/organization_memberships`;
var getEntityUrl = params => `${getBaseUrl16(params)}/${params.organizationMembershipId}`;
var get41 = (http4, params) => {
    return get(http4, getEntityUrl(params));
};
var getMany34 = (http4, params) => {
    return get(http4, getBaseUrl16(params), {
        params: params.query
    });
};
var update20 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    const { role } = data;
    return put(
        http4,
        getEntityUrl(params),
        { role },
        {
            headers: {
                ...headers,
                "X-Contentful-Version": rawData.sys.version ?? 0
            }
        }
    );
};
var del27 = (http4, params) => {
    return del(http4, getEntityUrl(params));
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/oauth-application.mjs
var oauth_application_exports = {};
__export(oauth_application_exports, {
    create: () => create29,
    del: () => del28,
    get: () => get42,
    getManyForUser: () => getManyForUser,
    update: () => update21
});
var get42 = (http4, params) => {
    return get(http4, `/users/${params.userId}/oauth_applications/${params.oauthApplicationId}`);
};
var getManyForUser = (http4, params) => {
    return get(http4, `/users/${params.userId}/oauth_applications`, {
        params: params.query
    });
};
var create29 = (http4, params, rawData, headers) => {
    return post(http4, `/users/${params.userId}/oauth_applications`, rawData, {
        headers
    });
};
var update21 = (http4, params, rawData, headers) => {
    return put(http4, `/users/${params.userId}/oauth_applications/${params.oauthApplicationId}`, rawData, {
        headers
    });
};
var del28 = (http4, params) => {
    return del(http4, `/users/${params.userId}/oauth_applications/${params.oauthApplicationId}`);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/personal-access-token.mjs
var personal_access_token_exports = {};
__export(personal_access_token_exports, {
    create: () => create30,
    get: () => get43,
    getMany: () => getMany35,
    revoke: () => revoke2
});
var get43 = (http4, params) => {
    return get(http4, `/users/me/access_tokens/${params.tokenId}`);
};
var getMany35 = (http4, params) => {
    return get(http4, "/users/me/access_tokens", {
        params: params.query
    });
};
var create30 = (http4, _params, rawData, headers) => {
    return post(http4, "/users/me/access_tokens", rawData, {
        headers
    });
};
var revoke2 = (http4, params) => {
    return put(http4, `/users/me/access_tokens/${params.tokenId}/revoked`, null);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/preview-api-key.mjs
var preview_api_key_exports = {};
__export(preview_api_key_exports, {
    get: () => get44,
    getMany: () => getMany36
});
var get44 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/preview_api_keys/${params.previewApiKeyId}`);
};
var getMany36 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/preview_api_keys`, {
        params: params.query
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/release.mjs
var release_exports = {};
__export(release_exports, {
    archive: () => archive3,
    create: () => create31,
    del: () => del29,
    get: () => get45,
    publish: () => publish6,
    query: () => query,
    unarchive: () => unarchive3,
    unpublish: () => unpublish6,
    update: () => update22,
    validate: () => validate3
});
var get45 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}`);
};
var query = (http4, params) => {
    const releaseSchemaVersion = params.query?.["sys.schemaVersion"] ?? params.releaseSchemaVersion ?? void 0;
    if (releaseSchemaVersion !== void 0) {
        params.query = { ...params.query, "sys.schemaVersion": releaseSchemaVersion };
    }
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases`, {
        params: params.query
    });
};
var create31 = (http4, params, payload) => {
    const releaseSchemaVersion = payload.sys?.schemaVersion ?? params.releaseSchemaVersion;
    if (releaseSchemaVersion === "Release.v2") {
        payload.sys = { ...payload.sys, type: "Release", schemaVersion: "Release.v2" };
    }
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases`, payload);
};
var update22 = (http4, params, payload, headers) => {
    const releaseSchemaVersion = payload.sys?.schemaVersion ?? params.releaseSchemaVersion;
    if (releaseSchemaVersion === "Release.v2") {
        payload.sys = { ...payload.sys, type: "Release", schemaVersion: "Release.v2" };
    }
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}`, payload, {
        headers: {
            "X-Contentful-Version": params.version,
            ...headers
        }
    });
};
var del29 = (http4, params) => {
    return del(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}`);
};
var publish6 = (http4, params, headers) => {
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/published`, null, {
        headers: {
            "X-Contentful-Version": params.version,
            ...headers
        }
    });
};
var unpublish6 = (http4, params, headers) => {
    return del(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/published`, {
        headers: {
            "X-Contentful-Version": params.version,
            ...headers
        }
    });
};
var validate3 = (http4, params, payload) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/validate`, payload);
};
var archive3 = (http4, params) => {
    return put(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/archived`, null, {
        headers: {
            "X-Contentful-Version": params.version
        }
    });
};
var unarchive3 = (http4, params) => {
    return del(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/archived`, {
        headers: {
            "X-Contentful-Version": params.version
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/release-action.mjs
var release_action_exports = {};
__export(release_action_exports, {
    get: () => get46,
    getMany: () => getMany37,
    queryForRelease: () => queryForRelease
});
var get46 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/releases/${params.releaseId}/actions/${params.actionId}`);
};
var getMany37 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/release_actions`, {
        params: params.query
    });
};
var queryForRelease = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/release_actions`, {
        params: {
            "sys.release.sys.id[in]": params.releaseId,
            ...params.query
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/resource.mjs
var resource_exports = {};
__export(resource_exports, {
    getMany: () => getMany38
});
var getBaseUrl17 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/resource_types/${params.resourceTypeId}/resources`;
var getMany38 = (http4, params) =>
    get(http4, getBaseUrl17(params), {
        params: params.query
    });

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/resource-provider.mjs
var resource_provider_exports = {};
__export(resource_provider_exports, {
    del: () => del30,
    get: () => get47,
    upsert: () => upsert5
});
var getBaseUrl18 = params => `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/resource_provider`;
var get47 = (http4, params) => {
    return get(http4, getBaseUrl18(params));
};
var upsert5 = (http4, params, rawData, headers) => {
    return put(http4, getBaseUrl18(params), rawData, { headers });
};
var del30 = (http4, params) => {
    return del(http4, getBaseUrl18(params));
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/resource-type.mjs
var resource_type_exports = {};
__export(resource_type_exports, {
    del: () => del31,
    get: () => get48,
    getForEnvironment: () => getForEnvironment2,
    getMany: () => getMany39,
    upsert: () => upsert6
});
var getBaseUrl19 = params => `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/resource_provider/resource_types`;
var getEntityUrl2 = params => `${getBaseUrl19(params)}/${params.resourceTypeId}`;
var getSpaceEnvUrl = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/resource_types`;
var get48 = (http4, params) => {
    return get(http4, getEntityUrl2(params));
};
var upsert6 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return put(http4, getEntityUrl2(params), data, { headers });
};
var del31 = (http4, params) => {
    return del(http4, getEntityUrl2(params));
};
var getMany39 = (http4, params) => {
    return get(http4, getBaseUrl19(params));
};
var getForEnvironment2 = (http4, params) => {
    return get(http4, getSpaceEnvUrl(params));
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/role.mjs
var role_exports = {};
__export(role_exports, {
    create: () => create32,
    createWithId: () => createWithId12,
    del: () => del32,
    get: () => get49,
    getMany: () => getMany40,
    getManyForOrganization: () => getManyForOrganization2,
    update: () => update23
});
var get49 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/roles/${params.roleId}`);
};
var getMany40 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/roles`, {
        params: normalizeSelect(params.query)
    });
};
var getManyForOrganization2 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/roles`, {
        params: normalizeSelect(params.query)
    });
};
var create32 = (http4, params, data, headers) => {
    return post(http4, `/spaces/${params.spaceId}/roles`, data, {
        headers
    });
};
var createWithId12 = (http4, params, data, headers) => {
    return put(http4, `/spaces/${params.spaceId}/roles/${params.roleId}`, data, {
        headers
    });
};
var update23 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, `/spaces/${params.spaceId}/roles/${params.roleId}`, data, {
        headers: {
            ...headers,
            "X-Contentful-Version": rawData.sys.version ?? 0
        }
    });
};
var del32 = (http4, params) => {
    return del(http4, `/spaces/${params.spaceId}/roles/${params.roleId}`);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/scheduled-action.mjs
var scheduled_action_exports = {};
__export(scheduled_action_exports, {
    create: () => create33,
    del: () => del33,
    get: () => get50,
    getMany: () => getMany41,
    update: () => update24
});
var get50 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/scheduled_actions/${params.scheduledActionId}`, {
        params: {
            "environment.sys.id": params.environmentId
        }
    });
};
var getMany41 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/scheduled_actions`, {
        params: normalizeSelect(params.query)
    });
};
var create33 = (http4, params, data) => {
    return post(http4, `/spaces/${params.spaceId}/scheduled_actions`, data);
};
var del33 = (http4, params) => {
    return del(http4, `/spaces/${params.spaceId}/scheduled_actions/${params.scheduledActionId}`, {
        params: {
            "environment.sys.id": params.environmentId
        }
    });
};
var update24 = (http4, params, data) => {
    return put(http4, `/spaces/${params.spaceId}/scheduled_actions/${params.scheduledActionId}`, data, {
        params: {
            "environment.sys.id": data.environment?.sys.id
        },
        headers: {
            "X-Contentful-Version": params.version
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/content-semantics-index.mjs
var content_semantics_index_exports = {};
__export(content_semantics_index_exports, {
    create: () => create34,
    del: () => del34,
    get: () => get51,
    getMany: () => getMany42,
    getManyForEnvironment: () => getManyForEnvironment3
});
var get51 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/semantic/search-index/${params.indexId}`);
};
var getMany42 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/semantic/search-index`, { params: params.status ? { status: params.status } : void 0 });
};
var getManyForEnvironment3 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/semantic/search-index`, { params: params.status ? { status: params.status } : void 0 });
};
var create34 = (http4, params, data) => {
    return post(http4, `/organizations/${params.organizationId}/semantic/search-index`, data);
};
var del34 = (http4, params) => {
    return del(http4, `/organizations/${params.organizationId}/semantic/search-index/${params.indexId}`);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/semantic-duplicates.mjs
var semantic_duplicates_exports = {};
__export(semantic_duplicates_exports, {
    get: () => get52
});
var get52 = (http4, params, data, headers) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/semantic/duplicates`, data, { headers });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/semantic-recommendations.mjs
var semantic_recommendations_exports = {};
__export(semantic_recommendations_exports, {
    get: () => get53
});
var get53 = (http4, params, data, headers) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/semantic/recommendations`, data, { headers });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/semantic-reference-suggestions.mjs
var semantic_reference_suggestions_exports = {};
__export(semantic_reference_suggestions_exports, {
    get: () => get54
});
var get54 = (http4, params, data, headers) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/semantic/reference-suggestions`, data, { headers });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/semantic-search.mjs
var semantic_search_exports = {};
__export(semantic_search_exports, {
    get: () => get55
});
var get55 = (http4, params, data, headers) => {
    return post(http4, `/spaces/${params.spaceId}/environments/${params.environmentId}/semantic/search`, data, { headers });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/semantic-settings.mjs
var semantic_settings_exports = {};
__export(semantic_settings_exports, {
    get: () => get56
});
var get56 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/semantic/settings`);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/snapshot.mjs
var snapshot_exports = {};
__export(snapshot_exports, {
    getForContentType: () => getForContentType,
    getForEntry: () => getForEntry,
    getManyForContentType: () => getManyForContentType,
    getManyForEntry: () => getManyForEntry
});
var getBaseEntryUrl = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/entries/${params.entryId}/snapshots`;
var getEntryUrl = params => getBaseEntryUrl(params) + `/${params.snapshotId}`;
var getManyForEntry = (http4, params) => {
    return get(http4, getBaseEntryUrl(params), {
        params: normalizeSelect(params.query)
    });
};
var getForEntry = (http4, params) => {
    return get(http4, getEntryUrl(params));
};
var getBaseContentTypeUrl = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/content_types/${params.contentTypeId}/snapshots`;
var getContentTypeUrl2 = params => getBaseContentTypeUrl(params) + `/${params.snapshotId}`;
var getManyForContentType = (http4, params) => {
    return get(http4, getBaseContentTypeUrl(params), {
        params: normalizeSelect(params.query)
    });
};
var getForContentType = (http4, params) => {
    return get(http4, getContentTypeUrl2(params));
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/space.mjs
var space_exports = {};
__export(space_exports, {
    create: () => create35,
    del: () => del35,
    get: () => get57,
    getMany: () => getMany43,
    getManyForOrganization: () => getManyForOrganization3,
    unarchive: () => unarchive4,
    update: () => update25
});
var get57 = (http4, params) =>
    get(http4, `/spaces/${params.spaceId}`, {
        params: params.include ? { include: params.include } : void 0
    });
var getMany43 = (http4, params) =>
    get(http4, `/spaces`, {
        params: { ...params.query, ...(params.include ? { include: params.include } : {}) },
        headers: params.organizationId ? { "X-Contentful-Organization": params.organizationId } : void 0
    });
var getManyForOrganization3 = (http4, params) =>
    get(http4, `/organizations/${params.organizationId}/spaces`, {
        params: params.query
    });
var create35 = (http4, params, payload, headers) => {
    return post(http4, `/spaces`, payload, {
        headers: params.organizationId ? { ...headers, "X-Contentful-Organization": params.organizationId } : headers
    });
};
var update25 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, `/spaces/${params.spaceId}`, data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var unarchive4 = (http4, params, data, headers) => {
    return post(http4, `/spaces/${params.spaceId}/unarchive`, data, {
        headers
    });
};
var del35 = (http4, params) => del(http4, `/spaces/${params.spaceId}`);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/space-add-on.mjs
var space_add_on_exports = {};
__export(space_add_on_exports, {
    getMany: () => getMany44,
    updateAllocations: () => updateAllocations
});
var getMany44 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/space_add_ons`, {
        params: normalizeSelect(params.query)
    });
};
var updateAllocations = (http4, params, data, headers) => {
    return put(http4, `/spaces/${params.spaceId}/space_add_ons`, data, {
        headers
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/space-member.mjs
var space_member_exports = {};
__export(space_member_exports, {
    get: () => get58,
    getMany: () => getMany45
});
var get58 = (http4, params) => get(http4, `/spaces/${params.spaceId}/space_members/${params.spaceMemberId}`);
var getMany45 = (http4, params) =>
    get(http4, `/spaces/${params.spaceId}/space_members`, {
        params: params.query
    });

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/space-membership.mjs
var space_membership_exports = {};
__export(space_membership_exports, {
    create: () => create36,
    createWithId: () => createWithId13,
    del: () => del36,
    get: () => get59,
    getForOrganization: () => getForOrganization2,
    getMany: () => getMany46,
    getManyForOrganization: () => getManyForOrganization4,
    update: () => update26
});
function spaceMembershipDeprecationWarning() {
    console.warn("The user attribute in the space membership root is deprecated. The attribute has been moved inside the sys  object (i.e. sys.user)");
}
var getBaseUrl20 = params => `/spaces/${params.spaceId}/space_memberships`;
var getEntityUrl3 = params => `${getBaseUrl20(params)}/${params.spaceMembershipId}`;
var get59 = (http4, params) => {
    spaceMembershipDeprecationWarning();
    return get(http4, getEntityUrl3(params));
};
var getMany46 = (http4, params) => {
    spaceMembershipDeprecationWarning();
    return get(http4, getBaseUrl20(params), {
        params: params.query
    });
};
var getForOrganization2 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/space_memberships/${params.spaceMembershipId}`);
};
var getManyForOrganization4 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/space_memberships`, {
        params: params.query
    });
};
var create36 = (http4, params, data, headers) => {
    spaceMembershipDeprecationWarning();
    return post(http4, getBaseUrl20(params), data, {
        headers
    });
};
var createWithId13 = (http4, params, data, headers) => {
    spaceMembershipDeprecationWarning();
    return put(http4, getEntityUrl3(params), data, {
        headers
    });
};
var update26 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getEntityUrl3(params), data, {
        headers: {
            ...headers,
            "X-Contentful-Version": rawData.sys.version ?? 0
        }
    });
};
var del36 = (http4, params) => {
    return del(http4, getEntityUrl3(params));
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/tag.mjs
var tag_exports = {};
__export(tag_exports, {
    createWithId: () => createWithId14,
    del: () => del37,
    get: () => get60,
    getMany: () => getMany47,
    update: () => update27
});
var getBaseUrl21 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/tags`;
var getTagUrl = params => getBaseUrl21(params) + `/${params.tagId}`;
var get60 = (http4, params) => get(http4, getTagUrl(params));
var getMany47 = (http4, params) =>
    get(http4, getBaseUrl21(params), {
        params: params.query
    });
var createWithId14 = (http4, params, rawData) => {
    const data = index(rawData);
    return put(http4, getTagUrl(params), data, {
        headers: { "X-Contentful-Tag-Visibility": rawData.sys.visibility ?? "private" }
    });
};
var update27 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getTagUrl(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del37 = (http4, { version, ...params }) => {
    return del(http4, getTagUrl(params), { headers: { "X-Contentful-Version": version } });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/task.mjs
var task_exports = {};
__export(task_exports, {
    create: () => create37,
    del: () => del38,
    get: () => get61,
    getAll: () => getAll2,
    getMany: () => getMany48,
    update: () => update28
});
var getSpaceEnvBaseUrl2 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}`;
function getParentPlural2(parentEntityType) {
    switch (parentEntityType) {
        case "Entry":
            return "entries";
        case "Experience":
            return "experiences";
        case "Fragment":
            return "fragments";
        case "Template":
            return "templates";
        case "ComponentType":
            return "component_types";
    }
}
var normalizeTaskParentParams = paramsOrg =>
    "entryId" in paramsOrg
        ? {
              spaceId: paramsOrg.spaceId,
              environmentId: paramsOrg.environmentId,
              parentEntityType: "Entry",
              parentEntityId: paramsOrg.entryId
          }
        : paramsOrg;
var getBaseUrl22 = paramsOrg => {
    const params = normalizeTaskParentParams(paramsOrg);
    const parentPlural = getParentPlural2(params.parentEntityType);
    return `${getSpaceEnvBaseUrl2(params)}/${parentPlural}/${params.parentEntityId}/tasks`;
};
var getTaskUrl = params => `${getBaseUrl22(params)}/${params.taskId}`;
var get61 = (http4, params) => get(http4, getTaskUrl(params));
var getMany48 = (http4, params) =>
    get(http4, getBaseUrl22(params), {
        params: normalizeSelect(params.query)
    });
var getAll2 = getMany48;
var create37 = (http4, params, rawData) => {
    const data = index(rawData);
    return post(http4, getBaseUrl22(params), data);
};
var update28 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getTaskUrl(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del38 = (http4, { version, ...params }) => {
    return del(http4, getTaskUrl(params), { headers: { "X-Contentful-Version": version } });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/team.mjs
var team_exports = {};
__export(team_exports, {
    create: () => create38,
    del: () => del39,
    get: () => get62,
    getMany: () => getMany49,
    getManyForSpace: () => getManyForSpace,
    update: () => update29
});
var getBaseUrl23 = params => `/organizations/${params.organizationId}/teams`;
var getEntityUrl4 = params => `${getBaseUrl23(params)}/${params.teamId}`;
var get62 = (http4, params) => get(http4, getEntityUrl4(params));
var getMany49 = (http4, params) =>
    get(http4, getBaseUrl23(params), {
        params: normalizeSelect(params.query)
    });
var getManyForSpace = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/teams`, {
        params: normalizeSelect(params.query)
    });
};
var create38 = (http4, params, rawData, headers) => {
    return post(http4, getBaseUrl23(params), rawData, { headers });
};
var update29 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getEntityUrl4(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del39 = (http4, params) => del(http4, getEntityUrl4(params));

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/team-membership.mjs
var team_membership_exports = {};
__export(team_membership_exports, {
    create: () => create39,
    del: () => del40,
    get: () => get63,
    getManyForOrganization: () => getManyForOrganization5,
    getManyForTeam: () => getManyForTeam,
    update: () => update30
});
var getBaseUrl24 = params => `/organizations/${params.organizationId}/teams/${params.teamId}/team_memberships`;
var getEntityUrl5 = params => `/organizations/${params.organizationId}/teams/${params.teamId}/team_memberships/${params.teamMembershipId}`;
var get63 = (http4, params) => get(http4, getEntityUrl5(params));
var getManyForOrganization5 = (http4, params) =>
    get(http4, `/organizations/${params.organizationId}/team_memberships`, {
        params: normalizeSelect(params.query)
    });
var getManyForTeam = (http4, params) => {
    return get(http4, getBaseUrl24(params), {
        params: normalizeSelect(params.query)
    });
};
var create39 = (http4, params, rawData, headers) => {
    return post(http4, getBaseUrl24(params), rawData, { headers });
};
var update30 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getEntityUrl5(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version || 0,
            ...headers
        }
    });
};
var del40 = (http4, params) => del(http4, getEntityUrl5(params));

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/team-space-membership.mjs
var team_space_membership_exports = {};
__export(team_space_membership_exports, {
    create: () => create40,
    del: () => del41,
    get: () => get64,
    getForOrganization: () => getForOrganization3,
    getMany: () => getMany50,
    getManyForOrganization: () => getManyForOrganization6,
    update: () => update31
});
var getBaseUrl25 = params => `/spaces/${params.spaceId}/team_space_memberships`;
var getEntityUrl6 = params => `${getBaseUrl25(params)}/${params.teamSpaceMembershipId}`;
var get64 = (http4, params) => get(http4, getEntityUrl6(params));
var getMany50 = (http4, params) =>
    get(http4, getBaseUrl25(params), {
        params: params.query
    });
var getForOrganization3 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/team_space_memberships/${params.teamSpaceMembershipId}`);
};
var getManyForOrganization6 = (http4, params) => {
    const query2 = params.query || {};
    if (params.teamId) {
        query2["sys.team.sys.id"] = params.teamId;
    }
    return get(http4, `/organizations/${params.organizationId}/team_space_memberships`, {
        params: params.query
    });
};
var create40 = (http4, params, rawData, headers) => {
    return post(http4, getBaseUrl25(params), rawData, {
        headers: {
            "x-contentful-team": params.teamId,
            ...headers
        }
    });
};
var update31 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getEntityUrl6(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version || 0,
            "x-contentful-team": rawData.sys.team.sys.id,
            ...headers
        }
    });
};
var del41 = (http4, params) => {
    return del(http4, getEntityUrl6(params));
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/ui-config.mjs
var ui_config_exports = {};
__export(ui_config_exports, {
    get: () => get65,
    update: () => update32
});
var getUrl = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/ui_config`;
var get65 = (http4, params) => {
    return get(http4, getUrl(params));
};
var update32 = (http4, params, rawData) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getUrl(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/upload-credentials.mjs
var upload_credentials_exports = {};
__export(upload_credentials_exports, {
    create: () => create41
});
var getBaseUrl26 = params => {
    return `/spaces/${params.spaceId}/environments/${params.environmentId ?? "master"}/upload_credentials`;
};
var create41 = (http4, params) => {
    const httpUpload = getUploadHttpClient(http4);
    const path = getBaseUrl26(params);
    return post(httpUpload, path);
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/usage.mjs
var usage_exports = {};
__export(usage_exports, {
    getManyForOrganization: () => getManyForOrganization7,
    getManyForSpace: () => getManyForSpace2
});
var getManyForSpace2 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/space_periodic_usages`, {
        params: params.query
    });
};
var getManyForOrganization7 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/organization_periodic_usages`, {
        params: params.query
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/user.mjs
var user_exports = {};
__export(user_exports, {
    getCurrent: () => getCurrent,
    getForOrganization: () => getForOrganization4,
    getForSpace: () => getForSpace,
    getManyForOrganization: () => getManyForOrganization8,
    getManyForSpace: () => getManyForSpace3
});
var getForSpace = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/users/${params.userId}`);
};
var getCurrent = (http4, params) => get(http4, `/users/me`, { params: params?.query });
var getManyForSpace3 = (http4, params) => {
    return get(http4, `/spaces/${params.spaceId}/users`, {
        params: params.query
    });
};
var getForOrganization4 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/users/${params.userId}`);
};
var getManyForOrganization8 = (http4, params) => {
    return get(http4, `/organizations/${params.organizationId}/users`, {
        params: params.query
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/user-ui-config.mjs
var user_ui_config_exports = {};
__export(user_ui_config_exports, {
    get: () => get66,
    update: () => update33
});
var getUrl2 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/ui_config/me`;
var get66 = (http4, params) => {
    return get(http4, getUrl2(params));
};
var update33 = (http4, params, rawData) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getUrl2(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0
        }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/webhook.mjs
var webhook_exports = {};
__export(webhook_exports, {
    create: () => create42,
    createWithId: () => createWithId15,
    del: () => del42,
    deleteRetryPolicy: () => deleteRetryPolicy,
    deleteSigningSecret: () => deleteSigningSecret,
    get: () => get67,
    getCallDetails: () => getCallDetails2,
    getHealthStatus: () => getHealthStatus,
    getMany: () => getMany51,
    getManyCallDetails: () => getManyCallDetails,
    getRetryPolicy: () => getRetryPolicy,
    getSigningSecret: () => getSigningSecret,
    update: () => update34,
    upsertRetryPolicy: () => upsertRetryPolicy,
    upsertSigningSecret: () => upsertSigningSecret
});
var getBaseUrl27 = params => `/spaces/${params.spaceId}/webhook_definitions`;
var getWebhookCallBaseUrl = params => `/spaces/${params.spaceId}/webhooks`;
var getWebhookUrl = params => `${getBaseUrl27(params)}/${params.webhookDefinitionId}`;
var getWebhookCallUrl = params => `${getWebhookCallBaseUrl(params)}/${params.webhookDefinitionId}/calls`;
var getWebhookCallDetailsUrl = params => `${getWebhookCallBaseUrl(params)}/${params.webhookDefinitionId}/calls/${params.callId}`;
var getWebhookHealthUrl = params => `${getWebhookCallBaseUrl(params)}/${params.webhookDefinitionId}/health`;
var getWebhookSettingsUrl = params => `/spaces/${params.spaceId}/webhook_settings`;
var getWebhookSigningSecretUrl = params => `${getWebhookSettingsUrl(params)}/signing_secret`;
var getWebhookRetryPolicyUrl = params => `${getWebhookSettingsUrl(params)}/retry_policy`;
var get67 = (http4, params) => {
    return get(http4, getWebhookUrl(params));
};
var getManyCallDetails = (http4, params) => {
    return get(http4, getWebhookCallUrl(params), {
        params: normalizeSelect(params.query)
    });
};
var getCallDetails2 = (http4, params) => {
    return get(http4, getWebhookCallDetailsUrl(params));
};
var getHealthStatus = (http4, params) => {
    return get(http4, getWebhookHealthUrl(params));
};
var getMany51 = (http4, params) => {
    return get(http4, getBaseUrl27(params), {
        params: normalizeSelect(params.query)
    });
};
var getSigningSecret = (http4, params) => {
    return get(http4, getWebhookSigningSecretUrl(params));
};
var getRetryPolicy = (http4, params) => {
    return get(http4, getWebhookRetryPolicyUrl(params));
};
var create42 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return post(http4, getBaseUrl27(params), data, { headers });
};
var createWithId15 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return put(http4, getWebhookUrl(params), data, { headers });
};
var update34 = async (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getWebhookUrl(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var upsertSigningSecret = async (http4, params, rawData) => {
    const data = index(rawData);
    return put(http4, getWebhookSigningSecretUrl(params), data);
};
var upsertRetryPolicy = async (http4, params, rawData) => {
    const data = index(rawData);
    return put(http4, getWebhookRetryPolicyUrl(params), data);
};
var del42 = (http4, params) => {
    return del(http4, getWebhookUrl(params));
};
var deleteSigningSecret = async (http4, params) => {
    return del(http4, getWebhookSigningSecretUrl(params));
};
var deleteRetryPolicy = async (http4, params) => {
    return del(http4, getWebhookRetryPolicyUrl(params));
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/workflow.mjs
var workflow_exports = {};
__export(workflow_exports, {
    complete: () => complete,
    create: () => create43,
    del: () => del43,
    get: () => get68,
    getMany: () => getMany52,
    update: () => update35
});
var getBaseUrl28 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/workflows`;
var getWorkflowUrl = params => `${getBaseUrl28(params)}/${params.workflowId}`;
var completeWorkflowUrl = params => `${getWorkflowUrl(params)}/complete`;
var getMany52 = (http4, params, headers) =>
    get(http4, getBaseUrl28(params), {
        headers,
        params: params.query
    });
var get68 = (http4, params, headers) =>
    get(http4, getWorkflowUrl(params), {
        headers
    });
var create43 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return post(http4, getBaseUrl28(params), data, {
        headers
    });
};
var update35 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getWorkflowUrl(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del43 = (http4, { version, ...params }, headers) => {
    return del(http4, getWorkflowUrl(params), {
        headers: { "X-Contentful-Version": version, ...headers }
    });
};
var complete = (http4, { version, ...params }, headers) => {
    return put(http4, completeWorkflowUrl(params), null, {
        headers: { "X-Contentful-Version": version, ...headers }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/workflow-definition.mjs
var workflow_definition_exports = {};
__export(workflow_definition_exports, {
    create: () => create44,
    del: () => del44,
    get: () => get69,
    getMany: () => getMany53,
    update: () => update36
});
var getBaseUrl29 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/workflow_definitions`;
var getWorkflowDefinitionUrl = params => `${getBaseUrl29(params)}/${params.workflowDefinitionId}`;
var get69 = (http4, params, headers) =>
    get(http4, getWorkflowDefinitionUrl(params), {
        headers
    });
var getMany53 = (http4, params, headers) =>
    get(http4, getBaseUrl29(params), {
        headers,
        params: params.query
    });
var create44 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    return post(http4, getBaseUrl29(params), data, {
        headers
    });
};
var update36 = (http4, params, rawData, headers) => {
    const data = index(rawData);
    delete data.sys;
    return put(http4, getWorkflowDefinitionUrl(params), data, {
        headers: {
            "X-Contentful-Version": rawData.sys.version ?? 0,
            ...headers
        }
    });
};
var del44 = (http4, { version, ...params }, headers) => {
    return del(http4, getWorkflowDefinitionUrl(params), {
        headers: { "X-Contentful-Version": version, ...headers }
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/workflows-changelog.mjs
var workflows_changelog_exports = {};
__export(workflows_changelog_exports, {
    getMany: () => getMany54
});
var getBaseUrl30 = params => `/spaces/${params.spaceId}/environments/${params.environmentId}/workflows_changelog`;
var getMany54 = (http4, params, headers) =>
    get(http4, getBaseUrl30(params), {
        headers,
        params: params.query
    });

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/endpoints/index.mjs
var endpoints = {
    AiAction: ai_action_exports,
    AiActionInvocation: ai_action_invocation_exports,
    Agent: agent_exports,
    AgentRun: agent_run_exports,
    ApiKey: api_key_exports,
    AutomationDefinition: automation_definition_exports,
    AutomationExecution: automation_execution_exports,
    AppAction: app_action_exports,
    AppActionCall: app_action_call_exports,
    AppBundle: app_bundle_exports,
    AppDefinition: app_definition_exports,
    AppInstallation: app_installation_exports,
    AppUpload: app_upload_exports,
    AppSignedRequest: app_signed_request_exports,
    AppSigningSecret: app_signing_secret_exports,
    AppEventSubscription: app_event_subscription_exports,
    AppKey: app_key_exports,
    AppAccessToken: app_access_token_exports,
    AppDetails: app_details_exports,
    Asset: asset_exports,
    AssetKey: asset_key_exports,
    AvailableLicense: available_license_exports,
    BulkAction: bulk_action_exports,
    Comment: comment_exports,
    ComponentType: component_type_exports,
    Concept: concept_exports,
    ConceptScheme: concept_scheme_exports,
    ContentType: content_type_exports,
    EditorInterface: editor_interface_exports,
    EligibleLicense: eligible_license_exports,
    Entry: entry_exports,
    Environment: environment_exports,
    EnvironmentAlias: environment_alias_exports,
    EnvironmentTemplate: environment_template_exports,
    EnvironmentTemplateInstallation: environment_template_installation_exports,
    Extension: extension_exports,
    Function: function_exports,
    FunctionLog: function_log_exports,
    Http: http_exports,
    Locale: locale_exports,
    Organization: organization_exports,
    OrganizationInvitation: organization_invitation_exports,
    OrganizationMembership: organization_membership_exports,
    OAuthApplication: oauth_application_exports,
    PersonalAccessToken: personal_access_token_exports,
    AccessToken: access_token_exports,
    PreviewApiKey: preview_api_key_exports,
    Release: release_exports,
    ReleaseAsset: release_asset_exports,
    ReleaseEntry: release_entry_exports,
    ReleaseAction: release_action_exports,
    Resource: resource_exports,
    ResourceProvider: resource_provider_exports,
    ResourceType: resource_type_exports,
    Role: role_exports,
    ScheduledAction: scheduled_action_exports,
    ContentSemanticsIndex: content_semantics_index_exports,
    SemanticDuplicates: semantic_duplicates_exports,
    SemanticRecommendations: semantic_recommendations_exports,
    SemanticReferenceSuggestions: semantic_reference_suggestions_exports,
    SemanticSearch: semantic_search_exports,
    SemanticSettings: semantic_settings_exports,
    Snapshot: snapshot_exports,
    Space: space_exports,
    SpaceAddOn: space_add_on_exports,
    SpaceMember: space_member_exports,
    SpaceMembership: space_membership_exports,
    Tag: tag_exports,
    Task: task_exports,
    Team: team_exports,
    TeamMembership: team_membership_exports,
    TeamSpaceMembership: team_space_membership_exports,
    UIConfig: ui_config_exports,
    Upload: upload_exports,
    UploadCredential: upload_credentials_exports,
    Usage: usage_exports,
    User: user_exports,
    UserUIConfig: user_ui_config_exports,
    Webhook: webhook_exports,
    WorkflowDefinition: workflow_definition_exports,
    Workflow: workflow_exports,
    WorkflowsChangelog: workflows_changelog_exports
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/make-request.mjs
var makeRequest = async ({ axiosInstance, entityType, action: actionInput, params, payload, headers, userAgent }) => {
    const action = actionInput === "delete" ? "del" : actionInput;
    const endpoint =
        // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        endpoints[entityType]?.[action];
    if (endpoint === void 0) {
        throw new Error("Unknown endpoint");
    }
    return await endpoint(axiosInstance, params, payload, {
        ...headers,
        // overwrite the userAgent with the one passed in the request
        ...(userAgent ? { "X-Contentful-User-Agent": userAgent } : {})
    });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/adapters/REST/rest-adapter.mjs
var defaultHostParameters = {
    defaultHostname: "api.contentful.com",
    defaultHostnameUpload: "upload.contentful.com"
};
var RestAdapter = class {
    constructor(params) {
        if (!params.accessToken) {
            throw new TypeError("Expected parameter accessToken");
        }
        const copiedParams = index(params);
        copiedParams.httpAgent = params.httpAgent;
        copiedParams.httpsAgent = params.httpsAgent;
        this.params = {
            ...defaultHostParameters,
            ...copiedParams
        };
        this.axiosInstance = (0, import_contentful_sdk_core5.createHttpClient)(axios_default, {
            ...this.params,
            headers: {
                "Content-Type": "application/vnd.contentful.management.v1+json",
                // possibly define a default user agent?
                ...(params.userAgent ? { "X-Contentful-User-Agent": params.userAgent } : {}),
                ...this.params.headers
            }
        });
    }
    async makeRequest(opts) {
        return makeRequest({ ...opts, axiosInstance: this.axiosInstance });
    }
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-adapter.mjs
function createAdapter(params) {
    if ("apiAdapter" in params) {
        return params.apiAdapter;
    } else {
        return new RestAdapter(params);
    }
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-contentful-api.mjs
var import_contentful_sdk_core80 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/space.mjs
var import_contentful_sdk_core57 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-space-api.mjs
var import_contentful_sdk_core56 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/eligible-license.mjs
var import_contentful_sdk_core6 = __toESM(require_cjs2(), 1);
function wrapEligibleLicense(makeRequest2, data) {
    return (0, import_contentful_sdk_core6.toPlainObject)(index(data));
}
var wrapEligibleLicenseCollection = wrapCollection(wrapEligibleLicense);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/environment.mjs
var import_contentful_sdk_core42 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/enhance-with-methods.mjs
function enhanceWithMethods(baseObject, methodsObject) {
    return Object.keys(methodsObject).reduce((enhancedObject, methodName) => {
        Object.defineProperty(enhancedObject, methodName, {
            enumerable: false,
            configurable: true,
            writable: false,
            value: methodsObject[methodName]
        });
        return enhancedObject;
    }, baseObject);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-environment-api.mjs
var import_contentful_sdk_core41 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/release-action.mjs
var import_contentful_sdk_core7 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/methods/utils.mjs
function sleep(durationMs) {
    return new Promise(resolve => setTimeout(resolve, durationMs));
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/methods/action.mjs
var DEFAULT_MAX_RETRIES = 30;
var DEFAULT_INITIAL_DELAY_MS = 1e3;
var DEFAULT_RETRY_INTERVAL_MS = 2e3;
var AsyncActionProcessingError = class extends Error {
    constructor(message, action) {
        super(message);
        this.action = action;
        this.name = this.constructor.name;
    }
};
var AsyncActionFailedError = class extends AsyncActionProcessingError {};
async function pollAsyncActionStatus(actionFunction, options) {
    let retryCount = 0;
    let done = false;
    let action;
    const maxRetries = options?.retryCount ?? DEFAULT_MAX_RETRIES;
    const retryIntervalMs = options?.retryIntervalMs ?? DEFAULT_RETRY_INTERVAL_MS;
    const initialDelayMs = options?.initialDelayMs ?? DEFAULT_INITIAL_DELAY_MS;
    const throwOnFailedExecution = options?.throwOnFailedExecution ?? true;
    await sleep(initialDelayMs);
    while (retryCount < maxRetries && !done) {
        action = await actionFunction();
        if (action && ["succeeded", "failed"].includes(action.sys.status)) {
            done = true;
            if (action.sys.status === "failed" && throwOnFailedExecution) {
                throw new AsyncActionFailedError(`${action.sys.type} failed to execute.`, action);
            }
            return action;
        }
        await sleep(retryIntervalMs);
        retryCount += 1;
    }
    throw new AsyncActionProcessingError(`${action?.sys.type} didn't finish processing within the expected timeframe.`, action);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/release-action.mjs
function createReleaseActionApi(makeRequest2) {
    const getParams3 = self2 => {
        const action = self2.toPlainObject();
        return {
            spaceId: action.sys.space.sys.id,
            environmentId: action.sys.environment.sys.id,
            releaseId: action.sys.release.sys.id,
            actionId: action.sys.id
        };
    };
    return {
        async get() {
            const params = getParams3(this);
            return makeRequest2({
                entityType: "ReleaseAction",
                action: "get",
                params
            }).then(releaseAction => wrapReleaseAction(makeRequest2, releaseAction));
        },
        /** Waits for a Release Action to complete */
        async waitProcessing(options) {
            return pollAsyncActionStatus(async () => this.get(), options);
        }
    };
}
function wrapReleaseAction(makeRequest2, data) {
    const releaseAction = (0, import_contentful_sdk_core7.toPlainObject)(index(data));
    const releaseActionWithApiMethods = enhanceWithMethods(releaseAction, createReleaseActionApi(makeRequest2));
    return (0, import_contentful_sdk_core7.freezeSys)(releaseActionWithApiMethods);
}
var wrapReleaseActionCollection = wrapCollection(wrapReleaseAction);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/release.mjs
var import_contentful_sdk_core8 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/common-types.mjs
var ScheduledActionReferenceFilters;
(function (ScheduledActionReferenceFilters2) {
    ScheduledActionReferenceFilters2["contentTypeAnnotationNotIn"] = "sys.contentType.metadata.annotations.ContentType[nin]";
})(ScheduledActionReferenceFilters || (ScheduledActionReferenceFilters = {}));

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/release.mjs
function createReleaseApi(makeRequest2) {
    const getParams3 = self2 => {
        const release = self2.toPlainObject();
        return {
            spaceId: release.sys.space.sys.id,
            environmentId: release.sys.environment.sys.id,
            releaseId: release.sys.id,
            version: release.sys.version
        };
    };
    return {
        async archive() {
            const params = getParams3(this);
            return makeRequest2({
                entityType: "Release",
                action: "archive",
                params
            }).then(release => wrapRelease(makeRequest2, release));
        },
        async unarchive() {
            const params = getParams3(this);
            return makeRequest2({
                entityType: "Release",
                action: "unarchive",
                params
            }).then(release => wrapRelease(makeRequest2, release));
        },
        async update(payload) {
            const params = getParams3(this);
            return makeRequest2({
                entityType: "Release",
                action: "update",
                params,
                payload
            }).then(release => wrapRelease(makeRequest2, release));
        },
        async delete() {
            const params = getParams3(this);
            await makeRequest2({
                entityType: "Release",
                action: "delete",
                params
            });
        },
        async publish(options) {
            const params = getParams3(this);
            return makeRequest2({
                entityType: "Release",
                action: "publish",
                params
            })
                .then(data => wrapReleaseAction(makeRequest2, data))
                .then(action => action.waitProcessing(options));
        },
        async unpublish(options) {
            const params = getParams3(this);
            return makeRequest2({
                entityType: "Release",
                action: "unpublish",
                params
            })
                .then(data => wrapReleaseAction(makeRequest2, data))
                .then(action => action.waitProcessing(options));
        },
        async validate(options) {
            const params = getParams3(this);
            return makeRequest2({
                entityType: "Release",
                action: "validate",
                params,
                payload: options?.payload
            })
                .then(data => wrapReleaseAction(makeRequest2, data))
                .then(action => action.waitProcessing(options?.processingOptions));
        }
    };
}
function wrapRelease(makeRequest2, data) {
    const release = (0, import_contentful_sdk_core8.toPlainObject)(index(data));
    const releaseWithApiMethods = enhanceWithMethods(release, createReleaseApi(makeRequest2));
    return (0, import_contentful_sdk_core8.freezeSys)(releaseWithApiMethods);
}
var wrapReleaseCollection = wrapCursorPaginatedCollection(wrapRelease);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/tag.mjs
var import_contentful_sdk_core9 = __toESM(require_cjs2(), 1);
function createTagApi(makeRequest2) {
    const getParams3 = tag => ({
        spaceId: tag.sys.space.sys.id,
        environmentId: tag.sys.environment.sys.id,
        tagId: tag.sys.id
    });
    return {
        update: function () {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Tag",
                action: "update",
                params: getParams3(raw),
                payload: raw
            }).then(data => wrapTag(makeRequest2, data));
        },
        delete: function () {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Tag",
                action: "delete",
                params: {
                    ...getParams3(raw),
                    version: raw.sys.version
                }
            }).then(() => {});
        }
    };
}
function wrapTag(makeRequest2, data) {
    const tag = (0, import_contentful_sdk_core9.toPlainObject)(index(data));
    const tagWithMethods = enhanceWithMethods(tag, createTagApi(makeRequest2));
    return (0, import_contentful_sdk_core9.freezeSys)(tagWithMethods);
}
var wrapTagCollection = wrapCollection(wrapTag);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/ui-config.mjs
var import_contentful_sdk_core10 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-ui-config-api.mjs
function createUIConfigApi(makeRequest2) {
    const getParams3 = self2 => {
        const uiConfig = self2.toPlainObject();
        return {
            params: {
                spaceId: uiConfig.sys.space.sys.id,
                environmentId: uiConfig.sys.environment.sys.id
            },
            raw: uiConfig
        };
    };
    return {
        /**
         * Sends an update to the server with any changes made to the object's properties
         * @returns Object returned from the server with updated changes.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getUIConfig())
         * .then((uiConfig) => {
         *   uiConfig.entryListViews = [...]
         *   return uiConfig.update()
         * })
         * .then((uiConfig) => console.log(`UIConfig updated.`))
         * .catch(console.error)
         * ```
         */
        update: async function update37() {
            const { raw, params } = getParams3(this);
            const data = await makeRequest2({
                entityType: "UIConfig",
                action: "update",
                params,
                payload: raw
            });
            return wrapUIConfig(makeRequest2, data);
        }
    };
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/ui-config.mjs
function wrapUIConfig(makeRequest2, data) {
    const user = (0, import_contentful_sdk_core10.toPlainObject)(index(data));
    const userWithMethods = enhanceWithMethods(user, createUIConfigApi(makeRequest2));
    return (0, import_contentful_sdk_core10.freezeSys)(userWithMethods);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/user-ui-config.mjs
var import_contentful_sdk_core11 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-user-ui-config-api.mjs
function createUserUIConfigApi(makeRequest2) {
    const getParams3 = self2 => {
        const userUIConfig = self2.toPlainObject();
        return {
            params: {
                spaceId: userUIConfig.sys.space.sys.id,
                environmentId: userUIConfig.sys.environment.sys.id
            },
            raw: userUIConfig
        };
    };
    return {
        /**
         * Sends an update to the server with any changes made to the object's properties
         * @returns Object returned from the server with updated changes.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getUserUIConfig())
         * .then((uiConfig) => {
         *   uiConfig.entryListViews = [...]
         *   return uiConfig.update()
         * })
         * .then((uiConfig) => console.log(`UserUIConfig updated.`))
         * .catch(console.error)
         * ```
         */
        update: async function update37() {
            const { raw, params } = getParams3(this);
            const data = await makeRequest2({
                entityType: "UserUIConfig",
                action: "update",
                params,
                payload: raw
            });
            return wrapUserUIConfig(makeRequest2, data);
        }
    };
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/user-ui-config.mjs
function wrapUserUIConfig(makeRequest2, data) {
    const user = (0, import_contentful_sdk_core11.toPlainObject)(index(data));
    const userWithMethods = enhanceWithMethods(user, createUserUIConfigApi(makeRequest2));
    return (0, import_contentful_sdk_core11.freezeSys)(userWithMethods);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/environment-template-installation.mjs
var import_contentful_sdk_core12 = __toESM(require_cjs2(), 1);
var EnvironmentTemplateInstallationStatuses;
(function (EnvironmentTemplateInstallationStatuses2) {
    EnvironmentTemplateInstallationStatuses2["created"] = "created";
    EnvironmentTemplateInstallationStatuses2["inProgress"] = "inProgress";
    EnvironmentTemplateInstallationStatuses2["failed"] = "failed";
    EnvironmentTemplateInstallationStatuses2["succeeded"] = "succeeded";
    EnvironmentTemplateInstallationStatuses2["disconnected"] = "disconnected";
    EnvironmentTemplateInstallationStatuses2["inRetry"] = "inRetry";
})(EnvironmentTemplateInstallationStatuses || (EnvironmentTemplateInstallationStatuses = {}));
function wrapEnvironmentTemplateInstallation(makeRequest2, data) {
    const environmentTemplate = (0, import_contentful_sdk_core12.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core12.freezeSys)(environmentTemplate);
}
var wrapEnvironmentTemplateInstallationCollection = wrapCursorPaginatedCollection(wrapEnvironmentTemplateInstallation);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/function.mjs
var import_contentful_sdk_core13 = __toESM(require_cjs2(), 1);
function createFunctionApi(makeRequest2) {
    return {
        getFunction: function getFunction() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Function",
                action: "get",
                params: {
                    organizationId: raw.sys.organization.sys.id,
                    appDefinitionId: raw.sys.appDefinition.sys.id,
                    functionId: raw.sys.id
                }
            }).then(data => wrapFunction(makeRequest2, data));
        },
        getManyFunctions: function getManyFunctions() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Function",
                action: "getMany",
                params: {
                    appDefinitionId: raw.sys.appDefinition.sys.id,
                    organizationId: raw.sys.organization.sys.id
                }
            }).then(data => wrapFunctionCollection(makeRequest2, data));
        },
        getManyFunctionsForEnvironment(spaceId, environmentId, appInstallationId) {
            return makeRequest2({
                entityType: "Function",
                action: "getManyForEnvironment",
                params: {
                    spaceId,
                    environmentId,
                    appInstallationId
                }
            }).then(data => wrapFunctionCollection(makeRequest2, data));
        }
    };
}
function wrapFunction(makeRequest2, data) {
    const func = (0, import_contentful_sdk_core13.toPlainObject)(index(data));
    const funcWithMethods = enhanceWithMethods(func, createFunctionApi(makeRequest2));
    return (0, import_contentful_sdk_core13.freezeSys)(funcWithMethods);
}
var wrapFunctionCollection = wrapCollection(wrapFunction);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/function-log.mjs
var import_contentful_sdk_core14 = __toESM(require_cjs2(), 1);
function wrapFunctionLog(makeRequest2, data) {
    const functionLog = (0, import_contentful_sdk_core14.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core14.freezeSys)(functionLog);
}
var wrapFunctionLogCollection = wrapCollection(wrapFunctionLog);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/content-type.mjs
var import_contentful_sdk_core17 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/plain/checks.mjs
var isPublished = data => !!data.sys.publishedVersion;
var isUpdated = data => {
    return !!(data.sys.publishedVersion && data.sys.version > data.sys.publishedVersion + 1);
};
var isDraft = data => !data.sys.publishedVersion;
var isArchived = data => !!data.sys.archivedVersion;

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/editor-interface.mjs
var import_contentful_sdk_core15 = __toESM(require_cjs2(), 1);
function createEditorInterfaceApi(makeRequest2) {
    return {
        update: function () {
            const self2 = this;
            const raw = self2.toPlainObject();
            return makeRequest2({
                entityType: "EditorInterface",
                action: "update",
                params: {
                    spaceId: self2.sys.space.sys.id,
                    environmentId: self2.sys.environment.sys.id,
                    contentTypeId: self2.sys.contentType.sys.id
                },
                payload: raw
            }).then(response => wrapEditorInterface(makeRequest2, response));
        },
        getControlForField: function (fieldId) {
            const self2 = this;
            const result = (self2.controls || []).filter(control => {
                return control.fieldId === fieldId;
            });
            return result && result.length > 0 ? result[0] : null;
        }
    };
}
function wrapEditorInterface(makeRequest2, data) {
    const editorInterface = (0, import_contentful_sdk_core15.toPlainObject)(index(data));
    const editorInterfaceWithMethods = enhanceWithMethods(editorInterface, createEditorInterfaceApi(makeRequest2));
    return (0, import_contentful_sdk_core15.freezeSys)(editorInterfaceWithMethods);
}
var wrapEditorInterfaceCollection = wrapCollection(wrapEditorInterface);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/snapshot.mjs
var import_contentful_sdk_core16 = __toESM(require_cjs2(), 1);
function createSnapshotApi() {
    return {/* In case the snapshot object evolve later */};
}
function wrapSnapshot(_makeRequest, data) {
    const snapshot = (0, import_contentful_sdk_core16.toPlainObject)(index(data));
    const snapshotWithMethods = enhanceWithMethods(snapshot, createSnapshotApi());
    return (0, import_contentful_sdk_core16.freezeSys)(snapshotWithMethods);
}
var wrapSnapshotCollection = wrapCollection(wrapSnapshot);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/methods/content-type.mjs
var findAndUpdateField = function (contentType, fieldId, omitOrDelete) {
    const field = contentType.fields.find(field2 => field2.id === fieldId);
    if (!field) {
        return Promise.reject(new Error(`Tried to omitAndDeleteField on a nonexistent field, ${fieldId}, on the content type ${contentType.name}.`));
    }
    field[omitOrDelete] = true;
    return Promise.resolve(contentType);
};
var omitAndDeleteField = (makeRequest2, { fieldId, ...params }, contentType) => {
    return findAndUpdateField(contentType, fieldId, "omitted")
        .then(newContentType => {
            return makeRequest2({
                entityType: "ContentType",
                action: "update",
                params,
                payload: newContentType
            });
        })
        .then(newContentType => {
            return makeRequest2({
                entityType: "ContentType",
                action: "publish",
                params,
                payload: newContentType
            });
        })
        .then(newContentType => {
            return findAndUpdateField(newContentType, fieldId, "deleted");
        })
        .then(newContentType => {
            return makeRequest2({
                entityType: "ContentType",
                action: "update",
                params,
                payload: newContentType
            });
        });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/content-type.mjs
function createContentTypeApi(makeRequest2) {
    const getParams3 = self2 => {
        const contentType = self2.toPlainObject();
        return {
            raw: contentType,
            params: {
                spaceId: contentType.sys.space.sys.id,
                environmentId: contentType.sys.environment.sys.id,
                contentTypeId: contentType.sys.id
            }
        };
    };
    return {
        update: function () {
            const { raw, params } = getParams3(this);
            return makeRequest2({
                entityType: "ContentType",
                action: "update",
                params,
                payload: raw
            }).then(data => wrapContentType(makeRequest2, data));
        },
        delete: function () {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "ContentType",
                action: "delete",
                params
            }).then(() => {});
        },
        publish: function () {
            const { raw, params } = getParams3(this);
            return makeRequest2({
                entityType: "ContentType",
                action: "publish",
                params,
                payload: raw
            }).then(data => wrapContentType(makeRequest2, data));
        },
        unpublish: function () {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "ContentType",
                action: "unpublish",
                params
            }).then(data => wrapContentType(makeRequest2, data));
        },
        getEditorInterface: function () {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "EditorInterface",
                action: "get",
                params
            }).then(data => wrapEditorInterface(makeRequest2, data));
        },
        getSnapshots: function (query2 = {}) {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Snapshot",
                action: "getManyForContentType",
                params: { ...params, query: query2 }
            }).then(data => wrapSnapshotCollection(makeRequest2, data));
        },
        getSnapshot: function (snapshotId) {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Snapshot",
                action: "getForContentType",
                params: { ...params, snapshotId }
            }).then(data => wrapSnapshot(makeRequest2, data));
        },
        isPublished: function () {
            return isPublished(this);
        },
        isUpdated: function () {
            return isUpdated(this);
        },
        isDraft: function () {
            return isDraft(this);
        },
        omitAndDeleteField: function (fieldId) {
            const { raw, params } = getParams3(this);
            return omitAndDeleteField(makeRequest2, { ...params, fieldId }, raw).then(data => wrapContentType(makeRequest2, data));
        }
    };
}
function wrapContentType(makeRequest2, data) {
    const contentType = (0, import_contentful_sdk_core17.toPlainObject)(index(data));
    const contentTypeWithMethods = enhanceWithMethods(contentType, createContentTypeApi(makeRequest2));
    return (0, import_contentful_sdk_core17.freezeSys)(contentTypeWithMethods);
}
var wrapContentTypeCollection = wrapCollection(wrapContentType);
var wrapContentTypeCursorPaginatedCollection = wrapCursorPaginatedCollection(wrapContentType);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/entry.mjs
var import_contentful_sdk_core20 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/task.mjs
var import_contentful_sdk_core18 = __toESM(require_cjs2(), 1);
function createTaskApi(makeRequest2) {
    const getParams3 = task => {
        const parentEntity = task.sys.parentEntity;
        return {
            spaceId: task.sys.space.sys.id,
            environmentId: task.sys.environment.sys.id,
            parentEntityType: parentEntity.sys.linkType,
            parentEntityId: parentEntity.sys.id,
            taskId: task.sys.id
        };
    };
    return {
        update: function () {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Task",
                action: "update",
                params: getParams3(raw),
                payload: raw
            }).then(data => wrapTask(makeRequest2, data));
        },
        delete: function () {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Task",
                action: "delete",
                params: {
                    ...getParams3(raw),
                    version: raw.sys.version
                }
            }).then(() => {});
        }
    };
}
function wrapTask(makeRequest2, data) {
    const task = (0, import_contentful_sdk_core18.toPlainObject)(index(data));
    const taskWithMethods = enhanceWithMethods(task, createTaskApi(makeRequest2));
    return (0, import_contentful_sdk_core18.freezeSys)(taskWithMethods);
}
var wrapTaskCollection = wrapCollection(wrapTask);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/comment.mjs
var import_contentful_sdk_core19 = __toESM(require_cjs2(), 1);
var CommentNode;
(function (CommentNode2) {
    CommentNode2["Document"] = "document";
    CommentNode2["Paragraph"] = "paragraph";
    CommentNode2["Mention"] = "mention";
})(CommentNode || (CommentNode = {}));
function createCommentApi(makeRequest2) {
    const getParams3 = comment => {
        const parentEntity = comment.sys.parentEntity;
        return {
            spaceId: comment.sys.space.sys.id,
            environmentId: comment.sys.environment.sys.id,
            commentId: comment.sys.id,
            parentEntityType: parentEntity.sys.linkType,
            parentEntityId: parentEntity.sys.id
        };
    };
    return {
        update: async function () {
            const raw = this.toPlainObject();
            const data = await makeRequest2({
                entityType: "Comment",
                action: "update",
                params: getParams3(raw),
                payload: raw
            });
            return wrapComment(makeRequest2, data);
        },
        delete: async function () {
            const raw = this.toPlainObject();
            await makeRequest2({
                entityType: "Comment",
                action: "delete",
                params: {
                    ...getParams3(raw),
                    version: raw.sys.version
                }
            });
        }
    };
}
function wrapComment(makeRequest2, data) {
    const comment = (0, import_contentful_sdk_core19.toPlainObject)(index(data));
    const commentWithMethods = enhanceWithMethods(comment, createCommentApi(makeRequest2));
    return (0, import_contentful_sdk_core19.freezeSys)(commentWithMethods);
}
var wrapCommentCollection = wrapCollection(wrapComment);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-entry-api.mjs
function createEntryApi(makeRequest2) {
    const getParams3 = self2 => {
        const entry = self2.toPlainObject();
        return {
            params: {
                spaceId: entry.sys.space.sys.id,
                environmentId: entry.sys.environment.sys.id,
                entryId: entry.sys.id
            },
            raw: entry
        };
    };
    return {
        /**
         * Sends an update to the server with any changes made to the object's properties
         * @returns Object returned from the server with updated changes.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getEntry('<entry_id>'))
         * .then((entry) => {
         *   entry.fields.title['en-US'] = 'New entry title'
         *   return entry.update()
         * })
         * .then((entry) => console.log(`Entry ${entry.sys.id} updated.`))
         * .catch(console.error)
         * ```
         */
        update: function update37() {
            const { raw, params } = getParams3(this);
            return makeRequest2({
                entityType: "Entry",
                action: "update",
                params,
                payload: raw
            }).then(data => wrapEntry(makeRequest2, data));
        },
        /**
         * Sends an JSON patch to the server with any changes made to the object's properties
         * @returns Object returned from the server with updated changes.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getEntry('<entry_id>'))
         * .then((entry) => entry.patch([
         *   {
         *     op: 'replace',
         *     path: '/fields/title/en-US',
         *     value: 'New entry title'
         *   }
         * ]))
         * .then((entry) => console.log(`Entry ${entry.sys.id} updated.`))
         * .catch(console.error)
         * ```
         */
        patch: function patch7(ops) {
            const { raw, params } = getParams3(this);
            return makeRequest2({
                entityType: "Entry",
                action: "patch",
                params: {
                    ...params,
                    version: raw.sys.version
                },
                payload: ops
            }).then(data => wrapEntry(makeRequest2, data));
        },
        /**
         * Deletes this object on the server.
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getEntry('<entry_id>'))
         * .then((entry) => entry.delete())
         * .then(() => console.log(`Entry deleted.`))
         * .catch(console.error)
         * ```
         */
        delete: function del45() {
            const { params } = getParams3(this);
            return makeRequest2({ entityType: "Entry", action: "delete", params });
        },
        /**
         * Publishes the object
         * @returns Object returned from the server with updated metadata.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getEntry('<entry_id>'))
         * .then((entry) => entry.publish())
         * .then((entry) => console.log(`Entry ${entry.sys.id} published.`))
         * .catch(console.error)
         * ```
         */
        publish: function publish7() {
            const { raw, params } = getParams3(this);
            return makeRequest2({
                entityType: "Entry",
                action: "publish",
                params,
                payload: raw
            }).then(data => wrapEntry(makeRequest2, data));
        },
        /**
         * Unpublishes the object
         * @returns Object returned from the server with updated metadata.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getEntry('<entry_id>'))
         * .then((entry) => entry.unpublish())
         * .then((entry) => console.log(`Entry ${entry.sys.id} unpublished.`))
         * .catch(console.error)
         * ```
         */
        unpublish: function unpublish7() {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Entry",
                action: "unpublish",
                params
            }).then(data => wrapEntry(makeRequest2, data));
        },
        /**
         * Archives the object
         * @returns Object returned from the server with updated metadata.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getEntry('<entry_id>'))
         * .then((entry) => entry.archive())
         * .then((entry) => console.log(`Entry ${entry.sys.id} archived.`))
         * .catch(console.error)
         * ```
         */
        archive: function archive4() {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Entry",
                action: "archive",
                params
            }).then(data => wrapEntry(makeRequest2, data));
        },
        /**
         * Unarchives the object
         * @returns Object returned from the server with updated metadata.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getEntry('<entry_id>'))
         * .then((entry) => entry.unarchive())
         * .then((entry) => console.log(`Entry ${entry.sys.id} unarchived.`))
         * .catch(console.error)
         * ```
         */
        unarchive: function unarchive5() {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Entry",
                action: "unarchive",
                params
            }).then(data => wrapEntry(makeRequest2, data));
        },
        /**
         * Gets all snapshots of an entry
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getEntry('<entry_id>'))
         * .then((entry) => entry.getSnapshots())
         * .then((snapshots) => console.log(snapshots.items))
         * .catch(console.error)
         * ```
         */
        getSnapshots: function (query2 = {}) {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Snapshot",
                action: "getManyForEntry",
                params: { ...params, query: query2 }
            }).then(data => wrapSnapshotCollection(makeRequest2, data));
        },
        /**
         * Gets a snapshot of an entry
         * @param snapshotId - Id of the snapshot
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getEntry('<entry_id>'))
         * .then((entry) => entry.getSnapshot('<snapshot_id>'))
         * .then((snapshot) => console.log(snapshot))
         * .catch(console.error)
         * ```
         */
        getSnapshot: function (snapshotId) {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Snapshot",
                action: "getForEntry",
                params: { ...params, snapshotId }
            }).then(data => wrapSnapshot(makeRequest2, data));
        },
        /**
         * Creates a new comment for an entry
         * @param data Object representation of the Comment to be created
         * @returns Promise for the newly created Comment
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getEntry('<entry-id>'))
         * .then((entry) => entry.createComment({
         *   body: 'Something left to do'
         * }))
         * .then((comment) => console.log(comment))
         * .catch(console.error)
         * ```
         */
        createComment: function (data) {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Comment",
                action: "create",
                params: {
                    spaceId: params.spaceId,
                    environmentId: params.environmentId,
                    parentEntityId: params.entryId,
                    parentEntityType: "Entry"
                },
                payload: data
            }).then(data2 => wrapComment(makeRequest2, data2));
        },
        /**
         * Gets all comments of an entry
         * @returns
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getEntry('<entry-id>'))
         * .then((entry) => entry.getComments())
         * .then((comments) => console.log(comments))
         * .catch(console.error)
         * ```
         */
        getComments: function () {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Comment",
                action: "getMany",
                params
            }).then(data => wrapCommentCollection(makeRequest2, data));
        },
        /**
         * Gets a comment of an entry
         * @returns
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getEntry('<entry-id>'))
         * .then((entry) => entry.getComment(`<comment-id>`))
         * .then((comment) => console.log(comment))
         * .catch(console.error)
         * ```
         */
        getComment: function (id) {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Comment",
                action: "get",
                params: {
                    ...params,
                    commentId: id
                }
            }).then(data => wrapComment(makeRequest2, data));
        },
        /**
         * Creates a new task for an entry
         * @param data Object representation of the Task to be created
         * @returns Promise for the newly created Task
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getEntry('<entry-id>'))
         * .then((entry) => entry.createTask({
         *   body: 'Something left to do',
         *   assignedTo: '<user-id>',
         *   status: 'active'
         * }))
         * .then((task) => console.log(task))
         * .catch(console.error)
         * ```
         */
        createTask: function (data) {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Task",
                action: "create",
                params,
                payload: data
            }).then(data2 => wrapTask(makeRequest2, data2));
        },
        /**
         * Gets all tasks of an entry
         * @returns
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getEntry('<entry-id>'))
         * .then((entry) => entry.getTasks())
         * .then((tasks) => console.log(tasks))
         * .catch(console.error)
         * ```
         */
        getTasks: function (query2 = {}) {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Task",
                action: "getMany",
                params: { ...params, query: query2 }
            }).then(data => wrapTaskCollection(makeRequest2, data));
        },
        /**
         * Gets a task of an entry
         * @returns
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getEntry('<entry-id>'))
         * .then((entry) => entry.getTask(`<task-id>`))
         * .then((task) => console.log(task))
         * .catch(console.error)
         * ```
         */
        getTask: function (id) {
            const { params } = getParams3(this);
            return makeRequest2({
                entityType: "Task",
                action: "get",
                params: {
                    ...params,
                    taskId: id
                }
            }).then(data => wrapTask(makeRequest2, data));
        },
        /**
         * Checks if the entry is published. A published entry might have unpublished changes
         */
        isPublished: function isPublished$1() {
            const raw = this.toPlainObject();
            return isPublished(raw);
        },
        /**
         * Checks if the entry is updated. This means the entry was previously published but has unpublished changes.
         */
        isUpdated: function isUpdated$1() {
            const raw = this.toPlainObject();
            return isUpdated(raw);
        },
        /**
         * Checks if the entry is in draft mode. This means it is not published.
         */
        isDraft: function isDraft$1() {
            const raw = this.toPlainObject();
            return isDraft(raw);
        },
        /**
         * Checks if entry is archived. This means it's not exposed to the Delivery/Preview APIs.
         */
        isArchived: function isArchived$1() {
            const raw = this.toPlainObject();
            return isArchived(raw);
        },
        /**
         * Recursively collects references of an entry and their descendants
         */
        references: function references2(options) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Entry",
                action: "references",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.environment.sys.id,
                    entryId: raw.sys.id,
                    include: options?.include
                }
            }).then(response => wrapEntryCollection(makeRequest2, response));
        }
    };
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/entry.mjs
function wrapEntry(makeRequest2, data) {
    const entry = (0, import_contentful_sdk_core20.toPlainObject)(index(data));
    const entryWithMethods = enhanceWithMethods(entry, createEntryApi(makeRequest2));
    return (0, import_contentful_sdk_core20.freezeSys)(entryWithMethods);
}
var wrapEntryCollection = wrapCollection(wrapEntry);
var wrapEntryTypeCursorPaginatedCollection = wrapCursorPaginatedCollection(wrapEntry);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/asset.mjs
var import_contentful_sdk_core21 = __toESM(require_cjs2(), 1);
function createAssetApi(makeRequest2) {
    const getParams3 = raw => {
        return {
            spaceId: raw.sys.space.sys.id,
            environmentId: raw.sys.environment.sys.id,
            assetId: raw.sys.id
        };
    };
    return {
        processForLocale: function processForLocale3(locale, options) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "processForLocale",
                params: {
                    ...getParams3(raw),
                    locale,
                    options,
                    asset: raw
                }
            }).then(data => wrapAsset(makeRequest2, data));
        },
        processForAllLocales: function processForAllLocales3(options) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "processForAllLocales",
                params: {
                    ...getParams3(raw),
                    asset: raw,
                    options
                }
            }).then(data => wrapAsset(makeRequest2, data));
        },
        update: function update37() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "update",
                params: getParams3(raw),
                payload: raw,
                headers: {}
            }).then(data => wrapAsset(makeRequest2, data));
        },
        delete: function del45() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "delete",
                params: getParams3(raw)
            });
        },
        publish: function publish7() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "publish",
                params: getParams3(raw),
                payload: raw
            }).then(data => wrapAsset(makeRequest2, data));
        },
        unpublish: function unpublish7() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "unpublish",
                params: getParams3(raw)
            }).then(data => wrapAsset(makeRequest2, data));
        },
        archive: function archive4() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "archive",
                params: getParams3(raw)
            }).then(data => wrapAsset(makeRequest2, data));
        },
        unarchive: function unarchive5() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "unarchive",
                params: getParams3(raw)
            }).then(data => wrapAsset(makeRequest2, data));
        },
        isPublished: function isPublished$1() {
            const raw = this.toPlainObject();
            return isPublished(raw);
        },
        isUpdated: function isUpdated$1() {
            const raw = this.toPlainObject();
            return isUpdated(raw);
        },
        isDraft: function isDraft$1() {
            const raw = this.toPlainObject();
            return isDraft(raw);
        },
        isArchived: function isArchived$1() {
            const raw = this.toPlainObject();
            return isArchived(raw);
        }
    };
}
function wrapAsset(makeRequest2, data) {
    const asset = (0, import_contentful_sdk_core21.toPlainObject)(index(data));
    const assetWithMethods = enhanceWithMethods(asset, createAssetApi(makeRequest2));
    return (0, import_contentful_sdk_core21.freezeSys)(assetWithMethods);
}
var wrapAssetCollection = wrapCollection(wrapAsset);
var wrapAssetTypeCursorPaginatedCollection = wrapCursorPaginatedCollection(wrapAsset);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/asset-key.mjs
var import_contentful_sdk_core22 = __toESM(require_cjs2(), 1);
function wrapAssetKey(_makeRequest, data) {
    const assetKey = (0, import_contentful_sdk_core22.toPlainObject)(index(data));
    return assetKey;
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/locale.mjs
var import_contentful_sdk_core23 = __toESM(require_cjs2(), 1);
function createLocaleApi(makeRequest2) {
    const getParams3 = locale => ({
        spaceId: locale.sys.space.sys.id,
        environmentId: locale.sys.environment.sys.id,
        localeId: locale.sys.id
    });
    return {
        update: function () {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Locale",
                action: "update",
                params: getParams3(raw),
                payload: raw
            }).then(data => wrapLocale(makeRequest2, data));
        },
        delete: function () {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Locale",
                action: "delete",
                params: getParams3(raw)
            }).then(() => {});
        }
    };
}
function wrapLocale(makeRequest2, data) {
    delete data.internal_code;
    const locale = (0, import_contentful_sdk_core23.toPlainObject)(index(data));
    const localeWithMethods = enhanceWithMethods(locale, createLocaleApi(makeRequest2));
    return (0, import_contentful_sdk_core23.freezeSys)(localeWithMethods);
}
var wrapLocaleCollection = wrapCollection(wrapLocale);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/upload.mjs
var import_contentful_sdk_core24 = __toESM(require_cjs2(), 1);
function createUploadApi(makeRequest2) {
    return {
        delete: async function del45() {
            const raw = this.toPlainObject();
            await makeRequest2({
                entityType: "Upload",
                action: "delete",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    uploadId: raw.sys.id
                }
            });
        }
    };
}
function wrapUpload(makeRequest2, data) {
    const upload = (0, import_contentful_sdk_core24.toPlainObject)(index(data));
    const uploadWithMethods = enhanceWithMethods(upload, createUploadApi(makeRequest2));
    return (0, import_contentful_sdk_core24.freezeSys)(uploadWithMethods);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/extension.mjs
var import_contentful_sdk_core25 = __toESM(require_cjs2(), 1);
function createExtensionApi(makeRequest2) {
    const getParams3 = data => ({
        spaceId: data.sys.space.sys.id,
        environmentId: data.sys.environment.sys.id,
        extensionId: data.sys.id
    });
    return {
        update: function update37() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "Extension",
                action: "update",
                params: getParams3(data),
                payload: data
            }).then(response => wrapExtension(makeRequest2, response));
        },
        delete: function del45() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "Extension",
                action: "delete",
                params: getParams3(data)
            });
        }
    };
}
function wrapExtension(makeRequest2, data) {
    const extension = (0, import_contentful_sdk_core25.toPlainObject)(index(data));
    const extensionWithMethods = enhanceWithMethods(extension, createExtensionApi(makeRequest2));
    return (0, import_contentful_sdk_core25.freezeSys)(extensionWithMethods);
}
var wrapExtensionCollection = wrapCollection(wrapExtension);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-installation.mjs
var import_contentful_sdk_core26 = __toESM(require_cjs2(), 1);
function createAppInstallationApi(makeRequest2) {
    const getParams3 = data => ({
        spaceId: data.sys.space.sys.id,
        environmentId: data.sys.environment.sys.id,
        appDefinitionId: data.sys.appDefinition.sys.id
    });
    return {
        update: function update37() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "AppInstallation",
                action: "upsert",
                params: getParams3(data),
                headers: {},
                payload: data
            }).then(data2 => wrapAppInstallation(makeRequest2, data2));
        },
        delete: function del45() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "AppInstallation",
                action: "delete",
                params: getParams3(data)
            });
        }
    };
}
function wrapAppInstallation(makeRequest2, data) {
    const appInstallation = (0, import_contentful_sdk_core26.toPlainObject)(index(data));
    const appInstallationWithMethods = enhanceWithMethods(appInstallation, createAppInstallationApi(makeRequest2));
    return (0, import_contentful_sdk_core26.freezeSys)(appInstallationWithMethods);
}
var wrapAppInstallationCollection = wrapCollection(wrapAppInstallation);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-signed-request.mjs
var import_contentful_sdk_core27 = __toESM(require_cjs2(), 1);
function wrapAppSignedRequest(_makeRequest, data) {
    const signedRequest = (0, import_contentful_sdk_core27.toPlainObject)(index(data));
    return signedRequest;
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-action-call.mjs
var import_contentful_sdk_core28 = __toESM(require_cjs2(), 1);
function createAppActionCallApi(makeRequest2, retryOptions) {
    return {
        createWithResponse: function (params, payload) {
            return makeRequest2({
                entityType: "AppActionCall",
                action: "createWithResponse",
                params: { ...params, ...retryOptions },
                payload
            }).then(data => wrapAppActionCallResponse(makeRequest2, data));
        },
        getCallDetails: function getCallDetails3(params) {
            return makeRequest2({
                entityType: "AppActionCall",
                action: "getCallDetails",
                params
            }).then(data => wrapAppActionCallResponse(makeRequest2, data));
        },
        get: function get70(params) {
            return makeRequest2({
                entityType: "AppActionCall",
                action: "get",
                params
            }).then(data => wrapAppActionCall(makeRequest2, data));
        },
        createWithResult: function (params, payload) {
            return makeRequest2({
                entityType: "AppActionCall",
                action: "createWithResult",
                params: { ...params, ...retryOptions },
                payload
            }).then(data => wrapAppActionCall(makeRequest2, data));
        }
    };
}
function wrapAppActionCall(makeRequest2, data) {
    const signedRequest = (0, import_contentful_sdk_core28.toPlainObject)(index(data));
    const signedRequestWithMethods = enhanceWithMethods(signedRequest, createAppActionCallApi(makeRequest2));
    return signedRequestWithMethods;
}
function wrapAppActionCallResponse(makeRequest2, data, retryOptions) {
    const appActionCallResponse = (0, import_contentful_sdk_core28.toPlainObject)(index(data));
    const appActionCallResponseWithMethods = enhanceWithMethods(appActionCallResponse, createAppActionCallApi(makeRequest2, retryOptions));
    return appActionCallResponseWithMethods;
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/bulk-action.mjs
var import_contentful_sdk_core29 = __toESM(require_cjs2(), 1);
var BulkActionStatus;
(function (BulkActionStatus2) {
    BulkActionStatus2["created"] = "created";
    BulkActionStatus2["inProgress"] = "inProgress";
    BulkActionStatus2["succeeded"] = "succeeded";
    BulkActionStatus2["failed"] = "failed";
})(BulkActionStatus || (BulkActionStatus = {}));
Object.values(BulkActionStatus);
function createBulkActionApi(makeRequest2) {
    const getParams3 = self2 => {
        const bulkAction = self2.toPlainObject();
        return {
            spaceId: bulkAction.sys.space.sys.id,
            environmentId: bulkAction.sys.environment.sys.id,
            bulkActionId: bulkAction.sys.id
        };
    };
    return {
        async get() {
            const params = getParams3(this);
            return makeRequest2({
                entityType: "BulkAction",
                action: "get",
                params
            }).then(bulkAction => wrapBulkAction(makeRequest2, bulkAction));
        },
        async waitProcessing(options) {
            return pollAsyncActionStatus(async () => this.get(), options);
        }
    };
}
function wrapBulkAction(makeRequest2, data) {
    const bulkAction = (0, import_contentful_sdk_core29.toPlainObject)(index(data));
    const bulkActionWithApiMethods = enhanceWithMethods(bulkAction, createBulkActionApi(makeRequest2));
    return (0, import_contentful_sdk_core29.freezeSys)(bulkActionWithApiMethods);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-access-token.mjs
var import_contentful_sdk_core30 = __toESM(require_cjs2(), 1);
function wrapAppAccessToken(_makeRequest, data) {
    const appAccessToken = (0, import_contentful_sdk_core30.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core30.freezeSys)(appAccessToken);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/resource-type.mjs
var import_contentful_sdk_core31 = __toESM(require_cjs2(), 1);
function createResourceTypeApi(makeRequest2) {
    return {
        /**
         * Sends an update to the server with any changes made to the object's properties
         * @returns Object returned from the server with updated changes.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinition('<app_def_id>'))
         * .then((appDefinition) => appDefinition.getResourceType())
         * .then((resourceType) => {
         *    resourceType.name = '<new_name>'
         *    return resourceType.upsert()
         * })
         * .catch(console.error)
         * ```
         */
        upsert: function upsert7() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "ResourceType",
                action: "upsert",
                params: getParams(data),
                headers: {},
                payload: getUpsertParams(data)
            }).then(data2 => wrapResourceType(makeRequest2, data2));
        },
        /**
         * Deletes this object on the server.
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinition('<app_def_id>'))
         * .then((appDefinition) => appDefinition.getResourceType())
         * .then((resourceType) => resourceType.delete())
         * .catch(console.error)
         * ```
         */
        delete: function del45() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "ResourceType",
                action: "delete",
                params: getParams(data)
            });
        }
    };
}
var getParams = data => ({
    organizationId: data.sys.organization.sys.id,
    appDefinitionId: data.sys.appDefinition.sys.id,
    resourceTypeId: data.sys.id
});
var getUpsertParams = data => ({
    name: data.name,
    defaultFieldMapping: data.defaultFieldMapping
});
function wrapResourceType(makeRequest2, data) {
    const resourceType = (0, import_contentful_sdk_core31.toPlainObject)(index(data));
    const ResourceTypeWithMethods = enhanceWithMethods(resourceType, createResourceTypeApi(makeRequest2));
    return (0, import_contentful_sdk_core31.freezeSys)(ResourceTypeWithMethods);
}
function wrapResourceTypeforEnvironment(makeRequest2, data) {
    const resourceType = (0, import_contentful_sdk_core31.toPlainObject)(data);
    return (0, import_contentful_sdk_core31.freezeSys)(resourceType);
}
var wrapResourceTypesForEnvironmentCollection = wrapCursorPaginatedCollection(wrapResourceTypeforEnvironment);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/resource.mjs
var import_contentful_sdk_core32 = __toESM(require_cjs2(), 1);
function wrapResource(makeRequest2, data) {
    const resource = (0, import_contentful_sdk_core32.toPlainObject)(data);
    return (0, import_contentful_sdk_core32.freezeSys)(resource);
}
var wrapResourceCollection = wrapCursorPaginatedCollection(wrapResource);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/ai-action-invocation.mjs
var import_contentful_sdk_core33 = __toESM(require_cjs2(), 1);
function wrapAiActionInvocation(makeRequest2, data) {
    const invocation = (0, import_contentful_sdk_core33.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core33.freezeSys)(invocation);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/agent.mjs
var import_contentful_sdk_core35 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/agent-run.mjs
var import_contentful_sdk_core34 = __toESM(require_cjs2(), 1);
function wrapAgentRun(_makeRequest, data) {
    const agentRun = (0, import_contentful_sdk_core34.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core34.freezeSys)(agentRun);
}
function wrapAgentGenerateResponse(_makeRequest, data) {
    const response = (0, import_contentful_sdk_core34.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core34.freezeSys)(response);
}
var wrapAgentRunCollection = wrapCollection(wrapAgentRun);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/agent.mjs
function createAgentApi(makeRequest2) {
    const getParams3 = data => ({
        spaceId: data.sys.space.sys.id,
        environmentId: data.sys.environment.sys.id,
        agentId: data.sys.id
    });
    return {
        generate: function generate2(payload) {
            const self2 = this;
            return makeRequest2({
                entityType: "Agent",
                action: "generate",
                params: getParams3(self2),
                payload
            }).then(data => wrapAgentGenerateResponse(makeRequest2, data));
        }
    };
}
function wrapAgent(makeRequest2, data) {
    const agent = (0, import_contentful_sdk_core35.toPlainObject)(index(data));
    const agentWithMethods = enhanceWithMethods(agent, createAgentApi(makeRequest2));
    return (0, import_contentful_sdk_core35.freezeSys)(agentWithMethods);
}
var wrapAgentCollection = wrapCollection(wrapAgent);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/semantic-duplicates.mjs
var import_contentful_sdk_core36 = __toESM(require_cjs2(), 1);
function wrapSemanticDuplicates(_makeRequest, data) {
    const result = (0, import_contentful_sdk_core36.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core36.freezeSys)(result);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/semantic-recommendations.mjs
var import_contentful_sdk_core37 = __toESM(require_cjs2(), 1);
function wrapSemanticRecommendations(_makeRequest, data) {
    const result = (0, import_contentful_sdk_core37.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core37.freezeSys)(result);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/semantic-reference-suggestions.mjs
var import_contentful_sdk_core38 = __toESM(require_cjs2(), 1);
function wrapSemanticReferenceSuggestions(_makeRequest, data) {
    const result = (0, import_contentful_sdk_core38.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core38.freezeSys)(result);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/semantic-search.mjs
var import_contentful_sdk_core39 = __toESM(require_cjs2(), 1);
function wrapSemanticSearch(_makeRequest, data) {
    const result = (0, import_contentful_sdk_core39.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core39.freezeSys)(result);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/content-semantics-index.mjs
var import_contentful_sdk_core40 = __toESM(require_cjs2(), 1);
function wrapContentSemanticsIndex(_makeRequest, data) {
    const result = (0, import_contentful_sdk_core40.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core40.freezeSys)(result);
}
function wrapContentSemanticsIndexCollection(_makeRequest, data) {
    const result = (0, import_contentful_sdk_core40.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core40.freezeSys)(result);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-environment-api.mjs
function createEnvironmentApi(makeRequest2) {
    return {
        /**
         * Deletes the environment
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.delete())
         * .then(() => console.log('Environment deleted.'))
         * .catch(console.error)
         * ```
         */
        delete: function deleteEnvironment() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Environment",
                action: "delete",
                params: { spaceId: raw.sys.space.sys.id, environmentId: raw.sys.id }
            }).then(() => {});
        },
        /**
         * Updates the environment
         * @returns Promise for the updated environment.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => {
         *   environment.name = 'New name'
         *   return environment.update()
         * })
         * .then((environment) => console.log(`Environment ${environment.sys.id} renamed.`)
         * .catch(console.error)
         * ```
         */
        update: function updateEnvironment() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Environment",
                action: "update",
                params: { spaceId: raw.sys.space.sys.id, environmentId: raw.sys.id },
                payload: raw
            }).then(data => wrapEnvironment(makeRequest2, data));
        },
        /**
         * Creates SDK Entry object (locally) from entry data
         * @param entryData - Entry Data
         * @returns Entry
         * @example ```javascript
         * environment.getEntry('entryId').then(entry => {
         *
         *   // Build a plainObject in order to make it usable for React (saving in state or redux)
         *   const plainObject = entry.toPlainObject();
         *
         *   // The entry is being updated in some way as plainObject:
         *   const updatedPlainObject = {
         *     ...plainObject,
         *     fields: {
         *       ...plainObject.fields,
         *       title: {
         *         'en-US': 'updatedTitle'
         *       }
         *     }
         *   };
         *
         *   // Rebuild an sdk object out of the updated plainObject:
         *   const entryWithMethodsAgain = environment.getEntryFromData(updatedPlainObject);
         *
         *   // Update with help of the sdk method:
         *   entryWithMethodsAgain.update();
         *
         * });
         * ```
         **/
        getEntryFromData(entryData) {
            return wrapEntry(makeRequest2, entryData);
        },
        /**
         * Creates SDK Asset object (locally) from entry data
         * @param assetData - Asset ID
         * @returns Asset
         * @example ```javascript
         * environment.getAsset('asset_id').then(asset => {
         *
         *   // Build a plainObject in order to make it usable for React (saving in state or redux)
         *   const plainObject = asset.toPlainObject();
         *
         *   // The asset is being updated in some way as plainObject:
         *   const updatedPlainObject = {
         *     ...plainObject,
         *     fields: {
         *       ...plainObject.fields,
         *       title: {
         *         'en-US': 'updatedTitle'
         *       }
         *     }
         *   };
         *
         *   // Rebuild an sdk object out of the updated plainObject:
         *   const assetWithMethodsAgain = environment.getAssetFromData(updatedPlainObject);
         *
         *   // Update with help of the sdk method:
         *   assetWithMethodsAgain.update();
         *
         * });
         * ```
         */
        getAssetFromData(assetData) {
            return wrapAsset(makeRequest2, assetData);
        },
        /**
         *
         * @description Get a BulkAction by ID.
         *  See: https://www.contentful.com/developers/docs/references/content-management-api/#/reference/bulk-actions/bulk-action
         * @param bulkActionId - ID of the BulkAction to fetch
         * @returns - Promise with the BulkAction
         *
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getBulkAction('<bulk_action_id>'))
         * .then((bulkAction) => console.log(bulkAction))
         * ```
         */
        getBulkAction(bulkActionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "BulkAction",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    bulkActionId
                }
            }).then(data => wrapBulkAction(makeRequest2, data));
        },
        /**
         * @description Creates a BulkAction that will attempt to publish all items contained in the payload.
         * See: https://www.contentful.com/developers/docs/references/content-management-api/#/reference/bulk-actions/publish-bulk-action
         * @param {BulkActionPayload} payload - Object containing the items to be processed in the bulkAction
         * @returns - Promise with the BulkAction
         *
         * @example
         *
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * const payload = {
         *  entities: {
         *    sys: { type: 'Array' }
         *    items: [
         *      { sys: { type: 'Link', id: '<entry-id>', linkType: 'Entry', version: 2 } }
         *    ]
         *  }
         * }
         *
         * // Using Thenables
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.createPublishBulkAction(payload))
         * .then((bulkAction) => console.log(bulkAction.waitProcessing()))
         * .catch(console.error)
         *
         * // Using async/await
         * try {
         *  const space = await client.getSpace('<space_id>')
         *  const environment = await space.getEnvironment('<environment_id>')
         *  const bulkActionInProgress = await environment.createPublishBulkAction(payload)
         *
         *  // You can wait for a recently created BulkAction to be processed by using `bulkAction.waitProcessing()`
         *  const bulkActionCompleted = await bulkActionInProgress.waitProcessing()
         *  console.log(bulkActionCompleted)
         * } catch (error) {
         *  console.log(error)
         * }
         * ```
         */
        createPublishBulkAction(payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "BulkAction",
                action: "publish",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload
            }).then(data => wrapBulkAction(makeRequest2, data));
        },
        /**
         * @description Creates a BulkAction that will attempt to validate all items contained in the payload.
         * See: https://www.contentful.com/developers/docs/references/content-management-api/#/reference/bulk-actions/validate-bulk-action
         * @param {BulkActionPayload} payload - Object containing the items to be processed in the bulkAction
         * @returns - Promise with the BulkAction
         *
         * @example
         *
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * const payload = {
         *  action: 'publish',
         *  entities: {
         *    sys: { type: 'Array' }
         *    items: [
         *      { sys: { type: 'Link', id: '<entry-id>', linkType: 'Entry' } }
         *    ]
         *  }
         * }
         *
         * // Using Thenables
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.createValidateBulkAction(payload))
         * .then((bulkAction) => console.log(bulkAction.waitProcessing()))
         * .catch(console.error)
         *
         * // Using async/await
         * try {
         *  const space = await client.getSpace('<space_id>')
         *  const environment = await space.getEnvironment('<environment_id>')
         *  const bulkActionInProgress = await environment.createValidateBulkAction(payload)
         *
         *  // You can wait for a recently created BulkAction to be processed by using `bulkAction.waitProcessing()`
         *  const bulkActionCompleted = await bulkActionInProgress.waitProcessing()
         *  console.log(bulkActionCompleted)
         * } catch (error) {
         *  console.log(error)
         * }
         * ```
         */
        createValidateBulkAction(payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "BulkAction",
                action: "validate",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload
            }).then(data => wrapBulkAction(makeRequest2, data));
        },
        /**
         * @description Creates a BulkAction that will attempt to unpublish all items contained in the payload.
         * See: https://www.contentful.com/developers/docs/references/content-management-api/#/reference/bulk-actions/unpublish-bulk-action
         * @param {BulkActionPayload} payload - Object containing the items to be processed in the bulkAction
         * @returns - Promise with the BulkAction
         *
         * @example
         *
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * const payload = {
         *  entities: {
         *    sys: { type: 'Array' }
         *    items: [
         *      { sys: { type: 'Link', id: 'entry-id', linkType: 'Entry' } }
         *    ]
         *  }
         * }
         *
         * // Using Thenables
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.createUnpublishBulkAction(payload))
         * .then((bulkAction) => console.log(bulkAction.waitProcessing()))
         * .catch(console.error)
         *
         * // Using async/await
         * try {
         *  const space = await clientgetSpace('<space_id>')
         *  const environment = await space.getEnvironment('<environment_id>')
         *  const bulkActionInProgress = await environment.createUnpublishBulkAction(payload)
         *
         *  // You can wait for a recently created BulkAction to be processed by using `bulkAction.waitProcessing()`
         *  const bulkActionCompleted = await bulkActionInProgress.waitProcessing()
         *  console.log(bulkActionCompleted)
         * } catch (error) {
         *  console.log(error)
         * }
         * ```
         */
        createUnpublishBulkAction(payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "BulkAction",
                action: "unpublish",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload
            }).then(data => wrapBulkAction(makeRequest2, data));
        },
        /**
         * Gets a Content Type
         * @param contentTypeId - Content Type ID
         * @returns Promise for a Content Type
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getContentType('<content_type_id>'))
         * .then((contentType) => console.log(contentType))
         * .catch(console.error)
         * ```
         */
        getContentType(contentTypeId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ContentType",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    contentTypeId
                }
            }).then(data => wrapContentType(makeRequest2, data));
        },
        /**
         * Gets a collection of Content Types
         * @param query - Object with search parameters. Check the <a href="https://www.contentful.com/developers/docs/javascript/tutorials/using-js-cda-sdk/#retrieving-entries-with-search-parameters">JS SDK tutorial</a> and the <a href="https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters">REST API reference</a> for more details.
         * @returns Promise for a collection of Content Types
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getContentTypes())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getContentTypes(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ContentType",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapContentTypeCollection(makeRequest2, data));
        },
        /**
         * Gets a collection of Content Types with cursor based pagination
         * @param query - Object with cursor pagination parameters. Check the <a href="https://www.contentful.com/developers/docs/references/content-management-api/#/introduction/cursor-pagination">REST API reference</a> for more details.
         * @returns Promise for a collection of Content Types
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getContentTypesWithCursor())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getContentTypesWithCursor(query2 = {}) {
            const raw = this.toPlainObject();
            const normalizedQueryParams = normalizeCursorPaginationParameters(query2);
            return makeRequest2({
                entityType: "ContentType",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: normalizedQueryParams }).params
                }
            }).then(data => wrapContentTypeCursorPaginatedCollection(makeRequest2, normalizeCursorPaginationResponse(data)));
        },
        /**
         * Creates a Content Type
         * @param data - Object representation of the Content Type to be created
         * @returns Promise for the newly created Content Type
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createContentType({
         *   name: 'Blog Post',
         *   fields: [
         *     {
         *       id: 'title',
         *       name: 'Title',
         *       required: true,
         *       localized: false,
         *       type: 'Text'
         *     }
         *   ]
         * }))
         * .then((contentType) => console.log(contentType))
         * .catch(console.error)
         * ```
         */
        createContentType(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ContentType",
                action: "create",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload: data
            }).then(response => wrapContentType(makeRequest2, response));
        },
        /**
         * Creates a Content Type with a custom ID
         * @param contentTypeId - Content Type ID
         * @param data - Object representation of the Content Type to be created
         * @returns Promise for the newly created Content Type
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createContentTypeWithId('<content-type-id>', {
         *   name: 'Blog Post',
         *   fields: [
         *     {
         *       id: 'title',
         *       name: 'Title',
         *       required: true,
         *       localized: false,
         *       type: 'Text'
         *     }
         *   ]
         * }))
         * .then((contentType) => console.log(contentType))
         * .catch(console.error)
         * ```
         */
        createContentTypeWithId(contentTypeId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ContentType",
                action: "createWithId",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    contentTypeId
                },
                payload: data
            }).then(response => wrapContentType(makeRequest2, response));
        },
        /**
         * Gets an EditorInterface for a ContentType
         * @param contentTypeId - Content Type ID
         * @returns Promise for an EditorInterface
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getEditorInterfaceForContentType('<content_type_id>'))
         * .then((EditorInterface) => console.log(EditorInterface))
         * .catch(console.error)
         * ```
         */
        getEditorInterfaceForContentType(contentTypeId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EditorInterface",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    contentTypeId
                }
            }).then(response => wrapEditorInterface(makeRequest2, response));
        },
        /**
         * Gets all EditorInterfaces
         * @returns Promise for a collection of EditorInterface
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getEditorInterfaces())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getEditorInterfaces() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EditorInterface",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                }
            }).then(response => wrapEditorInterfaceCollection(makeRequest2, response));
        },
        /**
         * Gets an Entry
         * Warning: if you are using the select operator, when saving, any field that was not selected will be removed
         * from your entry in the backend
         * @param id - Entry ID
         * @param query - Object with search parameters. In this method it's only useful for `locale`.
         * @returns Promise for an Entry
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getEntry('<entry-id>'))
         * .then((entry) => console.log(entry))
         * .catch(console.error)
         * ```
         */
        getEntry(id, query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Entry",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    entryId: id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapEntry(makeRequest2, data));
        },
        /**
         * Deletes an Entry of this environment
         * @param id - Entry ID
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.deleteEntry("4bmLXiuviAZH3jkj5DLRWE"))
         * .then(() => console.log('Entry deleted.'))
         * .catch(console.error)
         * ```
         */
        deleteEntry(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Entry",
                action: "delete",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    entryId: id
                }
            }).then(() => {});
        },
        /**
         * Gets a collection of Entries
         * Warning: if you are using the select operator, when saving, any field that was not selected will be removed
         * from your entry in the backend
         * @param query - Object with search parameters. Check the <a href="https://www.contentful.com/developers/docs/javascript/tutorials/using-js-cda-sdk/#retrieving-entries-with-search-parameters">JS SDK tutorial</a> and the <a href="https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters">REST API reference</a> for more details.
         * @returns Promise for a collection of Entries
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getEntries({'content_type': 'foo'})) // you can add more queries as 'key': 'value'
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getEntries(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Entry",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapEntryCollection(makeRequest2, data));
        },
        /**
         * Gets a collection of Entries with cursor based pagination
         * Warning: if you are using the select operator, when saving, any field that was not selected will be removed
         * from your entry in the backend
         * @param query - Object with cursor pagination parameters. Check the <a href="https://www.contentful.com/developers/docs/references/content-management-api/#/introduction/cursor-pagination">REST API reference</a> for more details.
         * @returns Promise for a collection of Entries
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getEntriesWithCursor({'content_type': 'foo'})) // you can add more queries as 'key': 'value'
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getEntriesWithCursor(query2 = {}) {
            const raw = this.toPlainObject();
            const normalizedQueryParams = normalizeCursorPaginationParameters(query2);
            return makeRequest2({
                entityType: "Entry",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: normalizedQueryParams }).params
                }
            }).then(data => wrapEntryTypeCursorPaginatedCollection(makeRequest2, normalizeCursorPaginationResponse(data)));
        },
        /**
         * Gets a collection of published Entries
         * @param query - Object with search parameters. Check the <a href="https://www.contentful.com/developers/docs/javascript/tutorials/using-js-cda-sdk/#retrieving-entries-with-search-parameters">JS SDK tutorial</a> and the <a href="https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters">REST API reference</a> for more details.
         * @returns Promise for a collection of published Entries
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getPublishedEntries({'content_type': 'foo'})) // you can add more queries as 'key': 'value'
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getPublishedEntries(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Entry",
                action: "getPublished",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapEntryCollection(makeRequest2, data));
        },
        /**
         * Gets a collection of published Entries with cursor based pagination
         * @param query - Object with cursor pagination parameters. Check the <a href="https://www.contentful.com/developers/docs/references/content-management-api/#/introduction/cursor-pagination">REST API reference</a> for more details.
         * @returns Promise for a collection of published Entries
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getPublishedEntriesWithCursor())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getPublishedEntriesWithCursor(query2 = {}) {
            const raw = this.toPlainObject();
            const normalizedQueryParams = normalizeCursorPaginationParameters(query2);
            return makeRequest2({
                entityType: "Entry",
                action: "getPublished",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: normalizedQueryParams }).params
                }
            }).then(data => wrapEntryTypeCursorPaginatedCollection(makeRequest2, normalizeCursorPaginationResponse(data)));
        },
        /**
         * Creates a Entry
         * @param contentTypeId - The Content Type ID of the newly created Entry
         * @param data - Object representation of the Entry to be created
         * @returns Promise for the newly created Entry
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createEntry('<content_type_id>', {
         *   fields: {
         *     title: {
         *       'en-US': 'Entry title'
         *     }
         *   }
         * }))
         * .then((entry) => console.log(entry))
         * .catch(console.error)
         * ```
         */
        createEntry(contentTypeId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Entry",
                action: "create",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    contentTypeId
                },
                payload: data
            }).then(response => wrapEntry(makeRequest2, response));
        },
        /**
         * Creates a Entry with a custom ID
         * @param contentTypeId - The Content Type of the newly created Entry
         * @param id - Entry ID
         * @param data - Object representation of the Entry to be created
         * @returns Promise for the newly created Entry
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * // Create entry
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createEntryWithId('<content_type_id>', '<entry_id>', {
         *   fields: {
         *     title: {
         *       'en-US': 'Entry title'
         *     }
         *   }
         * }))
         * .then((entry) => console.log(entry))
         * .catch(console.error)
         * ```
         */
        createEntryWithId(contentTypeId, id, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Entry",
                action: "createWithId",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    entryId: id,
                    contentTypeId
                },
                payload: data
            }).then(response => wrapEntry(makeRequest2, response));
        },
        /**
         * Get entry references
         * @param entryId - Entry ID
         * @param {Object} options.include - Level of the entry descendants from 1 up to 10 maximum
         * @returns Promise of Entry references
         * @example ```javascript
         * const contentful = require('contentful-management');
         *
         * const client = contentful.createClient({
         *  accessToken: '<contentful_management_api_key>
         * })
         *
         * // Get entry references
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getEntryReferences('<entry_id>', {include: number}))
         * .then((entry) => console.log(entry.includes))
         * // or
         * .then((environment) => environment.getEntry('<entry_id>')).then((entry) => entry.references({include: number}))
         * .catch(console.error)
         * ```
         */
        getEntryReferences(entryId, options) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Entry",
                action: "references",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    entryId,
                    include: options?.include
                }
            }).then(response => wrapEntryCollection(makeRequest2, response));
        },
        /**
         * Gets an Asset
         * Warning: if you are using the select operator, when saving, any field that was not selected will be removed
         * from your entry in the backend
         * @param id - Asset ID
         * @param query - Object with search parameters. In this method it's only useful for `locale`.
         * @returns Promise for an Asset
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getAsset('<asset_id>'))
         * .then((asset) => console.log(asset))
         * .catch(console.error)
         * ```
         */
        getAsset(id, query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    assetId: id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapAsset(makeRequest2, data));
        },
        /**
         * Gets a collection of Assets
         * Warning: if you are using the select operator, when saving, any field that was not selected will be removed
         * from your entry in the backend
         * @param query - Object with search parameters. Check the <a href="https://www.contentful.com/developers/docs/javascript/tutorials/using-js-cda-sdk/#retrieving-entries-with-search-parameters">JS SDK tutorial</a> and the <a href="https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters">REST API reference</a> for more details.
         * @returns Promise for a collection of Assets
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getAssets())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getAssets(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapAssetCollection(makeRequest2, data));
        },
        /**
         * Gets a collection of Assets with cursor based pagination
         * Warning: if you are using the select operator, when saving, any field that was not selected will be removed
         * from your entry in the backend
         * @param query - Object with cursor pagination parameters. Check the <a href="https://www.contentful.com/developers/docs/references/content-management-api/#/introduction/cursor-pagination">REST API reference</a> for more details.
         * @returns Promise for a collection of Assets
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getAssetsWithCursor())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getAssetsWithCursor(query2 = {}) {
            const raw = this.toPlainObject();
            const normalizedQueryParams = normalizeCursorPaginationParameters(query2);
            return makeRequest2({
                entityType: "Asset",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: normalizedQueryParams }).params
                }
            }).then(data => wrapAssetTypeCursorPaginatedCollection(makeRequest2, normalizeCursorPaginationResponse(data)));
        },
        /**
         * Gets a collection of published Assets
         * @param query - Object with search parameters. Check the <a href="https://www.contentful.com/developers/docs/javascript/tutorials/using-js-cda-sdk/#retrieving-entries-with-search-parameters">JS SDK tutorial</a> and the <a href="https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters">REST API reference</a> for more details.
         * @returns Promise for a collection of published Assets
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getPublishedAssets())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getPublishedAssets(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "getPublished",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapAssetCollection(makeRequest2, data));
        },
        /**
         * Gets a collection of published Assets with cursor based pagination
         * @param query - Object with cursor pagination parameters. Check the <a href="https://www.contentful.com/developers/docs/references/content-management-api/#/introduction/cursor-pagination">REST API reference</a> for more details.
         * @returns Promise for a collection of published Assets
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getPublishedAssetsWithCursor())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getPublishedAssetsWithCursor(query2 = {}) {
            const raw = this.toPlainObject();
            const normalizedQueryParams = normalizeCursorPaginationParameters(query2);
            return makeRequest2({
                entityType: "Asset",
                action: "getPublished",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: normalizedQueryParams }).params
                }
            }).then(data => wrapAssetTypeCursorPaginatedCollection(makeRequest2, normalizeCursorPaginationResponse(data)));
        },
        /**
         * Creates a Asset. After creation, call asset.processForLocale or asset.processForAllLocales to start asset processing.
         * @param data - Object representation of the Asset to be created. Note that the field object should have an upload property on asset creation, which will be removed and replaced with an url property when processing is finished.
         * @returns Promise for the newly created Asset
         * @example ```javascript
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * // Create asset
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createAsset({
         *   fields: {
         *     title: {
         *       'en-US': 'Playsam Streamliner'
         *    },
         *    file: {
         *       'en-US': {
         *         contentType: 'image/jpeg',
         *        fileName: 'example.jpeg',
         *        upload: 'https://example.com/example.jpg'
         *      }
         *    }
         *   }
         * }))
         * .then((asset) => asset.processForLocale("en-US")) // OR asset.processForAllLocales()
         * .then((asset) => console.log(asset))
         * .catch(console.error)
         * ```
         */
        createAsset(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "create",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload: data
            }).then(response => wrapAsset(makeRequest2, response));
        },
        /**
         * Creates a Asset with a custom ID. After creation, call asset.processForLocale or asset.processForAllLocales to start asset processing.
         * @param id - Asset ID
         * @param data - Object representation of the Asset to be created. Note that the field object should have an upload property on asset creation, which will be removed and replaced with an url property when processing is finished.
         * @returns Promise for the newly created Asset
         * @example ```javascript
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * // Create asset
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createAssetWithId('<asset_id>', {
         *   title: {
         *     'en-US': 'Playsam Streamliner'
         *   },
         *   file: {
         *     'en-US': {
         *       contentType: 'image/jpeg',
         *       fileName: 'example.jpeg',
         *       upload: 'https://example.com/example.jpg'
         *     }
         *   }
         * }))
         * .then((asset) => asset.process())
         * .then((asset) => console.log(asset))
         * .catch(console.error)
         * ```
         */
        createAssetWithId(id, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "createWithId",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    assetId: id
                },
                payload: data
            }).then(response => wrapAsset(makeRequest2, response));
        },
        /**
         * Creates a Asset based on files. After creation, call asset.processForLocale or asset.processForAllLocales to start asset processing.
         * @param data - Object representation of the Asset to be created. Note that the field object should have an uploadFrom property on asset creation, which will be removed and replaced with an url property when processing is finished.
         * @param data.fields.file.[LOCALE].file - Can be a string, an ArrayBuffer or a Stream.
         * @returns Promise for the newly created Asset
         * @example ```javascript
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createAssetFromFiles({
         *   fields: {
         *     file: {
         *       'en-US': {
         *          contentType: 'image/jpeg',
         *          fileName: 'filename_english.jpg',
         *          file: createReadStream('path/to/filename_english.jpg')
         *       },
         *       'de-DE': {
         *          contentType: 'image/svg+xml',
         *          fileName: 'filename_german.svg',
         *          file: '<svg><path fill="red" d="M50 50h150v50H50z"/></svg>'
         *       }
         *     }
         *   }
         * }))
         * .then((asset) => console.log(asset))
         * .catch(console.error)
         * ```
         */
        createAssetFromFiles(data, options) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Asset",
                action: "createFromFiles",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    uploadTimeout: options?.uploadTimeout
                },
                payload: data
            }).then(response => wrapAsset(makeRequest2, response));
        },
        /**
         * Creates an asset key for signing asset URLs (Embargoed Assets)
         * @param data Object with request payload
         * @param data.expiresAt number a UNIX timestamp in the future (but not more than 48 hours from time of calling)
         * @returns Promise for the newly created AssetKey
         * @example ```javascript
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * // Create assetKey
         * now = () => Math.floor(Date.now() / 1000)
         * const withExpiryIn1Hour = () => now() + 1 * 60 * 60
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createAssetKey({ expiresAt: withExpiryIn1Hour() }))
         * .then((policy, secret) => console.log({ policy, secret }))
         * .catch(console.error)
         * ```
         */
        createAssetKey(payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AssetKey",
                action: "create",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload
            }).then(data => wrapAssetKey(makeRequest2, data));
        },
        /**
         * Gets an Upload
         * @param id - Upload ID
         * @returns Promise for an Upload
         * @example ```javascript
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * const uploadStream = createReadStream('path/to/filename_english.jpg')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getUpload('<upload-id>')
         * .then((upload) => console.log(upload))
         * .catch(console.error)
         */
        getUpload(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Upload",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    uploadId: id
                }
            }).then(data => wrapUpload(makeRequest2, data));
        },
        /**
         * Creates a Upload.
         * @param data - Object with file information.
         * @param data.file - Actual file content. Can be a string, an ArrayBuffer or a Stream.
         * @returns Upload object containing information about the uploaded file.
         * @example ```javascript
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * const uploadStream = createReadStream('path/to/filename_english.jpg')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createUpload({file: uploadStream})
         * .then((upload) => console.log(upload))
         * .catch(console.error)
         * ```
         */
        createUpload: function createUpload(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Upload",
                action: "create",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload: data
            }).then(data2 => wrapUpload(makeRequest2, data2));
        },
        /**
         * Gets a Locale
         * @param localeId - Locale ID
         * @returns Promise for an Locale
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getLocale('<locale_id>'))
         * .then((locale) => console.log(locale))
         * .catch(console.error)
         * ```
         */
        getLocale(localeId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Locale",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    localeId
                }
            }).then(data => wrapLocale(makeRequest2, data));
        },
        /**
         * Gets a collection of Locales
         * @returns Promise for a collection of Locales
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getLocales())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getLocales(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Locale",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapLocaleCollection(makeRequest2, data));
        },
        /**
         * Creates a Locale
         * @param data - Object representation of the Locale to be created
         * @returns Promise for the newly created Locale
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * // Create locale
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createLocale({
         *   name: 'German (Austria)',
         *   code: 'de-AT',
         *   fallbackCode: 'de-DE',
         *   optional: true
         * }))
         * .then((locale) => console.log(locale))
         * .catch(console.error)
         * ```
         */
        createLocale(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Locale",
                action: "create",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload: data
            }).then(response => wrapLocale(makeRequest2, response));
        },
        /**
         * Gets an UI Extension
         * @param id - Extension ID
         * @returns Promise for an UI Extension
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getUiExtension('<extension-id>'))
         * .then((extension) => console.log(extension))
         * .catch(console.error)
         * ```
         */
        getUiExtension(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Extension",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    extensionId: id
                }
            }).then(data => wrapExtension(makeRequest2, data));
        },
        /**
         * Gets a collection of UI Extension
         * @returns Promise for a collection of UI Extensions
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getUiExtensions()
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getUiExtensions() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Extension",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                }
            }).then(response => wrapExtensionCollection(makeRequest2, response));
        },
        /**
         * Creates a UI Extension
         * @param data - Object representation of the UI Extension to be created
         * @returns Promise for the newly created UI Extension
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createUiExtension({
         *   extension: {
         *     name: 'My awesome extension',
         *     src: 'https://example.com/my',
         *     fieldTypes: [
         *       {
         *         type: 'Symbol'
         *       },
         *       {
         *         type: 'Text'
         *       }
         *     ],
         *     sidebar: false
         *   }
         * }))
         * .then((extension) => console.log(extension))
         * .catch(console.error)
         * ```
         */
        createUiExtension(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Extension",
                action: "create",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload: data
            }).then(response => wrapExtension(makeRequest2, response));
        },
        /**
         * Creates a UI Extension with a custom ID
         * @param id - Extension ID
         * @param data - Object representation of the UI Extension to be created
         * @returns Promise for the newly created UI Extension
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createUiExtensionWithId('<extension_id>', {
         *   extension: {
         *     name: 'My awesome extension',
         *     src: 'https://example.com/my',
         *     fieldTypes: [
         *       {
         *         type: 'Symbol'
         *       },
         *       {
         *         type: 'Text'
         *       }
         *     ],
         *     sidebar: false
         *   }
         * }))
         * .then((extension) => console.log(extension))
         * .catch(console.error)
         * ```
         */
        createUiExtensionWithId(id, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Extension",
                action: "createWithId",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    extensionId: id
                },
                payload: data
            }).then(response => wrapExtension(makeRequest2, response));
        },
        /**
         * Creates an App Installation
         * @param appDefinitionId - AppDefinition ID
         * @param data - AppInstallation data
         * @param options.acceptAllTerms - Flag for accepting Apps' Marketplace EULA, Terms, and Privacy policy (need to pass `{acceptAllTerms: true}` to install a marketplace app)
         * @returns Promise for an App Installation
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         *  .then((space) => space.getEnvironment('<environment-id>'))
         *  .then((environment) => environment.createAppInstallation('<app_definition_id>', {
         *    parameters: {
         *      someParameter: someValue
         *    }
         *   })
         *  .then((appInstallation) => console.log(appInstallation))
         *  .catch(console.error)
         *  ```
         */
        createAppInstallation(appDefinitionId, data, { acceptAllTerms } = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppInstallation",
                action: "upsert",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    appDefinitionId,
                    acceptAllTerms
                },
                payload: data
            }).then(payload => wrapAppInstallation(makeRequest2, payload));
        },
        /**
         * Gets an App Installation
         * @param id - AppDefinition ID
         * @returns Promise for an App Installation
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         *  .then((space) => space.getEnvironment('<environment-id>'))
         *  .then((environment) => environment.getAppInstallation('<app-definition-id>'))
         *  .then((appInstallation) => console.log(appInstallation))
         *  .catch(console.error)
         *  ```
         */
        getAppInstallation(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppInstallation",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    appDefinitionId: id
                }
            }).then(data => wrapAppInstallation(makeRequest2, data));
        },
        /**
         * Gets a collection of App Installation
         * @returns Promise for a collection of App Installations
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         *  .then((space) => space.getEnvironment('<environment-id>'))
         *  .then((environment) => environment.getAppInstallations()
         *  .then((response) => console.log(response.items))
         *  .catch(console.error)
         *  ```
         */
        getAppInstallations() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppInstallation",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                }
            }).then(data => wrapAppInstallationCollection(makeRequest2, data));
        },
        /**
         * Creates an app action call
         * @param appDefinitionId - AppDefinition ID
         * @param appActionId - action ID
         * @param data - App Action Call data
         * @returns Promise for an App Action Call
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * const data = {
         *   headers: {
         *     'x-my-header': 'some-value'
         *   },
         *   body: {
         *     'some-body-value': true
         *   }
         * }
         *
         * client.getSpace('<space_id>')
         *  .then((space) => space.getEnvironment('<environment-id>'))
         *  .then((environment) => environment.createAppActionCall('<app_definition_id>', '<action_id>', data)
         *  .then((appActionCall) => console.log(appActionCall))
         *  .catch(console.error)
         *  ```
         */
        createAppActionCall(appDefinitionId, appActionId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppActionCall",
                action: "create",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    appDefinitionId,
                    appActionId
                },
                payload: data
            }).then(payload => wrapAppActionCall(makeRequest2, payload));
        },
        /**
         * Gets the raw response (headers/body) for a completed App Action Call
         * @param appDefinitionId - AppDefinition ID
         * @param appActionId - App Action ID
         * @param callId - App Action Call ID
         * @returns Promise for the raw response object including `response.body` and optional `response.headers`
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client
         *   .getSpace('<space_id>')
         *   .then((space) => space.getEnvironment('<environment_id>'))
         *   .then((environment) => environment.getAppActionCallResponse('<app_definition_id>', '<app_action_id>', '<call_id>'))
         *   .then((raw) => console.log(raw.response.body))
         *   .catch(console.error)
         * ```
         */
        getAppActionCallResponse(appDefinitionId, appActionId, callId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppActionCall",
                action: "getResponse",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    appDefinitionId,
                    appActionId,
                    callId
                }
            });
        },
        /**
         * Creates an app signed request
         * @param appDefinitionId - AppDefinition ID
         * @param data - SignedRequest data
         * @returns Promise for a Signed Request
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * const data = {
         *   method: 'POST',
         *   path: '/request_path',
         *   body: '{ "key": "data" }',
         *   headers: {
         *     'x-my-header': 'some-value'
         *   },
         * }
         *
         * client.getSpace('<space_id>')
         *  .then((space) => space.getEnvironment('<environment-id>'))
         *  .then((environment) => environment.createAppSignedRequest('<app_definition_id>', data)
         *  .then((signedRequest) => console.log(signedRequest))
         *  .catch(console.error)
         *  ```
         */
        createAppSignedRequest(appDefinitionId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppSignedRequest",
                action: "create",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    appDefinitionId
                },
                payload: data
            }).then(payload => wrapAppSignedRequest(makeRequest2, payload));
        },
        /**
         * Creates an app access token
         * @param appDefinitionId - AppDefinition ID
         * @param data - Json Web Token
         * @returns Promise for an app access token
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const { sign } = require('jsonwebtoken')
         *
         * const signOptions = { algorithm: 'RS256', issuer: '<app_definition_id>', expiresIn: '10m' }
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * const data = {
         *   jwt: sign({}, '<private_key>', signOptions)
         * }
         *
         * client.getSpace('<space_id>')
         *  .then((space) => space.getEnvironment('<environment-id>'))
         *  .then((environment) => environment.createAppAccessToken('<app_definition_id>', data)
         *  .then((appAccessToken) => console.log(appAccessToken))
         *  .catch(console.error)
         *  ```
         */
        createAppAccessToken(appDefinitionId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppAccessToken",
                action: "create",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    appDefinitionId
                },
                payload: data
            }).then(payload => wrapAppAccessToken(makeRequest2, payload));
        },
        /**
         * Gets a collection of Functions for a given environment
         * @param appInstallationId
         * @param {import('../common-types').AcceptsQueryOptions} query  - optional query parameter for filtering functions by action
         * @returns Promise containing wrapped collection of Functions in an environment
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client
         *    .getSpace('<space-id>')
         *    .then((space) => space.getEnvironment('<environment-id>'))
         *    .then((environment) => environment.getFunctionsForEnvironment('<app-installation-id>',  { 'accepts[all]': '<action>' }))
         *    .then((functions) => console.log(functions.items))
         *    .catch(console.error)
         * ```
         */
        getFunctionsForEnvironment(appInstallationId, query2) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Function",
                action: "getManyForEnvironment",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    appInstallationId,
                    query: query2
                }
            }).then(data => wrapFunctionCollection(makeRequest2, data));
        },
        /**
         * Gets a collection of FunctionLogs for a given app installation id and FunctionId
         * @param appInstallationId
         * @param functionId
         * @param {import('../common-types').CursorBasedParams} query  - optional query parameter for pagination (limit, nextPage, prevPage)
         * @returns Promise containing wrapped collection of FunctionLogs
         * * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client
         *    .getSpace('<space-id>')
         *    .then((space) => space.getEnvironment('<environment-id>'))
         *    .then((environment) =>
         *       environment.getFunctionLogs(
         *          '<app-installation-id>',
         *          '<function-id>',
         *          {
         *            query: {
         *              // optional limit
         *              limit: 10,
         *              // optional interval query
         *              'sys.createdAt[gte]': start,
         *              'sys.createdAt[lt]': end,
         *              // optional cursor based pagination parameters
         *              pagePrev: '<page_prev>',
         *            },
         *          },
         *       )
         *     )
         *     .then((functionLogs) => console.log(functionLog.items))
         *     .catch(console.error)
         * ```
         */
        getFunctionLogs(appInstallationId, functionId, query2) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "FunctionLog",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    appInstallationId,
                    functionId,
                    query: query2 ? (0, import_contentful_sdk_core41.createRequestConfig)({ query: query2 }).params : void 0
                }
            }).then(data => wrapFunctionLogCollection(makeRequest2, data));
        },
        /**
         * Gets a FunctionLog by appInstallationId, functionId and logId
         * @param appInstallationId
         * @param functionId
         * @param logId
         * @returns Promise containing a wrapped FunctionLog
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client
         *    .getSpace(<space-id>)
         *    .then((space) => space.getEnvironment('<environment-id>'))
         *    .then((environment) =>
         *       environment.getFunctionLog(
         *          '<app-installation-id>',
         *          '<function-id>',
         *          '<log-id>'
         *       )
         *     )
         *     .then((functionLog) => console.log(functionLog))
         *     .catch(console.error)
         * ```
         */
        getFunctionLog(appInstallationId, functionId, logId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "FunctionLog",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    appInstallationId,
                    functionId,
                    logId
                }
            }).then(data => wrapFunctionLog(makeRequest2, data));
        },
        /**
         * Gets all snapshots of an entry
         * @func getEntrySnapshots
         * @param entryId - Entry ID
         * @param query - query additional query paramaters
         * @returns Promise for a collection of Entry Snapshots
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getEntrySnapshots('<entry_id>'))
         * .then((snapshots) => console.log(snapshots.items))
         * .catch(console.error)
         * ```
         */
        getEntrySnapshots(entryId, query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Snapshot",
                action: "getManyForEntry",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    entryId,
                    query: query2
                }
            }).then(data => wrapSnapshotCollection(makeRequest2, data));
        },
        /**
         * Gets all snapshots of a contentType
         * @func getContentTypeSnapshots
         * @param contentTypeId - Content Type ID
         * @param query - query additional query paramaters
         * @returns Promise for a collection of Content Type Snapshots
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getContentTypeSnapshots('<contentTypeId>'))
         * .then((snapshots) => console.log(snapshots.items))
         * .catch(console.error)
         * ```
         */
        getContentTypeSnapshots(contentTypeId, query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Snapshot",
                action: "getManyForContentType",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    contentTypeId,
                    query: query2
                }
            }).then(data => wrapSnapshotCollection(makeRequest2, data));
        },
        createTag(id, name, visibility) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Tag",
                action: "createWithId",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    tagId: id
                },
                payload: {
                    name,
                    sys: { visibility: visibility ?? "private" }
                }
            }).then(data => wrapTag(makeRequest2, data));
        },
        getTags(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Tag",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: (0, import_contentful_sdk_core41.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapTagCollection(makeRequest2, data));
        },
        getTag(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Tag",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    tagId: id
                }
            }).then(data => wrapTag(makeRequest2, data));
        },
        /**
         * Retrieves a Release by ID
         * @param releaseId
         * @returns Promise containing a wrapped Release
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getRelease('<release_id>'))
         * .then((release) => console.log(release))
         * .catch(console.error)
         * ```
         */
        getRelease(releaseId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Release",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    releaseId
                }
            }).then(data => wrapRelease(makeRequest2, data));
        },
        /**
         * Gets a Collection of Releases,
         * @param {ReleaseQueryOptions} query filtering options for the collection result
         * @returns Promise containing a wrapped Release Collection
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getReleases({ 'entities.sys.id[in]': '<asset_id>,<entry_id>' }))
         * .then((releases) => console.log(releases))
         * .catch(console.error)
         * ```
         */
        getReleases(query2) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Release",
                action: "query",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: query2
                }
            }).then(data => wrapReleaseCollection(makeRequest2, data));
        },
        /**
         * Creates a new Release with the entities and title in the payload
         * @param payload Object containing the payload in order to create a Release
         * @returns Promise containing a wrapped Release, that has other helper methods within.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * const payload = {
         *   title: 'My Release',
         *   entities: {
         *     sys: { type: 'Array' },
         *     items: [
         *      { sys: { linkType: 'Entry', type: 'Link', id: '<entry_id>' } }
         *     ]
         *   }
         * }
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.createRelease(payload))
         * .then((release) => console.log(release))
         * .catch(console.error)
         * ```
         */
        createRelease(payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Release",
                action: "create",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload
            }).then(data => wrapRelease(makeRequest2, data));
        },
        /**
         * Updates a Release and replaces all the properties.
         * @param {object} options,
         * @param options.releaseId the ID of the release
         * @param options.payload the payload to be updated in the Release
         * @param options.version Release sys.version that to be updated
         * @returns Promise containing a wrapped Release, that has helper methods within.
         *
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         *
         * const payload = {
         *   title: "Updated Release title",
         *   entities: {
         *     sys: { type: 'Array' },
         *     items: [
         *        { sys: { linkType: 'Entry', type: 'Link', id: '<entry_id>' } }
         *     ]
         *   }
         * }
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.updateRelease({ releaseId: '<release_id>', version: 1, payload } ))
         * .then((release) => console.log(release))
         * .catch(console.error)
         * ```
         */
        updateRelease({ releaseId, payload, version }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Release",
                action: "update",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    releaseId,
                    version
                },
                payload
            }).then(data => wrapRelease(makeRequest2, data));
        },
        /**
         * Deletes a Release by ID - does not delete any entities.
         * @param releaseId the ID of the release
         *
         * @returns Promise containing a wrapped Release, that has helper methods within.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.deleteRelease('<release_id>')
         * .catch(console.error)
         * ```
         */
        deleteRelease(releaseId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Release",
                action: "delete",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    releaseId
                }
            });
        },
        /**
         * Publishes all Entities contained in a Release.
         * @param options.releaseId the ID of the release
         * @param options.version the version of the release that is to be published
         * @returns Promise containing a wrapped Release, that has helper methods within.
         *
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.publishRelease({ releaseId: '<release_id>', version: 1 }))
         * .catch(console.error)
         * ```
         */
        publishRelease({ releaseId, version }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Release",
                action: "publish",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    releaseId,
                    version
                }
            }).then(data => wrapReleaseAction(makeRequest2, data));
        },
        /**
         * Unpublishes all Entities contained in a Release.
         * @param options.releaseId the ID of the release
         * @param options.version the version of the release that is to be published
         * @returns Promise containing a wrapped Release, that has helper methods within.
         *
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.unpublishRelease({ releaseId: '<release_id>', version: 1 }))
         * .catch(console.error)
         * ```
         */
        unpublishRelease({ releaseId, version }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Release",
                action: "unpublish",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    releaseId,
                    version
                }
            }).then(data => wrapReleaseAction(makeRequest2, data));
        },
        /**
         * Validates all Entities contained in a Release against an action (publish or unpublish)
         * @param options.releaseId the ID of the release
         * @param options.payload (optional) the type of action to be validated against
         *
         * @returns Promise containing a wrapped Release, that has helper methods within.
         *
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.validateRelease({ releaseId: '<release_id>', payload: { action: 'unpublish' } }))
         * .catch(console.error)
         * ```
         */
        validateRelease({ releaseId, payload }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Release",
                action: "validate",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    releaseId
                },
                payload
            }).then(data => wrapReleaseAction(makeRequest2, data));
        },
        /**
         * Archives a Release and prevents new operations (publishing, unpublishing adding new entities etc).
         * @param options.releaseId the ID of the release
         * @param options.version the version of the release that is to be archived
         * @returns Promise containing a wrapped Release, that has helper methods within.
         *
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.archiveRelease({ releaseId: '<release_id>', version: 1 }))
         * .catch(console.error)
         * ```
         */
        archiveRelease({ releaseId, version }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Release",
                action: "archive",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    releaseId,
                    version
                }
            }).then(data => wrapRelease(makeRequest2, data));
        },
        /**
         * Unarchives a previously archived Release - this enables the release to be published, unpublished etc.
         * @param options.releaseId the ID of the release
         * @param options.version the version of the release that is to be unarchived
         * @returns Promise containing a wrapped Release, that has helper methods within.
         *
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.unarchiveRelease({ releaseId: '<release_id>', version: 1 }))
         * .catch(console.error)
         * ```
         */
        unarchiveRelease({ releaseId, version }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Release",
                action: "unarchive",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    releaseId,
                    version
                }
            }).then(data => wrapRelease(makeRequest2, data));
        },
        /**
         * Retrieves a ReleaseAction by ID
         * @param params.releaseId The ID of a Release
         * @param params.actionId The ID of a Release Action
         * @returns Promise containing a wrapped ReleaseAction
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getReleaseAction({ releaseId: '<release_id>', actionId: '<action_id>' }))
         * .then((releaseAction) => console.log(releaseAction))
         * .catch(console.error)
         * ```
         */
        getReleaseAction({ actionId, releaseId }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ReleaseAction",
                action: "get",
                params: {
                    actionId,
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    releaseId
                }
            }).then(data => wrapReleaseAction(makeRequest2, data));
        },
        /**
         * Gets a Collection of ReleaseActions
         * @param {string} params.releaseId ID of the Release to fetch the actions from
         * @param {ReleaseQueryOptions} params.query filtering options for the collection result
         * @returns Promise containing a wrapped ReleaseAction Collection
         *
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment-id>'))
         * .then((environment) => environment.getReleaseActions({ query: { 'sys.id[in]': '<id_1>,<id_2>', 'sys.release.sys.id[in]': '<id1>,<id2>' } }))
         * .then((releaseActions) => console.log(releaseActions))
         * .catch(console.error)
         * ```
         */
        getReleaseActions({ query: query2 }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ReleaseAction",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: query2
                }
            }).then(data => wrapReleaseActionCollection(makeRequest2, data));
        },
        async getUIConfig() {
            const raw = this.toPlainObject();
            const data = await makeRequest2({
                entityType: "UIConfig",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                }
            });
            return wrapUIConfig(makeRequest2, data);
        },
        async getUserUIConfig() {
            const raw = this.toPlainObject();
            const data = await makeRequest2({
                entityType: "UserUIConfig",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                }
            });
            return wrapUserUIConfig(makeRequest2, data);
        },
        /**
         * Gets a collection of all environment template installations in the environment for a given template
         * @param environmentTemplateId - Environment template ID to return installations for
         * @param [options.installationId] - Installation ID to filter for a specific installation
         * @returns Promise for a collection of EnvironmentTemplateInstallations
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getEnvironmentTemplateInstallations('<environment_template_id>'))
         * .then((installations) => console.log(installations.items))
         * .catch(console.error)
         * ```
         */
        async getEnvironmentTemplateInstallations(environmentTemplateId, { installationId, ...query2 } = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentTemplateInstallation",
                action: "getForEnvironment",
                params: {
                    environmentTemplateId,
                    ...(installationId && { installationId }),
                    query: { ...(0, import_contentful_sdk_core41.createRequestConfig)({ query: query2 }).params },
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                }
            }).then(data => wrapEnvironmentTemplateInstallationCollection(makeRequest2, data));
        },
        /**
         * Gets a collection of all resource types based on native external references app installations in the environment
         * @param query - BasicCursorPaginationOptions
         * @returns Promise for a collection of ResourceTypes
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getResourceTypes({limit: 10}))
         * .then((installations) => console.log(installations.items))
         * .catch(console.error)
         * ```
         */
        async getResourceTypes(query2) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ResourceType",
                action: "getForEnvironment",
                params: {
                    query: query2,
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                }
            }).then(data => wrapResourceTypesForEnvironmentCollection(makeRequest2, data));
        },
        /**
         * Gets a collection of all resources for a given resource type based on native external references app installations in the environment
         * @param resourceTypeId - Id of the resourceType to get its resources
         * @param query - Either LookupQuery options with 'sys.urn[in]' param or a Search query with 'query' param, in both cases you can add pagination options
         * @returns Promise for a collection of Resources for a given resourceTypeId
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * // Search Query
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * // <search_query> is a string you want to search for in the external resources
         * .then((environment) => environment.getResourcesForResourceType('<resource_type_id>', {query: '<search_query>', limit: 10}))
         * .then((installations) => console.log(installations.items))
         * .catch(console.error)
         *
         * // Lookup query
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => environment.getResourcesForResourceType('<resource_type_id>', {'sys.urn[in]': '<resource_urn1>,<resource_urn2>', limit: 10}))
         * .then((installations) => console.log(installations.items))
         * .catch(console.error)
         * ```
         */
        async getResourcesForResourceType(resourceTypeId, query2) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Resource",
                action: "getMany",
                params: {
                    query: query2,
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    resourceTypeId
                }
            }).then(data => wrapResourceCollection(makeRequest2, data));
        },
        /**
         * Invokes an AI Action.
         * @param aiActionId - The ID of the AI Action to invoke.
         * @param payload - The invocation payload.
         * @returns Promise for an AI Action Invocation.
         * @example ```javascript
         * client.getSpace('<space_id>')
         *   .then(space => space.getEnvironment('<environment_id>'))
         *   .then(environment => environment.invokeAiAction('<ai_action_id>', {
         *     variables: [  ...  ],
         *     outputFormat: 'RichText'
         *   }))
         *   .then(invocation => console.log(invocation))
         *   .catch(console.error)
         * ```
         */
        invokeAiAction(aiActionId, payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AiAction",
                action: "invoke",
                params: { spaceId: raw.sys.space.sys.id, environmentId: raw.sys.id, aiActionId },
                payload
            }).then(data => wrapAiActionInvocation(makeRequest2, data));
        },
        /**
         * Retrieves an AI Action Invocation.
         * @param params - Object containing the AI Action ID and the Invocation ID.
         * @returns Promise for an AI Action Invocation.
         * @example ```javascript
         * client.getSpace('<space_id>')
         *   .then(space => space.getEnvironment('<environment_id>'))
         *   .then(environment => environment.getAiActionInvocation({
         *      aiActionId: '<ai_action_id>',
         *      invocationId: '<invocation_id>'
         *   }))
         *   .then(invocation => console.log(invocation))
         *   .catch(console.error)
         * ```
         */
        getAiActionInvocation({ aiActionId, invocationId }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AiActionInvocation",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    aiActionId,
                    invocationId
                }
            }).then(data => wrapAiActionInvocation(makeRequest2, data));
        },
        /**
         * Retrieves Semantic Duplicates for the given entity ID
         * @param payload - Object containing the entityId and optional filters
         * @returns Promise for Semantic Duplicates
         * @example ```javascript
         * client.getSpace('<space_id>')
         *   .then(space => space.getEnvironment('<environment_id>'))
         *   .then(environment => environment.getSemanticDuplicates({
         *      entityId: '<entity_id>',
         *      filters: {
         *        contentTypeIds: ['<content_type_id1>', '<content_type_id2>'],
         *      }
         *    })
         */
        getSemanticDuplicates(payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SemanticDuplicates",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload
            }).then(data => wrapSemanticDuplicates(makeRequest2, data));
        },
        /**
         * Retrieves Semantic Recommendations for the given entity IDs
         * @param payload - Object containing the entityIds and optional filters
         * @returns Promise for Semantic Recommendations
         * @example ```javascript
         * client.getSpace('<space_id>')
         *   .then(space => space.getEnvironment('<environment_id>'))
         *   .then(environment => environment.getSemanticRecommendations({
         *      entityIds: ['<entity_id>'],
         *      filters: {
         *        contentTypeIds: ['<content_type_id1>', '<content_type_id2>'],
         *      }
         *    })
         */
        getSemanticRecommendations(payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SemanticRecommendations",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload
            }).then(data => wrapSemanticRecommendations(makeRequest2, data));
        },
        /**
         * Retrieves Semantic Reference Suggestions for the given entity ID and its reference field ID
         * @param payload - Object containing the entityId and referenceFieldId
         * @returns Promise for Semantic Reference Suggestions
         * @example ```javascript
         * client.getSpace('<space_id>')
         *   .then(space => space.getEnvironment('<environment_id>'))
         *   .then(environment => environment.getSemanticReferenceSuggestions({
         *      entityId: '<entity_id>',
         *      referenceFieldId: '<reference_field_id>',
         *    })
         */
        getSemanticReferenceSuggestions(payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SemanticReferenceSuggestions",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload
            }).then(data => wrapSemanticReferenceSuggestions(makeRequest2, data));
        },
        /**
         * Retrieves Semantic Search results for the given query
         * @param payload - Object containing the search query and optional filters
         * @returns Promise for Semantic Search results
         * @example ```javascript
         * client.getSpace('<space_id>')
         *   .then(space => space.getEnvironment('<environment_id>'))
         *   .then(environment => environment.getSemanticSearch({
         *      query: '<search_query>',
         *      filters: {
         *        contentTypeIds: ['<content_type_id1>', '<content_type_id2>'],
         *      }
         *    })
         */
        getSemanticSearch(payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SemanticSearch",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                },
                payload
            }).then(data => wrapSemanticSearch(makeRequest2, data));
        },
        /**
         * Gets all content semantics indexes for the environment
         * @return Promise for a collection of ContentSemanticsIndex
         * @example ```javascript
         * client.getSpace('<space_id>')
         *   .then(space => space.getEnvironment('<environment_id>'))
         *   .then(environment => environment.getContentSemanticsIndexes())
         */
        getContentSemanticsIndexes() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ContentSemanticsIndex",
                action: "getManyForEnvironment",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                }
            }).then(data => wrapContentSemanticsIndexCollection(makeRequest2, data));
        },
        /**
         * Gets an AI Agent
         * @param agentId - AI Agent ID
         * @returns Promise for an AI Agent
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         *   .then((space) => space.getEnvironment('<environment_id>'))
         *   .then((environment) => environment.getAgent('<agent_id>'))
         *   .then((agent) => console.log(agent))
         *   .catch(console.error)
         * ```
         */
        getAgent(agentId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Agent",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    agentId
                }
            }).then(data => wrapAgent(makeRequest2, data));
        },
        /**
         * Gets a collection of AI Agents
         * @returns Promise for a collection of AI Agents
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         *   .then((space) => space.getEnvironment('<environment_id>'))
         *   .then((environment) => environment.getAgents())
         *   .then((response) => console.log(response.items))
         *   .catch(console.error)
         * ```
         */
        getAgents() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Agent",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id
                }
            }).then(data => wrapAgentCollection(makeRequest2, data));
        },
        /**
         * Generates content using an AI Agent
         * @param agentId - AI Agent ID
         * @param payload - Generation payload
         * @returns Promise for a simplified response containing `sys.id`, `sys.type`, and `sys.status`.
         *         Use `getAgentRun()` with the returned `sys.id` to poll for full results.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * async function generateContent() {
         *   const client = contentful.createClient({
         *     accessToken: '<content_management_api_key>'
         *   })
         *
         *   const space = await client.getSpace('<space_id>')
         *   const environment = await space.getEnvironment('<environment_id>')
         *
         *   // Start generation (returns 202 Accepted)
         *   const response = await environment.generateWithAgent('<agent_id>', {
         *     messages: [
         *       {
         *         parts: [{ type: 'text', text: 'Write a short poem about Contentful' }],
         *         role: 'user'
         *       }
         *     ]
         *   })
         *
         *   // Poll for full results
         *   let run = await environment.getAgentRun(response.sys.id)
         *   while (run.sys.status === 'IN_PROGRESS') {
         *     await new Promise((resolve) => setTimeout(resolve, 1000))
         *     run = await environment.getAgentRun(response.sys.id)
         *   }
         *
         *   console.log(run)
         * }
         * ```
         */
        generateWithAgent(agentId, payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Agent",
                action: "generate",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    agentId
                },
                payload
            }).then(data => wrapAgentGenerateResponse(makeRequest2, data));
        },
        /**
         * Gets an AI Agent Run
         * @param runId - AI Agent Run ID
         * @returns Promise for an AI Agent Run
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         *   .then((space) => space.getEnvironment('<environment_id>'))
         *   .then((environment) => environment.getAgentRun('<run_id>'))
         *   .then((run) => console.log(run))
         *   .catch(console.error)
         * ```
         */
        getAgentRun(runId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AgentRun",
                action: "get",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    runId
                }
            }).then(data => wrapAgentRun(makeRequest2, data));
        },
        /**
         * Gets a collection of AI Agent Runs with optional filtering
         * @param query - Object with search parameters (agentIn, statusIn)
         * @returns Promise for a collection of AI Agent Runs
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         *   .then((space) => space.getEnvironment('<environment_id>'))
         *   .then((environment) => environment.getAgentRuns({
         *     agentIn: ['agent1', 'agent2'],
         *     statusIn: ['COMPLETED', 'IN_PROGRESS']
         *   }))
         *   .then((response) => console.log(response.items))
         *   .catch(console.error)
         * ```
         */
        getAgentRuns(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AgentRun",
                action: "getMany",
                params: {
                    spaceId: raw.sys.space.sys.id,
                    environmentId: raw.sys.id,
                    query: query2
                }
            }).then(data => wrapAgentRunCollection(makeRequest2, data));
        }
    };
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/environment.mjs
function wrapEnvironment(makeRequest2, data) {
    const environment = (0, import_contentful_sdk_core42.toPlainObject)(index(data));
    const environmentApi = createEnvironmentApi(makeRequest2);
    const enhancedEnvironment = enhanceWithMethods(environment, environmentApi);
    return (0, import_contentful_sdk_core42.freezeSys)(enhancedEnvironment);
}
var wrapEnvironmentCollection = wrapCollection(wrapEnvironment);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/webhook.mjs
var import_contentful_sdk_core43 = __toESM(require_cjs2(), 1);
function createWebhookApi(makeRequest2) {
    const getParams3 = data => ({
        spaceId: data.sys.space.sys.id,
        webhookDefinitionId: data.sys.id
    });
    return {
        update: function update37() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "update",
                params: getParams3(data),
                payload: data
            }).then(data2 => wrapWebhook(makeRequest2, data2));
        },
        delete: function del45() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "delete",
                params: getParams3(data)
            });
        },
        getCalls: function getCalls() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "getManyCallDetails",
                params: getParams3(data)
            });
        },
        getCall: function getCall(id) {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "getCallDetails",
                params: { ...getParams3(data), callId: id }
            });
        },
        getHealth: function getHealth() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "getHealthStatus",
                params: getParams3(data)
            });
        }
    };
}
function wrapWebhook(makeRequest2, data) {
    const webhook = (0, import_contentful_sdk_core43.toPlainObject)(index(data));
    const webhookWithMethods = enhanceWithMethods(webhook, createWebhookApi(makeRequest2));
    return (0, import_contentful_sdk_core43.freezeSys)(webhookWithMethods);
}
var wrapWebhookCollection = wrapCollection(wrapWebhook);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/role.mjs
var import_contentful_sdk_core44 = __toESM(require_cjs2(), 1);
function createRoleApi(makeRequest2) {
    const getParams3 = data => ({
        spaceId: data.sys.space.sys.id,
        roleId: data.sys.id
    });
    return {
        update: function update37() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "Role",
                action: "update",
                params: getParams3(data),
                payload: data
            }).then(data2 => wrapRole(makeRequest2, data2));
        },
        delete: function del45() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "Role",
                action: "delete",
                params: getParams3(data)
            });
        }
    };
}
function wrapRole(makeRequest2, data) {
    const role = (0, import_contentful_sdk_core44.toPlainObject)(index(data));
    const roleWithMethods = enhanceWithMethods(role, createRoleApi(makeRequest2));
    return (0, import_contentful_sdk_core44.freezeSys)(roleWithMethods);
}
var wrapRoleCollection = wrapCollection(wrapRole);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/user.mjs
var import_contentful_sdk_core45 = __toESM(require_cjs2(), 1);
function wrapUser(_makeRequest, data) {
    const user = (0, import_contentful_sdk_core45.toPlainObject)(index(data));
    const userWithMethods = enhanceWithMethods(user, {});
    return (0, import_contentful_sdk_core45.freezeSys)(userWithMethods);
}
var wrapUserCollection = wrapCollection(wrapUser);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/space-add-on.mjs
var import_contentful_sdk_core46 = __toESM(require_cjs2(), 1);
function wrapSpaceAddOn(makeRequest2, data) {
    const spaceAddOn = (0, import_contentful_sdk_core46.toPlainObject)(index(data));
    const spaceAddOnWithMethods = enhanceWithMethods(spaceAddOn, {});
    return (0, import_contentful_sdk_core46.freezeSys)(spaceAddOnWithMethods);
}
var wrapSpaceAddOnCollection = wrapCollection(wrapSpaceAddOn);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/space-member.mjs
var import_contentful_sdk_core47 = __toESM(require_cjs2(), 1);
function wrapSpaceMember(_makeRequest, data) {
    const spaceMember = (0, import_contentful_sdk_core47.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core47.freezeSys)(spaceMember);
}
var wrapSpaceMemberCollection = wrapCollection(wrapSpaceMember);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/space-membership.mjs
var import_contentful_sdk_core48 = __toESM(require_cjs2(), 1);
function createSpaceMembershipApi(makeRequest2) {
    const getParams3 = data => ({
        spaceId: data.sys.space.sys.id,
        spaceMembershipId: data.sys.id
    });
    return {
        update: function update37() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "SpaceMembership",
                action: "update",
                params: getParams3(data),
                payload: data
            }).then(data2 => wrapSpaceMembership(makeRequest2, data2));
        },
        delete: function del45() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "SpaceMembership",
                action: "delete",
                params: getParams3(data)
            });
        }
    };
}
function wrapSpaceMembership(makeRequest2, data) {
    const spaceMembership = (0, import_contentful_sdk_core48.toPlainObject)(index(data));
    const spaceMembershipWithMethods = enhanceWithMethods(spaceMembership, createSpaceMembershipApi(makeRequest2));
    return (0, import_contentful_sdk_core48.freezeSys)(spaceMembershipWithMethods);
}
var wrapSpaceMembershipCollection = wrapCollection(wrapSpaceMembership);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/team-space-membership.mjs
var import_contentful_sdk_core49 = __toESM(require_cjs2(), 1);
function createTeamSpaceMembershipApi(makeRequest2) {
    const getParams3 = data => ({
        teamSpaceMembershipId: data.sys.id,
        spaceId: data.sys.space.sys.id
    });
    return {
        update: function () {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "TeamSpaceMembership",
                action: "update",
                params: getParams3(raw),
                payload: raw
            }).then(data => wrapTeamSpaceMembership(makeRequest2, data));
        },
        delete: function del45() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "TeamSpaceMembership",
                action: "delete",
                params: getParams3(data)
            });
        }
    };
}
function wrapTeamSpaceMembership(makeRequest2, data) {
    const teamSpaceMembership = (0, import_contentful_sdk_core49.toPlainObject)(index(data));
    const teamSpaceMembershipWithMethods = enhanceWithMethods(teamSpaceMembership, createTeamSpaceMembershipApi(makeRequest2));
    return (0, import_contentful_sdk_core49.freezeSys)(teamSpaceMembershipWithMethods);
}
var wrapTeamSpaceMembershipCollection = wrapCollection(wrapTeamSpaceMembership);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/team.mjs
var import_contentful_sdk_core50 = __toESM(require_cjs2(), 1);
function createTeamApi(makeRequest2) {
    const getParams3 = data => ({
        teamId: data.sys.id,
        organizationId: data.sys.organization.sys.id
    });
    return {
        update: function update37() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Team",
                action: "update",
                params: getParams3(raw),
                payload: raw
            }).then(data => wrapTeam(makeRequest2, data));
        },
        delete: function del45() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Team",
                action: "delete",
                params: getParams3(raw)
            });
        }
    };
}
function wrapTeam(makeRequest2, data) {
    const team = (0, import_contentful_sdk_core50.toPlainObject)(index(data));
    const teamWithMethods = enhanceWithMethods(team, createTeamApi(makeRequest2));
    return (0, import_contentful_sdk_core50.freezeSys)(teamWithMethods);
}
var wrapTeamCollection = wrapCollection(wrapTeam);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/api-key.mjs
var import_contentful_sdk_core51 = __toESM(require_cjs2(), 1);
function createApiKeyApi(makeRequest2) {
    const getParams3 = data => ({
        spaceId: data.sys.space?.sys.id ?? "",
        apiKeyId: data.sys.id
    });
    return {
        update: function update37() {
            const self2 = this;
            return makeRequest2({
                entityType: "ApiKey",
                action: "update",
                params: getParams3(self2),
                payload: self2,
                headers: {}
            }).then(data => wrapApiKey(makeRequest2, data));
        },
        delete: function del45() {
            const self2 = this;
            return makeRequest2({
                entityType: "ApiKey",
                action: "delete",
                params: getParams3(self2)
            });
        }
    };
}
function wrapApiKey(makeRequest2, data) {
    const apiKey = (0, import_contentful_sdk_core51.toPlainObject)(index(data));
    const apiKeyWithMethods = enhanceWithMethods(apiKey, createApiKeyApi(makeRequest2));
    return (0, import_contentful_sdk_core51.freezeSys)(apiKeyWithMethods);
}
var wrapApiKeyCollection = wrapCollection(wrapApiKey);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/environment-alias.mjs
var import_contentful_sdk_core52 = __toESM(require_cjs2(), 1);
function createEnvironmentAliasApi(makeRequest2) {
    const getParams3 = alias => ({
        spaceId: alias.sys.space.sys.id,
        environmentAliasId: alias.sys.id
    });
    return {
        update: function () {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentAlias",
                action: "update",
                params: getParams3(raw),
                payload: raw
            }).then(data => wrapEnvironmentAlias(makeRequest2, data));
        },
        delete: function () {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentAlias",
                action: "delete",
                params: getParams3(raw)
            }).then(() => {});
        }
    };
}
function wrapEnvironmentAlias(makeRequest2, data) {
    const alias = (0, import_contentful_sdk_core52.toPlainObject)(index(data));
    const enhancedAlias = enhanceWithMethods(alias, createEnvironmentAliasApi(makeRequest2));
    return (0, import_contentful_sdk_core52.freezeSys)(enhancedAlias);
}
var wrapEnvironmentAliasCollection = wrapCollection(wrapEnvironmentAlias);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/preview-api-key.mjs
var import_contentful_sdk_core53 = __toESM(require_cjs2(), 1);
function createPreviewApiKeyApi() {
    return {};
}
function wrapPreviewApiKey(_makeRequest, data) {
    const previewApiKey = (0, import_contentful_sdk_core53.toPlainObject)(index(data));
    const previewApiKeyWithMethods = enhanceWithMethods(previewApiKey, createPreviewApiKeyApi());
    return (0, import_contentful_sdk_core53.freezeSys)(previewApiKeyWithMethods);
}
var wrapPreviewApiKeyCollection = wrapCollection(wrapPreviewApiKey);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/scheduled-action.mjs
var import_contentful_sdk_core54 = __toESM(require_cjs2(), 1);
var ScheduledActionStatus;
(function (ScheduledActionStatus2) {
    ScheduledActionStatus2["scheduled"] = "scheduled";
    ScheduledActionStatus2["inProgress"] = "inProgress";
    ScheduledActionStatus2["succeeded"] = "succeeded";
    ScheduledActionStatus2["failed"] = "failed";
    ScheduledActionStatus2["canceled"] = "canceled";
})(ScheduledActionStatus || (ScheduledActionStatus = {}));
function getInstanceMethods(makeRequest2) {
    const getParams3 = self2 => {
        const scheduledAction = self2.toPlainObject();
        return {
            spaceId: scheduledAction.sys.space.sys.id,
            environmentId: scheduledAction.environment?.sys.id,
            scheduledActionId: scheduledAction.sys.id,
            version: scheduledAction.sys.version
        };
    };
    return {
        /**
         * Cancels the current Scheduled Action schedule.
         *
         * @example ```javascript
         *  const contentful = require('contentful-management');
         *
         *  const client = contentful.createClient({
         *    accessToken: '<content_management_api_key>'
         *  })
         *
         *  client.getSpace('<space_id>')
         *    .then((space) => {
         *      return space.createScheduledAction({
         *        entity: {
         *          sys: {
         *            type: 'Link',
         *            linkType: 'Entry',
         *            id: '<entry_id>'
         *          }
         *        },
         *        environment: {
         *          sys: {
         *            type: 'Link',
         *            linkType: 'Environment',
         *            id: '<environment_id>'
         *          }
         *        },
         *        action: 'publish',
         *        scheduledFor: {
         *          datetime: <ISO_date_string>,
         *          timezone: 'Europe/Berlin'
         *        }
         *      })
         *    .then((scheduledAction) => scheduledAction.delete())
         *    .then((deletedScheduledAction) => console.log(deletedScheduledAction))
         *    .catch(console.error);
         * ```
         */
        async delete() {
            const params = getParams3(this);
            return makeRequest2({
                entityType: "ScheduledAction",
                action: "delete",
                params
            }).then(data => wrapScheduledAction(makeRequest2, data));
        },
        /**
         * Update the current scheduled action. Currently, only changes made to the `scheduledFor` property will be saved.
         *
         * @example ```javascript
         *  const contentful = require('contentful-management');
         *
         *  const client = contentful.createClient({
         *    accessToken: '<content_management_api_key>'
         *  })
         *
         *  client.getSpace('<space_id>')
         *    .then((space) => {
         *      return space.createScheduledAction({
         *        entity: {
         *          sys: {
         *            type: 'Link',
         *            linkType: 'Entry',
         *            id: '<entry_id>'
         *          }
         *        },
         *        environment: {
         *          sys: {
         *            type: 'Link',
         *            linkType: 'Environment',
         *            id: '<environment_id>'
         *          }
         *        },
         *        action: 'publish',
         *        scheduledFor: {
         *          datetime: <ISO_date_string>,
         *          timezone: 'Europe/Berlin'
         *        }
         *      })
         *    .then((scheduledAction) => {
         *      scheduledAction.scheduledFor.timezone = 'Europe/Paris';
         *      return scheduledAction.update();
         *    })
         *    .then((scheduledAction) => console.log(scheduledAction))
         *    .catch(console.error);
         * ```
         */
        async update() {
            const params = getParams3(this);
            const { sys, ...payload } = this.toPlainObject();
            return makeRequest2({
                entityType: "ScheduledAction",
                action: "update",
                params,
                payload
            }).then(data => wrapScheduledAction(makeRequest2, data));
        }
    };
}
function wrapScheduledAction(makeRequest2, data) {
    const scheduledAction = (0, import_contentful_sdk_core54.toPlainObject)(index(data));
    const scheduledActionWithMethods = enhanceWithMethods(scheduledAction, getInstanceMethods(makeRequest2));
    return (0, import_contentful_sdk_core54.freezeSys)(scheduledActionWithMethods);
}
var wrapScheduledActionCollection = wrapCollection(wrapScheduledAction);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/ai-action.mjs
var import_contentful_sdk_core55 = __toESM(require_cjs2(), 1);
function createAiActionApi(makeRequest2) {
    const getParams3 = data => ({
        spaceId: data.sys.space.sys.id,
        aiActionId: data.sys.id
    });
    return {
        update: function update37() {
            const self2 = this;
            return makeRequest2({
                entityType: "AiAction",
                action: "update",
                params: getParams3(self2),
                payload: self2
            }).then(data => wrapAiAction(makeRequest2, data));
        },
        delete: function del45() {
            const self2 = this;
            return makeRequest2({
                entityType: "AiAction",
                action: "delete",
                params: getParams3(self2)
            });
        },
        publish: function publish7() {
            const self2 = this;
            return makeRequest2({
                entityType: "AiAction",
                action: "publish",
                params: {
                    aiActionId: self2.sys.id,
                    spaceId: self2.sys.space.sys.id,
                    version: self2.sys.version
                }
            }).then(data => wrapAiAction(makeRequest2, data));
        },
        unpublish: function unpublish7() {
            const self2 = this;
            return makeRequest2({
                entityType: "AiAction",
                action: "unpublish",
                params: getParams3(self2)
            }).then(data => wrapAiAction(makeRequest2, data));
        },
        invoke: function invoke2(environmentId, payload) {
            const self2 = this;
            return makeRequest2({
                entityType: "AiAction",
                action: "invoke",
                params: {
                    spaceId: self2.sys.space.sys.id,
                    environmentId,
                    aiActionId: self2.sys.id
                },
                payload
            }).then(data => wrapAiActionInvocation(makeRequest2, data));
        }
    };
}
function wrapAiAction(makeRequest2, data) {
    const aiAction = (0, import_contentful_sdk_core55.toPlainObject)(index(data));
    const aiActionWithMethods = enhanceWithMethods(aiAction, createAiActionApi(makeRequest2));
    return (0, import_contentful_sdk_core55.freezeSys)(aiActionWithMethods);
}
var wrapAiActionCollection = wrapCollection(wrapAiAction);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-space-api.mjs
function createSpaceApi(makeRequest2) {
    return {
        /**
         * Deletes the space
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         *   .then((space) => space.delete())
         *   .then(() => console.log('Space deleted.'))
         *   .catch(console.error)
         * ```
         */
        delete: function deleteSpace() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Space",
                action: "delete",
                params: { spaceId: raw.sys.id }
            });
        },
        /**
         * Updates the space
         * @returns Promise for the updated space.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => {
         *   space.name = 'New name'
         *   return space.update()
         * })
         * .then((space) => console.log(`Space ${space.sys.id} renamed.`)
         * .catch(console.error)
         * ```
         */
        update: function updateSpace() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Space",
                action: "update",
                params: { spaceId: raw.sys.id },
                payload: raw,
                headers: {}
            }).then(data => wrapSpace(makeRequest2, data));
        },
        /**
         * Unarchives the space
         * @returns Promise for the unarchived space.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => {
         *   return space.unarchive({productId: 'id'})
         * })
         * .then((space) => console.log(`Space ${space.sys.id} unarchived.`)
         * .catch(console.error)
         * ```
         */
        unarchive: function unarchiveSpace(productId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Space",
                action: "unarchive",
                params: { spaceId: raw.sys.id },
                payload: { productId },
                headers: {}
            }).then(data => wrapSpace(makeRequest2, data));
        },
        /**
         * Gets an environment
         * @param id - Environment ID
         * @returns Promise for an Environment
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironment('<environment_id>'))
         * .then((environment) => console.log(environment))
         * .catch(console.error)
         * ```
         */
        getEnvironment(environmentId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Environment",
                action: "get",
                params: { spaceId: raw.sys.id, environmentId }
            }).then(data => wrapEnvironment(makeRequest2, data));
        },
        /**
         * Gets a collection of Environments
         * @returns Promise for a collection of Environment
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironments())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getEnvironments(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Environment",
                action: "getMany",
                params: { spaceId: raw.sys.id, query: query2 }
            }).then(data => wrapEnvironmentCollection(makeRequest2, data));
        },
        /**
         * Creates an environment
         * @param data - Object representation of the Environment to be created
         * @returns Promise for the newly created Environment
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.createEnvironment({ name: 'Staging' }))
         * .then((environment) => console.log(environment))
         * .catch(console.error)
         * ```
         */
        createEnvironment(data = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Environment",
                action: "create",
                params: {
                    spaceId: raw.sys.id
                },
                payload: data
            }).then(response => wrapEnvironment(makeRequest2, response));
        },
        /**
         * Creates an Environment with a custom ID
         * @param id - Environment ID
         * @param data - Object representation of the Environment to be created
         * @param sourceEnvironmentId - ID of the source environment that will be copied to create the new environment. Default is "master"
         * @returns Promise for the newly created Environment
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.createEnvironmentWithId('<environment-id>', { name: 'Staging'}, 'master'))
         * .then((environment) => console.log(environment))
         * .catch(console.error)
         * ```
         */
        createEnvironmentWithId(id, data, sourceEnvironmentId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Environment",
                action: "createWithId",
                params: {
                    spaceId: raw.sys.id,
                    environmentId: id,
                    sourceEnvironmentId
                },
                payload: data
            }).then(response => wrapEnvironment(makeRequest2, response));
        },
        /**
         * Gets a Webhook
         * @param id - Webhook ID
         * @returns Promise for a Webhook
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getWebhook('<webhook_id>'))
         * .then((webhook) => console.log(webhook))
         * .catch(console.error)
         * ```
         */
        getWebhook(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "get",
                params: { spaceId: raw.sys.id, webhookDefinitionId: id }
            }).then(data => wrapWebhook(makeRequest2, data));
        },
        /**
         * Gets a collection of Webhooks
         * @returns Promise for a collection of Webhooks
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getWebhooks())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getWebhooks() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "getMany",
                params: { spaceId: raw.sys.id }
            }).then(data => wrapWebhookCollection(makeRequest2, data));
        },
        /**
         * Fetch a webhook signing secret
         * @returns Promise for the redacted webhook signing secret in this space
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         *   .then((space) => space.getWebhookSigningSecret())
         *   .then((response) => console.log(response.redactedValue))
         *   .catch(console.error)
         * ```
         */
        getWebhookSigningSecret: function getSigningSecret2() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "getSigningSecret",
                params: { spaceId: raw.sys.id }
            });
        },
        /**
         * Fetch a webhook retry policy
         * @returns Promise for the redacted webhook retry policy in this space
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         *   .then((space) => space.getRetryPolicy())
         *   .then((response) => console.log(response.redactedValue))
         *   .catch(console.error)
         * ```
         */
        getWebhookRetryPolicy: function getWebhookRetryPolicy() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "getRetryPolicy",
                params: { spaceId: raw.sys.id }
            });
        },
        /**
         * Creates a Webhook
         * @param data - Object representation of the Webhook to be created
         * @returns Promise for the newly created Webhook
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.createWebhook({
         *   'name': 'My webhook',
         *   'url': 'https://www.example.com/test',
         *   'topics': [
         *     'Entry.create',
         *     'ContentType.create',
         *     '*.publish',
         *     'Asset.*'
         *   ]
         * }))
         * .then((webhook) => console.log(webhook))
         * .catch(console.error)
         * ```
         */
        createWebhook(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "create",
                params: { spaceId: raw.sys.id },
                payload: data
            }).then(data2 => wrapWebhook(makeRequest2, data2));
        },
        /**
         * Creates a Webhook with a custom ID
         * @param id - Webhook ID
         * @param  data - Object representation of the Webhook to be created
         * @returns Promise for the newly created Webhook
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.createWebhookWithId('<webhook_id>', {
         *   'name': 'My webhook',
         *   'url': 'https://www.example.com/test',
         *   'topics': [
         *     'Entry.create',
         *     'ContentType.create',
         *     '*.publish',
         *     'Asset.*'
         *   ]
         * }))
         * .then((webhook) => console.log(webhook))
         * .catch(console.error)
         * ```
         */
        createWebhookWithId(id, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "createWithId",
                params: { spaceId: raw.sys.id, webhookDefinitionId: id },
                payload: data
            }).then(data2 => wrapWebhook(makeRequest2, data2));
        },
        /**
         * Create or update the webhook signing secret for this space
         * @param data 64 character string that will be used to sign the webhook calls
         * @returns Promise for the redacted webhook signing secret that was created or updated
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const crypto = require('crypto')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * const signingSecret = client.getSpace('<space_id>')
         *   .then((space) => space.upsertWebhookSigningSecret({
         *     value: crypto.randomBytes(32).toString('hex')
         *   }))
         *   .then((response) => console.log(response.redactedValue))
         *   .catch(console.error)
         * ```
         */
        upsertWebhookSigningSecret: function getSigningSecret2(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "upsertSigningSecret",
                params: { spaceId: raw.sys.id },
                payload: data
            });
        },
        /**
         * Create or update the webhook retry policy for this space
         * @param data the maxRetries with integer value >= 2 and <= 99 value to set in the Retry Policy
         * @returns Promise for the redacted webhook retry policy that was created or updated
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * const retryPolicy = client.getSpace('<space_id>')
         *   .then((space) => space.upsertWebhookRetryPolicy({
         *     maxRetries: 15
         *   }))
         *   .then((response) => console.log(response.redactedValue))
         *   .catch(console.error)
         * ```
         */
        upsertWebhookRetryPolicy: function upsertWebhookRetryPolicy(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "upsertRetryPolicy",
                params: { spaceId: raw.sys.id },
                payload: data
            });
        },
        /**
         * Delete the webhook signing secret for this space
         * @returns Promise<void>
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         *   .then((space) => space.deleteWebhookSigningSecret())
         *   .then(() => console.log("success"))
         *   .catch(console.error)
         * ```
         */
        deleteWebhookSigningSecret: function getSigningSecret2() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "deleteSigningSecret",
                params: { spaceId: raw.sys.id }
            });
        },
        /**
         * Delete the webhook retry policy for this space
         * @returns Promise<void>
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         *   .then((space) => space.deleteWebhookRetryPolicy())
         *   .then(() => console.log("success"))
         *   .catch(console.error)
         * ```
         */
        deleteWebhookRetryPolicy: function deleteRetryPolicy2() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Webhook",
                action: "deleteRetryPolicy",
                params: { spaceId: raw.sys.id }
            });
        },
        /**
         * Gets a Role
         * @param id - Role ID
         * @returns Promise for a Role
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.createRole({
         *   fields: {
         *     title: {
         *       'en-US': 'Role title'
         *     }
         *   }
         * }))
         * .then((role) => console.log(role))
         * .catch(console.error)
         * ```
         */
        getRole(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Role",
                action: "get",
                params: { spaceId: raw.sys.id, roleId: id }
            }).then(data => wrapRole(makeRequest2, data));
        },
        /**
         * Gets a collection of Roles
         * @returns Promise for a collection of Roles
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getRoles())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getRoles(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Role",
                action: "getMany",
                params: { spaceId: raw.sys.id, query: (0, import_contentful_sdk_core56.createRequestConfig)({ query: query2 }).params }
            }).then(data => wrapRoleCollection(makeRequest2, data));
        },
        /**
         * Creates a Role
         * @param data - Object representation of the Role to be created
         * @returns  Promise for the newly created Role
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * client.getSpace('<space_id>')
         * .then((space) => space.createRole({
         *   name: 'My Role',
         *   description: 'foobar role',
         *   permissions: {
         *     ContentDelivery: 'all',
         *     ContentModel: ['read'],
         *     Settings: []
         *   },
         *   policies: [
         *     {
         *       effect: 'allow',
         *       actions: 'all',
         *       constraint: {
         *         and: [
         *           {
         *             equals: [
         *               { doc: 'sys.type' },
         *               'Entry'
         *             ]
         *           },
         *           {
         *             equals: [
         *               { doc: 'sys.type' },
         *               'Asset'
         *             ]
         *           }
         *         ]
         *       }
         *     }
         *   ]
         * }))
         * .then((role) => console.log(role))
         * .catch(console.error)
         * ```
         */
        createRole(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Role",
                action: "create",
                params: { spaceId: raw.sys.id },
                payload: data
            }).then(data2 => wrapRole(makeRequest2, data2));
        },
        /**
         * Creates a Role with a custom ID
         * @param id - Role ID
         * @param data - Object representation of the Role to be created
         * @returns Promise for the newly created Role
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * client.getSpace('<space_id>')
         * .then((space) => space.createRoleWithId('<role-id>', {
         *   name: 'My Role',
         *   description: 'foobar role',
         *   permissions: {
         *     ContentDelivery: 'all',
         *     ContentModel: ['read'],
         *     Settings: []
         *   },
         *   policies: [
         *     {
         *       effect: 'allow',
         *       actions: 'all',
         *       constraint: {
         *         and: [
         *           {
         *             equals: [
         *               { doc: 'sys.type' },
         *               'Entry'
         *             ]
         *           },
         *           {
         *             equals: [
         *               { doc: 'sys.type' },
         *               'Asset'
         *             ]
         *           }
         *         ]
         *       }
         *     }
         *   ]
         * }))
         * .then((role) => console.log(role))
         * .catch(console.error)
         * ```
         */
        createRoleWithId(id, roleData) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Role",
                action: "createWithId",
                params: { spaceId: raw.sys.id, roleId: id },
                payload: roleData
            }).then(data => wrapRole(makeRequest2, data));
        },
        /**
         * Gets a User
         * @param userId - User ID
         * @returns Promise for a User
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getSpaceUser('id'))
         * .then((user) => console.log(user))
         * .catch(console.error)
         * ```
         */
        getSpaceUser(userId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "User",
                action: "getForSpace",
                params: {
                    spaceId: raw.sys.id,
                    userId
                }
            }).then(data => wrapUser(makeRequest2, data));
        },
        /**
         * Gets a collection of Users in a space
         * @param query - Object with search parameters. Check the <a href="https://www.contentful.com/developers/docs/javascript/tutorials/using-js-cda-sdk/#retrieving-entries-with-search-parameters">JS SDK tutorial</a> and the <a href="https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters">REST API reference</a> for more details.
         * @returns Promise a collection of Users in a space
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getSpaceUsers(query))
         * .then((data) => console.log(data))
         * .catch(console.error)
         * ```
         */
        getSpaceUsers(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "User",
                action: "getManyForSpace",
                params: {
                    spaceId: raw.sys.id,
                    query: (0, import_contentful_sdk_core56.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapUserCollection(makeRequest2, data));
        },
        /**
         * Gets a collection of teams for a space
         * @param query
         * @returns Promise for a collection of teams for a space
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getTeams())
         * .then((teamsCollection) => console.log(teamsCollection))
         * .catch(console.error)
         * ```
         */
        getTeams(query2 = { limit: 100 }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Team",
                action: "getManyForSpace",
                params: {
                    spaceId: raw.sys.id,
                    query: (0, import_contentful_sdk_core56.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapTeamCollection(makeRequest2, data));
        },
        /**
         * Gets a Space Member
         * @param id Get Space Member by user_id
         * @returns Promise for a Space Member
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getSpaceMember(id))
         * .then((spaceMember) => console.log(spaceMember))
         * .catch(console.error)
         * ```
         */
        getSpaceMember(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SpaceMember",
                action: "get",
                params: { spaceId: raw.sys.id, spaceMemberId: id }
            }).then(data => wrapSpaceMember(makeRequest2, data));
        },
        /**
         * Gets a collection of Space Members
         * @param query
         * @returns Promise for a collection of Space Members
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getSpaceMembers({'limit': 100}))
         * .then((spaceMemberCollection) => console.log(spaceMemberCollection))
         * .catch(console.error)
         * ```
         */
        getSpaceMembers(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SpaceMember",
                action: "getMany",
                params: {
                    spaceId: raw.sys.id,
                    query: (0, import_contentful_sdk_core56.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapSpaceMemberCollection(makeRequest2, data));
        },
        /**
         * Gets a Space Membership
         * Warning: the user attribute in the space membership root is deprecated. The attribute has been moved inside the sys  object (i.e. sys.user).
         * @param id - Space Membership ID
         * @returns Promise for a Space Membership
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getSpaceMembership('id'))
         * .then((spaceMembership) => console.log(spaceMembership))
         * .catch(console.error)
         * ```
         */
        getSpaceMembership(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SpaceMembership",
                action: "get",
                params: { spaceId: raw.sys.id, spaceMembershipId: id }
            }).then(data => wrapSpaceMembership(makeRequest2, data));
        },
        /**
         * Gets a collection of Space Memberships
         * Warning: the user attribute in the space membership root is deprecated. The attribute has been moved inside the sys  object (i.e. sys.user).
         * @param query - Object with search parameters. Check the <a href="https://www.contentful.com/developers/docs/javascript/tutorials/using-js-cda-sdk/#retrieving-entries-with-search-parameters">JS SDK tutorial</a> and the <a href="https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters">REST API reference</a> for more details.
         * @returns Promise for a collection of Space Memberships
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getSpaceMemberships({'limit': 100})) // you can add more queries as 'key': 'value'
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getSpaceMemberships(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SpaceMembership",
                action: "getMany",
                params: {
                    spaceId: raw.sys.id,
                    query: (0, import_contentful_sdk_core56.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapSpaceMembershipCollection(makeRequest2, data));
        },
        /**
         * Creates a Space Membership
         * Warning: the user attribute in the space membership root is deprecated. The attribute has been moved inside the sys  object (i.e. sys.user).
         * @param  data - Object representation of the Space Membership to be created
         * @returns Promise for the newly created Space Membership
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.createSpaceMembership({
         *   admin: false,
         *   roles: [
         *     {
         *       type: 'Link',
         *       linkType: 'Role',
         *       id: '<role_id>'
         *     }
         *   ],
         *   email: 'foo@example.com'
         * }))
         * .then((spaceMembership) => console.log(spaceMembership))
         * .catch(console.error)
         * ```
         */
        createSpaceMembership(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SpaceMembership",
                action: "create",
                params: {
                    spaceId: raw.sys.id
                },
                payload: data
            }).then(response => wrapSpaceMembership(makeRequest2, response));
        },
        /**
         * Creates a Space Membership with a custom ID
         * Warning: the user attribute in the space membership root is deprecated. The attribute has been moved inside the sys  object (i.e. sys.user).
         * @param id - Space Membership ID
         * @param data - Object representation of the Space Membership to be created
         * @returns Promise for the newly created Space Membership
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.createSpaceMembershipWithId('<space-membership-id>', {
         *   admin: false,
         *   roles: [
         *     {
         *       type: 'Link',
         *       linkType: 'Role',
         *       id: '<role_id>'
         *     }
         *   ],
         *   email: 'foo@example.com'
         * }))
         * .then((spaceMembership) => console.log(spaceMembership))
         * .catch(console.error)
         * ```
         */
        createSpaceMembershipWithId(id, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SpaceMembership",
                action: "createWithId",
                params: {
                    spaceId: raw.sys.id,
                    spaceMembershipId: id
                },
                payload: data
            }).then(response => wrapSpaceMembership(makeRequest2, response));
        },
        /**
         * Gets a Team Space Membership
         * @param id - Team Space Membership ID
         * @returns Promise for a Team Space Membership
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getTeamSpaceMembership('team_space_membership_id'))
         * .then((teamSpaceMembership) => console.log(teamSpaceMembership))
         * .catch(console.error)
         * ```
         */
        getTeamSpaceMembership(teamSpaceMembershipId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "TeamSpaceMembership",
                action: "get",
                params: {
                    spaceId: raw.sys.id,
                    teamSpaceMembershipId
                }
            }).then(data => wrapTeamSpaceMembership(makeRequest2, data));
        },
        /**
         * Gets a collection of Team Space Memberships
         * @param query - Object with search parameters. Check the <a href="https://www.contentful.com/developers/docs/javascript/tutorials/using-js-cda-sdk/#retrieving-entries-with-search-parameters">JS SDK tutorial</a> and the <a href="https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters">REST API reference</a> for more details.
         * @returns Promise for a collection of Team Space Memberships
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getTeamSpaceMemberships())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getTeamSpaceMemberships(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "TeamSpaceMembership",
                action: "getMany",
                params: {
                    spaceId: raw.sys.id,
                    query: (0, import_contentful_sdk_core56.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapTeamSpaceMembershipCollection(makeRequest2, data));
        },
        /**
    * Creates a Team Space Membership
    * @param id - Team ID
    * @param data - Object representation of the Team Space Membership to be created
    * @returns Promise for the newly created Team Space Membership
    * @example ```javascript
    * const contentful = require('contentful-management')
    *
    * const client = contentful.createClient({
    *   accessToken: '<content_management_api_key>'
    * })
    *
    * client.getSpace('<space_id>')
    * .then((space) => space.createTeamSpaceMembership('team_id', {
    *   admin: false,
    *   roles: [
    *    {
           sys: {
    *       type: 'Link',
    *       linkType: 'Role',
    *       id: '<role_id>'
    *      }
    *    }
    *   ],
    * }))
    * .then((teamSpaceMembership) => console.log(teamSpaceMembership))
    * .catch(console.error)
    * ```
    */
        createTeamSpaceMembership(teamId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "TeamSpaceMembership",
                action: "create",
                params: {
                    spaceId: raw.sys.id,
                    teamId
                },
                payload: data
            }).then(response => wrapTeamSpaceMembership(makeRequest2, response));
        },
        /**
         * Gets a Api Key
         * @param id - API Key ID
         * @returns  Promise for a Api Key
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getApiKey('<apikey-id>'))
         * .then((apikey) => console.log(apikey))
         * .catch(console.error)
         * ```
         */
        getApiKey(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ApiKey",
                action: "get",
                params: {
                    spaceId: raw.sys.id,
                    apiKeyId: id
                }
            }).then(data => wrapApiKey(makeRequest2, data));
        },
        /**
         * Gets a collection of Api Keys
         * @returns Promise for a collection of Api Keys
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getApiKeys())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getApiKeys() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ApiKey",
                action: "getMany",
                params: {
                    spaceId: raw.sys.id
                }
            }).then(data => wrapApiKeyCollection(makeRequest2, data));
        },
        /**
         * Gets a collection of preview Api Keys
         * @returns Promise for a collection of Preview Api Keys
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getPreviewApiKeys())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getPreviewApiKeys() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "PreviewApiKey",
                action: "getMany",
                params: {
                    spaceId: raw.sys.id
                }
            }).then(data => wrapPreviewApiKeyCollection(makeRequest2, data));
        },
        /**
         * Gets a preview Api Key
         * @param id - Preview API Key ID
         * @returns  Promise for a Preview Api Key
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getPreviewApiKey('<preview-apikey-id>'))
         * .then((previewApikey) => console.log(previewApikey))
         * .catch(console.error)
         * ```
         */
        getPreviewApiKey(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "PreviewApiKey",
                action: "get",
                params: {
                    spaceId: raw.sys.id,
                    previewApiKeyId: id
                }
            }).then(data => wrapPreviewApiKey(makeRequest2, data));
        },
        /**
         * Creates a Api Key
         * @param payload - Object representation of the Api Key to be created
         * @returns Promise for the newly created Api Key
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.createApiKey({
         *   name: 'API Key name',
         *   environments:[
         *    {
         *     sys: {
         *      type: 'Link'
         *      linkType: 'Environment',
         *      id:'<environment_id>'
         *     }
         *    }
         *   ]
         *   }
         * }))
         * .then((apiKey) => console.log(apiKey))
         * .catch(console.error)
         * ```
         */
        createApiKey: function createApiKey(payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ApiKey",
                action: "create",
                params: { spaceId: raw.sys.id },
                payload
            }).then(data => wrapApiKey(makeRequest2, data));
        },
        /**
         * Creates a Api Key with a custom ID
         * @param id - Api Key ID
         * @param payload - Object representation of the Api Key to be created
         * @returns Promise for the newly created Api Key
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.createApiKeyWithId('<api-key-id>', {
         *   name: 'API Key name'
         *   environments:[
         *    {
         *     sys: {
         *      type: 'Link'
         *      linkType: 'Environment',
         *      id:'<environment_id>'
         *     }
         *    }
         *   ]
         *   }
         * }))
         * .then((apiKey) => console.log(apiKey))
         * .catch(console.error)
         * ```
         */
        createApiKeyWithId(id, payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ApiKey",
                action: "createWithId",
                params: { spaceId: raw.sys.id, apiKeyId: id },
                payload
            }).then(data => wrapApiKey(makeRequest2, data));
        },
        /**
         * Creates an EnvironmentAlias with a custom ID
         * @param environmentAliasId - EnvironmentAlias ID
         * @param data - Object representation of the EnvironmentAlias to be created
         * @returns Promise for the newly created EnvironmentAlias
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.createEnvironmentAliasWithId('<environment-alias-id>', {
         *   environment: {
         *     sys: { type: 'Link', linkType: 'Environment', id: 'targetEnvironment' }
         *   }
         * }))
         * .then((environmentAlias) => console.log(environmentAlias))
         * .catch(console.error)
         * ```
         */
        createEnvironmentAliasWithId(environmentAliasId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentAlias",
                action: "createWithId",
                params: { spaceId: raw.sys.id, environmentAliasId },
                payload: data
            }).then(response => wrapEnvironmentAlias(makeRequest2, response));
        },
        /**
         * Gets an Environment Alias
         * @param Environment Alias ID
         * @returns Promise for an Environment Alias
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironmentAlias('<alias-id>'))
         * .then((alias) => console.log(alias))
         * .catch(console.error)
         * ```
         */
        getEnvironmentAlias(environmentAliasId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentAlias",
                action: "get",
                params: { spaceId: raw.sys.id, environmentAliasId }
            }).then(data => wrapEnvironmentAlias(makeRequest2, data));
        },
        /**
         * Gets a collection of Environment Aliases
         * @returns Promise for a collection of Environment Aliases
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEnvironmentAliases()
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getEnvironmentAliases() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentAlias",
                action: "getMany",
                params: {
                    spaceId: raw.sys.id
                }
            }).then(data => wrapEnvironmentAliasCollection(makeRequest2, data));
        },
        /**
         * Query for scheduled actions in space.
         * @param query - Object with search parameters. The enviroment id field is mandatory. Check the <a href="https://www.contentful.com/developers/docs/references/content-management-api/#/reference/scheduled-actions/scheduled-actions-collection">REST API reference</a> for more details.
         * @returns Promise for the scheduled actions query
         *
         * @example ```javascript
         *  const contentful = require('contentful-management');
         *
         *  const client = contentful.createClient({
         *    accessToken: '<content_management_api_key>'
         *  })
         *
         *  client.getSpace('<space_id>')
         *    .then((space) => space.getScheduledActions({
         *      'environment.sys.id': '<environment_id>',
         *      'sys.status': 'scheduled'
         *    }))
         *    .then((scheduledActionCollection) => console.log(scheduledActionCollection.items))
         *    .catch(console.error)
         * ```
         */
        getScheduledActions(query2) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ScheduledAction",
                action: "getMany",
                params: { spaceId: raw.sys.id, query: query2 }
            }).then(response => wrapScheduledActionCollection(makeRequest2, response));
        },
        /**
         * Get a Scheduled Action in the current space by environment and ID.
         *
         * @throws if the Scheduled Action cannot be found or the user doesn't have permission to read schedules from the entity of the scheduled action itself.
         * @returns Promise with the Scheduled Action
         * @example ```javascript
         *  const contentful = require('contentful-management');
         *
         *  const client = contentful.createClient({
         *    accessToken: '<content_management_api_key>'
         *  })
         *
         *  client.getSpace('<space_id>')
         *    .then((space) => space.getScheduledAction({
         *      scheduledActionId: '<scheduled-action-id>',
         *      environmentId: '<environmentId>'
         *    }))
         *    .then((scheduledAction) => console.log(scheduledAction))
         *    .catch(console.error)
         * ```
         */
        getScheduledAction({ scheduledActionId, environmentId }) {
            const space = this.toPlainObject();
            return makeRequest2({
                entityType: "ScheduledAction",
                action: "get",
                params: {
                    spaceId: space.sys.id,
                    environmentId,
                    scheduledActionId
                }
            }).then(scheduledAction => wrapScheduledAction(makeRequest2, scheduledAction));
        },
        /**
         * Creates a scheduled action
         * @param data - Object representation of the scheduled action to be created
         * @returns Promise for the newly created scheduled actions
         * @example ```javascript
         *  const contentful = require('contentful-management');
         *
         *  const client = contentful.createClient({
         *    accessToken: '<content_management_api_key>'
         *  })
         *
         *  client.getSpace('<space_id>')
         *    .then((space) => space.createScheduledAction({
         *      entity: {
         *        sys: {
         *          type: 'Link',
         *          linkType: 'Entry',
         *          id: '<entry_id>'
         *        }
         *      },
         *      environment: {
         *        sys: {
         *          type: 'Link',
         *          linkType: 'Environment',
         *          id: '<environment_id>'
         *        }
         *      },
         *      action: 'publish',
         *      scheduledFor: {
         *        datetime: <ISO_date_string>,
         *        timezone: 'Europe/Berlin'
         *      }
         *    }))
         *    .then((scheduledAction) => console.log(scheduledAction))
         *    .catch(console.error)
         * ```
         */
        createScheduledAction(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ScheduledAction",
                action: "create",
                params: { spaceId: raw.sys.id },
                payload: data
            }).then(response => wrapScheduledAction(makeRequest2, response));
        },
        /**
         * Update a scheduled action
         * @param {object} options
         * @param options.scheduledActionId the id of the scheduled action to update
         * @param options.version the sys.version of the scheduled action to be updated
         * @param payload the scheduled actions object with updates, omitting sys object
         * @returns Promise containing a wrapped scheduled action with helper methods
         * @example ```javascript
         *  const contentful = require('contentful-management');
         *
         *  const client = contentful.createClient({
         *    accessToken: '<content_management_api_key>'
         *  })
         *
         *  client.getSpace('<space_id>')
         *    .then((space) => {
         *      return space.createScheduledAction({
         *        entity: {
         *          sys: {
         *            type: 'Link',
         *            linkType: 'Entry',
         *            id: '<entry_id>'
         *          }
         *        },
         *        environment: {
         *          sys: {
         *            type: 'Link',
         *            linkType: 'Environment',
         *            id: '<environment_id>'
         *          }
         *        },
         *        action: 'publish',
         *        scheduledFor: {
         *          datetime: <ISO_date_string>,
         *          timezone: 'Europe/Berlin'
         *        }
         *      })
         *      .then((scheduledAction) => {
         *        const { _sys, ...payload } = scheduledAction;
         *        return space.updateScheduledAction({
         *          ...payload,
         *          scheduledFor: {
         *            ...payload.scheduledFor,
         *            timezone: 'Europe/Paris'
         *          }
         *        })
         *      })
         *    .then((scheduledAction) => console.log(scheduledAction))
         *    .catch(console.error);
         * ```
         */
        updateScheduledAction({ scheduledActionId, payload, version }) {
            const spaceProps = this.toPlainObject();
            return makeRequest2({
                entityType: "ScheduledAction",
                action: "update",
                params: {
                    spaceId: spaceProps.sys.id,
                    version,
                    scheduledActionId
                },
                payload
            }).then(response => wrapScheduledAction(makeRequest2, response));
        },
        /**
         * Cancels a Scheduled Action.
         * Only cancels actions that have not yet executed.
         *
         * @param {object} options
         * @param options.scheduledActionId the id of the scheduled action to be canceled
         * @param options.environmentId the environment ID of the scheduled action to be canceled
         * @throws if the Scheduled Action cannot be found or the user doesn't have permissions in the entity in the action.
         * @returns Promise containing a wrapped Scheduled Action with helper methods
         * @example ```javascript
         *  const contentful = require('contentful-management');
         *
         *  const client = contentful.createClient({
         *    accessToken: '<content_management_api_key>'
         *  })
         *
         *  // Given that an Scheduled Action is scheduled
         *  client.getSpace('<space_id>')
         *    .then((space) => space.deleteScheduledAction({
         *        environmentId: '<environment-id>',
         *        scheduledActionId: '<scheduled-action-id>'
         *     }))
         *     // The scheduled Action sys.status is now 'canceled'
         *    .then((scheduledAction) => console.log(scheduledAction))
         *    .catch(console.error);
         * ```
         */
        deleteScheduledAction({ scheduledActionId, environmentId }) {
            const spaceProps = this.toPlainObject();
            return makeRequest2({
                entityType: "ScheduledAction",
                action: "delete",
                params: {
                    spaceId: spaceProps.sys.id,
                    environmentId,
                    scheduledActionId
                }
            }).then(response => wrapScheduledAction(makeRequest2, response));
        },
        /**
         * Gets a single AI Action.
         * @param aiActionId - AI Action ID
         * @returns Promise for an AI Action
         * @example
         * ```javascript
         * client.getSpace('<space_id>')
         *   .then((space) => space.getAiAction('<ai_action_id>'))
         *   .then((aiAction) => console.log(aiAction))
         *   .catch(console.error)
         * ```
         */
        getAiAction(aiActionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AiAction",
                action: "get",
                params: { spaceId: raw.sys.id, aiActionId }
            }).then(data => wrapAiAction(makeRequest2, data));
        },
        /**
         * Gets a collection of AI Actions.
         * @param query - Object with search parameters.
         * @returns Promise for a collection of AI Actions
         * @example
         * ```javascript
         * client.getSpace('<space_id>')
         *   .then((space) => space.getAiActions({ limit: 10 }))
         *   .then((response) => console.log(response.items))
         *   .catch(console.error)
         * ```
         */
        getAiActions(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AiAction",
                action: "getMany",
                params: { spaceId: raw.sys.id, query: query2 }
            }).then(data => wrapAiActionCollection(makeRequest2, data));
        },
        /**
         * Creates an AI Action.
         * @param data - Object representation of the AI Action to be created
         * @returns Promise for the newly created AI Action
         * @example
         * ```javascript
         * client.getSpace('<space_id>')
         *   .then((space) => space.createAiAction({
         *     name: 'My AI Action',
         *     description: 'Description here',
         *     configuration: { modelType: 'model-x', modelTemperature: 0.7 },
         *     instruction: { template: 'Do something: {{var.input}}', variables: [], conditions: [] },
         *     testCases: []
         *   }))
         *   .then((aiAction) => console.log(aiAction))
         *   .catch(console.error)
         * ```
         */
        createAiAction(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AiAction",
                action: "create",
                params: { spaceId: raw.sys.id },
                payload: data
            }).then(response => wrapAiAction(makeRequest2, response));
        },
        /**
         * Updates an AI Action.
         * @param aiActionId - AI Action ID
         * @param data - Object representation of the AI Action update
         * @returns Promise for the updated AI Action
         * @example
         * ```javascript
         * client.getSpace('<space_id>')
         *   .then((space) => space.updateAiAction('<ai_action_id>', { name: 'New Name', ... }))
         *   .then((aiAction) => console.log(aiAction))
         *   .catch(console.error)
         * ```
         */
        updateAiAction(aiActionId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AiAction",
                action: "update",
                params: { spaceId: raw.sys.id, aiActionId },
                payload: data,
                headers: { "X-Contentful-Version": data.sys.version ?? 0 }
            }).then(response => wrapAiAction(makeRequest2, response));
        },
        /**
         * Publishes an AI Action.
         * @param aiActionId - AI Action ID
         * @param data - Object representation of the AI Action to be published
         * @returns Promise for the published AI Action
         * @example
         * ```javascript
         * client.getSpace('<space_id>')
         *   .then((space) => space.publishAiAction('<ai_action_id>', { ... }))
         *   .then((aiAction) => console.log(aiAction))
         *   .catch(console.error)
         * ```
         */
        publishAiAction(aiActionId, { version }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AiAction",
                action: "publish",
                params: { spaceId: raw.sys.id, aiActionId, version }
            }).then(response => wrapAiAction(makeRequest2, response));
        },
        /**
         * Unpublishes an AI Action.
         * @param aiActionId - AI Action ID
         * @returns Promise for the unpublished AI Action
         * @example
         * ```javascript
         * client.getSpace('<space_id>')
         *   .then((space) => space.unpublishAiAction('<ai_action_id>'))
         *   .then((aiAction) => console.log(aiAction))
         *   .catch(console.error)
         * ```
         */
        unpublishAiAction(aiActionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AiAction",
                action: "unpublish",
                params: { spaceId: raw.sys.id, aiActionId }
            }).then(response => wrapAiAction(makeRequest2, response));
        },
        /**
         * Deletes an AI Action.
         * @param aiActionId - AI Action ID
         * @returns Promise for deletion (void)
         * @example
         * ```javascript
         * client.getSpace('<space_id>')
         *   .then((space) => space.deleteAiAction('<ai_action_id>'))
         *   .then(() => console.log('AI Action deleted'))
         *   .catch(console.error)
         * ```
         */
        deleteAiAction(aiActionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AiAction",
                action: "delete",
                params: { spaceId: raw.sys.id, aiActionId }
            });
        },
        /**
         * Gets a collection of Space Add-ons
         * @param query - Object with search parameters (skip, limit)
         * @returns Promise for a collection of Space Add-ons
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getSpaceAddOns())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getSpaceAddOns(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SpaceAddOn",
                action: "getMany",
                params: { spaceId: raw.sys.id, query: (0, import_contentful_sdk_core56.createRequestConfig)({ query: query2 }).params }
            }).then(data => wrapSpaceAddOnCollection(makeRequest2, data));
        },
        /**
         * Gets a collection of Eligible Licenses for the space
         * @param query - Object with search parameters. The API supports pagination with skip and limit parameters.
         * @returns Promise for a collection of Eligible Licenses that can be assigned to this space
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.getEligibleLicenses({ limit: 10, skip: 0 }))
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getEligibleLicenses(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EligibleLicense",
                action: "getMany",
                params: {
                    spaceId: raw.sys.id,
                    query: (0, import_contentful_sdk_core56.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapEligibleLicenseCollection(makeRequest2, data));
        },
        /**
         * Updates Space Add-on allocations
         * @param allocations - Array of add-on allocation updates
         * @returns Promise for the updated collection of Space Add-ons
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => space.updateSpaceAddOnAllocations([
         *   { add_on: 'contentTypes', allocation: 10 },
         *   { add_on: 'records', allocation: 1000 }
         * ]))
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        updateSpaceAddOnAllocations(allocations) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SpaceAddOn",
                action: "updateAllocations",
                params: { spaceId: raw.sys.id },
                payload: allocations
            }).then(data => wrapSpaceAddOnCollection(makeRequest2, data));
        }
    };
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/space.mjs
function wrapSpace(makeRequest2, data) {
    const space = (0, import_contentful_sdk_core57.toPlainObject)(index(data));
    const spaceApi = createSpaceApi(makeRequest2);
    const enhancedSpace = enhanceWithMethods(space, spaceApi);
    return (0, import_contentful_sdk_core57.freezeSys)(enhancedSpace);
}
var wrapSpaceCollection = wrapCollection(wrapSpace);
var wrapSpaceCursorPaginatedCollection = wrapCursorPaginatedCollection(wrapSpace);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/personal-access-token.mjs
var import_contentful_sdk_core58 = __toESM(require_cjs2(), 1);
function wrapPersonalAccessToken(makeRequest2, data) {
    const personalAccessToken = (0, import_contentful_sdk_core58.toPlainObject)(index(data));
    const personalAccessTokenWithMethods = enhanceWithMethods(personalAccessToken, {
        revoke: function () {
            return makeRequest2({
                entityType: "PersonalAccessToken",
                action: "revoke",
                params: { tokenId: data.sys.id }
            }).then(data2 => wrapPersonalAccessToken(makeRequest2, data2));
        }
    });
    return (0, import_contentful_sdk_core58.freezeSys)(personalAccessTokenWithMethods);
}
var wrapPersonalAccessTokenCollection = wrapCollection(wrapPersonalAccessToken);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/access-token.mjs
var import_contentful_sdk_core59 = __toESM(require_cjs2(), 1);
function wrapAccessToken(makeRequest2, data) {
    const AccessToken = (0, import_contentful_sdk_core59.toPlainObject)(index(data));
    const accessTokenWithMethods = enhanceWithMethods(AccessToken, {
        revoke: function () {
            return makeRequest2({
                entityType: "AccessToken",
                action: "revoke",
                params: { tokenId: data.sys.id }
            }).then(data2 => wrapAccessToken(makeRequest2, data2));
        }
    });
    return (0, import_contentful_sdk_core59.freezeSys)(accessTokenWithMethods);
}
var wrapAccessTokenCollection = wrapCollection(wrapAccessToken);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/organization.mjs
var import_contentful_sdk_core75 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-organization-api.mjs
var import_contentful_sdk_core74 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-definition.mjs
var import_contentful_sdk_core62 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-bundle.mjs
var import_contentful_sdk_core60 = __toESM(require_cjs2(), 1);
function createAppBundleApi(makeRequest2) {
    const getParams3 = data => ({
        organizationId: data.sys.organization.sys.id,
        appDefinitionId: data.sys.appDefinition.sys.id,
        appBundleId: data.sys.id
    });
    return {
        delete: function del45() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "AppBundle",
                action: "delete",
                params: getParams3(data)
            });
        }
    };
}
function wrapAppBundle(makeRequest2, data) {
    const appBundle = (0, import_contentful_sdk_core60.toPlainObject)(index(data));
    const appBundleWithMethods = enhanceWithMethods(appBundle, createAppBundleApi(makeRequest2));
    return (0, import_contentful_sdk_core60.freezeSys)(appBundleWithMethods);
}
var wrapAppBundleCollection = wrapCollection(wrapAppBundle);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/resource-provider.mjs
var import_contentful_sdk_core61 = __toESM(require_cjs2(), 1);
function createResourceProviderApi(makeRequest2) {
    return {
        /**
         * Sends an update to the server with any changes made to the object's properties
         * @returns Object returned from the server with updated changes.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinition('<app_def_id>'))
         * .then((appDefinition) => appDefinition.getResourceProvider())
         * .then((resourceProvider) => {
         *    resourceProvider.function.sys.id = '<new_contentful_function_id>'
         *    return resourceProvider.upsert()
         * })
         * .catch(console.error)
         * ```
         */
        upsert: function upsert7() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "ResourceProvider",
                action: "upsert",
                params: getParams2(data),
                headers: {},
                payload: getUpsertParams2(data)
            }).then(data2 => wrapResourceProvider(makeRequest2, data2));
        },
        /**
         * Deletes this object on the server.
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinition('<app_def_id>'))
         * .then((appDefinition) => appDefinition.getResourceProvider())
         * .then((resourceProvider) => resourceProvider.delete())
         * .catch(console.error)
         * ```
         */
        delete: function del45() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "ResourceProvider",
                action: "delete",
                params: getParams2(data)
            });
        },
        getResourceType: function getResourceType(id) {
            return makeRequest2({
                entityType: "ResourceType",
                action: "get",
                params: {
                    organizationId: this.sys.organization.sys.id,
                    appDefinitionId: this.sys.appDefinition.sys.id,
                    resourceTypeId: id
                }
            }).then(data => wrapResourceType(makeRequest2, data));
        },
        upsertResourceType: function upsertResourceType(id, data) {
            return makeRequest2({
                entityType: "ResourceType",
                action: "upsert",
                params: {
                    organizationId: this.sys.organization.sys.id,
                    appDefinitionId: this.sys.appDefinition.sys.id,
                    resourceTypeId: id
                },
                headers: {},
                payload: data
            }).then(data2 => wrapResourceType(makeRequest2, data2));
        },
        getResourceTypes: function getResourceTypes() {
            return makeRequest2({
                entityType: "ResourceType",
                action: "getMany",
                params: {
                    organizationId: this.sys.organization.sys.id,
                    appDefinitionId: this.sys.appDefinition.sys.id
                }
            }).then(data => {
                data.items = data.items.map(item => wrapResourceType(makeRequest2, item));
                return data;
            });
        }
    };
}
var getParams2 = data => ({
    organizationId: data.sys.organization.sys.id,
    appDefinitionId: data.sys.appDefinition.sys.id
});
var getUpsertParams2 = data => ({
    sys: { id: data.sys.id },
    type: data.type,
    function: data.function
});
function wrapResourceProvider(makeRequest2, data) {
    const resourceProvider = (0, import_contentful_sdk_core61.toPlainObject)(index(data));
    const ResourceProviderWithMethods = enhanceWithMethods(resourceProvider, createResourceProviderApi(makeRequest2));
    return (0, import_contentful_sdk_core61.freezeSys)(ResourceProviderWithMethods);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-app-definition-api.mjs
function createAppDefinitionApi(makeRequest2) {
    const getParams3 = data => ({
        appDefinitionId: data.sys.id,
        organizationId: data.sys.organization.sys.id
    });
    return {
        /**
         * Sends an update to the server with any changes made to the object's properties
         * @returns Object returned from the server with updated changes.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinition('<app_def_id>'))
         * .then((appDefinition) => {
         *   appDefinition.name = 'New App Definition name'
         *   return appDefinition.update()
         * })
         * .then((appDefinition) => console.log(`App Definition ${appDefinition.sys.id} updated.`))
         * .catch(console.error)
         * ```
         */
        update: function update37() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "AppDefinition",
                action: "update",
                params: getParams3(data),
                headers: {},
                payload: data
            }).then(data2 => wrapAppDefinition(makeRequest2, data2));
        },
        /**
         * Deletes this object on the server.
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinition('<app_def_id>'))
         * .then((appDefinition) => appDefinition.delete())
         * .then(() => console.log(`App Definition deleted.`))
         * .catch(console.error)
         * ```
         */
        delete: function del45() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "AppDefinition",
                action: "delete",
                params: getParams3(data)
            });
        },
        /**
         * Gets an app bundle
         * @param id - AppBundle ID
         * @returns Promise for an AppBundle
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinition('<app_def_id>'))
         * .then((appDefinition) => appDefinition.getAppBundle('<app_upload_id>'))
         * .then((appBundle) => console.log(appBundle))
         * .catch(console.error)
         * ```
         */
        getAppBundle(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppBundle",
                action: "get",
                params: {
                    appBundleId: id,
                    appDefinitionId: raw.sys.id,
                    organizationId: raw.sys.organization.sys.id
                }
            }).then(data => wrapAppBundle(makeRequest2, data));
        },
        /**
         * Gets a collection of AppBundles
         * @returns Promise for a collection of AppBundles
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinition('<app_def_id>'))
         * .then((appDefinition) => appDefinition.getAppBundles())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getAppBundles(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppBundle",
                action: "getMany",
                params: { organizationId: raw.sys.organization.sys.id, appDefinitionId: raw.sys.id, query: query2 }
            }).then(data => wrapAppBundleCollection(makeRequest2, data));
        },
        /**
         * Creates an app bundle
         * @param Object representation of the App Bundle to be created
         * @returns Promise for the newly created AppBundle
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinition('<app_def_id>'))
         * .then((appDefinition) => appDefinition.createAppBundle('<app_upload_id>'))
         * .then((appBundle) => console.log(appBundle))
         * .catch(console.error)
         * ```
         */
        createAppBundle(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppBundle",
                action: "create",
                params: {
                    appDefinitionId: raw.sys.id,
                    organizationId: raw.sys.organization.sys.id
                },
                payload: data
            }).then(data2 => wrapAppBundle(makeRequest2, data2));
        },
        /**
         * Gets a list of App Installations across an org for given organization and App Definition
         * If a spaceId is provided in the query object, it will return the App Installations for that specific space.
         * @returns Promise for the newly created AppBundle
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * client.getAppDefinition('<organization_id>', '<app_definition_id>')
         * .then((appDefinition) => appDefinition.getInstallationsForOrg(
         *   { spaceId: '<space_id>' } // optional
         * ))
         * .then((appInstallationsForOrg) => console.log(appInstallationsForOrg.items))
         * .catch(console.error)
         * ```
         */
        getInstallationsForOrg(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppDefinition",
                action: "getInstallationsForOrg",
                params: {
                    appDefinitionId: raw.sys.id,
                    organizationId: raw.sys.organization.sys.id,
                    query: query2
                }
            });
        },
        /**
         * Creates or updates a resource provider
         * @param data representation of the ResourceProvider
         * @returns Promise for the newly created or updated ResourceProvider
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * // You need a valid AppDefinition with an activated AppBundle that has a contentful function configured
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinition('<app_def_id>'))
         * .then((appDefinition) => appDefinition.upsertResourceProvider({
         *    sys: {
         *      id: '<resource_provider_id>'
         *    },
         *    type: 'function',
         *    function: {
         *      sys: {
         *        id: '<contentful_function_id>',
         *        type: 'Link'
         *        linkType: 'Function'
         *      }
         *    }
         * }))
         * .then((resourceProvider) => console.log(resourceProvider))
         * .catch(console.error)
         * ```
         */
        upsertResourceProvider(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ResourceProvider",
                action: "upsert",
                params: {
                    appDefinitionId: raw.sys.id,
                    organizationId: raw.sys.organization.sys.id
                },
                payload: data
            }).then(payload => wrapResourceProvider(makeRequest2, payload));
        },
        /**
         * Gets a Resource Provider
         * @returns Promise for a Resource Provider
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinition('<app_def_id>'))
         * .then((appDefinition) => appDefinition.getResourceProvider())
         * .then((resourceProvider) => console.log(resourceProvider))
         * .catch(console.error)
         * ```
         */
        getResourceProvider() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ResourceProvider",
                action: "get",
                params: {
                    appDefinitionId: raw.sys.id,
                    organizationId: raw.sys.organization.sys.id
                }
            }).then(payload => wrapResourceProvider(makeRequest2, payload));
        }
    };
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-definition.mjs
function wrapAppDefinition(makeRequest2, data) {
    const appDefinition = (0, import_contentful_sdk_core62.toPlainObject)(index(data));
    const appDefinitionWithMethods = enhanceWithMethods(appDefinition, createAppDefinitionApi(makeRequest2));
    return (0, import_contentful_sdk_core62.freezeSys)(appDefinitionWithMethods);
}
var wrapAppDefinitionCollection = wrapCollection(wrapAppDefinition);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/organization-membership.mjs
var import_contentful_sdk_core63 = __toESM(require_cjs2(), 1);
function createOrganizationMembershipApi(makeRequest2, organizationId) {
    const getParams3 = data => ({
        organizationMembershipId: data.sys.id,
        organizationId
    });
    return {
        update: function () {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "OrganizationMembership",
                action: "update",
                params: getParams3(raw),
                payload: raw
            }).then(data => wrapOrganizationMembership(makeRequest2, data, organizationId));
        },
        delete: function del45() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "OrganizationMembership",
                action: "delete",
                params: getParams3(raw)
            });
        }
    };
}
function wrapOrganizationMembership(makeRequest2, data, organizationId) {
    const organizationMembership = (0, import_contentful_sdk_core63.toPlainObject)(index(data));
    const organizationMembershipWithMethods = enhanceWithMethods(organizationMembership, createOrganizationMembershipApi(makeRequest2, organizationId));
    return (0, import_contentful_sdk_core63.freezeSys)(organizationMembershipWithMethods);
}
var wrapOrganizationMembershipCollection = wrapCollection(wrapOrganizationMembership);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/team-membership.mjs
var import_contentful_sdk_core64 = __toESM(require_cjs2(), 1);
function createTeamMembershipApi(makeRequest2) {
    const getParams3 = data => ({
        teamMembershipId: data.sys.id,
        teamId: data.sys.team.sys.id,
        organizationId: data.sys.organization.sys.id
    });
    return {
        update: function () {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "TeamMembership",
                action: "update",
                params: getParams3(raw),
                payload: raw
            }).then(data => wrapTeamMembership(makeRequest2, data));
        },
        delete: function del45() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "TeamMembership",
                action: "delete",
                params: getParams3(raw)
            });
        }
    };
}
function wrapTeamMembership(makeRequest2, data) {
    const teamMembership = (0, import_contentful_sdk_core64.toPlainObject)(index(data));
    const teamMembershipWithMethods = enhanceWithMethods(teamMembership, createTeamMembershipApi(makeRequest2));
    return (0, import_contentful_sdk_core64.freezeSys)(teamMembershipWithMethods);
}
var wrapTeamMembershipCollection = wrapCollection(wrapTeamMembership);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/organization-invitation.mjs
var import_contentful_sdk_core65 = __toESM(require_cjs2(), 1);
function wrapOrganizationInvitation(_makeRequest, data) {
    const invitation = (0, import_contentful_sdk_core65.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core65.freezeSys)(invitation);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-upload.mjs
var import_contentful_sdk_core66 = __toESM(require_cjs2(), 1);
function createAppUploadApi(makeRequest2) {
    const getParams3 = data => ({
        organizationId: data.sys.organization.sys.id,
        appUploadId: data.sys.id
    });
    return {
        delete: function del45() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "AppUpload",
                action: "delete",
                params: getParams3(data)
            });
        }
    };
}
function wrapAppUpload(makeRequest2, data) {
    const appUpload = (0, import_contentful_sdk_core66.toPlainObject)(index(data));
    const appUploadWithMethods = enhanceWithMethods(appUpload, createAppUploadApi(makeRequest2));
    return (0, import_contentful_sdk_core66.freezeSys)(appUploadWithMethods);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-signing-secret.mjs
var import_contentful_sdk_core67 = __toESM(require_cjs2(), 1);
function createSigningSecretApi(makeRequest2) {
    const getParams3 = data => ({
        organizationId: data.sys.organization.sys.id,
        appDefinitionId: data.sys.appDefinition.sys.id
    });
    return {
        delete: function del45() {
            const self2 = this;
            return makeRequest2({
                entityType: "AppSigningSecret",
                action: "delete",
                params: getParams3(self2)
            });
        }
    };
}
function wrapAppSigningSecret(makeRequest2, data) {
    const signingSecret = (0, import_contentful_sdk_core67.toPlainObject)(index(data));
    return enhanceWithMethods(signingSecret, createSigningSecretApi(makeRequest2));
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-event-subscription.mjs
var import_contentful_sdk_core68 = __toESM(require_cjs2(), 1);
function createEventSubscriptionApi(makeRequest2) {
    const getParams3 = data => ({
        organizationId: data.sys.organization.sys.id,
        appDefinitionId: data.sys.appDefinition.sys.id
    });
    return {
        delete: function del45() {
            const self2 = this;
            return makeRequest2({
                entityType: "AppEventSubscription",
                action: "delete",
                params: getParams3(self2)
            });
        }
    };
}
function wrapAppEventSubscription(makeRequest2, data) {
    const eventSubscription = (0, import_contentful_sdk_core68.toPlainObject)(index(data));
    return enhanceWithMethods(eventSubscription, createEventSubscriptionApi(makeRequest2));
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-key.mjs
var import_contentful_sdk_core69 = __toESM(require_cjs2(), 1);
function createKeyApi(makeRequest2) {
    const getParams3 = data => ({
        organizationId: data.sys.organization.sys.id,
        appDefinitionId: data.sys.appDefinition.sys.id,
        fingerprint: data.sys.id
    });
    return {
        delete: function del45() {
            const self2 = this;
            return makeRequest2({
                entityType: "AppKey",
                action: "delete",
                params: getParams3(self2)
            });
        }
    };
}
function wrapAppKey(makeRequest2, data) {
    const key = (0, import_contentful_sdk_core69.toPlainObject)(index(data));
    return enhanceWithMethods(key, createKeyApi(makeRequest2));
}
var wrapAppKeyCollection = wrapCollection(wrapAppKey);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-details.mjs
var import_contentful_sdk_core70 = __toESM(require_cjs2(), 1);
function createAppDetailsApi(makeRequest2) {
    const getParams3 = data => ({
        organizationId: data.sys.organization.sys.id,
        appDefinitionId: data.sys.appDefinition.sys.id
    });
    return {
        delete: function del45() {
            const self2 = this;
            return makeRequest2({
                entityType: "AppDetails",
                action: "delete",
                params: getParams3(self2)
            });
        }
    };
}
function wrapAppDetails(makeRequest2, data) {
    const appDetails = (0, import_contentful_sdk_core70.toPlainObject)(index(data));
    return enhanceWithMethods(appDetails, createAppDetailsApi(makeRequest2));
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/app-action.mjs
var import_contentful_sdk_core71 = __toESM(require_cjs2(), 1);
function createAppActionApi(makeRequest2) {
    const getParams3 = data => ({
        organizationId: data.sys.organization.sys.id,
        appDefinitionId: data.sys.appDefinition.sys.id,
        appActionId: data.sys.id
    });
    return {
        delete: function del45() {
            const data = this.toPlainObject();
            return makeRequest2({
                entityType: "AppAction",
                action: "delete",
                params: getParams3(data)
            });
        }
    };
}
function wrapAppAction(makeRequest2, data) {
    const appAction = (0, import_contentful_sdk_core71.toPlainObject)(index(data));
    const appActionWithMethods = enhanceWithMethods(appAction, createAppActionApi(makeRequest2));
    return (0, import_contentful_sdk_core71.freezeSys)(appActionWithMethods);
}
var wrapAppActionCollection = wrapCollection(wrapAppAction);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/available-license.mjs
var import_contentful_sdk_core72 = __toESM(require_cjs2(), 1);
function wrapAvailableLicense(makeRequest2, data) {
    return (0, import_contentful_sdk_core72.toPlainObject)(index(data));
}
var wrapAvailableLicenseCollection = wrapCollection(wrapAvailableLicense);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/semantic-settings.mjs
var import_contentful_sdk_core73 = __toESM(require_cjs2(), 1);
function wrapContentSemanticsSettings(_makeRequest, data) {
    const result = (0, import_contentful_sdk_core73.toPlainObject)(index(data));
    return (0, import_contentful_sdk_core73.freezeSys)(result);
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-organization-api.mjs
function createOrganizationApi(makeRequest2) {
    return {
        /**
         * Gets a collection of spaces in the organization
         * @param query - Object with search parameters. Check the <a href="https://www.contentful.com/developers/docs/javascript/tutorials/using-js-cda-sdk/#retrieving-entries-with-search-parameters">JS SDK tutorial</a> and the <a href="https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters">REST API reference</a> for more details.
         * @returns Promise a collection of Spaces in the organization
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<organization_id>')
         * .then((organization) => organization.getSpaces())
         * .then((spaces) => console.log(spaces))
         * .catch(console.error)
         * ```
         */
        getSpaces(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Space",
                action: "getManyForOrganization",
                params: {
                    organizationId: raw.sys.id,
                    query: (0, import_contentful_sdk_core74.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapSpaceCollection(makeRequest2, data));
        },
        /**
         * Gets a User
         * @returns Promise for a User
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<organization_id>')
         * .then((organization) => organization.getUser('id'))
         * .then((user) => console.log(user))
         * .catch(console.error)
         * ```
         */
        getUser(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "User",
                action: "getForOrganization",
                params: { organizationId: raw.sys.id, userId: id }
            }).then(data => wrapUser(makeRequest2, data));
        },
        /**
         * Gets a collection of Users in organization
         * @param query - Object with search parameters. Check the <a href="https://www.contentful.com/developers/docs/javascript/tutorials/using-js-cda-sdk/#retrieving-entries-with-search-parameters">JS SDK tutorial</a> and the <a href="https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters">REST API reference</a> for more details.
         * @returns Promise a collection of Users in organization
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<organization_id>')
         * .then((organization) => organization.getUsers())
         * .then((users) => console.log(users))
         * .catch(console.error)
         * ```
         */
        getUsers(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "User",
                action: "getManyForOrganization",
                params: {
                    organizationId: raw.sys.id,
                    query: (0, import_contentful_sdk_core74.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapUserCollection(makeRequest2, data));
        },
        /**
         * Gets an Organization Membership
         * @param id - Organization Membership ID
         * @returns Promise for an Organization Membership
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('organization_id')
         * .then((organization) => organization.getOrganizationMembership('organizationMembership_id'))
         * .then((organizationMembership) => console.log(organizationMembership))
         * .catch(console.error)
         * ```
         */
        getOrganizationMembership(id) {
            const raw = this.toPlainObject();
            const organizationId = raw.sys.id;
            return makeRequest2({
                entityType: "OrganizationMembership",
                action: "get",
                params: {
                    organizationId,
                    organizationMembershipId: id
                }
            }).then(data => wrapOrganizationMembership(makeRequest2, data, organizationId));
        },
        /**
         * Gets a collection of Organization Memberships
         * @param  params - Object with search parameters. Check the <a href="https://www.contentful.com/developers/docs/javascript/tutorials/using-js-cda-sdk/#retrieving-entries-with-search-parameters">JS SDK tutorial</a> and the <a href="https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters">REST API reference</a> for more details.
         * @returns Promise for a collection of Organization Memberships
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('organization_id')
         * .then((organization) => organization.getOrganizationMemberships({'limit': 100})) // you can add more queries as 'key': 'value'
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getOrganizationMemberships(params = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "OrganizationMembership",
                action: "getMany",
                params: {
                    organizationId: raw.sys.id,
                    ...params
                }
            }).then(data => wrapOrganizationMembershipCollection(makeRequest2, data, raw.sys.id));
        },
        /**
         * Creates a Team
         * @param data representation of the Team to be created
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.createTeam({
         *    name: 'new team',
         *    description: 'new team description'
         *  }))
         * .then((team) => console.log(team))
         * .catch(console.error)
         * ```
         */
        createTeam(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Team",
                action: "create",
                params: { organizationId: raw.sys.id },
                payload: data
            }).then(data2 => wrapTeam(makeRequest2, data2));
        },
        /**
         * Gets an Team
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('orgId')
         * .then((organization) => organization.getTeam('teamId'))
         * .then((team) => console.log(team))
         * .catch(console.error)
         * ```
         */
        getTeam(teamId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Team",
                action: "get",
                params: { organizationId: raw.sys.id, teamId }
            }).then(data => wrapTeam(makeRequest2, data));
        },
        /**
         * Gets all Teams in an organization
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('orgId')
         * .then((organization) => organization.getTeams())
         * .then((teams) => console.log(teams))
         * .catch(console.error)
         * ```
         */
        getTeams(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Team",
                action: "getMany",
                params: {
                    organizationId: raw.sys.id,
                    query: (0, import_contentful_sdk_core74.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapTeamCollection(makeRequest2, data));
        },
        /**
         * Creates a Team membership
         * @param teamId - Id of the team the membership will be created in
         * @param data - Object representation of the Team Membership to be created
         * @returns Promise for the newly created TeamMembership
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('organizationId')
         * .then((org) => org.createTeamMembership('teamId', {
         *    admin: true,
         *    organizationMembershipId: 'organizationMembershipId'
         *  }))
         * .then((teamMembership) => console.log(teamMembership))
         * .catch(console.error)
         * ```
         */
        createTeamMembership(teamId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "TeamMembership",
                action: "create",
                params: { organizationId: raw.sys.id, teamId },
                payload: data
            }).then(data2 => wrapTeamMembership(makeRequest2, data2));
        },
        /**
         * Gets an Team Membership from the team with given teamId
         * @returns Promise for an Team Membership
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('organizationId')
         * .then((organization) => organization.getTeamMembership('teamId', 'teamMembership_id'))
         * .then((teamMembership) => console.log(teamMembership))
         * .catch(console.error)
         * ```
         */
        getTeamMembership(teamId, teamMembershipId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "TeamMembership",
                action: "get",
                params: { organizationId: raw.sys.id, teamId, teamMembershipId }
            }).then(data => wrapTeamMembership(makeRequest2, data));
        },
        /**
         * Get all Team Memberships. If teamID is provided in the optional config object, it will return all Team Memberships in that team. By default, returns all team memberships for the organization.
         * @returns Promise for a Team Membership Collection
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('organizationId')
         * .then((organization) => organization.getTeamMemberships('teamId'))
         * .then((teamMemberships) => console.log(teamMemberships))
         * .catch(console.error)
         * ```
         */
        getTeamMemberships(opts = {}) {
            const { teamId, query: query2 = {} } = opts;
            const raw = this.toPlainObject();
            if (teamId) {
                return makeRequest2({
                    entityType: "TeamMembership",
                    action: "getManyForTeam",
                    params: {
                        organizationId: raw.sys.id,
                        teamId,
                        query: (0, import_contentful_sdk_core74.createRequestConfig)({ query: query2 }).params
                    }
                }).then(data => wrapTeamMembershipCollection(makeRequest2, data));
            }
            return makeRequest2({
                entityType: "TeamMembership",
                action: "getManyForOrganization",
                params: {
                    organizationId: raw.sys.id,
                    query: (0, import_contentful_sdk_core74.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapTeamMembershipCollection(makeRequest2, data));
        },
        /**
         * Get all Team Space Memberships. If teamID is provided in the optional config object, it will return all Team Space Memberships in that team. By default, returns all team space memberships across all teams in the organization.
         * @returns Promise for a Team Space Membership Collection
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('organizationId')
         * .then((organization) => organization.getTeamSpaceMemberships('teamId'))
         * .then((teamSpaceMemberships) => console.log(teamSpaceMemberships))
         * .catch(console.error)
         * ```
         */
        getTeamSpaceMemberships(opts = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "TeamSpaceMembership",
                action: "getManyForOrganization",
                params: {
                    organizationId: raw.sys.id,
                    query: (0, import_contentful_sdk_core74.createRequestConfig)({ query: opts.query || {} }).params,
                    teamId: opts.teamId
                }
            }).then(data => wrapTeamSpaceMembershipCollection(makeRequest2, data));
        },
        /**
         * Get a Team Space Membership with given teamSpaceMembershipId
         * @returns Promise for a Team Space Membership
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('organizationId')
         * .then((organization) => organization.getTeamSpaceMembership('teamSpaceMembershipId'))
         * .then((teamSpaceMembership) => console.log(teamSpaceMembership))
         * .catch(console.error)]
         * ```
         */
        getTeamSpaceMembership(teamSpaceMembershipId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "TeamSpaceMembership",
                action: "getForOrganization",
                params: {
                    organizationId: raw.sys.id,
                    teamSpaceMembershipId
                }
            }).then(data => wrapTeamSpaceMembership(makeRequest2, data));
        },
        /**
         * Gets an Space Membership in Organization
         * @param id - Organiztion Space Membership ID
         * @returns Promise for a Space Membership in an organization
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('organization_id')
         * .then((organization) => organization.getOrganizationSpaceMembership('organizationSpaceMembership_id'))
         * .then((organizationMembership) => console.log(organizationMembership))
         * .catch(console.error)
         * ```
         */
        getOrganizationSpaceMembership(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SpaceMembership",
                action: "getForOrganization",
                params: {
                    organizationId: raw.sys.id,
                    spaceMembershipId: id
                }
            }).then(data => wrapSpaceMembership(makeRequest2, data));
        },
        /**
         * Gets a collection Space Memberships in organization
         * @param query - Object with search parameters. Check the <a href="https://www.contentful.com/developers/docs/javascript/tutorials/using-js-cda-sdk/#retrieving-entries-with-search-parameters">JS SDK tutorial</a> and the <a href="https://www.contentful.com/developers/docs/references/content-delivery-api/#/reference/search-parameters">REST API reference</a> for more details.
         * @returns Promise for a Space Membership collection across all spaces in the organization
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('organization_id')
         * .then((organization) => organization.getOrganizationSpaceMemberships()) // you can add queries like 'limit': 100
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getOrganizationSpaceMemberships(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SpaceMembership",
                action: "getManyForOrganization",
                params: {
                    organizationId: raw.sys.id,
                    query: (0, import_contentful_sdk_core74.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapSpaceMembershipCollection(makeRequest2, data));
        },
        /**
         * Gets a collection of Available Licenses for the organization
         * @param query - Object with search parameters. The API supports pagination with skip and limit parameters.
         * @returns Promise for a collection of Available Licenses
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('organization_id')
         * .then((organization) => organization.getAvailableLicenses({ limit: 10, skip: 0 }))
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getAvailableLicenses(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AvailableLicense",
                action: "getMany",
                params: {
                    organizationId: raw.sys.id,
                    query: (0, import_contentful_sdk_core74.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapAvailableLicenseCollection(makeRequest2, data));
        },
        /**
         * Gets an Invitation in Organization
         * @returns Promise for a OrganizationInvitation in an organization
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((organization) => organization.getOrganizationInvitation('invitation_id'))
         * .then((invitation) => console.log(invitation))
         * .catch(console.error)
         * ```
         */
        getOrganizationInvitation(invitationId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "OrganizationInvitation",
                action: "get",
                params: {
                    organizationId: raw.sys.id,
                    invitationId
                }
            }).then(data => wrapOrganizationInvitation(makeRequest2, data));
        },
        /**
         * Create an Invitation in Organization
         * @returns Promise for a OrganizationInvitation in an organization
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         *  .then((organization) => organization.createOrganizationInvitation({
         *    email: 'user.email@example.com'
         *    firstName: 'User First Name'
         *    lastName: 'User Last Name'
         *    role: 'developer'
         *  })
         * .catch(console.error)
         * ```
         */
        createOrganizationInvitation(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "OrganizationInvitation",
                action: "create",
                params: {
                    organizationId: raw.sys.id
                },
                payload: data
            }).then(data2 => wrapOrganizationInvitation(makeRequest2, data2));
        },
        /**
         * Gets a collection of Roles
         * @returns Promise for a collection of Roles
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getRoles())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getRoles(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Role",
                action: "getManyForOrganization",
                params: { organizationId: raw.sys.id, query: (0, import_contentful_sdk_core74.createRequestConfig)({ query: query2 }).params }
            }).then(data => wrapRoleCollection(makeRequest2, data));
        },
        /**
         * Creates an app definition
         * @param Object representation of the App Definition to be created
         * @returns Promise for the newly created AppDefinition
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.createAppDefinition({
         *    name: 'Example app',
         *    locations: [{ location: 'app-config' }],
         *    src: "http://my-app-host.com/my-app"
         *  }))
         * .then((appDefinition) => console.log(appDefinition))
         * .catch(console.error)
         * ```
         */
        createAppDefinition(data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppDefinition",
                action: "create",
                params: { organizationId: raw.sys.id },
                payload: data
            }).then(data2 => wrapAppDefinition(makeRequest2, data2));
        },
        /**
         * Gets all app definitions
         * @returns Promise for a collection of App Definitions
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinitions())
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getAppDefinitions(query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppDefinition",
                action: "getMany",
                params: { organizationId: raw.sys.id, query: query2 }
            }).then(data => wrapAppDefinitionCollection(makeRequest2, data));
        },
        /**
         * Gets an app definition
         * @returns Promise for an App Definition
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDefinition('<app_definition_id>'))
         * .then((appDefinition) => console.log(appDefinition))
         * .catch(console.error)
         * ```
         */
        getAppDefinition(id) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppDefinition",
                action: "get",
                params: { organizationId: raw.sys.id, appDefinitionId: id }
            }).then(data => wrapAppDefinition(makeRequest2, data));
        },
        /**
         * Gets an app upload
         * @returns Promise for an App Upload
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppUpload('<app_upload_id>'))
         * .then((appUpload) => console.log(appUpload))
         * .catch(console.error)
         * ```
         */
        getAppUpload(appUploadId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppUpload",
                action: "get",
                params: { organizationId: raw.sys.id, appUploadId }
            }).then(data => wrapAppUpload(makeRequest2, data));
        },
        /**
         * Creates an app upload
         * @returns Promise for an App Upload
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.createAppUpload('some_zip_file'))
         * .then((appUpload) => console.log(appUpload))
         * .catch(console.error)
         * ```
         */
        createAppUpload(file) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppUpload",
                action: "create",
                params: { organizationId: raw.sys.id },
                payload: { file }
            }).then(data => wrapAppUpload(makeRequest2, data));
        },
        /**
         * Creates or updates an app signing secret
         * @returns Promise for an App SigningSecret
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.upsertAppSigningSecret('app_definition_id', { value: 'tsren3s1....wn1e' }))
         * .then((appSigningSecret) => console.log(appSigningSecret))
         * .catch(console.error)
         * ```
         */
        upsertAppSigningSecret(appDefinitionId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppSigningSecret",
                action: "upsert",
                params: { organizationId: raw.sys.id, appDefinitionId },
                payload: data
            }).then(payload => wrapAppSigningSecret(makeRequest2, payload));
        },
        /**
         * Gets an app signing secret
         * @returns Promise for an App SigningSecret
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppSigningSecret('app_definition_id'))
         * .then((appSigningSecret) => console.log(appSigningSecret))
         * .catch(console.error)
         * ```
         */
        getAppSigningSecret(appDefinitionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppSigningSecret",
                action: "get",
                params: { organizationId: raw.sys.id, appDefinitionId }
            }).then(payload => wrapAppSigningSecret(makeRequest2, payload));
        },
        /**
         * Deletes an app signing secret
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.deleteAppSigningSecret('app_definition_id'))
         * .then((result) => console.log(result))
         * .catch(console.error)
         * ```
         */
        deleteAppSigningSecret(appDefinitionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppSigningSecret",
                action: "delete",
                params: { organizationId: raw.sys.id, appDefinitionId }
            }).then(() => {});
        },
        /**
         * Creates or updates an app event subscription
         * @returns Promise for an App Event Subscription
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.upsertAppEventSubscription('app_definition_id', { targetUrl: '<target_url>', topics: ['<topic>'] }))
         * .then((appEventSubscription) => console.log(appEventSubscription))
         * .catch(console.error)
         * ```
         */
        upsertAppEventSubscription(appDefinitionId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppEventSubscription",
                action: "upsert",
                params: { organizationId: raw.sys.id, appDefinitionId },
                payload: data
            }).then(payload => wrapAppEventSubscription(makeRequest2, payload));
        },
        /**
         * Gets an app event subscription
         * @returns Promise for an App Event Subscription
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppEventSubscription('app_definition_id'))
         * .then((appEventSubscription) => console.log(appEventSubscription))
         * .catch(console.error)
         * ```
         */
        getAppEventSubscription(appDefinitionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppEventSubscription",
                action: "get",
                params: { organizationId: raw.sys.id, appDefinitionId }
            }).then(payload => wrapAppEventSubscription(makeRequest2, payload));
        },
        /**
         * Deletes the current App Event Subscription for the given App
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.deleteAppEventSubscription('app_definition_id'))
         * .then((result) => console.log(result))
         * .catch(console.error)
         * ```
         */
        deleteAppEventSubscription(appDefinitionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppEventSubscription",
                action: "delete",
                params: { organizationId: raw.sys.id, appDefinitionId }
            }).then(() => {});
        },
        /**
         * Creates or updates an app event subscription
         * @returns Promise for an App Event Subscription
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * // generate a new private key
         * client.getOrganization('<org_id>')
         * .then((org) => org.upsertAppEventSubscription('app_definition_id', { generate: true }))
         * .then((appEventSubscription) => console.log(appEventSubscription))
         * .catch(console.error)
         *
         * // or use an existing JSON Web Key
         * client.getOrganization('<org_id>')
         * .then((org) => org.upsertAppEventSubscription('app_definition_id', { jwk: 'jwk' }))
         * .then((appEventSubscription) => console.log(appEventSubscription))
         * .catch(console.error)
         * ```
         */
        createAppKey(appDefinitionId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppKey",
                action: "create",
                params: { organizationId: raw.sys.id, appDefinitionId },
                payload: data
            }).then(payload => wrapAppKey(makeRequest2, payload));
        },
        /**
         * Gets an app key by fingerprint
         * @returns Promise for an App Key
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppKey('app_definition_id', 'fingerprint'))
         * .then((appKey) => console.log(appKey))
         * .catch(console.error)
         * ```
         */
        getAppKey(appDefinitionId, fingerprint) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppKey",
                action: "get",
                params: { organizationId: raw.sys.id, appDefinitionId, fingerprint }
            }).then(payload => wrapAppKey(makeRequest2, payload));
        },
        /**
         * Gets all keys for the given app
         * @returns Promise for an array of App Keys
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * // with default pagination
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppKeys('app_definition_id'))
         * .then((appKeys) => console.log(appKeys))
         * .catch(console.error)
         *
         * // with explicit pagination
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppKeys('app_definition_id', { skip: 'skip', limit: 'limit' }))
         * .then((appKeys) => console.log(appKeys))
         * .catch(console.error)
         * ```
         */
        getAppKeys(appDefinitionId, query2 = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppKey",
                action: "getMany",
                params: {
                    organizationId: raw.sys.id,
                    appDefinitionId,
                    query: (0, import_contentful_sdk_core74.createRequestConfig)({ query: query2 }).params
                }
            }).then(payload => wrapAppKeyCollection(makeRequest2, payload));
        },
        /**
         * Deletes an app key by fingerprint.
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.deleteAppKey('app_definition_id', 'fingerprint'))
         * .then((result) => console.log(result))
         * .catch(console.error)
         * ```
         */
        deleteAppKey(appDefinitionId, fingerprint) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppKey",
                action: "delete",
                params: { organizationId: raw.sys.id, appDefinitionId, fingerprint }
            }).then(() => {});
        },
        /**
         * Creates or updates an app details entity
         * @returns Promise for an App Details
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.upsertAppDetails('app_definition_id',
         *   { icon: { value: 'base_64_image', type: 'base64' }}
         *  ))
         * .then((appDetails) => console.log(appDetails))
         * .catch(console.error)
         * ```
         */
        upsertAppDetails(appDefinitionId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppDetails",
                action: "upsert",
                params: { organizationId: raw.sys.id, appDefinitionId },
                payload: data
            }).then(payload => wrapAppDetails(makeRequest2, payload));
        },
        /**
         * Gets an app details entity
         * @returns Promise for an App Details
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppDetails('app_definition_id'))
         * .then((appDetails) => console.log(appDetails))
         * .catch(console.error)
         * ```
         */
        getAppDetails(appDefinitionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppDetails",
                action: "get",
                params: { organizationId: raw.sys.id, appDefinitionId }
            }).then(payload => wrapAppDetails(makeRequest2, payload));
        },
        /**
         * Deletes an app details entity.
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.deleteAppDetails('app_definition_id'))
         * .then((result) => console.log(result))
         * .catch(console.error)
         * ```
         */
        deleteAppDetails(appDefinitionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppDetails",
                action: "delete",
                params: { organizationId: raw.sys.id, appDefinitionId }
            }).then(() => {});
        },
        /**
         * Creates an app action entity.
         * @returns Promise that resolves an App Action entity
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.createAppAction('app_definition_id', {
         *    type: 'endpoint',
         *    name: 'my nice new app action',
         *    url: 'https://www.somewhere.com/action'
         *  }))
         * .then((appAction) => console.log(appAction))
         * .catch(console.error)
         * ```
         */
        createAppAction(appDefinitionId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppAction",
                action: "create",
                params: { organizationId: raw.sys.id, appDefinitionId },
                payload: data
            }).then(payload => wrapAppAction(makeRequest2, payload));
        },
        /**
         * Updates an existing app action entity.
         * @returns Promise that resolves an App Action entity
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.updateAppAction('app_definition_id', 'app_action_id', {
         *    type: 'endpoint',
         *    name: 'my nice updated app action',
         *    url: 'https://www.somewhere-else.com/action'
         *  }))
         * .then((appAction) => console.log(appAction))
         * .catch(console.error)
         * ```
         */
        updateAppAction(appDefinitionId, appActionId, data) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppAction",
                action: "update",
                params: { organizationId: raw.sys.id, appDefinitionId, appActionId },
                payload: data
            }).then(payload => wrapAppAction(makeRequest2, payload));
        },
        /**
         * Deletes an app action entity.
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.deleteAppAction('app_definition_id', 'app_action_id'))
         * .then((result) => console.log(result))
         * .catch(console.error)
         * ```
         */
        deleteAppAction(appDefinitionId, appActionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppAction",
                action: "delete",
                params: { organizationId: raw.sys.id, appDefinitionId, appActionId }
            }).then(() => {});
        },
        /**
         * Gets an existing app action entity.
         * @returns Promise that resolves an App Action entity
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppAction('app_definition_id', 'app_action_id'))
         * .then((appAction) => console.log(appAction))
         * .catch(console.error)
         * ```
         */
        getAppAction(appDefinitionId, appActionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppAction",
                action: "get",
                params: { organizationId: raw.sys.id, appDefinitionId, appActionId }
            }).then(payload => wrapAppAction(makeRequest2, payload));
        },
        /**
         * Gets existing app actions for an App Definition.
         * @returns Promise that resolves an App Action entity
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => org.getAppActions('app_definition_id'))
         * .then((appActions) => console.log(appActions))
         * .catch(console.error)
         * ```
         */
        getAppActions(appDefinitionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "AppAction",
                action: "getMany",
                params: { organizationId: raw.sys.id, appDefinitionId }
            }).then(payload => wrapAppActionCollection(makeRequest2, payload));
        },
        /**
         * Gets an app function
         * @param appDefinitionId
         * @param functionId
         * @returns Promise for a Function
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * const org = await client.getOrganization('<org_id>')
         * const functions = await org.getFunction('<app_definition_id>', '<function_id>')
         */
        getFunction(appDefinitionId, functionId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Function",
                action: "get",
                params: { organizationId: raw.sys.id, appDefinitionId, functionId }
            }).then(payload => wrapFunction(makeRequest2, payload));
        },
        /**
         * Gets a collection of app functions.
         * @param appDefinitionId
         * @param {import('../common-types').AcceptsQueryOptions} query  - optional query parameter for filtering functions by action
         * @returns Promise for a Function
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * const org = await client.getOrganization('<org_id>')
         * const functions = await org.getFunctions('<app_definition_id>', { 'accepts[all]': '<action>' })
         */
        getFunctions(appDefinitionId, query2) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "Function",
                action: "getMany",
                params: { organizationId: raw.sys.id, appDefinitionId, query: query2 }
            }).then(payload => wrapFunctionCollection(makeRequest2, payload));
        },
        /**
         * Gets the semantic settings for the organization
         * @return Promise for ContentSemanticsSettings
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * const org = await client.getOrganization('<org_id>')
         * const settings = await org.getSemanticSettings()
         */
        getSemanticSettings() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "SemanticSettings",
                action: "get",
                params: { organizationId: raw.sys.id }
            }).then(data => wrapContentSemanticsSettings(makeRequest2, data));
        },
        /**
         * Gets all content semantics indexes for the organization
         * @return Promise for a collection of ContentSemanticsIndex
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * const org = await client.getOrganization('<org_id>')
         * const indexes = await org.getContentSemanticsIndexes()
         */
        getContentSemanticsIndexes() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ContentSemanticsIndex",
                action: "getMany",
                params: { organizationId: raw.sys.id }
            }).then(data => wrapContentSemanticsIndexCollection(makeRequest2, data));
        },
        /**
         * Gets a single content semantics index by ID
         * @param indexId - ID of the content semantics index
         * @return Promise for a ContentSemanticsIndex
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * const org = await client.getOrganization('<org_id>')
         * const index = await org.getContentSemanticsIndex('<index_id>')
         */
        getContentSemanticsIndex(indexId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ContentSemanticsIndex",
                action: "get",
                params: { organizationId: raw.sys.id, indexId }
            }).then(data => wrapContentSemanticsIndex(makeRequest2, data));
        },
        /**
         * Creates a new content semantics index for the organization
         * @param payload - Object containing spaceId and locale
         * @return Promise for the created ContentSemanticsIndex
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * const org = await client.getOrganization('<org_id>')
         * const index = await org.createContentSemanticsIndex({ spaceId: '<space_id>', locale: 'en-US' })
         */
        createContentSemanticsIndex(payload) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ContentSemanticsIndex",
                action: "create",
                params: { organizationId: raw.sys.id },
                payload
            }).then(data => wrapContentSemanticsIndex(makeRequest2, data));
        },
        /**
         * Deletes a content semantics index by ID
         * @param indexId - ID of the content semantics index to delete
         * @return Promise for the deletion
         * @example ```javascript
         * const contentful = require('contentful-management')
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * const org = await client.getOrganization('<org_id>')
         * await org.deleteContentSemanticsIndex('<index_id>')
         */
        deleteContentSemanticsIndex(indexId) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "ContentSemanticsIndex",
                action: "delete",
                params: { organizationId: raw.sys.id, indexId }
            });
        }
    };
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/organization.mjs
function wrapOrganization(makeRequest2, data) {
    const org = (0, import_contentful_sdk_core75.toPlainObject)(index(data));
    const orgApi = createOrganizationApi(makeRequest2);
    const enhancedOrganization = enhanceWithMethods(org, orgApi);
    return (0, import_contentful_sdk_core75.freezeSys)(enhancedOrganization);
}
var wrapOrganizationCollection = wrapCollection(wrapOrganization);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/usage.mjs
var import_contentful_sdk_core76 = __toESM(require_cjs2(), 1);
function wrapUsage(_makeRequest, data) {
    const usage = (0, import_contentful_sdk_core76.toPlainObject)(index(data));
    const usageWithMethods = enhanceWithMethods(usage, {});
    return (0, import_contentful_sdk_core76.freezeSys)(usageWithMethods);
}
var wrapUsageCollection = wrapCollection(wrapUsage);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/environment-template.mjs
var import_contentful_sdk_core78 = __toESM(require_cjs2(), 1);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-environment-template-api.mjs
var import_contentful_sdk_core77 = __toESM(require_cjs2(), 1);
function createEnvironmentTemplateApi(makeRequest2, organizationId) {
    return {
        /**
         * Updates a environment template
         * @returns Promise for new version of the template
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getEnvironmentTemplate('<organization_id>', '<environment_template_id>')
         * .then((environmentTemplate) => {
         *   environmentTemplate.name = 'New name'
         *   return environmentTemplate.update()
         * })
         * .then((environmentTemplate) =>
         *   console.log(`Environment template ${environmentTemplate.sys.id} renamed.`)
         * ).catch(console.error)
         * ```
         */
        update: function updateEnvironmentTemplate() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentTemplate",
                action: "update",
                params: { organizationId, environmentTemplateId: raw.sys.id },
                payload: raw
            }).then(data => wrapEnvironmentTemplate(makeRequest2, data, organizationId));
        },
        /**
         * Updates environment template version data
         * @param version.versionName - Name of the environment template version
         * @param version.versionDescription - Description of the environment template version
         * @returns Promise for an updated EnvironmentTemplate
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getEnvironmentTemplate('<organization_id>', '<environment_template_id>')
         * .then((environmentTemplate) => {
         *   return environmentTemplate.updateVersion({
         *     versionName: 'New Name',
         *     versionDescription: 'New Description',
         *   })
         * })
         * .then((environmentTemplate) =>
         *   console.log(`Environment template version ${environmentTemplate.sys.id} renamed.`)
         * ).catch(console.error)
         * ```
         */
        updateVersion: function updateEnvironmentTemplateVersion({ versionName, versionDescription }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentTemplate",
                action: "versionUpdate",
                params: { organizationId, environmentTemplateId: raw.sys.id, version: raw.sys.version },
                payload: { versionName, versionDescription }
            }).then(data => wrapEnvironmentTemplate(makeRequest2, data, organizationId));
        },
        /**
         * Deletes the environment template
         * @returns Promise for the deletion. It contains no data, but the Promise error case should be handled.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getEnvironmentTemplate('<organization_id>', '<environment_template_id>')
         *   .then((environmentTemplate) => environmentTemplate.delete())
         *   .then(() => console.log('Environment template deleted.'))
         *   .catch(console.error)
         * ```
         */
        delete: function deleteEnvironmentTemplate() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentTemplate",
                action: "delete",
                params: { organizationId, environmentTemplateId: raw.sys.id }
            });
        },
        /**
         * Gets a collection of all versions for the environment template
         * @returns Promise for a EnvironmentTemplate
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         * client.getEnvironmentTemplate('<organization_id>', '<environment_template_id>')
         * .then((environmentTemplate) => environmentTemplate.getVersions())
         * .then((environmentTemplateVersions) => console.log(environmentTemplateVersions.items))
         * .catch(console.error)
         * ```
         */
        getVersions: function getEnvironmentTemplateVersions() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentTemplate",
                action: "versions",
                params: {
                    organizationId,
                    environmentTemplateId: raw.sys.id
                }
            }).then(data => wrapEnvironmentTemplateCollection(makeRequest2, data, organizationId));
        },
        /**
         * Gets a collection of all installations for the environment template
         * @param [installationParams.spaceId] - Space ID to filter installations by space and environment
         * @param [installationParams.environmentId] - Environment ID to filter installations by space and environment
         * @param [installationParams.latestOnly] - Boolean flag to only return the latest installation per environment
         * @returns Promise for a collection of EnvironmentTemplateInstallations
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getEnvironmentTemplate('<organization_id>', '<environment_template_id>')
         * .then((environmentTemplate) => environmentTemplate.getInstallations())
         * .then((environmentTemplateInstallations) =>
         *   console.log(environmentTemplateInstallations.items)
         * )
         * .catch(console.error)
         * ```
         */
        getInstallations: function getEnvironmentTemplateInstallations({ spaceId, environmentId, latestOnly, ...query2 } = {}) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentTemplateInstallation",
                action: "getMany",
                params: {
                    organizationId,
                    environmentTemplateId: raw.sys.id,
                    query: { ...(0, import_contentful_sdk_core77.createRequestConfig)({ query: query2 }).params },
                    spaceId,
                    environmentId,
                    latestOnly
                }
            }).then(data => wrapEnvironmentTemplateInstallationCollection(makeRequest2, data));
        },
        /**
         * Validates an environment template against a given space and environment
         * @param params.spaceId - Space ID where the template should be installed into
         * @param params.environmentId - Environment ID where the template should be installed into
         * @param [params.version] - Version of the template
         * @param [params.installation.takeover] - Already existing Content types to takeover in the target environment
         * @param [params.changeSet] - Change set which should be applied
         * @returns Promise for a EnvironmentTemplateValidation
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getEnvironmentTemplate('<organization_id>', '<environment_template_id>')
         * .then((environmentTemplate) => environmentTemplate.validate({
         *   spaceId: '<space_id>',
         *   environmentId: '<environment_id>',
         *   version: <version>,
         * }))
         * .then((validationResult) => console.log(validationResult))
         * .catch(console.error)
         * ```
         */
        validate: function validateEnvironmentTemplate({ spaceId, environmentId, version, takeover, changeSet }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentTemplate",
                action: "validate",
                params: {
                    spaceId,
                    version,
                    environmentId,
                    environmentTemplateId: raw.sys.id
                },
                payload: {
                    ...(takeover && { takeover }),
                    ...(changeSet && { changeSet })
                }
            });
        },
        /**
         * Installs a template against a given space and environment
         * @param params.spaceId - Space ID where the template should be installed into
         * @param params.environmentId - Environment ID where the template should be installed into
         * @param params.installation.version- Template version which should be installed
         * @param [params.installation.takeover] - Already existing Content types tp takeover in the target environment
         * @param [params.changeSet] - Change set which should be applied
         * @returns Promise for a EnvironmentTemplateInstallation
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getEnvironmentTemplate('<organization_id>', '<environment_template_id>')
         * .then((environmentTemplate) => environmentTemplate.validate({
         *   spaceId: '<space_id>',
         *   environmentId: '<environment_id>',
         *   installation: {
         *     version: <version>,
         *   }
         * }))
         * .then((installation) => console.log(installation))
         * .catch(console.error)
         * ```
         */
        install: function installEnvironmentTemplate({ spaceId, environmentId, installation }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentTemplate",
                action: "install",
                params: {
                    spaceId,
                    environmentId,
                    environmentTemplateId: raw.sys.id
                },
                payload: installation
            });
        },
        /**
         * Disconnects the template from a given environment
         * @param params.spaceId - Space ID where the template should be installed into
         * @param params.environmentId - Environment ID where the template should be installed into
         * @returns Promise for the disconnection with no data
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getEnvironmentTemplate('<organization_id>', '<environment_template_id>')
         * .then(environmentTemplate) => environmentTemplate.disconnected())
         * .then(() => console.log('Template disconnected'))
         * .catch(console.error)
         * ```
         */
        disconnect: function disconnectEnvironmentTemplate({ spaceId, environmentId }) {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "EnvironmentTemplate",
                action: "disconnect",
                params: {
                    spaceId,
                    environmentId,
                    environmentTemplateId: raw.sys.id
                }
            });
        }
    };
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/environment-template.mjs
function wrapEnvironmentTemplate(makeRequest2, data, organizationId) {
    const environmentTemplate = (0, import_contentful_sdk_core78.toPlainObject)(index(data));
    const environmentTemplateApi = createEnvironmentTemplateApi(makeRequest2, organizationId);
    const enhancedEnvironmentTemplate = enhanceWithMethods(environmentTemplate, environmentTemplateApi);
    return (0, import_contentful_sdk_core78.freezeSys)(enhancedEnvironmentTemplate);
}
var wrapEnvironmentTemplateCollection = wrapCursorPaginatedCollection(wrapEnvironmentTemplate);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/oauth-application.mjs
var import_contentful_sdk_core79 = __toESM(require_cjs2(), 1);
var ScopeValues;
(function (ScopeValues2) {
    ScopeValues2["Read"] = "content_management_read";
    ScopeValues2["Manage"] = "content_management_manage";
})(ScopeValues || (ScopeValues = {}));
function createOAuthApplicationApi(makeRequest2, userId) {
    const getParams3 = data => ({
        userId,
        oauthApplicationId: data.sys.id
    });
    return {
        /**
         * Updates an OAuth application
         * @returns Promise for the updated OAuth application
         */
        async update() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "OAuthApplication",
                action: "update",
                params: getParams3(raw),
                payload: raw
            });
        },
        /**
         * Deletes an OAuth application
         * @returns Promise for the deleted OAuth application
         */
        async delete() {
            const raw = this.toPlainObject();
            return makeRequest2({
                entityType: "OAuthApplication",
                action: "delete",
                params: getParams3(raw)
            });
        }
    };
}
function wrapOAuthApplication(makeRequest2, data, userId) {
    const oauthApplication = (0, import_contentful_sdk_core79.toPlainObject)(index(data));
    const oauthApplicationWithMethods = enhanceWithMethods(oauthApplication, createOAuthApplicationApi(makeRequest2, userId));
    return (0, import_contentful_sdk_core79.freezeSys)(oauthApplicationWithMethods);
}
var wrapOAuthApplicationCollection = wrapCursorPaginatedCollection(wrapOAuthApplication);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/create-contentful-api.mjs
function createClientApi(makeRequest2) {
    return {
        /**
         * Gets all environment templates for a given organization with the lasted version
         * @param organizationId - Organization ID
         * @returns Promise for a collection of EnvironmentTemplates
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getEnvironmentTemplates('<organization_id>')
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getEnvironmentTemplates: function getEnvironmentTemplates(organizationId, query2 = {}) {
            return makeRequest2({
                entityType: "EnvironmentTemplate",
                action: "getMany",
                params: { organizationId, query: (0, import_contentful_sdk_core80.createRequestConfig)({ query: query2 }).params }
            }).then(data => wrapEnvironmentTemplateCollection(makeRequest2, data, organizationId));
        },
        /**
         * Gets the lasted version environment template if params.version is not specified
         * @param params.organizationId - Organization ID
         * @param params.environmentTemplateId - Environment template ID
         * @param [params.version] - Template version number to return a specific version of the environment template
         * @returns Promise for a EnvironmentTemplate
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getEnvironmentTemplate({
         *   organizationId: '<organization_id>',
         *   environmentTemplateId: '<environment_template_id>',
         *   version: version>
         * })
         * .then((space) => console.log(space))
         * .catch(console.error)
         * ```
         */
        getEnvironmentTemplate: function getEnvironmentTemplate({ organizationId, environmentTemplateId, version, query: query2 = {} }) {
            return makeRequest2({
                entityType: "EnvironmentTemplate",
                action: "get",
                params: {
                    organizationId,
                    environmentTemplateId,
                    version,
                    query: (0, import_contentful_sdk_core80.createRequestConfig)({ query: query2 }).params
                }
            }).then(data => wrapEnvironmentTemplate(makeRequest2, data, organizationId));
        },
        /**
         * Creates an environment template
         * @param organizationId - Organization ID
         * @param environmentTemplateData - Object representation of the environment template to be created
         * @returns Promise for the newly created EnvironmentTemplate
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.createEnvironmentTemplate('<organization_id>', {<environment_template_date>})
         * .then((environmentTemplate) => console.log(environmentTemplate))
         * .catch(console.error)
         * ```
         */
        createEnvironmentTemplate: function createEnvironmentTemplate(organizationId, environmentTemplateData) {
            return makeRequest2({
                entityType: "EnvironmentTemplate",
                action: "create",
                params: { organizationId },
                payload: environmentTemplateData
            }).then(data => wrapEnvironmentTemplate(makeRequest2, data, organizationId));
        },
        /**
         * Gets all spaces
         * @returns Promise for a collection of Spaces
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpaces()
         * .then((response) => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getSpaces: function getSpaces(query2 = {}, organizationId) {
            const { cursor, include, ...rest } = query2;
            const normalizedQuery = cursor ? normalizeCursorPaginationParameters(rest) : rest;
            return makeRequest2({
                entityType: "Space",
                action: "getMany",
                params: {
                    query: (0, import_contentful_sdk_core80.createRequestConfig)({ query: normalizedQuery }).params,
                    organizationId,
                    include
                }
            }).then(data =>
                // makeRequest returns the union type; cursor determines which branch is present at runtime so the casts are required
                cursor ? wrapSpaceCursorPaginatedCollection(makeRequest2, normalizeCursorPaginationResponse(data)) : wrapSpaceCollection(makeRequest2, data)
            );
        },
        /**
         * Gets a space
         * @param spaceId - Space ID
         * @returns Promise for a Space
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpace('<space_id>')
         * .then((space) => console.log(space))
         * .catch(console.error)
         * ```
         */
        getSpace: function getSpace(spaceId, { include } = {}) {
            return makeRequest2({
                entityType: "Space",
                action: "get",
                params: { spaceId, include }
            }).then(data => wrapSpace(makeRequest2, data));
        },
        /**
         * Creates a space
         * @param spaceData - Object representation of the Space to be created
         * @param organizationId - Organization ID, if the associated token can manage more than one organization.
         * @returns Promise for the newly created Space
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.createSpace({
         *   name: 'Name of new space'
         * })
         * .then((space) => console.log(space))
         * .catch(console.error)
         * ```
         */
        createSpace: function createSpace(spaceData, organizationId) {
            return makeRequest2({
                entityType: "Space",
                action: "create",
                params: { organizationId },
                payload: spaceData
            }).then(data => wrapSpace(makeRequest2, data));
        },
        /**
         * Gets an organization
         * @param  id - Organization ID
         * @returns Promise for a Organization
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganization('<org_id>')
         * .then((org) => console.log(org))
         * .catch(console.error)
         * ```
         */
        getOrganization: function getOrganization(id) {
            return makeRequest2({
                entityType: "Organization",
                action: "get",
                params: { organizationId: id }
            }).then(data => wrapOrganization(makeRequest2, data));
        },
        /**
         * Gets a collection of Organizations
         * @returns Promise for a collection of Organizations
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganizations()
         * .then(result => console.log(result.items))
         * .catch(console.error)
         * ```
         */
        getOrganizations: function getOrganizations(query2 = {}) {
            return makeRequest2({
                entityType: "Organization",
                action: "getMany",
                params: { query: (0, import_contentful_sdk_core80.createRequestConfig)({ query: query2 }).params }
            }).then(data => wrapOrganizationCollection(makeRequest2, data));
        },
        /**
         * Gets the authenticated user
         * @returns Promise for a User
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getCurrentUser()
         * .then(user => console.log(user.firstName))
         * .catch(console.error)
         * ```
         */
        getCurrentUser: function getCurrentUser(params) {
            return makeRequest2({
                entityType: "User",
                action: "getCurrent",
                params
            }).then(data => wrapUser(makeRequest2, data));
        },
        /**
         *
         * @param params
         * @returns Promise of a OAuthApplication
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *  accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOAuthApplication({
         * userId: '<user_id>'
         * oauthApplicationId: '<oauth_application_id>'
         * }).then(oauthApplication => console.log(oauthApplication))
         * .catch(console.error)
         */
        getOAuthApplication: function getOAuthApplication(params) {
            const { userId } = params;
            return makeRequest2({
                entityType: "OAuthApplication",
                action: "get",
                params
            }).then(data => wrapOAuthApplication(makeRequest2, data, userId));
        },
        /**
         *
         * @param params
         * @returns Promise of list of user's OAuthApplications
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *  accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOAuthApplications({
         * userId: '<user_id>'}).then(oauthApplications => console.log(oauthApplications))
         * .catch(console.error)
         */
        getOAuthApplications: function getOAuthApplications(params) {
            const { userId } = params;
            return makeRequest2({
                entityType: "OAuthApplication",
                action: "getManyForUser",
                params
            }).then(data => wrapOAuthApplicationCollection(makeRequest2, data, userId));
        },
        /**
         *
         * @param params
         * @returns Promise of a new OAuth application.
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *  accessToken: '<content_management_api_key>'
         * })
         *
         * client.createOAuthApplication({
         * userId: '<user_id>'},
         * { name: '<name>',
         *   description: '<description>',
         *   scopes: ['scope'],
         *   redirectUri: '<redirectUri>',
         *   confidential: '<true/false>'}).then(oauthApplications => console.log(oauthApplications))
         * .catch(console.error)
         */
        createOAuthApplication: function createOAuthApplication(params, rawData) {
            const { userId } = params;
            return makeRequest2({
                entityType: "OAuthApplication",
                action: "create",
                params,
                payload: rawData
            }).then(data => wrapOAuthApplication(makeRequest2, data, userId));
        },
        /**
         * Gets App Definition
         * @returns Promise for App Definition
         * @param organizationId - Id of the organization where the app is installed
         * @param appDefinitionId - Id of the app that will be returned
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getAppDefinition(<'org_id'>, <'app_id'>)
         * .then(appDefinition => console.log(appDefinition.name))
         * .catch(console.error)
         * ```
         */
        getAppDefinition: function getAppDefinition(params) {
            return makeRequest2({
                entityType: "AppDefinition",
                action: "get",
                params
            }).then(data => wrapAppDefinition(makeRequest2, data));
        },
        /**
         * Creates a personal access token
         * @param data - personal access token config
         * @returns Promise for a Token
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.createPersonalAccessToken(
         *  {
         *    "name": "My Token",
         *    "scope": [
         *      "content_management_manage"
         *    ]
         *  }
         * )
         * .then(personalAccessToken => console.log(personalAccessToken.token))
         * .catch(console.error)
         * ```
         */
        createPersonalAccessToken: function createPersonalAccessToken2(data) {
            return makeRequest2({
                /**
                 * When the `PersonalAccessToken` entity is removed, replace the `entityType` with `AccessToken`
                 * and update the action to `createPersonalToken` to ultilize the new entity called AccessToken.
                 */
                entityType: "PersonalAccessToken",
                action: "create",
                params: {},
                payload: data
            }).then(response => wrapPersonalAccessToken(makeRequest2, response));
        },
        /**
         * @deprecated - use getAccessToken instead
         *
         * Gets a personal access token
         * @param data - personal access token config
         * @returns Promise for a Token
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getPersonalAccessToken(tokenId)
         * .then(token => console.log(token.token))
         * .catch(console.error)
         * ```
         */
        getPersonalAccessToken: function getPersonalAccessToken(tokenId) {
            return makeRequest2({
                entityType: "PersonalAccessToken",
                action: "get",
                params: { tokenId }
            }).then(data => wrapPersonalAccessToken(makeRequest2, data));
        },
        /**
         * @deprecated - use getAccessTokens instead
         *
         * Gets all personal access tokens
         * @returns Promise for a Token
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getPersonalAccessTokens()
         * .then(response => console.log(response.items))
         * .catch(console.error)
         * ```
         */
        getPersonalAccessTokens: function getPersonalAccessTokens() {
            return makeRequest2({
                entityType: "PersonalAccessToken",
                action: "getMany",
                params: {}
            }).then(data => wrapPersonalAccessTokenCollection(makeRequest2, data));
        },
        /**
         * Gets a users access token
         * @param data - users access token config
         * @returns Promise for a Token
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getAccessToken(tokenId)
         * .then(token => console.log(token.token))
         * .catch(console.error)
         * ```
         */
        getAccessToken: function getAccessToken(tokenId) {
            return makeRequest2({
                entityType: "AccessToken",
                action: "get",
                params: { tokenId }
            }).then(data => wrapAccessToken(makeRequest2, data));
        },
        /**
         * Gets all user access tokens
         * @returns Promise for a Token
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getAccessTokens()
         * .then(response => console.log(reponse.items))
         * .catch(console.error)
         * ```
         */
        getAccessTokens: function getAccessTokens() {
            return makeRequest2({
                entityType: "AccessToken",
                action: "getMany",
                params: {}
            }).then(data => wrapAccessTokenCollection(makeRequest2, data));
        },
        /**
         * Retrieves a list of redacted versions of access tokens for an organization, accessible to owners or administrators of an organization.
         *
         * @returns Promise for a Token
         * @example ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganizationAccessTokens(organizationId)
         * .then(response => console.log(reponse.items))
         * .catch(console.error)
         * ```
         */
        getOrganizationAccessTokens: function getOrganizationAccessTokens(organizationId, query2 = {}) {
            return makeRequest2({
                entityType: "AccessToken",
                action: "getManyForOrganization",
                params: { organizationId, query: query2 }
            }).then(data => wrapAccessTokenCollection(makeRequest2, data));
        },
        /**
         * Get organization usage grouped by {@link UsageMetricEnum metric}
         *
         * @param organizationId - Id of an organization
         * @param query - Query parameters
         * @returns Promise of a collection of usages
         * @example ```javascript
         *
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getOrganizationUsage('<organizationId>', {
         *    'metric[in]': 'cma,gql',
         *    'dateRange.startAt': '2019-10-22',
         *    'dateRange.endAt': '2019-11-10'
         *    }
         * })
         * .then(result => console.log(result.items))
         * .catch(console.error)
         * ```
         */
        getOrganizationUsage: function getOrganizationUsage(organizationId, query2 = {}) {
            return makeRequest2({
                entityType: "Usage",
                action: "getManyForOrganization",
                params: { organizationId, query: query2 }
            }).then(data => wrapUsageCollection(makeRequest2, data));
        },
        /**
         * Get organization usage grouped by space and metric
         *
         * @param organizationId - Id of an organization
         * @param query - Query parameters
         * @returns Promise of a collection of usages
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.getSpaceUsage('<organizationId>', {
         *    skip: 0,
         *    limit: 10,
         *    'metric[in]': 'cda,cpa,gql',
         *    'dateRange.startAt': '2019-10-22',
         *    'dateRange.endAt': '2020-11-30'
         *    }
         * })
         * .then(result => console.log(result.items))
         * .catch(console.error)
         * ```
         */
        getSpaceUsage: function getSpaceUsage(organizationId, query2 = {}) {
            return makeRequest2({
                entityType: "Usage",
                action: "getManyForSpace",
                params: {
                    organizationId,
                    query: query2
                }
            }).then(data => wrapUsageCollection(makeRequest2, data));
        },
        /**
         * Make a custom request to the Contentful management API's /spaces endpoint
         * @param opts - axios request options (https://github.com/mzabriskie/axios)
         * @returns Promise for the response data
         * ```javascript
         * const contentful = require('contentful-management')
         *
         * const client = contentful.createClient({
         *   accessToken: '<content_management_api_key>'
         * })
         *
         * client.rawRequest({
         *   method: 'GET',
         *   url: '/custom/path'
         * })
         * .then((responseData) => console.log(responseData))
         * .catch(console.error)
         * ```
         */
        rawRequest: function rawRequest({ url: url2, ...config }) {
            return makeRequest2({
                entityType: "Http",
                action: "request",
                params: { url: url2, config }
            });
        }
    };
}

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/plain/wrappers/wrap.mjs
var wrap = ({ makeRequest: makeRequest2, defaults: defaults2 }, entityType, action) => {
    return (params, payload, headers) =>
        // @ts-expect-error see above
        makeRequest2({
            entityType,
            action,
            params: { ...defaults2, ...params },
            payload,
            // Required after adding optional headers to a delete method for the first time
            headers
        });
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/plain/plain-client.mjs
var createPlainClient = (makeRequest2, defaults2) => {
    const wrapParams = { makeRequest: makeRequest2, defaults: defaults2 };
    return {
        raw: {
            getDefaultParams: () => defaults2,
            get: (url2, config) =>
                makeRequest2({
                    entityType: "Http",
                    action: "get",
                    params: { url: url2, config }
                }),
            patch: (url2, payload, config) =>
                makeRequest2({
                    entityType: "Http",
                    action: "patch",
                    params: { url: url2, config },
                    payload
                }),
            post: (url2, payload, config) =>
                makeRequest2({
                    entityType: "Http",
                    action: "post",
                    params: { url: url2, config },
                    payload
                }),
            put: (url2, payload, config) =>
                makeRequest2({
                    entityType: "Http",
                    action: "put",
                    params: { url: url2, config },
                    payload
                }),
            delete: (url2, config) =>
                makeRequest2({
                    entityType: "Http",
                    action: "delete",
                    params: { url: url2, config }
                }),
            http: (url2, config) =>
                makeRequest2({
                    entityType: "Http",
                    action: "request",
                    params: { url: url2, config }
                })
        },
        aiAction: {
            get: wrap(wrapParams, "AiAction", "get"),
            getMany: wrap(wrapParams, "AiAction", "getMany"),
            create: wrap(wrapParams, "AiAction", "create"),
            update: wrap(wrapParams, "AiAction", "update"),
            delete: wrap(wrapParams, "AiAction", "delete"),
            publish: wrap(wrapParams, "AiAction", "publish"),
            unpublish: wrap(wrapParams, "AiAction", "unpublish"),
            invoke: wrap(wrapParams, "AiAction", "invoke")
        },
        aiActionInvocation: {
            get: wrap(wrapParams, "AiActionInvocation", "get")
        },
        agent: {
            get: wrap(wrapParams, "Agent", "get"),
            getMany: wrap(wrapParams, "Agent", "getMany"),
            generate: wrap(wrapParams, "Agent", "generate")
        },
        agentRun: {
            get: wrap(wrapParams, "AgentRun", "get"),
            getMany: wrap(wrapParams, "AgentRun", "getMany"),
            resumeRun: wrap(wrapParams, "AgentRun", "resumeRun")
        },
        automationDefinition: {
            get: wrap(wrapParams, "AutomationDefinition", "get"),
            getMany: wrap(wrapParams, "AutomationDefinition", "getMany"),
            create: wrap(wrapParams, "AutomationDefinition", "create"),
            update: wrap(wrapParams, "AutomationDefinition", "update"),
            delete: wrap(wrapParams, "AutomationDefinition", "delete")
        },
        automationExecution: {
            get: wrap(wrapParams, "AutomationExecution", "get"),
            getMany: wrap(wrapParams, "AutomationExecution", "getMany"),
            getForAutomationDefinition: wrap(wrapParams, "AutomationExecution", "getForAutomationDefinition")
        },
        appAction: {
            get: wrap(wrapParams, "AppAction", "get"),
            getMany: wrap(wrapParams, "AppAction", "getMany"),
            getManyForEnvironment: wrap(wrapParams, "AppAction", "getManyForEnvironment"),
            delete: wrap(wrapParams, "AppAction", "delete"),
            create: wrap(wrapParams, "AppAction", "create"),
            update: wrap(wrapParams, "AppAction", "update")
        },
        appActionCall: {
            create: wrap(wrapParams, "AppActionCall", "create"),
            getCallDetails: wrap(wrapParams, "AppActionCall", "getCallDetails"),
            createWithResponse: wrap(wrapParams, "AppActionCall", "createWithResponse"),
            get: wrap(wrapParams, "AppActionCall", "get"),
            createWithResult: wrap(wrapParams, "AppActionCall", "createWithResult"),
            getResponse: wrap(wrapParams, "AppActionCall", "getResponse")
        },
        appBundle: {
            get: wrap(wrapParams, "AppBundle", "get"),
            getMany: wrap(wrapParams, "AppBundle", "getMany"),
            delete: wrap(wrapParams, "AppBundle", "delete"),
            create: wrap(wrapParams, "AppBundle", "create")
        },
        appDetails: {
            upsert: wrap(wrapParams, "AppDetails", "upsert"),
            get: wrap(wrapParams, "AppDetails", "get"),
            delete: wrap(wrapParams, "AppDetails", "delete")
        },
        appEventSubscription: {
            upsert: wrap(wrapParams, "AppEventSubscription", "upsert"),
            get: wrap(wrapParams, "AppEventSubscription", "get"),
            delete: wrap(wrapParams, "AppEventSubscription", "delete")
        },
        appKey: {
            create: wrap(wrapParams, "AppKey", "create"),
            get: wrap(wrapParams, "AppKey", "get"),
            getMany: wrap(wrapParams, "AppKey", "getMany"),
            delete: wrap(wrapParams, "AppKey", "delete")
        },
        appSignedRequest: {
            create: wrap(wrapParams, "AppSignedRequest", "create")
        },
        appSigningSecret: {
            upsert: wrap(wrapParams, "AppSigningSecret", "upsert"),
            get: wrap(wrapParams, "AppSigningSecret", "get"),
            delete: wrap(wrapParams, "AppSigningSecret", "delete")
        },
        appAccessToken: {
            create: wrap(wrapParams, "AppAccessToken", "create")
        },
        concept: {
            create: wrap(wrapParams, "Concept", "create"),
            createWithId: wrap(wrapParams, "Concept", "createWithId"),
            get: wrap(wrapParams, "Concept", "get"),
            delete: wrap(wrapParams, "Concept", "delete"),
            patch: wrap(wrapParams, "Concept", "patch"),
            update: wrap(wrapParams, "Concept", "update"),
            getMany: wrap(wrapParams, "Concept", "getMany"),
            getDescendants: wrap(wrapParams, "Concept", "getDescendants"),
            getAncestors: wrap(wrapParams, "Concept", "getAncestors"),
            getTotal: wrap(wrapParams, "Concept", "getTotal")
        },
        conceptScheme: {
            get: wrap(wrapParams, "ConceptScheme", "get"),
            getMany: wrap(wrapParams, "ConceptScheme", "getMany"),
            getTotal: wrap(wrapParams, "ConceptScheme", "getTotal"),
            delete: wrap(wrapParams, "ConceptScheme", "delete"),
            create: wrap(wrapParams, "ConceptScheme", "create"),
            createWithId: wrap(wrapParams, "ConceptScheme", "createWithId"),
            patch: wrap(wrapParams, "ConceptScheme", "patch"),
            update: wrap(wrapParams, "ConceptScheme", "update")
        },
        function: {
            get: wrap(wrapParams, "Function", "get"),
            getMany: wrap(wrapParams, "Function", "getMany"),
            getManyForEnvironment: wrap(wrapParams, "Function", "getManyForEnvironment")
        },
        functionLog: {
            get: wrap(wrapParams, "FunctionLog", "get"),
            getMany: wrap(wrapParams, "FunctionLog", "getMany")
        },
        editorInterface: {
            get: wrap(wrapParams, "EditorInterface", "get"),
            getMany: wrap(wrapParams, "EditorInterface", "getMany"),
            update: wrap(wrapParams, "EditorInterface", "update")
        },
        space: {
            get: wrap(wrapParams, "Space", "get"),
            getMany: wrap(wrapParams, "Space", "getMany"),
            getManyForOrganization: wrap(wrapParams, "Space", "getManyForOrganization"),
            update: wrap(wrapParams, "Space", "update"),
            delete: wrap(wrapParams, "Space", "delete"),
            create: wrap(wrapParams, "Space", "create")
        },
        environment: {
            get: wrap(wrapParams, "Environment", "get"),
            getMany: wrap(wrapParams, "Environment", "getMany"),
            create: wrap(wrapParams, "Environment", "create"),
            createWithId: wrap(wrapParams, "Environment", "createWithId"),
            update: wrap(wrapParams, "Environment", "update"),
            delete: wrap(wrapParams, "Environment", "delete")
        },
        environmentAlias: {
            get: wrap(wrapParams, "EnvironmentAlias", "get"),
            getMany: wrap(wrapParams, "EnvironmentAlias", "getMany"),
            createWithId: wrap(wrapParams, "EnvironmentAlias", "createWithId"),
            update: wrap(wrapParams, "EnvironmentAlias", "update"),
            delete: wrap(wrapParams, "EnvironmentAlias", "delete")
        },
        environmentTemplate: {
            get: wrap(wrapParams, "EnvironmentTemplate", "get"),
            getMany: wrap(wrapParams, "EnvironmentTemplate", "getMany"),
            create: wrap(wrapParams, "EnvironmentTemplate", "create"),
            versionUpdate: wrap(wrapParams, "EnvironmentTemplate", "versionUpdate"),
            update: wrap(wrapParams, "EnvironmentTemplate", "update"),
            install: wrap(wrapParams, "EnvironmentTemplate", "install"),
            versions: wrap(wrapParams, "EnvironmentTemplate", "versions"),
            validate: wrap(wrapParams, "EnvironmentTemplate", "validate"),
            disconnect: wrap(wrapParams, "EnvironmentTemplate", "disconnect"),
            delete: wrap(wrapParams, "EnvironmentTemplate", "delete")
        },
        environmentTemplateInstallation: {
            getMany: wrap(wrapParams, "EnvironmentTemplateInstallation", "getMany"),
            getForEnvironment: wrap(wrapParams, "EnvironmentTemplateInstallation", "getForEnvironment")
        },
        bulkAction: {
            get: wrap(wrapParams, "BulkAction", "get"),
            publish: wrap(wrapParams, "BulkAction", "publish"),
            unpublish: wrap(wrapParams, "BulkAction", "unpublish"),
            validate: wrap(wrapParams, "BulkAction", "validate"),
            getV2: wrap(wrapParams, "BulkAction", "getV2"),
            publishV2: wrap(wrapParams, "BulkAction", "publishV2"),
            unpublishV2: wrap(wrapParams, "BulkAction", "unpublishV2"),
            validateV2: wrap(wrapParams, "BulkAction", "validateV2")
        },
        comment: {
            get: wrap(wrapParams, "Comment", "get"),
            getMany: wrap(wrapParams, "Comment", "getMany"),
            create: wrap(wrapParams, "Comment", "create"),
            update: wrap(wrapParams, "Comment", "update"),
            delete: wrap(wrapParams, "Comment", "delete")
        },
        componentType: {
            getMany: wrap(wrapParams, "ComponentType", "getMany")
        },
        contentType: {
            get: wrap(wrapParams, "ContentType", "get"),
            getMany: wrap(wrapParams, "ContentType", "getMany"),
            getManyWithCursor: wrap(wrapParams, "ContentType", "getManyWithCursor"),
            update: wrap(wrapParams, "ContentType", "update"),
            delete: wrap(wrapParams, "ContentType", "delete"),
            publish: wrap(wrapParams, "ContentType", "publish"),
            unpublish: wrap(wrapParams, "ContentType", "unpublish"),
            create: wrap(wrapParams, "ContentType", "create"),
            createWithId: wrap(wrapParams, "ContentType", "createWithId"),
            omitAndDeleteField: (params, contentType, fieldId) => omitAndDeleteField(makeRequest2, { ...{ ...defaults2, ...params }, fieldId }, contentType)
        },
        user: {
            getManyForSpace: wrap(wrapParams, "User", "getManyForSpace"),
            getForSpace: wrap(wrapParams, "User", "getForSpace"),
            getCurrent: wrap(wrapParams, "User", "getCurrent"),
            getForOrganization: wrap(wrapParams, "User", "getForOrganization"),
            getManyForOrganization: wrap(wrapParams, "User", "getManyForOrganization")
        },
        task: {
            get: wrap(wrapParams, "Task", "get"),
            getMany: wrap(wrapParams, "Task", "getMany"),
            create: wrap(wrapParams, "Task", "create"),
            update: wrap(wrapParams, "Task", "update"),
            delete: wrap(wrapParams, "Task", "delete")
        },
        entry: {
            getPublished: wrap(wrapParams, "Entry", "getPublished"),
            getPublishedWithCursor: wrap(wrapParams, "Entry", "getPublishedWithCursor"),
            getMany: wrap(wrapParams, "Entry", "getMany"),
            getManyWithCursor: wrap(wrapParams, "Entry", "getManyWithCursor"),
            get: wrap(wrapParams, "Entry", "get"),
            update: wrap(wrapParams, "Entry", "update"),
            patch: wrap(wrapParams, "Entry", "patch"),
            delete: wrap(wrapParams, "Entry", "delete"),
            publish: wrap(wrapParams, "Entry", "publish"),
            unpublish: wrap(wrapParams, "Entry", "unpublish"),
            archive: wrap(wrapParams, "Entry", "archive"),
            unarchive: wrap(wrapParams, "Entry", "unarchive"),
            create: wrap(wrapParams, "Entry", "create"),
            createWithId: wrap(wrapParams, "Entry", "createWithId"),
            references: wrap(wrapParams, "Entry", "references")
        },
        asset: {
            getPublished: wrap(wrapParams, "Asset", "getPublished"),
            getPublishedWithCursor: wrap(wrapParams, "Asset", "getPublishedWithCursor"),
            getMany: wrap(wrapParams, "Asset", "getMany"),
            getManyWithCursor: wrap(wrapParams, "Asset", "getManyWithCursor"),
            get: wrap(wrapParams, "Asset", "get"),
            update: wrap(wrapParams, "Asset", "update"),
            delete: wrap(wrapParams, "Asset", "delete"),
            publish: wrap(wrapParams, "Asset", "publish"),
            unpublish: wrap(wrapParams, "Asset", "unpublish"),
            archive: wrap(wrapParams, "Asset", "archive"),
            unarchive: wrap(wrapParams, "Asset", "unarchive"),
            create: wrap(wrapParams, "Asset", "create"),
            createWithId: wrap(wrapParams, "Asset", "createWithId"),
            createFromFiles: wrap(wrapParams, "Asset", "createFromFiles"),
            processForAllLocales: (params, asset, options) =>
                makeRequest2({
                    entityType: "Asset",
                    action: "processForAllLocales",
                    params: {
                        ...{ ...defaults2, ...params },
                        options,
                        asset
                    }
                }),
            processForLocale: (params, asset, locale, options) =>
                makeRequest2({
                    entityType: "Asset",
                    action: "processForLocale",
                    params: {
                        ...{ ...defaults2, ...params },
                        locale,
                        asset,
                        options
                    }
                })
        },
        appUpload: {
            get: wrap(wrapParams, "AppUpload", "get"),
            delete: wrap(wrapParams, "AppUpload", "delete"),
            create: wrap(wrapParams, "AppUpload", "create")
        },
        assetKey: {
            create: wrap(wrapParams, "AssetKey", "create")
        },
        upload: {
            get: wrap(wrapParams, "Upload", "get"),
            create: wrap(wrapParams, "Upload", "create"),
            delete: wrap(wrapParams, "Upload", "delete")
        },
        uploadCredential: {
            create: wrap(wrapParams, "UploadCredential", "create")
        },
        locale: {
            get: wrap(wrapParams, "Locale", "get"),
            getMany: wrap(wrapParams, "Locale", "getMany"),
            delete: wrap(wrapParams, "Locale", "delete"),
            update: wrap(wrapParams, "Locale", "update"),
            create: wrap(wrapParams, "Locale", "create")
        },
        personalAccessToken: {
            get: wrap(wrapParams, "PersonalAccessToken", "get"),
            getMany: wrap(wrapParams, "PersonalAccessToken", "getMany"),
            create: (data, headers) =>
                makeRequest2({
                    entityType: "PersonalAccessToken",
                    action: "create",
                    params: {},
                    headers,
                    payload: data
                }),
            revoke: wrap(wrapParams, "PersonalAccessToken", "revoke")
        },
        accessToken: {
            get: wrap(wrapParams, "AccessToken", "get"),
            getMany: wrap(wrapParams, "AccessToken", "getMany"),
            createPersonalAccessToken: (data, headers) =>
                makeRequest2({
                    entityType: "AccessToken",
                    action: "createPersonalAccessToken",
                    params: {},
                    headers,
                    payload: data
                }),
            revoke: wrap(wrapParams, "AccessToken", "revoke"),
            getManyForOrganization: wrap(wrapParams, "AccessToken", "getManyForOrganization")
        },
        usage: {
            getManyForSpace: wrap(wrapParams, "Usage", "getManyForSpace"),
            getManyForOrganization: wrap(wrapParams, "Usage", "getManyForOrganization")
        },
        release: {
            asset: {
                get: wrap(wrapParams, "ReleaseAsset", "get"),
                getMany: wrap(wrapParams, "ReleaseAsset", "getMany"),
                update: wrap(wrapParams, "ReleaseAsset", "update"),
                create: wrap(wrapParams, "ReleaseAsset", "create"),
                createWithId: wrap(wrapParams, "ReleaseAsset", "createWithId"),
                createFromFiles: wrap(wrapParams, "ReleaseAsset", "createFromFiles"),
                processForAllLocales: (params, asset, options) =>
                    makeRequest2({
                        entityType: "ReleaseAsset",
                        action: "processForAllLocales",
                        params: {
                            ...{ ...defaults2, ...params },
                            options,
                            asset
                        }
                    }),
                processForLocale: (params, asset, locale, options) =>
                    makeRequest2({
                        entityType: "ReleaseAsset",
                        action: "processForLocale",
                        params: {
                            ...{ ...defaults2, ...params },
                            locale,
                            asset,
                            options
                        }
                    })
            },
            entry: {
                get: wrap(wrapParams, "ReleaseEntry", "get"),
                getMany: wrap(wrapParams, "ReleaseEntry", "getMany"),
                update: wrap(wrapParams, "ReleaseEntry", "update"),
                patch: wrap(wrapParams, "ReleaseEntry", "patch"),
                create: wrap(wrapParams, "ReleaseEntry", "create"),
                createWithId: wrap(wrapParams, "ReleaseEntry", "createWithId")
            },
            archive: wrap(wrapParams, "Release", "archive"),
            get: wrap(wrapParams, "Release", "get"),
            query: wrap(wrapParams, "Release", "query"),
            create: wrap(wrapParams, "Release", "create"),
            update: wrap(wrapParams, "Release", "update"),
            delete: wrap(wrapParams, "Release", "delete"),
            publish: wrap(wrapParams, "Release", "publish"),
            unarchive: wrap(wrapParams, "Release", "unarchive"),
            unpublish: wrap(wrapParams, "Release", "unpublish"),
            validate: wrap(wrapParams, "Release", "validate")
        },
        releaseAction: {
            get: wrap(wrapParams, "ReleaseAction", "get"),
            getMany: wrap(wrapParams, "ReleaseAction", "getMany"),
            queryForRelease: wrap(wrapParams, "ReleaseAction", "queryForRelease")
        },
        role: {
            get: wrap(wrapParams, "Role", "get"),
            getMany: wrap(wrapParams, "Role", "getMany"),
            getManyForOrganization: wrap(wrapParams, "Role", "getManyForOrganization"),
            create: wrap(wrapParams, "Role", "create"),
            createWithId: wrap(wrapParams, "Role", "createWithId"),
            update: wrap(wrapParams, "Role", "update"),
            delete: wrap(wrapParams, "Role", "delete")
        },
        scheduledActions: {
            get: wrap(wrapParams, "ScheduledAction", "get"),
            getMany: wrap(wrapParams, "ScheduledAction", "getMany"),
            create: wrap(wrapParams, "ScheduledAction", "create"),
            delete: wrap(wrapParams, "ScheduledAction", "delete"),
            update: wrap(wrapParams, "ScheduledAction", "update")
        },
        previewApiKey: {
            get: wrap(wrapParams, "PreviewApiKey", "get"),
            getMany: wrap(wrapParams, "PreviewApiKey", "getMany")
        },
        apiKey: {
            get: wrap(wrapParams, "ApiKey", "get"),
            getMany: wrap(wrapParams, "ApiKey", "getMany"),
            create: wrap(wrapParams, "ApiKey", "create"),
            createWithId: wrap(wrapParams, "ApiKey", "createWithId"),
            update: wrap(wrapParams, "ApiKey", "update"),
            delete: wrap(wrapParams, "ApiKey", "delete")
        },
        appDefinition: {
            get: wrap(wrapParams, "AppDefinition", "get"),
            getMany: wrap(wrapParams, "AppDefinition", "getMany"),
            create: wrap(wrapParams, "AppDefinition", "create"),
            update: wrap(wrapParams, "AppDefinition", "update"),
            delete: wrap(wrapParams, "AppDefinition", "delete"),
            getInstallationsForOrg: wrap(wrapParams, "AppDefinition", "getInstallationsForOrg")
        },
        appInstallation: {
            get: wrap(wrapParams, "AppInstallation", "get"),
            getMany: wrap(wrapParams, "AppInstallation", "getMany"),
            getForOrganization: wrap(wrapParams, "AppInstallation", "getForOrganization"),
            upsert: wrap(wrapParams, "AppInstallation", "upsert"),
            delete: wrap(wrapParams, "AppInstallation", "delete")
        },
        resource: {
            getMany: wrap(wrapParams, "Resource", "getMany")
        },
        resourceProvider: {
            get: wrap(wrapParams, "ResourceProvider", "get"),
            upsert: wrap(wrapParams, "ResourceProvider", "upsert"),
            delete: wrap(wrapParams, "ResourceProvider", "delete")
        },
        resourceType: {
            get: wrap(wrapParams, "ResourceType", "get"),
            getMany: wrap(wrapParams, "ResourceType", "getMany"),
            upsert: wrap(wrapParams, "ResourceType", "upsert"),
            delete: wrap(wrapParams, "ResourceType", "delete"),
            getForEnvironment: wrap(wrapParams, "ResourceType", "getForEnvironment")
        },
        extension: {
            get: wrap(wrapParams, "Extension", "get"),
            getMany: wrap(wrapParams, "Extension", "getMany"),
            create: wrap(wrapParams, "Extension", "create"),
            createWithId: wrap(wrapParams, "Extension", "createWithId"),
            update: wrap(wrapParams, "Extension", "update"),
            delete: wrap(wrapParams, "Extension", "delete")
        },
        webhook: {
            get: wrap(wrapParams, "Webhook", "get"),
            getMany: wrap(wrapParams, "Webhook", "getMany"),
            getHealthStatus: wrap(wrapParams, "Webhook", "getHealthStatus"),
            getCallDetails: wrap(wrapParams, "Webhook", "getCallDetails"),
            getSigningSecret: wrap(wrapParams, "Webhook", "getSigningSecret"),
            getRetryPolicy: wrap(wrapParams, "Webhook", "getRetryPolicy"),
            getManyCallDetails: wrap(wrapParams, "Webhook", "getManyCallDetails"),
            create: wrap(wrapParams, "Webhook", "create"),
            update: wrap(wrapParams, "Webhook", "update"),
            upsertSigningSecret: wrap(wrapParams, "Webhook", "upsertSigningSecret"),
            upsertRetryPolicy: wrap(wrapParams, "Webhook", "upsertRetryPolicy"),
            delete: wrap(wrapParams, "Webhook", "delete"),
            deleteSigningSecret: wrap(wrapParams, "Webhook", "deleteSigningSecret"),
            deleteRetryPolicy: wrap(wrapParams, "Webhook", "deleteRetryPolicy")
        },
        snapshot: {
            getManyForEntry: wrap(wrapParams, "Snapshot", "getManyForEntry"),
            getForEntry: wrap(wrapParams, "Snapshot", "getForEntry"),
            getManyForContentType: wrap(wrapParams, "Snapshot", "getManyForContentType"),
            getForContentType: wrap(wrapParams, "Snapshot", "getForContentType")
        },
        tag: {
            get: wrap(wrapParams, "Tag", "get"),
            getMany: wrap(wrapParams, "Tag", "getMany"),
            createWithId: wrap(wrapParams, "Tag", "createWithId"),
            update: wrap(wrapParams, "Tag", "update"),
            delete: wrap(wrapParams, "Tag", "delete")
        },
        organization: {
            getAll: wrap(wrapParams, "Organization", "getMany"),
            get: wrap(wrapParams, "Organization", "get")
        },
        organizationInvitation: {
            get: wrap(wrapParams, "OrganizationInvitation", "get"),
            create: wrap(wrapParams, "OrganizationInvitation", "create")
        },
        organizationMembership: {
            get: wrap(wrapParams, "OrganizationMembership", "get"),
            getMany: wrap(wrapParams, "OrganizationMembership", "getMany"),
            update: wrap(wrapParams, "OrganizationMembership", "update"),
            delete: wrap(wrapParams, "OrganizationMembership", "delete")
        },
        oauthApplication: {
            get: wrap(wrapParams, "OAuthApplication", "get"),
            getManyForUser: wrap(wrapParams, "OAuthApplication", "getManyForUser"),
            update: wrap(wrapParams, "OAuthApplication", "update"),
            delete: wrap(wrapParams, "OAuthApplication", "delete"),
            create: wrap(wrapParams, "OAuthApplication", "create")
        },
        semanticDuplicates: {
            get: wrap(wrapParams, "SemanticDuplicates", "get")
        },
        semanticRecommendations: {
            get: wrap(wrapParams, "SemanticRecommendations", "get")
        },
        semanticReferenceSuggestions: {
            get: wrap(wrapParams, "SemanticReferenceSuggestions", "get")
        },
        semanticSearch: {
            get: wrap(wrapParams, "SemanticSearch", "get")
        },
        semanticSettings: {
            get: wrap(wrapParams, "SemanticSettings", "get")
        },
        contentSemanticsIndex: {
            get: wrap(wrapParams, "ContentSemanticsIndex", "get"),
            getMany: wrap(wrapParams, "ContentSemanticsIndex", "getMany"),
            getManyForEnvironment: wrap(wrapParams, "ContentSemanticsIndex", "getManyForEnvironment"),
            create: wrap(wrapParams, "ContentSemanticsIndex", "create"),
            delete: wrap(wrapParams, "ContentSemanticsIndex", "delete")
        },
        spaceMember: {
            get: wrap(wrapParams, "SpaceMember", "get"),
            getMany: wrap(wrapParams, "SpaceMember", "getMany")
        },
        spaceMembership: {
            get: wrap(wrapParams, "SpaceMembership", "get"),
            getMany: wrap(wrapParams, "SpaceMembership", "getMany"),
            getForOrganization: wrap(wrapParams, "SpaceMembership", "getForOrganization"),
            getManyForOrganization: wrap(wrapParams, "SpaceMembership", "getManyForOrganization"),
            create: wrap(wrapParams, "SpaceMembership", "create"),
            createWithId: wrap(wrapParams, "SpaceMembership", "createWithId"),
            update: wrap(wrapParams, "SpaceMembership", "update"),
            delete: wrap(wrapParams, "SpaceMembership", "delete")
        },
        team: {
            get: wrap(wrapParams, "Team", "get"),
            getMany: wrap(wrapParams, "Team", "getMany"),
            getManyForSpace: wrap(wrapParams, "Team", "getManyForSpace"),
            create: wrap(wrapParams, "Team", "create"),
            update: wrap(wrapParams, "Team", "update"),
            delete: wrap(wrapParams, "Team", "delete")
        },
        teamMembership: {
            get: wrap(wrapParams, "TeamMembership", "get"),
            getManyForOrganization: wrap(wrapParams, "TeamMembership", "getManyForOrganization"),
            getManyForTeam: wrap(wrapParams, "TeamMembership", "getManyForTeam"),
            create: wrap(wrapParams, "TeamMembership", "create"),
            update: wrap(wrapParams, "TeamMembership", "update"),
            delete: wrap(wrapParams, "TeamMembership", "delete")
        },
        teamSpaceMembership: {
            get: wrap(wrapParams, "TeamSpaceMembership", "get"),
            getMany: wrap(wrapParams, "TeamSpaceMembership", "getMany"),
            getForOrganization: wrap(wrapParams, "TeamSpaceMembership", "getForOrganization"),
            getManyForOrganization: wrap(wrapParams, "TeamSpaceMembership", "getManyForOrganization"),
            create: wrap(wrapParams, "TeamSpaceMembership", "create"),
            update: wrap(wrapParams, "TeamSpaceMembership", "update"),
            delete: wrap(wrapParams, "TeamSpaceMembership", "delete")
        },
        uiConfig: {
            get: wrap(wrapParams, "UIConfig", "get"),
            update: wrap(wrapParams, "UIConfig", "update")
        },
        userUIConfig: {
            get: wrap(wrapParams, "UserUIConfig", "get"),
            update: wrap(wrapParams, "UserUIConfig", "update")
        },
        workflowDefinition: {
            get: wrap(wrapParams, "WorkflowDefinition", "get"),
            getMany: wrap(wrapParams, "WorkflowDefinition", "getMany"),
            create: wrap(wrapParams, "WorkflowDefinition", "create"),
            update: wrap(wrapParams, "WorkflowDefinition", "update"),
            delete: wrap(wrapParams, "WorkflowDefinition", "delete")
        },
        workflow: {
            get: wrap(wrapParams, "Workflow", "get"),
            getMany: wrap(wrapParams, "Workflow", "getMany"),
            create: wrap(wrapParams, "Workflow", "create"),
            update: wrap(wrapParams, "Workflow", "update"),
            delete: wrap(wrapParams, "Workflow", "delete"),
            complete: wrap(wrapParams, "Workflow", "complete")
        },
        workflowsChangelog: {
            getMany: wrap(wrapParams, "WorkflowsChangelog", "getMany")
        }
    };
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/constants/editor-interface-defaults/types.mjs
var WidgetNamespace;
(function (WidgetNamespace2) {
    WidgetNamespace2["BUILTIN"] = "builtin";
    WidgetNamespace2["EXTENSION"] = "extension";
    WidgetNamespace2["SIDEBAR_BUILTIN"] = "sidebar-builtin";
    WidgetNamespace2["APP"] = "app";
    WidgetNamespace2["EDITOR_BUILTIN"] = "editor-builtin";
})(WidgetNamespace || (WidgetNamespace = {}));
var DEFAULT_EDITOR_ID = "default-editor";

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/constants/editor-interface-defaults/sidebar-defaults.mjs
var SidebarWidgetTypes = {
    USERS: "users-widget",
    CONTENT_PREVIEW: "content-preview-widget",
    TRANSLATION: "translation-widget",
    INCOMING_LINKS: "incoming-links-widget",
    PUBLICATION: "publication-widget",
    RELEASES: "releases-widget",
    VERSIONS: "versions-widget"
};
var Publication = {
    widgetId: SidebarWidgetTypes.PUBLICATION,
    widgetNamespace: WidgetNamespace.SIDEBAR_BUILTIN,
    name: "Publish & Status",
    description: "Built-in - View entry status, publish, etc."
};
var Releases = {
    widgetId: SidebarWidgetTypes.RELEASES,
    widgetNamespace: WidgetNamespace.SIDEBAR_BUILTIN,
    name: "Release",
    description: "Built-in - View release, add to it, etc."
};
var ContentPreview = {
    widgetId: SidebarWidgetTypes.CONTENT_PREVIEW,
    widgetNamespace: WidgetNamespace.SIDEBAR_BUILTIN,
    name: "Preview",
    description: "Built-in - Displays preview functionality."
};
var Links = {
    widgetId: SidebarWidgetTypes.INCOMING_LINKS,
    widgetNamespace: WidgetNamespace.SIDEBAR_BUILTIN,
    name: "Links",
    description: "Built-in - Shows where an entry is linked."
};
var Translation = {
    widgetId: SidebarWidgetTypes.TRANSLATION,
    widgetNamespace: WidgetNamespace.SIDEBAR_BUILTIN,
    name: "Translation",
    description: "Built-in - Manage which translations are visible."
};
var Versions = {
    widgetId: SidebarWidgetTypes.VERSIONS,
    widgetNamespace: WidgetNamespace.SIDEBAR_BUILTIN,
    name: "Versions",
    description: "Built-in - View previously published versions. Available only for master environment."
};
var Users = {
    widgetId: SidebarWidgetTypes.USERS,
    widgetNamespace: WidgetNamespace.SIDEBAR_BUILTIN,
    name: "Users",
    description: "Built-in - Displays users on the same entry."
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/constants/editor-interface-defaults/editors-defaults.mjs
var EntryEditorWidgetTypes = {
    DEFAULT_EDITOR: {
        name: "Editor",
        id: DEFAULT_EDITOR_ID
    },
    REFERENCE_TREE: {
        name: "References",
        id: "reference-tree"
    },
    TAGS_EDITOR: {
        name: "Tags",
        id: "tags-editor"
    }
};
var DefaultEntryEditor = {
    widgetId: EntryEditorWidgetTypes.DEFAULT_EDITOR.id,
    widgetNamespace: WidgetNamespace.EDITOR_BUILTIN,
    name: EntryEditorWidgetTypes.DEFAULT_EDITOR.name
};
var ReferencesEntryEditor = {
    widgetId: EntryEditorWidgetTypes.REFERENCE_TREE.id,
    widgetNamespace: WidgetNamespace.EDITOR_BUILTIN,
    name: EntryEditorWidgetTypes.REFERENCE_TREE.name
};
var TagsEditor = {
    widgetId: EntryEditorWidgetTypes.TAGS_EDITOR.id,
    widgetNamespace: WidgetNamespace.EDITOR_BUILTIN,
    name: EntryEditorWidgetTypes.TAGS_EDITOR.name
};

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/constants/editor-interface-defaults/controls-defaults.mjs
var INTERNAL_TO_API = {
    Symbol: { type: "Symbol" },
    Text: { type: "Text" },
    RichText: { type: "RichText" },
    Integer: { type: "Integer" },
    Number: { type: "Number" },
    Boolean: { type: "Boolean" },
    Date: { type: "Date" },
    Location: { type: "Location" },
    Object: { type: "Object" },
    File: { type: "File" },
    Entry: { type: "Link", linkType: "Entry" },
    Asset: { type: "Link", linkType: "Asset" },
    Resource: { type: "ResourceLink" },
    Symbols: { type: "Array", items: { type: "Symbol" } },
    Entries: { type: "Array", items: { type: "Link", linkType: "Entry" } },
    Assets: { type: "Array", items: { type: "Link", linkType: "Asset" } },
    Resources: { type: "Array", items: { type: "ResourceLink" } }
};
var FIELD_TYPES = Object.keys(INTERNAL_TO_API);

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/entities/workflow-definition.mjs
var import_contentful_sdk_core81 = __toESM(require_cjs2(), 1);
var WorkflowStepPermissionType;
(function (WorkflowStepPermissionType2) {
    WorkflowStepPermissionType2["EntityPermission"] = "entity_permission";
    WorkflowStepPermissionType2["WorkflowPermission"] = "workflow_permission";
})(WorkflowStepPermissionType || (WorkflowStepPermissionType = {}));
var WorkflowStepPermissionAction;
(function (WorkflowStepPermissionAction2) {
    WorkflowStepPermissionAction2["Edit"] = "edit";
    WorkflowStepPermissionAction2["Publish"] = "publish";
    WorkflowStepPermissionAction2["Delete"] = "delete";
})(WorkflowStepPermissionAction || (WorkflowStepPermissionAction = {}));
var WorkflowStepPermissionEffect;
(function (WorkflowStepPermissionEffect2) {
    WorkflowStepPermissionEffect2["Allow"] = "allow";
    WorkflowStepPermissionEffect2["Deny"] = "deny";
})(WorkflowStepPermissionEffect || (WorkflowStepPermissionEffect = {}));
var WorkflowStepActionType;
(function (WorkflowStepActionType2) {
    WorkflowStepActionType2["App"] = "app";
    WorkflowStepActionType2["Email"] = "email";
    WorkflowStepActionType2["Task"] = "task";
})(WorkflowStepActionType || (WorkflowStepActionType = {}));

// node_modules/.pnpm/contentful-management@12.8.0/node_modules/contentful-management/dist/esm/index.mjs
function createClient(clientOptions, opts = {}) {
    const sdkMain = opts.type === "legacy" ? "contentful-management.js" : "contentful-management-plain.js";
    const userAgent = (0, import_contentful_sdk_core82.getUserAgentHeader)(
        // @ts-expect-error "0.0.0-determined-by-semantic-release" is injected by rollup at build time
        `${sdkMain}/${"0.0.0-determined-by-semantic-release"}`,
        clientOptions.application,
        clientOptions.integration,
        clientOptions.feature
    );
    const adapter2 = createAdapter({ ...clientOptions, userAgent });
    const makeRequest2 = options => adapter2.makeRequest({ ...options, userAgent });
    if (opts.type === "legacy") {
        console.warn("[contentful-management] The nested (legacy) client is deprecated and will be removed in the next major version. Please migrate to the plain client. See the README for migration guidance.");
        return createClientApi(makeRequest2);
    } else {
        return createPlainClient(makeRequest2, opts.defaults);
    }
}

// src/publish-core.ts
var CURSOR_PAGE_SIZE = 1e3;
var BULK_ACTION_MAX_ITEMS = 200;
var BULK_ACTION_POLL_MS = 2e3;
var BULK_ACTION_POLL_STAGGER_MS = 400;
var BULK_ACTION_MAX_POLLS = 60;
var MAX_BULK_ACTION_CONCURRENCY = 5;
var DEFAULT_BULK_ACTION_CONCURRENCY = 1;
function createDefaultPlainClient(config) {
    return createScopedClient(config.accessToken, config.spaceId, config.environmentId);
}
function createScopedClient(accessToken, spaceId, environmentId) {
    return createClient(
        { accessToken },
        {
            defaults: {
                spaceId,
                environmentId
            }
        }
    );
}
function normalizeTag(item) {
    if (typeof item !== "object" || item === null) {
        return null;
    }
    const candidate = item;
    const id = candidate.sys?.id?.trim();
    if (!id) {
        return null;
    }
    return {
        id,
        name: candidate.name?.trim() || id
    };
}
function normalizeSpace(item) {
    if (typeof item !== "object" || item === null) {
        return null;
    }
    const candidate = item;
    const id = candidate.sys?.id?.trim();
    if (!id) {
        return null;
    }
    return {
        id,
        name: candidate.name?.trim() || id
    };
}
function normalizeEnvironment(item) {
    if (typeof item !== "object" || item === null) {
        return null;
    }
    const candidate = item;
    const id = candidate.sys?.id?.trim();
    if (!id) {
        return null;
    }
    return {
        id,
        name: candidate.name?.trim() || id
    };
}
async function loadAccessibleSpaces(params) {
    const token = params.token.trim();
    if (!token) {
        throw new Error("token is required");
    }
    const client = createClient({ accessToken: token }, { type: "plain" });
    const response = await client.space.getMany({
        query: {
            limit: 1e3
        }
    });
    return (response.items ?? [])
        .map(item => normalizeSpace(item))
        .filter(item => item !== null)
        .sort((a, b) => a.name.localeCompare(b.name));
}
async function loadSpaceEnvironments(params) {
    const token = params.token.trim();
    const spaceId = params.spaceId.trim();
    if (!token) {
        throw new Error("token is required");
    }
    if (!spaceId) {
        throw new Error("spaceId is required");
    }
    const client = createClient({ accessToken: token }, { type: "plain" });
    const response = await client.environment.getMany({
        spaceId,
        query: {
            limit: 1e3
        }
    });
    return (response.items ?? [])
        .map(item => normalizeEnvironment(item))
        .filter(item => item !== null)
        .sort((a, b) => a.name.localeCompare(b.name));
}
async function loadTagsForScope(params) {
    const token = params.token.trim();
    const spaceId = params.spaceId.trim();
    const environmentId = params.environmentId.trim();
    if (!token) {
        throw new Error("token is required");
    }
    if (!spaceId) {
        throw new Error("spaceId is required");
    }
    if (!environmentId) {
        throw new Error("environmentId is required");
    }
    const client = createScopedClient(token, spaceId, environmentId);
    const response = await client.tag.getMany({
        spaceId,
        environmentId,
        query: {
            limit: 1e3
        }
    });
    return (response.items ?? [])
        .map(item => normalizeTag(item))
        .filter(item => item !== null)
        .sort((a, b) => a.name.localeCompare(b.name));
}
function emitLog(hooks, level, message, meta) {
    hooks?.onLog?.({ level, message, ...(meta ? { meta } : {}) });
}
function emitProgress(hooks, progress) {
    hooks?.onProgress?.(progress);
}
function sleep2(ms) {
    return new Promise(resolveSleep => {
        setTimeout(resolveSleep, ms);
    });
}
function getContentTypeId(entry) {
    return entry.sys.contentType.sys.id;
}
function needsPublish(entry) {
    return isDraft(entry) || isUpdated(entry);
}
function needsAction(entry, action) {
    if (action === "publish") {
        return needsPublish(entry);
    }
    return isPublished(entry);
}
function getActionLabels(action) {
    if (action === "unpublish") {
        return {
            infinitive: "unpublish",
            gerund: "unpublishing",
            past: "unpublished",
            noun: "unpublishable",
            listHeading: "Unpublishable entry IDs",
            noEntriesMessage: "No entries to unpublish."
        };
    }
    return {
        infinitive: "publish",
        gerund: "publishing",
        past: "published",
        noun: "publishable",
        listHeading: "Publishable entry IDs",
        noEntriesMessage: "No entries to publish."
    };
}
function toSuccessStatus(action) {
    return action === "publish" ? "published" : "unpublished";
}
function getErrorMessage(error) {
    if (error instanceof Error) {
        return error.message;
    }
    if (typeof error === "object" && error !== null && "message" in error) {
        return String(error.message);
    }
    return String(error);
}
function chunkEntries(items, chunkSize) {
    const chunks = [];
    for (let i = 0; i < items.length; i += chunkSize) {
        chunks.push(items.slice(i, i + chunkSize));
    }
    return chunks;
}
async function mapWithConcurrency(items, concurrency, fn) {
    if (items.length === 0) {
        return [];
    }
    const results = new Array(items.length);
    let nextIndex = 0;
    async function worker() {
        while (true) {
            const index2 = nextIndex;
            nextIndex += 1;
            if (index2 >= items.length) {
                return;
            }
            results[index2] = await fn(items[index2], index2);
        }
    }
    await Promise.all(Array.from({ length: Math.min(concurrency, items.length) }, () => worker()));
    return results;
}
async function collectTaggedEntries(client, tagId, action, maxPublishable, hooks) {
    const publishableEntries = [];
    const actionLabels = getActionLabels(action);
    let scannedCount = 0;
    let page = 0;
    let pageNext;
    emitLog(hooks, "error", `Fetching entries tagged ${tagId} to ${actionLabels.infinitive}...`);
    while (true) {
        page += 1;
        const query2 = pageNext ? { limit: CURSOR_PAGE_SIZE, pageNext } : { "metadata.tags.sys.id[in]": tagId, limit: CURSOR_PAGE_SIZE };
        const response = await client.entry.getManyWithCursor({
            query: query2
        });
        for (const entry of response.items) {
            scannedCount += 1;
            if (needsAction(entry, action)) {
                publishableEntries.push(entry);
                if (publishableEntries.length >= maxPublishable) {
                    emitLog(hooks, "error", `  page ${page}: fetched ${scannedCount} entries (stopped early after finding ${maxPublishable} ${actionLabels.noun})`);
                    emitProgress(hooks, {
                        stage: "collecting",
                        page,
                        scannedCount,
                        publishableCount: publishableEntries.length
                    });
                    return { publishableEntries, scannedCount, fullyScanned: false };
                }
            }
        }
        emitLog(hooks, "error", `  page ${page}: fetched ${scannedCount} entries`);
        emitProgress(hooks, {
            stage: "collecting",
            page,
            scannedCount,
            publishableCount: publishableEntries.length
        });
        if (publishableEntries.length >= maxPublishable) {
            return { publishableEntries, scannedCount, fullyScanned: false };
        }
        const hasMore = Boolean(response.pages?.next) && response.items.length > 0;
        if (!hasMore) {
            return { publishableEntries, scannedCount, fullyScanned: true };
        }
        pageNext = response.pages.next;
    }
}
function buildBulkPublishPayload(entries) {
    return {
        entities: {
            sys: { type: "Array" },
            items: entries.map(entry => ({
                sys: {
                    type: "Link",
                    linkType: "Entry",
                    id: entry.sys.id,
                    version: entry.sys.version
                }
            }))
        }
    };
}
async function waitForBulkAction(client, config, bulkActionId, pollSlot, hooks) {
    const sleepFn = hooks?.sleep ?? sleep2;
    if (pollSlot > 0) {
        await sleepFn(pollSlot * BULK_ACTION_POLL_STAGGER_MS);
    }
    for (let attempt = 0; attempt < BULK_ACTION_MAX_POLLS; attempt++) {
        const action = await client.bulkAction.get({
            spaceId: config.spaceId,
            environmentId: config.environmentId,
            bulkActionId
        });
        if (action.sys.status === "succeeded" || action.sys.status === "failed") {
            return action;
        }
        await sleepFn(BULK_ACTION_POLL_MS);
    }
    throw new Error(`BulkAction ${bulkActionId} did not finish within ${(BULK_ACTION_MAX_POLLS * BULK_ACTION_POLL_MS) / 1e3}s`);
}
function parseBulkActionResults(entries, bulkAction, action) {
    const successStatus = toSuccessStatus(action);
    if (bulkAction.sys.status === "succeeded") {
        return entries.map(entry => ({
            entryId: entry.sys.id,
            contentType: getContentTypeId(entry),
            status: successStatus
        }));
    }
    const failedById = /* @__PURE__ */ new Map();
    for (const item of bulkAction.error?.details?.errors ?? []) {
        const entryId = item.entity?.sys?.id;
        if (entryId) {
            failedById.set(entryId, getErrorMessage(item.error));
        }
    }
    return entries.map(entry => {
        const error = failedById.get(entry.sys.id);
        return {
            entryId: entry.sys.id,
            contentType: getContentTypeId(entry),
            status: error ? "failed" : successStatus,
            ...(error ? { error } : {})
        };
    });
}
async function publishBatch(client, config, entries, action, batchNumber, batchCount, concurrency, verbose, hooks) {
    const actionLabels = getActionLabels(action);
    emitProgress(hooks, { stage: "publishing", batchNumber, batchCount });
    if (batchCount > 1) {
        emitLog(hooks, "error", `Starting bulk ${actionLabels.infinitive} batch ${batchNumber}/${batchCount} (${entries.length} entries)...`);
    } else {
        emitLog(hooks, "error", `Starting bulk ${actionLabels.infinitive} for ${entries.length} entries...`);
    }
    const bulkActionInProgress =
        action === "publish"
            ? await client.bulkAction.publish(
                  {
                      spaceId: config.spaceId,
                      environmentId: config.environmentId
                  },
                  buildBulkPublishPayload(entries)
              )
            : await client.bulkAction.unpublish(
                  {
                      spaceId: config.spaceId,
                      environmentId: config.environmentId
                  },
                  buildBulkPublishPayload(entries)
              );
    if (verbose) {
        emitLog(hooks, "error", `  bulk action id: ${bulkActionInProgress.sys.id}`);
    }
    const bulkAction = await waitForBulkAction(client, config, bulkActionInProgress.sys.id, (batchNumber - 1) % concurrency, hooks);
    const results = parseBulkActionResults(entries, bulkAction, action);
    if (verbose) {
        for (const result of results) {
            if (result.status !== "failed") {
                emitLog(hooks, "info", `${actionLabels.past[0].toUpperCase()}${actionLabels.past.slice(1)} ${result.entryId} (${result.contentType})`);
            } else {
                emitLog(hooks, "error", `Failed ${result.entryId} (${result.contentType}): ${result.error}`);
            }
        }
    }
    return results;
}
async function publishEntries(client, config, entries, action, concurrency, verbose, hooks) {
    const actionLabels = getActionLabels(action);
    const batches = chunkEntries(entries, BULK_ACTION_MAX_ITEMS);
    if (batches.length > 1) {
        emitLog(hooks, "error", `Splitting ${entries.length} entries into ${batches.length} bulk ${actionLabels.infinitive} actions (max ${BULK_ACTION_MAX_ITEMS} per action)...`);
    }
    if (concurrency > 1 && batches.length > 1) {
        emitLog(hooks, "error", `Running up to ${concurrency} bulk actions in parallel (Contentful allows ${MAX_BULK_ACTION_CONCURRENCY} active per space)...`);
    }
    const batchResults = await mapWithConcurrency(batches, concurrency, (batch, index2) => publishBatch(client, config, batch, action, index2 + 1, batches.length, concurrency, verbose, hooks));
    return batchResults.flat();
}
function logEntry(entry, verbose, hooks) {
    if (!verbose) {
        return;
    }
    const { id, contentType, version, publishedVersion } = entry.sys;
    emitLog(hooks, "info", `  ${id} (${contentType.sys.id}) version=${version} publishedVersion=${publishedVersion ?? "none"}`);
}
function parseAction(value) {
    if (value === "publish" || value === "unpublish") {
        return value;
    }
    throw new Error("Unknown --action value. Valid actions: publish, unpublish");
}
function normalizePublishJobParams(params) {
    const token = params.token.trim();
    const tagId = params.tagId?.trim();
    const providedSpaceId = params.spaceId?.trim();
    const providedEnvironmentId = params.environmentId?.trim();
    if (!token) {
        throw new Error("token is required");
    }
    if (!tagId) {
        throw new Error("tagId is required");
    }
    if (!Number.isInteger(params.limit) || params.limit <= 0) {
        throw new Error("limit must be a positive integer");
    }
    const concurrency = params.concurrency ?? DEFAULT_BULK_ACTION_CONCURRENCY;
    if (!Number.isInteger(concurrency) || concurrency <= 0) {
        throw new Error("concurrency must be a positive integer");
    }
    if (concurrency > MAX_BULK_ACTION_CONCURRENCY) {
        throw new Error(`concurrency must be at most ${MAX_BULK_ACTION_CONCURRENCY} (Contentful max active bulk actions per space)`);
    }
    const action = parseAction(params.action ?? "publish");
    if ((providedSpaceId && !providedEnvironmentId) || (!providedSpaceId && providedEnvironmentId)) {
        throw new Error("spaceId and environmentId must be provided together");
    }
    const target = params.target?.trim() || void 0;
    if (!providedSpaceId || !providedEnvironmentId) {
        throw new Error("spaceId and environmentId are required");
    }
    const legacyTargetUsed = Boolean(target);
    return {
        token,
        target,
        legacyTargetUsed,
        spaceId: providedSpaceId,
        environmentId: providedEnvironmentId,
        tagId,
        action,
        limit: params.limit,
        dryRun: params.dryRun ?? false,
        verbose: params.verbose ?? false,
        concurrency
    };
}
function getConfig(params) {
    return {
        accessToken: params.token,
        target: params.target,
        legacyTargetUsed: params.legacyTargetUsed,
        action: params.action,
        spaceId: params.spaceId,
        environmentId: params.environmentId,
        tagId: params.tagId
    };
}
async function runPublishJob(params, hooks) {
    const normalizedParams = normalizePublishJobParams(params);
    const config = getConfig(normalizedParams);
    const actionLabels = getActionLabels(config.action);
    const clientFactory = hooks?.createClient ?? createDefaultPlainClient;
    const client = clientFactory(config);
    emitLog(hooks, "info", `Scope: space ${config.spaceId}, environment ${config.environmentId}`);
    emitLog(hooks, "info", `Action: ${config.action}`);
    const { publishableEntries, scannedCount, fullyScanned } = await collectTaggedEntries(client, config.tagId, config.action, normalizedParams.limit, hooks);
    const limitedEntries = publishableEntries.slice(0, normalizedParams.limit);
    if (fullyScanned) {
        emitLog(hooks, "info", `Found ${scannedCount} entries tagged ${config.tagId}`);
        emitLog(hooks, "info", `  ${publishableEntries.length} need ${actionLabels.gerund}`);
        emitLog(hooks, "info", `  ${scannedCount - publishableEntries.length} already up to date (skipped)`);
    } else {
        emitLog(hooks, "info", `Scanned ${scannedCount} entries tagged ${config.tagId}`);
        emitLog(hooks, "info", `  At least ${publishableEntries.length} need ${actionLabels.gerund} (scan stopped after reaching --limit)`);
    }
    if (publishableEntries.length > 0) {
        emitLog(hooks, "info", `  Limit: ${normalizedParams.limit} (${normalizedParams.dryRun ? "listing" : actionLabels.gerund} first ${limitedEntries.length} of ${publishableEntries.length})`);
    }
    if (normalizedParams.dryRun) {
        if (limitedEntries.length === 0) {
            emitLog(
                hooks,
                "info",
                `
${actionLabels.noEntriesMessage}`
            );
            return {
                success: true,
                summary: {
                    target: config.target,
                    action: config.action,
                    targetLabel: config.targetLabel,
                    spaceId: config.spaceId,
                    environmentId: config.environmentId,
                    tagId: config.tagId,
                    scannedCount,
                    fullyScanned,
                    publishableCount: publishableEntries.length,
                    selectedCount: 0,
                    publishedCount: 0,
                    failedCount: 0,
                    dryRun: true,
                    limit: normalizedParams.limit,
                    concurrency: normalizedParams.concurrency,
                    results: []
                }
            };
        }
        emitLog(
            hooks,
            "info",
            `
${actionLabels.listHeading}:`
        );
        for (const entry of limitedEntries) {
            logEntry(entry, normalizedParams.verbose, hooks);
            if (!normalizedParams.verbose) {
                emitLog(hooks, "info", `  ${entry.sys.id}`);
            }
        }
        return {
            success: true,
            summary: {
                target: config.target,
                action: config.action,
                targetLabel: config.targetLabel,
                spaceId: config.spaceId,
                environmentId: config.environmentId,
                tagId: config.tagId,
                scannedCount,
                fullyScanned,
                publishableCount: publishableEntries.length,
                selectedCount: limitedEntries.length,
                publishedCount: 0,
                failedCount: 0,
                dryRun: true,
                limit: normalizedParams.limit,
                concurrency: normalizedParams.concurrency,
                results: []
            }
        };
    }
    if (limitedEntries.length === 0) {
        emitLog(
            hooks,
            "info",
            `
${actionLabels.noEntriesMessage}`
        );
        return {
            success: true,
            summary: {
                target: config.target,
                action: config.action,
                targetLabel: config.targetLabel,
                spaceId: config.spaceId,
                environmentId: config.environmentId,
                tagId: config.tagId,
                scannedCount,
                fullyScanned,
                publishableCount: 0,
                selectedCount: 0,
                publishedCount: 0,
                failedCount: 0,
                dryRun: false,
                limit: normalizedParams.limit,
                concurrency: normalizedParams.concurrency,
                results: []
            }
        };
    }
    const results = await publishEntries(client, config, limitedEntries, config.action, normalizedParams.concurrency, normalizedParams.verbose, hooks);
    const publishedCount = results.filter(r => r.status !== "failed").length;
    const failedResults = results.filter(r => r.status === "failed");
    emitLog(
        hooks,
        "info",
        `
${actionLabels.past[0].toUpperCase()}${actionLabels.past.slice(1)}: ${publishedCount}`
    );
    emitLog(hooks, "info", `Failed: ${failedResults.length}`);
    for (const result of failedResults) {
        emitLog(hooks, "info", `  - ${result.entryId}: ${result.error}`);
    }
    return {
        success: failedResults.length === 0,
        summary: {
            target: config.target,
            action: config.action,
            targetLabel: config.targetLabel,
            spaceId: config.spaceId,
            environmentId: config.environmentId,
            tagId: config.tagId,
            scannedCount,
            fullyScanned,
            publishableCount: publishableEntries.length,
            selectedCount: limitedEntries.length,
            publishedCount,
            failedCount: failedResults.length,
            dryRun: false,
            limit: normalizedParams.limit,
            concurrency: normalizedParams.concurrency,
            results
        }
    };
}

// src/publish-worker.ts
function emit(event) {
    process.stdout.write(`${JSON.stringify(event)}
`);
}
async function readStdin() {
    return new Promise((resolveRead, rejectRead) => {
        let data = "";
        process.stdin.setEncoding("utf8");
        process.stdin.on("data", chunk => {
            data += chunk;
        });
        process.stdin.on("end", () => {
            resolveRead(data);
        });
        process.stdin.on("error", error => {
            rejectRead(error);
        });
    });
}
function parseInteger(value, field, { min, max }) {
    if (typeof value !== "number" || !Number.isInteger(value)) {
        throw new Error(`${field} must be an integer`);
    }
    if (value < min) {
        throw new Error(`${field} must be at least ${min}`);
    }
    if (max !== void 0 && value > max) {
        throw new Error(`${field} must be at most ${max}`);
    }
    return value;
}
function parseBoolean(value, field) {
    if (typeof value !== "boolean") {
        throw new Error(`${field} must be a boolean`);
    }
    return value;
}
function parseOptionalString(value, field) {
    if (value === void 0) {
        return void 0;
    }
    if (typeof value !== "string") {
        throw new Error(`${field} must be a string`);
    }
    const trimmed = value.trim();
    return trimmed === "" ? void 0 : trimmed;
}
function parseRequiredString(value, field) {
    const parsed = parseOptionalString(value, field);
    if (!parsed) {
        throw new Error(`${field} is required`);
    }
    return parsed;
}
function parsePayload(raw) {
    if (typeof raw !== "object" || raw === null || Array.isArray(raw)) {
        throw new Error("Payload must be a JSON object");
    }
    const payload = raw;
    if (typeof payload.token !== "string" || payload.token.trim() === "") {
        throw new Error("token is required");
    }
    const command =
        payload.command === void 0
            ? "run"
            : typeof payload.command === "string"
              ? payload.command
              : (() => {
                    throw new Error("command must be a string");
                })();
    if (command === "load-spaces") {
        return {
            kind: "load-spaces",
            token: payload.token
        };
    }
    if (command === "load-environments") {
        return {
            kind: "load-environments",
            token: payload.token,
            spaceId: parseRequiredString(payload.spaceId, "spaceId")
        };
    }
    if (command === "load-tags") {
        return {
            kind: "load-tags",
            token: payload.token,
            spaceId: parseRequiredString(payload.spaceId, "spaceId"),
            environmentId: parseRequiredString(payload.environmentId, "environmentId")
        };
    }
    if (command !== "run") {
        throw new Error("command must be one of: run, load-spaces, load-environments, load-tags");
    }
    const spaceId = parseRequiredString(payload.spaceId, "spaceId");
    const environmentId = parseRequiredString(payload.environmentId, "environmentId");
    if (typeof payload.tagId !== "string" || payload.tagId.trim() === "") {
        throw new Error("tagId is required");
    }
    const action =
        payload.action === void 0
            ? "publish"
            : typeof payload.action === "string"
              ? parseAction(payload.action)
              : (() => {
                    throw new Error("action must be a string");
                })();
    const limit = parseInteger(payload.limit, "limit", { min: 1 });
    const concurrencyValue = payload.concurrency === void 0 ? DEFAULT_BULK_ACTION_CONCURRENCY : payload.concurrency;
    const concurrency = parseInteger(concurrencyValue, "concurrency", {
        min: 1,
        max: MAX_BULK_ACTION_CONCURRENCY
    });
    const dryRun = payload.dryRun === void 0 ? false : parseBoolean(payload.dryRun, "dryRun");
    const verbose = payload.verbose === void 0 ? false : parseBoolean(payload.verbose, "verbose");
    return {
        kind: "run",
        params: {
            token: payload.token,
            spaceId,
            environmentId,
            tagId: payload.tagId,
            action,
            limit,
            concurrency,
            dryRun,
            verbose
        }
    };
}
async function readPayloadFromInput() {
    const payloadText = process.argv[2] ?? (await readStdin());
    if (!payloadText || payloadText.trim() === "") {
        throw new Error("Missing payload JSON");
    }
    let rawPayload;
    try {
        rawPayload = JSON.parse(payloadText);
    } catch {
        throw new Error("Payload must be valid JSON");
    }
    return parsePayload(rawPayload);
}
async function main() {
    try {
        const command = await readPayloadFromInput();
        if (command.kind === "load-spaces") {
            const spaces = await loadAccessibleSpaces({
                token: command.token
            });
            emit({
                type: "done",
                success: true,
                summary: {
                    spaces
                },
                timestamp: /* @__PURE__ */ new Date().toISOString()
            });
            process.exitCode = 0;
            return;
        }
        if (command.kind === "load-environments") {
            const environments = await loadSpaceEnvironments({
                token: command.token,
                spaceId: command.spaceId
            });
            emit({
                type: "done",
                success: true,
                summary: {
                    environments
                },
                timestamp: /* @__PURE__ */ new Date().toISOString()
            });
            process.exitCode = 0;
            return;
        }
        if (command.kind === "load-tags") {
            const tags = await loadTagsForScope({
                token: command.token,
                spaceId: command.spaceId,
                environmentId: command.environmentId
            });
            emit({
                type: "done",
                success: true,
                summary: {
                    tags
                },
                timestamp: /* @__PURE__ */ new Date().toISOString()
            });
            process.exitCode = 0;
            return;
        }
        const result = await runPublishJob(command.params, {
            onLog: log => {
                emit({
                    type: "log",
                    level: log.level,
                    message: log.message,
                    ...(log.meta ? { meta: log.meta } : {}),
                    timestamp: /* @__PURE__ */ new Date().toISOString()
                });
            }
        });
        emit({
            type: "done",
            success: result.success,
            summary: result.summary,
            timestamp: /* @__PURE__ */ new Date().toISOString()
        });
        process.exitCode = result.success ? 0 : 1;
    } catch (error) {
        const message = getErrorMessage(error);
        emit({
            type: "error",
            message,
            timestamp: /* @__PURE__ */ new Date().toISOString()
        });
        process.exitCode = 1;
    }
}
if (require.main === module) {
    main().catch(error => {
        emit({
            type: "error",
            message: getErrorMessage(error),
            timestamp: /* @__PURE__ */ new Date().toISOString()
        });
        process.exitCode = 1;
    });
}
// Annotate the CommonJS export names for ESM import in node:
0 &&
    (module.exports = {
        parsePayload
    });
